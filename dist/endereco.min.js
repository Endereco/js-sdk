/******/ (function() { // webpackBootstrap
/******/ 	var __webpack_modules__ = ({

/***/ 128:
/***/ (function(module) {

module.exports = function (exec) {
  try {
    return { e: false, v: exec() };
  } catch (e) {
    return { e: true, v: e };
  }
};


/***/ }),

/***/ 182:
/***/ (function(module, exports, __webpack_require__) {

/* module decorator */ module = __webpack_require__.nmd(module);
/**
 * Lodash (Custom Build) <https://lodash.com/>
 * Build: `lodash modularize exports="npm" -o ./`
 * Copyright OpenJS Foundation and other contributors <https://openjsf.org/>
 * Released under MIT license <https://lodash.com/license>
 * Based on Underscore.js 1.8.3 <http://underscorejs.org/LICENSE>
 * Copyright Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
 */

/** Used as the size to enable large array optimizations. */
var LARGE_ARRAY_SIZE = 200;

/** Used to stand-in for `undefined` hash values. */
var HASH_UNDEFINED = '__lodash_hash_undefined__';

/** Used to detect hot functions by number of calls within a span of milliseconds. */
var HOT_COUNT = 800,
    HOT_SPAN = 16;

/** Used as references for various `Number` constants. */
var MAX_SAFE_INTEGER = 9007199254740991;

/** `Object#toString` result references. */
var argsTag = '[object Arguments]',
    arrayTag = '[object Array]',
    asyncTag = '[object AsyncFunction]',
    boolTag = '[object Boolean]',
    dateTag = '[object Date]',
    errorTag = '[object Error]',
    funcTag = '[object Function]',
    genTag = '[object GeneratorFunction]',
    mapTag = '[object Map]',
    numberTag = '[object Number]',
    nullTag = '[object Null]',
    objectTag = '[object Object]',
    proxyTag = '[object Proxy]',
    regexpTag = '[object RegExp]',
    setTag = '[object Set]',
    stringTag = '[object String]',
    undefinedTag = '[object Undefined]',
    weakMapTag = '[object WeakMap]';

var arrayBufferTag = '[object ArrayBuffer]',
    dataViewTag = '[object DataView]',
    float32Tag = '[object Float32Array]',
    float64Tag = '[object Float64Array]',
    int8Tag = '[object Int8Array]',
    int16Tag = '[object Int16Array]',
    int32Tag = '[object Int32Array]',
    uint8Tag = '[object Uint8Array]',
    uint8ClampedTag = '[object Uint8ClampedArray]',
    uint16Tag = '[object Uint16Array]',
    uint32Tag = '[object Uint32Array]';

/**
 * Used to match `RegExp`
 * [syntax characters](http://ecma-international.org/ecma-262/7.0/#sec-patterns).
 */
var reRegExpChar = /[\\^$.*+?()[\]{}|]/g;

/** Used to detect host constructors (Safari). */
var reIsHostCtor = /^\[object .+?Constructor\]$/;

/** Used to detect unsigned integer values. */
var reIsUint = /^(?:0|[1-9]\d*)$/;

/** Used to identify `toStringTag` values of typed arrays. */
var typedArrayTags = {};
typedArrayTags[float32Tag] = typedArrayTags[float64Tag] =
typedArrayTags[int8Tag] = typedArrayTags[int16Tag] =
typedArrayTags[int32Tag] = typedArrayTags[uint8Tag] =
typedArrayTags[uint8ClampedTag] = typedArrayTags[uint16Tag] =
typedArrayTags[uint32Tag] = true;
typedArrayTags[argsTag] = typedArrayTags[arrayTag] =
typedArrayTags[arrayBufferTag] = typedArrayTags[boolTag] =
typedArrayTags[dataViewTag] = typedArrayTags[dateTag] =
typedArrayTags[errorTag] = typedArrayTags[funcTag] =
typedArrayTags[mapTag] = typedArrayTags[numberTag] =
typedArrayTags[objectTag] = typedArrayTags[regexpTag] =
typedArrayTags[setTag] = typedArrayTags[stringTag] =
typedArrayTags[weakMapTag] = false;

/** Detect free variable `global` from Node.js. */
var freeGlobal = typeof __webpack_require__.g == 'object' && __webpack_require__.g && __webpack_require__.g.Object === Object && __webpack_require__.g;

/** Detect free variable `self`. */
var freeSelf = typeof self == 'object' && self && self.Object === Object && self;

/** Used as a reference to the global object. */
var root = freeGlobal || freeSelf || Function('return this')();

/** Detect free variable `exports`. */
var freeExports =  true && exports && !exports.nodeType && exports;

/** Detect free variable `module`. */
var freeModule = freeExports && "object" == 'object' && module && !module.nodeType && module;

/** Detect the popular CommonJS extension `module.exports`. */
var moduleExports = freeModule && freeModule.exports === freeExports;

/** Detect free variable `process` from Node.js. */
var freeProcess = moduleExports && freeGlobal.process;

/** Used to access faster Node.js helpers. */
var nodeUtil = (function() {
  try {
    // Use `util.types` for Node.js 10+.
    var types = freeModule && freeModule.require && freeModule.require('util').types;

    if (types) {
      return types;
    }

    // Legacy `process.binding('util')` for Node.js < 10.
    return freeProcess && freeProcess.binding && freeProcess.binding('util');
  } catch (e) {}
}());

/* Node.js helper references. */
var nodeIsTypedArray = nodeUtil && nodeUtil.isTypedArray;

/**
 * A faster alternative to `Function#apply`, this function invokes `func`
 * with the `this` binding of `thisArg` and the arguments of `args`.
 *
 * @private
 * @param {Function} func The function to invoke.
 * @param {*} thisArg The `this` binding of `func`.
 * @param {Array} args The arguments to invoke `func` with.
 * @returns {*} Returns the result of `func`.
 */
function apply(func, thisArg, args) {
  switch (args.length) {
    case 0: return func.call(thisArg);
    case 1: return func.call(thisArg, args[0]);
    case 2: return func.call(thisArg, args[0], args[1]);
    case 3: return func.call(thisArg, args[0], args[1], args[2]);
  }
  return func.apply(thisArg, args);
}

/**
 * The base implementation of `_.times` without support for iteratee shorthands
 * or max array length checks.
 *
 * @private
 * @param {number} n The number of times to invoke `iteratee`.
 * @param {Function} iteratee The function invoked per iteration.
 * @returns {Array} Returns the array of results.
 */
function baseTimes(n, iteratee) {
  var index = -1,
      result = Array(n);

  while (++index < n) {
    result[index] = iteratee(index);
  }
  return result;
}

/**
 * The base implementation of `_.unary` without support for storing metadata.
 *
 * @private
 * @param {Function} func The function to cap arguments for.
 * @returns {Function} Returns the new capped function.
 */
function baseUnary(func) {
  return function(value) {
    return func(value);
  };
}

/**
 * Gets the value at `key` of `object`.
 *
 * @private
 * @param {Object} [object] The object to query.
 * @param {string} key The key of the property to get.
 * @returns {*} Returns the property value.
 */
function getValue(object, key) {
  return object == null ? undefined : object[key];
}

/**
 * Creates a unary function that invokes `func` with its argument transformed.
 *
 * @private
 * @param {Function} func The function to wrap.
 * @param {Function} transform The argument transform.
 * @returns {Function} Returns the new function.
 */
function overArg(func, transform) {
  return function(arg) {
    return func(transform(arg));
  };
}

/** Used for built-in method references. */
var arrayProto = Array.prototype,
    funcProto = Function.prototype,
    objectProto = Object.prototype;

/** Used to detect overreaching core-js shims. */
var coreJsData = root['__core-js_shared__'];

/** Used to resolve the decompiled source of functions. */
var funcToString = funcProto.toString;

/** Used to check objects for own properties. */
var hasOwnProperty = objectProto.hasOwnProperty;

/** Used to detect methods masquerading as native. */
var maskSrcKey = (function() {
  var uid = /[^.]+$/.exec(coreJsData && coreJsData.keys && coreJsData.keys.IE_PROTO || '');
  return uid ? ('Symbol(src)_1.' + uid) : '';
}());

/**
 * Used to resolve the
 * [`toStringTag`](http://ecma-international.org/ecma-262/7.0/#sec-object.prototype.tostring)
 * of values.
 */
var nativeObjectToString = objectProto.toString;

/** Used to infer the `Object` constructor. */
var objectCtorString = funcToString.call(Object);

/** Used to detect if a method is native. */
var reIsNative = RegExp('^' +
  funcToString.call(hasOwnProperty).replace(reRegExpChar, '\\$&')
  .replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, '$1.*?') + '$'
);

/** Built-in value references. */
var Buffer = moduleExports ? root.Buffer : undefined,
    Symbol = root.Symbol,
    Uint8Array = root.Uint8Array,
    allocUnsafe = Buffer ? Buffer.allocUnsafe : undefined,
    getPrototype = overArg(Object.getPrototypeOf, Object),
    objectCreate = Object.create,
    propertyIsEnumerable = objectProto.propertyIsEnumerable,
    splice = arrayProto.splice,
    symToStringTag = Symbol ? Symbol.toStringTag : undefined;

var defineProperty = (function() {
  try {
    var func = getNative(Object, 'defineProperty');
    func({}, '', {});
    return func;
  } catch (e) {}
}());

/* Built-in method references for those with the same name as other `lodash` methods. */
var nativeIsBuffer = Buffer ? Buffer.isBuffer : undefined,
    nativeMax = Math.max,
    nativeNow = Date.now;

/* Built-in method references that are verified to be native. */
var Map = getNative(root, 'Map'),
    nativeCreate = getNative(Object, 'create');

/**
 * The base implementation of `_.create` without support for assigning
 * properties to the created object.
 *
 * @private
 * @param {Object} proto The object to inherit from.
 * @returns {Object} Returns the new object.
 */
var baseCreate = (function() {
  function object() {}
  return function(proto) {
    if (!isObject(proto)) {
      return {};
    }
    if (objectCreate) {
      return objectCreate(proto);
    }
    object.prototype = proto;
    var result = new object;
    object.prototype = undefined;
    return result;
  };
}());

/**
 * Creates a hash object.
 *
 * @private
 * @constructor
 * @param {Array} [entries] The key-value pairs to cache.
 */
function Hash(entries) {
  var index = -1,
      length = entries == null ? 0 : entries.length;

  this.clear();
  while (++index < length) {
    var entry = entries[index];
    this.set(entry[0], entry[1]);
  }
}

/**
 * Removes all key-value entries from the hash.
 *
 * @private
 * @name clear
 * @memberOf Hash
 */
function hashClear() {
  this.__data__ = nativeCreate ? nativeCreate(null) : {};
  this.size = 0;
}

/**
 * Removes `key` and its value from the hash.
 *
 * @private
 * @name delete
 * @memberOf Hash
 * @param {Object} hash The hash to modify.
 * @param {string} key The key of the value to remove.
 * @returns {boolean} Returns `true` if the entry was removed, else `false`.
 */
function hashDelete(key) {
  var result = this.has(key) && delete this.__data__[key];
  this.size -= result ? 1 : 0;
  return result;
}

/**
 * Gets the hash value for `key`.
 *
 * @private
 * @name get
 * @memberOf Hash
 * @param {string} key The key of the value to get.
 * @returns {*} Returns the entry value.
 */
function hashGet(key) {
  var data = this.__data__;
  if (nativeCreate) {
    var result = data[key];
    return result === HASH_UNDEFINED ? undefined : result;
  }
  return hasOwnProperty.call(data, key) ? data[key] : undefined;
}

/**
 * Checks if a hash value for `key` exists.
 *
 * @private
 * @name has
 * @memberOf Hash
 * @param {string} key The key of the entry to check.
 * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
 */
function hashHas(key) {
  var data = this.__data__;
  return nativeCreate ? (data[key] !== undefined) : hasOwnProperty.call(data, key);
}

/**
 * Sets the hash `key` to `value`.
 *
 * @private
 * @name set
 * @memberOf Hash
 * @param {string} key The key of the value to set.
 * @param {*} value The value to set.
 * @returns {Object} Returns the hash instance.
 */
function hashSet(key, value) {
  var data = this.__data__;
  this.size += this.has(key) ? 0 : 1;
  data[key] = (nativeCreate && value === undefined) ? HASH_UNDEFINED : value;
  return this;
}

// Add methods to `Hash`.
Hash.prototype.clear = hashClear;
Hash.prototype['delete'] = hashDelete;
Hash.prototype.get = hashGet;
Hash.prototype.has = hashHas;
Hash.prototype.set = hashSet;

/**
 * Creates an list cache object.
 *
 * @private
 * @constructor
 * @param {Array} [entries] The key-value pairs to cache.
 */
function ListCache(entries) {
  var index = -1,
      length = entries == null ? 0 : entries.length;

  this.clear();
  while (++index < length) {
    var entry = entries[index];
    this.set(entry[0], entry[1]);
  }
}

/**
 * Removes all key-value entries from the list cache.
 *
 * @private
 * @name clear
 * @memberOf ListCache
 */
function listCacheClear() {
  this.__data__ = [];
  this.size = 0;
}

/**
 * Removes `key` and its value from the list cache.
 *
 * @private
 * @name delete
 * @memberOf ListCache
 * @param {string} key The key of the value to remove.
 * @returns {boolean} Returns `true` if the entry was removed, else `false`.
 */
function listCacheDelete(key) {
  var data = this.__data__,
      index = assocIndexOf(data, key);

  if (index < 0) {
    return false;
  }
  var lastIndex = data.length - 1;
  if (index == lastIndex) {
    data.pop();
  } else {
    splice.call(data, index, 1);
  }
  --this.size;
  return true;
}

/**
 * Gets the list cache value for `key`.
 *
 * @private
 * @name get
 * @memberOf ListCache
 * @param {string} key The key of the value to get.
 * @returns {*} Returns the entry value.
 */
function listCacheGet(key) {
  var data = this.__data__,
      index = assocIndexOf(data, key);

  return index < 0 ? undefined : data[index][1];
}

/**
 * Checks if a list cache value for `key` exists.
 *
 * @private
 * @name has
 * @memberOf ListCache
 * @param {string} key The key of the entry to check.
 * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
 */
function listCacheHas(key) {
  return assocIndexOf(this.__data__, key) > -1;
}

/**
 * Sets the list cache `key` to `value`.
 *
 * @private
 * @name set
 * @memberOf ListCache
 * @param {string} key The key of the value to set.
 * @param {*} value The value to set.
 * @returns {Object} Returns the list cache instance.
 */
function listCacheSet(key, value) {
  var data = this.__data__,
      index = assocIndexOf(data, key);

  if (index < 0) {
    ++this.size;
    data.push([key, value]);
  } else {
    data[index][1] = value;
  }
  return this;
}

// Add methods to `ListCache`.
ListCache.prototype.clear = listCacheClear;
ListCache.prototype['delete'] = listCacheDelete;
ListCache.prototype.get = listCacheGet;
ListCache.prototype.has = listCacheHas;
ListCache.prototype.set = listCacheSet;

/**
 * Creates a map cache object to store key-value pairs.
 *
 * @private
 * @constructor
 * @param {Array} [entries] The key-value pairs to cache.
 */
function MapCache(entries) {
  var index = -1,
      length = entries == null ? 0 : entries.length;

  this.clear();
  while (++index < length) {
    var entry = entries[index];
    this.set(entry[0], entry[1]);
  }
}

/**
 * Removes all key-value entries from the map.
 *
 * @private
 * @name clear
 * @memberOf MapCache
 */
function mapCacheClear() {
  this.size = 0;
  this.__data__ = {
    'hash': new Hash,
    'map': new (Map || ListCache),
    'string': new Hash
  };
}

/**
 * Removes `key` and its value from the map.
 *
 * @private
 * @name delete
 * @memberOf MapCache
 * @param {string} key The key of the value to remove.
 * @returns {boolean} Returns `true` if the entry was removed, else `false`.
 */
function mapCacheDelete(key) {
  var result = getMapData(this, key)['delete'](key);
  this.size -= result ? 1 : 0;
  return result;
}

/**
 * Gets the map value for `key`.
 *
 * @private
 * @name get
 * @memberOf MapCache
 * @param {string} key The key of the value to get.
 * @returns {*} Returns the entry value.
 */
function mapCacheGet(key) {
  return getMapData(this, key).get(key);
}

/**
 * Checks if a map value for `key` exists.
 *
 * @private
 * @name has
 * @memberOf MapCache
 * @param {string} key The key of the entry to check.
 * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
 */
function mapCacheHas(key) {
  return getMapData(this, key).has(key);
}

/**
 * Sets the map `key` to `value`.
 *
 * @private
 * @name set
 * @memberOf MapCache
 * @param {string} key The key of the value to set.
 * @param {*} value The value to set.
 * @returns {Object} Returns the map cache instance.
 */
function mapCacheSet(key, value) {
  var data = getMapData(this, key),
      size = data.size;

  data.set(key, value);
  this.size += data.size == size ? 0 : 1;
  return this;
}

// Add methods to `MapCache`.
MapCache.prototype.clear = mapCacheClear;
MapCache.prototype['delete'] = mapCacheDelete;
MapCache.prototype.get = mapCacheGet;
MapCache.prototype.has = mapCacheHas;
MapCache.prototype.set = mapCacheSet;

/**
 * Creates a stack cache object to store key-value pairs.
 *
 * @private
 * @constructor
 * @param {Array} [entries] The key-value pairs to cache.
 */
function Stack(entries) {
  var data = this.__data__ = new ListCache(entries);
  this.size = data.size;
}

/**
 * Removes all key-value entries from the stack.
 *
 * @private
 * @name clear
 * @memberOf Stack
 */
function stackClear() {
  this.__data__ = new ListCache;
  this.size = 0;
}

/**
 * Removes `key` and its value from the stack.
 *
 * @private
 * @name delete
 * @memberOf Stack
 * @param {string} key The key of the value to remove.
 * @returns {boolean} Returns `true` if the entry was removed, else `false`.
 */
function stackDelete(key) {
  var data = this.__data__,
      result = data['delete'](key);

  this.size = data.size;
  return result;
}

/**
 * Gets the stack value for `key`.
 *
 * @private
 * @name get
 * @memberOf Stack
 * @param {string} key The key of the value to get.
 * @returns {*} Returns the entry value.
 */
function stackGet(key) {
  return this.__data__.get(key);
}

/**
 * Checks if a stack value for `key` exists.
 *
 * @private
 * @name has
 * @memberOf Stack
 * @param {string} key The key of the entry to check.
 * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
 */
function stackHas(key) {
  return this.__data__.has(key);
}

/**
 * Sets the stack `key` to `value`.
 *
 * @private
 * @name set
 * @memberOf Stack
 * @param {string} key The key of the value to set.
 * @param {*} value The value to set.
 * @returns {Object} Returns the stack cache instance.
 */
function stackSet(key, value) {
  var data = this.__data__;
  if (data instanceof ListCache) {
    var pairs = data.__data__;
    if (!Map || (pairs.length < LARGE_ARRAY_SIZE - 1)) {
      pairs.push([key, value]);
      this.size = ++data.size;
      return this;
    }
    data = this.__data__ = new MapCache(pairs);
  }
  data.set(key, value);
  this.size = data.size;
  return this;
}

// Add methods to `Stack`.
Stack.prototype.clear = stackClear;
Stack.prototype['delete'] = stackDelete;
Stack.prototype.get = stackGet;
Stack.prototype.has = stackHas;
Stack.prototype.set = stackSet;

/**
 * Creates an array of the enumerable property names of the array-like `value`.
 *
 * @private
 * @param {*} value The value to query.
 * @param {boolean} inherited Specify returning inherited property names.
 * @returns {Array} Returns the array of property names.
 */
function arrayLikeKeys(value, inherited) {
  var isArr = isArray(value),
      isArg = !isArr && isArguments(value),
      isBuff = !isArr && !isArg && isBuffer(value),
      isType = !isArr && !isArg && !isBuff && isTypedArray(value),
      skipIndexes = isArr || isArg || isBuff || isType,
      result = skipIndexes ? baseTimes(value.length, String) : [],
      length = result.length;

  for (var key in value) {
    if ((inherited || hasOwnProperty.call(value, key)) &&
        !(skipIndexes && (
           // Safari 9 has enumerable `arguments.length` in strict mode.
           key == 'length' ||
           // Node.js 0.10 has enumerable non-index properties on buffers.
           (isBuff && (key == 'offset' || key == 'parent')) ||
           // PhantomJS 2 has enumerable non-index properties on typed arrays.
           (isType && (key == 'buffer' || key == 'byteLength' || key == 'byteOffset')) ||
           // Skip index properties.
           isIndex(key, length)
        ))) {
      result.push(key);
    }
  }
  return result;
}

/**
 * This function is like `assignValue` except that it doesn't assign
 * `undefined` values.
 *
 * @private
 * @param {Object} object The object to modify.
 * @param {string} key The key of the property to assign.
 * @param {*} value The value to assign.
 */
function assignMergeValue(object, key, value) {
  if ((value !== undefined && !eq(object[key], value)) ||
      (value === undefined && !(key in object))) {
    baseAssignValue(object, key, value);
  }
}

/**
 * Assigns `value` to `key` of `object` if the existing value is not equivalent
 * using [`SameValueZero`](http://ecma-international.org/ecma-262/7.0/#sec-samevaluezero)
 * for equality comparisons.
 *
 * @private
 * @param {Object} object The object to modify.
 * @param {string} key The key of the property to assign.
 * @param {*} value The value to assign.
 */
function assignValue(object, key, value) {
  var objValue = object[key];
  if (!(hasOwnProperty.call(object, key) && eq(objValue, value)) ||
      (value === undefined && !(key in object))) {
    baseAssignValue(object, key, value);
  }
}

/**
 * Gets the index at which the `key` is found in `array` of key-value pairs.
 *
 * @private
 * @param {Array} array The array to inspect.
 * @param {*} key The key to search for.
 * @returns {number} Returns the index of the matched value, else `-1`.
 */
function assocIndexOf(array, key) {
  var length = array.length;
  while (length--) {
    if (eq(array[length][0], key)) {
      return length;
    }
  }
  return -1;
}

/**
 * The base implementation of `assignValue` and `assignMergeValue` without
 * value checks.
 *
 * @private
 * @param {Object} object The object to modify.
 * @param {string} key The key of the property to assign.
 * @param {*} value The value to assign.
 */
function baseAssignValue(object, key, value) {
  if (key == '__proto__' && defineProperty) {
    defineProperty(object, key, {
      'configurable': true,
      'enumerable': true,
      'value': value,
      'writable': true
    });
  } else {
    object[key] = value;
  }
}

/**
 * The base implementation of `baseForOwn` which iterates over `object`
 * properties returned by `keysFunc` and invokes `iteratee` for each property.
 * Iteratee functions may exit iteration early by explicitly returning `false`.
 *
 * @private
 * @param {Object} object The object to iterate over.
 * @param {Function} iteratee The function invoked per iteration.
 * @param {Function} keysFunc The function to get the keys of `object`.
 * @returns {Object} Returns `object`.
 */
var baseFor = createBaseFor();

/**
 * The base implementation of `getTag` without fallbacks for buggy environments.
 *
 * @private
 * @param {*} value The value to query.
 * @returns {string} Returns the `toStringTag`.
 */
function baseGetTag(value) {
  if (value == null) {
    return value === undefined ? undefinedTag : nullTag;
  }
  return (symToStringTag && symToStringTag in Object(value))
    ? getRawTag(value)
    : objectToString(value);
}

/**
 * The base implementation of `_.isArguments`.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an `arguments` object,
 */
function baseIsArguments(value) {
  return isObjectLike(value) && baseGetTag(value) == argsTag;
}

/**
 * The base implementation of `_.isNative` without bad shim checks.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a native function,
 *  else `false`.
 */
function baseIsNative(value) {
  if (!isObject(value) || isMasked(value)) {
    return false;
  }
  var pattern = isFunction(value) ? reIsNative : reIsHostCtor;
  return pattern.test(toSource(value));
}

/**
 * The base implementation of `_.isTypedArray` without Node.js optimizations.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a typed array, else `false`.
 */
function baseIsTypedArray(value) {
  return isObjectLike(value) &&
    isLength(value.length) && !!typedArrayTags[baseGetTag(value)];
}

/**
 * The base implementation of `_.keysIn` which doesn't treat sparse arrays as dense.
 *
 * @private
 * @param {Object} object The object to query.
 * @returns {Array} Returns the array of property names.
 */
function baseKeysIn(object) {
  if (!isObject(object)) {
    return nativeKeysIn(object);
  }
  var isProto = isPrototype(object),
      result = [];

  for (var key in object) {
    if (!(key == 'constructor' && (isProto || !hasOwnProperty.call(object, key)))) {
      result.push(key);
    }
  }
  return result;
}

/**
 * The base implementation of `_.merge` without support for multiple sources.
 *
 * @private
 * @param {Object} object The destination object.
 * @param {Object} source The source object.
 * @param {number} srcIndex The index of `source`.
 * @param {Function} [customizer] The function to customize merged values.
 * @param {Object} [stack] Tracks traversed source values and their merged
 *  counterparts.
 */
function baseMerge(object, source, srcIndex, customizer, stack) {
  if (object === source) {
    return;
  }
  baseFor(source, function(srcValue, key) {
    stack || (stack = new Stack);
    if (isObject(srcValue)) {
      baseMergeDeep(object, source, key, srcIndex, baseMerge, customizer, stack);
    }
    else {
      var newValue = customizer
        ? customizer(safeGet(object, key), srcValue, (key + ''), object, source, stack)
        : undefined;

      if (newValue === undefined) {
        newValue = srcValue;
      }
      assignMergeValue(object, key, newValue);
    }
  }, keysIn);
}

/**
 * A specialized version of `baseMerge` for arrays and objects which performs
 * deep merges and tracks traversed objects enabling objects with circular
 * references to be merged.
 *
 * @private
 * @param {Object} object The destination object.
 * @param {Object} source The source object.
 * @param {string} key The key of the value to merge.
 * @param {number} srcIndex The index of `source`.
 * @param {Function} mergeFunc The function to merge values.
 * @param {Function} [customizer] The function to customize assigned values.
 * @param {Object} [stack] Tracks traversed source values and their merged
 *  counterparts.
 */
function baseMergeDeep(object, source, key, srcIndex, mergeFunc, customizer, stack) {
  var objValue = safeGet(object, key),
      srcValue = safeGet(source, key),
      stacked = stack.get(srcValue);

  if (stacked) {
    assignMergeValue(object, key, stacked);
    return;
  }
  var newValue = customizer
    ? customizer(objValue, srcValue, (key + ''), object, source, stack)
    : undefined;

  var isCommon = newValue === undefined;

  if (isCommon) {
    var isArr = isArray(srcValue),
        isBuff = !isArr && isBuffer(srcValue),
        isTyped = !isArr && !isBuff && isTypedArray(srcValue);

    newValue = srcValue;
    if (isArr || isBuff || isTyped) {
      if (isArray(objValue)) {
        newValue = objValue;
      }
      else if (isArrayLikeObject(objValue)) {
        newValue = copyArray(objValue);
      }
      else if (isBuff) {
        isCommon = false;
        newValue = cloneBuffer(srcValue, true);
      }
      else if (isTyped) {
        isCommon = false;
        newValue = cloneTypedArray(srcValue, true);
      }
      else {
        newValue = [];
      }
    }
    else if (isPlainObject(srcValue) || isArguments(srcValue)) {
      newValue = objValue;
      if (isArguments(objValue)) {
        newValue = toPlainObject(objValue);
      }
      else if (!isObject(objValue) || isFunction(objValue)) {
        newValue = initCloneObject(srcValue);
      }
    }
    else {
      isCommon = false;
    }
  }
  if (isCommon) {
    // Recursively merge objects and arrays (susceptible to call stack limits).
    stack.set(srcValue, newValue);
    mergeFunc(newValue, srcValue, srcIndex, customizer, stack);
    stack['delete'](srcValue);
  }
  assignMergeValue(object, key, newValue);
}

/**
 * The base implementation of `_.rest` which doesn't validate or coerce arguments.
 *
 * @private
 * @param {Function} func The function to apply a rest parameter to.
 * @param {number} [start=func.length-1] The start position of the rest parameter.
 * @returns {Function} Returns the new function.
 */
function baseRest(func, start) {
  return setToString(overRest(func, start, identity), func + '');
}

/**
 * The base implementation of `setToString` without support for hot loop shorting.
 *
 * @private
 * @param {Function} func The function to modify.
 * @param {Function} string The `toString` result.
 * @returns {Function} Returns `func`.
 */
var baseSetToString = !defineProperty ? identity : function(func, string) {
  return defineProperty(func, 'toString', {
    'configurable': true,
    'enumerable': false,
    'value': constant(string),
    'writable': true
  });
};

/**
 * Creates a clone of  `buffer`.
 *
 * @private
 * @param {Buffer} buffer The buffer to clone.
 * @param {boolean} [isDeep] Specify a deep clone.
 * @returns {Buffer} Returns the cloned buffer.
 */
function cloneBuffer(buffer, isDeep) {
  if (isDeep) {
    return buffer.slice();
  }
  var length = buffer.length,
      result = allocUnsafe ? allocUnsafe(length) : new buffer.constructor(length);

  buffer.copy(result);
  return result;
}

/**
 * Creates a clone of `arrayBuffer`.
 *
 * @private
 * @param {ArrayBuffer} arrayBuffer The array buffer to clone.
 * @returns {ArrayBuffer} Returns the cloned array buffer.
 */
function cloneArrayBuffer(arrayBuffer) {
  var result = new arrayBuffer.constructor(arrayBuffer.byteLength);
  new Uint8Array(result).set(new Uint8Array(arrayBuffer));
  return result;
}

/**
 * Creates a clone of `typedArray`.
 *
 * @private
 * @param {Object} typedArray The typed array to clone.
 * @param {boolean} [isDeep] Specify a deep clone.
 * @returns {Object} Returns the cloned typed array.
 */
function cloneTypedArray(typedArray, isDeep) {
  var buffer = isDeep ? cloneArrayBuffer(typedArray.buffer) : typedArray.buffer;
  return new typedArray.constructor(buffer, typedArray.byteOffset, typedArray.length);
}

/**
 * Copies the values of `source` to `array`.
 *
 * @private
 * @param {Array} source The array to copy values from.
 * @param {Array} [array=[]] The array to copy values to.
 * @returns {Array} Returns `array`.
 */
function copyArray(source, array) {
  var index = -1,
      length = source.length;

  array || (array = Array(length));
  while (++index < length) {
    array[index] = source[index];
  }
  return array;
}

/**
 * Copies properties of `source` to `object`.
 *
 * @private
 * @param {Object} source The object to copy properties from.
 * @param {Array} props The property identifiers to copy.
 * @param {Object} [object={}] The object to copy properties to.
 * @param {Function} [customizer] The function to customize copied values.
 * @returns {Object} Returns `object`.
 */
function copyObject(source, props, object, customizer) {
  var isNew = !object;
  object || (object = {});

  var index = -1,
      length = props.length;

  while (++index < length) {
    var key = props[index];

    var newValue = customizer
      ? customizer(object[key], source[key], key, object, source)
      : undefined;

    if (newValue === undefined) {
      newValue = source[key];
    }
    if (isNew) {
      baseAssignValue(object, key, newValue);
    } else {
      assignValue(object, key, newValue);
    }
  }
  return object;
}

/**
 * Creates a function like `_.assign`.
 *
 * @private
 * @param {Function} assigner The function to assign values.
 * @returns {Function} Returns the new assigner function.
 */
function createAssigner(assigner) {
  return baseRest(function(object, sources) {
    var index = -1,
        length = sources.length,
        customizer = length > 1 ? sources[length - 1] : undefined,
        guard = length > 2 ? sources[2] : undefined;

    customizer = (assigner.length > 3 && typeof customizer == 'function')
      ? (length--, customizer)
      : undefined;

    if (guard && isIterateeCall(sources[0], sources[1], guard)) {
      customizer = length < 3 ? undefined : customizer;
      length = 1;
    }
    object = Object(object);
    while (++index < length) {
      var source = sources[index];
      if (source) {
        assigner(object, source, index, customizer);
      }
    }
    return object;
  });
}

/**
 * Creates a base function for methods like `_.forIn` and `_.forOwn`.
 *
 * @private
 * @param {boolean} [fromRight] Specify iterating from right to left.
 * @returns {Function} Returns the new base function.
 */
function createBaseFor(fromRight) {
  return function(object, iteratee, keysFunc) {
    var index = -1,
        iterable = Object(object),
        props = keysFunc(object),
        length = props.length;

    while (length--) {
      var key = props[fromRight ? length : ++index];
      if (iteratee(iterable[key], key, iterable) === false) {
        break;
      }
    }
    return object;
  };
}

/**
 * Gets the data for `map`.
 *
 * @private
 * @param {Object} map The map to query.
 * @param {string} key The reference key.
 * @returns {*} Returns the map data.
 */
function getMapData(map, key) {
  var data = map.__data__;
  return isKeyable(key)
    ? data[typeof key == 'string' ? 'string' : 'hash']
    : data.map;
}

/**
 * Gets the native function at `key` of `object`.
 *
 * @private
 * @param {Object} object The object to query.
 * @param {string} key The key of the method to get.
 * @returns {*} Returns the function if it's native, else `undefined`.
 */
function getNative(object, key) {
  var value = getValue(object, key);
  return baseIsNative(value) ? value : undefined;
}

/**
 * A specialized version of `baseGetTag` which ignores `Symbol.toStringTag` values.
 *
 * @private
 * @param {*} value The value to query.
 * @returns {string} Returns the raw `toStringTag`.
 */
function getRawTag(value) {
  var isOwn = hasOwnProperty.call(value, symToStringTag),
      tag = value[symToStringTag];

  try {
    value[symToStringTag] = undefined;
    var unmasked = true;
  } catch (e) {}

  var result = nativeObjectToString.call(value);
  if (unmasked) {
    if (isOwn) {
      value[symToStringTag] = tag;
    } else {
      delete value[symToStringTag];
    }
  }
  return result;
}

/**
 * Initializes an object clone.
 *
 * @private
 * @param {Object} object The object to clone.
 * @returns {Object} Returns the initialized clone.
 */
function initCloneObject(object) {
  return (typeof object.constructor == 'function' && !isPrototype(object))
    ? baseCreate(getPrototype(object))
    : {};
}

/**
 * Checks if `value` is a valid array-like index.
 *
 * @private
 * @param {*} value The value to check.
 * @param {number} [length=MAX_SAFE_INTEGER] The upper bounds of a valid index.
 * @returns {boolean} Returns `true` if `value` is a valid index, else `false`.
 */
function isIndex(value, length) {
  var type = typeof value;
  length = length == null ? MAX_SAFE_INTEGER : length;

  return !!length &&
    (type == 'number' ||
      (type != 'symbol' && reIsUint.test(value))) &&
        (value > -1 && value % 1 == 0 && value < length);
}

/**
 * Checks if the given arguments are from an iteratee call.
 *
 * @private
 * @param {*} value The potential iteratee value argument.
 * @param {*} index The potential iteratee index or key argument.
 * @param {*} object The potential iteratee object argument.
 * @returns {boolean} Returns `true` if the arguments are from an iteratee call,
 *  else `false`.
 */
function isIterateeCall(value, index, object) {
  if (!isObject(object)) {
    return false;
  }
  var type = typeof index;
  if (type == 'number'
        ? (isArrayLike(object) && isIndex(index, object.length))
        : (type == 'string' && index in object)
      ) {
    return eq(object[index], value);
  }
  return false;
}

/**
 * Checks if `value` is suitable for use as unique object key.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is suitable, else `false`.
 */
function isKeyable(value) {
  var type = typeof value;
  return (type == 'string' || type == 'number' || type == 'symbol' || type == 'boolean')
    ? (value !== '__proto__')
    : (value === null);
}

/**
 * Checks if `func` has its source masked.
 *
 * @private
 * @param {Function} func The function to check.
 * @returns {boolean} Returns `true` if `func` is masked, else `false`.
 */
function isMasked(func) {
  return !!maskSrcKey && (maskSrcKey in func);
}

/**
 * Checks if `value` is likely a prototype object.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a prototype, else `false`.
 */
function isPrototype(value) {
  var Ctor = value && value.constructor,
      proto = (typeof Ctor == 'function' && Ctor.prototype) || objectProto;

  return value === proto;
}

/**
 * This function is like
 * [`Object.keys`](http://ecma-international.org/ecma-262/7.0/#sec-object.keys)
 * except that it includes inherited enumerable properties.
 *
 * @private
 * @param {Object} object The object to query.
 * @returns {Array} Returns the array of property names.
 */
function nativeKeysIn(object) {
  var result = [];
  if (object != null) {
    for (var key in Object(object)) {
      result.push(key);
    }
  }
  return result;
}

/**
 * Converts `value` to a string using `Object.prototype.toString`.
 *
 * @private
 * @param {*} value The value to convert.
 * @returns {string} Returns the converted string.
 */
function objectToString(value) {
  return nativeObjectToString.call(value);
}

/**
 * A specialized version of `baseRest` which transforms the rest array.
 *
 * @private
 * @param {Function} func The function to apply a rest parameter to.
 * @param {number} [start=func.length-1] The start position of the rest parameter.
 * @param {Function} transform The rest array transform.
 * @returns {Function} Returns the new function.
 */
function overRest(func, start, transform) {
  start = nativeMax(start === undefined ? (func.length - 1) : start, 0);
  return function() {
    var args = arguments,
        index = -1,
        length = nativeMax(args.length - start, 0),
        array = Array(length);

    while (++index < length) {
      array[index] = args[start + index];
    }
    index = -1;
    var otherArgs = Array(start + 1);
    while (++index < start) {
      otherArgs[index] = args[index];
    }
    otherArgs[start] = transform(array);
    return apply(func, this, otherArgs);
  };
}

/**
 * Gets the value at `key`, unless `key` is "__proto__" or "constructor".
 *
 * @private
 * @param {Object} object The object to query.
 * @param {string} key The key of the property to get.
 * @returns {*} Returns the property value.
 */
function safeGet(object, key) {
  if (key === 'constructor' && typeof object[key] === 'function') {
    return;
  }

  if (key == '__proto__') {
    return;
  }

  return object[key];
}

/**
 * Sets the `toString` method of `func` to return `string`.
 *
 * @private
 * @param {Function} func The function to modify.
 * @param {Function} string The `toString` result.
 * @returns {Function} Returns `func`.
 */
var setToString = shortOut(baseSetToString);

/**
 * Creates a function that'll short out and invoke `identity` instead
 * of `func` when it's called `HOT_COUNT` or more times in `HOT_SPAN`
 * milliseconds.
 *
 * @private
 * @param {Function} func The function to restrict.
 * @returns {Function} Returns the new shortable function.
 */
function shortOut(func) {
  var count = 0,
      lastCalled = 0;

  return function() {
    var stamp = nativeNow(),
        remaining = HOT_SPAN - (stamp - lastCalled);

    lastCalled = stamp;
    if (remaining > 0) {
      if (++count >= HOT_COUNT) {
        return arguments[0];
      }
    } else {
      count = 0;
    }
    return func.apply(undefined, arguments);
  };
}

/**
 * Converts `func` to its source code.
 *
 * @private
 * @param {Function} func The function to convert.
 * @returns {string} Returns the source code.
 */
function toSource(func) {
  if (func != null) {
    try {
      return funcToString.call(func);
    } catch (e) {}
    try {
      return (func + '');
    } catch (e) {}
  }
  return '';
}

/**
 * Performs a
 * [`SameValueZero`](http://ecma-international.org/ecma-262/7.0/#sec-samevaluezero)
 * comparison between two values to determine if they are equivalent.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to compare.
 * @param {*} other The other value to compare.
 * @returns {boolean} Returns `true` if the values are equivalent, else `false`.
 * @example
 *
 * var object = { 'a': 1 };
 * var other = { 'a': 1 };
 *
 * _.eq(object, object);
 * // => true
 *
 * _.eq(object, other);
 * // => false
 *
 * _.eq('a', 'a');
 * // => true
 *
 * _.eq('a', Object('a'));
 * // => false
 *
 * _.eq(NaN, NaN);
 * // => true
 */
function eq(value, other) {
  return value === other || (value !== value && other !== other);
}

/**
 * Checks if `value` is likely an `arguments` object.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an `arguments` object,
 *  else `false`.
 * @example
 *
 * _.isArguments(function() { return arguments; }());
 * // => true
 *
 * _.isArguments([1, 2, 3]);
 * // => false
 */
var isArguments = baseIsArguments(function() { return arguments; }()) ? baseIsArguments : function(value) {
  return isObjectLike(value) && hasOwnProperty.call(value, 'callee') &&
    !propertyIsEnumerable.call(value, 'callee');
};

/**
 * Checks if `value` is classified as an `Array` object.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an array, else `false`.
 * @example
 *
 * _.isArray([1, 2, 3]);
 * // => true
 *
 * _.isArray(document.body.children);
 * // => false
 *
 * _.isArray('abc');
 * // => false
 *
 * _.isArray(_.noop);
 * // => false
 */
var isArray = Array.isArray;

/**
 * Checks if `value` is array-like. A value is considered array-like if it's
 * not a function and has a `value.length` that's an integer greater than or
 * equal to `0` and less than or equal to `Number.MAX_SAFE_INTEGER`.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is array-like, else `false`.
 * @example
 *
 * _.isArrayLike([1, 2, 3]);
 * // => true
 *
 * _.isArrayLike(document.body.children);
 * // => true
 *
 * _.isArrayLike('abc');
 * // => true
 *
 * _.isArrayLike(_.noop);
 * // => false
 */
function isArrayLike(value) {
  return value != null && isLength(value.length) && !isFunction(value);
}

/**
 * This method is like `_.isArrayLike` except that it also checks if `value`
 * is an object.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an array-like object,
 *  else `false`.
 * @example
 *
 * _.isArrayLikeObject([1, 2, 3]);
 * // => true
 *
 * _.isArrayLikeObject(document.body.children);
 * // => true
 *
 * _.isArrayLikeObject('abc');
 * // => false
 *
 * _.isArrayLikeObject(_.noop);
 * // => false
 */
function isArrayLikeObject(value) {
  return isObjectLike(value) && isArrayLike(value);
}

/**
 * Checks if `value` is a buffer.
 *
 * @static
 * @memberOf _
 * @since 4.3.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a buffer, else `false`.
 * @example
 *
 * _.isBuffer(new Buffer(2));
 * // => true
 *
 * _.isBuffer(new Uint8Array(2));
 * // => false
 */
var isBuffer = nativeIsBuffer || stubFalse;

/**
 * Checks if `value` is classified as a `Function` object.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a function, else `false`.
 * @example
 *
 * _.isFunction(_);
 * // => true
 *
 * _.isFunction(/abc/);
 * // => false
 */
function isFunction(value) {
  if (!isObject(value)) {
    return false;
  }
  // The use of `Object#toString` avoids issues with the `typeof` operator
  // in Safari 9 which returns 'object' for typed arrays and other constructors.
  var tag = baseGetTag(value);
  return tag == funcTag || tag == genTag || tag == asyncTag || tag == proxyTag;
}

/**
 * Checks if `value` is a valid array-like length.
 *
 * **Note:** This method is loosely based on
 * [`ToLength`](http://ecma-international.org/ecma-262/7.0/#sec-tolength).
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a valid length, else `false`.
 * @example
 *
 * _.isLength(3);
 * // => true
 *
 * _.isLength(Number.MIN_VALUE);
 * // => false
 *
 * _.isLength(Infinity);
 * // => false
 *
 * _.isLength('3');
 * // => false
 */
function isLength(value) {
  return typeof value == 'number' &&
    value > -1 && value % 1 == 0 && value <= MAX_SAFE_INTEGER;
}

/**
 * Checks if `value` is the
 * [language type](http://www.ecma-international.org/ecma-262/7.0/#sec-ecmascript-language-types)
 * of `Object`. (e.g. arrays, functions, objects, regexes, `new Number(0)`, and `new String('')`)
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an object, else `false`.
 * @example
 *
 * _.isObject({});
 * // => true
 *
 * _.isObject([1, 2, 3]);
 * // => true
 *
 * _.isObject(_.noop);
 * // => true
 *
 * _.isObject(null);
 * // => false
 */
function isObject(value) {
  var type = typeof value;
  return value != null && (type == 'object' || type == 'function');
}

/**
 * Checks if `value` is object-like. A value is object-like if it's not `null`
 * and has a `typeof` result of "object".
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is object-like, else `false`.
 * @example
 *
 * _.isObjectLike({});
 * // => true
 *
 * _.isObjectLike([1, 2, 3]);
 * // => true
 *
 * _.isObjectLike(_.noop);
 * // => false
 *
 * _.isObjectLike(null);
 * // => false
 */
function isObjectLike(value) {
  return value != null && typeof value == 'object';
}

/**
 * Checks if `value` is a plain object, that is, an object created by the
 * `Object` constructor or one with a `[[Prototype]]` of `null`.
 *
 * @static
 * @memberOf _
 * @since 0.8.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a plain object, else `false`.
 * @example
 *
 * function Foo() {
 *   this.a = 1;
 * }
 *
 * _.isPlainObject(new Foo);
 * // => false
 *
 * _.isPlainObject([1, 2, 3]);
 * // => false
 *
 * _.isPlainObject({ 'x': 0, 'y': 0 });
 * // => true
 *
 * _.isPlainObject(Object.create(null));
 * // => true
 */
function isPlainObject(value) {
  if (!isObjectLike(value) || baseGetTag(value) != objectTag) {
    return false;
  }
  var proto = getPrototype(value);
  if (proto === null) {
    return true;
  }
  var Ctor = hasOwnProperty.call(proto, 'constructor') && proto.constructor;
  return typeof Ctor == 'function' && Ctor instanceof Ctor &&
    funcToString.call(Ctor) == objectCtorString;
}

/**
 * Checks if `value` is classified as a typed array.
 *
 * @static
 * @memberOf _
 * @since 3.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a typed array, else `false`.
 * @example
 *
 * _.isTypedArray(new Uint8Array);
 * // => true
 *
 * _.isTypedArray([]);
 * // => false
 */
var isTypedArray = nodeIsTypedArray ? baseUnary(nodeIsTypedArray) : baseIsTypedArray;

/**
 * Converts `value` to a plain object flattening inherited enumerable string
 * keyed properties of `value` to own properties of the plain object.
 *
 * @static
 * @memberOf _
 * @since 3.0.0
 * @category Lang
 * @param {*} value The value to convert.
 * @returns {Object} Returns the converted plain object.
 * @example
 *
 * function Foo() {
 *   this.b = 2;
 * }
 *
 * Foo.prototype.c = 3;
 *
 * _.assign({ 'a': 1 }, new Foo);
 * // => { 'a': 1, 'b': 2 }
 *
 * _.assign({ 'a': 1 }, _.toPlainObject(new Foo));
 * // => { 'a': 1, 'b': 2, 'c': 3 }
 */
function toPlainObject(value) {
  return copyObject(value, keysIn(value));
}

/**
 * Creates an array of the own and inherited enumerable property names of `object`.
 *
 * **Note:** Non-object values are coerced to objects.
 *
 * @static
 * @memberOf _
 * @since 3.0.0
 * @category Object
 * @param {Object} object The object to query.
 * @returns {Array} Returns the array of property names.
 * @example
 *
 * function Foo() {
 *   this.a = 1;
 *   this.b = 2;
 * }
 *
 * Foo.prototype.c = 3;
 *
 * _.keysIn(new Foo);
 * // => ['a', 'b', 'c'] (iteration order is not guaranteed)
 */
function keysIn(object) {
  return isArrayLike(object) ? arrayLikeKeys(object, true) : baseKeysIn(object);
}

/**
 * This method is like `_.assign` except that it recursively merges own and
 * inherited enumerable string keyed properties of source objects into the
 * destination object. Source properties that resolve to `undefined` are
 * skipped if a destination value exists. Array and plain object properties
 * are merged recursively. Other objects and value types are overridden by
 * assignment. Source objects are applied from left to right. Subsequent
 * sources overwrite property assignments of previous sources.
 *
 * **Note:** This method mutates `object`.
 *
 * @static
 * @memberOf _
 * @since 0.5.0
 * @category Object
 * @param {Object} object The destination object.
 * @param {...Object} [sources] The source objects.
 * @returns {Object} Returns `object`.
 * @example
 *
 * var object = {
 *   'a': [{ 'b': 2 }, { 'd': 4 }]
 * };
 *
 * var other = {
 *   'a': [{ 'c': 3 }, { 'e': 5 }]
 * };
 *
 * _.merge(object, other);
 * // => { 'a': [{ 'b': 2, 'c': 3 }, { 'd': 4, 'e': 5 }] }
 */
var merge = createAssigner(function(object, source, srcIndex) {
  baseMerge(object, source, srcIndex);
});

/**
 * Creates a function that returns `value`.
 *
 * @static
 * @memberOf _
 * @since 2.4.0
 * @category Util
 * @param {*} value The value to return from the new function.
 * @returns {Function} Returns the new constant function.
 * @example
 *
 * var objects = _.times(2, _.constant({ 'a': 1 }));
 *
 * console.log(objects);
 * // => [{ 'a': 1 }, { 'a': 1 }]
 *
 * console.log(objects[0] === objects[1]);
 * // => true
 */
function constant(value) {
  return function() {
    return value;
  };
}

/**
 * This method returns the first argument it receives.
 *
 * @static
 * @since 0.1.0
 * @memberOf _
 * @category Util
 * @param {*} value Any value.
 * @returns {*} Returns `value`.
 * @example
 *
 * var object = { 'a': 1 };
 *
 * console.log(_.identity(object) === object);
 * // => true
 */
function identity(value) {
  return value;
}

/**
 * This method returns `false`.
 *
 * @static
 * @memberOf _
 * @since 4.13.0
 * @category Util
 * @returns {boolean} Returns `false`.
 * @example
 *
 * _.times(2, _.stubFalse);
 * // => [false, false]
 */
function stubFalse() {
  return false;
}

module.exports = merge;


/***/ }),

/***/ 762:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var classof = __webpack_require__(4848);
var ITERATOR = __webpack_require__(7574)('iterator');
var Iterators = __webpack_require__(906);
module.exports = (__webpack_require__(6094).getIteratorMethod) = function (it) {
  if (it != undefined) return it[ITERATOR]
    || it['@@iterator']
    || Iterators[classof(it)];
};


/***/ }),

/***/ 906:
/***/ (function(module) {

module.exports = {};


/***/ }),

/***/ 1308:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var document = (__webpack_require__(7526).document);
module.exports = document && document.documentElement;


/***/ }),

/***/ 1384:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var global = __webpack_require__(7526);
var macrotask = (__webpack_require__(2780).set);
var Observer = global.MutationObserver || global.WebKitMutationObserver;
var process = global.process;
var Promise = global.Promise;
var isNode = __webpack_require__(5089)(process) == 'process';

module.exports = function () {
  var head, last, notify;

  var flush = function () {
    var parent, fn;
    if (isNode && (parent = process.domain)) parent.exit();
    while (head) {
      fn = head.fn;
      head = head.next;
      try {
        fn();
      } catch (e) {
        if (head) notify();
        else last = undefined;
        throw e;
      }
    } last = undefined;
    if (parent) parent.enter();
  };

  // Node.js
  if (isNode) {
    notify = function () {
      process.nextTick(flush);
    };
  // browsers with MutationObserver, except iOS Safari - https://github.com/zloirock/core-js/issues/339
  } else if (Observer && !(global.navigator && global.navigator.standalone)) {
    var toggle = true;
    var node = document.createTextNode('');
    new Observer(flush).observe(node, { characterData: true }); // eslint-disable-line no-new
    notify = function () {
      node.data = toggle = !toggle;
    };
  // environments with maybe non-completely correct, but existent Promise
  } else if (Promise && Promise.resolve) {
    // Promise.resolve without an argument throws an error in LG WebOS 2
    var promise = Promise.resolve(undefined);
    notify = function () {
      promise.then(flush);
    };
  // for other environments - macrotask based on:
  // - setImmediate
  // - MessageChannel
  // - window.postMessag
  // - onreadystatechange
  // - setTimeout
  } else {
    notify = function () {
      // strange IE + webpack dev server bug - use .call(global)
      macrotask.call(global, flush);
    };
  }

  return function (fn) {
    var task = { fn: fn, next: undefined };
    if (last) last.next = task;
    if (!head) {
      head = task;
      notify();
    } last = task;
  };
};


/***/ }),

/***/ 1485:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

// 7.1.15 ToLength
var toInteger = __webpack_require__(7087);
var min = Math.min;
module.exports = function (it) {
  return it > 0 ? min(toInteger(it), 0x1fffffffffffff) : 0; // pow(2, 53) - 1 == 9007199254740991
};


/***/ }),

/***/ 1508:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

// check on default Array iterator
var Iterators = __webpack_require__(906);
var ITERATOR = __webpack_require__(7574)('iterator');
var ArrayProto = Array.prototype;

module.exports = function (it) {
  return it !== undefined && (Iterators.Array === it || ArrayProto[ITERATOR] === it);
};


/***/ }),

/***/ 1601:
/***/ (function(module) {

"use strict";


module.exports = function (i) {
  return i[1];
};

/***/ }),

/***/ 1763:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

// Thank's IE8 for his funny defineProperty
module.exports = !__webpack_require__(9448)(function () {
  return Object.defineProperty({}, 'a', { get: function () { return 7; } }).a != 7;
});


/***/ }),

/***/ 1996:
/***/ (function(module) {

module.exports = function (bitmap, value) {
  return {
    enumerable: !(bitmap & 1),
    configurable: !(bitmap & 2),
    writable: !(bitmap & 4),
    value: value
  };
};


/***/ }),

/***/ 2127:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var global = __webpack_require__(7526);
var core = __webpack_require__(6094);
var hide = __webpack_require__(3341);
var redefine = __webpack_require__(8859);
var ctx = __webpack_require__(5052);
var PROTOTYPE = 'prototype';

var $export = function (type, name, source) {
  var IS_FORCED = type & $export.F;
  var IS_GLOBAL = type & $export.G;
  var IS_STATIC = type & $export.S;
  var IS_PROTO = type & $export.P;
  var IS_BIND = type & $export.B;
  var target = IS_GLOBAL ? global : IS_STATIC ? global[name] || (global[name] = {}) : (global[name] || {})[PROTOTYPE];
  var exports = IS_GLOBAL ? core : core[name] || (core[name] = {});
  var expProto = exports[PROTOTYPE] || (exports[PROTOTYPE] = {});
  var key, own, out, exp;
  if (IS_GLOBAL) source = name;
  for (key in source) {
    // contains in native
    own = !IS_FORCED && target && target[key] !== undefined;
    // export native or passed
    out = (own ? target : source)[key];
    // bind timers to global for call from export context
    exp = IS_BIND && own ? ctx(out, global) : IS_PROTO && typeof out == 'function' ? ctx(Function.call, out) : out;
    // extend global
    if (target) redefine(target, key, out, type & $export.U);
    // export
    if (exports[key] != out) hide(exports, key, exp);
    if (IS_PROTO && expProto[key] != out) expProto[key] = out;
  }
};
global.core = core;
// type bitmap
$export.F = 1;   // forced
$export.G = 2;   // global
$export.S = 4;   // static
$export.P = 8;   // proto
$export.B = 16;  // bind
$export.W = 32;  // wrap
$export.U = 64;  // safe
$export.R = 128; // real proto method for `library`
module.exports = $export;


/***/ }),

/***/ 2750:
/***/ (function(module) {

module.exports = false;


/***/ }),

/***/ 2770:
/***/ (function() {

// Polyfill for creating CustomEvents on IE9/10/11

// code pulled from:
// https://github.com/d4tocchini/customevent-polyfill
// https://developer.mozilla.org/en-US/docs/Web/API/CustomEvent#Polyfill

(function() {
  if (typeof window === 'undefined') {
    return;
  }

  try {
    var ce = new window.CustomEvent('test', { cancelable: true });
    ce.preventDefault();
    if (ce.defaultPrevented !== true) {
      // IE has problems with .preventDefault() on custom events
      // http://stackoverflow.com/questions/23349191
      throw new Error('Could not prevent default');
    }
  } catch (e) {
    var CustomEvent = function(event, params) {
      var evt, origPrevent;
      params = params || {};
      params.bubbles = !!params.bubbles;
      params.cancelable = !!params.cancelable;

      evt = document.createEvent('CustomEvent');
      evt.initCustomEvent(
        event,
        params.bubbles,
        params.cancelable,
        params.detail
      );
      origPrevent = evt.preventDefault;
      evt.preventDefault = function() {
        origPrevent.call(this);
        try {
          Object.defineProperty(this, 'defaultPrevented', {
            get: function() {
              return true;
            }
          });
        } catch (e) {
          this.defaultPrevented = true;
        }
      };
      return evt;
    };

    CustomEvent.prototype = window.Event.prototype;
    window.CustomEvent = CustomEvent; // expose definition to window
  }
})();


/***/ }),

/***/ 2780:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var ctx = __webpack_require__(5052);
var invoke = __webpack_require__(4877);
var html = __webpack_require__(1308);
var cel = __webpack_require__(6034);
var global = __webpack_require__(7526);
var process = global.process;
var setTask = global.setImmediate;
var clearTask = global.clearImmediate;
var MessageChannel = global.MessageChannel;
var Dispatch = global.Dispatch;
var counter = 0;
var queue = {};
var ONREADYSTATECHANGE = 'onreadystatechange';
var defer, channel, port;
var run = function () {
  var id = +this;
  // eslint-disable-next-line no-prototype-builtins
  if (queue.hasOwnProperty(id)) {
    var fn = queue[id];
    delete queue[id];
    fn();
  }
};
var listener = function (event) {
  run.call(event.data);
};
// Node.js 0.9+ & IE10+ has setImmediate, otherwise:
if (!setTask || !clearTask) {
  setTask = function setImmediate(fn) {
    var args = [];
    var i = 1;
    while (arguments.length > i) args.push(arguments[i++]);
    queue[++counter] = function () {
      // eslint-disable-next-line no-new-func
      invoke(typeof fn == 'function' ? fn : Function(fn), args);
    };
    defer(counter);
    return counter;
  };
  clearTask = function clearImmediate(id) {
    delete queue[id];
  };
  // Node.js 0.8-
  if (__webpack_require__(5089)(process) == 'process') {
    defer = function (id) {
      process.nextTick(ctx(run, id, 1));
    };
  // Sphere (JS game engine) Dispatch API
  } else if (Dispatch && Dispatch.now) {
    defer = function (id) {
      Dispatch.now(ctx(run, id, 1));
    };
  // Browsers with MessageChannel, includes WebWorkers
  } else if (MessageChannel) {
    channel = new MessageChannel();
    port = channel.port2;
    channel.port1.onmessage = listener;
    defer = ctx(port.postMessage, port, 1);
  // Browsers with postMessage, skip WebWorkers
  // IE8 has postMessage, but it's sync & typeof its postMessage is 'object'
  } else if (global.addEventListener && typeof postMessage == 'function' && !global.importScripts) {
    defer = function (id) {
      global.postMessage(id + '', '*');
    };
    global.addEventListener('message', listener, false);
  // IE8-
  } else if (ONREADYSTATECHANGE in cel('script')) {
    defer = function (id) {
      html.appendChild(cel('script'))[ONREADYSTATECHANGE] = function () {
        html.removeChild(this);
        run.call(id);
      };
    };
  // Rest old browsers
  } else {
    defer = function (id) {
      setTimeout(ctx(run, id, 1), 0);
    };
  }
}
module.exports = {
  set: setTask,
  clear: clearTask
};


/***/ }),

/***/ 2787:
/***/ (function(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   closest: function() { return /* binding */ closest; },
/* harmony export */   distance: function() { return /* binding */ distance; }
/* harmony export */ });
const peq = new Uint32Array(0x10000);
const myers_32 = (a, b) => {
    const n = a.length;
    const m = b.length;
    const lst = 1 << (n - 1);
    let pv = -1;
    let mv = 0;
    let sc = n;
    let i = n;
    while (i--) {
        peq[a.charCodeAt(i)] |= 1 << i;
    }
    for (i = 0; i < m; i++) {
        let eq = peq[b.charCodeAt(i)];
        const xv = eq | mv;
        eq |= ((eq & pv) + pv) ^ pv;
        mv |= ~(eq | pv);
        pv &= eq;
        if (mv & lst) {
            sc++;
        }
        if (pv & lst) {
            sc--;
        }
        mv = (mv << 1) | 1;
        pv = (pv << 1) | ~(xv | mv);
        mv &= xv;
    }
    i = n;
    while (i--) {
        peq[a.charCodeAt(i)] = 0;
    }
    return sc;
};
const myers_x = (b, a) => {
    const n = a.length;
    const m = b.length;
    const mhc = [];
    const phc = [];
    const hsize = Math.ceil(n / 32);
    const vsize = Math.ceil(m / 32);
    for (let i = 0; i < hsize; i++) {
        phc[i] = -1;
        mhc[i] = 0;
    }
    let j = 0;
    for (; j < vsize - 1; j++) {
        let mv = 0;
        let pv = -1;
        const start = j * 32;
        const vlen = Math.min(32, m) + start;
        for (let k = start; k < vlen; k++) {
            peq[b.charCodeAt(k)] |= 1 << k;
        }
        for (let i = 0; i < n; i++) {
            const eq = peq[a.charCodeAt(i)];
            const pb = (phc[(i / 32) | 0] >>> i) & 1;
            const mb = (mhc[(i / 32) | 0] >>> i) & 1;
            const xv = eq | mv;
            const xh = ((((eq | mb) & pv) + pv) ^ pv) | eq | mb;
            let ph = mv | ~(xh | pv);
            let mh = pv & xh;
            if ((ph >>> 31) ^ pb) {
                phc[(i / 32) | 0] ^= 1 << i;
            }
            if ((mh >>> 31) ^ mb) {
                mhc[(i / 32) | 0] ^= 1 << i;
            }
            ph = (ph << 1) | pb;
            mh = (mh << 1) | mb;
            pv = mh | ~(xv | ph);
            mv = ph & xv;
        }
        for (let k = start; k < vlen; k++) {
            peq[b.charCodeAt(k)] = 0;
        }
    }
    let mv = 0;
    let pv = -1;
    const start = j * 32;
    const vlen = Math.min(32, m - start) + start;
    for (let k = start; k < vlen; k++) {
        peq[b.charCodeAt(k)] |= 1 << k;
    }
    let score = m;
    for (let i = 0; i < n; i++) {
        const eq = peq[a.charCodeAt(i)];
        const pb = (phc[(i / 32) | 0] >>> i) & 1;
        const mb = (mhc[(i / 32) | 0] >>> i) & 1;
        const xv = eq | mv;
        const xh = ((((eq | mb) & pv) + pv) ^ pv) | eq | mb;
        let ph = mv | ~(xh | pv);
        let mh = pv & xh;
        score += (ph >>> (m - 1)) & 1;
        score -= (mh >>> (m - 1)) & 1;
        if ((ph >>> 31) ^ pb) {
            phc[(i / 32) | 0] ^= 1 << i;
        }
        if ((mh >>> 31) ^ mb) {
            mhc[(i / 32) | 0] ^= 1 << i;
        }
        ph = (ph << 1) | pb;
        mh = (mh << 1) | mb;
        pv = mh | ~(xv | ph);
        mv = ph & xv;
    }
    for (let k = start; k < vlen; k++) {
        peq[b.charCodeAt(k)] = 0;
    }
    return score;
};
const distance = (a, b) => {
    if (a.length < b.length) {
        const tmp = b;
        b = a;
        a = tmp;
    }
    if (b.length === 0) {
        return a.length;
    }
    if (a.length <= 32) {
        return myers_32(a, b);
    }
    return myers_x(a, b);
};
const closest = (str, arr) => {
    let min_distance = Infinity;
    let min_index = 0;
    for (let i = 0; i < arr.length; i++) {
        const dist = distance(str, arr[i]);
        if (dist < min_distance) {
            min_distance = dist;
            min_index = i;
        }
    }
    return arr[min_index];
};



/***/ }),

/***/ 2956:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

module.exports = !__webpack_require__(1763) && !__webpack_require__(9448)(function () {
  return Object.defineProperty(__webpack_require__(6034)('div'), 'a', { get: function () { return 7; } }).a != 7;
});


/***/ }),

/***/ 2997:
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var _node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(1601);
/* harmony import */ var _node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(6314);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__);
// Imports


var ___CSS_LOADER_EXPORT___ = _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default()((_node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0___default()));
// Module
___CSS_LOADER_EXPORT___.push([module.id, ".endereco-predictions-wrapper{position:absolute;z-index:99001;height:0}.endereco-predictions-wrapper .endereco-span--neutral{border-bottom:1px dotted #5cb85c;color:#5cb85c;font-weight:700;display:inline-block;padding:0}.endereco-predictions-wrapper .endereco-span--remove{display:none !important}.endereco-predictions-wrapper--longlist .endereco-predictions{overflow-y:scroll}.endereco-predictions-wrapper-inner{width:100%;position:relative;top:6px}.endereco-text-gray{color:#aaa}.endereco-predictions{background-color:#fff;padding:8px;margin:0;list-style:none;overflow:hidden;border-radius:4px;box-shadow:1px 1px 4px rgba(0,0,0,.25),0 0 12px rgba(0,0,0,.125);width:auto;position:absolute;max-height:280px;overflow-y:auto}.endereco-predictions .endereco-predictions__item.endereco-predictions__item.endereco-predictions__item{white-space:nowrap;margin:0;box-sizing:border-box;cursor:pointer;border:0 none;background-color:inherit;display:block;position:relative;overflow:hidden;padding:8px;color:#333;width:100%}.endereco-predictions .endereco-predictions__item.endereco-predictions__item.endereco-predictions__item *{pointer-events:none}.endereco-predictions .endereco-predictions__item.endereco-predictions__item.endereco-predictions__item:last-child{margin-bottom:0}.endereco-predictions .endereco-predictions__item.endereco-predictions__item.endereco-predictions__item:hover,.endereco-predictions .endereco-predictions__item.endereco-predictions__item.endereco-predictions__item.active{background-color:rgba(76,175,80,.1)}@keyframes fadein{from{opacity:0;transform:translateY(-10px)}to{opacity:1;transform:translateY(0px)}}.endereco-no-scroll{overflow:hidden}.endereco-popup-container{position:fixed;z-index:9900;background-color:rgba(0,0,0,.3);top:0;left:0;width:100vw;height:100vh;display:flex;justify-content:center;align-items:center;padding-top:80px;padding-bottom:120px;overflow-y:auto}@media screen and (min-width: 768px){.endereco-popup-container{padding-top:0;padding-bottom:0}}.endereco-popup-container--direction-ltr{direction:ltr}.endereco-popup-container--direction-rtl{direction:rtl}.endereco-modal{width:95%;box-sizing:border-box;margin:0;background-color:#fff;overflow:hidden;border-radius:4px;box-shadow:1px 1px 4px rgba(0,0,0,.25),0 0 12px rgba(0,0,0,.125);animation:fadein 300ms;animation-timing-function:ease-in-out;max-height:100vh;overflow-y:auto}@media screen and (min-width: 768px){.endereco-modal{max-width:480px}}@media screen and (min-width: 992px){.endereco-modal{max-width:560px}}.endereco-modal a{color:#0c517d}.endereco-modal--no-prediction.building_number_is_missing .endereco-building-number,.endereco-modal--no-prediction.building_number_not_found .endereco-building-number{display:inline-block;background-color:rgba(255,0,0,.125);border-bottom:1px solid red;padding:0 3px;min-width:20px;min-height:1em}.endereco-modal--no-prediction.building_number_is_missing .endereco-street-full,.endereco-modal--no-prediction.building_number_not_found .endereco-street-full{display:inline-block;background-color:rgba(255,0,0,.125);border-bottom:1px solid red;padding:0 3px;min-width:20px;min-height:1em}.endereco-modal--no-prediction.street_name_needs_correction .endereco-street-name{display:inline-block;background-color:rgba(255,0,0,.125);border-bottom:1px solid red;padding:0 3px;min-width:20px;min-height:1em}.endereco-modal--no-prediction.street_name_needs_correction .endereco-street-full{display:inline-block;background-color:rgba(255,0,0,.125);border-bottom:1px solid red;padding:0 3px;min-width:20px;min-height:1em}.endereco-modal--no-prediction.postal_code_needs_correction .endereco-postal-code{display:inline-block;background-color:rgba(255,0,0,.125);border-bottom:1px solid red;padding:0 3px;min-width:20px;min-height:1em}.endereco-modal--no-prediction.locality_needs_correction .endereco-locality{display:inline-block;background-color:rgba(255,0,0,.125);border-bottom:1px solid red;padding:0 3px;min-width:20px;min-height:1em}.endereco-modal--no-prediction.country_code_needs_correction .endereco-country-code,.endereco-modal--no-prediction.country_code_needs_correction .endereco-country-name{display:inline-block;background-color:rgba(255,0,0,.125);border-bottom:1px solid red;padding:0 3px;min-width:20px;min-height:1em}.endereco-modal--no-prediction.subdivision_code_needs_correction .endereco-subdivision-code,.endereco-modal--no-prediction.subdivision_code_needs_correction .endereco-subdivision-name{display:inline-block;background-color:rgba(255,0,0,.125);border-bottom:1px solid red;padding:0 3px;min-width:20px;min-height:1em}.endereco-modal__errors{margin:15px 0;background-color:rgba(255,0,0,.125);border-radius:4px;padding:10px}.endereco-modal__errors ul{padding:0 0 0 20px;margin:0;color:red}.endereco-modal__errors ul li{margin-bottom:.25em}.endereco-modal__errors ul li:last-child{margin-bottom:0}.endereco-modal__warning{padding:15px;background-color:#f6f6f6;margin:15px 0;border-radius:4px}.endereco-modal__header{color:#333;padding:15px 45px 15px 15px;font-family:sans-serif;position:relative}@media screen and (min-width: 768px){.endereco-modal__header{padding:30px 65px 15px 30px}}.endereco-modal__header-main{font-weight:700;text-align:left;color:red;margin:0;font-size:24px;line-height:1.25em}@media screen and (min-width: 768px){.endereco-modal__header-main{margin:0 0 10px}}.endereco-modal__header-sub{font-weight:400;text-align:left;color:#333;font-size:16px;margin:0 0 20px;line-height:1.5;display:none}@media screen and (min-width: 768px){.endereco-modal__header-sub{display:block;margin-bottom:0}}.endereco-modal__close{margin-left:auto;cursor:pointer;position:absolute;top:20px;right:20px;border:1px solid gray;border-radius:50%;width:20px;height:20px;display:flex;justify-content:center;align-items:center;color:gray}@media screen and (min-width: 768px){.endereco-modal__close{top:33px;right:30px}}.endereco-modal__close::after{content:\"✖\"}.endereco-modal__body{padding:7.5px 15px;font-family:sans-serif;background-color:#fff}@media screen and (min-width: 768px){.endereco-modal__body{padding:15px 30px}}.endereco-modal__address-container{padding:20px 20px 20px 23px;box-sizing:border-box;font-size:18px;font-weight:400;line-height:22px;cursor:pointer;border:1px solid gray;border-radius:4px;background-color:#fff;box-shadow:2px 2px 5px rgba(0,0,0,.125);display:block;font-family:serif;position:relative;overflow:hidden}.endereco-modal__address-confirmation{padding:0 0 0 15px;margin:15px 0;position:relative}.endereco-modal__address-confirmation label{font-weight:600;cursor:pointer;font-size:1.125em;padding-left:30px;position:relative;line-height:1.8em}.endereco-modal__address-confirmation input[type=checkbox]{width:20px;height:20px;margin-right:10px;position:absolute;top:0;left:0}.endereco-modal__secondary-link{margin-right:20px}.endereco-modal__footer{text-align:right;padding:7.5px 15px 15px;font-family:sans-serif}@media screen and (min-width: 768px){.endereco-modal__footer{padding:15px 30px 30px}}.endereco-modal__footer button{margin-left:10px}.endereco-modal__divider{position:relative}.endereco-modal__divider:first-child{margin-top:0 !important}.endereco-modal__divider:last-child{margin-bottom:0 !important}.endereco-modal__divider-innertext{display:inline-block;padding:2px 10px;background-color:#fff;margin-left:12px;position:relative;z-index:1}.endereco-modal__divider::after{width:100%;height:2px;background-color:rgba(0,0,0,.125);content:\" \";display:block;top:calc(50% - 1px);position:absolute}.endereco-address-predictions{list-style:none;margin:15px 0;padding:0;max-height:300px;overflow:hidden;overflow-y:auto}.endereco-address-predictions:last-child{margin-bottom:0 !important}.endereco-address-predictions__item{margin:10px 0;list-style:none;background-color:#fff}.endereco-address-predictions__item:first-child{margin-top:0 !important}.endereco-address-predictions__item:last-child{margin-bottom:0 !important}.endereco-address-predictions__radio{display:none}.endereco-address-predictions__label{padding:20px 20px 20px 65px;box-sizing:border-box;font-size:18px;font-weight:400;line-height:22px;cursor:pointer;border:1px solid #ccc;border-radius:4px;box-shadow:2px 2px 5px rgba(0,0,0,.125);display:block;font-family:serif;position:relative;overflow:hidden}.endereco-address-predictions__label::after{content:\" \";position:absolute;top:calc(50% - 5px);left:28px;font-size:56px;background-color:#4caf50;display:none;width:10px;height:10px;border-radius:50%;box-sizing:border-box}.endereco-address-predictions__label::before{position:absolute;top:calc(50% - 13px);left:20px;width:26px;height:26px;content:\" \";background-color:rgba(0,0,0,0);border:2px solid gray;border-radius:50%;box-sizing:border-box}.endereco-address-predictions--original .endereco-span--remove{display:inline-block;background-color:rgba(255,0,0,.125);border-bottom:1px solid red;padding:0 3px}.endereco-address-predictions--original .endereco-span--add{display:none !important}.endereco-address-predictions--suggestions .endereco-span--add{border-bottom:1px solid #4caf50;background-color:rgba(76,175,80,.125);display:inline-block;padding:0 3px}.endereco-address-predictions--suggestions .endereco-span--remove{display:none}.endereco-address-predictions--original .endereco-address-predictions__label{border-color:red}.endereco-address-predictions__radio:checked~.endereco-address-predictions__label,.endereco-address-predictions__item.active .endereco-address-predictions__label{border-color:#4caf50}.endereco-address-predictions__radio:checked~.endereco-address-predictions__label::after,.endereco-address-predictions__item.active .endereco-address-predictions__label::after{display:block}.endereco-address-predictions__radio:checked~.endereco-address-predictions__label::before,.endereco-address-predictions__item.active .endereco-address-predictions__label::before{border-color:#4caf50}.endereco-s--customer_verified input,.endereco-s--customer_verified select,.endereco-s--customer_verified .form-control,.endereco-s--customer_verified .btn,.endereco-s--salutation_correct input,.endereco-s--salutation_correct select,.endereco-s--salutation_correct .form-control,.endereco-s--salutation_correct .btn,.endereco-s--first_name_correct input,.endereco-s--first_name_correct select,.endereco-s--first_name_correct .form-control,.endereco-s--first_name_correct .btn,.endereco-s--last_name_correct input,.endereco-s--last_name_correct select,.endereco-s--last_name_correct .form-control,.endereco-s--last_name_correct .btn,.endereco-s--title_correct input,.endereco-s--title_correct select,.endereco-s--title_correct .form-control,.endereco-s--title_correct .btn,.endereco-s--email_correct input,.endereco-s--email_correct select,.endereco-s--email_correct .form-control,.endereco-s--email_correct .btn,.endereco-s--address_correct input,.endereco-s--address_correct select,.endereco-s--address_correct .form-control,.endereco-s--address_correct .btn,.endereco-s--postal_code_correct input,.endereco-s--postal_code_correct select,.endereco-s--postal_code_correct .form-control,.endereco-s--postal_code_correct .btn,.endereco-s--locality_correct input,.endereco-s--locality_correct select,.endereco-s--locality_correct .form-control,.endereco-s--locality_correct .btn,.endereco-s--street_name_correct input,.endereco-s--street_name_correct select,.endereco-s--street_name_correct .form-control,.endereco-s--street_name_correct .btn,.endereco-s--street_full_correct input,.endereco-s--street_full_correct select,.endereco-s--street_full_correct .form-control,.endereco-s--street_full_correct .btn,.endereco-s--building_number_correct input,.endereco-s--building_number_correct select,.endereco-s--building_number_correct .form-control,.endereco-s--building_number_correct .btn,.endereco-s--additional_info_correct input,.endereco-s--additional_info_correct select,.endereco-s--additional_info_correct .form-control,.endereco-s--additional_info_correct .btn,.endereco-s--country_code_correct input,.endereco-s--country_code_correct select,.endereco-s--country_code_correct .form-control,.endereco-s--country_code_correct .btn,.endereco-s--subdivision_code_correct input,.endereco-s--subdivision_code_correct select,.endereco-s--subdivision_code_correct .form-control,.endereco-s--subdivision_code_correct .btn,.endereco-s--phone_correct input,.endereco-s--phone_correct select,.endereco-s--phone_correct .form-control,.endereco-s--phone_correct .btn{border-color:#4caf50;background-color:#f8fcf8}.endereco-s--salutation_needs_correction input,.endereco-s--salutation_needs_correction select,.endereco-s--salutation_needs_correction .form-control,.endereco-s--salutation_needs_correction .btn,.endereco-s--first_name_needs_correction input,.endereco-s--first_name_needs_correction select,.endereco-s--first_name_needs_correction .form-control,.endereco-s--first_name_needs_correction .btn,.endereco-s--last_name_needs_correction input,.endereco-s--last_name_needs_correction select,.endereco-s--last_name_needs_correction .form-control,.endereco-s--last_name_needs_correction .btn,.endereco-s--title_needs_correction input,.endereco-s--title_needs_correction select,.endereco-s--title_needs_correction .form-control,.endereco-s--title_needs_correction .btn,.endereco-s--email_not_correct input,.endereco-s--email_not_correct select,.endereco-s--email_not_correct .form-control,.endereco-s--email_not_correct .btn,.endereco-s--address_needs_correction input,.endereco-s--address_needs_correction select,.endereco-s--address_needs_correction .form-control,.endereco-s--address_needs_correction .btn,.endereco-s--address_multiple_variants input,.endereco-s--address_multiple_variants select,.endereco-s--address_multiple_variants .form-control,.endereco-s--address_multiple_variants .btn,.endereco-s--address_not_found input,.endereco-s--address_not_found select,.endereco-s--address_not_found .form-control,.endereco-s--address_not_found .btn,.endereco-s--postal_code_needs_correction input,.endereco-s--postal_code_needs_correction select,.endereco-s--postal_code_needs_correction .form-control,.endereco-s--postal_code_needs_correction .btn,.endereco-s--locality_needs_correction input,.endereco-s--locality_needs_correction select,.endereco-s--locality_needs_correction .form-control,.endereco-s--locality_needs_correction .btn,.endereco-s--street_name_needs_correction input,.endereco-s--street_name_needs_correction select,.endereco-s--street_name_needs_correction .form-control,.endereco-s--street_name_needs_correction .btn,.endereco-s--street_full_needs_correction input,.endereco-s--street_full_needs_correction select,.endereco-s--street_full_needs_correction .form-control,.endereco-s--street_full_needs_correction .btn,.endereco-s--building_number_needs_correction input,.endereco-s--building_number_needs_correction select,.endereco-s--building_number_needs_correction .form-control,.endereco-s--building_number_needs_correction .btn,.endereco-s--additional_info_needs_correction input,.endereco-s--additional_info_needs_correction select,.endereco-s--additional_info_needs_correction .form-control,.endereco-s--additional_info_needs_correction .btn,.endereco-s--country_code_needs_correction input,.endereco-s--country_code_needs_correction select,.endereco-s--country_code_needs_correction .form-control,.endereco-s--country_code_needs_correction .btn,.endereco-s--subdivision_code_needs_correction input,.endereco-s--subdivision_code_needs_correction select,.endereco-s--subdivision_code_needs_correction .form-control,.endereco-s--subdivision_code_needs_correction .btn,.endereco-s--phone_invalid input,.endereco-s--phone_invalid select,.endereco-s--phone_invalid .form-control,.endereco-s--phone_invalid .btn,.endereco-s--phone_needs_correction input,.endereco-s--phone_needs_correction select,.endereco-s--phone_needs_correction .form-control,.endereco-s--phone_needs_correction .btn,.endereco-s--phone_wrong_type input,.endereco-s--phone_wrong_type select,.endereco-s--phone_wrong_type .form-control,.endereco-s--phone_wrong_type .btn{border-color:#f0ad4e;background-color:#fefbf8}.endereco-s--salutation_needs_correction.radio-inline,.endereco-s--first_name_needs_correction.radio-inline,.endereco-s--last_name_needs_correction.radio-inline,.endereco-s--title_needs_correction.radio-inline,.endereco-s--email_not_correct.radio-inline,.endereco-s--address_needs_correction.radio-inline,.endereco-s--address_multiple_variants.radio-inline,.endereco-s--address_not_found.radio-inline,.endereco-s--postal_code_needs_correction.radio-inline,.endereco-s--locality_needs_correction.radio-inline,.endereco-s--street_name_needs_correction.radio-inline,.endereco-s--street_full_needs_correction.radio-inline,.endereco-s--building_number_needs_correction.radio-inline,.endereco-s--additional_info_needs_correction.radio-inline,.endereco-s--country_code_needs_correction.radio-inline,.endereco-s--subdivision_code_needs_correction.radio-inline,.endereco-s--phone_invalid.radio-inline,.endereco-s--phone_needs_correction.radio-inline,.endereco-s--phone_wrong_type.radio-inline{border-bottom:2px dotted #f0ad4e}.endereco-status-wrapper-list{list-style:inside;margin-bottom:1rem;color:#f0ad4e;list-style:\"⚠ \" inside;padding:0}.endereco-status-wrapper-list__item--email_syntax_error{color:red;list-style:\"✗ \" inside}.endereco-status-wrapper-list__item--email_cant_receive{color:red;list-style:\"✗ \" inside}.endereco-status-wrapper-list__item--email_no_mx{color:red;list-style:\"✗ \" inside}.endereco-status-wrapper-list__item--phone_invalid{color:red;list-style:\"✗ \" inside}.endereco-status-wrapper-list__item--phone_should_be_fixed{color:red;list-style:\"✗ \" inside}.endereco-status-wrapper-list__item--phone_should_be_mobile{color:red;list-style:\"✗ \" inside}.endereco-button{border:0 none;border-radius:4px;color:#fff;background-color:#009ec0;padding:5px 15px;font-size:16px;box-shadow:1px 1px 2px #444;cursor:pointer;transition:background-color 300ms ease-in-out}.endereco-button:hover{background-color:rgb(0,116.03125,141);transition:background-color 300ms ease-in-out}.endereco-relative-container{position:relative}.endereco-big-flag{position:absolute;top:0;left:0;margin:0 !important;display:flex;z-index:1;cursor:pointer;align-items:center}.endereco-flag>svg{width:100%;height:100%}.endereco-flag-dropdown-element{margin:2px 0;padding:2px 0}.endereco-small-flag{max-width:20px;height:20px;margin-right:8px}.endereco-small-flag>svg{width:100%;height:100%}.endereco-country-code-col{min-width:30px}.endereco-cur-down{max-width:8px;margin:0 4px}.endereco-cur-down>svg{width:100%}.endereco-flag-dropdown{position:absolute;top:100%;left:0;z-index:2}.endereco-flag-dropdown-inner{background-color:#fff;padding:8px;margin:0;list-style:none;overflow:hidden;border-radius:4px;box-shadow:1px 1px 4px rgba(0,0,0,.25),0 0 12px rgba(0,0,0,.125);width:auto;max-height:280px;overflow-y:auto}.endereco-flag-dropdown-inner .endereco-flag-dropdown-element{display:flex;align-items:center;white-space:nowrap;margin:0;box-sizing:border-box;cursor:pointer;border:0 none;background-color:inherit;position:relative;overflow:hidden;padding:8px;color:#333;width:100%}.endereco-flag-dropdown-inner .endereco-flag-dropdown-element *{pointer-events:none}.endereco-flag-dropdown-inner .endereco-flag-dropdown-element:last-child{margin-bottom:0}.endereco-flag-dropdown-inner .endereco-flag-dropdown-element:hover,.endereco-flag-dropdown-inner .endereco-flag-dropdown-element.active{background-color:rgba(76,175,80,.1)}.endereco-hidden{display:none}", ""]);
// Exports
/* harmony default export */ __webpack_exports__.A = (___CSS_LOADER_EXPORT___);


/***/ }),

/***/ 3048:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

// 7.1.1 ToPrimitive(input [, PreferredType])
var isObject = __webpack_require__(3305);
// instead of the ES6 spec version, we didn't implement @@toPrimitive case
// and the second argument - flag - preferred type is a string
module.exports = function (it, S) {
  if (!isObject(it)) return it;
  var fn, val;
  if (S && typeof (fn = it.toString) == 'function' && !isObject(val = fn.call(it))) return val;
  if (typeof (fn = it.valueOf) == 'function' && !isObject(val = fn.call(it))) return val;
  if (!S && typeof (fn = it.toString) == 'function' && !isObject(val = fn.call(it))) return val;
  throw TypeError("Can't convert object to primitive value");
};


/***/ }),

/***/ 3305:
/***/ (function(module) {

module.exports = function (it) {
  return typeof it === 'object' ? it !== null : typeof it === 'function';
};


/***/ }),

/***/ 3341:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var dP = __webpack_require__(7967);
var createDesc = __webpack_require__(1996);
module.exports = __webpack_require__(1763) ? function (object, key, value) {
  return dP.f(object, key, createDesc(1, value));
} : function (object, key, value) {
  object[key] = value;
  return object;
};


/***/ }),

/***/ 3387:
/***/ (function(module) {

module.exports = function (it) {
  if (typeof it != 'function') throw TypeError(it + ' is not a function!');
  return it;
};


/***/ }),

/***/ 3844:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var def = (__webpack_require__(7967).f);
var has = __webpack_require__(7917);
var TAG = __webpack_require__(7574)('toStringTag');

module.exports = function (it, tag, stat) {
  if (it && !has(it = stat ? it : it.prototype, TAG)) def(it, TAG, { configurable: true, value: tag });
};


/***/ }),

/***/ 4228:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var isObject = __webpack_require__(3305);
module.exports = function (it) {
  if (!isObject(it)) throw TypeError(it + ' is not an object!');
  return it;
};


/***/ }),

/***/ 4258:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";

// 25.4.1.5 NewPromiseCapability(C)
var aFunction = __webpack_require__(3387);

function PromiseCapability(C) {
  var resolve, reject;
  this.promise = new C(function ($$resolve, $$reject) {
    if (resolve !== undefined || reject !== undefined) throw TypeError('Bad Promise constructor');
    resolve = $$resolve;
    reject = $$reject;
  });
  this.resolve = aFunction(resolve);
  this.reject = aFunction(reject);
}

module.exports.f = function (C) {
  return new PromiseCapability(C);
};


/***/ }),

/***/ 4415:
/***/ (function(module) {

var id = 0;
var px = Math.random();
module.exports = function (key) {
  return 'Symbol('.concat(key === undefined ? '' : key, ')_', (++id + px).toString(36));
};


/***/ }),

/***/ 4514:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var global = __webpack_require__(7526);
var navigator = global.navigator;

module.exports = navigator && navigator.userAgent || '';


/***/ }),

/***/ 4556:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var core = __webpack_require__(6094);
var global = __webpack_require__(7526);
var SHARED = '__core-js_shared__';
var store = global[SHARED] || (global[SHARED] = {});

(module.exports = function (key, value) {
  return store[key] || (store[key] = value !== undefined ? value : {});
})('versions', []).push({
  version: core.version,
  mode: __webpack_require__(2750) ? 'pure' : 'global',
  copyright: '© 2020 Denis Pushkarev (zloirock.ru)'
});


/***/ }),

/***/ 4848:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

// getting tag from 19.1.3.6 Object.prototype.toString()
var cof = __webpack_require__(5089);
var TAG = __webpack_require__(7574)('toStringTag');
// ES3 wrong here
var ARG = cof(function () { return arguments; }()) == 'Arguments';

// fallback for IE11 Script Access Denied error
var tryGet = function (it, key) {
  try {
    return it[key];
  } catch (e) { /* empty */ }
};

module.exports = function (it) {
  var O, T, B;
  return it === undefined ? 'Undefined' : it === null ? 'Null'
    // @@toStringTag case
    : typeof (T = tryGet(O = Object(it), TAG)) == 'string' ? T
    // builtinTag case
    : ARG ? cof(O)
    // ES3 arguments fallback
    : (B = cof(O)) == 'Object' && typeof O.callee == 'function' ? 'Arguments' : B;
};


/***/ }),

/***/ 4877:
/***/ (function(module) {

// fast apply, http://jsperf.lnkit.com/fast-apply/5
module.exports = function (fn, args, that) {
  var un = that === undefined;
  switch (args.length) {
    case 0: return un ? fn()
                      : fn.call(that);
    case 1: return un ? fn(args[0])
                      : fn.call(that, args[0]);
    case 2: return un ? fn(args[0], args[1])
                      : fn.call(that, args[0], args[1]);
    case 3: return un ? fn(args[0], args[1], args[2])
                      : fn.call(that, args[0], args[1], args[2]);
    case 4: return un ? fn(args[0], args[1], args[2], args[3])
                      : fn.call(that, args[0], args[1], args[2], args[3]);
  } return fn.apply(that, args);
};


/***/ }),

/***/ 5052:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

// optional / simple context binding
var aFunction = __webpack_require__(3387);
module.exports = function (fn, that, length) {
  aFunction(fn);
  if (that === undefined) return fn;
  switch (length) {
    case 1: return function (a) {
      return fn.call(that, a);
    };
    case 2: return function (a, b) {
      return fn.call(that, a, b);
    };
    case 3: return function (a, b, c) {
      return fn.call(that, a, b, c);
    };
  }
  return function (/* ...args */) {
    return fn.apply(that, arguments);
  };
};


/***/ }),

/***/ 5089:
/***/ (function(module) {

var toString = {}.toString;

module.exports = function (it) {
  return toString.call(it).slice(8, -1);
};


/***/ }),

/***/ 5762:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";

var global = __webpack_require__(7526);
var dP = __webpack_require__(7967);
var DESCRIPTORS = __webpack_require__(1763);
var SPECIES = __webpack_require__(7574)('species');

module.exports = function (KEY) {
  var C = global[KEY];
  if (DESCRIPTORS && C && !C[SPECIES]) dP.f(C, SPECIES, {
    configurable: true,
    get: function () { return this; }
  });
};


/***/ }),

/***/ 5957:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var anObject = __webpack_require__(4228);
var isObject = __webpack_require__(3305);
var newPromiseCapability = __webpack_require__(4258);

module.exports = function (C, x) {
  anObject(C);
  if (isObject(x) && x.constructor === C) return x;
  var promiseCapability = newPromiseCapability.f(C);
  var resolve = promiseCapability.resolve;
  resolve(x);
  return promiseCapability.promise;
};


/***/ }),

/***/ 6034:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var isObject = __webpack_require__(3305);
var document = (__webpack_require__(7526).document);
// typeof document.createElement is 'object' in old IE
var is = isObject(document) && isObject(document.createElement);
module.exports = function (it) {
  return is ? document.createElement(it) : {};
};


/***/ }),

/***/ 6065:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var redefine = __webpack_require__(8859);
module.exports = function (target, src, safe) {
  for (var key in src) redefine(target, key, src[key], safe);
  return target;
};


/***/ }),

/***/ 6094:
/***/ (function(module) {

var core = module.exports = { version: '2.6.12' };
if (typeof __e == 'number') __e = core; // eslint-disable-line no-undef


/***/ }),

/***/ 6314:
/***/ (function(module) {

"use strict";


/*
  MIT License http://www.opensource.org/licenses/mit-license.php
  Author Tobias Koppers @sokra
*/
module.exports = function (cssWithMappingToString) {
  var list = [];

  // return the list of modules as css string
  list.toString = function toString() {
    return this.map(function (item) {
      var content = "";
      var needLayer = typeof item[5] !== "undefined";
      if (item[4]) {
        content += "@supports (".concat(item[4], ") {");
      }
      if (item[2]) {
        content += "@media ".concat(item[2], " {");
      }
      if (needLayer) {
        content += "@layer".concat(item[5].length > 0 ? " ".concat(item[5]) : "", " {");
      }
      content += cssWithMappingToString(item);
      if (needLayer) {
        content += "}";
      }
      if (item[2]) {
        content += "}";
      }
      if (item[4]) {
        content += "}";
      }
      return content;
    }).join("");
  };

  // import a list of modules into the list
  list.i = function i(modules, media, dedupe, supports, layer) {
    if (typeof modules === "string") {
      modules = [[null, modules, undefined]];
    }
    var alreadyImportedModules = {};
    if (dedupe) {
      for (var k = 0; k < this.length; k++) {
        var id = this[k][0];
        if (id != null) {
          alreadyImportedModules[id] = true;
        }
      }
    }
    for (var _k = 0; _k < modules.length; _k++) {
      var item = [].concat(modules[_k]);
      if (dedupe && alreadyImportedModules[item[0]]) {
        continue;
      }
      if (typeof layer !== "undefined") {
        if (typeof item[5] === "undefined") {
          item[5] = layer;
        } else {
          item[1] = "@layer".concat(item[5].length > 0 ? " ".concat(item[5]) : "", " {").concat(item[1], "}");
          item[5] = layer;
        }
      }
      if (media) {
        if (!item[2]) {
          item[2] = media;
        } else {
          item[1] = "@media ".concat(item[2], " {").concat(item[1], "}");
          item[2] = media;
        }
      }
      if (supports) {
        if (!item[4]) {
          item[4] = "".concat(supports);
        } else {
          item[1] = "@supports (".concat(item[4], ") {").concat(item[1], "}");
          item[4] = supports;
        }
      }
      list.push(item);
    }
  };
  return list;
};

/***/ }),

/***/ 6440:
/***/ (function(module) {

module.exports = function (it, Constructor, name, forbiddenField) {
  if (!(it instanceof Constructor) || (forbiddenField !== undefined && forbiddenField in it)) {
    throw TypeError(name + ': incorrect invocation!');
  } return it;
};


/***/ }),

/***/ 6517:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";

var LIBRARY = __webpack_require__(2750);
var global = __webpack_require__(7526);
var ctx = __webpack_require__(5052);
var classof = __webpack_require__(4848);
var $export = __webpack_require__(2127);
var isObject = __webpack_require__(3305);
var aFunction = __webpack_require__(3387);
var anInstance = __webpack_require__(6440);
var forOf = __webpack_require__(8790);
var speciesConstructor = __webpack_require__(9190);
var task = (__webpack_require__(2780).set);
var microtask = __webpack_require__(1384)();
var newPromiseCapabilityModule = __webpack_require__(4258);
var perform = __webpack_require__(128);
var userAgent = __webpack_require__(4514);
var promiseResolve = __webpack_require__(5957);
var PROMISE = 'Promise';
var TypeError = global.TypeError;
var process = global.process;
var versions = process && process.versions;
var v8 = versions && versions.v8 || '';
var $Promise = global[PROMISE];
var isNode = classof(process) == 'process';
var empty = function () { /* empty */ };
var Internal, newGenericPromiseCapability, OwnPromiseCapability, Wrapper;
var newPromiseCapability = newGenericPromiseCapability = newPromiseCapabilityModule.f;

var USE_NATIVE = !!function () {
  try {
    // correct subclassing with @@species support
    var promise = $Promise.resolve(1);
    var FakePromise = (promise.constructor = {})[__webpack_require__(7574)('species')] = function (exec) {
      exec(empty, empty);
    };
    // unhandled rejections tracking support, NodeJS Promise without it fails @@species test
    return (isNode || typeof PromiseRejectionEvent == 'function')
      && promise.then(empty) instanceof FakePromise
      // v8 6.6 (Node 10 and Chrome 66) have a bug with resolving custom thenables
      // https://bugs.chromium.org/p/chromium/issues/detail?id=830565
      // we can't detect it synchronously, so just check versions
      && v8.indexOf('6.6') !== 0
      && userAgent.indexOf('Chrome/66') === -1;
  } catch (e) { /* empty */ }
}();

// helpers
var isThenable = function (it) {
  var then;
  return isObject(it) && typeof (then = it.then) == 'function' ? then : false;
};
var notify = function (promise, isReject) {
  if (promise._n) return;
  promise._n = true;
  var chain = promise._c;
  microtask(function () {
    var value = promise._v;
    var ok = promise._s == 1;
    var i = 0;
    var run = function (reaction) {
      var handler = ok ? reaction.ok : reaction.fail;
      var resolve = reaction.resolve;
      var reject = reaction.reject;
      var domain = reaction.domain;
      var result, then, exited;
      try {
        if (handler) {
          if (!ok) {
            if (promise._h == 2) onHandleUnhandled(promise);
            promise._h = 1;
          }
          if (handler === true) result = value;
          else {
            if (domain) domain.enter();
            result = handler(value); // may throw
            if (domain) {
              domain.exit();
              exited = true;
            }
          }
          if (result === reaction.promise) {
            reject(TypeError('Promise-chain cycle'));
          } else if (then = isThenable(result)) {
            then.call(result, resolve, reject);
          } else resolve(result);
        } else reject(value);
      } catch (e) {
        if (domain && !exited) domain.exit();
        reject(e);
      }
    };
    while (chain.length > i) run(chain[i++]); // variable length - can't use forEach
    promise._c = [];
    promise._n = false;
    if (isReject && !promise._h) onUnhandled(promise);
  });
};
var onUnhandled = function (promise) {
  task.call(global, function () {
    var value = promise._v;
    var unhandled = isUnhandled(promise);
    var result, handler, console;
    if (unhandled) {
      result = perform(function () {
        if (isNode) {
          process.emit('unhandledRejection', value, promise);
        } else if (handler = global.onunhandledrejection) {
          handler({ promise: promise, reason: value });
        } else if ((console = global.console) && console.error) {
          console.error('Unhandled promise rejection', value);
        }
      });
      // Browsers should not trigger `rejectionHandled` event if it was handled here, NodeJS - should
      promise._h = isNode || isUnhandled(promise) ? 2 : 1;
    } promise._a = undefined;
    if (unhandled && result.e) throw result.v;
  });
};
var isUnhandled = function (promise) {
  return promise._h !== 1 && (promise._a || promise._c).length === 0;
};
var onHandleUnhandled = function (promise) {
  task.call(global, function () {
    var handler;
    if (isNode) {
      process.emit('rejectionHandled', promise);
    } else if (handler = global.onrejectionhandled) {
      handler({ promise: promise, reason: promise._v });
    }
  });
};
var $reject = function (value) {
  var promise = this;
  if (promise._d) return;
  promise._d = true;
  promise = promise._w || promise; // unwrap
  promise._v = value;
  promise._s = 2;
  if (!promise._a) promise._a = promise._c.slice();
  notify(promise, true);
};
var $resolve = function (value) {
  var promise = this;
  var then;
  if (promise._d) return;
  promise._d = true;
  promise = promise._w || promise; // unwrap
  try {
    if (promise === value) throw TypeError("Promise can't be resolved itself");
    if (then = isThenable(value)) {
      microtask(function () {
        var wrapper = { _w: promise, _d: false }; // wrap
        try {
          then.call(value, ctx($resolve, wrapper, 1), ctx($reject, wrapper, 1));
        } catch (e) {
          $reject.call(wrapper, e);
        }
      });
    } else {
      promise._v = value;
      promise._s = 1;
      notify(promise, false);
    }
  } catch (e) {
    $reject.call({ _w: promise, _d: false }, e); // wrap
  }
};

// constructor polyfill
if (!USE_NATIVE) {
  // 25.4.3.1 Promise(executor)
  $Promise = function Promise(executor) {
    anInstance(this, $Promise, PROMISE, '_h');
    aFunction(executor);
    Internal.call(this);
    try {
      executor(ctx($resolve, this, 1), ctx($reject, this, 1));
    } catch (err) {
      $reject.call(this, err);
    }
  };
  // eslint-disable-next-line no-unused-vars
  Internal = function Promise(executor) {
    this._c = [];             // <- awaiting reactions
    this._a = undefined;      // <- checked in isUnhandled reactions
    this._s = 0;              // <- state
    this._d = false;          // <- done
    this._v = undefined;      // <- value
    this._h = 0;              // <- rejection state, 0 - default, 1 - handled, 2 - unhandled
    this._n = false;          // <- notify
  };
  Internal.prototype = __webpack_require__(6065)($Promise.prototype, {
    // 25.4.5.3 Promise.prototype.then(onFulfilled, onRejected)
    then: function then(onFulfilled, onRejected) {
      var reaction = newPromiseCapability(speciesConstructor(this, $Promise));
      reaction.ok = typeof onFulfilled == 'function' ? onFulfilled : true;
      reaction.fail = typeof onRejected == 'function' && onRejected;
      reaction.domain = isNode ? process.domain : undefined;
      this._c.push(reaction);
      if (this._a) this._a.push(reaction);
      if (this._s) notify(this, false);
      return reaction.promise;
    },
    // 25.4.5.1 Promise.prototype.catch(onRejected)
    'catch': function (onRejected) {
      return this.then(undefined, onRejected);
    }
  });
  OwnPromiseCapability = function () {
    var promise = new Internal();
    this.promise = promise;
    this.resolve = ctx($resolve, promise, 1);
    this.reject = ctx($reject, promise, 1);
  };
  newPromiseCapabilityModule.f = newPromiseCapability = function (C) {
    return C === $Promise || C === Wrapper
      ? new OwnPromiseCapability(C)
      : newGenericPromiseCapability(C);
  };
}

$export($export.G + $export.W + $export.F * !USE_NATIVE, { Promise: $Promise });
__webpack_require__(3844)($Promise, PROMISE);
__webpack_require__(5762)(PROMISE);
Wrapper = __webpack_require__(6094)[PROMISE];

// statics
$export($export.S + $export.F * !USE_NATIVE, PROMISE, {
  // 25.4.4.5 Promise.reject(r)
  reject: function reject(r) {
    var capability = newPromiseCapability(this);
    var $$reject = capability.reject;
    $$reject(r);
    return capability.promise;
  }
});
$export($export.S + $export.F * (LIBRARY || !USE_NATIVE), PROMISE, {
  // 25.4.4.6 Promise.resolve(x)
  resolve: function resolve(x) {
    return promiseResolve(LIBRARY && this === Wrapper ? $Promise : this, x);
  }
});
$export($export.S + $export.F * !(USE_NATIVE && __webpack_require__(8931)(function (iter) {
  $Promise.all(iter)['catch'](empty);
})), PROMISE, {
  // 25.4.4.1 Promise.all(iterable)
  all: function all(iterable) {
    var C = this;
    var capability = newPromiseCapability(C);
    var resolve = capability.resolve;
    var reject = capability.reject;
    var result = perform(function () {
      var values = [];
      var index = 0;
      var remaining = 1;
      forOf(iterable, false, function (promise) {
        var $index = index++;
        var alreadyCalled = false;
        values.push(undefined);
        remaining++;
        C.resolve(promise).then(function (value) {
          if (alreadyCalled) return;
          alreadyCalled = true;
          values[$index] = value;
          --remaining || resolve(values);
        }, reject);
      });
      --remaining || resolve(values);
    });
    if (result.e) reject(result.v);
    return capability.promise;
  },
  // 25.4.4.4 Promise.race(iterable)
  race: function race(iterable) {
    var C = this;
    var capability = newPromiseCapability(C);
    var reject = capability.reject;
    var result = perform(function () {
      forOf(iterable, false, function (promise) {
        C.resolve(promise).then(capability.resolve, reject);
      });
    });
    if (result.e) reject(result.v);
    return capability.promise;
  }
});


/***/ }),

/***/ 6630:
/***/ (function(module, exports, __webpack_require__) {

/* module decorator */ module = __webpack_require__.nmd(module);
var __WEBPACK_AMD_DEFINE_RESULT__;(function() {
  'use strict';
  
  var collator;
  try {
    collator = (typeof Intl !== "undefined" && typeof Intl.Collator !== "undefined") ? Intl.Collator("generic", { sensitivity: "base" }) : null;
  } catch (err){
    console.log("Collator could not be initialized and wouldn't be used");
  }

  var levenshtein = __webpack_require__(2787);

  // arrays to re-use
  var prevRow = [],
    str2Char = [];
  
  /**
   * Based on the algorithm at http://en.wikipedia.org/wiki/Levenshtein_distance.
   */
  var Levenshtein = {
    /**
     * Calculate levenshtein distance of the two strings.
     *
     * @param str1 String the first string.
     * @param str2 String the second string.
     * @param [options] Additional options.
     * @param [options.useCollator] Use `Intl.Collator` for locale-sensitive string comparison.
     * @return Integer the levenshtein distance (0 and above).
     */
    get: function(str1, str2, options) {
      var useCollator = (options && collator && options.useCollator);
      
      if (useCollator) {
        var str1Len = str1.length,
          str2Len = str2.length;
        
        // base cases
        if (str1Len === 0) return str2Len;
        if (str2Len === 0) return str1Len;

        // two rows
        var curCol, nextCol, i, j, tmp;

        // initialise previous row
        for (i=0; i<str2Len; ++i) {
          prevRow[i] = i;
          str2Char[i] = str2.charCodeAt(i);
        }
        prevRow[str2Len] = str2Len;

        var strCmp;
        // calculate current row distance from previous row using collator
        for (i = 0; i < str1Len; ++i) {
          nextCol = i + 1;

          for (j = 0; j < str2Len; ++j) {
            curCol = nextCol;

            // substution
            strCmp = 0 === collator.compare(str1.charAt(i), String.fromCharCode(str2Char[j]));

            nextCol = prevRow[j] + (strCmp ? 0 : 1);

            // insertion
            tmp = curCol + 1;
            if (nextCol > tmp) {
              nextCol = tmp;
            }
            // deletion
            tmp = prevRow[j + 1] + 1;
            if (nextCol > tmp) {
              nextCol = tmp;
            }

            // copy current col value into previous (in preparation for next iteration)
            prevRow[j] = curCol;
          }

          // copy last col value into previous (in preparation for next iteration)
          prevRow[j] = nextCol;
        }
        return nextCol;
      }
      return levenshtein.distance(str1, str2);
    }

  };

  // amd
  if ( true && __webpack_require__.amdD !== null && __webpack_require__.amdO) {
    !(__WEBPACK_AMD_DEFINE_RESULT__ = (function() {
      return Levenshtein;
    }).call(exports, __webpack_require__, exports, module),
		__WEBPACK_AMD_DEFINE_RESULT__ !== undefined && (module.exports = __WEBPACK_AMD_DEFINE_RESULT__));
  }
  // commonjs
  else if ( true && module !== null && typeof exports !== "undefined" && module.exports === exports) {
    module.exports = Levenshtein;
  }
  // web worker
  else if (typeof self !== "undefined" && typeof self.postMessage === 'function' && typeof self.importScripts === 'function') {
    self.Levenshtein = Levenshtein;
  }
  // browser main thread
  else if (typeof window !== "undefined" && window !== null) {
    window.Levenshtein = Levenshtein;
  }
}());


/***/ }),

/***/ 7087:
/***/ (function(module) {

// 7.1.4 ToInteger
var ceil = Math.ceil;
var floor = Math.floor;
module.exports = function (it) {
  return isNaN(it = +it) ? 0 : (it > 0 ? floor : ceil)(it);
};


/***/ }),

/***/ 7368:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

// call something on iterator step with safe closing on error
var anObject = __webpack_require__(4228);
module.exports = function (iterator, fn, value, entries) {
  try {
    return entries ? fn(anObject(value)[0], value[1]) : fn(value);
  // 7.4.6 IteratorClose(iterator, completion)
  } catch (e) {
    var ret = iterator['return'];
    if (ret !== undefined) anObject(ret.call(iterator));
    throw e;
  }
};


/***/ }),

/***/ 7526:
/***/ (function(module) {

// https://github.com/zloirock/core-js/issues/86#issuecomment-115759028
var global = module.exports = typeof window != 'undefined' && window.Math == Math
  ? window : typeof self != 'undefined' && self.Math == Math ? self
  // eslint-disable-next-line no-new-func
  : Function('return this')();
if (typeof __g == 'number') __g = global; // eslint-disable-line no-undef


/***/ }),

/***/ 7574:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var store = __webpack_require__(4556)('wks');
var uid = __webpack_require__(4415);
var Symbol = (__webpack_require__(7526).Symbol);
var USE_SYMBOL = typeof Symbol == 'function';

var $exports = module.exports = function (name) {
  return store[name] || (store[name] =
    USE_SYMBOL && Symbol[name] || (USE_SYMBOL ? Symbol : uid)('Symbol.' + name));
};

$exports.store = store;


/***/ }),

/***/ 7917:
/***/ (function(module) {

var hasOwnProperty = {}.hasOwnProperty;
module.exports = function (it, key) {
  return hasOwnProperty.call(it, key);
};


/***/ }),

/***/ 7967:
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

var anObject = __webpack_require__(4228);
var IE8_DOM_DEFINE = __webpack_require__(2956);
var toPrimitive = __webpack_require__(3048);
var dP = Object.defineProperty;

exports.f = __webpack_require__(1763) ? Object.defineProperty : function defineProperty(O, P, Attributes) {
  anObject(O);
  P = toPrimitive(P, true);
  anObject(Attributes);
  if (IE8_DOM_DEFINE) try {
    return dP(O, P, Attributes);
  } catch (e) { /* empty */ }
  if ('get' in Attributes || 'set' in Attributes) throw TypeError('Accessors not supported!');
  if ('value' in Attributes) O[P] = Attributes.value;
  return O;
};


/***/ }),

/***/ 8142:
/***/ (function(module, exports, __webpack_require__) {

/* module decorator */ module = __webpack_require__.nmd(module);
/**
 * Lodash (Custom Build) <https://lodash.com/>
 * Build: `lodash modularize exports="npm" -o ./`
 * Copyright JS Foundation and other contributors <https://js.foundation/>
 * Released under MIT license <https://lodash.com/license>
 * Based on Underscore.js 1.8.3 <http://underscorejs.org/LICENSE>
 * Copyright Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
 */

/** Used as the size to enable large array optimizations. */
var LARGE_ARRAY_SIZE = 200;

/** Used to stand-in for `undefined` hash values. */
var HASH_UNDEFINED = '__lodash_hash_undefined__';

/** Used to compose bitmasks for value comparisons. */
var COMPARE_PARTIAL_FLAG = 1,
    COMPARE_UNORDERED_FLAG = 2;

/** Used as references for various `Number` constants. */
var MAX_SAFE_INTEGER = 9007199254740991;

/** `Object#toString` result references. */
var argsTag = '[object Arguments]',
    arrayTag = '[object Array]',
    asyncTag = '[object AsyncFunction]',
    boolTag = '[object Boolean]',
    dateTag = '[object Date]',
    errorTag = '[object Error]',
    funcTag = '[object Function]',
    genTag = '[object GeneratorFunction]',
    mapTag = '[object Map]',
    numberTag = '[object Number]',
    nullTag = '[object Null]',
    objectTag = '[object Object]',
    promiseTag = '[object Promise]',
    proxyTag = '[object Proxy]',
    regexpTag = '[object RegExp]',
    setTag = '[object Set]',
    stringTag = '[object String]',
    symbolTag = '[object Symbol]',
    undefinedTag = '[object Undefined]',
    weakMapTag = '[object WeakMap]';

var arrayBufferTag = '[object ArrayBuffer]',
    dataViewTag = '[object DataView]',
    float32Tag = '[object Float32Array]',
    float64Tag = '[object Float64Array]',
    int8Tag = '[object Int8Array]',
    int16Tag = '[object Int16Array]',
    int32Tag = '[object Int32Array]',
    uint8Tag = '[object Uint8Array]',
    uint8ClampedTag = '[object Uint8ClampedArray]',
    uint16Tag = '[object Uint16Array]',
    uint32Tag = '[object Uint32Array]';

/**
 * Used to match `RegExp`
 * [syntax characters](http://ecma-international.org/ecma-262/7.0/#sec-patterns).
 */
var reRegExpChar = /[\\^$.*+?()[\]{}|]/g;

/** Used to detect host constructors (Safari). */
var reIsHostCtor = /^\[object .+?Constructor\]$/;

/** Used to detect unsigned integer values. */
var reIsUint = /^(?:0|[1-9]\d*)$/;

/** Used to identify `toStringTag` values of typed arrays. */
var typedArrayTags = {};
typedArrayTags[float32Tag] = typedArrayTags[float64Tag] =
typedArrayTags[int8Tag] = typedArrayTags[int16Tag] =
typedArrayTags[int32Tag] = typedArrayTags[uint8Tag] =
typedArrayTags[uint8ClampedTag] = typedArrayTags[uint16Tag] =
typedArrayTags[uint32Tag] = true;
typedArrayTags[argsTag] = typedArrayTags[arrayTag] =
typedArrayTags[arrayBufferTag] = typedArrayTags[boolTag] =
typedArrayTags[dataViewTag] = typedArrayTags[dateTag] =
typedArrayTags[errorTag] = typedArrayTags[funcTag] =
typedArrayTags[mapTag] = typedArrayTags[numberTag] =
typedArrayTags[objectTag] = typedArrayTags[regexpTag] =
typedArrayTags[setTag] = typedArrayTags[stringTag] =
typedArrayTags[weakMapTag] = false;

/** Detect free variable `global` from Node.js. */
var freeGlobal = typeof __webpack_require__.g == 'object' && __webpack_require__.g && __webpack_require__.g.Object === Object && __webpack_require__.g;

/** Detect free variable `self`. */
var freeSelf = typeof self == 'object' && self && self.Object === Object && self;

/** Used as a reference to the global object. */
var root = freeGlobal || freeSelf || Function('return this')();

/** Detect free variable `exports`. */
var freeExports =  true && exports && !exports.nodeType && exports;

/** Detect free variable `module`. */
var freeModule = freeExports && "object" == 'object' && module && !module.nodeType && module;

/** Detect the popular CommonJS extension `module.exports`. */
var moduleExports = freeModule && freeModule.exports === freeExports;

/** Detect free variable `process` from Node.js. */
var freeProcess = moduleExports && freeGlobal.process;

/** Used to access faster Node.js helpers. */
var nodeUtil = (function() {
  try {
    return freeProcess && freeProcess.binding && freeProcess.binding('util');
  } catch (e) {}
}());

/* Node.js helper references. */
var nodeIsTypedArray = nodeUtil && nodeUtil.isTypedArray;

/**
 * A specialized version of `_.filter` for arrays without support for
 * iteratee shorthands.
 *
 * @private
 * @param {Array} [array] The array to iterate over.
 * @param {Function} predicate The function invoked per iteration.
 * @returns {Array} Returns the new filtered array.
 */
function arrayFilter(array, predicate) {
  var index = -1,
      length = array == null ? 0 : array.length,
      resIndex = 0,
      result = [];

  while (++index < length) {
    var value = array[index];
    if (predicate(value, index, array)) {
      result[resIndex++] = value;
    }
  }
  return result;
}

/**
 * Appends the elements of `values` to `array`.
 *
 * @private
 * @param {Array} array The array to modify.
 * @param {Array} values The values to append.
 * @returns {Array} Returns `array`.
 */
function arrayPush(array, values) {
  var index = -1,
      length = values.length,
      offset = array.length;

  while (++index < length) {
    array[offset + index] = values[index];
  }
  return array;
}

/**
 * A specialized version of `_.some` for arrays without support for iteratee
 * shorthands.
 *
 * @private
 * @param {Array} [array] The array to iterate over.
 * @param {Function} predicate The function invoked per iteration.
 * @returns {boolean} Returns `true` if any element passes the predicate check,
 *  else `false`.
 */
function arraySome(array, predicate) {
  var index = -1,
      length = array == null ? 0 : array.length;

  while (++index < length) {
    if (predicate(array[index], index, array)) {
      return true;
    }
  }
  return false;
}

/**
 * The base implementation of `_.times` without support for iteratee shorthands
 * or max array length checks.
 *
 * @private
 * @param {number} n The number of times to invoke `iteratee`.
 * @param {Function} iteratee The function invoked per iteration.
 * @returns {Array} Returns the array of results.
 */
function baseTimes(n, iteratee) {
  var index = -1,
      result = Array(n);

  while (++index < n) {
    result[index] = iteratee(index);
  }
  return result;
}

/**
 * The base implementation of `_.unary` without support for storing metadata.
 *
 * @private
 * @param {Function} func The function to cap arguments for.
 * @returns {Function} Returns the new capped function.
 */
function baseUnary(func) {
  return function(value) {
    return func(value);
  };
}

/**
 * Checks if a `cache` value for `key` exists.
 *
 * @private
 * @param {Object} cache The cache to query.
 * @param {string} key The key of the entry to check.
 * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
 */
function cacheHas(cache, key) {
  return cache.has(key);
}

/**
 * Gets the value at `key` of `object`.
 *
 * @private
 * @param {Object} [object] The object to query.
 * @param {string} key The key of the property to get.
 * @returns {*} Returns the property value.
 */
function getValue(object, key) {
  return object == null ? undefined : object[key];
}

/**
 * Converts `map` to its key-value pairs.
 *
 * @private
 * @param {Object} map The map to convert.
 * @returns {Array} Returns the key-value pairs.
 */
function mapToArray(map) {
  var index = -1,
      result = Array(map.size);

  map.forEach(function(value, key) {
    result[++index] = [key, value];
  });
  return result;
}

/**
 * Creates a unary function that invokes `func` with its argument transformed.
 *
 * @private
 * @param {Function} func The function to wrap.
 * @param {Function} transform The argument transform.
 * @returns {Function} Returns the new function.
 */
function overArg(func, transform) {
  return function(arg) {
    return func(transform(arg));
  };
}

/**
 * Converts `set` to an array of its values.
 *
 * @private
 * @param {Object} set The set to convert.
 * @returns {Array} Returns the values.
 */
function setToArray(set) {
  var index = -1,
      result = Array(set.size);

  set.forEach(function(value) {
    result[++index] = value;
  });
  return result;
}

/** Used for built-in method references. */
var arrayProto = Array.prototype,
    funcProto = Function.prototype,
    objectProto = Object.prototype;

/** Used to detect overreaching core-js shims. */
var coreJsData = root['__core-js_shared__'];

/** Used to resolve the decompiled source of functions. */
var funcToString = funcProto.toString;

/** Used to check objects for own properties. */
var hasOwnProperty = objectProto.hasOwnProperty;

/** Used to detect methods masquerading as native. */
var maskSrcKey = (function() {
  var uid = /[^.]+$/.exec(coreJsData && coreJsData.keys && coreJsData.keys.IE_PROTO || '');
  return uid ? ('Symbol(src)_1.' + uid) : '';
}());

/**
 * Used to resolve the
 * [`toStringTag`](http://ecma-international.org/ecma-262/7.0/#sec-object.prototype.tostring)
 * of values.
 */
var nativeObjectToString = objectProto.toString;

/** Used to detect if a method is native. */
var reIsNative = RegExp('^' +
  funcToString.call(hasOwnProperty).replace(reRegExpChar, '\\$&')
  .replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, '$1.*?') + '$'
);

/** Built-in value references. */
var Buffer = moduleExports ? root.Buffer : undefined,
    Symbol = root.Symbol,
    Uint8Array = root.Uint8Array,
    propertyIsEnumerable = objectProto.propertyIsEnumerable,
    splice = arrayProto.splice,
    symToStringTag = Symbol ? Symbol.toStringTag : undefined;

/* Built-in method references for those with the same name as other `lodash` methods. */
var nativeGetSymbols = Object.getOwnPropertySymbols,
    nativeIsBuffer = Buffer ? Buffer.isBuffer : undefined,
    nativeKeys = overArg(Object.keys, Object);

/* Built-in method references that are verified to be native. */
var DataView = getNative(root, 'DataView'),
    Map = getNative(root, 'Map'),
    Promise = getNative(root, 'Promise'),
    Set = getNative(root, 'Set'),
    WeakMap = getNative(root, 'WeakMap'),
    nativeCreate = getNative(Object, 'create');

/** Used to detect maps, sets, and weakmaps. */
var dataViewCtorString = toSource(DataView),
    mapCtorString = toSource(Map),
    promiseCtorString = toSource(Promise),
    setCtorString = toSource(Set),
    weakMapCtorString = toSource(WeakMap);

/** Used to convert symbols to primitives and strings. */
var symbolProto = Symbol ? Symbol.prototype : undefined,
    symbolValueOf = symbolProto ? symbolProto.valueOf : undefined;

/**
 * Creates a hash object.
 *
 * @private
 * @constructor
 * @param {Array} [entries] The key-value pairs to cache.
 */
function Hash(entries) {
  var index = -1,
      length = entries == null ? 0 : entries.length;

  this.clear();
  while (++index < length) {
    var entry = entries[index];
    this.set(entry[0], entry[1]);
  }
}

/**
 * Removes all key-value entries from the hash.
 *
 * @private
 * @name clear
 * @memberOf Hash
 */
function hashClear() {
  this.__data__ = nativeCreate ? nativeCreate(null) : {};
  this.size = 0;
}

/**
 * Removes `key` and its value from the hash.
 *
 * @private
 * @name delete
 * @memberOf Hash
 * @param {Object} hash The hash to modify.
 * @param {string} key The key of the value to remove.
 * @returns {boolean} Returns `true` if the entry was removed, else `false`.
 */
function hashDelete(key) {
  var result = this.has(key) && delete this.__data__[key];
  this.size -= result ? 1 : 0;
  return result;
}

/**
 * Gets the hash value for `key`.
 *
 * @private
 * @name get
 * @memberOf Hash
 * @param {string} key The key of the value to get.
 * @returns {*} Returns the entry value.
 */
function hashGet(key) {
  var data = this.__data__;
  if (nativeCreate) {
    var result = data[key];
    return result === HASH_UNDEFINED ? undefined : result;
  }
  return hasOwnProperty.call(data, key) ? data[key] : undefined;
}

/**
 * Checks if a hash value for `key` exists.
 *
 * @private
 * @name has
 * @memberOf Hash
 * @param {string} key The key of the entry to check.
 * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
 */
function hashHas(key) {
  var data = this.__data__;
  return nativeCreate ? (data[key] !== undefined) : hasOwnProperty.call(data, key);
}

/**
 * Sets the hash `key` to `value`.
 *
 * @private
 * @name set
 * @memberOf Hash
 * @param {string} key The key of the value to set.
 * @param {*} value The value to set.
 * @returns {Object} Returns the hash instance.
 */
function hashSet(key, value) {
  var data = this.__data__;
  this.size += this.has(key) ? 0 : 1;
  data[key] = (nativeCreate && value === undefined) ? HASH_UNDEFINED : value;
  return this;
}

// Add methods to `Hash`.
Hash.prototype.clear = hashClear;
Hash.prototype['delete'] = hashDelete;
Hash.prototype.get = hashGet;
Hash.prototype.has = hashHas;
Hash.prototype.set = hashSet;

/**
 * Creates an list cache object.
 *
 * @private
 * @constructor
 * @param {Array} [entries] The key-value pairs to cache.
 */
function ListCache(entries) {
  var index = -1,
      length = entries == null ? 0 : entries.length;

  this.clear();
  while (++index < length) {
    var entry = entries[index];
    this.set(entry[0], entry[1]);
  }
}

/**
 * Removes all key-value entries from the list cache.
 *
 * @private
 * @name clear
 * @memberOf ListCache
 */
function listCacheClear() {
  this.__data__ = [];
  this.size = 0;
}

/**
 * Removes `key` and its value from the list cache.
 *
 * @private
 * @name delete
 * @memberOf ListCache
 * @param {string} key The key of the value to remove.
 * @returns {boolean} Returns `true` if the entry was removed, else `false`.
 */
function listCacheDelete(key) {
  var data = this.__data__,
      index = assocIndexOf(data, key);

  if (index < 0) {
    return false;
  }
  var lastIndex = data.length - 1;
  if (index == lastIndex) {
    data.pop();
  } else {
    splice.call(data, index, 1);
  }
  --this.size;
  return true;
}

/**
 * Gets the list cache value for `key`.
 *
 * @private
 * @name get
 * @memberOf ListCache
 * @param {string} key The key of the value to get.
 * @returns {*} Returns the entry value.
 */
function listCacheGet(key) {
  var data = this.__data__,
      index = assocIndexOf(data, key);

  return index < 0 ? undefined : data[index][1];
}

/**
 * Checks if a list cache value for `key` exists.
 *
 * @private
 * @name has
 * @memberOf ListCache
 * @param {string} key The key of the entry to check.
 * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
 */
function listCacheHas(key) {
  return assocIndexOf(this.__data__, key) > -1;
}

/**
 * Sets the list cache `key` to `value`.
 *
 * @private
 * @name set
 * @memberOf ListCache
 * @param {string} key The key of the value to set.
 * @param {*} value The value to set.
 * @returns {Object} Returns the list cache instance.
 */
function listCacheSet(key, value) {
  var data = this.__data__,
      index = assocIndexOf(data, key);

  if (index < 0) {
    ++this.size;
    data.push([key, value]);
  } else {
    data[index][1] = value;
  }
  return this;
}

// Add methods to `ListCache`.
ListCache.prototype.clear = listCacheClear;
ListCache.prototype['delete'] = listCacheDelete;
ListCache.prototype.get = listCacheGet;
ListCache.prototype.has = listCacheHas;
ListCache.prototype.set = listCacheSet;

/**
 * Creates a map cache object to store key-value pairs.
 *
 * @private
 * @constructor
 * @param {Array} [entries] The key-value pairs to cache.
 */
function MapCache(entries) {
  var index = -1,
      length = entries == null ? 0 : entries.length;

  this.clear();
  while (++index < length) {
    var entry = entries[index];
    this.set(entry[0], entry[1]);
  }
}

/**
 * Removes all key-value entries from the map.
 *
 * @private
 * @name clear
 * @memberOf MapCache
 */
function mapCacheClear() {
  this.size = 0;
  this.__data__ = {
    'hash': new Hash,
    'map': new (Map || ListCache),
    'string': new Hash
  };
}

/**
 * Removes `key` and its value from the map.
 *
 * @private
 * @name delete
 * @memberOf MapCache
 * @param {string} key The key of the value to remove.
 * @returns {boolean} Returns `true` if the entry was removed, else `false`.
 */
function mapCacheDelete(key) {
  var result = getMapData(this, key)['delete'](key);
  this.size -= result ? 1 : 0;
  return result;
}

/**
 * Gets the map value for `key`.
 *
 * @private
 * @name get
 * @memberOf MapCache
 * @param {string} key The key of the value to get.
 * @returns {*} Returns the entry value.
 */
function mapCacheGet(key) {
  return getMapData(this, key).get(key);
}

/**
 * Checks if a map value for `key` exists.
 *
 * @private
 * @name has
 * @memberOf MapCache
 * @param {string} key The key of the entry to check.
 * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
 */
function mapCacheHas(key) {
  return getMapData(this, key).has(key);
}

/**
 * Sets the map `key` to `value`.
 *
 * @private
 * @name set
 * @memberOf MapCache
 * @param {string} key The key of the value to set.
 * @param {*} value The value to set.
 * @returns {Object} Returns the map cache instance.
 */
function mapCacheSet(key, value) {
  var data = getMapData(this, key),
      size = data.size;

  data.set(key, value);
  this.size += data.size == size ? 0 : 1;
  return this;
}

// Add methods to `MapCache`.
MapCache.prototype.clear = mapCacheClear;
MapCache.prototype['delete'] = mapCacheDelete;
MapCache.prototype.get = mapCacheGet;
MapCache.prototype.has = mapCacheHas;
MapCache.prototype.set = mapCacheSet;

/**
 *
 * Creates an array cache object to store unique values.
 *
 * @private
 * @constructor
 * @param {Array} [values] The values to cache.
 */
function SetCache(values) {
  var index = -1,
      length = values == null ? 0 : values.length;

  this.__data__ = new MapCache;
  while (++index < length) {
    this.add(values[index]);
  }
}

/**
 * Adds `value` to the array cache.
 *
 * @private
 * @name add
 * @memberOf SetCache
 * @alias push
 * @param {*} value The value to cache.
 * @returns {Object} Returns the cache instance.
 */
function setCacheAdd(value) {
  this.__data__.set(value, HASH_UNDEFINED);
  return this;
}

/**
 * Checks if `value` is in the array cache.
 *
 * @private
 * @name has
 * @memberOf SetCache
 * @param {*} value The value to search for.
 * @returns {number} Returns `true` if `value` is found, else `false`.
 */
function setCacheHas(value) {
  return this.__data__.has(value);
}

// Add methods to `SetCache`.
SetCache.prototype.add = SetCache.prototype.push = setCacheAdd;
SetCache.prototype.has = setCacheHas;

/**
 * Creates a stack cache object to store key-value pairs.
 *
 * @private
 * @constructor
 * @param {Array} [entries] The key-value pairs to cache.
 */
function Stack(entries) {
  var data = this.__data__ = new ListCache(entries);
  this.size = data.size;
}

/**
 * Removes all key-value entries from the stack.
 *
 * @private
 * @name clear
 * @memberOf Stack
 */
function stackClear() {
  this.__data__ = new ListCache;
  this.size = 0;
}

/**
 * Removes `key` and its value from the stack.
 *
 * @private
 * @name delete
 * @memberOf Stack
 * @param {string} key The key of the value to remove.
 * @returns {boolean} Returns `true` if the entry was removed, else `false`.
 */
function stackDelete(key) {
  var data = this.__data__,
      result = data['delete'](key);

  this.size = data.size;
  return result;
}

/**
 * Gets the stack value for `key`.
 *
 * @private
 * @name get
 * @memberOf Stack
 * @param {string} key The key of the value to get.
 * @returns {*} Returns the entry value.
 */
function stackGet(key) {
  return this.__data__.get(key);
}

/**
 * Checks if a stack value for `key` exists.
 *
 * @private
 * @name has
 * @memberOf Stack
 * @param {string} key The key of the entry to check.
 * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
 */
function stackHas(key) {
  return this.__data__.has(key);
}

/**
 * Sets the stack `key` to `value`.
 *
 * @private
 * @name set
 * @memberOf Stack
 * @param {string} key The key of the value to set.
 * @param {*} value The value to set.
 * @returns {Object} Returns the stack cache instance.
 */
function stackSet(key, value) {
  var data = this.__data__;
  if (data instanceof ListCache) {
    var pairs = data.__data__;
    if (!Map || (pairs.length < LARGE_ARRAY_SIZE - 1)) {
      pairs.push([key, value]);
      this.size = ++data.size;
      return this;
    }
    data = this.__data__ = new MapCache(pairs);
  }
  data.set(key, value);
  this.size = data.size;
  return this;
}

// Add methods to `Stack`.
Stack.prototype.clear = stackClear;
Stack.prototype['delete'] = stackDelete;
Stack.prototype.get = stackGet;
Stack.prototype.has = stackHas;
Stack.prototype.set = stackSet;

/**
 * Creates an array of the enumerable property names of the array-like `value`.
 *
 * @private
 * @param {*} value The value to query.
 * @param {boolean} inherited Specify returning inherited property names.
 * @returns {Array} Returns the array of property names.
 */
function arrayLikeKeys(value, inherited) {
  var isArr = isArray(value),
      isArg = !isArr && isArguments(value),
      isBuff = !isArr && !isArg && isBuffer(value),
      isType = !isArr && !isArg && !isBuff && isTypedArray(value),
      skipIndexes = isArr || isArg || isBuff || isType,
      result = skipIndexes ? baseTimes(value.length, String) : [],
      length = result.length;

  for (var key in value) {
    if ((inherited || hasOwnProperty.call(value, key)) &&
        !(skipIndexes && (
           // Safari 9 has enumerable `arguments.length` in strict mode.
           key == 'length' ||
           // Node.js 0.10 has enumerable non-index properties on buffers.
           (isBuff && (key == 'offset' || key == 'parent')) ||
           // PhantomJS 2 has enumerable non-index properties on typed arrays.
           (isType && (key == 'buffer' || key == 'byteLength' || key == 'byteOffset')) ||
           // Skip index properties.
           isIndex(key, length)
        ))) {
      result.push(key);
    }
  }
  return result;
}

/**
 * Gets the index at which the `key` is found in `array` of key-value pairs.
 *
 * @private
 * @param {Array} array The array to inspect.
 * @param {*} key The key to search for.
 * @returns {number} Returns the index of the matched value, else `-1`.
 */
function assocIndexOf(array, key) {
  var length = array.length;
  while (length--) {
    if (eq(array[length][0], key)) {
      return length;
    }
  }
  return -1;
}

/**
 * The base implementation of `getAllKeys` and `getAllKeysIn` which uses
 * `keysFunc` and `symbolsFunc` to get the enumerable property names and
 * symbols of `object`.
 *
 * @private
 * @param {Object} object The object to query.
 * @param {Function} keysFunc The function to get the keys of `object`.
 * @param {Function} symbolsFunc The function to get the symbols of `object`.
 * @returns {Array} Returns the array of property names and symbols.
 */
function baseGetAllKeys(object, keysFunc, symbolsFunc) {
  var result = keysFunc(object);
  return isArray(object) ? result : arrayPush(result, symbolsFunc(object));
}

/**
 * The base implementation of `getTag` without fallbacks for buggy environments.
 *
 * @private
 * @param {*} value The value to query.
 * @returns {string} Returns the `toStringTag`.
 */
function baseGetTag(value) {
  if (value == null) {
    return value === undefined ? undefinedTag : nullTag;
  }
  return (symToStringTag && symToStringTag in Object(value))
    ? getRawTag(value)
    : objectToString(value);
}

/**
 * The base implementation of `_.isArguments`.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an `arguments` object,
 */
function baseIsArguments(value) {
  return isObjectLike(value) && baseGetTag(value) == argsTag;
}

/**
 * The base implementation of `_.isEqual` which supports partial comparisons
 * and tracks traversed objects.
 *
 * @private
 * @param {*} value The value to compare.
 * @param {*} other The other value to compare.
 * @param {boolean} bitmask The bitmask flags.
 *  1 - Unordered comparison
 *  2 - Partial comparison
 * @param {Function} [customizer] The function to customize comparisons.
 * @param {Object} [stack] Tracks traversed `value` and `other` objects.
 * @returns {boolean} Returns `true` if the values are equivalent, else `false`.
 */
function baseIsEqual(value, other, bitmask, customizer, stack) {
  if (value === other) {
    return true;
  }
  if (value == null || other == null || (!isObjectLike(value) && !isObjectLike(other))) {
    return value !== value && other !== other;
  }
  return baseIsEqualDeep(value, other, bitmask, customizer, baseIsEqual, stack);
}

/**
 * A specialized version of `baseIsEqual` for arrays and objects which performs
 * deep comparisons and tracks traversed objects enabling objects with circular
 * references to be compared.
 *
 * @private
 * @param {Object} object The object to compare.
 * @param {Object} other The other object to compare.
 * @param {number} bitmask The bitmask flags. See `baseIsEqual` for more details.
 * @param {Function} customizer The function to customize comparisons.
 * @param {Function} equalFunc The function to determine equivalents of values.
 * @param {Object} [stack] Tracks traversed `object` and `other` objects.
 * @returns {boolean} Returns `true` if the objects are equivalent, else `false`.
 */
function baseIsEqualDeep(object, other, bitmask, customizer, equalFunc, stack) {
  var objIsArr = isArray(object),
      othIsArr = isArray(other),
      objTag = objIsArr ? arrayTag : getTag(object),
      othTag = othIsArr ? arrayTag : getTag(other);

  objTag = objTag == argsTag ? objectTag : objTag;
  othTag = othTag == argsTag ? objectTag : othTag;

  var objIsObj = objTag == objectTag,
      othIsObj = othTag == objectTag,
      isSameTag = objTag == othTag;

  if (isSameTag && isBuffer(object)) {
    if (!isBuffer(other)) {
      return false;
    }
    objIsArr = true;
    objIsObj = false;
  }
  if (isSameTag && !objIsObj) {
    stack || (stack = new Stack);
    return (objIsArr || isTypedArray(object))
      ? equalArrays(object, other, bitmask, customizer, equalFunc, stack)
      : equalByTag(object, other, objTag, bitmask, customizer, equalFunc, stack);
  }
  if (!(bitmask & COMPARE_PARTIAL_FLAG)) {
    var objIsWrapped = objIsObj && hasOwnProperty.call(object, '__wrapped__'),
        othIsWrapped = othIsObj && hasOwnProperty.call(other, '__wrapped__');

    if (objIsWrapped || othIsWrapped) {
      var objUnwrapped = objIsWrapped ? object.value() : object,
          othUnwrapped = othIsWrapped ? other.value() : other;

      stack || (stack = new Stack);
      return equalFunc(objUnwrapped, othUnwrapped, bitmask, customizer, stack);
    }
  }
  if (!isSameTag) {
    return false;
  }
  stack || (stack = new Stack);
  return equalObjects(object, other, bitmask, customizer, equalFunc, stack);
}

/**
 * The base implementation of `_.isNative` without bad shim checks.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a native function,
 *  else `false`.
 */
function baseIsNative(value) {
  if (!isObject(value) || isMasked(value)) {
    return false;
  }
  var pattern = isFunction(value) ? reIsNative : reIsHostCtor;
  return pattern.test(toSource(value));
}

/**
 * The base implementation of `_.isTypedArray` without Node.js optimizations.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a typed array, else `false`.
 */
function baseIsTypedArray(value) {
  return isObjectLike(value) &&
    isLength(value.length) && !!typedArrayTags[baseGetTag(value)];
}

/**
 * The base implementation of `_.keys` which doesn't treat sparse arrays as dense.
 *
 * @private
 * @param {Object} object The object to query.
 * @returns {Array} Returns the array of property names.
 */
function baseKeys(object) {
  if (!isPrototype(object)) {
    return nativeKeys(object);
  }
  var result = [];
  for (var key in Object(object)) {
    if (hasOwnProperty.call(object, key) && key != 'constructor') {
      result.push(key);
    }
  }
  return result;
}

/**
 * A specialized version of `baseIsEqualDeep` for arrays with support for
 * partial deep comparisons.
 *
 * @private
 * @param {Array} array The array to compare.
 * @param {Array} other The other array to compare.
 * @param {number} bitmask The bitmask flags. See `baseIsEqual` for more details.
 * @param {Function} customizer The function to customize comparisons.
 * @param {Function} equalFunc The function to determine equivalents of values.
 * @param {Object} stack Tracks traversed `array` and `other` objects.
 * @returns {boolean} Returns `true` if the arrays are equivalent, else `false`.
 */
function equalArrays(array, other, bitmask, customizer, equalFunc, stack) {
  var isPartial = bitmask & COMPARE_PARTIAL_FLAG,
      arrLength = array.length,
      othLength = other.length;

  if (arrLength != othLength && !(isPartial && othLength > arrLength)) {
    return false;
  }
  // Assume cyclic values are equal.
  var stacked = stack.get(array);
  if (stacked && stack.get(other)) {
    return stacked == other;
  }
  var index = -1,
      result = true,
      seen = (bitmask & COMPARE_UNORDERED_FLAG) ? new SetCache : undefined;

  stack.set(array, other);
  stack.set(other, array);

  // Ignore non-index properties.
  while (++index < arrLength) {
    var arrValue = array[index],
        othValue = other[index];

    if (customizer) {
      var compared = isPartial
        ? customizer(othValue, arrValue, index, other, array, stack)
        : customizer(arrValue, othValue, index, array, other, stack);
    }
    if (compared !== undefined) {
      if (compared) {
        continue;
      }
      result = false;
      break;
    }
    // Recursively compare arrays (susceptible to call stack limits).
    if (seen) {
      if (!arraySome(other, function(othValue, othIndex) {
            if (!cacheHas(seen, othIndex) &&
                (arrValue === othValue || equalFunc(arrValue, othValue, bitmask, customizer, stack))) {
              return seen.push(othIndex);
            }
          })) {
        result = false;
        break;
      }
    } else if (!(
          arrValue === othValue ||
            equalFunc(arrValue, othValue, bitmask, customizer, stack)
        )) {
      result = false;
      break;
    }
  }
  stack['delete'](array);
  stack['delete'](other);
  return result;
}

/**
 * A specialized version of `baseIsEqualDeep` for comparing objects of
 * the same `toStringTag`.
 *
 * **Note:** This function only supports comparing values with tags of
 * `Boolean`, `Date`, `Error`, `Number`, `RegExp`, or `String`.
 *
 * @private
 * @param {Object} object The object to compare.
 * @param {Object} other The other object to compare.
 * @param {string} tag The `toStringTag` of the objects to compare.
 * @param {number} bitmask The bitmask flags. See `baseIsEqual` for more details.
 * @param {Function} customizer The function to customize comparisons.
 * @param {Function} equalFunc The function to determine equivalents of values.
 * @param {Object} stack Tracks traversed `object` and `other` objects.
 * @returns {boolean} Returns `true` if the objects are equivalent, else `false`.
 */
function equalByTag(object, other, tag, bitmask, customizer, equalFunc, stack) {
  switch (tag) {
    case dataViewTag:
      if ((object.byteLength != other.byteLength) ||
          (object.byteOffset != other.byteOffset)) {
        return false;
      }
      object = object.buffer;
      other = other.buffer;

    case arrayBufferTag:
      if ((object.byteLength != other.byteLength) ||
          !equalFunc(new Uint8Array(object), new Uint8Array(other))) {
        return false;
      }
      return true;

    case boolTag:
    case dateTag:
    case numberTag:
      // Coerce booleans to `1` or `0` and dates to milliseconds.
      // Invalid dates are coerced to `NaN`.
      return eq(+object, +other);

    case errorTag:
      return object.name == other.name && object.message == other.message;

    case regexpTag:
    case stringTag:
      // Coerce regexes to strings and treat strings, primitives and objects,
      // as equal. See http://www.ecma-international.org/ecma-262/7.0/#sec-regexp.prototype.tostring
      // for more details.
      return object == (other + '');

    case mapTag:
      var convert = mapToArray;

    case setTag:
      var isPartial = bitmask & COMPARE_PARTIAL_FLAG;
      convert || (convert = setToArray);

      if (object.size != other.size && !isPartial) {
        return false;
      }
      // Assume cyclic values are equal.
      var stacked = stack.get(object);
      if (stacked) {
        return stacked == other;
      }
      bitmask |= COMPARE_UNORDERED_FLAG;

      // Recursively compare objects (susceptible to call stack limits).
      stack.set(object, other);
      var result = equalArrays(convert(object), convert(other), bitmask, customizer, equalFunc, stack);
      stack['delete'](object);
      return result;

    case symbolTag:
      if (symbolValueOf) {
        return symbolValueOf.call(object) == symbolValueOf.call(other);
      }
  }
  return false;
}

/**
 * A specialized version of `baseIsEqualDeep` for objects with support for
 * partial deep comparisons.
 *
 * @private
 * @param {Object} object The object to compare.
 * @param {Object} other The other object to compare.
 * @param {number} bitmask The bitmask flags. See `baseIsEqual` for more details.
 * @param {Function} customizer The function to customize comparisons.
 * @param {Function} equalFunc The function to determine equivalents of values.
 * @param {Object} stack Tracks traversed `object` and `other` objects.
 * @returns {boolean} Returns `true` if the objects are equivalent, else `false`.
 */
function equalObjects(object, other, bitmask, customizer, equalFunc, stack) {
  var isPartial = bitmask & COMPARE_PARTIAL_FLAG,
      objProps = getAllKeys(object),
      objLength = objProps.length,
      othProps = getAllKeys(other),
      othLength = othProps.length;

  if (objLength != othLength && !isPartial) {
    return false;
  }
  var index = objLength;
  while (index--) {
    var key = objProps[index];
    if (!(isPartial ? key in other : hasOwnProperty.call(other, key))) {
      return false;
    }
  }
  // Assume cyclic values are equal.
  var stacked = stack.get(object);
  if (stacked && stack.get(other)) {
    return stacked == other;
  }
  var result = true;
  stack.set(object, other);
  stack.set(other, object);

  var skipCtor = isPartial;
  while (++index < objLength) {
    key = objProps[index];
    var objValue = object[key],
        othValue = other[key];

    if (customizer) {
      var compared = isPartial
        ? customizer(othValue, objValue, key, other, object, stack)
        : customizer(objValue, othValue, key, object, other, stack);
    }
    // Recursively compare objects (susceptible to call stack limits).
    if (!(compared === undefined
          ? (objValue === othValue || equalFunc(objValue, othValue, bitmask, customizer, stack))
          : compared
        )) {
      result = false;
      break;
    }
    skipCtor || (skipCtor = key == 'constructor');
  }
  if (result && !skipCtor) {
    var objCtor = object.constructor,
        othCtor = other.constructor;

    // Non `Object` object instances with different constructors are not equal.
    if (objCtor != othCtor &&
        ('constructor' in object && 'constructor' in other) &&
        !(typeof objCtor == 'function' && objCtor instanceof objCtor &&
          typeof othCtor == 'function' && othCtor instanceof othCtor)) {
      result = false;
    }
  }
  stack['delete'](object);
  stack['delete'](other);
  return result;
}

/**
 * Creates an array of own enumerable property names and symbols of `object`.
 *
 * @private
 * @param {Object} object The object to query.
 * @returns {Array} Returns the array of property names and symbols.
 */
function getAllKeys(object) {
  return baseGetAllKeys(object, keys, getSymbols);
}

/**
 * Gets the data for `map`.
 *
 * @private
 * @param {Object} map The map to query.
 * @param {string} key The reference key.
 * @returns {*} Returns the map data.
 */
function getMapData(map, key) {
  var data = map.__data__;
  return isKeyable(key)
    ? data[typeof key == 'string' ? 'string' : 'hash']
    : data.map;
}

/**
 * Gets the native function at `key` of `object`.
 *
 * @private
 * @param {Object} object The object to query.
 * @param {string} key The key of the method to get.
 * @returns {*} Returns the function if it's native, else `undefined`.
 */
function getNative(object, key) {
  var value = getValue(object, key);
  return baseIsNative(value) ? value : undefined;
}

/**
 * A specialized version of `baseGetTag` which ignores `Symbol.toStringTag` values.
 *
 * @private
 * @param {*} value The value to query.
 * @returns {string} Returns the raw `toStringTag`.
 */
function getRawTag(value) {
  var isOwn = hasOwnProperty.call(value, symToStringTag),
      tag = value[symToStringTag];

  try {
    value[symToStringTag] = undefined;
    var unmasked = true;
  } catch (e) {}

  var result = nativeObjectToString.call(value);
  if (unmasked) {
    if (isOwn) {
      value[symToStringTag] = tag;
    } else {
      delete value[symToStringTag];
    }
  }
  return result;
}

/**
 * Creates an array of the own enumerable symbols of `object`.
 *
 * @private
 * @param {Object} object The object to query.
 * @returns {Array} Returns the array of symbols.
 */
var getSymbols = !nativeGetSymbols ? stubArray : function(object) {
  if (object == null) {
    return [];
  }
  object = Object(object);
  return arrayFilter(nativeGetSymbols(object), function(symbol) {
    return propertyIsEnumerable.call(object, symbol);
  });
};

/**
 * Gets the `toStringTag` of `value`.
 *
 * @private
 * @param {*} value The value to query.
 * @returns {string} Returns the `toStringTag`.
 */
var getTag = baseGetTag;

// Fallback for data views, maps, sets, and weak maps in IE 11 and promises in Node.js < 6.
if ((DataView && getTag(new DataView(new ArrayBuffer(1))) != dataViewTag) ||
    (Map && getTag(new Map) != mapTag) ||
    (Promise && getTag(Promise.resolve()) != promiseTag) ||
    (Set && getTag(new Set) != setTag) ||
    (WeakMap && getTag(new WeakMap) != weakMapTag)) {
  getTag = function(value) {
    var result = baseGetTag(value),
        Ctor = result == objectTag ? value.constructor : undefined,
        ctorString = Ctor ? toSource(Ctor) : '';

    if (ctorString) {
      switch (ctorString) {
        case dataViewCtorString: return dataViewTag;
        case mapCtorString: return mapTag;
        case promiseCtorString: return promiseTag;
        case setCtorString: return setTag;
        case weakMapCtorString: return weakMapTag;
      }
    }
    return result;
  };
}

/**
 * Checks if `value` is a valid array-like index.
 *
 * @private
 * @param {*} value The value to check.
 * @param {number} [length=MAX_SAFE_INTEGER] The upper bounds of a valid index.
 * @returns {boolean} Returns `true` if `value` is a valid index, else `false`.
 */
function isIndex(value, length) {
  length = length == null ? MAX_SAFE_INTEGER : length;
  return !!length &&
    (typeof value == 'number' || reIsUint.test(value)) &&
    (value > -1 && value % 1 == 0 && value < length);
}

/**
 * Checks if `value` is suitable for use as unique object key.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is suitable, else `false`.
 */
function isKeyable(value) {
  var type = typeof value;
  return (type == 'string' || type == 'number' || type == 'symbol' || type == 'boolean')
    ? (value !== '__proto__')
    : (value === null);
}

/**
 * Checks if `func` has its source masked.
 *
 * @private
 * @param {Function} func The function to check.
 * @returns {boolean} Returns `true` if `func` is masked, else `false`.
 */
function isMasked(func) {
  return !!maskSrcKey && (maskSrcKey in func);
}

/**
 * Checks if `value` is likely a prototype object.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a prototype, else `false`.
 */
function isPrototype(value) {
  var Ctor = value && value.constructor,
      proto = (typeof Ctor == 'function' && Ctor.prototype) || objectProto;

  return value === proto;
}

/**
 * Converts `value` to a string using `Object.prototype.toString`.
 *
 * @private
 * @param {*} value The value to convert.
 * @returns {string} Returns the converted string.
 */
function objectToString(value) {
  return nativeObjectToString.call(value);
}

/**
 * Converts `func` to its source code.
 *
 * @private
 * @param {Function} func The function to convert.
 * @returns {string} Returns the source code.
 */
function toSource(func) {
  if (func != null) {
    try {
      return funcToString.call(func);
    } catch (e) {}
    try {
      return (func + '');
    } catch (e) {}
  }
  return '';
}

/**
 * Performs a
 * [`SameValueZero`](http://ecma-international.org/ecma-262/7.0/#sec-samevaluezero)
 * comparison between two values to determine if they are equivalent.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to compare.
 * @param {*} other The other value to compare.
 * @returns {boolean} Returns `true` if the values are equivalent, else `false`.
 * @example
 *
 * var object = { 'a': 1 };
 * var other = { 'a': 1 };
 *
 * _.eq(object, object);
 * // => true
 *
 * _.eq(object, other);
 * // => false
 *
 * _.eq('a', 'a');
 * // => true
 *
 * _.eq('a', Object('a'));
 * // => false
 *
 * _.eq(NaN, NaN);
 * // => true
 */
function eq(value, other) {
  return value === other || (value !== value && other !== other);
}

/**
 * Checks if `value` is likely an `arguments` object.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an `arguments` object,
 *  else `false`.
 * @example
 *
 * _.isArguments(function() { return arguments; }());
 * // => true
 *
 * _.isArguments([1, 2, 3]);
 * // => false
 */
var isArguments = baseIsArguments(function() { return arguments; }()) ? baseIsArguments : function(value) {
  return isObjectLike(value) && hasOwnProperty.call(value, 'callee') &&
    !propertyIsEnumerable.call(value, 'callee');
};

/**
 * Checks if `value` is classified as an `Array` object.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an array, else `false`.
 * @example
 *
 * _.isArray([1, 2, 3]);
 * // => true
 *
 * _.isArray(document.body.children);
 * // => false
 *
 * _.isArray('abc');
 * // => false
 *
 * _.isArray(_.noop);
 * // => false
 */
var isArray = Array.isArray;

/**
 * Checks if `value` is array-like. A value is considered array-like if it's
 * not a function and has a `value.length` that's an integer greater than or
 * equal to `0` and less than or equal to `Number.MAX_SAFE_INTEGER`.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is array-like, else `false`.
 * @example
 *
 * _.isArrayLike([1, 2, 3]);
 * // => true
 *
 * _.isArrayLike(document.body.children);
 * // => true
 *
 * _.isArrayLike('abc');
 * // => true
 *
 * _.isArrayLike(_.noop);
 * // => false
 */
function isArrayLike(value) {
  return value != null && isLength(value.length) && !isFunction(value);
}

/**
 * Checks if `value` is a buffer.
 *
 * @static
 * @memberOf _
 * @since 4.3.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a buffer, else `false`.
 * @example
 *
 * _.isBuffer(new Buffer(2));
 * // => true
 *
 * _.isBuffer(new Uint8Array(2));
 * // => false
 */
var isBuffer = nativeIsBuffer || stubFalse;

/**
 * Performs a deep comparison between two values to determine if they are
 * equivalent.
 *
 * **Note:** This method supports comparing arrays, array buffers, booleans,
 * date objects, error objects, maps, numbers, `Object` objects, regexes,
 * sets, strings, symbols, and typed arrays. `Object` objects are compared
 * by their own, not inherited, enumerable properties. Functions and DOM
 * nodes are compared by strict equality, i.e. `===`.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to compare.
 * @param {*} other The other value to compare.
 * @returns {boolean} Returns `true` if the values are equivalent, else `false`.
 * @example
 *
 * var object = { 'a': 1 };
 * var other = { 'a': 1 };
 *
 * _.isEqual(object, other);
 * // => true
 *
 * object === other;
 * // => false
 */
function isEqual(value, other) {
  return baseIsEqual(value, other);
}

/**
 * Checks if `value` is classified as a `Function` object.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a function, else `false`.
 * @example
 *
 * _.isFunction(_);
 * // => true
 *
 * _.isFunction(/abc/);
 * // => false
 */
function isFunction(value) {
  if (!isObject(value)) {
    return false;
  }
  // The use of `Object#toString` avoids issues with the `typeof` operator
  // in Safari 9 which returns 'object' for typed arrays and other constructors.
  var tag = baseGetTag(value);
  return tag == funcTag || tag == genTag || tag == asyncTag || tag == proxyTag;
}

/**
 * Checks if `value` is a valid array-like length.
 *
 * **Note:** This method is loosely based on
 * [`ToLength`](http://ecma-international.org/ecma-262/7.0/#sec-tolength).
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a valid length, else `false`.
 * @example
 *
 * _.isLength(3);
 * // => true
 *
 * _.isLength(Number.MIN_VALUE);
 * // => false
 *
 * _.isLength(Infinity);
 * // => false
 *
 * _.isLength('3');
 * // => false
 */
function isLength(value) {
  return typeof value == 'number' &&
    value > -1 && value % 1 == 0 && value <= MAX_SAFE_INTEGER;
}

/**
 * Checks if `value` is the
 * [language type](http://www.ecma-international.org/ecma-262/7.0/#sec-ecmascript-language-types)
 * of `Object`. (e.g. arrays, functions, objects, regexes, `new Number(0)`, and `new String('')`)
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an object, else `false`.
 * @example
 *
 * _.isObject({});
 * // => true
 *
 * _.isObject([1, 2, 3]);
 * // => true
 *
 * _.isObject(_.noop);
 * // => true
 *
 * _.isObject(null);
 * // => false
 */
function isObject(value) {
  var type = typeof value;
  return value != null && (type == 'object' || type == 'function');
}

/**
 * Checks if `value` is object-like. A value is object-like if it's not `null`
 * and has a `typeof` result of "object".
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is object-like, else `false`.
 * @example
 *
 * _.isObjectLike({});
 * // => true
 *
 * _.isObjectLike([1, 2, 3]);
 * // => true
 *
 * _.isObjectLike(_.noop);
 * // => false
 *
 * _.isObjectLike(null);
 * // => false
 */
function isObjectLike(value) {
  return value != null && typeof value == 'object';
}

/**
 * Checks if `value` is classified as a typed array.
 *
 * @static
 * @memberOf _
 * @since 3.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a typed array, else `false`.
 * @example
 *
 * _.isTypedArray(new Uint8Array);
 * // => true
 *
 * _.isTypedArray([]);
 * // => false
 */
var isTypedArray = nodeIsTypedArray ? baseUnary(nodeIsTypedArray) : baseIsTypedArray;

/**
 * Creates an array of the own enumerable property names of `object`.
 *
 * **Note:** Non-object values are coerced to objects. See the
 * [ES spec](http://ecma-international.org/ecma-262/7.0/#sec-object.keys)
 * for more details.
 *
 * @static
 * @since 0.1.0
 * @memberOf _
 * @category Object
 * @param {Object} object The object to query.
 * @returns {Array} Returns the array of property names.
 * @example
 *
 * function Foo() {
 *   this.a = 1;
 *   this.b = 2;
 * }
 *
 * Foo.prototype.c = 3;
 *
 * _.keys(new Foo);
 * // => ['a', 'b'] (iteration order is not guaranteed)
 *
 * _.keys('hi');
 * // => ['0', '1']
 */
function keys(object) {
  return isArrayLike(object) ? arrayLikeKeys(object) : baseKeys(object);
}

/**
 * This method returns a new empty array.
 *
 * @static
 * @memberOf _
 * @since 4.13.0
 * @category Util
 * @returns {Array} Returns the new empty array.
 * @example
 *
 * var arrays = _.times(2, _.stubArray);
 *
 * console.log(arrays);
 * // => [[], []]
 *
 * console.log(arrays[0] === arrays[1]);
 * // => false
 */
function stubArray() {
  return [];
}

/**
 * This method returns `false`.
 *
 * @static
 * @memberOf _
 * @since 4.13.0
 * @category Util
 * @returns {boolean} Returns `false`.
 * @example
 *
 * _.times(2, _.stubFalse);
 * // => [false, false]
 */
function stubFalse() {
  return false;
}

module.exports = isEqual;


/***/ }),

/***/ 8583:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

"use strict";
// https://github.com/tc39/proposal-promise-finally

var $export = __webpack_require__(2127);
var core = __webpack_require__(6094);
var global = __webpack_require__(7526);
var speciesConstructor = __webpack_require__(9190);
var promiseResolve = __webpack_require__(5957);

$export($export.P + $export.R, 'Promise', { 'finally': function (onFinally) {
  var C = speciesConstructor(this, core.Promise || global.Promise);
  var isFunction = typeof onFinally == 'function';
  return this.then(
    isFunction ? function (x) {
      return promiseResolve(C, onFinally()).then(function () { return x; });
    } : onFinally,
    isFunction ? function (e) {
      return promiseResolve(C, onFinally()).then(function () { throw e; });
    } : onFinally
  );
} });


/***/ }),

/***/ 8790:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var ctx = __webpack_require__(5052);
var call = __webpack_require__(7368);
var isArrayIter = __webpack_require__(1508);
var anObject = __webpack_require__(4228);
var toLength = __webpack_require__(1485);
var getIterFn = __webpack_require__(762);
var BREAK = {};
var RETURN = {};
var exports = module.exports = function (iterable, entries, fn, that, ITERATOR) {
  var iterFn = ITERATOR ? function () { return iterable; } : getIterFn(iterable);
  var f = ctx(fn, that, entries ? 2 : 1);
  var index = 0;
  var length, step, iterator, result;
  if (typeof iterFn != 'function') throw TypeError(iterable + ' is not iterable!');
  // fast case for arrays with default iterator
  if (isArrayIter(iterFn)) for (length = toLength(iterable.length); length > index; index++) {
    result = entries ? f(anObject(step = iterable[index])[0], step[1]) : f(iterable[index]);
    if (result === BREAK || result === RETURN) return result;
  } else for (iterator = iterFn.call(iterable); !(step = iterator.next()).done;) {
    result = call(iterator, f, step.value, entries);
    if (result === BREAK || result === RETURN) return result;
  }
};
exports.BREAK = BREAK;
exports.RETURN = RETURN;


/***/ }),

/***/ 8859:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var global = __webpack_require__(7526);
var hide = __webpack_require__(3341);
var has = __webpack_require__(7917);
var SRC = __webpack_require__(4415)('src');
var $toString = __webpack_require__(9461);
var TO_STRING = 'toString';
var TPL = ('' + $toString).split(TO_STRING);

(__webpack_require__(6094).inspectSource) = function (it) {
  return $toString.call(it);
};

(module.exports = function (O, key, val, safe) {
  var isFunction = typeof val == 'function';
  if (isFunction) has(val, 'name') || hide(val, 'name', key);
  if (O[key] === val) return;
  if (isFunction) has(val, SRC) || hide(val, SRC, O[key] ? '' + O[key] : TPL.join(String(key)));
  if (O === global) {
    O[key] = val;
  } else if (!safe) {
    delete O[key];
    hide(O, key, val);
  } else if (O[key]) {
    O[key] = val;
  } else {
    hide(O, key, val);
  }
// add fake Function#toString for correct work wrapped methods / constructors with methods like LoDash isNative
})(Function.prototype, TO_STRING, function toString() {
  return typeof this == 'function' && this[SRC] || $toString.call(this);
});


/***/ }),

/***/ 8931:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var ITERATOR = __webpack_require__(7574)('iterator');
var SAFE_CLOSING = false;

try {
  var riter = [7][ITERATOR]();
  riter['return'] = function () { SAFE_CLOSING = true; };
  // eslint-disable-next-line no-throw-literal
  Array.from(riter, function () { throw 2; });
} catch (e) { /* empty */ }

module.exports = function (exec, skipClosing) {
  if (!skipClosing && !SAFE_CLOSING) return false;
  var safe = false;
  try {
    var arr = [7];
    var iter = arr[ITERATOR]();
    iter.next = function () { return { done: safe = true }; };
    arr[ITERATOR] = function () { return iter; };
    exec(arr);
  } catch (e) { /* empty */ }
  return safe;
};


/***/ }),

/***/ 8978:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";

__webpack_require__(6517);
__webpack_require__(8583);
/* unused reexport */ __webpack_require__(6094).Promise["finally"];


/***/ }),

/***/ 9190:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

// 7.3.20 SpeciesConstructor(O, defaultConstructor)
var anObject = __webpack_require__(4228);
var aFunction = __webpack_require__(3387);
var SPECIES = __webpack_require__(7574)('species');
module.exports = function (O, D) {
  var C = anObject(O).constructor;
  var S;
  return C === undefined || (S = anObject(C)[SPECIES]) == undefined ? D : aFunction(S);
};


/***/ }),

/***/ 9448:
/***/ (function(module) {

module.exports = function (exec) {
  try {
    return !!exec();
  } catch (e) {
    return true;
  }
};


/***/ }),

/***/ 9461:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

module.exports = __webpack_require__(4556)('native-function-to-string', Function.toString);


/***/ })

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			id: moduleId,
/******/ 			loaded: false,
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId](module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Flag the module as loaded
/******/ 		module.loaded = true;
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/amd define */
/******/ 	!function() {
/******/ 		__webpack_require__.amdD = function () {
/******/ 			throw new Error('define cannot be used indirect');
/******/ 		};
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/amd options */
/******/ 	!function() {
/******/ 		__webpack_require__.amdO = {};
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/compat get default export */
/******/ 	!function() {
/******/ 		// getDefaultExport function for compatibility with non-harmony modules
/******/ 		__webpack_require__.n = function(module) {
/******/ 			var getter = module && module.__esModule ?
/******/ 				function() { return module['default']; } :
/******/ 				function() { return module; };
/******/ 			__webpack_require__.d(getter, { a: getter });
/******/ 			return getter;
/******/ 		};
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/define property getters */
/******/ 	!function() {
/******/ 		// define getter functions for harmony exports
/******/ 		__webpack_require__.d = function(exports, definition) {
/******/ 			for(var key in definition) {
/******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/global */
/******/ 	!function() {
/******/ 		__webpack_require__.g = (function() {
/******/ 			if (typeof globalThis === 'object') return globalThis;
/******/ 			try {
/******/ 				return this || new Function('return this')();
/******/ 			} catch (e) {
/******/ 				if (typeof window === 'object') return window;
/******/ 			}
/******/ 		})();
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	!function() {
/******/ 		__webpack_require__.o = function(obj, prop) { return Object.prototype.hasOwnProperty.call(obj, prop); }
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	!function() {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = function(exports) {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/node module decorator */
/******/ 	!function() {
/******/ 		__webpack_require__.nmd = function(module) {
/******/ 			module.paths = [];
/******/ 			if (!module.children) module.children = [];
/******/ 			return module;
/******/ 		};
/******/ 	}();
/******/ 	
/************************************************************************/
var __webpack_exports__ = {};
// This entry needs to be wrapped in an IIFE because it needs to be in strict mode.
!function() {
"use strict";

// NAMESPACE OBJECT: ./node_modules/axios/lib/platform/common/utils.js
var common_utils_namespaceObject = {};
__webpack_require__.r(common_utils_namespaceObject);
__webpack_require__.d(common_utils_namespaceObject, {
  hasBrowserEnv: function() { return hasBrowserEnv; },
  hasStandardBrowserEnv: function() { return hasStandardBrowserEnv; },
  hasStandardBrowserWebWorkerEnv: function() { return hasStandardBrowserWebWorkerEnv; },
  navigator: function() { return _navigator; },
  origin: function() { return origin; }
});

;// ./node_modules/promise-polyfill/src/finally.js
/**
 * @this {Promise}
 */
function finallyConstructor(callback) {
  var constructor = this.constructor;
  return this.then(
    function(value) {
      // @ts-ignore
      return constructor.resolve(callback()).then(function() {
        return value;
      });
    },
    function(reason) {
      // @ts-ignore
      return constructor.resolve(callback()).then(function() {
        // @ts-ignore
        return constructor.reject(reason);
      });
    }
  );
}

/* harmony default export */ var src_finally = (finallyConstructor);

;// ./node_modules/promise-polyfill/src/allSettled.js
function allSettled(arr) {
  var P = this;
  return new P(function(resolve, reject) {
    if (!(arr && typeof arr.length !== 'undefined')) {
      return reject(
        new TypeError(
          typeof arr +
            ' ' +
            arr +
            ' is not iterable(cannot read property Symbol(Symbol.iterator))'
        )
      );
    }
    var args = Array.prototype.slice.call(arr);
    if (args.length === 0) return resolve([]);
    var remaining = args.length;

    function res(i, val) {
      if (val && (typeof val === 'object' || typeof val === 'function')) {
        var then = val.then;
        if (typeof then === 'function') {
          then.call(
            val,
            function(val) {
              res(i, val);
            },
            function(e) {
              args[i] = { status: 'rejected', reason: e };
              if (--remaining === 0) {
                resolve(args);
              }
            }
          );
          return;
        }
      }
      args[i] = { status: 'fulfilled', value: val };
      if (--remaining === 0) {
        resolve(args);
      }
    }

    for (var i = 0; i < args.length; i++) {
      res(i, args[i]);
    }
  });
}

/* harmony default export */ var src_allSettled = (allSettled);

;// ./node_modules/promise-polyfill/src/any.js
/**
 * @constructor
 */
function AggregateError(errors, message) {
  (this.name = 'AggregateError'), (this.errors = errors);
  this.message = message || '';
}
AggregateError.prototype = Error.prototype;

function any(arr) {
  var P = this;
  return new P(function(resolve, reject) {
    if (!(arr && typeof arr.length !== 'undefined')) {
      return reject(new TypeError('Promise.any accepts an array'));
    }

    var args = Array.prototype.slice.call(arr);
    if (args.length === 0) return reject();

    var rejectionReasons = [];
    for (var i = 0; i < args.length; i++) {
      try {
        P.resolve(args[i])
          .then(resolve)
          .catch(function(error) {
            rejectionReasons.push(error);
            if (rejectionReasons.length === args.length) {
              reject(
                new AggregateError(
                  rejectionReasons,
                  'All promises were rejected'
                )
              );
            }
          });
      } catch (ex) {
        reject(ex);
      }
    }
  });
}

/* harmony default export */ var src_any = (any);

;// ./node_modules/promise-polyfill/src/index.js




// Store setTimeout reference so promise-polyfill will be unaffected by
// other code modifying setTimeout (like sinon.useFakeTimers())
var setTimeoutFunc = setTimeout;

function isArray(x) {
  return Boolean(x && typeof x.length !== 'undefined');
}

function noop() {}

// Polyfill for Function.prototype.bind
function bind(fn, thisArg) {
  return function() {
    fn.apply(thisArg, arguments);
  };
}

/**
 * @constructor
 * @param {Function} fn
 */
function src_Promise(fn) {
  if (!(this instanceof src_Promise))
    throw new TypeError('Promises must be constructed via new');
  if (typeof fn !== 'function') throw new TypeError('not a function');
  /** @type {!number} */
  this._state = 0;
  /** @type {!boolean} */
  this._handled = false;
  /** @type {Promise|undefined} */
  this._value = undefined;
  /** @type {!Array<!Function>} */
  this._deferreds = [];

  doResolve(fn, this);
}

function handle(self, deferred) {
  while (self._state === 3) {
    self = self._value;
  }
  if (self._state === 0) {
    self._deferreds.push(deferred);
    return;
  }
  self._handled = true;
  src_Promise._immediateFn(function() {
    var cb = self._state === 1 ? deferred.onFulfilled : deferred.onRejected;
    if (cb === null) {
      (self._state === 1 ? resolve : reject)(deferred.promise, self._value);
      return;
    }
    var ret;
    try {
      ret = cb(self._value);
    } catch (e) {
      reject(deferred.promise, e);
      return;
    }
    resolve(deferred.promise, ret);
  });
}

function resolve(self, newValue) {
  try {
    // Promise Resolution Procedure: https://github.com/promises-aplus/promises-spec#the-promise-resolution-procedure
    if (newValue === self)
      throw new TypeError('A promise cannot be resolved with itself.');
    if (
      newValue &&
      (typeof newValue === 'object' || typeof newValue === 'function')
    ) {
      var then = newValue.then;
      if (newValue instanceof src_Promise) {
        self._state = 3;
        self._value = newValue;
        finale(self);
        return;
      } else if (typeof then === 'function') {
        doResolve(bind(then, newValue), self);
        return;
      }
    }
    self._state = 1;
    self._value = newValue;
    finale(self);
  } catch (e) {
    reject(self, e);
  }
}

function reject(self, newValue) {
  self._state = 2;
  self._value = newValue;
  finale(self);
}

function finale(self) {
  if (self._state === 2 && self._deferreds.length === 0) {
    src_Promise._immediateFn(function() {
      if (!self._handled) {
        src_Promise._unhandledRejectionFn(self._value);
      }
    });
  }

  for (var i = 0, len = self._deferreds.length; i < len; i++) {
    handle(self, self._deferreds[i]);
  }
  self._deferreds = null;
}

/**
 * @constructor
 */
function Handler(onFulfilled, onRejected, promise) {
  this.onFulfilled = typeof onFulfilled === 'function' ? onFulfilled : null;
  this.onRejected = typeof onRejected === 'function' ? onRejected : null;
  this.promise = promise;
}

/**
 * Take a potentially misbehaving resolver function and make sure
 * onFulfilled and onRejected are only called once.
 *
 * Makes no guarantees about asynchrony.
 */
function doResolve(fn, self) {
  var done = false;
  try {
    fn(
      function(value) {
        if (done) return;
        done = true;
        resolve(self, value);
      },
      function(reason) {
        if (done) return;
        done = true;
        reject(self, reason);
      }
    );
  } catch (ex) {
    if (done) return;
    done = true;
    reject(self, ex);
  }
}

src_Promise.prototype['catch'] = function(onRejected) {
  return this.then(null, onRejected);
};

src_Promise.prototype.then = function(onFulfilled, onRejected) {
  // @ts-ignore
  var prom = new this.constructor(noop);

  handle(this, new Handler(onFulfilled, onRejected, prom));
  return prom;
};

src_Promise.prototype['finally'] = src_finally;

src_Promise.all = function(arr) {
  return new src_Promise(function(resolve, reject) {
    if (!isArray(arr)) {
      return reject(new TypeError('Promise.all accepts an array'));
    }

    var args = Array.prototype.slice.call(arr);
    if (args.length === 0) return resolve([]);
    var remaining = args.length;

    function res(i, val) {
      try {
        if (val && (typeof val === 'object' || typeof val === 'function')) {
          var then = val.then;
          if (typeof then === 'function') {
            then.call(
              val,
              function(val) {
                res(i, val);
              },
              reject
            );
            return;
          }
        }
        args[i] = val;
        if (--remaining === 0) {
          resolve(args);
        }
      } catch (ex) {
        reject(ex);
      }
    }

    for (var i = 0; i < args.length; i++) {
      res(i, args[i]);
    }
  });
};

src_Promise.any = src_any;

src_Promise.allSettled = src_allSettled;

src_Promise.resolve = function(value) {
  if (value && typeof value === 'object' && value.constructor === src_Promise) {
    return value;
  }

  return new src_Promise(function(resolve) {
    resolve(value);
  });
};

src_Promise.reject = function(value) {
  return new src_Promise(function(resolve, reject) {
    reject(value);
  });
};

src_Promise.race = function(arr) {
  return new src_Promise(function(resolve, reject) {
    if (!isArray(arr)) {
      return reject(new TypeError('Promise.race accepts an array'));
    }

    for (var i = 0, len = arr.length; i < len; i++) {
      src_Promise.resolve(arr[i]).then(resolve, reject);
    }
  });
};

// Use polyfill for setImmediate for performance gains
src_Promise._immediateFn =
  // @ts-ignore
  (typeof setImmediate === 'function' &&
    function(fn) {
      // @ts-ignore
      setImmediate(fn);
    }) ||
  function(fn) {
    setTimeoutFunc(fn, 0);
  };

src_Promise._unhandledRejectionFn = function _unhandledRejectionFn(err) {
  if (typeof console !== 'undefined' && console) {
    console.warn('Possible Unhandled Promise Rejection:', err); // eslint-disable-line no-console
  }
};

/* harmony default export */ var src = (src_Promise);

// EXTERNAL MODULE: ./node_modules/lodash.merge/index.js
var lodash_merge = __webpack_require__(182);
var lodash_merge_default = /*#__PURE__*/__webpack_require__.n(lodash_merge);
;// ./node_modules/axios/lib/helpers/bind.js


function bind_bind(fn, thisArg) {
  return function wrap() {
    return fn.apply(thisArg, arguments);
  };
}

;// ./node_modules/axios/lib/utils.js




// utils is a library of generic helper functions non-specific to axios

const {toString: utils_toString} = Object.prototype;
const {getPrototypeOf} = Object;

const kindOf = (cache => thing => {
    const str = utils_toString.call(thing);
    return cache[str] || (cache[str] = str.slice(8, -1).toLowerCase());
})(Object.create(null));

const kindOfTest = (type) => {
  type = type.toLowerCase();
  return (thing) => kindOf(thing) === type
}

const typeOfTest = type => thing => typeof thing === type;

/**
 * Determine if a value is an Array
 *
 * @param {Object} val The value to test
 *
 * @returns {boolean} True if value is an Array, otherwise false
 */
const {isArray: utils_isArray} = Array;

/**
 * Determine if a value is undefined
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if the value is undefined, otherwise false
 */
const isUndefined = typeOfTest('undefined');

/**
 * Determine if a value is a Buffer
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a Buffer, otherwise false
 */
function isBuffer(val) {
  return val !== null && !isUndefined(val) && val.constructor !== null && !isUndefined(val.constructor)
    && isFunction(val.constructor.isBuffer) && val.constructor.isBuffer(val);
}

/**
 * Determine if a value is an ArrayBuffer
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is an ArrayBuffer, otherwise false
 */
const isArrayBuffer = kindOfTest('ArrayBuffer');


/**
 * Determine if a value is a view on an ArrayBuffer
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a view on an ArrayBuffer, otherwise false
 */
function isArrayBufferView(val) {
  let result;
  if ((typeof ArrayBuffer !== 'undefined') && (ArrayBuffer.isView)) {
    result = ArrayBuffer.isView(val);
  } else {
    result = (val) && (val.buffer) && (isArrayBuffer(val.buffer));
  }
  return result;
}

/**
 * Determine if a value is a String
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a String, otherwise false
 */
const isString = typeOfTest('string');

/**
 * Determine if a value is a Function
 *
 * @param {*} val The value to test
 * @returns {boolean} True if value is a Function, otherwise false
 */
const isFunction = typeOfTest('function');

/**
 * Determine if a value is a Number
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a Number, otherwise false
 */
const isNumber = typeOfTest('number');

/**
 * Determine if a value is an Object
 *
 * @param {*} thing The value to test
 *
 * @returns {boolean} True if value is an Object, otherwise false
 */
const isObject = (thing) => thing !== null && typeof thing === 'object';

/**
 * Determine if a value is a Boolean
 *
 * @param {*} thing The value to test
 * @returns {boolean} True if value is a Boolean, otherwise false
 */
const isBoolean = thing => thing === true || thing === false;

/**
 * Determine if a value is a plain Object
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a plain Object, otherwise false
 */
const isPlainObject = (val) => {
  if (kindOf(val) !== 'object') {
    return false;
  }

  const prototype = getPrototypeOf(val);
  return (prototype === null || prototype === Object.prototype || Object.getPrototypeOf(prototype) === null) && !(Symbol.toStringTag in val) && !(Symbol.iterator in val);
}

/**
 * Determine if a value is a Date
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a Date, otherwise false
 */
const isDate = kindOfTest('Date');

/**
 * Determine if a value is a File
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a File, otherwise false
 */
const isFile = kindOfTest('File');

/**
 * Determine if a value is a Blob
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a Blob, otherwise false
 */
const isBlob = kindOfTest('Blob');

/**
 * Determine if a value is a FileList
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a File, otherwise false
 */
const isFileList = kindOfTest('FileList');

/**
 * Determine if a value is a Stream
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a Stream, otherwise false
 */
const isStream = (val) => isObject(val) && isFunction(val.pipe);

/**
 * Determine if a value is a FormData
 *
 * @param {*} thing The value to test
 *
 * @returns {boolean} True if value is an FormData, otherwise false
 */
const isFormData = (thing) => {
  let kind;
  return thing && (
    (typeof FormData === 'function' && thing instanceof FormData) || (
      isFunction(thing.append) && (
        (kind = kindOf(thing)) === 'formdata' ||
        // detect form-data instance
        (kind === 'object' && isFunction(thing.toString) && thing.toString() === '[object FormData]')
      )
    )
  )
}

/**
 * Determine if a value is a URLSearchParams object
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a URLSearchParams object, otherwise false
 */
const isURLSearchParams = kindOfTest('URLSearchParams');

const [isReadableStream, isRequest, isResponse, isHeaders] = ['ReadableStream', 'Request', 'Response', 'Headers'].map(kindOfTest);

/**
 * Trim excess whitespace off the beginning and end of a string
 *
 * @param {String} str The String to trim
 *
 * @returns {String} The String freed of excess whitespace
 */
const trim = (str) => str.trim ?
  str.trim() : str.replace(/^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g, '');

/**
 * Iterate over an Array or an Object invoking a function for each item.
 *
 * If `obj` is an Array callback will be called passing
 * the value, index, and complete array for each item.
 *
 * If 'obj' is an Object callback will be called passing
 * the value, key, and complete object for each property.
 *
 * @param {Object|Array} obj The object to iterate
 * @param {Function} fn The callback to invoke for each item
 *
 * @param {Boolean} [allOwnKeys = false]
 * @returns {any}
 */
function forEach(obj, fn, {allOwnKeys = false} = {}) {
  // Don't bother if no value provided
  if (obj === null || typeof obj === 'undefined') {
    return;
  }

  let i;
  let l;

  // Force an array if not already something iterable
  if (typeof obj !== 'object') {
    /*eslint no-param-reassign:0*/
    obj = [obj];
  }

  if (utils_isArray(obj)) {
    // Iterate over array values
    for (i = 0, l = obj.length; i < l; i++) {
      fn.call(null, obj[i], i, obj);
    }
  } else {
    // Iterate over object keys
    const keys = allOwnKeys ? Object.getOwnPropertyNames(obj) : Object.keys(obj);
    const len = keys.length;
    let key;

    for (i = 0; i < len; i++) {
      key = keys[i];
      fn.call(null, obj[key], key, obj);
    }
  }
}

function findKey(obj, key) {
  key = key.toLowerCase();
  const keys = Object.keys(obj);
  let i = keys.length;
  let _key;
  while (i-- > 0) {
    _key = keys[i];
    if (key === _key.toLowerCase()) {
      return _key;
    }
  }
  return null;
}

const _global = (() => {
  /*eslint no-undef:0*/
  if (typeof globalThis !== "undefined") return globalThis;
  return typeof self !== "undefined" ? self : (typeof window !== 'undefined' ? window : global)
})();

const isContextDefined = (context) => !isUndefined(context) && context !== _global;

/**
 * Accepts varargs expecting each argument to be an object, then
 * immutably merges the properties of each object and returns result.
 *
 * When multiple objects contain the same key the later object in
 * the arguments list will take precedence.
 *
 * Example:
 *
 * ```js
 * var result = merge({foo: 123}, {foo: 456});
 * console.log(result.foo); // outputs 456
 * ```
 *
 * @param {Object} obj1 Object to merge
 *
 * @returns {Object} Result of all merge properties
 */
function merge(/* obj1, obj2, obj3, ... */) {
  const {caseless} = isContextDefined(this) && this || {};
  const result = {};
  const assignValue = (val, key) => {
    const targetKey = caseless && findKey(result, key) || key;
    if (isPlainObject(result[targetKey]) && isPlainObject(val)) {
      result[targetKey] = merge(result[targetKey], val);
    } else if (isPlainObject(val)) {
      result[targetKey] = merge({}, val);
    } else if (utils_isArray(val)) {
      result[targetKey] = val.slice();
    } else {
      result[targetKey] = val;
    }
  }

  for (let i = 0, l = arguments.length; i < l; i++) {
    arguments[i] && forEach(arguments[i], assignValue);
  }
  return result;
}

/**
 * Extends object a by mutably adding to it the properties of object b.
 *
 * @param {Object} a The object to be extended
 * @param {Object} b The object to copy properties from
 * @param {Object} thisArg The object to bind function to
 *
 * @param {Boolean} [allOwnKeys]
 * @returns {Object} The resulting value of object a
 */
const extend = (a, b, thisArg, {allOwnKeys}= {}) => {
  forEach(b, (val, key) => {
    if (thisArg && isFunction(val)) {
      a[key] = bind_bind(val, thisArg);
    } else {
      a[key] = val;
    }
  }, {allOwnKeys});
  return a;
}

/**
 * Remove byte order marker. This catches EF BB BF (the UTF-8 BOM)
 *
 * @param {string} content with BOM
 *
 * @returns {string} content value without BOM
 */
const stripBOM = (content) => {
  if (content.charCodeAt(0) === 0xFEFF) {
    content = content.slice(1);
  }
  return content;
}

/**
 * Inherit the prototype methods from one constructor into another
 * @param {function} constructor
 * @param {function} superConstructor
 * @param {object} [props]
 * @param {object} [descriptors]
 *
 * @returns {void}
 */
const inherits = (constructor, superConstructor, props, descriptors) => {
  constructor.prototype = Object.create(superConstructor.prototype, descriptors);
  constructor.prototype.constructor = constructor;
  Object.defineProperty(constructor, 'super', {
    value: superConstructor.prototype
  });
  props && Object.assign(constructor.prototype, props);
}

/**
 * Resolve object with deep prototype chain to a flat object
 * @param {Object} sourceObj source object
 * @param {Object} [destObj]
 * @param {Function|Boolean} [filter]
 * @param {Function} [propFilter]
 *
 * @returns {Object}
 */
const toFlatObject = (sourceObj, destObj, filter, propFilter) => {
  let props;
  let i;
  let prop;
  const merged = {};

  destObj = destObj || {};
  // eslint-disable-next-line no-eq-null,eqeqeq
  if (sourceObj == null) return destObj;

  do {
    props = Object.getOwnPropertyNames(sourceObj);
    i = props.length;
    while (i-- > 0) {
      prop = props[i];
      if ((!propFilter || propFilter(prop, sourceObj, destObj)) && !merged[prop]) {
        destObj[prop] = sourceObj[prop];
        merged[prop] = true;
      }
    }
    sourceObj = filter !== false && getPrototypeOf(sourceObj);
  } while (sourceObj && (!filter || filter(sourceObj, destObj)) && sourceObj !== Object.prototype);

  return destObj;
}

/**
 * Determines whether a string ends with the characters of a specified string
 *
 * @param {String} str
 * @param {String} searchString
 * @param {Number} [position= 0]
 *
 * @returns {boolean}
 */
const endsWith = (str, searchString, position) => {
  str = String(str);
  if (position === undefined || position > str.length) {
    position = str.length;
  }
  position -= searchString.length;
  const lastIndex = str.indexOf(searchString, position);
  return lastIndex !== -1 && lastIndex === position;
}


/**
 * Returns new array from array like object or null if failed
 *
 * @param {*} [thing]
 *
 * @returns {?Array}
 */
const toArray = (thing) => {
  if (!thing) return null;
  if (utils_isArray(thing)) return thing;
  let i = thing.length;
  if (!isNumber(i)) return null;
  const arr = new Array(i);
  while (i-- > 0) {
    arr[i] = thing[i];
  }
  return arr;
}

/**
 * Checking if the Uint8Array exists and if it does, it returns a function that checks if the
 * thing passed in is an instance of Uint8Array
 *
 * @param {TypedArray}
 *
 * @returns {Array}
 */
// eslint-disable-next-line func-names
const isTypedArray = (TypedArray => {
  // eslint-disable-next-line func-names
  return thing => {
    return TypedArray && thing instanceof TypedArray;
  };
})(typeof Uint8Array !== 'undefined' && getPrototypeOf(Uint8Array));

/**
 * For each entry in the object, call the function with the key and value.
 *
 * @param {Object<any, any>} obj - The object to iterate over.
 * @param {Function} fn - The function to call for each entry.
 *
 * @returns {void}
 */
const forEachEntry = (obj, fn) => {
  const generator = obj && obj[Symbol.iterator];

  const iterator = generator.call(obj);

  let result;

  while ((result = iterator.next()) && !result.done) {
    const pair = result.value;
    fn.call(obj, pair[0], pair[1]);
  }
}

/**
 * It takes a regular expression and a string, and returns an array of all the matches
 *
 * @param {string} regExp - The regular expression to match against.
 * @param {string} str - The string to search.
 *
 * @returns {Array<boolean>}
 */
const matchAll = (regExp, str) => {
  let matches;
  const arr = [];

  while ((matches = regExp.exec(str)) !== null) {
    arr.push(matches);
  }

  return arr;
}

/* Checking if the kindOfTest function returns true when passed an HTMLFormElement. */
const isHTMLForm = kindOfTest('HTMLFormElement');

const toCamelCase = str => {
  return str.toLowerCase().replace(/[-_\s]([a-z\d])(\w*)/g,
    function replacer(m, p1, p2) {
      return p1.toUpperCase() + p2;
    }
  );
};

/* Creating a function that will check if an object has a property. */
const utils_hasOwnProperty = (({hasOwnProperty}) => (obj, prop) => hasOwnProperty.call(obj, prop))(Object.prototype);

/**
 * Determine if a value is a RegExp object
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a RegExp object, otherwise false
 */
const isRegExp = kindOfTest('RegExp');

const reduceDescriptors = (obj, reducer) => {
  const descriptors = Object.getOwnPropertyDescriptors(obj);
  const reducedDescriptors = {};

  forEach(descriptors, (descriptor, name) => {
    let ret;
    if ((ret = reducer(descriptor, name, obj)) !== false) {
      reducedDescriptors[name] = ret || descriptor;
    }
  });

  Object.defineProperties(obj, reducedDescriptors);
}

/**
 * Makes all methods read-only
 * @param {Object} obj
 */

const freezeMethods = (obj) => {
  reduceDescriptors(obj, (descriptor, name) => {
    // skip restricted props in strict mode
    if (isFunction(obj) && ['arguments', 'caller', 'callee'].indexOf(name) !== -1) {
      return false;
    }

    const value = obj[name];

    if (!isFunction(value)) return;

    descriptor.enumerable = false;

    if ('writable' in descriptor) {
      descriptor.writable = false;
      return;
    }

    if (!descriptor.set) {
      descriptor.set = () => {
        throw Error('Can not rewrite read-only method \'' + name + '\'');
      };
    }
  });
}

const toObjectSet = (arrayOrString, delimiter) => {
  const obj = {};

  const define = (arr) => {
    arr.forEach(value => {
      obj[value] = true;
    });
  }

  utils_isArray(arrayOrString) ? define(arrayOrString) : define(String(arrayOrString).split(delimiter));

  return obj;
}

const utils_noop = () => {}

const toFiniteNumber = (value, defaultValue) => {
  return value != null && Number.isFinite(value = +value) ? value : defaultValue;
}

/**
 * If the thing is a FormData object, return true, otherwise return false.
 *
 * @param {unknown} thing - The thing to check.
 *
 * @returns {boolean}
 */
function isSpecCompliantForm(thing) {
  return !!(thing && isFunction(thing.append) && thing[Symbol.toStringTag] === 'FormData' && thing[Symbol.iterator]);
}

const toJSONObject = (obj) => {
  const stack = new Array(10);

  const visit = (source, i) => {

    if (isObject(source)) {
      if (stack.indexOf(source) >= 0) {
        return;
      }

      if(!('toJSON' in source)) {
        stack[i] = source;
        const target = utils_isArray(source) ? [] : {};

        forEach(source, (value, key) => {
          const reducedValue = visit(value, i + 1);
          !isUndefined(reducedValue) && (target[key] = reducedValue);
        });

        stack[i] = undefined;

        return target;
      }
    }

    return source;
  }

  return visit(obj, 0);
}

const isAsyncFn = kindOfTest('AsyncFunction');

const isThenable = (thing) =>
  thing && (isObject(thing) || isFunction(thing)) && isFunction(thing.then) && isFunction(thing.catch);

// original code
// https://github.com/DigitalBrainJS/AxiosPromise/blob/16deab13710ec09779922131f3fa5954320f83ab/lib/utils.js#L11-L34

const _setImmediate = ((setImmediateSupported, postMessageSupported) => {
  if (setImmediateSupported) {
    return setImmediate;
  }

  return postMessageSupported ? ((token, callbacks) => {
    _global.addEventListener("message", ({source, data}) => {
      if (source === _global && data === token) {
        callbacks.length && callbacks.shift()();
      }
    }, false);

    return (cb) => {
      callbacks.push(cb);
      _global.postMessage(token, "*");
    }
  })(`axios@${Math.random()}`, []) : (cb) => setTimeout(cb);
})(
  typeof setImmediate === 'function',
  isFunction(_global.postMessage)
);

const asap = typeof queueMicrotask !== 'undefined' ?
  queueMicrotask.bind(_global) : ( typeof process !== 'undefined' && process.nextTick || _setImmediate);

// *********************

/* harmony default export */ var utils = ({
  isArray: utils_isArray,
  isArrayBuffer,
  isBuffer,
  isFormData,
  isArrayBufferView,
  isString,
  isNumber,
  isBoolean,
  isObject,
  isPlainObject,
  isReadableStream,
  isRequest,
  isResponse,
  isHeaders,
  isUndefined,
  isDate,
  isFile,
  isBlob,
  isRegExp,
  isFunction,
  isStream,
  isURLSearchParams,
  isTypedArray,
  isFileList,
  forEach,
  merge,
  extend,
  trim,
  stripBOM,
  inherits,
  toFlatObject,
  kindOf,
  kindOfTest,
  endsWith,
  toArray,
  forEachEntry,
  matchAll,
  isHTMLForm,
  hasOwnProperty: utils_hasOwnProperty,
  hasOwnProp: utils_hasOwnProperty, // an alias to avoid ESLint no-prototype-builtins detection
  reduceDescriptors,
  freezeMethods,
  toObjectSet,
  toCamelCase,
  noop: utils_noop,
  toFiniteNumber,
  findKey,
  global: _global,
  isContextDefined,
  isSpecCompliantForm,
  toJSONObject,
  isAsyncFn,
  isThenable,
  setImmediate: _setImmediate,
  asap
});

;// ./node_modules/axios/lib/core/AxiosError.js




/**
 * Create an Error with the specified message, config, error code, request and response.
 *
 * @param {string} message The error message.
 * @param {string} [code] The error code (for example, 'ECONNABORTED').
 * @param {Object} [config] The config.
 * @param {Object} [request] The request.
 * @param {Object} [response] The response.
 *
 * @returns {Error} The created error.
 */
function AxiosError(message, code, config, request, response) {
  Error.call(this);

  if (Error.captureStackTrace) {
    Error.captureStackTrace(this, this.constructor);
  } else {
    this.stack = (new Error()).stack;
  }

  this.message = message;
  this.name = 'AxiosError';
  code && (this.code = code);
  config && (this.config = config);
  request && (this.request = request);
  if (response) {
    this.response = response;
    this.status = response.status ? response.status : null;
  }
}

utils.inherits(AxiosError, Error, {
  toJSON: function toJSON() {
    return {
      // Standard
      message: this.message,
      name: this.name,
      // Microsoft
      description: this.description,
      number: this.number,
      // Mozilla
      fileName: this.fileName,
      lineNumber: this.lineNumber,
      columnNumber: this.columnNumber,
      stack: this.stack,
      // Axios
      config: utils.toJSONObject(this.config),
      code: this.code,
      status: this.status
    };
  }
});

const AxiosError_prototype = AxiosError.prototype;
const descriptors = {};

[
  'ERR_BAD_OPTION_VALUE',
  'ERR_BAD_OPTION',
  'ECONNABORTED',
  'ETIMEDOUT',
  'ERR_NETWORK',
  'ERR_FR_TOO_MANY_REDIRECTS',
  'ERR_DEPRECATED',
  'ERR_BAD_RESPONSE',
  'ERR_BAD_REQUEST',
  'ERR_CANCELED',
  'ERR_NOT_SUPPORT',
  'ERR_INVALID_URL'
// eslint-disable-next-line func-names
].forEach(code => {
  descriptors[code] = {value: code};
});

Object.defineProperties(AxiosError, descriptors);
Object.defineProperty(AxiosError_prototype, 'isAxiosError', {value: true});

// eslint-disable-next-line func-names
AxiosError.from = (error, code, config, request, response, customProps) => {
  const axiosError = Object.create(AxiosError_prototype);

  utils.toFlatObject(error, axiosError, function filter(obj) {
    return obj !== Error.prototype;
  }, prop => {
    return prop !== 'isAxiosError';
  });

  AxiosError.call(axiosError, error.message, code, config, request, response);

  axiosError.cause = error;

  axiosError.name = error.name;

  customProps && Object.assign(axiosError, customProps);

  return axiosError;
};

/* harmony default export */ var core_AxiosError = (AxiosError);

;// ./node_modules/axios/lib/helpers/null.js
// eslint-disable-next-line strict
/* harmony default export */ var helpers_null = (null);

;// ./node_modules/axios/lib/helpers/toFormData.js




// temporary hotfix to avoid circular references until AxiosURLSearchParams is refactored


/**
 * Determines if the given thing is a array or js object.
 *
 * @param {string} thing - The object or array to be visited.
 *
 * @returns {boolean}
 */
function isVisitable(thing) {
  return utils.isPlainObject(thing) || utils.isArray(thing);
}

/**
 * It removes the brackets from the end of a string
 *
 * @param {string} key - The key of the parameter.
 *
 * @returns {string} the key without the brackets.
 */
function removeBrackets(key) {
  return utils.endsWith(key, '[]') ? key.slice(0, -2) : key;
}

/**
 * It takes a path, a key, and a boolean, and returns a string
 *
 * @param {string} path - The path to the current key.
 * @param {string} key - The key of the current object being iterated over.
 * @param {string} dots - If true, the key will be rendered with dots instead of brackets.
 *
 * @returns {string} The path to the current key.
 */
function renderKey(path, key, dots) {
  if (!path) return key;
  return path.concat(key).map(function each(token, i) {
    // eslint-disable-next-line no-param-reassign
    token = removeBrackets(token);
    return !dots && i ? '[' + token + ']' : token;
  }).join(dots ? '.' : '');
}

/**
 * If the array is an array and none of its elements are visitable, then it's a flat array.
 *
 * @param {Array<any>} arr - The array to check
 *
 * @returns {boolean}
 */
function isFlatArray(arr) {
  return utils.isArray(arr) && !arr.some(isVisitable);
}

const predicates = utils.toFlatObject(utils, {}, null, function filter(prop) {
  return /^is[A-Z]/.test(prop);
});

/**
 * Convert a data object to FormData
 *
 * @param {Object} obj
 * @param {?Object} [formData]
 * @param {?Object} [options]
 * @param {Function} [options.visitor]
 * @param {Boolean} [options.metaTokens = true]
 * @param {Boolean} [options.dots = false]
 * @param {?Boolean} [options.indexes = false]
 *
 * @returns {Object}
 **/

/**
 * It converts an object into a FormData object
 *
 * @param {Object<any, any>} obj - The object to convert to form data.
 * @param {string} formData - The FormData object to append to.
 * @param {Object<string, any>} options
 *
 * @returns
 */
function toFormData(obj, formData, options) {
  if (!utils.isObject(obj)) {
    throw new TypeError('target must be an object');
  }

  // eslint-disable-next-line no-param-reassign
  formData = formData || new (helpers_null || FormData)();

  // eslint-disable-next-line no-param-reassign
  options = utils.toFlatObject(options, {
    metaTokens: true,
    dots: false,
    indexes: false
  }, false, function defined(option, source) {
    // eslint-disable-next-line no-eq-null,eqeqeq
    return !utils.isUndefined(source[option]);
  });

  const metaTokens = options.metaTokens;
  // eslint-disable-next-line no-use-before-define
  const visitor = options.visitor || defaultVisitor;
  const dots = options.dots;
  const indexes = options.indexes;
  const _Blob = options.Blob || typeof Blob !== 'undefined' && Blob;
  const useBlob = _Blob && utils.isSpecCompliantForm(formData);

  if (!utils.isFunction(visitor)) {
    throw new TypeError('visitor must be a function');
  }

  function convertValue(value) {
    if (value === null) return '';

    if (utils.isDate(value)) {
      return value.toISOString();
    }

    if (!useBlob && utils.isBlob(value)) {
      throw new core_AxiosError('Blob is not supported. Use a Buffer instead.');
    }

    if (utils.isArrayBuffer(value) || utils.isTypedArray(value)) {
      return useBlob && typeof Blob === 'function' ? new Blob([value]) : Buffer.from(value);
    }

    return value;
  }

  /**
   * Default visitor.
   *
   * @param {*} value
   * @param {String|Number} key
   * @param {Array<String|Number>} path
   * @this {FormData}
   *
   * @returns {boolean} return true to visit the each prop of the value recursively
   */
  function defaultVisitor(value, key, path) {
    let arr = value;

    if (value && !path && typeof value === 'object') {
      if (utils.endsWith(key, '{}')) {
        // eslint-disable-next-line no-param-reassign
        key = metaTokens ? key : key.slice(0, -2);
        // eslint-disable-next-line no-param-reassign
        value = JSON.stringify(value);
      } else if (
        (utils.isArray(value) && isFlatArray(value)) ||
        ((utils.isFileList(value) || utils.endsWith(key, '[]')) && (arr = utils.toArray(value))
        )) {
        // eslint-disable-next-line no-param-reassign
        key = removeBrackets(key);

        arr.forEach(function each(el, index) {
          !(utils.isUndefined(el) || el === null) && formData.append(
            // eslint-disable-next-line no-nested-ternary
            indexes === true ? renderKey([key], index, dots) : (indexes === null ? key : key + '[]'),
            convertValue(el)
          );
        });
        return false;
      }
    }

    if (isVisitable(value)) {
      return true;
    }

    formData.append(renderKey(path, key, dots), convertValue(value));

    return false;
  }

  const stack = [];

  const exposedHelpers = Object.assign(predicates, {
    defaultVisitor,
    convertValue,
    isVisitable
  });

  function build(value, path) {
    if (utils.isUndefined(value)) return;

    if (stack.indexOf(value) !== -1) {
      throw Error('Circular reference detected in ' + path.join('.'));
    }

    stack.push(value);

    utils.forEach(value, function each(el, key) {
      const result = !(utils.isUndefined(el) || el === null) && visitor.call(
        formData, el, utils.isString(key) ? key.trim() : key, path, exposedHelpers
      );

      if (result === true) {
        build(el, path ? path.concat(key) : [key]);
      }
    });

    stack.pop();
  }

  if (!utils.isObject(obj)) {
    throw new TypeError('data must be an object');
  }

  build(obj);

  return formData;
}

/* harmony default export */ var helpers_toFormData = (toFormData);

;// ./node_modules/axios/lib/helpers/AxiosURLSearchParams.js




/**
 * It encodes a string by replacing all characters that are not in the unreserved set with
 * their percent-encoded equivalents
 *
 * @param {string} str - The string to encode.
 *
 * @returns {string} The encoded string.
 */
function encode(str) {
  const charMap = {
    '!': '%21',
    "'": '%27',
    '(': '%28',
    ')': '%29',
    '~': '%7E',
    '%20': '+',
    '%00': '\x00'
  };
  return encodeURIComponent(str).replace(/[!'()~]|%20|%00/g, function replacer(match) {
    return charMap[match];
  });
}

/**
 * It takes a params object and converts it to a FormData object
 *
 * @param {Object<string, any>} params - The parameters to be converted to a FormData object.
 * @param {Object<string, any>} options - The options object passed to the Axios constructor.
 *
 * @returns {void}
 */
function AxiosURLSearchParams(params, options) {
  this._pairs = [];

  params && helpers_toFormData(params, this, options);
}

const AxiosURLSearchParams_prototype = AxiosURLSearchParams.prototype;

AxiosURLSearchParams_prototype.append = function append(name, value) {
  this._pairs.push([name, value]);
};

AxiosURLSearchParams_prototype.toString = function toString(encoder) {
  const _encode = encoder ? function(value) {
    return encoder.call(this, value, encode);
  } : encode;

  return this._pairs.map(function each(pair) {
    return _encode(pair[0]) + '=' + _encode(pair[1]);
  }, '').join('&');
};

/* harmony default export */ var helpers_AxiosURLSearchParams = (AxiosURLSearchParams);

;// ./node_modules/axios/lib/helpers/buildURL.js





/**
 * It replaces all instances of the characters `:`, `$`, `,`, `+`, `[`, and `]` with their
 * URI encoded counterparts
 *
 * @param {string} val The value to be encoded.
 *
 * @returns {string} The encoded value.
 */
function buildURL_encode(val) {
  return encodeURIComponent(val).
    replace(/%3A/gi, ':').
    replace(/%24/g, '$').
    replace(/%2C/gi, ',').
    replace(/%20/g, '+').
    replace(/%5B/gi, '[').
    replace(/%5D/gi, ']');
}

/**
 * Build a URL by appending params to the end
 *
 * @param {string} url The base of the url (e.g., http://www.google.com)
 * @param {object} [params] The params to be appended
 * @param {?(object|Function)} options
 *
 * @returns {string} The formatted url
 */
function buildURL(url, params, options) {
  /*eslint no-param-reassign:0*/
  if (!params) {
    return url;
  }
  
  const _encode = options && options.encode || buildURL_encode;

  if (utils.isFunction(options)) {
    options = {
      serialize: options
    };
  } 

  const serializeFn = options && options.serialize;

  let serializedParams;

  if (serializeFn) {
    serializedParams = serializeFn(params, options);
  } else {
    serializedParams = utils.isURLSearchParams(params) ?
      params.toString() :
      new helpers_AxiosURLSearchParams(params, options).toString(_encode);
  }

  if (serializedParams) {
    const hashmarkIndex = url.indexOf("#");

    if (hashmarkIndex !== -1) {
      url = url.slice(0, hashmarkIndex);
    }
    url += (url.indexOf('?') === -1 ? '?' : '&') + serializedParams;
  }

  return url;
}

;// ./node_modules/axios/lib/core/InterceptorManager.js




class InterceptorManager {
  constructor() {
    this.handlers = [];
  }

  /**
   * Add a new interceptor to the stack
   *
   * @param {Function} fulfilled The function to handle `then` for a `Promise`
   * @param {Function} rejected The function to handle `reject` for a `Promise`
   *
   * @return {Number} An ID used to remove interceptor later
   */
  use(fulfilled, rejected, options) {
    this.handlers.push({
      fulfilled,
      rejected,
      synchronous: options ? options.synchronous : false,
      runWhen: options ? options.runWhen : null
    });
    return this.handlers.length - 1;
  }

  /**
   * Remove an interceptor from the stack
   *
   * @param {Number} id The ID that was returned by `use`
   *
   * @returns {Boolean} `true` if the interceptor was removed, `false` otherwise
   */
  eject(id) {
    if (this.handlers[id]) {
      this.handlers[id] = null;
    }
  }

  /**
   * Clear all interceptors from the stack
   *
   * @returns {void}
   */
  clear() {
    if (this.handlers) {
      this.handlers = [];
    }
  }

  /**
   * Iterate over all the registered interceptors
   *
   * This method is particularly useful for skipping over any
   * interceptors that may have become `null` calling `eject`.
   *
   * @param {Function} fn The function to call for each interceptor
   *
   * @returns {void}
   */
  forEach(fn) {
    utils.forEach(this.handlers, function forEachHandler(h) {
      if (h !== null) {
        fn(h);
      }
    });
  }
}

/* harmony default export */ var core_InterceptorManager = (InterceptorManager);

;// ./node_modules/axios/lib/defaults/transitional.js


/* harmony default export */ var defaults_transitional = ({
  silentJSONParsing: true,
  forcedJSONParsing: true,
  clarifyTimeoutError: false
});

;// ./node_modules/axios/lib/platform/browser/classes/URLSearchParams.js



/* harmony default export */ var classes_URLSearchParams = (typeof URLSearchParams !== 'undefined' ? URLSearchParams : helpers_AxiosURLSearchParams);

;// ./node_modules/axios/lib/platform/browser/classes/FormData.js


/* harmony default export */ var classes_FormData = (typeof FormData !== 'undefined' ? FormData : null);

;// ./node_modules/axios/lib/platform/browser/classes/Blob.js


/* harmony default export */ var classes_Blob = (typeof Blob !== 'undefined' ? Blob : null);

;// ./node_modules/axios/lib/platform/browser/index.js




/* harmony default export */ var browser = ({
  isBrowser: true,
  classes: {
    URLSearchParams: classes_URLSearchParams,
    FormData: classes_FormData,
    Blob: classes_Blob
  },
  protocols: ['http', 'https', 'file', 'blob', 'url', 'data']
});

;// ./node_modules/axios/lib/platform/common/utils.js
const hasBrowserEnv = typeof window !== 'undefined' && typeof document !== 'undefined';

const _navigator = typeof navigator === 'object' && navigator || undefined;

/**
 * Determine if we're running in a standard browser environment
 *
 * This allows axios to run in a web worker, and react-native.
 * Both environments support XMLHttpRequest, but not fully standard globals.
 *
 * web workers:
 *  typeof window -> undefined
 *  typeof document -> undefined
 *
 * react-native:
 *  navigator.product -> 'ReactNative'
 * nativescript
 *  navigator.product -> 'NativeScript' or 'NS'
 *
 * @returns {boolean}
 */
const hasStandardBrowserEnv = hasBrowserEnv &&
  (!_navigator || ['ReactNative', 'NativeScript', 'NS'].indexOf(_navigator.product) < 0);

/**
 * Determine if we're running in a standard browser webWorker environment
 *
 * Although the `isStandardBrowserEnv` method indicates that
 * `allows axios to run in a web worker`, the WebWorker will still be
 * filtered out due to its judgment standard
 * `typeof window !== 'undefined' && typeof document !== 'undefined'`.
 * This leads to a problem when axios post `FormData` in webWorker
 */
const hasStandardBrowserWebWorkerEnv = (() => {
  return (
    typeof WorkerGlobalScope !== 'undefined' &&
    // eslint-disable-next-line no-undef
    self instanceof WorkerGlobalScope &&
    typeof self.importScripts === 'function'
  );
})();

const origin = hasBrowserEnv && window.location.href || 'http://localhost';



;// ./node_modules/axios/lib/platform/index.js



/* harmony default export */ var platform = ({
  ...common_utils_namespaceObject,
  ...browser
});

;// ./node_modules/axios/lib/helpers/toURLEncodedForm.js






function toURLEncodedForm(data, options) {
  return helpers_toFormData(data, new platform.classes.URLSearchParams(), Object.assign({
    visitor: function(value, key, path, helpers) {
      if (platform.isNode && utils.isBuffer(value)) {
        this.append(key, value.toString('base64'));
        return false;
      }

      return helpers.defaultVisitor.apply(this, arguments);
    }
  }, options));
}

;// ./node_modules/axios/lib/helpers/formDataToJSON.js




/**
 * It takes a string like `foo[x][y][z]` and returns an array like `['foo', 'x', 'y', 'z']
 *
 * @param {string} name - The name of the property to get.
 *
 * @returns An array of strings.
 */
function parsePropPath(name) {
  // foo[x][y][z]
  // foo.x.y.z
  // foo-x-y-z
  // foo x y z
  return utils.matchAll(/\w+|\[(\w*)]/g, name).map(match => {
    return match[0] === '[]' ? '' : match[1] || match[0];
  });
}

/**
 * Convert an array to an object.
 *
 * @param {Array<any>} arr - The array to convert to an object.
 *
 * @returns An object with the same keys and values as the array.
 */
function arrayToObject(arr) {
  const obj = {};
  const keys = Object.keys(arr);
  let i;
  const len = keys.length;
  let key;
  for (i = 0; i < len; i++) {
    key = keys[i];
    obj[key] = arr[key];
  }
  return obj;
}

/**
 * It takes a FormData object and returns a JavaScript object
 *
 * @param {string} formData The FormData object to convert to JSON.
 *
 * @returns {Object<string, any> | null} The converted object.
 */
function formDataToJSON(formData) {
  function buildPath(path, value, target, index) {
    let name = path[index++];

    if (name === '__proto__') return true;

    const isNumericKey = Number.isFinite(+name);
    const isLast = index >= path.length;
    name = !name && utils.isArray(target) ? target.length : name;

    if (isLast) {
      if (utils.hasOwnProp(target, name)) {
        target[name] = [target[name], value];
      } else {
        target[name] = value;
      }

      return !isNumericKey;
    }

    if (!target[name] || !utils.isObject(target[name])) {
      target[name] = [];
    }

    const result = buildPath(path, value, target[name], index);

    if (result && utils.isArray(target[name])) {
      target[name] = arrayToObject(target[name]);
    }

    return !isNumericKey;
  }

  if (utils.isFormData(formData) && utils.isFunction(formData.entries)) {
    const obj = {};

    utils.forEachEntry(formData, (name, value) => {
      buildPath(parsePropPath(name), value, obj, 0);
    });

    return obj;
  }

  return null;
}

/* harmony default export */ var helpers_formDataToJSON = (formDataToJSON);

;// ./node_modules/axios/lib/defaults/index.js










/**
 * It takes a string, tries to parse it, and if it fails, it returns the stringified version
 * of the input
 *
 * @param {any} rawValue - The value to be stringified.
 * @param {Function} parser - A function that parses a string into a JavaScript object.
 * @param {Function} encoder - A function that takes a value and returns a string.
 *
 * @returns {string} A stringified version of the rawValue.
 */
function stringifySafely(rawValue, parser, encoder) {
  if (utils.isString(rawValue)) {
    try {
      (parser || JSON.parse)(rawValue);
      return utils.trim(rawValue);
    } catch (e) {
      if (e.name !== 'SyntaxError') {
        throw e;
      }
    }
  }

  return (encoder || JSON.stringify)(rawValue);
}

const defaults = {

  transitional: defaults_transitional,

  adapter: ['xhr', 'http', 'fetch'],

  transformRequest: [function transformRequest(data, headers) {
    const contentType = headers.getContentType() || '';
    const hasJSONContentType = contentType.indexOf('application/json') > -1;
    const isObjectPayload = utils.isObject(data);

    if (isObjectPayload && utils.isHTMLForm(data)) {
      data = new FormData(data);
    }

    const isFormData = utils.isFormData(data);

    if (isFormData) {
      return hasJSONContentType ? JSON.stringify(helpers_formDataToJSON(data)) : data;
    }

    if (utils.isArrayBuffer(data) ||
      utils.isBuffer(data) ||
      utils.isStream(data) ||
      utils.isFile(data) ||
      utils.isBlob(data) ||
      utils.isReadableStream(data)
    ) {
      return data;
    }
    if (utils.isArrayBufferView(data)) {
      return data.buffer;
    }
    if (utils.isURLSearchParams(data)) {
      headers.setContentType('application/x-www-form-urlencoded;charset=utf-8', false);
      return data.toString();
    }

    let isFileList;

    if (isObjectPayload) {
      if (contentType.indexOf('application/x-www-form-urlencoded') > -1) {
        return toURLEncodedForm(data, this.formSerializer).toString();
      }

      if ((isFileList = utils.isFileList(data)) || contentType.indexOf('multipart/form-data') > -1) {
        const _FormData = this.env && this.env.FormData;

        return helpers_toFormData(
          isFileList ? {'files[]': data} : data,
          _FormData && new _FormData(),
          this.formSerializer
        );
      }
    }

    if (isObjectPayload || hasJSONContentType ) {
      headers.setContentType('application/json', false);
      return stringifySafely(data);
    }

    return data;
  }],

  transformResponse: [function transformResponse(data) {
    const transitional = this.transitional || defaults.transitional;
    const forcedJSONParsing = transitional && transitional.forcedJSONParsing;
    const JSONRequested = this.responseType === 'json';

    if (utils.isResponse(data) || utils.isReadableStream(data)) {
      return data;
    }

    if (data && utils.isString(data) && ((forcedJSONParsing && !this.responseType) || JSONRequested)) {
      const silentJSONParsing = transitional && transitional.silentJSONParsing;
      const strictJSONParsing = !silentJSONParsing && JSONRequested;

      try {
        return JSON.parse(data);
      } catch (e) {
        if (strictJSONParsing) {
          if (e.name === 'SyntaxError') {
            throw core_AxiosError.from(e, core_AxiosError.ERR_BAD_RESPONSE, this, null, this.response);
          }
          throw e;
        }
      }
    }

    return data;
  }],

  /**
   * A timeout in milliseconds to abort a request. If set to 0 (default) a
   * timeout is not created.
   */
  timeout: 0,

  xsrfCookieName: 'XSRF-TOKEN',
  xsrfHeaderName: 'X-XSRF-TOKEN',

  maxContentLength: -1,
  maxBodyLength: -1,

  env: {
    FormData: platform.classes.FormData,
    Blob: platform.classes.Blob
  },

  validateStatus: function validateStatus(status) {
    return status >= 200 && status < 300;
  },

  headers: {
    common: {
      'Accept': 'application/json, text/plain, */*',
      'Content-Type': undefined
    }
  }
};

utils.forEach(['delete', 'get', 'head', 'post', 'put', 'patch'], (method) => {
  defaults.headers[method] = {};
});

/* harmony default export */ var lib_defaults = (defaults);

;// ./node_modules/axios/lib/helpers/parseHeaders.js




// RawAxiosHeaders whose duplicates are ignored by node
// c.f. https://nodejs.org/api/http.html#http_message_headers
const ignoreDuplicateOf = utils.toObjectSet([
  'age', 'authorization', 'content-length', 'content-type', 'etag',
  'expires', 'from', 'host', 'if-modified-since', 'if-unmodified-since',
  'last-modified', 'location', 'max-forwards', 'proxy-authorization',
  'referer', 'retry-after', 'user-agent'
]);

/**
 * Parse headers into an object
 *
 * ```
 * Date: Wed, 27 Aug 2014 08:58:49 GMT
 * Content-Type: application/json
 * Connection: keep-alive
 * Transfer-Encoding: chunked
 * ```
 *
 * @param {String} rawHeaders Headers needing to be parsed
 *
 * @returns {Object} Headers parsed into an object
 */
/* harmony default export */ var parseHeaders = (rawHeaders => {
  const parsed = {};
  let key;
  let val;
  let i;

  rawHeaders && rawHeaders.split('\n').forEach(function parser(line) {
    i = line.indexOf(':');
    key = line.substring(0, i).trim().toLowerCase();
    val = line.substring(i + 1).trim();

    if (!key || (parsed[key] && ignoreDuplicateOf[key])) {
      return;
    }

    if (key === 'set-cookie') {
      if (parsed[key]) {
        parsed[key].push(val);
      } else {
        parsed[key] = [val];
      }
    } else {
      parsed[key] = parsed[key] ? parsed[key] + ', ' + val : val;
    }
  });

  return parsed;
});

;// ./node_modules/axios/lib/core/AxiosHeaders.js





const $internals = Symbol('internals');

function normalizeHeader(header) {
  return header && String(header).trim().toLowerCase();
}

function normalizeValue(value) {
  if (value === false || value == null) {
    return value;
  }

  return utils.isArray(value) ? value.map(normalizeValue) : String(value);
}

function parseTokens(str) {
  const tokens = Object.create(null);
  const tokensRE = /([^\s,;=]+)\s*(?:=\s*([^,;]+))?/g;
  let match;

  while ((match = tokensRE.exec(str))) {
    tokens[match[1]] = match[2];
  }

  return tokens;
}

const isValidHeaderName = (str) => /^[-_a-zA-Z0-9^`|~,!#$%&'*+.]+$/.test(str.trim());

function matchHeaderValue(context, value, header, filter, isHeaderNameFilter) {
  if (utils.isFunction(filter)) {
    return filter.call(this, value, header);
  }

  if (isHeaderNameFilter) {
    value = header;
  }

  if (!utils.isString(value)) return;

  if (utils.isString(filter)) {
    return value.indexOf(filter) !== -1;
  }

  if (utils.isRegExp(filter)) {
    return filter.test(value);
  }
}

function formatHeader(header) {
  return header.trim()
    .toLowerCase().replace(/([a-z\d])(\w*)/g, (w, char, str) => {
      return char.toUpperCase() + str;
    });
}

function buildAccessors(obj, header) {
  const accessorName = utils.toCamelCase(' ' + header);

  ['get', 'set', 'has'].forEach(methodName => {
    Object.defineProperty(obj, methodName + accessorName, {
      value: function(arg1, arg2, arg3) {
        return this[methodName].call(this, header, arg1, arg2, arg3);
      },
      configurable: true
    });
  });
}

class AxiosHeaders {
  constructor(headers) {
    headers && this.set(headers);
  }

  set(header, valueOrRewrite, rewrite) {
    const self = this;

    function setHeader(_value, _header, _rewrite) {
      const lHeader = normalizeHeader(_header);

      if (!lHeader) {
        throw new Error('header name must be a non-empty string');
      }

      const key = utils.findKey(self, lHeader);

      if(!key || self[key] === undefined || _rewrite === true || (_rewrite === undefined && self[key] !== false)) {
        self[key || _header] = normalizeValue(_value);
      }
    }

    const setHeaders = (headers, _rewrite) =>
      utils.forEach(headers, (_value, _header) => setHeader(_value, _header, _rewrite));

    if (utils.isPlainObject(header) || header instanceof this.constructor) {
      setHeaders(header, valueOrRewrite)
    } else if(utils.isString(header) && (header = header.trim()) && !isValidHeaderName(header)) {
      setHeaders(parseHeaders(header), valueOrRewrite);
    } else if (utils.isHeaders(header)) {
      for (const [key, value] of header.entries()) {
        setHeader(value, key, rewrite);
      }
    } else {
      header != null && setHeader(valueOrRewrite, header, rewrite);
    }

    return this;
  }

  get(header, parser) {
    header = normalizeHeader(header);

    if (header) {
      const key = utils.findKey(this, header);

      if (key) {
        const value = this[key];

        if (!parser) {
          return value;
        }

        if (parser === true) {
          return parseTokens(value);
        }

        if (utils.isFunction(parser)) {
          return parser.call(this, value, key);
        }

        if (utils.isRegExp(parser)) {
          return parser.exec(value);
        }

        throw new TypeError('parser must be boolean|regexp|function');
      }
    }
  }

  has(header, matcher) {
    header = normalizeHeader(header);

    if (header) {
      const key = utils.findKey(this, header);

      return !!(key && this[key] !== undefined && (!matcher || matchHeaderValue(this, this[key], key, matcher)));
    }

    return false;
  }

  delete(header, matcher) {
    const self = this;
    let deleted = false;

    function deleteHeader(_header) {
      _header = normalizeHeader(_header);

      if (_header) {
        const key = utils.findKey(self, _header);

        if (key && (!matcher || matchHeaderValue(self, self[key], key, matcher))) {
          delete self[key];

          deleted = true;
        }
      }
    }

    if (utils.isArray(header)) {
      header.forEach(deleteHeader);
    } else {
      deleteHeader(header);
    }

    return deleted;
  }

  clear(matcher) {
    const keys = Object.keys(this);
    let i = keys.length;
    let deleted = false;

    while (i--) {
      const key = keys[i];
      if(!matcher || matchHeaderValue(this, this[key], key, matcher, true)) {
        delete this[key];
        deleted = true;
      }
    }

    return deleted;
  }

  normalize(format) {
    const self = this;
    const headers = {};

    utils.forEach(this, (value, header) => {
      const key = utils.findKey(headers, header);

      if (key) {
        self[key] = normalizeValue(value);
        delete self[header];
        return;
      }

      const normalized = format ? formatHeader(header) : String(header).trim();

      if (normalized !== header) {
        delete self[header];
      }

      self[normalized] = normalizeValue(value);

      headers[normalized] = true;
    });

    return this;
  }

  concat(...targets) {
    return this.constructor.concat(this, ...targets);
  }

  toJSON(asStrings) {
    const obj = Object.create(null);

    utils.forEach(this, (value, header) => {
      value != null && value !== false && (obj[header] = asStrings && utils.isArray(value) ? value.join(', ') : value);
    });

    return obj;
  }

  [Symbol.iterator]() {
    return Object.entries(this.toJSON())[Symbol.iterator]();
  }

  toString() {
    return Object.entries(this.toJSON()).map(([header, value]) => header + ': ' + value).join('\n');
  }

  get [Symbol.toStringTag]() {
    return 'AxiosHeaders';
  }

  static from(thing) {
    return thing instanceof this ? thing : new this(thing);
  }

  static concat(first, ...targets) {
    const computed = new this(first);

    targets.forEach((target) => computed.set(target));

    return computed;
  }

  static accessor(header) {
    const internals = this[$internals] = (this[$internals] = {
      accessors: {}
    });

    const accessors = internals.accessors;
    const prototype = this.prototype;

    function defineAccessor(_header) {
      const lHeader = normalizeHeader(_header);

      if (!accessors[lHeader]) {
        buildAccessors(prototype, _header);
        accessors[lHeader] = true;
      }
    }

    utils.isArray(header) ? header.forEach(defineAccessor) : defineAccessor(header);

    return this;
  }
}

AxiosHeaders.accessor(['Content-Type', 'Content-Length', 'Accept', 'Accept-Encoding', 'User-Agent', 'Authorization']);

// reserved names hotfix
utils.reduceDescriptors(AxiosHeaders.prototype, ({value}, key) => {
  let mapped = key[0].toUpperCase() + key.slice(1); // map `set` => `Set`
  return {
    get: () => value,
    set(headerValue) {
      this[mapped] = headerValue;
    }
  }
});

utils.freezeMethods(AxiosHeaders);

/* harmony default export */ var core_AxiosHeaders = (AxiosHeaders);

;// ./node_modules/axios/lib/core/transformData.js






/**
 * Transform the data for a request or a response
 *
 * @param {Array|Function} fns A single function or Array of functions
 * @param {?Object} response The response object
 *
 * @returns {*} The resulting transformed data
 */
function transformData(fns, response) {
  const config = this || lib_defaults;
  const context = response || config;
  const headers = core_AxiosHeaders.from(context.headers);
  let data = context.data;

  utils.forEach(fns, function transform(fn) {
    data = fn.call(config, data, headers.normalize(), response ? response.status : undefined);
  });

  headers.normalize();

  return data;
}

;// ./node_modules/axios/lib/cancel/isCancel.js


function isCancel(value) {
  return !!(value && value.__CANCEL__);
}

;// ./node_modules/axios/lib/cancel/CanceledError.js





/**
 * A `CanceledError` is an object that is thrown when an operation is canceled.
 *
 * @param {string=} message The message.
 * @param {Object=} config The config.
 * @param {Object=} request The request.
 *
 * @returns {CanceledError} The created error.
 */
function CanceledError(message, config, request) {
  // eslint-disable-next-line no-eq-null,eqeqeq
  core_AxiosError.call(this, message == null ? 'canceled' : message, core_AxiosError.ERR_CANCELED, config, request);
  this.name = 'CanceledError';
}

utils.inherits(CanceledError, core_AxiosError, {
  __CANCEL__: true
});

/* harmony default export */ var cancel_CanceledError = (CanceledError);

;// ./node_modules/axios/lib/core/settle.js




/**
 * Resolve or reject a Promise based on response status.
 *
 * @param {Function} resolve A function that resolves the promise.
 * @param {Function} reject A function that rejects the promise.
 * @param {object} response The response.
 *
 * @returns {object} The response.
 */
function settle(resolve, reject, response) {
  const validateStatus = response.config.validateStatus;
  if (!response.status || !validateStatus || validateStatus(response.status)) {
    resolve(response);
  } else {
    reject(new core_AxiosError(
      'Request failed with status code ' + response.status,
      [core_AxiosError.ERR_BAD_REQUEST, core_AxiosError.ERR_BAD_RESPONSE][Math.floor(response.status / 100) - 4],
      response.config,
      response.request,
      response
    ));
  }
}

;// ./node_modules/axios/lib/helpers/parseProtocol.js


function parseProtocol(url) {
  const match = /^([-+\w]{1,25})(:?\/\/|:)/.exec(url);
  return match && match[1] || '';
}

;// ./node_modules/axios/lib/helpers/speedometer.js


/**
 * Calculate data maxRate
 * @param {Number} [samplesCount= 10]
 * @param {Number} [min= 1000]
 * @returns {Function}
 */
function speedometer(samplesCount, min) {
  samplesCount = samplesCount || 10;
  const bytes = new Array(samplesCount);
  const timestamps = new Array(samplesCount);
  let head = 0;
  let tail = 0;
  let firstSampleTS;

  min = min !== undefined ? min : 1000;

  return function push(chunkLength) {
    const now = Date.now();

    const startedAt = timestamps[tail];

    if (!firstSampleTS) {
      firstSampleTS = now;
    }

    bytes[head] = chunkLength;
    timestamps[head] = now;

    let i = tail;
    let bytesCount = 0;

    while (i !== head) {
      bytesCount += bytes[i++];
      i = i % samplesCount;
    }

    head = (head + 1) % samplesCount;

    if (head === tail) {
      tail = (tail + 1) % samplesCount;
    }

    if (now - firstSampleTS < min) {
      return;
    }

    const passed = startedAt && now - startedAt;

    return passed ? Math.round(bytesCount * 1000 / passed) : undefined;
  };
}

/* harmony default export */ var helpers_speedometer = (speedometer);

;// ./node_modules/axios/lib/helpers/throttle.js
/**
 * Throttle decorator
 * @param {Function} fn
 * @param {Number} freq
 * @return {Function}
 */
function throttle(fn, freq) {
  let timestamp = 0;
  let threshold = 1000 / freq;
  let lastArgs;
  let timer;

  const invoke = (args, now = Date.now()) => {
    timestamp = now;
    lastArgs = null;
    if (timer) {
      clearTimeout(timer);
      timer = null;
    }
    fn.apply(null, args);
  }

  const throttled = (...args) => {
    const now = Date.now();
    const passed = now - timestamp;
    if ( passed >= threshold) {
      invoke(args, now);
    } else {
      lastArgs = args;
      if (!timer) {
        timer = setTimeout(() => {
          timer = null;
          invoke(lastArgs)
        }, threshold - passed);
      }
    }
  }

  const flush = () => lastArgs && invoke(lastArgs);

  return [throttled, flush];
}

/* harmony default export */ var helpers_throttle = (throttle);

;// ./node_modules/axios/lib/helpers/progressEventReducer.js




const progressEventReducer = (listener, isDownloadStream, freq = 3) => {
  let bytesNotified = 0;
  const _speedometer = helpers_speedometer(50, 250);

  return helpers_throttle(e => {
    const loaded = e.loaded;
    const total = e.lengthComputable ? e.total : undefined;
    const progressBytes = loaded - bytesNotified;
    const rate = _speedometer(progressBytes);
    const inRange = loaded <= total;

    bytesNotified = loaded;

    const data = {
      loaded,
      total,
      progress: total ? (loaded / total) : undefined,
      bytes: progressBytes,
      rate: rate ? rate : undefined,
      estimated: rate && total && inRange ? (total - loaded) / rate : undefined,
      event: e,
      lengthComputable: total != null,
      [isDownloadStream ? 'download' : 'upload']: true
    };

    listener(data);
  }, freq);
}

const progressEventDecorator = (total, throttled) => {
  const lengthComputable = total != null;

  return [(loaded) => throttled[0]({
    lengthComputable,
    total,
    loaded
  }), throttled[1]];
}

const asyncDecorator = (fn) => (...args) => utils.asap(() => fn(...args));

;// ./node_modules/axios/lib/helpers/isURLSameOrigin.js


/* harmony default export */ var isURLSameOrigin = (platform.hasStandardBrowserEnv ? ((origin, isMSIE) => (url) => {
  url = new URL(url, platform.origin);

  return (
    origin.protocol === url.protocol &&
    origin.host === url.host &&
    (isMSIE || origin.port === url.port)
  );
})(
  new URL(platform.origin),
  platform.navigator && /(msie|trident)/i.test(platform.navigator.userAgent)
) : () => true);

;// ./node_modules/axios/lib/helpers/cookies.js



/* harmony default export */ var cookies = (platform.hasStandardBrowserEnv ?

  // Standard browser envs support document.cookie
  {
    write(name, value, expires, path, domain, secure) {
      const cookie = [name + '=' + encodeURIComponent(value)];

      utils.isNumber(expires) && cookie.push('expires=' + new Date(expires).toGMTString());

      utils.isString(path) && cookie.push('path=' + path);

      utils.isString(domain) && cookie.push('domain=' + domain);

      secure === true && cookie.push('secure');

      document.cookie = cookie.join('; ');
    },

    read(name) {
      const match = document.cookie.match(new RegExp('(^|;\\s*)(' + name + ')=([^;]*)'));
      return (match ? decodeURIComponent(match[3]) : null);
    },

    remove(name) {
      this.write(name, '', Date.now() - 86400000);
    }
  }

  :

  // Non-standard browser env (web workers, react-native) lack needed support.
  {
    write() {},
    read() {
      return null;
    },
    remove() {}
  });


;// ./node_modules/axios/lib/helpers/isAbsoluteURL.js


/**
 * Determines whether the specified URL is absolute
 *
 * @param {string} url The URL to test
 *
 * @returns {boolean} True if the specified URL is absolute, otherwise false
 */
function isAbsoluteURL(url) {
  // A URL is considered absolute if it begins with "<scheme>://" or "//" (protocol-relative URL).
  // RFC 3986 defines scheme name as a sequence of characters beginning with a letter and followed
  // by any combination of letters, digits, plus, period, or hyphen.
  return /^([a-z][a-z\d+\-.]*:)?\/\//i.test(url);
}

;// ./node_modules/axios/lib/helpers/combineURLs.js


/**
 * Creates a new URL by combining the specified URLs
 *
 * @param {string} baseURL The base URL
 * @param {string} relativeURL The relative URL
 *
 * @returns {string} The combined URL
 */
function combineURLs(baseURL, relativeURL) {
  return relativeURL
    ? baseURL.replace(/\/?\/$/, '') + '/' + relativeURL.replace(/^\/+/, '')
    : baseURL;
}

;// ./node_modules/axios/lib/core/buildFullPath.js





/**
 * Creates a new URL by combining the baseURL with the requestedURL,
 * only when the requestedURL is not already an absolute URL.
 * If the requestURL is absolute, this function returns the requestedURL untouched.
 *
 * @param {string} baseURL The base URL
 * @param {string} requestedURL Absolute or relative URL to combine
 *
 * @returns {string} The combined full path
 */
function buildFullPath(baseURL, requestedURL, allowAbsoluteUrls) {
  let isRelativeUrl = !isAbsoluteURL(requestedURL);
  if (baseURL && isRelativeUrl || allowAbsoluteUrls == false) {
    return combineURLs(baseURL, requestedURL);
  }
  return requestedURL;
}

;// ./node_modules/axios/lib/core/mergeConfig.js





const headersToObject = (thing) => thing instanceof core_AxiosHeaders ? { ...thing } : thing;

/**
 * Config-specific merge-function which creates a new config-object
 * by merging two configuration objects together.
 *
 * @param {Object} config1
 * @param {Object} config2
 *
 * @returns {Object} New object resulting from merging config2 to config1
 */
function mergeConfig(config1, config2) {
  // eslint-disable-next-line no-param-reassign
  config2 = config2 || {};
  const config = {};

  function getMergedValue(target, source, prop, caseless) {
    if (utils.isPlainObject(target) && utils.isPlainObject(source)) {
      return utils.merge.call({caseless}, target, source);
    } else if (utils.isPlainObject(source)) {
      return utils.merge({}, source);
    } else if (utils.isArray(source)) {
      return source.slice();
    }
    return source;
  }

  // eslint-disable-next-line consistent-return
  function mergeDeepProperties(a, b, prop , caseless) {
    if (!utils.isUndefined(b)) {
      return getMergedValue(a, b, prop , caseless);
    } else if (!utils.isUndefined(a)) {
      return getMergedValue(undefined, a, prop , caseless);
    }
  }

  // eslint-disable-next-line consistent-return
  function valueFromConfig2(a, b) {
    if (!utils.isUndefined(b)) {
      return getMergedValue(undefined, b);
    }
  }

  // eslint-disable-next-line consistent-return
  function defaultToConfig2(a, b) {
    if (!utils.isUndefined(b)) {
      return getMergedValue(undefined, b);
    } else if (!utils.isUndefined(a)) {
      return getMergedValue(undefined, a);
    }
  }

  // eslint-disable-next-line consistent-return
  function mergeDirectKeys(a, b, prop) {
    if (prop in config2) {
      return getMergedValue(a, b);
    } else if (prop in config1) {
      return getMergedValue(undefined, a);
    }
  }

  const mergeMap = {
    url: valueFromConfig2,
    method: valueFromConfig2,
    data: valueFromConfig2,
    baseURL: defaultToConfig2,
    transformRequest: defaultToConfig2,
    transformResponse: defaultToConfig2,
    paramsSerializer: defaultToConfig2,
    timeout: defaultToConfig2,
    timeoutMessage: defaultToConfig2,
    withCredentials: defaultToConfig2,
    withXSRFToken: defaultToConfig2,
    adapter: defaultToConfig2,
    responseType: defaultToConfig2,
    xsrfCookieName: defaultToConfig2,
    xsrfHeaderName: defaultToConfig2,
    onUploadProgress: defaultToConfig2,
    onDownloadProgress: defaultToConfig2,
    decompress: defaultToConfig2,
    maxContentLength: defaultToConfig2,
    maxBodyLength: defaultToConfig2,
    beforeRedirect: defaultToConfig2,
    transport: defaultToConfig2,
    httpAgent: defaultToConfig2,
    httpsAgent: defaultToConfig2,
    cancelToken: defaultToConfig2,
    socketPath: defaultToConfig2,
    responseEncoding: defaultToConfig2,
    validateStatus: mergeDirectKeys,
    headers: (a, b , prop) => mergeDeepProperties(headersToObject(a), headersToObject(b),prop, true)
  };

  utils.forEach(Object.keys(Object.assign({}, config1, config2)), function computeConfigValue(prop) {
    const merge = mergeMap[prop] || mergeDeepProperties;
    const configValue = merge(config1[prop], config2[prop], prop);
    (utils.isUndefined(configValue) && merge !== mergeDirectKeys) || (config[prop] = configValue);
  });

  return config;
}

;// ./node_modules/axios/lib/helpers/resolveConfig.js









/* harmony default export */ var resolveConfig = ((config) => {
  const newConfig = mergeConfig({}, config);

  let {data, withXSRFToken, xsrfHeaderName, xsrfCookieName, headers, auth} = newConfig;

  newConfig.headers = headers = core_AxiosHeaders.from(headers);

  newConfig.url = buildURL(buildFullPath(newConfig.baseURL, newConfig.url), config.params, config.paramsSerializer);

  // HTTP basic authentication
  if (auth) {
    headers.set('Authorization', 'Basic ' +
      btoa((auth.username || '') + ':' + (auth.password ? unescape(encodeURIComponent(auth.password)) : ''))
    );
  }

  let contentType;

  if (utils.isFormData(data)) {
    if (platform.hasStandardBrowserEnv || platform.hasStandardBrowserWebWorkerEnv) {
      headers.setContentType(undefined); // Let the browser set it
    } else if ((contentType = headers.getContentType()) !== false) {
      // fix semicolon duplication issue for ReactNative FormData implementation
      const [type, ...tokens] = contentType ? contentType.split(';').map(token => token.trim()).filter(Boolean) : [];
      headers.setContentType([type || 'multipart/form-data', ...tokens].join('; '));
    }
  }

  // Add xsrf header
  // This is only done if running in a standard browser environment.
  // Specifically not if we're in a web worker, or react-native.

  if (platform.hasStandardBrowserEnv) {
    withXSRFToken && utils.isFunction(withXSRFToken) && (withXSRFToken = withXSRFToken(newConfig));

    if (withXSRFToken || (withXSRFToken !== false && isURLSameOrigin(newConfig.url))) {
      // Add xsrf header
      const xsrfValue = xsrfHeaderName && xsrfCookieName && cookies.read(xsrfCookieName);

      if (xsrfValue) {
        headers.set(xsrfHeaderName, xsrfValue);
      }
    }
  }

  return newConfig;
});


;// ./node_modules/axios/lib/adapters/xhr.js











const isXHRAdapterSupported = typeof XMLHttpRequest !== 'undefined';

/* harmony default export */ var xhr = (isXHRAdapterSupported && function (config) {
  return new Promise(function dispatchXhrRequest(resolve, reject) {
    const _config = resolveConfig(config);
    let requestData = _config.data;
    const requestHeaders = core_AxiosHeaders.from(_config.headers).normalize();
    let {responseType, onUploadProgress, onDownloadProgress} = _config;
    let onCanceled;
    let uploadThrottled, downloadThrottled;
    let flushUpload, flushDownload;

    function done() {
      flushUpload && flushUpload(); // flush events
      flushDownload && flushDownload(); // flush events

      _config.cancelToken && _config.cancelToken.unsubscribe(onCanceled);

      _config.signal && _config.signal.removeEventListener('abort', onCanceled);
    }

    let request = new XMLHttpRequest();

    request.open(_config.method.toUpperCase(), _config.url, true);

    // Set the request timeout in MS
    request.timeout = _config.timeout;

    function onloadend() {
      if (!request) {
        return;
      }
      // Prepare the response
      const responseHeaders = core_AxiosHeaders.from(
        'getAllResponseHeaders' in request && request.getAllResponseHeaders()
      );
      const responseData = !responseType || responseType === 'text' || responseType === 'json' ?
        request.responseText : request.response;
      const response = {
        data: responseData,
        status: request.status,
        statusText: request.statusText,
        headers: responseHeaders,
        config,
        request
      };

      settle(function _resolve(value) {
        resolve(value);
        done();
      }, function _reject(err) {
        reject(err);
        done();
      }, response);

      // Clean up request
      request = null;
    }

    if ('onloadend' in request) {
      // Use onloadend if available
      request.onloadend = onloadend;
    } else {
      // Listen for ready state to emulate onloadend
      request.onreadystatechange = function handleLoad() {
        if (!request || request.readyState !== 4) {
          return;
        }

        // The request errored out and we didn't get a response, this will be
        // handled by onerror instead
        // With one exception: request that using file: protocol, most browsers
        // will return status as 0 even though it's a successful request
        if (request.status === 0 && !(request.responseURL && request.responseURL.indexOf('file:') === 0)) {
          return;
        }
        // readystate handler is calling before onerror or ontimeout handlers,
        // so we should call onloadend on the next 'tick'
        setTimeout(onloadend);
      };
    }

    // Handle browser request cancellation (as opposed to a manual cancellation)
    request.onabort = function handleAbort() {
      if (!request) {
        return;
      }

      reject(new core_AxiosError('Request aborted', core_AxiosError.ECONNABORTED, config, request));

      // Clean up request
      request = null;
    };

    // Handle low level network errors
    request.onerror = function handleError() {
      // Real errors are hidden from us by the browser
      // onerror should only fire if it's a network error
      reject(new core_AxiosError('Network Error', core_AxiosError.ERR_NETWORK, config, request));

      // Clean up request
      request = null;
    };

    // Handle timeout
    request.ontimeout = function handleTimeout() {
      let timeoutErrorMessage = _config.timeout ? 'timeout of ' + _config.timeout + 'ms exceeded' : 'timeout exceeded';
      const transitional = _config.transitional || defaults_transitional;
      if (_config.timeoutErrorMessage) {
        timeoutErrorMessage = _config.timeoutErrorMessage;
      }
      reject(new core_AxiosError(
        timeoutErrorMessage,
        transitional.clarifyTimeoutError ? core_AxiosError.ETIMEDOUT : core_AxiosError.ECONNABORTED,
        config,
        request));

      // Clean up request
      request = null;
    };

    // Remove Content-Type if data is undefined
    requestData === undefined && requestHeaders.setContentType(null);

    // Add headers to the request
    if ('setRequestHeader' in request) {
      utils.forEach(requestHeaders.toJSON(), function setRequestHeader(val, key) {
        request.setRequestHeader(key, val);
      });
    }

    // Add withCredentials to request if needed
    if (!utils.isUndefined(_config.withCredentials)) {
      request.withCredentials = !!_config.withCredentials;
    }

    // Add responseType to request if needed
    if (responseType && responseType !== 'json') {
      request.responseType = _config.responseType;
    }

    // Handle progress if needed
    if (onDownloadProgress) {
      ([downloadThrottled, flushDownload] = progressEventReducer(onDownloadProgress, true));
      request.addEventListener('progress', downloadThrottled);
    }

    // Not all browsers support upload events
    if (onUploadProgress && request.upload) {
      ([uploadThrottled, flushUpload] = progressEventReducer(onUploadProgress));

      request.upload.addEventListener('progress', uploadThrottled);

      request.upload.addEventListener('loadend', flushUpload);
    }

    if (_config.cancelToken || _config.signal) {
      // Handle cancellation
      // eslint-disable-next-line func-names
      onCanceled = cancel => {
        if (!request) {
          return;
        }
        reject(!cancel || cancel.type ? new cancel_CanceledError(null, config, request) : cancel);
        request.abort();
        request = null;
      };

      _config.cancelToken && _config.cancelToken.subscribe(onCanceled);
      if (_config.signal) {
        _config.signal.aborted ? onCanceled() : _config.signal.addEventListener('abort', onCanceled);
      }
    }

    const protocol = parseProtocol(_config.url);

    if (protocol && platform.protocols.indexOf(protocol) === -1) {
      reject(new core_AxiosError('Unsupported protocol ' + protocol + ':', core_AxiosError.ERR_BAD_REQUEST, config));
      return;
    }


    // Send the request
    request.send(requestData || null);
  });
});

;// ./node_modules/axios/lib/helpers/composeSignals.js




const composeSignals = (signals, timeout) => {
  const {length} = (signals = signals ? signals.filter(Boolean) : []);

  if (timeout || length) {
    let controller = new AbortController();

    let aborted;

    const onabort = function (reason) {
      if (!aborted) {
        aborted = true;
        unsubscribe();
        const err = reason instanceof Error ? reason : this.reason;
        controller.abort(err instanceof core_AxiosError ? err : new cancel_CanceledError(err instanceof Error ? err.message : err));
      }
    }

    let timer = timeout && setTimeout(() => {
      timer = null;
      onabort(new core_AxiosError(`timeout ${timeout} of ms exceeded`, core_AxiosError.ETIMEDOUT))
    }, timeout)

    const unsubscribe = () => {
      if (signals) {
        timer && clearTimeout(timer);
        timer = null;
        signals.forEach(signal => {
          signal.unsubscribe ? signal.unsubscribe(onabort) : signal.removeEventListener('abort', onabort);
        });
        signals = null;
      }
    }

    signals.forEach((signal) => signal.addEventListener('abort', onabort));

    const {signal} = controller;

    signal.unsubscribe = () => utils.asap(unsubscribe);

    return signal;
  }
}

/* harmony default export */ var helpers_composeSignals = (composeSignals);

;// ./node_modules/axios/lib/helpers/trackStream.js

const streamChunk = function* (chunk, chunkSize) {
  let len = chunk.byteLength;

  if (!chunkSize || len < chunkSize) {
    yield chunk;
    return;
  }

  let pos = 0;
  let end;

  while (pos < len) {
    end = pos + chunkSize;
    yield chunk.slice(pos, end);
    pos = end;
  }
}

const readBytes = async function* (iterable, chunkSize) {
  for await (const chunk of readStream(iterable)) {
    yield* streamChunk(chunk, chunkSize);
  }
}

const readStream = async function* (stream) {
  if (stream[Symbol.asyncIterator]) {
    yield* stream;
    return;
  }

  const reader = stream.getReader();
  try {
    for (;;) {
      const {done, value} = await reader.read();
      if (done) {
        break;
      }
      yield value;
    }
  } finally {
    await reader.cancel();
  }
}

const trackStream = (stream, chunkSize, onProgress, onFinish) => {
  const iterator = readBytes(stream, chunkSize);

  let bytes = 0;
  let done;
  let _onFinish = (e) => {
    if (!done) {
      done = true;
      onFinish && onFinish(e);
    }
  }

  return new ReadableStream({
    async pull(controller) {
      try {
        const {done, value} = await iterator.next();

        if (done) {
         _onFinish();
          controller.close();
          return;
        }

        let len = value.byteLength;
        if (onProgress) {
          let loadedBytes = bytes += len;
          onProgress(loadedBytes);
        }
        controller.enqueue(new Uint8Array(value));
      } catch (err) {
        _onFinish(err);
        throw err;
      }
    },
    cancel(reason) {
      _onFinish(reason);
      return iterator.return();
    }
  }, {
    highWaterMark: 2
  })
}

;// ./node_modules/axios/lib/adapters/fetch.js










const isFetchSupported = typeof fetch === 'function' && typeof Request === 'function' && typeof Response === 'function';
const isReadableStreamSupported = isFetchSupported && typeof ReadableStream === 'function';

// used only inside the fetch adapter
const encodeText = isFetchSupported && (typeof TextEncoder === 'function' ?
    ((encoder) => (str) => encoder.encode(str))(new TextEncoder()) :
    async (str) => new Uint8Array(await new Response(str).arrayBuffer())
);

const test = (fn, ...args) => {
  try {
    return !!fn(...args);
  } catch (e) {
    return false
  }
}

const supportsRequestStream = isReadableStreamSupported && test(() => {
  let duplexAccessed = false;

  const hasContentType = new Request(platform.origin, {
    body: new ReadableStream(),
    method: 'POST',
    get duplex() {
      duplexAccessed = true;
      return 'half';
    },
  }).headers.has('Content-Type');

  return duplexAccessed && !hasContentType;
});

const DEFAULT_CHUNK_SIZE = 64 * 1024;

const supportsResponseStream = isReadableStreamSupported &&
  test(() => utils.isReadableStream(new Response('').body));


const resolvers = {
  stream: supportsResponseStream && ((res) => res.body)
};

isFetchSupported && (((res) => {
  ['text', 'arrayBuffer', 'blob', 'formData', 'stream'].forEach(type => {
    !resolvers[type] && (resolvers[type] = utils.isFunction(res[type]) ? (res) => res[type]() :
      (_, config) => {
        throw new core_AxiosError(`Response type '${type}' is not supported`, core_AxiosError.ERR_NOT_SUPPORT, config);
      })
  });
})(new Response));

const getBodyLength = async (body) => {
  if (body == null) {
    return 0;
  }

  if(utils.isBlob(body)) {
    return body.size;
  }

  if(utils.isSpecCompliantForm(body)) {
    const _request = new Request(platform.origin, {
      method: 'POST',
      body,
    });
    return (await _request.arrayBuffer()).byteLength;
  }

  if(utils.isArrayBufferView(body) || utils.isArrayBuffer(body)) {
    return body.byteLength;
  }

  if(utils.isURLSearchParams(body)) {
    body = body + '';
  }

  if(utils.isString(body)) {
    return (await encodeText(body)).byteLength;
  }
}

const resolveBodyLength = async (headers, body) => {
  const length = utils.toFiniteNumber(headers.getContentLength());

  return length == null ? getBodyLength(body) : length;
}

/* harmony default export */ var adapters_fetch = (isFetchSupported && (async (config) => {
  let {
    url,
    method,
    data,
    signal,
    cancelToken,
    timeout,
    onDownloadProgress,
    onUploadProgress,
    responseType,
    headers,
    withCredentials = 'same-origin',
    fetchOptions
  } = resolveConfig(config);

  responseType = responseType ? (responseType + '').toLowerCase() : 'text';

  let composedSignal = helpers_composeSignals([signal, cancelToken && cancelToken.toAbortSignal()], timeout);

  let request;

  const unsubscribe = composedSignal && composedSignal.unsubscribe && (() => {
      composedSignal.unsubscribe();
  });

  let requestContentLength;

  try {
    if (
      onUploadProgress && supportsRequestStream && method !== 'get' && method !== 'head' &&
      (requestContentLength = await resolveBodyLength(headers, data)) !== 0
    ) {
      let _request = new Request(url, {
        method: 'POST',
        body: data,
        duplex: "half"
      });

      let contentTypeHeader;

      if (utils.isFormData(data) && (contentTypeHeader = _request.headers.get('content-type'))) {
        headers.setContentType(contentTypeHeader)
      }

      if (_request.body) {
        const [onProgress, flush] = progressEventDecorator(
          requestContentLength,
          progressEventReducer(asyncDecorator(onUploadProgress))
        );

        data = trackStream(_request.body, DEFAULT_CHUNK_SIZE, onProgress, flush);
      }
    }

    if (!utils.isString(withCredentials)) {
      withCredentials = withCredentials ? 'include' : 'omit';
    }

    // Cloudflare Workers throws when credentials are defined
    // see https://github.com/cloudflare/workerd/issues/902
    const isCredentialsSupported = "credentials" in Request.prototype;
    request = new Request(url, {
      ...fetchOptions,
      signal: composedSignal,
      method: method.toUpperCase(),
      headers: headers.normalize().toJSON(),
      body: data,
      duplex: "half",
      credentials: isCredentialsSupported ? withCredentials : undefined
    });

    let response = await fetch(request);

    const isStreamResponse = supportsResponseStream && (responseType === 'stream' || responseType === 'response');

    if (supportsResponseStream && (onDownloadProgress || (isStreamResponse && unsubscribe))) {
      const options = {};

      ['status', 'statusText', 'headers'].forEach(prop => {
        options[prop] = response[prop];
      });

      const responseContentLength = utils.toFiniteNumber(response.headers.get('content-length'));

      const [onProgress, flush] = onDownloadProgress && progressEventDecorator(
        responseContentLength,
        progressEventReducer(asyncDecorator(onDownloadProgress), true)
      ) || [];

      response = new Response(
        trackStream(response.body, DEFAULT_CHUNK_SIZE, onProgress, () => {
          flush && flush();
          unsubscribe && unsubscribe();
        }),
        options
      );
    }

    responseType = responseType || 'text';

    let responseData = await resolvers[utils.findKey(resolvers, responseType) || 'text'](response, config);

    !isStreamResponse && unsubscribe && unsubscribe();

    return await new Promise((resolve, reject) => {
      settle(resolve, reject, {
        data: responseData,
        headers: core_AxiosHeaders.from(response.headers),
        status: response.status,
        statusText: response.statusText,
        config,
        request
      })
    })
  } catch (err) {
    unsubscribe && unsubscribe();

    if (err && err.name === 'TypeError' && /fetch/i.test(err.message)) {
      throw Object.assign(
        new core_AxiosError('Network Error', core_AxiosError.ERR_NETWORK, config, request),
        {
          cause: err.cause || err
        }
      )
    }

    throw core_AxiosError.from(err, err && err.code, config, request);
  }
}));



;// ./node_modules/axios/lib/adapters/adapters.js






const knownAdapters = {
  http: helpers_null,
  xhr: xhr,
  fetch: adapters_fetch
}

utils.forEach(knownAdapters, (fn, value) => {
  if (fn) {
    try {
      Object.defineProperty(fn, 'name', {value});
    } catch (e) {
      // eslint-disable-next-line no-empty
    }
    Object.defineProperty(fn, 'adapterName', {value});
  }
});

const renderReason = (reason) => `- ${reason}`;

const isResolvedHandle = (adapter) => utils.isFunction(adapter) || adapter === null || adapter === false;

/* harmony default export */ var adapters = ({
  getAdapter: (adapters) => {
    adapters = utils.isArray(adapters) ? adapters : [adapters];

    const {length} = adapters;
    let nameOrAdapter;
    let adapter;

    const rejectedReasons = {};

    for (let i = 0; i < length; i++) {
      nameOrAdapter = adapters[i];
      let id;

      adapter = nameOrAdapter;

      if (!isResolvedHandle(nameOrAdapter)) {
        adapter = knownAdapters[(id = String(nameOrAdapter)).toLowerCase()];

        if (adapter === undefined) {
          throw new core_AxiosError(`Unknown adapter '${id}'`);
        }
      }

      if (adapter) {
        break;
      }

      rejectedReasons[id || '#' + i] = adapter;
    }

    if (!adapter) {

      const reasons = Object.entries(rejectedReasons)
        .map(([id, state]) => `adapter ${id} ` +
          (state === false ? 'is not supported by the environment' : 'is not available in the build')
        );

      let s = length ?
        (reasons.length > 1 ? 'since :\n' + reasons.map(renderReason).join('\n') : ' ' + renderReason(reasons[0])) :
        'as no adapter specified';

      throw new core_AxiosError(
        `There is no suitable adapter to dispatch the request ` + s,
        'ERR_NOT_SUPPORT'
      );
    }

    return adapter;
  },
  adapters: knownAdapters
});

;// ./node_modules/axios/lib/core/dispatchRequest.js









/**
 * Throws a `CanceledError` if cancellation has been requested.
 *
 * @param {Object} config The config that is to be used for the request
 *
 * @returns {void}
 */
function throwIfCancellationRequested(config) {
  if (config.cancelToken) {
    config.cancelToken.throwIfRequested();
  }

  if (config.signal && config.signal.aborted) {
    throw new cancel_CanceledError(null, config);
  }
}

/**
 * Dispatch a request to the server using the configured adapter.
 *
 * @param {object} config The config that is to be used for the request
 *
 * @returns {Promise} The Promise to be fulfilled
 */
function dispatchRequest(config) {
  throwIfCancellationRequested(config);

  config.headers = core_AxiosHeaders.from(config.headers);

  // Transform request data
  config.data = transformData.call(
    config,
    config.transformRequest
  );

  if (['post', 'put', 'patch'].indexOf(config.method) !== -1) {
    config.headers.setContentType('application/x-www-form-urlencoded', false);
  }

  const adapter = adapters.getAdapter(config.adapter || lib_defaults.adapter);

  return adapter(config).then(function onAdapterResolution(response) {
    throwIfCancellationRequested(config);

    // Transform response data
    response.data = transformData.call(
      config,
      config.transformResponse,
      response
    );

    response.headers = core_AxiosHeaders.from(response.headers);

    return response;
  }, function onAdapterRejection(reason) {
    if (!isCancel(reason)) {
      throwIfCancellationRequested(config);

      // Transform response data
      if (reason && reason.response) {
        reason.response.data = transformData.call(
          config,
          config.transformResponse,
          reason.response
        );
        reason.response.headers = core_AxiosHeaders.from(reason.response.headers);
      }
    }

    return Promise.reject(reason);
  });
}

;// ./node_modules/axios/lib/env/data.js
const VERSION = "1.8.1";
;// ./node_modules/axios/lib/helpers/validator.js





const validators = {};

// eslint-disable-next-line func-names
['object', 'boolean', 'number', 'function', 'string', 'symbol'].forEach((type, i) => {
  validators[type] = function validator(thing) {
    return typeof thing === type || 'a' + (i < 1 ? 'n ' : ' ') + type;
  };
});

const deprecatedWarnings = {};

/**
 * Transitional option validator
 *
 * @param {function|boolean?} validator - set to false if the transitional option has been removed
 * @param {string?} version - deprecated version / removed since version
 * @param {string?} message - some message with additional info
 *
 * @returns {function}
 */
validators.transitional = function transitional(validator, version, message) {
  function formatMessage(opt, desc) {
    return '[Axios v' + VERSION + '] Transitional option \'' + opt + '\'' + desc + (message ? '. ' + message : '');
  }

  // eslint-disable-next-line func-names
  return (value, opt, opts) => {
    if (validator === false) {
      throw new core_AxiosError(
        formatMessage(opt, ' has been removed' + (version ? ' in ' + version : '')),
        core_AxiosError.ERR_DEPRECATED
      );
    }

    if (version && !deprecatedWarnings[opt]) {
      deprecatedWarnings[opt] = true;
      // eslint-disable-next-line no-console
      console.warn(
        formatMessage(
          opt,
          ' has been deprecated since v' + version + ' and will be removed in the near future'
        )
      );
    }

    return validator ? validator(value, opt, opts) : true;
  };
};

validators.spelling = function spelling(correctSpelling) {
  return (value, opt) => {
    // eslint-disable-next-line no-console
    console.warn(`${opt} is likely a misspelling of ${correctSpelling}`);
    return true;
  }
};

/**
 * Assert object's properties type
 *
 * @param {object} options
 * @param {object} schema
 * @param {boolean?} allowUnknown
 *
 * @returns {object}
 */

function assertOptions(options, schema, allowUnknown) {
  if (typeof options !== 'object') {
    throw new core_AxiosError('options must be an object', core_AxiosError.ERR_BAD_OPTION_VALUE);
  }
  const keys = Object.keys(options);
  let i = keys.length;
  while (i-- > 0) {
    const opt = keys[i];
    const validator = schema[opt];
    if (validator) {
      const value = options[opt];
      const result = value === undefined || validator(value, opt, options);
      if (result !== true) {
        throw new core_AxiosError('option ' + opt + ' must be ' + result, core_AxiosError.ERR_BAD_OPTION_VALUE);
      }
      continue;
    }
    if (allowUnknown !== true) {
      throw new core_AxiosError('Unknown option ' + opt, core_AxiosError.ERR_BAD_OPTION);
    }
  }
}

/* harmony default export */ var validator = ({
  assertOptions,
  validators
});

;// ./node_modules/axios/lib/core/Axios.js











const Axios_validators = validator.validators;

/**
 * Create a new instance of Axios
 *
 * @param {Object} instanceConfig The default config for the instance
 *
 * @return {Axios} A new instance of Axios
 */
class Axios {
  constructor(instanceConfig) {
    this.defaults = instanceConfig;
    this.interceptors = {
      request: new core_InterceptorManager(),
      response: new core_InterceptorManager()
    };
  }

  /**
   * Dispatch a request
   *
   * @param {String|Object} configOrUrl The config specific for this request (merged with this.defaults)
   * @param {?Object} config
   *
   * @returns {Promise} The Promise to be fulfilled
   */
  async request(configOrUrl, config) {
    try {
      return await this._request(configOrUrl, config);
    } catch (err) {
      if (err instanceof Error) {
        let dummy = {};

        Error.captureStackTrace ? Error.captureStackTrace(dummy) : (dummy = new Error());

        // slice off the Error: ... line
        const stack = dummy.stack ? dummy.stack.replace(/^.+\n/, '') : '';
        try {
          if (!err.stack) {
            err.stack = stack;
            // match without the 2 top stack lines
          } else if (stack && !String(err.stack).endsWith(stack.replace(/^.+\n.+\n/, ''))) {
            err.stack += '\n' + stack
          }
        } catch (e) {
          // ignore the case where "stack" is an un-writable property
        }
      }

      throw err;
    }
  }

  _request(configOrUrl, config) {
    /*eslint no-param-reassign:0*/
    // Allow for axios('example/url'[, config]) a la fetch API
    if (typeof configOrUrl === 'string') {
      config = config || {};
      config.url = configOrUrl;
    } else {
      config = configOrUrl || {};
    }

    config = mergeConfig(this.defaults, config);

    const {transitional, paramsSerializer, headers} = config;

    if (transitional !== undefined) {
      validator.assertOptions(transitional, {
        silentJSONParsing: Axios_validators.transitional(Axios_validators.boolean),
        forcedJSONParsing: Axios_validators.transitional(Axios_validators.boolean),
        clarifyTimeoutError: Axios_validators.transitional(Axios_validators.boolean)
      }, false);
    }

    if (paramsSerializer != null) {
      if (utils.isFunction(paramsSerializer)) {
        config.paramsSerializer = {
          serialize: paramsSerializer
        }
      } else {
        validator.assertOptions(paramsSerializer, {
          encode: Axios_validators.function,
          serialize: Axios_validators.function
        }, true);
      }
    }

    // Set config.allowAbsoluteUrls
    if (config.allowAbsoluteUrls !== undefined) {
      // do nothing
    } else if (this.defaults.allowAbsoluteUrls !== undefined) {
      config.allowAbsoluteUrls = this.defaults.allowAbsoluteUrls;
    } else {
      config.allowAbsoluteUrls = true;
    }

    validator.assertOptions(config, {
      baseUrl: Axios_validators.spelling('baseURL'),
      withXsrfToken: Axios_validators.spelling('withXSRFToken')
    }, true);

    // Set config.method
    config.method = (config.method || this.defaults.method || 'get').toLowerCase();

    // Flatten headers
    let contextHeaders = headers && utils.merge(
      headers.common,
      headers[config.method]
    );

    headers && utils.forEach(
      ['delete', 'get', 'head', 'post', 'put', 'patch', 'common'],
      (method) => {
        delete headers[method];
      }
    );

    config.headers = core_AxiosHeaders.concat(contextHeaders, headers);

    // filter out skipped interceptors
    const requestInterceptorChain = [];
    let synchronousRequestInterceptors = true;
    this.interceptors.request.forEach(function unshiftRequestInterceptors(interceptor) {
      if (typeof interceptor.runWhen === 'function' && interceptor.runWhen(config) === false) {
        return;
      }

      synchronousRequestInterceptors = synchronousRequestInterceptors && interceptor.synchronous;

      requestInterceptorChain.unshift(interceptor.fulfilled, interceptor.rejected);
    });

    const responseInterceptorChain = [];
    this.interceptors.response.forEach(function pushResponseInterceptors(interceptor) {
      responseInterceptorChain.push(interceptor.fulfilled, interceptor.rejected);
    });

    let promise;
    let i = 0;
    let len;

    if (!synchronousRequestInterceptors) {
      const chain = [dispatchRequest.bind(this), undefined];
      chain.unshift.apply(chain, requestInterceptorChain);
      chain.push.apply(chain, responseInterceptorChain);
      len = chain.length;

      promise = Promise.resolve(config);

      while (i < len) {
        promise = promise.then(chain[i++], chain[i++]);
      }

      return promise;
    }

    len = requestInterceptorChain.length;

    let newConfig = config;

    i = 0;

    while (i < len) {
      const onFulfilled = requestInterceptorChain[i++];
      const onRejected = requestInterceptorChain[i++];
      try {
        newConfig = onFulfilled(newConfig);
      } catch (error) {
        onRejected.call(this, error);
        break;
      }
    }

    try {
      promise = dispatchRequest.call(this, newConfig);
    } catch (error) {
      return Promise.reject(error);
    }

    i = 0;
    len = responseInterceptorChain.length;

    while (i < len) {
      promise = promise.then(responseInterceptorChain[i++], responseInterceptorChain[i++]);
    }

    return promise;
  }

  getUri(config) {
    config = mergeConfig(this.defaults, config);
    const fullPath = buildFullPath(config.baseURL, config.url, config.allowAbsoluteUrls);
    return buildURL(fullPath, config.params, config.paramsSerializer);
  }
}

// Provide aliases for supported request methods
utils.forEach(['delete', 'get', 'head', 'options'], function forEachMethodNoData(method) {
  /*eslint func-names:0*/
  Axios.prototype[method] = function(url, config) {
    return this.request(mergeConfig(config || {}, {
      method,
      url,
      data: (config || {}).data
    }));
  };
});

utils.forEach(['post', 'put', 'patch'], function forEachMethodWithData(method) {
  /*eslint func-names:0*/

  function generateHTTPMethod(isForm) {
    return function httpMethod(url, data, config) {
      return this.request(mergeConfig(config || {}, {
        method,
        headers: isForm ? {
          'Content-Type': 'multipart/form-data'
        } : {},
        url,
        data
      }));
    };
  }

  Axios.prototype[method] = generateHTTPMethod();

  Axios.prototype[method + 'Form'] = generateHTTPMethod(true);
});

/* harmony default export */ var core_Axios = (Axios);

;// ./node_modules/axios/lib/cancel/CancelToken.js




/**
 * A `CancelToken` is an object that can be used to request cancellation of an operation.
 *
 * @param {Function} executor The executor function.
 *
 * @returns {CancelToken}
 */
class CancelToken {
  constructor(executor) {
    if (typeof executor !== 'function') {
      throw new TypeError('executor must be a function.');
    }

    let resolvePromise;

    this.promise = new Promise(function promiseExecutor(resolve) {
      resolvePromise = resolve;
    });

    const token = this;

    // eslint-disable-next-line func-names
    this.promise.then(cancel => {
      if (!token._listeners) return;

      let i = token._listeners.length;

      while (i-- > 0) {
        token._listeners[i](cancel);
      }
      token._listeners = null;
    });

    // eslint-disable-next-line func-names
    this.promise.then = onfulfilled => {
      let _resolve;
      // eslint-disable-next-line func-names
      const promise = new Promise(resolve => {
        token.subscribe(resolve);
        _resolve = resolve;
      }).then(onfulfilled);

      promise.cancel = function reject() {
        token.unsubscribe(_resolve);
      };

      return promise;
    };

    executor(function cancel(message, config, request) {
      if (token.reason) {
        // Cancellation has already been requested
        return;
      }

      token.reason = new cancel_CanceledError(message, config, request);
      resolvePromise(token.reason);
    });
  }

  /**
   * Throws a `CanceledError` if cancellation has been requested.
   */
  throwIfRequested() {
    if (this.reason) {
      throw this.reason;
    }
  }

  /**
   * Subscribe to the cancel signal
   */

  subscribe(listener) {
    if (this.reason) {
      listener(this.reason);
      return;
    }

    if (this._listeners) {
      this._listeners.push(listener);
    } else {
      this._listeners = [listener];
    }
  }

  /**
   * Unsubscribe from the cancel signal
   */

  unsubscribe(listener) {
    if (!this._listeners) {
      return;
    }
    const index = this._listeners.indexOf(listener);
    if (index !== -1) {
      this._listeners.splice(index, 1);
    }
  }

  toAbortSignal() {
    const controller = new AbortController();

    const abort = (err) => {
      controller.abort(err);
    };

    this.subscribe(abort);

    controller.signal.unsubscribe = () => this.unsubscribe(abort);

    return controller.signal;
  }

  /**
   * Returns an object that contains a new `CancelToken` and a function that, when called,
   * cancels the `CancelToken`.
   */
  static source() {
    let cancel;
    const token = new CancelToken(function executor(c) {
      cancel = c;
    });
    return {
      token,
      cancel
    };
  }
}

/* harmony default export */ var cancel_CancelToken = (CancelToken);

;// ./node_modules/axios/lib/helpers/spread.js


/**
 * Syntactic sugar for invoking a function and expanding an array for arguments.
 *
 * Common use case would be to use `Function.prototype.apply`.
 *
 *  ```js
 *  function f(x, y, z) {}
 *  var args = [1, 2, 3];
 *  f.apply(null, args);
 *  ```
 *
 * With `spread` this example can be re-written.
 *
 *  ```js
 *  spread(function(x, y, z) {})([1, 2, 3]);
 *  ```
 *
 * @param {Function} callback
 *
 * @returns {Function}
 */
function spread(callback) {
  return function wrap(arr) {
    return callback.apply(null, arr);
  };
}

;// ./node_modules/axios/lib/helpers/isAxiosError.js




/**
 * Determines whether the payload is an error thrown by Axios
 *
 * @param {*} payload The value to test
 *
 * @returns {boolean} True if the payload is an error thrown by Axios, otherwise false
 */
function isAxiosError(payload) {
  return utils.isObject(payload) && (payload.isAxiosError === true);
}

;// ./node_modules/axios/lib/helpers/HttpStatusCode.js
const HttpStatusCode = {
  Continue: 100,
  SwitchingProtocols: 101,
  Processing: 102,
  EarlyHints: 103,
  Ok: 200,
  Created: 201,
  Accepted: 202,
  NonAuthoritativeInformation: 203,
  NoContent: 204,
  ResetContent: 205,
  PartialContent: 206,
  MultiStatus: 207,
  AlreadyReported: 208,
  ImUsed: 226,
  MultipleChoices: 300,
  MovedPermanently: 301,
  Found: 302,
  SeeOther: 303,
  NotModified: 304,
  UseProxy: 305,
  Unused: 306,
  TemporaryRedirect: 307,
  PermanentRedirect: 308,
  BadRequest: 400,
  Unauthorized: 401,
  PaymentRequired: 402,
  Forbidden: 403,
  NotFound: 404,
  MethodNotAllowed: 405,
  NotAcceptable: 406,
  ProxyAuthenticationRequired: 407,
  RequestTimeout: 408,
  Conflict: 409,
  Gone: 410,
  LengthRequired: 411,
  PreconditionFailed: 412,
  PayloadTooLarge: 413,
  UriTooLong: 414,
  UnsupportedMediaType: 415,
  RangeNotSatisfiable: 416,
  ExpectationFailed: 417,
  ImATeapot: 418,
  MisdirectedRequest: 421,
  UnprocessableEntity: 422,
  Locked: 423,
  FailedDependency: 424,
  TooEarly: 425,
  UpgradeRequired: 426,
  PreconditionRequired: 428,
  TooManyRequests: 429,
  RequestHeaderFieldsTooLarge: 431,
  UnavailableForLegalReasons: 451,
  InternalServerError: 500,
  NotImplemented: 501,
  BadGateway: 502,
  ServiceUnavailable: 503,
  GatewayTimeout: 504,
  HttpVersionNotSupported: 505,
  VariantAlsoNegotiates: 506,
  InsufficientStorage: 507,
  LoopDetected: 508,
  NotExtended: 510,
  NetworkAuthenticationRequired: 511,
};

Object.entries(HttpStatusCode).forEach(([key, value]) => {
  HttpStatusCode[value] = key;
});

/* harmony default export */ var helpers_HttpStatusCode = (HttpStatusCode);

;// ./node_modules/axios/lib/axios.js




















/**
 * Create an instance of Axios
 *
 * @param {Object} defaultConfig The default config for the instance
 *
 * @returns {Axios} A new instance of Axios
 */
function createInstance(defaultConfig) {
  const context = new core_Axios(defaultConfig);
  const instance = bind_bind(core_Axios.prototype.request, context);

  // Copy axios.prototype to instance
  utils.extend(instance, core_Axios.prototype, context, {allOwnKeys: true});

  // Copy context to instance
  utils.extend(instance, context, null, {allOwnKeys: true});

  // Factory for creating new instances
  instance.create = function create(instanceConfig) {
    return createInstance(mergeConfig(defaultConfig, instanceConfig));
  };

  return instance;
}

// Create the default instance to be exported
const axios = createInstance(lib_defaults);

// Expose Axios class to allow class inheritance
axios.Axios = core_Axios;

// Expose Cancel & CancelToken
axios.CanceledError = cancel_CanceledError;
axios.CancelToken = cancel_CancelToken;
axios.isCancel = isCancel;
axios.VERSION = VERSION;
axios.toFormData = helpers_toFormData;

// Expose AxiosError class
axios.AxiosError = core_AxiosError;

// alias for CanceledError for backward compatibility
axios.Cancel = axios.CanceledError;

// Expose all/spread
axios.all = function all(promises) {
  return Promise.all(promises);
};

axios.spread = spread;

// Expose isAxiosError
axios.isAxiosError = isAxiosError;

// Expose mergeConfig
axios.mergeConfig = mergeConfig;

axios.AxiosHeaders = core_AxiosHeaders;

axios.formToJSON = thing => helpers_formDataToJSON(utils.isHTMLForm(thing) ? new FormData(thing) : thing);

axios.getAdapter = adapters.getAdapter;

axios.HttpStatusCode = helpers_HttpStatusCode;

axios.default = axios;

// this module should only have a default export
/* harmony default export */ var lib_axios = (axios);

;// ./node_modules/mustache/mustache.mjs
/*!
 * mustache.js - Logic-less {{mustache}} templates with JavaScript
 * http://github.com/janl/mustache.js
 */

var objectToString = Object.prototype.toString;
var mustache_isArray = Array.isArray || function isArrayPolyfill (object) {
  return objectToString.call(object) === '[object Array]';
};

function mustache_isFunction (object) {
  return typeof object === 'function';
}

/**
 * More correct typeof string handling array
 * which normally returns typeof 'object'
 */
function typeStr (obj) {
  return mustache_isArray(obj) ? 'array' : typeof obj;
}

function escapeRegExp (string) {
  return string.replace(/[\-\[\]{}()*+?.,\\\^$|#\s]/g, '\\$&');
}

/**
 * Null safe way of checking whether or not an object,
 * including its prototype, has a given property
 */
function hasProperty (obj, propName) {
  return obj != null && typeof obj === 'object' && (propName in obj);
}

/**
 * Safe way of detecting whether or not the given thing is a primitive and
 * whether it has the given property
 */
function primitiveHasOwnProperty (primitive, propName) {
  return (
    primitive != null
    && typeof primitive !== 'object'
    && primitive.hasOwnProperty
    && primitive.hasOwnProperty(propName)
  );
}

// Workaround for https://issues.apache.org/jira/browse/COUCHDB-577
// See https://github.com/janl/mustache.js/issues/189
var regExpTest = RegExp.prototype.test;
function testRegExp (re, string) {
  return regExpTest.call(re, string);
}

var nonSpaceRe = /\S/;
function isWhitespace (string) {
  return !testRegExp(nonSpaceRe, string);
}

var entityMap = {
  '&': '&amp;',
  '<': '&lt;',
  '>': '&gt;',
  '"': '&quot;',
  "'": '&#39;',
  '/': '&#x2F;',
  '`': '&#x60;',
  '=': '&#x3D;'
};

function escapeHtml (string) {
  return String(string).replace(/[&<>"'`=\/]/g, function fromEntityMap (s) {
    return entityMap[s];
  });
}

var whiteRe = /\s*/;
var spaceRe = /\s+/;
var equalsRe = /\s*=/;
var curlyRe = /\s*\}/;
var tagRe = /#|\^|\/|>|\{|&|=|!/;

/**
 * Breaks up the given `template` string into a tree of tokens. If the `tags`
 * argument is given here it must be an array with two string values: the
 * opening and closing tags used in the template (e.g. [ "<%", "%>" ]). Of
 * course, the default is to use mustaches (i.e. mustache.tags).
 *
 * A token is an array with at least 4 elements. The first element is the
 * mustache symbol that was used inside the tag, e.g. "#" or "&". If the tag
 * did not contain a symbol (i.e. {{myValue}}) this element is "name". For
 * all text that appears outside a symbol this element is "text".
 *
 * The second element of a token is its "value". For mustache tags this is
 * whatever else was inside the tag besides the opening symbol. For text tokens
 * this is the text itself.
 *
 * The third and fourth elements of the token are the start and end indices,
 * respectively, of the token in the original template.
 *
 * Tokens that are the root node of a subtree contain two more elements: 1) an
 * array of tokens in the subtree and 2) the index in the original template at
 * which the closing tag for that section begins.
 *
 * Tokens for partials also contain two more elements: 1) a string value of
 * indendation prior to that tag and 2) the index of that tag on that line -
 * eg a value of 2 indicates the partial is the third tag on this line.
 */
function parseTemplate (template, tags) {
  if (!template)
    return [];
  var lineHasNonSpace = false;
  var sections = [];     // Stack to hold section tokens
  var tokens = [];       // Buffer to hold the tokens
  var spaces = [];       // Indices of whitespace tokens on the current line
  var hasTag = false;    // Is there a {{tag}} on the current line?
  var nonSpace = false;  // Is there a non-space char on the current line?
  var indentation = '';  // Tracks indentation for tags that use it
  var tagIndex = 0;      // Stores a count of number of tags encountered on a line

  // Strips all whitespace tokens array for the current line
  // if there was a {{#tag}} on it and otherwise only space.
  function stripSpace () {
    if (hasTag && !nonSpace) {
      while (spaces.length)
        delete tokens[spaces.pop()];
    } else {
      spaces = [];
    }

    hasTag = false;
    nonSpace = false;
  }

  var openingTagRe, closingTagRe, closingCurlyRe;
  function compileTags (tagsToCompile) {
    if (typeof tagsToCompile === 'string')
      tagsToCompile = tagsToCompile.split(spaceRe, 2);

    if (!mustache_isArray(tagsToCompile) || tagsToCompile.length !== 2)
      throw new Error('Invalid tags: ' + tagsToCompile);

    openingTagRe = new RegExp(escapeRegExp(tagsToCompile[0]) + '\\s*');
    closingTagRe = new RegExp('\\s*' + escapeRegExp(tagsToCompile[1]));
    closingCurlyRe = new RegExp('\\s*' + escapeRegExp('}' + tagsToCompile[1]));
  }

  compileTags(tags || mustache.tags);

  var scanner = new Scanner(template);

  var start, type, value, chr, token, openSection;
  while (!scanner.eos()) {
    start = scanner.pos;

    // Match any text between tags.
    value = scanner.scanUntil(openingTagRe);

    if (value) {
      for (var i = 0, valueLength = value.length; i < valueLength; ++i) {
        chr = value.charAt(i);

        if (isWhitespace(chr)) {
          spaces.push(tokens.length);
          indentation += chr;
        } else {
          nonSpace = true;
          lineHasNonSpace = true;
          indentation += ' ';
        }

        tokens.push([ 'text', chr, start, start + 1 ]);
        start += 1;

        // Check for whitespace on the current line.
        if (chr === '\n') {
          stripSpace();
          indentation = '';
          tagIndex = 0;
          lineHasNonSpace = false;
        }
      }
    }

    // Match the opening tag.
    if (!scanner.scan(openingTagRe))
      break;

    hasTag = true;

    // Get the tag type.
    type = scanner.scan(tagRe) || 'name';
    scanner.scan(whiteRe);

    // Get the tag value.
    if (type === '=') {
      value = scanner.scanUntil(equalsRe);
      scanner.scan(equalsRe);
      scanner.scanUntil(closingTagRe);
    } else if (type === '{') {
      value = scanner.scanUntil(closingCurlyRe);
      scanner.scan(curlyRe);
      scanner.scanUntil(closingTagRe);
      type = '&';
    } else {
      value = scanner.scanUntil(closingTagRe);
    }

    // Match the closing tag.
    if (!scanner.scan(closingTagRe))
      throw new Error('Unclosed tag at ' + scanner.pos);

    if (type == '>') {
      token = [ type, value, start, scanner.pos, indentation, tagIndex, lineHasNonSpace ];
    } else {
      token = [ type, value, start, scanner.pos ];
    }
    tagIndex++;
    tokens.push(token);

    if (type === '#' || type === '^') {
      sections.push(token);
    } else if (type === '/') {
      // Check section nesting.
      openSection = sections.pop();

      if (!openSection)
        throw new Error('Unopened section "' + value + '" at ' + start);

      if (openSection[1] !== value)
        throw new Error('Unclosed section "' + openSection[1] + '" at ' + start);
    } else if (type === 'name' || type === '{' || type === '&') {
      nonSpace = true;
    } else if (type === '=') {
      // Set the tags for the next time around.
      compileTags(value);
    }
  }

  stripSpace();

  // Make sure there are no open sections when we're done.
  openSection = sections.pop();

  if (openSection)
    throw new Error('Unclosed section "' + openSection[1] + '" at ' + scanner.pos);

  return nestTokens(squashTokens(tokens));
}

/**
 * Combines the values of consecutive text tokens in the given `tokens` array
 * to a single token.
 */
function squashTokens (tokens) {
  var squashedTokens = [];

  var token, lastToken;
  for (var i = 0, numTokens = tokens.length; i < numTokens; ++i) {
    token = tokens[i];

    if (token) {
      if (token[0] === 'text' && lastToken && lastToken[0] === 'text') {
        lastToken[1] += token[1];
        lastToken[3] = token[3];
      } else {
        squashedTokens.push(token);
        lastToken = token;
      }
    }
  }

  return squashedTokens;
}

/**
 * Forms the given array of `tokens` into a nested tree structure where
 * tokens that represent a section have two additional items: 1) an array of
 * all tokens that appear in that section and 2) the index in the original
 * template that represents the end of that section.
 */
function nestTokens (tokens) {
  var nestedTokens = [];
  var collector = nestedTokens;
  var sections = [];

  var token, section;
  for (var i = 0, numTokens = tokens.length; i < numTokens; ++i) {
    token = tokens[i];

    switch (token[0]) {
      case '#':
      case '^':
        collector.push(token);
        sections.push(token);
        collector = token[4] = [];
        break;
      case '/':
        section = sections.pop();
        section[5] = token[2];
        collector = sections.length > 0 ? sections[sections.length - 1][4] : nestedTokens;
        break;
      default:
        collector.push(token);
    }
  }

  return nestedTokens;
}

/**
 * A simple string scanner that is used by the template parser to find
 * tokens in template strings.
 */
function Scanner (string) {
  this.string = string;
  this.tail = string;
  this.pos = 0;
}

/**
 * Returns `true` if the tail is empty (end of string).
 */
Scanner.prototype.eos = function eos () {
  return this.tail === '';
};

/**
 * Tries to match the given regular expression at the current position.
 * Returns the matched text if it can match, the empty string otherwise.
 */
Scanner.prototype.scan = function scan (re) {
  var match = this.tail.match(re);

  if (!match || match.index !== 0)
    return '';

  var string = match[0];

  this.tail = this.tail.substring(string.length);
  this.pos += string.length;

  return string;
};

/**
 * Skips all text until the given regular expression can be matched. Returns
 * the skipped string, which is the entire tail if no match can be made.
 */
Scanner.prototype.scanUntil = function scanUntil (re) {
  var index = this.tail.search(re), match;

  switch (index) {
    case -1:
      match = this.tail;
      this.tail = '';
      break;
    case 0:
      match = '';
      break;
    default:
      match = this.tail.substring(0, index);
      this.tail = this.tail.substring(index);
  }

  this.pos += match.length;

  return match;
};

/**
 * Represents a rendering context by wrapping a view object and
 * maintaining a reference to the parent context.
 */
function Context (view, parentContext) {
  this.view = view;
  this.cache = { '.': this.view };
  this.parent = parentContext;
}

/**
 * Creates a new context using the given view with this context
 * as the parent.
 */
Context.prototype.push = function push (view) {
  return new Context(view, this);
};

/**
 * Returns the value of the given name in this context, traversing
 * up the context hierarchy if the value is absent in this context's view.
 */
Context.prototype.lookup = function lookup (name) {
  var cache = this.cache;

  var value;
  if (cache.hasOwnProperty(name)) {
    value = cache[name];
  } else {
    var context = this, intermediateValue, names, index, lookupHit = false;

    while (context) {
      if (name.indexOf('.') > 0) {
        intermediateValue = context.view;
        names = name.split('.');
        index = 0;

        /**
         * Using the dot notion path in `name`, we descend through the
         * nested objects.
         *
         * To be certain that the lookup has been successful, we have to
         * check if the last object in the path actually has the property
         * we are looking for. We store the result in `lookupHit`.
         *
         * This is specially necessary for when the value has been set to
         * `undefined` and we want to avoid looking up parent contexts.
         *
         * In the case where dot notation is used, we consider the lookup
         * to be successful even if the last "object" in the path is
         * not actually an object but a primitive (e.g., a string, or an
         * integer), because it is sometimes useful to access a property
         * of an autoboxed primitive, such as the length of a string.
         **/
        while (intermediateValue != null && index < names.length) {
          if (index === names.length - 1)
            lookupHit = (
              hasProperty(intermediateValue, names[index])
              || primitiveHasOwnProperty(intermediateValue, names[index])
            );

          intermediateValue = intermediateValue[names[index++]];
        }
      } else {
        intermediateValue = context.view[name];

        /**
         * Only checking against `hasProperty`, which always returns `false` if
         * `context.view` is not an object. Deliberately omitting the check
         * against `primitiveHasOwnProperty` if dot notation is not used.
         *
         * Consider this example:
         * ```
         * Mustache.render("The length of a football field is {{#length}}{{length}}{{/length}}.", {length: "100 yards"})
         * ```
         *
         * If we were to check also against `primitiveHasOwnProperty`, as we do
         * in the dot notation case, then render call would return:
         *
         * "The length of a football field is 9."
         *
         * rather than the expected:
         *
         * "The length of a football field is 100 yards."
         **/
        lookupHit = hasProperty(context.view, name);
      }

      if (lookupHit) {
        value = intermediateValue;
        break;
      }

      context = context.parent;
    }

    cache[name] = value;
  }

  if (mustache_isFunction(value))
    value = value.call(this.view);

  return value;
};

/**
 * A Writer knows how to take a stream of tokens and render them to a
 * string, given a context. It also maintains a cache of templates to
 * avoid the need to parse the same template twice.
 */
function Writer () {
  this.templateCache = {
    _cache: {},
    set: function set (key, value) {
      this._cache[key] = value;
    },
    get: function get (key) {
      return this._cache[key];
    },
    clear: function clear () {
      this._cache = {};
    }
  };
}

/**
 * Clears all cached templates in this writer.
 */
Writer.prototype.clearCache = function clearCache () {
  if (typeof this.templateCache !== 'undefined') {
    this.templateCache.clear();
  }
};

/**
 * Parses and caches the given `template` according to the given `tags` or
 * `mustache.tags` if `tags` is omitted,  and returns the array of tokens
 * that is generated from the parse.
 */
Writer.prototype.parse = function parse (template, tags) {
  var cache = this.templateCache;
  var cacheKey = template + ':' + (tags || mustache.tags).join(':');
  var isCacheEnabled = typeof cache !== 'undefined';
  var tokens = isCacheEnabled ? cache.get(cacheKey) : undefined;

  if (tokens == undefined) {
    tokens = parseTemplate(template, tags);
    isCacheEnabled && cache.set(cacheKey, tokens);
  }
  return tokens;
};

/**
 * High-level method that is used to render the given `template` with
 * the given `view`.
 *
 * The optional `partials` argument may be an object that contains the
 * names and templates of partials that are used in the template. It may
 * also be a function that is used to load partial templates on the fly
 * that takes a single argument: the name of the partial.
 *
 * If the optional `config` argument is given here, then it should be an
 * object with a `tags` attribute or an `escape` attribute or both.
 * If an array is passed, then it will be interpreted the same way as
 * a `tags` attribute on a `config` object.
 *
 * The `tags` attribute of a `config` object must be an array with two
 * string values: the opening and closing tags used in the template (e.g.
 * [ "<%", "%>" ]). The default is to mustache.tags.
 *
 * The `escape` attribute of a `config` object must be a function which
 * accepts a string as input and outputs a safely escaped string.
 * If an `escape` function is not provided, then an HTML-safe string
 * escaping function is used as the default.
 */
Writer.prototype.render = function render (template, view, partials, config) {
  var tags = this.getConfigTags(config);
  var tokens = this.parse(template, tags);
  var context = (view instanceof Context) ? view : new Context(view, undefined);
  return this.renderTokens(tokens, context, partials, template, config);
};

/**
 * Low-level method that renders the given array of `tokens` using
 * the given `context` and `partials`.
 *
 * Note: The `originalTemplate` is only ever used to extract the portion
 * of the original template that was contained in a higher-order section.
 * If the template doesn't use higher-order sections, this argument may
 * be omitted.
 */
Writer.prototype.renderTokens = function renderTokens (tokens, context, partials, originalTemplate, config) {
  var buffer = '';

  var token, symbol, value;
  for (var i = 0, numTokens = tokens.length; i < numTokens; ++i) {
    value = undefined;
    token = tokens[i];
    symbol = token[0];

    if (symbol === '#') value = this.renderSection(token, context, partials, originalTemplate, config);
    else if (symbol === '^') value = this.renderInverted(token, context, partials, originalTemplate, config);
    else if (symbol === '>') value = this.renderPartial(token, context, partials, config);
    else if (symbol === '&') value = this.unescapedValue(token, context);
    else if (symbol === 'name') value = this.escapedValue(token, context, config);
    else if (symbol === 'text') value = this.rawValue(token);

    if (value !== undefined)
      buffer += value;
  }

  return buffer;
};

Writer.prototype.renderSection = function renderSection (token, context, partials, originalTemplate, config) {
  var self = this;
  var buffer = '';
  var value = context.lookup(token[1]);

  // This function is used to render an arbitrary template
  // in the current context by higher-order sections.
  function subRender (template) {
    return self.render(template, context, partials, config);
  }

  if (!value) return;

  if (mustache_isArray(value)) {
    for (var j = 0, valueLength = value.length; j < valueLength; ++j) {
      buffer += this.renderTokens(token[4], context.push(value[j]), partials, originalTemplate, config);
    }
  } else if (typeof value === 'object' || typeof value === 'string' || typeof value === 'number') {
    buffer += this.renderTokens(token[4], context.push(value), partials, originalTemplate, config);
  } else if (mustache_isFunction(value)) {
    if (typeof originalTemplate !== 'string')
      throw new Error('Cannot use higher-order sections without the original template');

    // Extract the portion of the original template that the section contains.
    value = value.call(context.view, originalTemplate.slice(token[3], token[5]), subRender);

    if (value != null)
      buffer += value;
  } else {
    buffer += this.renderTokens(token[4], context, partials, originalTemplate, config);
  }
  return buffer;
};

Writer.prototype.renderInverted = function renderInverted (token, context, partials, originalTemplate, config) {
  var value = context.lookup(token[1]);

  // Use JavaScript's definition of falsy. Include empty arrays.
  // See https://github.com/janl/mustache.js/issues/186
  if (!value || (mustache_isArray(value) && value.length === 0))
    return this.renderTokens(token[4], context, partials, originalTemplate, config);
};

Writer.prototype.indentPartial = function indentPartial (partial, indentation, lineHasNonSpace) {
  var filteredIndentation = indentation.replace(/[^ \t]/g, '');
  var partialByNl = partial.split('\n');
  for (var i = 0; i < partialByNl.length; i++) {
    if (partialByNl[i].length && (i > 0 || !lineHasNonSpace)) {
      partialByNl[i] = filteredIndentation + partialByNl[i];
    }
  }
  return partialByNl.join('\n');
};

Writer.prototype.renderPartial = function renderPartial (token, context, partials, config) {
  if (!partials) return;
  var tags = this.getConfigTags(config);

  var value = mustache_isFunction(partials) ? partials(token[1]) : partials[token[1]];
  if (value != null) {
    var lineHasNonSpace = token[6];
    var tagIndex = token[5];
    var indentation = token[4];
    var indentedValue = value;
    if (tagIndex == 0 && indentation) {
      indentedValue = this.indentPartial(value, indentation, lineHasNonSpace);
    }
    var tokens = this.parse(indentedValue, tags);
    return this.renderTokens(tokens, context, partials, indentedValue, config);
  }
};

Writer.prototype.unescapedValue = function unescapedValue (token, context) {
  var value = context.lookup(token[1]);
  if (value != null)
    return value;
};

Writer.prototype.escapedValue = function escapedValue (token, context, config) {
  var escape = this.getConfigEscape(config) || mustache.escape;
  var value = context.lookup(token[1]);
  if (value != null)
    return (typeof value === 'number' && escape === mustache.escape) ? String(value) : escape(value);
};

Writer.prototype.rawValue = function rawValue (token) {
  return token[1];
};

Writer.prototype.getConfigTags = function getConfigTags (config) {
  if (mustache_isArray(config)) {
    return config;
  }
  else if (config && typeof config === 'object') {
    return config.tags;
  }
  else {
    return undefined;
  }
};

Writer.prototype.getConfigEscape = function getConfigEscape (config) {
  if (config && typeof config === 'object' && !mustache_isArray(config)) {
    return config.escape;
  }
  else {
    return undefined;
  }
};

var mustache = {
  name: 'mustache.js',
  version: '4.2.0',
  tags: [ '{{', '}}' ],
  clearCache: undefined,
  escape: undefined,
  parse: undefined,
  render: undefined,
  Scanner: undefined,
  Context: undefined,
  Writer: undefined,
  /**
   * Allows a user to override the default caching strategy, by providing an
   * object with set, get and clear methods. This can also be used to disable
   * the cache by setting it to the literal `undefined`.
   */
  set templateCache (cache) {
    defaultWriter.templateCache = cache;
  },
  /**
   * Gets the default or overridden caching object from the default writer.
   */
  get templateCache () {
    return defaultWriter.templateCache;
  }
};

// All high-level mustache.* functions use this writer.
var defaultWriter = new Writer();

/**
 * Clears all cached templates in the default writer.
 */
mustache.clearCache = function clearCache () {
  return defaultWriter.clearCache();
};

/**
 * Parses and caches the given template in the default writer and returns the
 * array of tokens it contains. Doing this ahead of time avoids the need to
 * parse templates on the fly as they are rendered.
 */
mustache.parse = function parse (template, tags) {
  return defaultWriter.parse(template, tags);
};

/**
 * Renders the `template` with the given `view`, `partials`, and `config`
 * using the default writer.
 */
mustache.render = function render (template, view, partials, config) {
  if (typeof template !== 'string') {
    throw new TypeError('Invalid template! Template should be a "string" ' +
                        'but "' + typeStr(template) + '" was given as the first ' +
                        'argument for mustache#render(template, view, partials)');
  }

  return defaultWriter.render(template, view, partials, config);
};

// Export the escaping function so that the user may override it.
// See https://github.com/janl/mustache.js/issues/244
mustache.escape = escapeHtml;

// Export these mainly for testing, but also for advanced usage.
mustache.Scanner = Scanner;
mustache.Context = Context;
mustache.Writer = Writer;

/* harmony default export */ var mustache_mustache = (mustache);

// EXTERNAL MODULE: ./node_modules/fast-levenshtein/levenshtein.js
var levenshtein = __webpack_require__(6630);
var levenshtein_default = /*#__PURE__*/__webpack_require__.n(levenshtein);
// EXTERNAL MODULE: ./node_modules/lodash.isequal/index.js
var lodash_isequal = __webpack_require__(8142);
var lodash_isequal_default = /*#__PURE__*/__webpack_require__.n(lodash_isequal);
;// ./node_modules/uuid/dist/esm-browser/native.js
const randomUUID = typeof crypto !== 'undefined' && crypto.randomUUID && crypto.randomUUID.bind(crypto);
/* harmony default export */ var esm_browser_native = ({ randomUUID });

;// ./node_modules/uuid/dist/esm-browser/rng.js
let getRandomValues;
const rnds8 = new Uint8Array(16);
function rng() {
    if (!getRandomValues) {
        if (typeof crypto === 'undefined' || !crypto.getRandomValues) {
            throw new Error('crypto.getRandomValues() not supported. See https://github.com/uuidjs/uuid#getrandomvalues-not-supported');
        }
        getRandomValues = crypto.getRandomValues.bind(crypto);
    }
    return getRandomValues(rnds8);
}

;// ./node_modules/uuid/dist/esm-browser/stringify.js

const byteToHex = [];
for (let i = 0; i < 256; ++i) {
    byteToHex.push((i + 0x100).toString(16).slice(1));
}
function unsafeStringify(arr, offset = 0) {
    return (byteToHex[arr[offset + 0]] +
        byteToHex[arr[offset + 1]] +
        byteToHex[arr[offset + 2]] +
        byteToHex[arr[offset + 3]] +
        '-' +
        byteToHex[arr[offset + 4]] +
        byteToHex[arr[offset + 5]] +
        '-' +
        byteToHex[arr[offset + 6]] +
        byteToHex[arr[offset + 7]] +
        '-' +
        byteToHex[arr[offset + 8]] +
        byteToHex[arr[offset + 9]] +
        '-' +
        byteToHex[arr[offset + 10]] +
        byteToHex[arr[offset + 11]] +
        byteToHex[arr[offset + 12]] +
        byteToHex[arr[offset + 13]] +
        byteToHex[arr[offset + 14]] +
        byteToHex[arr[offset + 15]]).toLowerCase();
}
function stringify(arr, offset = 0) {
    const uuid = unsafeStringify(arr, offset);
    if (!validate(uuid)) {
        throw TypeError('Stringified UUID is invalid');
    }
    return uuid;
}
/* harmony default export */ var esm_browser_stringify = ((/* unused pure expression or super */ null && (stringify)));

;// ./node_modules/uuid/dist/esm-browser/v4.js



function v4(options, buf, offset) {
    if (esm_browser_native.randomUUID && !buf && !options) {
        return esm_browser_native.randomUUID();
    }
    options = options || {};
    const rnds = options.random ?? options.rng?.() ?? rng();
    if (rnds.length < 16) {
        throw new Error('Random bytes length must be >= 16');
    }
    rnds[6] = (rnds[6] & 0x0f) | 0x40;
    rnds[8] = (rnds[8] & 0x3f) | 0x80;
    if (buf) {
        offset = offset || 0;
        if (offset < 0 || offset + 16 > buf.length) {
            throw new RangeError(`UUID byte range ${offset}:${offset + 15} is out of buffer bounds`);
        }
        for (let i = 0; i < 16; ++i) {
            buf[offset + i] = rnds[i];
        }
        return buf;
    }
    return unsafeStringify(rnds);
}
/* harmony default export */ var esm_browser_v4 = (v4);

;// ./modules/components/base.js
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _regeneratorRuntime() { "use strict"; /*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/facebook/regenerator/blob/main/LICENSE */ _regeneratorRuntime = function _regeneratorRuntime() { return e; }; var t, e = {}, r = Object.prototype, n = r.hasOwnProperty, o = Object.defineProperty || function (t, e, r) { t[e] = r.value; }, i = "function" == typeof Symbol ? Symbol : {}, a = i.iterator || "@@iterator", c = i.asyncIterator || "@@asyncIterator", u = i.toStringTag || "@@toStringTag"; function define(t, e, r) { return Object.defineProperty(t, e, { value: r, enumerable: !0, configurable: !0, writable: !0 }), t[e]; } try { define({}, ""); } catch (t) { define = function define(t, e, r) { return t[e] = r; }; } function wrap(t, e, r, n) { var i = e && e.prototype instanceof Generator ? e : Generator, a = Object.create(i.prototype), c = new Context(n || []); return o(a, "_invoke", { value: makeInvokeMethod(t, r, c) }), a; } function tryCatch(t, e, r) { try { return { type: "normal", arg: t.call(e, r) }; } catch (t) { return { type: "throw", arg: t }; } } e.wrap = wrap; var h = "suspendedStart", l = "suspendedYield", f = "executing", s = "completed", y = {}; function Generator() {} function GeneratorFunction() {} function GeneratorFunctionPrototype() {} var p = {}; define(p, a, function () { return this; }); var d = Object.getPrototypeOf, v = d && d(d(values([]))); v && v !== r && n.call(v, a) && (p = v); var g = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(p); function defineIteratorMethods(t) { ["next", "throw", "return"].forEach(function (e) { define(t, e, function (t) { return this._invoke(e, t); }); }); } function AsyncIterator(t, e) { function invoke(r, o, i, a) { var c = tryCatch(t[r], t, o); if ("throw" !== c.type) { var u = c.arg, h = u.value; return h && "object" == _typeof(h) && n.call(h, "__await") ? e.resolve(h.__await).then(function (t) { invoke("next", t, i, a); }, function (t) { invoke("throw", t, i, a); }) : e.resolve(h).then(function (t) { u.value = t, i(u); }, function (t) { return invoke("throw", t, i, a); }); } a(c.arg); } var r; o(this, "_invoke", { value: function value(t, n) { function callInvokeWithMethodAndArg() { return new e(function (e, r) { invoke(t, n, e, r); }); } return r = r ? r.then(callInvokeWithMethodAndArg, callInvokeWithMethodAndArg) : callInvokeWithMethodAndArg(); } }); } function makeInvokeMethod(e, r, n) { var o = h; return function (i, a) { if (o === f) throw Error("Generator is already running"); if (o === s) { if ("throw" === i) throw a; return { value: t, done: !0 }; } for (n.method = i, n.arg = a;;) { var c = n.delegate; if (c) { var u = maybeInvokeDelegate(c, n); if (u) { if (u === y) continue; return u; } } if ("next" === n.method) n.sent = n._sent = n.arg;else if ("throw" === n.method) { if (o === h) throw o = s, n.arg; n.dispatchException(n.arg); } else "return" === n.method && n.abrupt("return", n.arg); o = f; var p = tryCatch(e, r, n); if ("normal" === p.type) { if (o = n.done ? s : l, p.arg === y) continue; return { value: p.arg, done: n.done }; } "throw" === p.type && (o = s, n.method = "throw", n.arg = p.arg); } }; } function maybeInvokeDelegate(e, r) { var n = r.method, o = e.iterator[n]; if (o === t) return r.delegate = null, "throw" === n && e.iterator.return && (r.method = "return", r.arg = t, maybeInvokeDelegate(e, r), "throw" === r.method) || "return" !== n && (r.method = "throw", r.arg = new TypeError("The iterator does not provide a '" + n + "' method")), y; var i = tryCatch(o, e.iterator, r.arg); if ("throw" === i.type) return r.method = "throw", r.arg = i.arg, r.delegate = null, y; var a = i.arg; return a ? a.done ? (r[e.resultName] = a.value, r.next = e.nextLoc, "return" !== r.method && (r.method = "next", r.arg = t), r.delegate = null, y) : a : (r.method = "throw", r.arg = new TypeError("iterator result is not an object"), r.delegate = null, y); } function pushTryEntry(t) { var e = { tryLoc: t[0] }; 1 in t && (e.catchLoc = t[1]), 2 in t && (e.finallyLoc = t[2], e.afterLoc = t[3]), this.tryEntries.push(e); } function resetTryEntry(t) { var e = t.completion || {}; e.type = "normal", delete e.arg, t.completion = e; } function Context(t) { this.tryEntries = [{ tryLoc: "root" }], t.forEach(pushTryEntry, this), this.reset(!0); } function values(e) { if (e || "" === e) { var r = e[a]; if (r) return r.call(e); if ("function" == typeof e.next) return e; if (!isNaN(e.length)) { var o = -1, i = function next() { for (; ++o < e.length;) if (n.call(e, o)) return next.value = e[o], next.done = !1, next; return next.value = t, next.done = !0, next; }; return i.next = i; } } throw new TypeError(_typeof(e) + " is not iterable"); } return GeneratorFunction.prototype = GeneratorFunctionPrototype, o(g, "constructor", { value: GeneratorFunctionPrototype, configurable: !0 }), o(GeneratorFunctionPrototype, "constructor", { value: GeneratorFunction, configurable: !0 }), GeneratorFunction.displayName = define(GeneratorFunctionPrototype, u, "GeneratorFunction"), e.isGeneratorFunction = function (t) { var e = "function" == typeof t && t.constructor; return !!e && (e === GeneratorFunction || "GeneratorFunction" === (e.displayName || e.name)); }, e.mark = function (t) { return Object.setPrototypeOf ? Object.setPrototypeOf(t, GeneratorFunctionPrototype) : (t.__proto__ = GeneratorFunctionPrototype, define(t, u, "GeneratorFunction")), t.prototype = Object.create(g), t; }, e.awrap = function (t) { return { __await: t }; }, defineIteratorMethods(AsyncIterator.prototype), define(AsyncIterator.prototype, c, function () { return this; }), e.AsyncIterator = AsyncIterator, e.async = function (t, r, n, o, i) { void 0 === i && (i = Promise); var a = new AsyncIterator(wrap(t, r, n, o), i); return e.isGeneratorFunction(r) ? a : a.next().then(function (t) { return t.done ? t.value : a.next(); }); }, defineIteratorMethods(g), define(g, u, "Generator"), define(g, a, function () { return this; }), define(g, "toString", function () { return "[object Generator]"; }), e.keys = function (t) { var e = Object(t), r = []; for (var n in e) r.push(n); return r.reverse(), function next() { for (; r.length;) { var t = r.pop(); if (t in e) return next.value = t, next.done = !1, next; } return next.done = !0, next; }; }, e.values = values, Context.prototype = { constructor: Context, reset: function reset(e) { if (this.prev = 0, this.next = 0, this.sent = this._sent = t, this.done = !1, this.delegate = null, this.method = "next", this.arg = t, this.tryEntries.forEach(resetTryEntry), !e) for (var r in this) "t" === r.charAt(0) && n.call(this, r) && !isNaN(+r.slice(1)) && (this[r] = t); }, stop: function stop() { this.done = !0; var t = this.tryEntries[0].completion; if ("throw" === t.type) throw t.arg; return this.rval; }, dispatchException: function dispatchException(e) { if (this.done) throw e; var r = this; function handle(n, o) { return a.type = "throw", a.arg = e, r.next = n, o && (r.method = "next", r.arg = t), !!o; } for (var o = this.tryEntries.length - 1; o >= 0; --o) { var i = this.tryEntries[o], a = i.completion; if ("root" === i.tryLoc) return handle("end"); if (i.tryLoc <= this.prev) { var c = n.call(i, "catchLoc"), u = n.call(i, "finallyLoc"); if (c && u) { if (this.prev < i.catchLoc) return handle(i.catchLoc, !0); if (this.prev < i.finallyLoc) return handle(i.finallyLoc); } else if (c) { if (this.prev < i.catchLoc) return handle(i.catchLoc, !0); } else { if (!u) throw Error("try statement without catch or finally"); if (this.prev < i.finallyLoc) return handle(i.finallyLoc); } } } }, abrupt: function abrupt(t, e) { for (var r = this.tryEntries.length - 1; r >= 0; --r) { var o = this.tryEntries[r]; if (o.tryLoc <= this.prev && n.call(o, "finallyLoc") && this.prev < o.finallyLoc) { var i = o; break; } } i && ("break" === t || "continue" === t) && i.tryLoc <= e && e <= i.finallyLoc && (i = null); var a = i ? i.completion : {}; return a.type = t, a.arg = e, i ? (this.method = "next", this.next = i.finallyLoc, y) : this.complete(a); }, complete: function complete(t, e) { if ("throw" === t.type) throw t.arg; return "break" === t.type || "continue" === t.type ? this.next = t.arg : "return" === t.type ? (this.rval = this.arg = t.arg, this.method = "return", this.next = "end") : "normal" === t.type && e && (this.next = e), y; }, finish: function finish(t) { for (var e = this.tryEntries.length - 1; e >= 0; --e) { var r = this.tryEntries[e]; if (r.finallyLoc === t) return this.complete(r.completion, r.afterLoc), resetTryEntry(r), y; } }, catch: function _catch(t) { for (var e = this.tryEntries.length - 1; e >= 0; --e) { var r = this.tryEntries[e]; if (r.tryLoc === t) { var n = r.completion; if ("throw" === n.type) { var o = n.arg; resetTryEntry(r); } return o; } } throw Error("illegal catch attempt"); }, delegateYield: function delegateYield(e, r, n) { return this.delegate = { iterator: values(e), resultName: r, nextLoc: n }, "next" === this.method && (this.arg = t), y; } }, e; }
function asyncGeneratorStep(n, t, e, r, o, a, c) { try { var i = n[a](c), u = i.value; } catch (n) { return void e(n); } i.done ? t(u) : Promise.resolve(u).then(r, o); }
function _asyncToGenerator(n) { return function () { var t = this, e = arguments; return new Promise(function (r, o) { var a = n.apply(t, e); function _next(n) { asyncGeneratorStep(a, r, o, _next, _throw, "next", n); } function _throw(n) { asyncGeneratorStep(a, r, o, _next, _throw, "throw", n); } _next(void 0); }); }; }







function EnderecoBase() {
  return {
    id: esm_browser_v4(),
    config: {
      agentName: "DefaultAgent v1.0.0",
      showDebugInfo: true,
      lang: 'de',
      splitStreet: true,
      useAutocomplete: true,
      ux: {
        smartFill: false,
        smartFillBlockTime: 600,
        resumeSubmit: true,
        disableBrowserAutocomplete: true,
        maxAutocompletePredictionItems: 100,
        maxAddressPredictionItems: 3,
        delay: {
          inputAssistant: 100,
          streetCopy: 600,
          onBlur: 300
        },
        requestTimeout: 8000,
        errorInsertMode: 'afterbegin',
        errorContainer: undefined
      },
      trigger: {
        onblur: true,
        onsubmit: true
      },
      templates: {
        default: ''
      },
      texts: {
        default: ''
      }
    },
    type: 'base',
    name: 'default',
    fullName: 'default',
    forms: [],
    formsWithSession: [],
    fieldNames: [],
    states: {},
    active: false,
    activate: function activate() {
      this.active = true;
    },
    deactivate: function deactivate() {
      this.active = false;
    },
    _changed: false,
    _awaits: 0,
    waitForExtension: function waitForExtension(extensions) {
      var timeout = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 10;
      var $self = this;
      return new src(function (resolve, reject) {
        if ('string' === typeof extensions) {
          extensions = [extensions];
        }
        var intervalObject = setInterval(function () {
          var allSet = true;
          extensions.forEach(function (extensionName) {
            if (!$self.hasLoadedExtension(extensionName)) {
              allSet = false;
            }
          });
          if (allSet) {
            clearTimeout(timeoutObject);
            clearInterval(intervalObject);
            resolve();
          }
        }, 100);
        var timeoutObject = setTimeout(function () {
          clearTimeout(timeoutObject);
          clearInterval(intervalObject);
          console.log('Timeout!');
          reject();
        }, timeout * 1000); // Transform seconds to milliseconds.
      });
    },
    waitForAllExtension: function waitForAllExtension() {
      var timeout = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 10;
      var $self = this;
      this._awaits++;
      return new src(function (resolve, reject) {
        var intervalObject = setInterval(function () {
          if (Object.keys($self.loadedExtensions).length === $self.extensions.length) {
            clearTimeout(timeoutObject);
            clearInterval(intervalObject);
            $self._awaits--;
            resolve($self);
          }
        }, 100);
        var timeoutObject = setTimeout(function () {
          clearTimeout(timeoutObject);
          clearInterval(intervalObject);
          console.log('Timeout!');
          $self._awaits--;
          reject($self);
        }, timeout * 1000); // Transform seconds to milliseconds.
      });
    },
    waitForActive: function waitForActive() {
      var $self = this;
      return new src(function (resolve, reject) {
        var intervalObject = setInterval(function () {
          if ($self.active) {
            clearInterval(intervalObject);
            resolve();
          }
        }, 100);
      });
    },
    waitUntilReady: function waitUntilReady() {
      var $self = this;
      return new src(function (resolve, reject) {
        var waitForAwaits = setInterval(function () {
          if (0 === $self._awaits) {
            clearInterval(waitForAwaits);
            resolve();
          }
        }, 100);
      });
    },
    anyActive: function anyActive() {
      var $self = this;
      var hasAny = false;
      $self.fieldNames.forEach(function (fieldName) {
        if ($self._subscribers[fieldName]) {
          $self._subscribers[fieldName].forEach(function (subscriber) {
            if (null !== subscriber.object.offsetParent && document.activeElement === subscriber.object) {
              hasAny = true;
            }
          });
        }
      });
      if (!!window.EnderecoIntegrator.$globalFilters && !!window.EnderecoIntegrator.$globalFilters.anyActive) {
        window.EnderecoIntegrator.$globalFilters.anyActive.forEach(function (callback) {
          hasAny = callback(hasAny, $self);
        });
      }
      return hasAny;
    },
    anyMissing: function anyMissing() {
      var $self = this;
      var hasAny = false;
      hasAny = false;
      $self.forms.forEach(function (form) {
        if (form.isConnected) {
          hasAny = true;
        }
      });
      if (!!window.EnderecoIntegrator.$globalFilters && !!window.EnderecoIntegrator.$globalFilters.anyMissing) {
        window.EnderecoIntegrator.$globalFilters.anyMissing.forEach(function (callback) {
          hasAny = callback(hasAny, $self);
        });
      }
      return !hasAny;
    },
    onAfterCreate: [],
    created: function created() {
      var $self = this;
      this.onAfterCreate.forEach(function (cb) {
        cb($self);
      });
    },
    onCloseModal: [],
    modalClosed: function modalClosed() {
      var $self = this;
      this.onCloseModal.forEach(function (cb) {
        cb($self);
      });
    },
    onAddressSelect: [],
    addressSelected: function addressSelected() {
      var $self = this;
      this.onAddressSelect.forEach(function (cb) {
        cb($self);
      });
    },
    onBeforeSubmitResume: [],
    beforeSubmitResume: function beforeSubmitResume() {
      var $self = this;
      this.onBeforeSubmitResume.forEach(function (cb) {
        cb($self);
      });
    },
    cb: {
      onFormSubmit: function onFormSubmit(e) {
        // Do nothing.
      }
    },
    util: {
      axios: lib_axios,
      Promise: src,
      Mustache: mustache_mustache,
      levenstein: (levenshtein_default()),
      merge: (lodash_merge_default()),
      isEqual: (lodash_isequal_default()),
      CustomEvent: CustomEvent,
      generateId: function generateId() {
        return esm_browser_v4();
      }
    },
    api: {},
    getEnderecoAPI: function getEnderecoAPI() {
      var $self = this;
      return {
        sendRequestToAPI: function () {
          var _sendRequestToAPI = _asyncToGenerator(/*#__PURE__*/_regeneratorRuntime().mark(function _callee(body, headers) {
            return _regeneratorRuntime().wrap(function _callee$(_context) {
              while (1) switch (_context.prev = _context.next) {
                case 0:
                  return _context.abrupt("return", $self.util.axios.post($self.config.apiUrl, body, {
                    timeout: $self.config.ux.requestTimeout,
                    headers: headers
                  }));
                case 1:
                case "end":
                  return _context.stop();
              }
            }, _callee);
          }));
          function sendRequestToAPI(_x, _x2) {
            return _sendRequestToAPI.apply(this, arguments);
          }
          return sendRequestToAPI;
        }()
      };
    },
    // Event Handling.
    _subscribers: {},
    addSubscriber: function addSubscriber(EnderecoSubscriberObject) {
      var $self = this;
      $self._awaits++;
      this.waitForAllExtension().then(function () {
        if (!EnderecoSubscriberObject) {
          console.log('No EnderecoSubscriber');
          return null;
        }
        EnderecoSubscriberObject.subject = $self;
        if (undefined === $self._subscribers[EnderecoSubscriberObject.propertyName]) {
          $self._subscribers[EnderecoSubscriberObject.propertyName] = [];
        }

        // Check if has form. Add to forms.
        if (!!EnderecoSubscriberObject.object && !!EnderecoSubscriberObject.object.form) {
          if (EnderecoSubscriberObject.subject.config.trigger.onsubmit) {
            setTimeout(function () {
              if (!!EnderecoSubscriberObject.object && !!EnderecoSubscriberObject.object.form) {
                EnderecoSubscriberObject.object.form.removeEventListener('submit', $self.cb.onFormSubmit);
                EnderecoSubscriberObject.object.form.addEventListener('submit', $self.cb.onFormSubmit);
              }
            }, 1000);
          }
          if (undefined !== $self.forms && $self.hasLoadedExtension('SessionExtension') && !$self.forms.includes(EnderecoSubscriberObject.object.form)) {
            $self.forms.push(EnderecoSubscriberObject.object.form);
          }
        }
        $self._subscribers[EnderecoSubscriberObject.propertyName].push(EnderecoSubscriberObject);
        if ($self.config.showDebugInfo) {
          console.log('Add Subscriber ', EnderecoSubscriberObject);
        }
      }).catch().finally(function () {
        $self._awaits--;
      });
    },
    removeSubscriber: function removeSubscriber(EnderecoSubscriber) {
      // TODO: remove subscriber from subscribers list.
    },
    syncValues: function syncValues(specificKeys) {
      var $self = this;
      return new src(function (resolve, reject) {
        var keys = specificKeys || Object.keys($self._subscribers);
        keys.forEach(function (key) {
          if ($self._subscribers[key]) {
            $self._subscribers[key].forEach(function (subscriber) {
              if (subscriber._subject) {
                subscriber._subject._awaits++;
              }
              // Sync values.
              $self.util.Promise.resolve(subscriber.value).then(function (subscriberValue) {
                var innerValue = $self[key];
                if (Array.isArray(innerValue)) {
                  innerValue = innerValue.join();
                }
                var innerValueEmpty = !innerValue && innerValue !== 0;
                var subscriberValueEmpty = !subscriberValue && subscriberValue !== 0;
                if (!subscriberValueEmpty && innerValueEmpty) {
                  $self[key] = subscriberValue;
                }
                if (subscriberValueEmpty && !innerValueEmpty) {
                  subscriber.value = $self[key];
                }
                if (subscriber._subject) {
                  subscriber._subject._awaits--;
                }
                resolve();
              }).catch(function (e) {
                if ($self.config.showDebugInfo) {
                  console.log('Error syncing values', e);
                }
                reject(e);
              });
            });
          }
        });
      });
    },
    setField: function setField(fieldName, fieldValue) {
      var markAsChanged = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : true;
      var $self = this;
      $self._awaits++;
      $self.util.Promise.resolve(fieldValue).then(function (value) {
        // Set inner value.
        $self['_' + fieldName] = fieldValue;
        $self._subscribers[fieldName].forEach(function (subscriber) {
          subscriber.value = value;
        });
        if (markAsChanged) {
          $self._changed = true;
        }
      }).catch().finally(function () {
        $self._awaits--;
      });
    },
    // Event listener block.
    _listeners: {},
    addEventListener: function addEventListener(eventName, listener) {
      var options = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};
      if (undefined === this._listeners[eventName]) {
        this._listeners[eventName] = [];
      }
      this._listeners[eventName].push(listener);
    },
    removeEventListener: function removeEventListener(eventName, listener) {
      var options = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};
    } // TODO: remove callback function from listeners array.
    ,
    // Extensions block.
    extensions: [],
    loadedExtensions: {},
    loadExtensions: function loadExtensions() {
      var $self = this;
      this.extensions.forEach(function (extension) {
        if (!$self.loadedExtensions[extension.name]) {
          $self._awaits++;
          extension.extend($self).then(function (extension) {
            $self.loadedExtensions[extension.name] = extension;
          }).catch(function (e) {
            if ($self.config.showDebugInfo) {
              console.log('Failed to load extension', e, extension);
            }
          }).finally(function () {
            $self._awaits--;
          });
        }
      });
    },
    hasLoadedExtension: function hasLoadedExtension(extensionName) {
      return undefined !== this.loadedExtensions[extensionName];
    },
    // Event management.
    fire: function fire(event) {
      if (this.config.showDebugInfo) {
        console.log('Fire', event);
      }
      if (undefined !== this._listeners[event.type]) {
        this._listeners[event.type].forEach(function (listener) {
          listener(event.detail);
        });
      }
    },
    trigger: function trigger(eventName, eventArgs) {}
  };
}
/* harmony default export */ var components_base = (EnderecoBase);
;// ./modules/extensions/fields/CountryCodeExtension.js
var CountryCodeExtension = {
  name: 'CountryCodeExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      // Add _address, _addressStatus.
      ExtendableObject._countryCode = ''; // Germany is a preselected default.
      ExtendableObject._subscribers.countryCode = [];

      // Add a setter callback promise.
      ExtendableObject.cb.setCountryCode = function (countryCode) {
        return new ExtendableObject.util.Promise(function (resolve, reject) {
          resolve(countryCode);
        });
      };
      ExtendableObject.cb.countryCodeChange = function (subscriber) {
        return function (e) {
          ExtendableObject.countryCode = subscriber.value;
          if (ExtendableObject.hasLoadedExtension('SubdivisionCodeExtension')) {
            if (0 < ExtendableObject._subscribers.subdivisionCode.length) {
              ExtendableObject._subdivisionCode = '';
              ExtendableObject._subscribers.subdivisionCode.forEach(function (subscriber) {
                subscriber.value = '';
              });
            }
          }
          if (ExtendableObject.active) {
            ExtendableObject._changed = true;
          }
        };
      };
      ExtendableObject.cb.countryCodeInput = function (subscriber) {
        return function (e) {
          ExtendableObject.countryCode = subscriber.value;
          if (ExtendableObject.hasLoadedExtension('SubdivisionCodeExtension')) {
            if (0 < ExtendableObject._subscribers.subdivisionCode.length) {
              ExtendableObject._subdivisionCode = '';
              ExtendableObject._subscribers.subdivisionCode.forEach(function (subscriber) {
                subscriber.value = '';
              });
            }
          }
          if (ExtendableObject.active) {
            ExtendableObject._changed = true;
            ExtendableObject.addressStatus = [];
          }
        };
      };
      ExtendableObject.cb.countryCodeBlur = function (subscriber) {
        return function (e) {
          if (ExtendableObject.type === 'address') {
            ExtendableObject.waitUntilReady().then(function () {
              if (ExtendableObject.onBlurTimeout) {
                clearTimeout(ExtendableObject.onBlurTimeout);
                ExtendableObject.onBlurTimeout = null;
              }
              ExtendableObject.onBlurTimeout = setTimeout(function () {
                if (ExtendableObject.config.trigger.onblur && !ExtendableObject.anyActive() && ExtendableObject.util.shouldBeChecked() && !window.EnderecoIntegrator.hasSubmit) {
                  // Second. Check Address.
                  ExtendableObject.onBlurTimeout = null;
                  ExtendableObject.util.checkAddress().catch();
                }
              }, 300);
            }).catch();
          }
        };
      };

      // Add getter and setter for fields.
      Object.defineProperty(ExtendableObject, 'countryCode', {
        get: function get() {
          return this._countryCode;
        },
        set: function set(value) {
          // value can be a string, array or promise.
          ExtendableObject._awaits++;
          ExtendableObject.util.Promise.resolve(value).then(function (value) {
            ExtendableObject._awaits++;
            ExtendableObject.cb.setCountryCode(value).then(function (value) {
              var oldValue = ExtendableObject._countryCode;
              var newValue = value.toUpperCase(); // Force normalize.
              if (ExtendableObject._countryCode !== value) {
                ExtendableObject._countryCode = value;

                // Inform all subscribers about the change while updating their inner state.
                ExtendableObject._subscribers.countryCode.forEach(function (subscriber) {
                  subscriber.value = value;
                });

                // Fire change event for listeners.
                if (ExtendableObject.active) {
                  ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                    detail: {
                      fieldName: 'countryCode',
                      oldValue: oldValue,
                      newValue: newValue,
                      object: ExtendableObject
                    }
                  }));
                }
              }
            }).catch(function (e) {
              if (ExtendableObject.config.showDebugInfo) {
                console.log('Error setting countryCode', e);
              }
            }).finally(function () {
              ExtendableObject._awaits--;
            });
          }).catch(function (e) {
            if (ExtendableObject.config.showDebugInfo) {
              console.log('Error setting countryCode', e);
            }
          }).finally(function () {
            ExtendableObject._awaits--;
          });
        }
      });
      if (ExtendableObject.config.showDebugInfo) {
        console.log('CountryCodeExtension applied');
      }

      // Add country code to field names list.
      ExtendableObject.fieldNames.push('countryCode');
      resolve($self);
    });
  }
};
/* harmony default export */ var fields_CountryCodeExtension = (CountryCodeExtension);
;// ./modules/extensions/checks/CountryCodeCheckExtension.js
var CountryCodeCheckExtension = {
  name: 'CountryCodeCheckExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      ExtendableObject.waitForExtension('CountryCodeExtension').then(function () {
        // Add email field.
        ExtendableObject._countryCodeStatus = '';
        ExtendableObject._subscribers.countryCodeStatus = [];

        // Add change event hadler.
        ExtendableObject.cb.countryCodeStatusChange = function (subscriber) {
          return function (e) {
            ExtendableObject.countryCodeStatus = subscriber.value;
          };
        };

        // Add the "emaiL" property
        Object.defineProperty(ExtendableObject, 'countryCodeStatus', {
          get: function get() {
            return this._countryCodeStatus;
          },
          set: function set(value) {
            var oldValue = ExtendableObject._countryCodeStatus;
            ExtendableObject._awaits++;
            ExtendableObject.util.Promise.resolve(value).then(function (value) {
              var newValue = value;
              if (oldValue !== newValue) {
                ExtendableObject._countryCodeStatus = newValue;

                // Inform all subscribers about the change.
                ExtendableObject._subscribers.countryCodeStatus.forEach(function (subscriber) {
                  subscriber.value = value;
                });
                ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                  detail: {
                    fieldName: 'countryCodeStatus',
                    oldValue: oldValue,
                    newValue: newValue,
                    object: ExtendableObject
                  }
                }));
              }
            }).catch().finally(function () {
              ExtendableObject._awaits--;
            });
          }
        });
        if (ExtendableObject.config.showDebugInfo) {
          console.log('CountryCodeCheckExtension applied');
        }
        resolve($self);
      }).catch(function () {
        console.log('Failed to load because of timeout');
        reject($self);
      });
    });
  }
};
/* harmony default export */ var checks_CountryCodeCheckExtension = (CountryCodeCheckExtension);
;// ./modules/extensions/fields/SubdivisionCodeExtension.js
var SubdivisionCodeExtension = {
  name: 'SubdivisionCodeExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      // Add _address, _addressStatus.
      ExtendableObject._subdivisionCode = ''; // Germany is a preselected default.
      ExtendableObject._subscribers.subdivisionCode = [];
      ExtendableObject._localSubdivisionCodeState = 1;
      ExtendableObject._globalSubdivisionCodeState = 1;

      // Add a setter callback promise.
      ExtendableObject.cb.setSubdivisionCode = function (countryCode) {
        return new ExtendableObject.util.Promise(function (resolve, reject) {
          resolve(countryCode);
        });
      };
      ExtendableObject.cb.subdivisionCodeChange = function (subscriber) {
        return function (e) {
          ExtendableObject.subdivisionCode = subscriber.value;
          if (ExtendableObject.active) {
            ExtendableObject._changed = true;
          }
        };
      };
      ExtendableObject.cb.subdivisionCodeInput = function (subscriber) {
        return function (e) {
          ExtendableObject.subdivisionCode = subscriber.value;
          if (ExtendableObject.active) {
            ExtendableObject._changed = true;
            ExtendableObject.addressStatus = [];
          }
        };
      };
      ExtendableObject.cb.subdivisionCodeBlur = function (subscriber) {
        return function (e) {
          ExtendableObject.waitUntilReady().then(function () {
            if (ExtendableObject.onBlurTimeout) {
              clearTimeout(ExtendableObject.onBlurTimeout);
              ExtendableObject.onBlurTimeout = null;
            }
            ExtendableObject.onBlurTimeout = setTimeout(function () {
              if (ExtendableObject.config.trigger.onblur && !ExtendableObject.anyActive() && ExtendableObject.util.shouldBeChecked() && !window.EnderecoIntegrator.hasSubmit) {
                // Second. Check Address.
                ExtendableObject.onBlurTimeout = null;
                ExtendableObject.util.checkAddress().catch();
              }
            }, 300);
          }).catch();
        };
      };

      // Add getter and setter for fields.
      Object.defineProperty(ExtendableObject, 'subdivisionCode', {
        get: function get() {
          return this._subdivisionCode;
        },
        set: function set(value) {
          // value can be a string, array or promise.
          ExtendableObject._awaits++;
          ExtendableObject.util.Promise.resolve(value).then(function (value) {
            ExtendableObject._awaits++;
            ExtendableObject.cb.setSubdivisionCode(value).then(function (value) {
              var oldValue = ExtendableObject._subdivisionCode;
              var newValue = value.toUpperCase(); // Force normalize.
              if (ExtendableObject._subdivisionCode !== value) {
                ExtendableObject._subdivisionCode = value;

                // Inform all subscribers about the change while updating their inner state.
                ExtendableObject._subscribers.subdivisionCode.forEach(function (subscriber) {
                  subscriber.value = value;
                });

                // Fire change event for listeners.
                if (ExtendableObject.active) {
                  ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                    detail: {
                      fieldName: 'subdivisionCode',
                      oldValue: oldValue,
                      newValue: newValue,
                      object: ExtendableObject
                    }
                  }));
                }
              }
            }).catch(function (e) {
              if (ExtendableObject.config.showDebugInfo) {
                console.log('Error setting subdivisionCode', e);
              }
            }).finally(function () {
              ExtendableObject._awaits--;
            });
          }).catch(function (e) {
            if (ExtendableObject.config.showDebugInfo) {
              console.log('Error setting subdivisionCode', e);
            }
          }).finally(function () {
            ExtendableObject._awaits--;
          });
        }
      });
      if (ExtendableObject.config.showDebugInfo) {
        console.log('SubdivisionCodeExtension applied');
      }

      // Add country code to field names list.
      ExtendableObject.fieldNames.push('subdivisionCode');
      resolve($self);
    });
  }
};
/* harmony default export */ var fields_SubdivisionCodeExtension = (SubdivisionCodeExtension);
;// ./modules/extensions/checks/SubdivisionCodeCheckExtension.js
var SubdivisionCodeCheckExtension = {
  name: 'SubdivisionCodeCheckExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      ExtendableObject.waitForExtension('SubdivisionCodeExtension').then(function () {
        // Add email field.
        ExtendableObject._subdivisionCodeStatus = '';
        ExtendableObject._subscribers.subdivisionCodeStatus = [];

        // Add change event hadler.
        ExtendableObject.cb.subdivisionCodeStatusChange = function (subscriber) {
          return function (e) {
            ExtendableObject.subdivisionCodeStatus = subscriber.value;
          };
        };

        // Add the "emaiL" property
        Object.defineProperty(ExtendableObject, 'subdivisionCodeStatus', {
          get: function get() {
            return this._subdivisionCodeStatus;
          },
          set: function set(value) {
            var oldValue = ExtendableObject._subdivisionCodeStatus;
            ExtendableObject._awaits++;
            ExtendableObject.util.Promise.resolve(value).then(function (value) {
              var newValue = value;
              if (oldValue !== newValue) {
                ExtendableObject._subdivisionCodeStatus = newValue;

                // Inform all subscribers about the change.
                ExtendableObject._subscribers.subdivisionCodeStatus.forEach(function (subscriber) {
                  subscriber.value = value;
                });
                ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                  detail: {
                    fieldName: 'subdivisionCodeStatus',
                    oldValue: oldValue,
                    newValue: newValue,
                    object: ExtendableObject
                  }
                }));
              }
            }).catch().finally(function () {
              ExtendableObject._awaits--;
            });
          }
        });
        if (ExtendableObject.config.showDebugInfo) {
          console.log('SubdivisionCodeCheckExtension applied');
        }
        resolve($self);
      }).catch(function () {
        console.log('Failed to load because of timeout');
        reject($self);
      });
    });
  }
};
/* harmony default export */ var checks_SubdivisionCodeCheckExtension = (SubdivisionCodeCheckExtension);
;// ./modules/extensions/fields/PostalCodeExtension.js
var PostalCodeExtension = {
  name: 'PostalCodeExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      ExtendableObject._postalCode = '';
      ExtendableObject._subscribers.postalCode = [];
      ExtendableObject.cb.setPostalCode = function (postalCode) {
        return new ExtendableObject.util.Promise(function (resolve, reject) {
          resolve(postalCode);
        });
      };
      ExtendableObject.cb.postalCodeChange = function (subscriber) {
        return function (e) {
          ExtendableObject.postalCode = subscriber.value;
        };
      };
      ExtendableObject.cb.postalCodeBlur = function (subscriber) {
        return function (e) {
          ExtendableObject.waitUntilReady().then(function () {
            if (ExtendableObject.onBlurTimeout) {
              clearTimeout(ExtendableObject.onBlurTimeout);
              ExtendableObject.onBlurTimeout = null;
            }
            ExtendableObject.onBlurTimeout = setTimeout(function () {
              if (ExtendableObject.config.trigger.onblur && !ExtendableObject.anyActive() && ExtendableObject.util.shouldBeChecked() && !window.EnderecoIntegrator.hasSubmit) {
                // Second. Check Address.
                ExtendableObject.onBlurTimeout = null;
                ExtendableObject.util.checkAddress().catch();
              }
            }, 300);
          }).catch();
        };
      };

      // Add getter and setter for fields.
      Object.defineProperty(ExtendableObject, 'postalCode', {
        get: function get() {
          return this._postalCode;
        },
        set: function set(value) {
          ExtendableObject._awaits++;
          ExtendableObject.util.Promise.resolve(value).then(function (value) {
            ExtendableObject._awaits++;
            ExtendableObject.cb.setPostalCode(value).then(function (value) {
              var oldValue = ExtendableObject._postalCode;
              var newValue = value;
              var notSame = ExtendableObject._postalCode !== value;

              // Chunk set.
              if (ExtendableObject.hasLoadedExtension('PostalCodeAutocompleteExtension')) {
                notSame = notSame || ExtendableObject._postalCodeChunk !== value;
              }
              if (notSame) {
                ExtendableObject._postalCode = value;
                if (ExtendableObject.hasLoadedExtension('PostalCodeAutocompleteExtension')) {
                  ExtendableObject._postalCodeChunk = value;

                  // Inform all subscribers about the change.
                  ExtendableObject._subscribers.postalCodeChunk.forEach(function (subscriber) {
                    subscriber.value = value;
                  });
                }

                // Inform all subscribers about the change.
                ExtendableObject._subscribers.postalCode.forEach(function (subscriber) {
                  subscriber.value = value;
                });
                if (ExtendableObject.active) {
                  ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                    detail: {
                      fieldName: 'postalCode',
                      oldValue: oldValue,
                      newValue: newValue,
                      object: ExtendableObject
                    }
                  }));
                }
              }
            }).catch(function (e) {
              if (ExtendableObject.config.showDebugInfo) {
                console.log('Error resolving postalCode', e);
              }
            }).finally(function () {
              ExtendableObject._awaits--;
            });
          }).catch().finally(function () {
            ExtendableObject._awaits--;
          });
        }
      });
      ExtendableObject.fieldNames.push('postalCode');
      if (ExtendableObject.config.showDebugInfo) {
        console.log('PostalCodeExtension applied');
      }
      resolve($self);
    });
  }
};
/* harmony default export */ var fields_PostalCodeExtension = (PostalCodeExtension);
;// ./modules/extensions/checks/PostalCodeCheckExtension.js
var PostalCodeCheckExtension = {
  name: 'PostalCodeCheckExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      ExtendableObject.waitForExtension('PostalCodeExtension').then(function () {
        // Add email field.
        ExtendableObject._postalCodeStatus = '';
        ExtendableObject._subscribers.postalCodeStatus = [];

        // Add change event hadler.
        ExtendableObject.cb.postalCodeStatusChange = function (subscriber) {
          return function (e) {
            ExtendableObject.postalCodeStatus = subscriber.value;
          };
        };

        // Add the "emaiL" property
        Object.defineProperty(ExtendableObject, 'postalCodeStatus', {
          get: function get() {
            return this._postalCodeStatus;
          },
          set: function set(value) {
            var oldValue = ExtendableObject._postalCodeStatus;
            ExtendableObject._awaits++;
            ExtendableObject.util.Promise.resolve(value).then(function (value) {
              var newValue = value;
              if (oldValue !== newValue) {
                ExtendableObject._postalCodeStatus = newValue;

                // Inform all subscribers about the change.
                ExtendableObject._subscribers.postalCodeStatus.forEach(function (subscriber) {
                  subscriber.value = value;
                });
                ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                  detail: {
                    fieldName: 'postalCodeStatus',
                    oldValue: oldValue,
                    newValue: newValue,
                    object: ExtendableObject
                  }
                }));
              }
            }).catch().finally(function () {
              ExtendableObject._awaits--;
            });
          }
        });
        if (ExtendableObject.config.showDebugInfo) {
          console.log('PostalCodeCheckExtension applied');
        }
        resolve($self);
      }).catch(function () {
        console.log('Failed to load because of timeout');
        reject($self);
      });
    });
  }
};
/* harmony default export */ var checks_PostalCodeCheckExtension = (PostalCodeCheckExtension);
;// ./node_modules/diff/lib/index.mjs
function Diff() {}
Diff.prototype = {
  diff: function diff(oldString, newString) {
    var _options$timeout;
    var options = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};
    var callback = options.callback;
    if (typeof options === 'function') {
      callback = options;
      options = {};
    }
    var self = this;
    function done(value) {
      value = self.postProcess(value, options);
      if (callback) {
        setTimeout(function () {
          callback(value);
        }, 0);
        return true;
      } else {
        return value;
      }
    }

    // Allow subclasses to massage the input prior to running
    oldString = this.castInput(oldString, options);
    newString = this.castInput(newString, options);
    oldString = this.removeEmpty(this.tokenize(oldString, options));
    newString = this.removeEmpty(this.tokenize(newString, options));
    var newLen = newString.length,
      oldLen = oldString.length;
    var editLength = 1;
    var maxEditLength = newLen + oldLen;
    if (options.maxEditLength != null) {
      maxEditLength = Math.min(maxEditLength, options.maxEditLength);
    }
    var maxExecutionTime = (_options$timeout = options.timeout) !== null && _options$timeout !== void 0 ? _options$timeout : Infinity;
    var abortAfterTimestamp = Date.now() + maxExecutionTime;
    var bestPath = [{
      oldPos: -1,
      lastComponent: undefined
    }];

    // Seed editLength = 0, i.e. the content starts with the same values
    var newPos = this.extractCommon(bestPath[0], newString, oldString, 0, options);
    if (bestPath[0].oldPos + 1 >= oldLen && newPos + 1 >= newLen) {
      // Identity per the equality and tokenizer
      return done(buildValues(self, bestPath[0].lastComponent, newString, oldString, self.useLongestToken));
    }

    // Once we hit the right edge of the edit graph on some diagonal k, we can
    // definitely reach the end of the edit graph in no more than k edits, so
    // there's no point in considering any moves to diagonal k+1 any more (from
    // which we're guaranteed to need at least k+1 more edits).
    // Similarly, once we've reached the bottom of the edit graph, there's no
    // point considering moves to lower diagonals.
    // We record this fact by setting minDiagonalToConsider and
    // maxDiagonalToConsider to some finite value once we've hit the edge of
    // the edit graph.
    // This optimization is not faithful to the original algorithm presented in
    // Myers's paper, which instead pointlessly extends D-paths off the end of
    // the edit graph - see page 7 of Myers's paper which notes this point
    // explicitly and illustrates it with a diagram. This has major performance
    // implications for some common scenarios. For instance, to compute a diff
    // where the new text simply appends d characters on the end of the
    // original text of length n, the true Myers algorithm will take O(n+d^2)
    // time while this optimization needs only O(n+d) time.
    var minDiagonalToConsider = -Infinity,
      maxDiagonalToConsider = Infinity;

    // Main worker method. checks all permutations of a given edit length for acceptance.
    function execEditLength() {
      for (var diagonalPath = Math.max(minDiagonalToConsider, -editLength); diagonalPath <= Math.min(maxDiagonalToConsider, editLength); diagonalPath += 2) {
        var basePath = void 0;
        var removePath = bestPath[diagonalPath - 1],
          addPath = bestPath[diagonalPath + 1];
        if (removePath) {
          // No one else is going to attempt to use this value, clear it
          bestPath[diagonalPath - 1] = undefined;
        }
        var canAdd = false;
        if (addPath) {
          // what newPos will be after we do an insertion:
          var addPathNewPos = addPath.oldPos - diagonalPath;
          canAdd = addPath && 0 <= addPathNewPos && addPathNewPos < newLen;
        }
        var canRemove = removePath && removePath.oldPos + 1 < oldLen;
        if (!canAdd && !canRemove) {
          // If this path is a terminal then prune
          bestPath[diagonalPath] = undefined;
          continue;
        }

        // Select the diagonal that we want to branch from. We select the prior
        // path whose position in the old string is the farthest from the origin
        // and does not pass the bounds of the diff graph
        if (!canRemove || canAdd && removePath.oldPos < addPath.oldPos) {
          basePath = self.addToPath(addPath, true, false, 0, options);
        } else {
          basePath = self.addToPath(removePath, false, true, 1, options);
        }
        newPos = self.extractCommon(basePath, newString, oldString, diagonalPath, options);
        if (basePath.oldPos + 1 >= oldLen && newPos + 1 >= newLen) {
          // If we have hit the end of both strings, then we are done
          return done(buildValues(self, basePath.lastComponent, newString, oldString, self.useLongestToken));
        } else {
          bestPath[diagonalPath] = basePath;
          if (basePath.oldPos + 1 >= oldLen) {
            maxDiagonalToConsider = Math.min(maxDiagonalToConsider, diagonalPath - 1);
          }
          if (newPos + 1 >= newLen) {
            minDiagonalToConsider = Math.max(minDiagonalToConsider, diagonalPath + 1);
          }
        }
      }
      editLength++;
    }

    // Performs the length of edit iteration. Is a bit fugly as this has to support the
    // sync and async mode which is never fun. Loops over execEditLength until a value
    // is produced, or until the edit length exceeds options.maxEditLength (if given),
    // in which case it will return undefined.
    if (callback) {
      (function exec() {
        setTimeout(function () {
          if (editLength > maxEditLength || Date.now() > abortAfterTimestamp) {
            return callback();
          }
          if (!execEditLength()) {
            exec();
          }
        }, 0);
      })();
    } else {
      while (editLength <= maxEditLength && Date.now() <= abortAfterTimestamp) {
        var ret = execEditLength();
        if (ret) {
          return ret;
        }
      }
    }
  },
  addToPath: function addToPath(path, added, removed, oldPosInc, options) {
    var last = path.lastComponent;
    if (last && !options.oneChangePerToken && last.added === added && last.removed === removed) {
      return {
        oldPos: path.oldPos + oldPosInc,
        lastComponent: {
          count: last.count + 1,
          added: added,
          removed: removed,
          previousComponent: last.previousComponent
        }
      };
    } else {
      return {
        oldPos: path.oldPos + oldPosInc,
        lastComponent: {
          count: 1,
          added: added,
          removed: removed,
          previousComponent: last
        }
      };
    }
  },
  extractCommon: function extractCommon(basePath, newString, oldString, diagonalPath, options) {
    var newLen = newString.length,
      oldLen = oldString.length,
      oldPos = basePath.oldPos,
      newPos = oldPos - diagonalPath,
      commonCount = 0;
    while (newPos + 1 < newLen && oldPos + 1 < oldLen && this.equals(oldString[oldPos + 1], newString[newPos + 1], options)) {
      newPos++;
      oldPos++;
      commonCount++;
      if (options.oneChangePerToken) {
        basePath.lastComponent = {
          count: 1,
          previousComponent: basePath.lastComponent,
          added: false,
          removed: false
        };
      }
    }
    if (commonCount && !options.oneChangePerToken) {
      basePath.lastComponent = {
        count: commonCount,
        previousComponent: basePath.lastComponent,
        added: false,
        removed: false
      };
    }
    basePath.oldPos = oldPos;
    return newPos;
  },
  equals: function equals(left, right, options) {
    if (options.comparator) {
      return options.comparator(left, right);
    } else {
      return left === right || options.ignoreCase && left.toLowerCase() === right.toLowerCase();
    }
  },
  removeEmpty: function removeEmpty(array) {
    var ret = [];
    for (var i = 0; i < array.length; i++) {
      if (array[i]) {
        ret.push(array[i]);
      }
    }
    return ret;
  },
  castInput: function castInput(value) {
    return value;
  },
  tokenize: function tokenize(value) {
    return Array.from(value);
  },
  join: function join(chars) {
    return chars.join('');
  },
  postProcess: function postProcess(changeObjects) {
    return changeObjects;
  }
};
function buildValues(diff, lastComponent, newString, oldString, useLongestToken) {
  // First we convert our linked list of components in reverse order to an
  // array in the right order:
  var components = [];
  var nextComponent;
  while (lastComponent) {
    components.push(lastComponent);
    nextComponent = lastComponent.previousComponent;
    delete lastComponent.previousComponent;
    lastComponent = nextComponent;
  }
  components.reverse();
  var componentPos = 0,
    componentLen = components.length,
    newPos = 0,
    oldPos = 0;
  for (; componentPos < componentLen; componentPos++) {
    var component = components[componentPos];
    if (!component.removed) {
      if (!component.added && useLongestToken) {
        var value = newString.slice(newPos, newPos + component.count);
        value = value.map(function (value, i) {
          var oldValue = oldString[oldPos + i];
          return oldValue.length > value.length ? oldValue : value;
        });
        component.value = diff.join(value);
      } else {
        component.value = diff.join(newString.slice(newPos, newPos + component.count));
      }
      newPos += component.count;

      // Common case
      if (!component.added) {
        oldPos += component.count;
      }
    } else {
      component.value = diff.join(oldString.slice(oldPos, oldPos + component.count));
      oldPos += component.count;
    }
  }
  return components;
}

var characterDiff = new Diff();
function diffChars(oldStr, newStr, options) {
  return characterDiff.diff(oldStr, newStr, options);
}

function longestCommonPrefix(str1, str2) {
  var i;
  for (i = 0; i < str1.length && i < str2.length; i++) {
    if (str1[i] != str2[i]) {
      return str1.slice(0, i);
    }
  }
  return str1.slice(0, i);
}
function longestCommonSuffix(str1, str2) {
  var i;

  // Unlike longestCommonPrefix, we need a special case to handle all scenarios
  // where we return the empty string since str1.slice(-0) will return the
  // entire string.
  if (!str1 || !str2 || str1[str1.length - 1] != str2[str2.length - 1]) {
    return '';
  }
  for (i = 0; i < str1.length && i < str2.length; i++) {
    if (str1[str1.length - (i + 1)] != str2[str2.length - (i + 1)]) {
      return str1.slice(-i);
    }
  }
  return str1.slice(-i);
}
function replacePrefix(string, oldPrefix, newPrefix) {
  if (string.slice(0, oldPrefix.length) != oldPrefix) {
    throw Error("string ".concat(JSON.stringify(string), " doesn't start with prefix ").concat(JSON.stringify(oldPrefix), "; this is a bug"));
  }
  return newPrefix + string.slice(oldPrefix.length);
}
function replaceSuffix(string, oldSuffix, newSuffix) {
  if (!oldSuffix) {
    return string + newSuffix;
  }
  if (string.slice(-oldSuffix.length) != oldSuffix) {
    throw Error("string ".concat(JSON.stringify(string), " doesn't end with suffix ").concat(JSON.stringify(oldSuffix), "; this is a bug"));
  }
  return string.slice(0, -oldSuffix.length) + newSuffix;
}
function removePrefix(string, oldPrefix) {
  return replacePrefix(string, oldPrefix, '');
}
function removeSuffix(string, oldSuffix) {
  return replaceSuffix(string, oldSuffix, '');
}
function maximumOverlap(string1, string2) {
  return string2.slice(0, overlapCount(string1, string2));
}

// Nicked from https://stackoverflow.com/a/60422853/1709587
function overlapCount(a, b) {
  // Deal with cases where the strings differ in length
  var startA = 0;
  if (a.length > b.length) {
    startA = a.length - b.length;
  }
  var endB = b.length;
  if (a.length < b.length) {
    endB = a.length;
  }
  // Create a back-reference for each index
  //   that should be followed in case of a mismatch.
  //   We only need B to make these references:
  var map = Array(endB);
  var k = 0; // Index that lags behind j
  map[0] = 0;
  for (var j = 1; j < endB; j++) {
    if (b[j] == b[k]) {
      map[j] = map[k]; // skip over the same character (optional optimisation)
    } else {
      map[j] = k;
    }
    while (k > 0 && b[j] != b[k]) {
      k = map[k];
    }
    if (b[j] == b[k]) {
      k++;
    }
  }
  // Phase 2: use these references while iterating over A
  k = 0;
  for (var i = startA; i < a.length; i++) {
    while (k > 0 && a[i] != b[k]) {
      k = map[k];
    }
    if (a[i] == b[k]) {
      k++;
    }
  }
  return k;
}

/**
 * Returns true if the string consistently uses Windows line endings.
 */
function hasOnlyWinLineEndings(string) {
  return string.includes('\r\n') && !string.startsWith('\n') && !string.match(/[^\r]\n/);
}

/**
 * Returns true if the string consistently uses Unix line endings.
 */
function hasOnlyUnixLineEndings(string) {
  return !string.includes('\r\n') && string.includes('\n');
}

// Based on https://en.wikipedia.org/wiki/Latin_script_in_Unicode
//
// Ranges and exceptions:
// Latin-1 Supplement, 0080–00FF
//  - U+00D7  × Multiplication sign
//  - U+00F7  ÷ Division sign
// Latin Extended-A, 0100–017F
// Latin Extended-B, 0180–024F
// IPA Extensions, 0250–02AF
// Spacing Modifier Letters, 02B0–02FF
//  - U+02C7  ˇ &#711;  Caron
//  - U+02D8  ˘ &#728;  Breve
//  - U+02D9  ˙ &#729;  Dot Above
//  - U+02DA  ˚ &#730;  Ring Above
//  - U+02DB  ˛ &#731;  Ogonek
//  - U+02DC  ˜ &#732;  Small Tilde
//  - U+02DD  ˝ &#733;  Double Acute Accent
// Latin Extended Additional, 1E00–1EFF
var extendedWordChars = "a-zA-Z0-9_\\u{C0}-\\u{FF}\\u{D8}-\\u{F6}\\u{F8}-\\u{2C6}\\u{2C8}-\\u{2D7}\\u{2DE}-\\u{2FF}\\u{1E00}-\\u{1EFF}";

// Each token is one of the following:
// - A punctuation mark plus the surrounding whitespace
// - A word plus the surrounding whitespace
// - Pure whitespace (but only in the special case where this the entire text
//   is just whitespace)
//
// We have to include surrounding whitespace in the tokens because the two
// alternative approaches produce horribly broken results:
// * If we just discard the whitespace, we can't fully reproduce the original
//   text from the sequence of tokens and any attempt to render the diff will
//   get the whitespace wrong.
// * If we have separate tokens for whitespace, then in a typical text every
//   second token will be a single space character. But this often results in
//   the optimal diff between two texts being a perverse one that preserves
//   the spaces between words but deletes and reinserts actual common words.
//   See https://github.com/kpdecker/jsdiff/issues/160#issuecomment-1866099640
//   for an example.
//
// Keeping the surrounding whitespace of course has implications for .equals
// and .join, not just .tokenize.

// This regex does NOT fully implement the tokenization rules described above.
// Instead, it gives runs of whitespace their own "token". The tokenize method
// then handles stitching whitespace tokens onto adjacent word or punctuation
// tokens.
var tokenizeIncludingWhitespace = new RegExp("[".concat(extendedWordChars, "]+|\\s+|[^").concat(extendedWordChars, "]"), 'ug');
var wordDiff = new Diff();
wordDiff.equals = function (left, right, options) {
  if (options.ignoreCase) {
    left = left.toLowerCase();
    right = right.toLowerCase();
  }
  return left.trim() === right.trim();
};
wordDiff.tokenize = function (value) {
  var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
  var parts;
  if (options.intlSegmenter) {
    if (options.intlSegmenter.resolvedOptions().granularity != 'word') {
      throw new Error('The segmenter passed must have a granularity of "word"');
    }
    parts = Array.from(options.intlSegmenter.segment(value), function (segment) {
      return segment.segment;
    });
  } else {
    parts = value.match(tokenizeIncludingWhitespace) || [];
  }
  var tokens = [];
  var prevPart = null;
  parts.forEach(function (part) {
    if (/\s/.test(part)) {
      if (prevPart == null) {
        tokens.push(part);
      } else {
        tokens.push(tokens.pop() + part);
      }
    } else if (/\s/.test(prevPart)) {
      if (tokens[tokens.length - 1] == prevPart) {
        tokens.push(tokens.pop() + part);
      } else {
        tokens.push(prevPart + part);
      }
    } else {
      tokens.push(part);
    }
    prevPart = part;
  });
  return tokens;
};
wordDiff.join = function (tokens) {
  // Tokens being joined here will always have appeared consecutively in the
  // same text, so we can simply strip off the leading whitespace from all the
  // tokens except the first (and except any whitespace-only tokens - but such
  // a token will always be the first and only token anyway) and then join them
  // and the whitespace around words and punctuation will end up correct.
  return tokens.map(function (token, i) {
    if (i == 0) {
      return token;
    } else {
      return token.replace(/^\s+/, '');
    }
  }).join('');
};
wordDiff.postProcess = function (changes, options) {
  if (!changes || options.oneChangePerToken) {
    return changes;
  }
  var lastKeep = null;
  // Change objects representing any insertion or deletion since the last
  // "keep" change object. There can be at most one of each.
  var insertion = null;
  var deletion = null;
  changes.forEach(function (change) {
    if (change.added) {
      insertion = change;
    } else if (change.removed) {
      deletion = change;
    } else {
      if (insertion || deletion) {
        // May be false at start of text
        dedupeWhitespaceInChangeObjects(lastKeep, deletion, insertion, change);
      }
      lastKeep = change;
      insertion = null;
      deletion = null;
    }
  });
  if (insertion || deletion) {
    dedupeWhitespaceInChangeObjects(lastKeep, deletion, insertion, null);
  }
  return changes;
};
function diffWords(oldStr, newStr, options) {
  // This option has never been documented and never will be (it's clearer to
  // just call `diffWordsWithSpace` directly if you need that behavior), but
  // has existed in jsdiff for a long time, so we retain support for it here
  // for the sake of backwards compatibility.
  if ((options === null || options === void 0 ? void 0 : options.ignoreWhitespace) != null && !options.ignoreWhitespace) {
    return diffWordsWithSpace(oldStr, newStr, options);
  }
  return wordDiff.diff(oldStr, newStr, options);
}
function dedupeWhitespaceInChangeObjects(startKeep, deletion, insertion, endKeep) {
  // Before returning, we tidy up the leading and trailing whitespace of the
  // change objects to eliminate cases where trailing whitespace in one object
  // is repeated as leading whitespace in the next.
  // Below are examples of the outcomes we want here to explain the code.
  // I=insert, K=keep, D=delete
  // 1. diffing 'foo bar baz' vs 'foo baz'
  //    Prior to cleanup, we have K:'foo ' D:' bar ' K:' baz'
  //    After cleanup, we want:   K:'foo ' D:'bar ' K:'baz'
  //
  // 2. Diffing 'foo bar baz' vs 'foo qux baz'
  //    Prior to cleanup, we have K:'foo ' D:' bar ' I:' qux ' K:' baz'
  //    After cleanup, we want K:'foo ' D:'bar' I:'qux' K:' baz'
  //
  // 3. Diffing 'foo\nbar baz' vs 'foo baz'
  //    Prior to cleanup, we have K:'foo ' D:'\nbar ' K:' baz'
  //    After cleanup, we want K'foo' D:'\nbar' K:' baz'
  //
  // 4. Diffing 'foo baz' vs 'foo\nbar baz'
  //    Prior to cleanup, we have K:'foo\n' I:'\nbar ' K:' baz'
  //    After cleanup, we ideally want K'foo' I:'\nbar' K:' baz'
  //    but don't actually manage this currently (the pre-cleanup change
  //    objects don't contain enough information to make it possible).
  //
  // 5. Diffing 'foo   bar baz' vs 'foo  baz'
  //    Prior to cleanup, we have K:'foo  ' D:'   bar ' K:'  baz'
  //    After cleanup, we want K:'foo  ' D:' bar ' K:'baz'
  //
  // Our handling is unavoidably imperfect in the case where there's a single
  // indel between keeps and the whitespace has changed. For instance, consider
  // diffing 'foo\tbar\nbaz' vs 'foo baz'. Unless we create an extra change
  // object to represent the insertion of the space character (which isn't even
  // a token), we have no way to avoid losing information about the texts'
  // original whitespace in the result we return. Still, we do our best to
  // output something that will look sensible if we e.g. print it with
  // insertions in green and deletions in red.

  // Between two "keep" change objects (or before the first or after the last
  // change object), we can have either:
  // * A "delete" followed by an "insert"
  // * Just an "insert"
  // * Just a "delete"
  // We handle the three cases separately.
  if (deletion && insertion) {
    var oldWsPrefix = deletion.value.match(/^\s*/)[0];
    var oldWsSuffix = deletion.value.match(/\s*$/)[0];
    var newWsPrefix = insertion.value.match(/^\s*/)[0];
    var newWsSuffix = insertion.value.match(/\s*$/)[0];
    if (startKeep) {
      var commonWsPrefix = longestCommonPrefix(oldWsPrefix, newWsPrefix);
      startKeep.value = replaceSuffix(startKeep.value, newWsPrefix, commonWsPrefix);
      deletion.value = removePrefix(deletion.value, commonWsPrefix);
      insertion.value = removePrefix(insertion.value, commonWsPrefix);
    }
    if (endKeep) {
      var commonWsSuffix = longestCommonSuffix(oldWsSuffix, newWsSuffix);
      endKeep.value = replacePrefix(endKeep.value, newWsSuffix, commonWsSuffix);
      deletion.value = removeSuffix(deletion.value, commonWsSuffix);
      insertion.value = removeSuffix(insertion.value, commonWsSuffix);
    }
  } else if (insertion) {
    // The whitespaces all reflect what was in the new text rather than
    // the old, so we essentially have no information about whitespace
    // insertion or deletion. We just want to dedupe the whitespace.
    // We do that by having each change object keep its trailing
    // whitespace and deleting duplicate leading whitespace where
    // present.
    if (startKeep) {
      insertion.value = insertion.value.replace(/^\s*/, '');
    }
    if (endKeep) {
      endKeep.value = endKeep.value.replace(/^\s*/, '');
    }
    // otherwise we've got a deletion and no insertion
  } else if (startKeep && endKeep) {
    var newWsFull = endKeep.value.match(/^\s*/)[0],
      delWsStart = deletion.value.match(/^\s*/)[0],
      delWsEnd = deletion.value.match(/\s*$/)[0];

    // Any whitespace that comes straight after startKeep in both the old and
    // new texts, assign to startKeep and remove from the deletion.
    var newWsStart = longestCommonPrefix(newWsFull, delWsStart);
    deletion.value = removePrefix(deletion.value, newWsStart);

    // Any whitespace that comes straight before endKeep in both the old and
    // new texts, and hasn't already been assigned to startKeep, assign to
    // endKeep and remove from the deletion.
    var newWsEnd = longestCommonSuffix(removePrefix(newWsFull, newWsStart), delWsEnd);
    deletion.value = removeSuffix(deletion.value, newWsEnd);
    endKeep.value = replacePrefix(endKeep.value, newWsFull, newWsEnd);

    // If there's any whitespace from the new text that HASN'T already been
    // assigned, assign it to the start:
    startKeep.value = replaceSuffix(startKeep.value, newWsFull, newWsFull.slice(0, newWsFull.length - newWsEnd.length));
  } else if (endKeep) {
    // We are at the start of the text. Preserve all the whitespace on
    // endKeep, and just remove whitespace from the end of deletion to the
    // extent that it overlaps with the start of endKeep.
    var endKeepWsPrefix = endKeep.value.match(/^\s*/)[0];
    var deletionWsSuffix = deletion.value.match(/\s*$/)[0];
    var overlap = maximumOverlap(deletionWsSuffix, endKeepWsPrefix);
    deletion.value = removeSuffix(deletion.value, overlap);
  } else if (startKeep) {
    // We are at the END of the text. Preserve all the whitespace on
    // startKeep, and just remove whitespace from the start of deletion to
    // the extent that it overlaps with the end of startKeep.
    var startKeepWsSuffix = startKeep.value.match(/\s*$/)[0];
    var deletionWsPrefix = deletion.value.match(/^\s*/)[0];
    var _overlap = maximumOverlap(startKeepWsSuffix, deletionWsPrefix);
    deletion.value = removePrefix(deletion.value, _overlap);
  }
}
var wordWithSpaceDiff = new Diff();
wordWithSpaceDiff.tokenize = function (value) {
  // Slightly different to the tokenizeIncludingWhitespace regex used above in
  // that this one treats each individual newline as a distinct tokens, rather
  // than merging them into other surrounding whitespace. This was requested
  // in https://github.com/kpdecker/jsdiff/issues/180 &
  //    https://github.com/kpdecker/jsdiff/issues/211
  var regex = new RegExp("(\\r?\\n)|[".concat(extendedWordChars, "]+|[^\\S\\n\\r]+|[^").concat(extendedWordChars, "]"), 'ug');
  return value.match(regex) || [];
};
function diffWordsWithSpace(oldStr, newStr, options) {
  return wordWithSpaceDiff.diff(oldStr, newStr, options);
}

function generateOptions(options, defaults) {
  if (typeof options === 'function') {
    defaults.callback = options;
  } else if (options) {
    for (var name in options) {
      /* istanbul ignore else */
      if (options.hasOwnProperty(name)) {
        defaults[name] = options[name];
      }
    }
  }
  return defaults;
}

var lineDiff = new Diff();
lineDiff.tokenize = function (value, options) {
  if (options.stripTrailingCr) {
    // remove one \r before \n to match GNU diff's --strip-trailing-cr behavior
    value = value.replace(/\r\n/g, '\n');
  }
  var retLines = [],
    linesAndNewlines = value.split(/(\n|\r\n)/);

  // Ignore the final empty token that occurs if the string ends with a new line
  if (!linesAndNewlines[linesAndNewlines.length - 1]) {
    linesAndNewlines.pop();
  }

  // Merge the content and line separators into single tokens
  for (var i = 0; i < linesAndNewlines.length; i++) {
    var line = linesAndNewlines[i];
    if (i % 2 && !options.newlineIsToken) {
      retLines[retLines.length - 1] += line;
    } else {
      retLines.push(line);
    }
  }
  return retLines;
};
lineDiff.equals = function (left, right, options) {
  // If we're ignoring whitespace, we need to normalise lines by stripping
  // whitespace before checking equality. (This has an annoying interaction
  // with newlineIsToken that requires special handling: if newlines get their
  // own token, then we DON'T want to trim the *newline* tokens down to empty
  // strings, since this would cause us to treat whitespace-only line content
  // as equal to a separator between lines, which would be weird and
  // inconsistent with the documented behavior of the options.)
  if (options.ignoreWhitespace) {
    if (!options.newlineIsToken || !left.includes('\n')) {
      left = left.trim();
    }
    if (!options.newlineIsToken || !right.includes('\n')) {
      right = right.trim();
    }
  } else if (options.ignoreNewlineAtEof && !options.newlineIsToken) {
    if (left.endsWith('\n')) {
      left = left.slice(0, -1);
    }
    if (right.endsWith('\n')) {
      right = right.slice(0, -1);
    }
  }
  return Diff.prototype.equals.call(this, left, right, options);
};
function diffLines(oldStr, newStr, callback) {
  return lineDiff.diff(oldStr, newStr, callback);
}

// Kept for backwards compatibility. This is a rather arbitrary wrapper method
// that just calls `diffLines` with `ignoreWhitespace: true`. It's confusing to
// have two ways to do exactly the same thing in the API, so we no longer
// document this one (library users should explicitly use `diffLines` with
// `ignoreWhitespace: true` instead) but we keep it around to maintain
// compatibility with code that used old versions.
function diffTrimmedLines(oldStr, newStr, callback) {
  var options = generateOptions(callback, {
    ignoreWhitespace: true
  });
  return lineDiff.diff(oldStr, newStr, options);
}

var sentenceDiff = new Diff();
sentenceDiff.tokenize = function (value) {
  return value.split(/(\S.+?[.!?])(?=\s+|$)/);
};
function diffSentences(oldStr, newStr, callback) {
  return sentenceDiff.diff(oldStr, newStr, callback);
}

var cssDiff = new Diff();
cssDiff.tokenize = function (value) {
  return value.split(/([{}:;,]|\s+)/);
};
function diffCss(oldStr, newStr, callback) {
  return cssDiff.diff(oldStr, newStr, callback);
}

function ownKeys(e, r) {
  var t = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var o = Object.getOwnPropertySymbols(e);
    r && (o = o.filter(function (r) {
      return Object.getOwnPropertyDescriptor(e, r).enumerable;
    })), t.push.apply(t, o);
  }
  return t;
}
function _objectSpread2(e) {
  for (var r = 1; r < arguments.length; r++) {
    var t = null != arguments[r] ? arguments[r] : {};
    r % 2 ? ownKeys(Object(t), !0).forEach(function (r) {
      _defineProperty(e, r, t[r]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) {
      Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
    });
  }
  return e;
}
function _toPrimitive(t, r) {
  if ("object" != typeof t || !t) return t;
  var e = t[Symbol.toPrimitive];
  if (void 0 !== e) {
    var i = e.call(t, r || "default");
    if ("object" != typeof i) return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return ("string" === r ? String : Number)(t);
}
function _toPropertyKey(t) {
  var i = _toPrimitive(t, "string");
  return "symbol" == typeof i ? i : i + "";
}
function lib_typeof(o) {
  "@babel/helpers - typeof";

  return lib_typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) {
    return typeof o;
  } : function (o) {
    return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
  }, lib_typeof(o);
}
function _defineProperty(obj, key, value) {
  key = _toPropertyKey(key);
  if (key in obj) {
    Object.defineProperty(obj, key, {
      value: value,
      enumerable: true,
      configurable: true,
      writable: true
    });
  } else {
    obj[key] = value;
  }
  return obj;
}
function _toConsumableArray(arr) {
  return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _unsupportedIterableToArray(arr) || _nonIterableSpread();
}
function _arrayWithoutHoles(arr) {
  if (Array.isArray(arr)) return _arrayLikeToArray(arr);
}
function _iterableToArray(iter) {
  if (typeof Symbol !== "undefined" && iter[Symbol.iterator] != null || iter["@@iterator"] != null) return Array.from(iter);
}
function _unsupportedIterableToArray(o, minLen) {
  if (!o) return;
  if (typeof o === "string") return _arrayLikeToArray(o, minLen);
  var n = Object.prototype.toString.call(o).slice(8, -1);
  if (n === "Object" && o.constructor) n = o.constructor.name;
  if (n === "Map" || n === "Set") return Array.from(o);
  if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen);
}
function _arrayLikeToArray(arr, len) {
  if (len == null || len > arr.length) len = arr.length;
  for (var i = 0, arr2 = new Array(len); i < len; i++) arr2[i] = arr[i];
  return arr2;
}
function _nonIterableSpread() {
  throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}

var jsonDiff = new Diff();
// Discriminate between two lines of pretty-printed, serialized JSON where one of them has a
// dangling comma and the other doesn't. Turns out including the dangling comma yields the nicest output:
jsonDiff.useLongestToken = true;
jsonDiff.tokenize = lineDiff.tokenize;
jsonDiff.castInput = function (value, options) {
  var undefinedReplacement = options.undefinedReplacement,
    _options$stringifyRep = options.stringifyReplacer,
    stringifyReplacer = _options$stringifyRep === void 0 ? function (k, v) {
      return typeof v === 'undefined' ? undefinedReplacement : v;
    } : _options$stringifyRep;
  return typeof value === 'string' ? value : JSON.stringify(canonicalize(value, null, null, stringifyReplacer), stringifyReplacer, '  ');
};
jsonDiff.equals = function (left, right, options) {
  return Diff.prototype.equals.call(jsonDiff, left.replace(/,([\r\n])/g, '$1'), right.replace(/,([\r\n])/g, '$1'), options);
};
function diffJson(oldObj, newObj, options) {
  return jsonDiff.diff(oldObj, newObj, options);
}

// This function handles the presence of circular references by bailing out when encountering an
// object that is already on the "stack" of items being processed. Accepts an optional replacer
function canonicalize(obj, stack, replacementStack, replacer, key) {
  stack = stack || [];
  replacementStack = replacementStack || [];
  if (replacer) {
    obj = replacer(key, obj);
  }
  var i;
  for (i = 0; i < stack.length; i += 1) {
    if (stack[i] === obj) {
      return replacementStack[i];
    }
  }
  var canonicalizedObj;
  if ('[object Array]' === Object.prototype.toString.call(obj)) {
    stack.push(obj);
    canonicalizedObj = new Array(obj.length);
    replacementStack.push(canonicalizedObj);
    for (i = 0; i < obj.length; i += 1) {
      canonicalizedObj[i] = canonicalize(obj[i], stack, replacementStack, replacer, key);
    }
    stack.pop();
    replacementStack.pop();
    return canonicalizedObj;
  }
  if (obj && obj.toJSON) {
    obj = obj.toJSON();
  }
  if (lib_typeof(obj) === 'object' && obj !== null) {
    stack.push(obj);
    canonicalizedObj = {};
    replacementStack.push(canonicalizedObj);
    var sortedKeys = [],
      _key;
    for (_key in obj) {
      /* istanbul ignore else */
      if (Object.prototype.hasOwnProperty.call(obj, _key)) {
        sortedKeys.push(_key);
      }
    }
    sortedKeys.sort();
    for (i = 0; i < sortedKeys.length; i += 1) {
      _key = sortedKeys[i];
      canonicalizedObj[_key] = canonicalize(obj[_key], stack, replacementStack, replacer, _key);
    }
    stack.pop();
    replacementStack.pop();
  } else {
    canonicalizedObj = obj;
  }
  return canonicalizedObj;
}

var arrayDiff = new Diff();
arrayDiff.tokenize = function (value) {
  return value.slice();
};
arrayDiff.join = arrayDiff.removeEmpty = function (value) {
  return value;
};
function diffArrays(oldArr, newArr, callback) {
  return arrayDiff.diff(oldArr, newArr, callback);
}

function unixToWin(patch) {
  if (Array.isArray(patch)) {
    return patch.map(unixToWin);
  }
  return _objectSpread2(_objectSpread2({}, patch), {}, {
    hunks: patch.hunks.map(function (hunk) {
      return _objectSpread2(_objectSpread2({}, hunk), {}, {
        lines: hunk.lines.map(function (line, i) {
          var _hunk$lines;
          return line.startsWith('\\') || line.endsWith('\r') || (_hunk$lines = hunk.lines[i + 1]) !== null && _hunk$lines !== void 0 && _hunk$lines.startsWith('\\') ? line : line + '\r';
        })
      });
    })
  });
}
function winToUnix(patch) {
  if (Array.isArray(patch)) {
    return patch.map(winToUnix);
  }
  return _objectSpread2(_objectSpread2({}, patch), {}, {
    hunks: patch.hunks.map(function (hunk) {
      return _objectSpread2(_objectSpread2({}, hunk), {}, {
        lines: hunk.lines.map(function (line) {
          return line.endsWith('\r') ? line.substring(0, line.length - 1) : line;
        })
      });
    })
  });
}

/**
 * Returns true if the patch consistently uses Unix line endings (or only involves one line and has
 * no line endings).
 */
function isUnix(patch) {
  if (!Array.isArray(patch)) {
    patch = [patch];
  }
  return !patch.some(function (index) {
    return index.hunks.some(function (hunk) {
      return hunk.lines.some(function (line) {
        return !line.startsWith('\\') && line.endsWith('\r');
      });
    });
  });
}

/**
 * Returns true if the patch uses Windows line endings and only Windows line endings.
 */
function isWin(patch) {
  if (!Array.isArray(patch)) {
    patch = [patch];
  }
  return patch.some(function (index) {
    return index.hunks.some(function (hunk) {
      return hunk.lines.some(function (line) {
        return line.endsWith('\r');
      });
    });
  }) && patch.every(function (index) {
    return index.hunks.every(function (hunk) {
      return hunk.lines.every(function (line, i) {
        var _hunk$lines2;
        return line.startsWith('\\') || line.endsWith('\r') || ((_hunk$lines2 = hunk.lines[i + 1]) === null || _hunk$lines2 === void 0 ? void 0 : _hunk$lines2.startsWith('\\'));
      });
    });
  });
}

function parsePatch(uniDiff) {
  var diffstr = uniDiff.split(/\n/),
    list = [],
    i = 0;
  function parseIndex() {
    var index = {};
    list.push(index);

    // Parse diff metadata
    while (i < diffstr.length) {
      var line = diffstr[i];

      // File header found, end parsing diff metadata
      if (/^(\-\-\-|\+\+\+|@@)\s/.test(line)) {
        break;
      }

      // Diff index
      var header = /^(?:Index:|diff(?: -r \w+)+)\s+(.+?)\s*$/.exec(line);
      if (header) {
        index.index = header[1];
      }
      i++;
    }

    // Parse file headers if they are defined. Unified diff requires them, but
    // there's no technical issues to have an isolated hunk without file header
    parseFileHeader(index);
    parseFileHeader(index);

    // Parse hunks
    index.hunks = [];
    while (i < diffstr.length) {
      var _line = diffstr[i];
      if (/^(Index:\s|diff\s|\-\-\-\s|\+\+\+\s|===================================================================)/.test(_line)) {
        break;
      } else if (/^@@/.test(_line)) {
        index.hunks.push(parseHunk());
      } else if (_line) {
        throw new Error('Unknown line ' + (i + 1) + ' ' + JSON.stringify(_line));
      } else {
        i++;
      }
    }
  }

  // Parses the --- and +++ headers, if none are found, no lines
  // are consumed.
  function parseFileHeader(index) {
    var fileHeader = /^(---|\+\+\+)\s+(.*)\r?$/.exec(diffstr[i]);
    if (fileHeader) {
      var keyPrefix = fileHeader[1] === '---' ? 'old' : 'new';
      var data = fileHeader[2].split('\t', 2);
      var fileName = data[0].replace(/\\\\/g, '\\');
      if (/^".*"$/.test(fileName)) {
        fileName = fileName.substr(1, fileName.length - 2);
      }
      index[keyPrefix + 'FileName'] = fileName;
      index[keyPrefix + 'Header'] = (data[1] || '').trim();
      i++;
    }
  }

  // Parses a hunk
  // This assumes that we are at the start of a hunk.
  function parseHunk() {
    var chunkHeaderIndex = i,
      chunkHeaderLine = diffstr[i++],
      chunkHeader = chunkHeaderLine.split(/@@ -(\d+)(?:,(\d+))? \+(\d+)(?:,(\d+))? @@/);
    var hunk = {
      oldStart: +chunkHeader[1],
      oldLines: typeof chunkHeader[2] === 'undefined' ? 1 : +chunkHeader[2],
      newStart: +chunkHeader[3],
      newLines: typeof chunkHeader[4] === 'undefined' ? 1 : +chunkHeader[4],
      lines: []
    };

    // Unified Diff Format quirk: If the chunk size is 0,
    // the first number is one lower than one would expect.
    // https://www.artima.com/weblogs/viewpost.jsp?thread=164293
    if (hunk.oldLines === 0) {
      hunk.oldStart += 1;
    }
    if (hunk.newLines === 0) {
      hunk.newStart += 1;
    }
    var addCount = 0,
      removeCount = 0;
    for (; i < diffstr.length && (removeCount < hunk.oldLines || addCount < hunk.newLines || (_diffstr$i = diffstr[i]) !== null && _diffstr$i !== void 0 && _diffstr$i.startsWith('\\')); i++) {
      var _diffstr$i;
      var operation = diffstr[i].length == 0 && i != diffstr.length - 1 ? ' ' : diffstr[i][0];
      if (operation === '+' || operation === '-' || operation === ' ' || operation === '\\') {
        hunk.lines.push(diffstr[i]);
        if (operation === '+') {
          addCount++;
        } else if (operation === '-') {
          removeCount++;
        } else if (operation === ' ') {
          addCount++;
          removeCount++;
        }
      } else {
        throw new Error("Hunk at line ".concat(chunkHeaderIndex + 1, " contained invalid line ").concat(diffstr[i]));
      }
    }

    // Handle the empty block count case
    if (!addCount && hunk.newLines === 1) {
      hunk.newLines = 0;
    }
    if (!removeCount && hunk.oldLines === 1) {
      hunk.oldLines = 0;
    }

    // Perform sanity checking
    if (addCount !== hunk.newLines) {
      throw new Error('Added line count did not match for hunk at line ' + (chunkHeaderIndex + 1));
    }
    if (removeCount !== hunk.oldLines) {
      throw new Error('Removed line count did not match for hunk at line ' + (chunkHeaderIndex + 1));
    }
    return hunk;
  }
  while (i < diffstr.length) {
    parseIndex();
  }
  return list;
}

// Iterator that traverses in the range of [min, max], stepping
// by distance from a given start position. I.e. for [0, 4], with
// start of 2, this will iterate 2, 3, 1, 4, 0.
function distanceIterator (start, minLine, maxLine) {
  var wantForward = true,
    backwardExhausted = false,
    forwardExhausted = false,
    localOffset = 1;
  return function iterator() {
    if (wantForward && !forwardExhausted) {
      if (backwardExhausted) {
        localOffset++;
      } else {
        wantForward = false;
      }

      // Check if trying to fit beyond text length, and if not, check it fits
      // after offset location (or desired location on first iteration)
      if (start + localOffset <= maxLine) {
        return start + localOffset;
      }
      forwardExhausted = true;
    }
    if (!backwardExhausted) {
      if (!forwardExhausted) {
        wantForward = true;
      }

      // Check if trying to fit before text beginning, and if not, check it fits
      // before offset location
      if (minLine <= start - localOffset) {
        return start - localOffset++;
      }
      backwardExhausted = true;
      return iterator();
    }

    // We tried to fit hunk before text beginning and beyond text length, then
    // hunk can't fit on the text. Return undefined
  };
}

function applyPatch(source, uniDiff) {
  var options = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};
  if (typeof uniDiff === 'string') {
    uniDiff = parsePatch(uniDiff);
  }
  if (Array.isArray(uniDiff)) {
    if (uniDiff.length > 1) {
      throw new Error('applyPatch only works with a single input.');
    }
    uniDiff = uniDiff[0];
  }
  if (options.autoConvertLineEndings || options.autoConvertLineEndings == null) {
    if (hasOnlyWinLineEndings(source) && isUnix(uniDiff)) {
      uniDiff = unixToWin(uniDiff);
    } else if (hasOnlyUnixLineEndings(source) && isWin(uniDiff)) {
      uniDiff = winToUnix(uniDiff);
    }
  }

  // Apply the diff to the input
  var lines = source.split('\n'),
    hunks = uniDiff.hunks,
    compareLine = options.compareLine || function (lineNumber, line, operation, patchContent) {
      return line === patchContent;
    },
    fuzzFactor = options.fuzzFactor || 0,
    minLine = 0;
  if (fuzzFactor < 0 || !Number.isInteger(fuzzFactor)) {
    throw new Error('fuzzFactor must be a non-negative integer');
  }

  // Special case for empty patch.
  if (!hunks.length) {
    return source;
  }

  // Before anything else, handle EOFNL insertion/removal. If the patch tells us to make a change
  // to the EOFNL that is redundant/impossible - i.e. to remove a newline that's not there, or add a
  // newline that already exists - then we either return false and fail to apply the patch (if
  // fuzzFactor is 0) or simply ignore the problem and do nothing (if fuzzFactor is >0).
  // If we do need to remove/add a newline at EOF, this will always be in the final hunk:
  var prevLine = '',
    removeEOFNL = false,
    addEOFNL = false;
  for (var i = 0; i < hunks[hunks.length - 1].lines.length; i++) {
    var line = hunks[hunks.length - 1].lines[i];
    if (line[0] == '\\') {
      if (prevLine[0] == '+') {
        removeEOFNL = true;
      } else if (prevLine[0] == '-') {
        addEOFNL = true;
      }
    }
    prevLine = line;
  }
  if (removeEOFNL) {
    if (addEOFNL) {
      // This means the final line gets changed but doesn't have a trailing newline in either the
      // original or patched version. In that case, we do nothing if fuzzFactor > 0, and if
      // fuzzFactor is 0, we simply validate that the source file has no trailing newline.
      if (!fuzzFactor && lines[lines.length - 1] == '') {
        return false;
      }
    } else if (lines[lines.length - 1] == '') {
      lines.pop();
    } else if (!fuzzFactor) {
      return false;
    }
  } else if (addEOFNL) {
    if (lines[lines.length - 1] != '') {
      lines.push('');
    } else if (!fuzzFactor) {
      return false;
    }
  }

  /**
   * Checks if the hunk can be made to fit at the provided location with at most `maxErrors`
   * insertions, substitutions, or deletions, while ensuring also that:
   * - lines deleted in the hunk match exactly, and
   * - wherever an insertion operation or block of insertion operations appears in the hunk, the
   *   immediately preceding and following lines of context match exactly
   *
   * `toPos` should be set such that lines[toPos] is meant to match hunkLines[0].
   *
   * If the hunk can be applied, returns an object with properties `oldLineLastI` and
   * `replacementLines`. Otherwise, returns null.
   */
  function applyHunk(hunkLines, toPos, maxErrors) {
    var hunkLinesI = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : 0;
    var lastContextLineMatched = arguments.length > 4 && arguments[4] !== undefined ? arguments[4] : true;
    var patchedLines = arguments.length > 5 && arguments[5] !== undefined ? arguments[5] : [];
    var patchedLinesLength = arguments.length > 6 && arguments[6] !== undefined ? arguments[6] : 0;
    var nConsecutiveOldContextLines = 0;
    var nextContextLineMustMatch = false;
    for (; hunkLinesI < hunkLines.length; hunkLinesI++) {
      var hunkLine = hunkLines[hunkLinesI],
        operation = hunkLine.length > 0 ? hunkLine[0] : ' ',
        content = hunkLine.length > 0 ? hunkLine.substr(1) : hunkLine;
      if (operation === '-') {
        if (compareLine(toPos + 1, lines[toPos], operation, content)) {
          toPos++;
          nConsecutiveOldContextLines = 0;
        } else {
          if (!maxErrors || lines[toPos] == null) {
            return null;
          }
          patchedLines[patchedLinesLength] = lines[toPos];
          return applyHunk(hunkLines, toPos + 1, maxErrors - 1, hunkLinesI, false, patchedLines, patchedLinesLength + 1);
        }
      }
      if (operation === '+') {
        if (!lastContextLineMatched) {
          return null;
        }
        patchedLines[patchedLinesLength] = content;
        patchedLinesLength++;
        nConsecutiveOldContextLines = 0;
        nextContextLineMustMatch = true;
      }
      if (operation === ' ') {
        nConsecutiveOldContextLines++;
        patchedLines[patchedLinesLength] = lines[toPos];
        if (compareLine(toPos + 1, lines[toPos], operation, content)) {
          patchedLinesLength++;
          lastContextLineMatched = true;
          nextContextLineMustMatch = false;
          toPos++;
        } else {
          if (nextContextLineMustMatch || !maxErrors) {
            return null;
          }

          // Consider 3 possibilities in sequence:
          // 1. lines contains a *substitution* not included in the patch context, or
          // 2. lines contains an *insertion* not included in the patch context, or
          // 3. lines contains a *deletion* not included in the patch context
          // The first two options are of course only possible if the line from lines is non-null -
          // i.e. only option 3 is possible if we've overrun the end of the old file.
          return lines[toPos] && (applyHunk(hunkLines, toPos + 1, maxErrors - 1, hunkLinesI + 1, false, patchedLines, patchedLinesLength + 1) || applyHunk(hunkLines, toPos + 1, maxErrors - 1, hunkLinesI, false, patchedLines, patchedLinesLength + 1)) || applyHunk(hunkLines, toPos, maxErrors - 1, hunkLinesI + 1, false, patchedLines, patchedLinesLength);
        }
      }
    }

    // Before returning, trim any unmodified context lines off the end of patchedLines and reduce
    // toPos (and thus oldLineLastI) accordingly. This allows later hunks to be applied to a region
    // that starts in this hunk's trailing context.
    patchedLinesLength -= nConsecutiveOldContextLines;
    toPos -= nConsecutiveOldContextLines;
    patchedLines.length = patchedLinesLength;
    return {
      patchedLines: patchedLines,
      oldLineLastI: toPos - 1
    };
  }
  var resultLines = [];

  // Search best fit offsets for each hunk based on the previous ones
  var prevHunkOffset = 0;
  for (var _i = 0; _i < hunks.length; _i++) {
    var hunk = hunks[_i];
    var hunkResult = void 0;
    var maxLine = lines.length - hunk.oldLines + fuzzFactor;
    var toPos = void 0;
    for (var maxErrors = 0; maxErrors <= fuzzFactor; maxErrors++) {
      toPos = hunk.oldStart + prevHunkOffset - 1;
      var iterator = distanceIterator(toPos, minLine, maxLine);
      for (; toPos !== undefined; toPos = iterator()) {
        hunkResult = applyHunk(hunk.lines, toPos, maxErrors);
        if (hunkResult) {
          break;
        }
      }
      if (hunkResult) {
        break;
      }
    }
    if (!hunkResult) {
      return false;
    }

    // Copy everything from the end of where we applied the last hunk to the start of this hunk
    for (var _i2 = minLine; _i2 < toPos; _i2++) {
      resultLines.push(lines[_i2]);
    }

    // Add the lines produced by applying the hunk:
    for (var _i3 = 0; _i3 < hunkResult.patchedLines.length; _i3++) {
      var _line = hunkResult.patchedLines[_i3];
      resultLines.push(_line);
    }

    // Set lower text limit to end of the current hunk, so next ones don't try
    // to fit over already patched text
    minLine = hunkResult.oldLineLastI + 1;

    // Note the offset between where the patch said the hunk should've applied and where we
    // applied it, so we can adjust future hunks accordingly:
    prevHunkOffset = toPos + 1 - hunk.oldStart;
  }

  // Copy over the rest of the lines from the old text
  for (var _i4 = minLine; _i4 < lines.length; _i4++) {
    resultLines.push(lines[_i4]);
  }
  return resultLines.join('\n');
}

// Wrapper that supports multiple file patches via callbacks.
function applyPatches(uniDiff, options) {
  if (typeof uniDiff === 'string') {
    uniDiff = parsePatch(uniDiff);
  }
  var currentIndex = 0;
  function processIndex() {
    var index = uniDiff[currentIndex++];
    if (!index) {
      return options.complete();
    }
    options.loadFile(index, function (err, data) {
      if (err) {
        return options.complete(err);
      }
      var updatedContent = applyPatch(data, index, options);
      options.patched(index, updatedContent, function (err) {
        if (err) {
          return options.complete(err);
        }
        processIndex();
      });
    });
  }
  processIndex();
}

function structuredPatch(oldFileName, newFileName, oldStr, newStr, oldHeader, newHeader, options) {
  if (!options) {
    options = {};
  }
  if (typeof options === 'function') {
    options = {
      callback: options
    };
  }
  if (typeof options.context === 'undefined') {
    options.context = 4;
  }
  if (options.newlineIsToken) {
    throw new Error('newlineIsToken may not be used with patch-generation functions, only with diffing functions');
  }
  if (!options.callback) {
    return diffLinesResultToPatch(diffLines(oldStr, newStr, options));
  } else {
    var _options = options,
      _callback = _options.callback;
    diffLines(oldStr, newStr, _objectSpread2(_objectSpread2({}, options), {}, {
      callback: function callback(diff) {
        var patch = diffLinesResultToPatch(diff);
        _callback(patch);
      }
    }));
  }
  function diffLinesResultToPatch(diff) {
    // STEP 1: Build up the patch with no "\ No newline at end of file" lines and with the arrays
    //         of lines containing trailing newline characters. We'll tidy up later...

    if (!diff) {
      return;
    }
    diff.push({
      value: '',
      lines: []
    }); // Append an empty value to make cleanup easier

    function contextLines(lines) {
      return lines.map(function (entry) {
        return ' ' + entry;
      });
    }
    var hunks = [];
    var oldRangeStart = 0,
      newRangeStart = 0,
      curRange = [],
      oldLine = 1,
      newLine = 1;
    var _loop = function _loop() {
      var current = diff[i],
        lines = current.lines || splitLines(current.value);
      current.lines = lines;
      if (current.added || current.removed) {
        var _curRange;
        // If we have previous context, start with that
        if (!oldRangeStart) {
          var prev = diff[i - 1];
          oldRangeStart = oldLine;
          newRangeStart = newLine;
          if (prev) {
            curRange = options.context > 0 ? contextLines(prev.lines.slice(-options.context)) : [];
            oldRangeStart -= curRange.length;
            newRangeStart -= curRange.length;
          }
        }

        // Output our changes
        (_curRange = curRange).push.apply(_curRange, _toConsumableArray(lines.map(function (entry) {
          return (current.added ? '+' : '-') + entry;
        })));

        // Track the updated file position
        if (current.added) {
          newLine += lines.length;
        } else {
          oldLine += lines.length;
        }
      } else {
        // Identical context lines. Track line changes
        if (oldRangeStart) {
          // Close out any changes that have been output (or join overlapping)
          if (lines.length <= options.context * 2 && i < diff.length - 2) {
            var _curRange2;
            // Overlapping
            (_curRange2 = curRange).push.apply(_curRange2, _toConsumableArray(contextLines(lines)));
          } else {
            var _curRange3;
            // end the range and output
            var contextSize = Math.min(lines.length, options.context);
            (_curRange3 = curRange).push.apply(_curRange3, _toConsumableArray(contextLines(lines.slice(0, contextSize))));
            var _hunk = {
              oldStart: oldRangeStart,
              oldLines: oldLine - oldRangeStart + contextSize,
              newStart: newRangeStart,
              newLines: newLine - newRangeStart + contextSize,
              lines: curRange
            };
            hunks.push(_hunk);
            oldRangeStart = 0;
            newRangeStart = 0;
            curRange = [];
          }
        }
        oldLine += lines.length;
        newLine += lines.length;
      }
    };
    for (var i = 0; i < diff.length; i++) {
      _loop();
    }

    // Step 2: eliminate the trailing `\n` from each line of each hunk, and, where needed, add
    //         "\ No newline at end of file".
    for (var _i = 0, _hunks = hunks; _i < _hunks.length; _i++) {
      var hunk = _hunks[_i];
      for (var _i2 = 0; _i2 < hunk.lines.length; _i2++) {
        if (hunk.lines[_i2].endsWith('\n')) {
          hunk.lines[_i2] = hunk.lines[_i2].slice(0, -1);
        } else {
          hunk.lines.splice(_i2 + 1, 0, '\\ No newline at end of file');
          _i2++; // Skip the line we just added, then continue iterating
        }
      }
    }
    return {
      oldFileName: oldFileName,
      newFileName: newFileName,
      oldHeader: oldHeader,
      newHeader: newHeader,
      hunks: hunks
    };
  }
}
function formatPatch(diff) {
  if (Array.isArray(diff)) {
    return diff.map(formatPatch).join('\n');
  }
  var ret = [];
  if (diff.oldFileName == diff.newFileName) {
    ret.push('Index: ' + diff.oldFileName);
  }
  ret.push('===================================================================');
  ret.push('--- ' + diff.oldFileName + (typeof diff.oldHeader === 'undefined' ? '' : '\t' + diff.oldHeader));
  ret.push('+++ ' + diff.newFileName + (typeof diff.newHeader === 'undefined' ? '' : '\t' + diff.newHeader));
  for (var i = 0; i < diff.hunks.length; i++) {
    var hunk = diff.hunks[i];
    // Unified Diff Format quirk: If the chunk size is 0,
    // the first number is one lower than one would expect.
    // https://www.artima.com/weblogs/viewpost.jsp?thread=164293
    if (hunk.oldLines === 0) {
      hunk.oldStart -= 1;
    }
    if (hunk.newLines === 0) {
      hunk.newStart -= 1;
    }
    ret.push('@@ -' + hunk.oldStart + ',' + hunk.oldLines + ' +' + hunk.newStart + ',' + hunk.newLines + ' @@');
    ret.push.apply(ret, hunk.lines);
  }
  return ret.join('\n') + '\n';
}
function createTwoFilesPatch(oldFileName, newFileName, oldStr, newStr, oldHeader, newHeader, options) {
  var _options2;
  if (typeof options === 'function') {
    options = {
      callback: options
    };
  }
  if (!((_options2 = options) !== null && _options2 !== void 0 && _options2.callback)) {
    var patchObj = structuredPatch(oldFileName, newFileName, oldStr, newStr, oldHeader, newHeader, options);
    if (!patchObj) {
      return;
    }
    return formatPatch(patchObj);
  } else {
    var _options3 = options,
      _callback2 = _options3.callback;
    structuredPatch(oldFileName, newFileName, oldStr, newStr, oldHeader, newHeader, _objectSpread2(_objectSpread2({}, options), {}, {
      callback: function callback(patchObj) {
        if (!patchObj) {
          _callback2();
        } else {
          _callback2(formatPatch(patchObj));
        }
      }
    }));
  }
}
function createPatch(fileName, oldStr, newStr, oldHeader, newHeader, options) {
  return createTwoFilesPatch(fileName, fileName, oldStr, newStr, oldHeader, newHeader, options);
}

/**
 * Split `text` into an array of lines, including the trailing newline character (where present)
 */
function splitLines(text) {
  var hasTrailingNl = text.endsWith('\n');
  var result = text.split('\n').map(function (line) {
    return line + '\n';
  });
  if (hasTrailingNl) {
    result.pop();
  } else {
    result.push(result.pop().slice(0, -1));
  }
  return result;
}

function arrayEqual(a, b) {
  if (a.length !== b.length) {
    return false;
  }
  return arrayStartsWith(a, b);
}
function arrayStartsWith(array, start) {
  if (start.length > array.length) {
    return false;
  }
  for (var i = 0; i < start.length; i++) {
    if (start[i] !== array[i]) {
      return false;
    }
  }
  return true;
}

function calcLineCount(hunk) {
  var _calcOldNewLineCount = calcOldNewLineCount(hunk.lines),
    oldLines = _calcOldNewLineCount.oldLines,
    newLines = _calcOldNewLineCount.newLines;
  if (oldLines !== undefined) {
    hunk.oldLines = oldLines;
  } else {
    delete hunk.oldLines;
  }
  if (newLines !== undefined) {
    hunk.newLines = newLines;
  } else {
    delete hunk.newLines;
  }
}
function lib_merge(mine, theirs, base) {
  mine = loadPatch(mine, base);
  theirs = loadPatch(theirs, base);
  var ret = {};

  // For index we just let it pass through as it doesn't have any necessary meaning.
  // Leaving sanity checks on this to the API consumer that may know more about the
  // meaning in their own context.
  if (mine.index || theirs.index) {
    ret.index = mine.index || theirs.index;
  }
  if (mine.newFileName || theirs.newFileName) {
    if (!fileNameChanged(mine)) {
      // No header or no change in ours, use theirs (and ours if theirs does not exist)
      ret.oldFileName = theirs.oldFileName || mine.oldFileName;
      ret.newFileName = theirs.newFileName || mine.newFileName;
      ret.oldHeader = theirs.oldHeader || mine.oldHeader;
      ret.newHeader = theirs.newHeader || mine.newHeader;
    } else if (!fileNameChanged(theirs)) {
      // No header or no change in theirs, use ours
      ret.oldFileName = mine.oldFileName;
      ret.newFileName = mine.newFileName;
      ret.oldHeader = mine.oldHeader;
      ret.newHeader = mine.newHeader;
    } else {
      // Both changed... figure it out
      ret.oldFileName = selectField(ret, mine.oldFileName, theirs.oldFileName);
      ret.newFileName = selectField(ret, mine.newFileName, theirs.newFileName);
      ret.oldHeader = selectField(ret, mine.oldHeader, theirs.oldHeader);
      ret.newHeader = selectField(ret, mine.newHeader, theirs.newHeader);
    }
  }
  ret.hunks = [];
  var mineIndex = 0,
    theirsIndex = 0,
    mineOffset = 0,
    theirsOffset = 0;
  while (mineIndex < mine.hunks.length || theirsIndex < theirs.hunks.length) {
    var mineCurrent = mine.hunks[mineIndex] || {
        oldStart: Infinity
      },
      theirsCurrent = theirs.hunks[theirsIndex] || {
        oldStart: Infinity
      };
    if (hunkBefore(mineCurrent, theirsCurrent)) {
      // This patch does not overlap with any of the others, yay.
      ret.hunks.push(cloneHunk(mineCurrent, mineOffset));
      mineIndex++;
      theirsOffset += mineCurrent.newLines - mineCurrent.oldLines;
    } else if (hunkBefore(theirsCurrent, mineCurrent)) {
      // This patch does not overlap with any of the others, yay.
      ret.hunks.push(cloneHunk(theirsCurrent, theirsOffset));
      theirsIndex++;
      mineOffset += theirsCurrent.newLines - theirsCurrent.oldLines;
    } else {
      // Overlap, merge as best we can
      var mergedHunk = {
        oldStart: Math.min(mineCurrent.oldStart, theirsCurrent.oldStart),
        oldLines: 0,
        newStart: Math.min(mineCurrent.newStart + mineOffset, theirsCurrent.oldStart + theirsOffset),
        newLines: 0,
        lines: []
      };
      mergeLines(mergedHunk, mineCurrent.oldStart, mineCurrent.lines, theirsCurrent.oldStart, theirsCurrent.lines);
      theirsIndex++;
      mineIndex++;
      ret.hunks.push(mergedHunk);
    }
  }
  return ret;
}
function loadPatch(param, base) {
  if (typeof param === 'string') {
    if (/^@@/m.test(param) || /^Index:/m.test(param)) {
      return parsePatch(param)[0];
    }
    if (!base) {
      throw new Error('Must provide a base reference or pass in a patch');
    }
    return structuredPatch(undefined, undefined, base, param);
  }
  return param;
}
function fileNameChanged(patch) {
  return patch.newFileName && patch.newFileName !== patch.oldFileName;
}
function selectField(index, mine, theirs) {
  if (mine === theirs) {
    return mine;
  } else {
    index.conflict = true;
    return {
      mine: mine,
      theirs: theirs
    };
  }
}
function hunkBefore(test, check) {
  return test.oldStart < check.oldStart && test.oldStart + test.oldLines < check.oldStart;
}
function cloneHunk(hunk, offset) {
  return {
    oldStart: hunk.oldStart,
    oldLines: hunk.oldLines,
    newStart: hunk.newStart + offset,
    newLines: hunk.newLines,
    lines: hunk.lines
  };
}
function mergeLines(hunk, mineOffset, mineLines, theirOffset, theirLines) {
  // This will generally result in a conflicted hunk, but there are cases where the context
  // is the only overlap where we can successfully merge the content here.
  var mine = {
      offset: mineOffset,
      lines: mineLines,
      index: 0
    },
    their = {
      offset: theirOffset,
      lines: theirLines,
      index: 0
    };

  // Handle any leading content
  insertLeading(hunk, mine, their);
  insertLeading(hunk, their, mine);

  // Now in the overlap content. Scan through and select the best changes from each.
  while (mine.index < mine.lines.length && their.index < their.lines.length) {
    var mineCurrent = mine.lines[mine.index],
      theirCurrent = their.lines[their.index];
    if ((mineCurrent[0] === '-' || mineCurrent[0] === '+') && (theirCurrent[0] === '-' || theirCurrent[0] === '+')) {
      // Both modified ...
      mutualChange(hunk, mine, their);
    } else if (mineCurrent[0] === '+' && theirCurrent[0] === ' ') {
      var _hunk$lines;
      // Mine inserted
      (_hunk$lines = hunk.lines).push.apply(_hunk$lines, _toConsumableArray(collectChange(mine)));
    } else if (theirCurrent[0] === '+' && mineCurrent[0] === ' ') {
      var _hunk$lines2;
      // Theirs inserted
      (_hunk$lines2 = hunk.lines).push.apply(_hunk$lines2, _toConsumableArray(collectChange(their)));
    } else if (mineCurrent[0] === '-' && theirCurrent[0] === ' ') {
      // Mine removed or edited
      removal(hunk, mine, their);
    } else if (theirCurrent[0] === '-' && mineCurrent[0] === ' ') {
      // Their removed or edited
      removal(hunk, their, mine, true);
    } else if (mineCurrent === theirCurrent) {
      // Context identity
      hunk.lines.push(mineCurrent);
      mine.index++;
      their.index++;
    } else {
      // Context mismatch
      conflict(hunk, collectChange(mine), collectChange(their));
    }
  }

  // Now push anything that may be remaining
  insertTrailing(hunk, mine);
  insertTrailing(hunk, their);
  calcLineCount(hunk);
}
function mutualChange(hunk, mine, their) {
  var myChanges = collectChange(mine),
    theirChanges = collectChange(their);
  if (allRemoves(myChanges) && allRemoves(theirChanges)) {
    // Special case for remove changes that are supersets of one another
    if (arrayStartsWith(myChanges, theirChanges) && skipRemoveSuperset(their, myChanges, myChanges.length - theirChanges.length)) {
      var _hunk$lines3;
      (_hunk$lines3 = hunk.lines).push.apply(_hunk$lines3, _toConsumableArray(myChanges));
      return;
    } else if (arrayStartsWith(theirChanges, myChanges) && skipRemoveSuperset(mine, theirChanges, theirChanges.length - myChanges.length)) {
      var _hunk$lines4;
      (_hunk$lines4 = hunk.lines).push.apply(_hunk$lines4, _toConsumableArray(theirChanges));
      return;
    }
  } else if (arrayEqual(myChanges, theirChanges)) {
    var _hunk$lines5;
    (_hunk$lines5 = hunk.lines).push.apply(_hunk$lines5, _toConsumableArray(myChanges));
    return;
  }
  conflict(hunk, myChanges, theirChanges);
}
function removal(hunk, mine, their, swap) {
  var myChanges = collectChange(mine),
    theirChanges = collectContext(their, myChanges);
  if (theirChanges.merged) {
    var _hunk$lines6;
    (_hunk$lines6 = hunk.lines).push.apply(_hunk$lines6, _toConsumableArray(theirChanges.merged));
  } else {
    conflict(hunk, swap ? theirChanges : myChanges, swap ? myChanges : theirChanges);
  }
}
function conflict(hunk, mine, their) {
  hunk.conflict = true;
  hunk.lines.push({
    conflict: true,
    mine: mine,
    theirs: their
  });
}
function insertLeading(hunk, insert, their) {
  while (insert.offset < their.offset && insert.index < insert.lines.length) {
    var line = insert.lines[insert.index++];
    hunk.lines.push(line);
    insert.offset++;
  }
}
function insertTrailing(hunk, insert) {
  while (insert.index < insert.lines.length) {
    var line = insert.lines[insert.index++];
    hunk.lines.push(line);
  }
}
function collectChange(state) {
  var ret = [],
    operation = state.lines[state.index][0];
  while (state.index < state.lines.length) {
    var line = state.lines[state.index];

    // Group additions that are immediately after subtractions and treat them as one "atomic" modify change.
    if (operation === '-' && line[0] === '+') {
      operation = '+';
    }
    if (operation === line[0]) {
      ret.push(line);
      state.index++;
    } else {
      break;
    }
  }
  return ret;
}
function collectContext(state, matchChanges) {
  var changes = [],
    merged = [],
    matchIndex = 0,
    contextChanges = false,
    conflicted = false;
  while (matchIndex < matchChanges.length && state.index < state.lines.length) {
    var change = state.lines[state.index],
      match = matchChanges[matchIndex];

    // Once we've hit our add, then we are done
    if (match[0] === '+') {
      break;
    }
    contextChanges = contextChanges || change[0] !== ' ';
    merged.push(match);
    matchIndex++;

    // Consume any additions in the other block as a conflict to attempt
    // to pull in the remaining context after this
    if (change[0] === '+') {
      conflicted = true;
      while (change[0] === '+') {
        changes.push(change);
        change = state.lines[++state.index];
      }
    }
    if (match.substr(1) === change.substr(1)) {
      changes.push(change);
      state.index++;
    } else {
      conflicted = true;
    }
  }
  if ((matchChanges[matchIndex] || '')[0] === '+' && contextChanges) {
    conflicted = true;
  }
  if (conflicted) {
    return changes;
  }
  while (matchIndex < matchChanges.length) {
    merged.push(matchChanges[matchIndex++]);
  }
  return {
    merged: merged,
    changes: changes
  };
}
function allRemoves(changes) {
  return changes.reduce(function (prev, change) {
    return prev && change[0] === '-';
  }, true);
}
function skipRemoveSuperset(state, removeChanges, delta) {
  for (var i = 0; i < delta; i++) {
    var changeContent = removeChanges[removeChanges.length - delta + i].substr(1);
    if (state.lines[state.index + i] !== ' ' + changeContent) {
      return false;
    }
  }
  state.index += delta;
  return true;
}
function calcOldNewLineCount(lines) {
  var oldLines = 0;
  var newLines = 0;
  lines.forEach(function (line) {
    if (typeof line !== 'string') {
      var myCount = calcOldNewLineCount(line.mine);
      var theirCount = calcOldNewLineCount(line.theirs);
      if (oldLines !== undefined) {
        if (myCount.oldLines === theirCount.oldLines) {
          oldLines += myCount.oldLines;
        } else {
          oldLines = undefined;
        }
      }
      if (newLines !== undefined) {
        if (myCount.newLines === theirCount.newLines) {
          newLines += myCount.newLines;
        } else {
          newLines = undefined;
        }
      }
    } else {
      if (newLines !== undefined && (line[0] === '+' || line[0] === ' ')) {
        newLines++;
      }
      if (oldLines !== undefined && (line[0] === '-' || line[0] === ' ')) {
        oldLines++;
      }
    }
  });
  return {
    oldLines: oldLines,
    newLines: newLines
  };
}

function reversePatch(structuredPatch) {
  if (Array.isArray(structuredPatch)) {
    return structuredPatch.map(reversePatch).reverse();
  }
  return _objectSpread2(_objectSpread2({}, structuredPatch), {}, {
    oldFileName: structuredPatch.newFileName,
    oldHeader: structuredPatch.newHeader,
    newFileName: structuredPatch.oldFileName,
    newHeader: structuredPatch.oldHeader,
    hunks: structuredPatch.hunks.map(function (hunk) {
      return {
        oldLines: hunk.newLines,
        oldStart: hunk.newStart,
        newLines: hunk.oldLines,
        newStart: hunk.oldStart,
        lines: hunk.lines.map(function (l) {
          if (l.startsWith('-')) {
            return "+".concat(l.slice(1));
          }
          if (l.startsWith('+')) {
            return "-".concat(l.slice(1));
          }
          return l;
        })
      };
    })
  });
}

// See: http://code.google.com/p/google-diff-match-patch/wiki/API
function convertChangesToDMP(changes) {
  var ret = [],
    change,
    operation;
  for (var i = 0; i < changes.length; i++) {
    change = changes[i];
    if (change.added) {
      operation = 1;
    } else if (change.removed) {
      operation = -1;
    } else {
      operation = 0;
    }
    ret.push([operation, change.value]);
  }
  return ret;
}

function convertChangesToXML(changes) {
  var ret = [];
  for (var i = 0; i < changes.length; i++) {
    var change = changes[i];
    if (change.added) {
      ret.push('<ins>');
    } else if (change.removed) {
      ret.push('<del>');
    }
    ret.push(escapeHTML(change.value));
    if (change.added) {
      ret.push('</ins>');
    } else if (change.removed) {
      ret.push('</del>');
    }
  }
  return ret.join('');
}
function escapeHTML(s) {
  var n = s;
  n = n.replace(/&/g, '&amp;');
  n = n.replace(/</g, '&lt;');
  n = n.replace(/>/g, '&gt;');
  n = n.replace(/"/g, '&quot;');
  return n;
}



;// ./templates/postal_code_predictions.html
// Module
var code = "<div endereco-postal-code-predictions data-id=\"{{ExtendableObject.id}}\" class=\"endereco-predictions-wrapper endereco-predictions-wrapper--direction-{{direction}} {{#longList}}endereco-predictions-wrapper--longlist{{/longList}}\" style=\"top:{};\"> <div class=\"endereco-predictions-wrapper-inner\"> <ul class=\"endereco-predictions\"> {{#predictions}} <li class=\"endereco-predictions__item {{#isActive}}active{{/isActive}}\" endereco-postal-code-prediction data-prediction-index=\"{{index}}\" data-prediction-type=\"postalCode\"> {{{postalCodeDiff}}}{{#locality}}, {{{locality}}}{{/locality}}{{#subdivisionName}}<span class=\"endereco-text-gray\">, {{{subdivisionName}}}</span> {{/subdivisionName}} </li> {{/predictions}} </ul> </div> </div> ";
// Exports
/* harmony default export */ var postal_code_predictions = (code);
;// ./modules/extensions/autocomplete/PostalCodeAutocompleteExtension.js


var PostalCodeAutocompleteExtension = {
  name: 'PostalCodeAutocompleteExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      ExtendableObject.waitForExtension('PostalCodeExtension').then(function () {
        var $postalCodeChunkTimeout;

        // Add fields.
        ExtendableObject._postalCodeAutocompleteRequestIndex = 1;
        ExtendableObject._postalCodeChunk = '';
        ExtendableObject._postalCodePredictions = [];
        ExtendableObject._postalCodePredictionsIndex = 0;
        ExtendableObject._postalCodeTimeout = null;
        ExtendableObject._subscribers.postalCodeChunk = [];

        // Add config templates.
        ExtendableObject.config.templates.postalCodePredictions = postal_code_predictions;

        // Add util function
        ExtendableObject.util.renderPostalCodePredictionsDropdown = function () {
          // Is subdivision visible?
          var $subdivVisible = false;
          if (0 < ExtendableObject._subscribers.subdivisionCode.length) {
            ExtendableObject._subscribers.subdivisionCode.forEach(function (listener) {
              if (!listener.object.disabled && listener.object.isConnected) {
                $subdivVisible = true;
              }
            });
          }

          // Render dropdown under the input element
          ExtendableObject._subscribers.postalCodeChunk.forEach(function (subscriber) {
            if (document.querySelector('[endereco-postal-code-predictions]')) {
              ExtendableObject._openDropdowns--;
              document.querySelector('[endereco-postal-code-predictions]').parentNode.removeChild(document.querySelector('[endereco-postal-code-predictions]'));
            }

            // If only one prediction and no difference, then dont render, just copy to fields.
            if (1 === ExtendableObject._postalCodePredictions.length && document.activeElement === subscriber.object && ExtendableObject.config.ux.smartFill) {
              var inputString = ExtendableObject.postalCodeChunk.toLowerCase();
              var bestMatch = ExtendableObject._postalCodePredictions[0].postalCode.substring(0, inputString.length).toLowerCase();
              if (2 > ExtendableObject.util.levenstein.get(inputString, bestMatch) && 3 < inputString.length) {
                ExtendableObject.cb.copyPostalCodeFromPrediction(0);
              }
              return false;
            }

            // Render predictions only if the field is active and there are predictions.
            if (0 < ExtendableObject._postalCodePredictions.length && document.activeElement === subscriber.object) {
              var startingIndex = 0;
              var preparedPredictions = [];

              // Prepare predictions.
              ExtendableObject._postalCodePredictions.forEach(function (prediction) {
                var diff = diffChars(ExtendableObject.postalCodeChunk, prediction.postalCode, {
                  ignoreCase: true
                });
                var postalCodeHtml = '';
                diff.forEach(function (part) {
                  var markClass = part.added ? 'endereco-span--add' : part.removed ? 'endereco-span--remove' : 'endereco-span--neutral';
                  postalCodeHtml += '<span class="' + markClass + '">' + part.value.replace(/[ ]/g, '&nbsp;') + '</span>';
                });
                var tempData = {
                  postalCode: prediction.postalCode,
                  locality: prediction.locality,
                  postalCodeDiff: postalCodeHtml
                };
                if (!!prediction.subdivisionCode) {
                  tempData.subdivisionCode = prediction.subdivisionCode;
                  if ($subdivVisible) {
                    if (!!window.EnderecoIntegrator.subdivisionCodeToNameMapping && !!window.EnderecoIntegrator.subdivisionCodeToNameMapping[prediction.subdivisionCode.toUpperCase()]) {
                      tempData.subdivisionName = window.EnderecoIntegrator.subdivisionCodeToNameMapping[prediction.subdivisionCode.toUpperCase()];
                    } else {
                      if (prediction.subdivisionCode.toUpperCase()) {
                        tempData.subdivisionName = prediction.subdivisionCode.split("-")[1];
                      } else {
                        tempData.subdivisionName = "&nbsp;";
                      }
                    }
                  }
                }
                preparedPredictions.push(tempData);
              });

              // Prepare dropdown.
              var predictionsHtml = ExtendableObject.util.Mustache.render(ExtendableObject.config.templates.postalCodePredictions, {
                ExtendableObject: ExtendableObject,
                predictions: preparedPredictions,
                offsetTop: subscriber.object.offsetTop + subscriber.object.offsetHeight,
                offsetLeft: subscriber.object.offsetLeft,
                width: subscriber.object.offsetWidth,
                direction: getComputedStyle(subscriber.object).direction,
                longList: preparedPredictions.length > 6,
                index: function index() {
                  return startingIndex - 1; // TODO: add loop finishing function to increase counter.
                },
                isActive: function isActive() {
                  var isActive = startingIndex === ExtendableObject._postalCodePredictionsIndex;
                  startingIndex++;
                  return isActive;
                }
              });

              // Attach it to HTML.
              subscriber.object.insertAdjacentHTML('afterend', predictionsHtml);
              ExtendableObject._openDropdowns++;
              document.querySelectorAll('[data-id="' + ExtendableObject.id + '"] [endereco-postal-code-prediction]').forEach(function (DOMElement) {
                DOMElement.addEventListener('mousedown', function (e) {
                  var index = parseInt(this.getAttribute('data-prediction-index'));
                  e.preventDefault();
                  e.stopPropagation();
                  ExtendableObject.cb.copyPostalCodeFromPrediction(index);
                });
              });
            }
          });
        };
        ExtendableObject.cb.copyPostalCodeFromPrediction = function () {
          var index = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : null;
          if (null === index) {
            index = ExtendableObject._postalCodePredictionsIndex;
          }
          if (0 <= index && ExtendableObject.postalCodePredictions[index].postalCode) {
            ExtendableObject.postalCode = ExtendableObject.postalCodePredictions[index].postalCode;
          }
          if (0 <= index && ExtendableObject.postalCodePredictions[index].locality) {
            ExtendableObject.locality = ExtendableObject.postalCodePredictions[index].locality;
          }
          if (0 <= index && ExtendableObject.postalCodePredictions[index].subdivisionCode) {
            ExtendableObject.subdivisionCode = ExtendableObject.postalCodePredictions[index].subdivisionCode;
          }
          ExtendableObject.postalCodePredictions = [];
          ExtendableObject._postalCodePredictionsIndex = 0;
        };
        ExtendableObject.cb.postalCodeChunkInput = function (subscriber) {
          return function (e) {
            clearTimeout(ExtendableObject._postalCodeTimeout);
            ExtendableObject._postalCodeTimeout = setTimeout(function () {
              ExtendableObject.postalCodePredictions = [];
              ExtendableObject._postalCodePredictionsIndex = 0;
              ExtendableObject.postalCodeChunk = subscriber.value;
            }, ExtendableObject.config.ux.delay.inputAssistant);
            if (ExtendableObject.active) {
              ExtendableObject._changed = true;
              ExtendableObject.addressStatus = [];
            }
          };
        };
        ExtendableObject.cb.postalCodeChunkBlur = function (subscriber) {
          return function (e) {
            var isAnyActive = ExtendableObject._subscribers.postalCode.some(function (sub) {
              return document.activeElement === sub.object;
            });
            if (!isAnyActive) {
              // Reset values and remove dropdown
              ExtendableObject.postalCodePredictions = [];
              ExtendableObject._postalCodePredictionsIndex = 0;
              var predictionsElement = document.querySelector('[endereco-postal-code-predictions]');
              if (predictionsElement) {
                ExtendableObject._openDropdowns--;
                predictionsElement.parentNode.removeChild(predictionsElement);
              }
            }
            if (!!$postalCodeChunkTimeout) {
              clearTimeout($postalCodeChunkTimeout);
            }
          };
        };

        // Key events.
        ExtendableObject.cb.postalCodeChunkKeydown = function () {
          return function (e) {
            if ('ArrowUp' === e.key || 'Up' === e.key) {
              e.preventDefault();
              e.stopPropagation();
              if (-1 < ExtendableObject._postalCodePredictionsIndex) {
                ExtendableObject._postalCodePredictionsIndex = ExtendableObject._postalCodePredictionsIndex - 1;
                ExtendableObject.util.renderPostalCodePredictionsDropdown();
              }
            } else if ('ArrowDown' === e.key || 'Down' === e.key) {
              e.preventDefault();
              e.stopPropagation();
              if (ExtendableObject._postalCodePredictionsIndex < ExtendableObject._postalCodePredictions.length - 1) {
                ExtendableObject._postalCodePredictionsIndex = ExtendableObject._postalCodePredictionsIndex + 1;
                ExtendableObject.util.renderPostalCodePredictionsDropdown();
              }
            } else if ('Tab' === e.key || 'Tab' === e.key) {
              // TODO: configurable activate in future releases.
              /*
              if (0 < ExtendableObject._postalCodePredictions.length) {
                  e.preventDefault();
                  e.stopPropagation();
                  ExtendableObject.cb.copyPostalCodeFromPrediction();
              }*/
            } else if ('Enter' === e.key || 'Enter' === e.key) {
              if (0 < ExtendableObject._postalCodePredictions.length) {
                e.preventDefault();
                e.stopPropagation();
                ExtendableObject.cb.copyPostalCodeFromPrediction();
              }
            } else if ('Backspace' === e.key || 'Backspace' === e.key) {
              ExtendableObject.config.ux.smartFill = false;
            }
          };
        };

        // Add property.
        Object.defineProperty(ExtendableObject, 'postalCodeChunk', {
          get: function get() {
            return this._postalCodeChunk;
          },
          set: function set(value) {
            ExtendableObject._awaits++;
            ExtendableObject.util.Promise.resolve(value).then(function (value) {
              if (ExtendableObject._postalCodeChunk !== value) {
                if (value.length < ExtendableObject._postalCodeChunk.length) {
                  ExtendableObject.config.ux.smartFill = false;
                }
                ExtendableObject._postalCodeChunk = value;
                if (!!$postalCodeChunkTimeout) {
                  clearTimeout($postalCodeChunkTimeout);
                }

                // Get predictions.
                if (ExtendableObject.config.useAutocomplete && ExtendableObject.active) {
                  ExtendableObject._postalCodeAutocompleteRequestIndex++;
                  var autocompleteRequestIndex = ExtendableObject._postalCodeAutocompleteRequestIndex * 1; // Create a copy.
                  var message = {
                    'jsonrpc': '2.0',
                    'id': ExtendableObject._postalCodeAutocompleteRequestIndex,
                    'method': 'postCodeAutocomplete',
                    'params': {
                      'country': ExtendableObject.countryCode,
                      'language': ExtendableObject.config.lang,
                      'postCode': ExtendableObject.postalCodeChunk,
                      'cityName': ExtendableObject.locality,
                      'street': ExtendableObject.streetName,
                      'houseNumber': ExtendableObject.buildingNumber
                    }
                  };

                  // Send user data to remote server for validation.
                  ExtendableObject._awaits++;
                  ExtendableObject.util.axios.post(ExtendableObject.config.apiUrl, message, {
                    timeout: ExtendableObject.config.ux.requestTimeout,
                    headers: {
                      'X-Auth-Key': ExtendableObject.config.apiKey,
                      'X-Agent': ExtendableObject.config.agentName,
                      'X-Remote-Api-Url': ExtendableObject.config.remoteApiUrl,
                      'X-Transaction-Referer': window.location.href,
                      'X-Transaction-Id': ExtendableObject.hasLoadedExtension('SessionExtension') ? ExtendableObject.sessionId : 'not_required'
                    }
                  }).then(function (response) {
                    var _response$data$error;
                    if (undefined !== response.data.result && undefined !== response.data.result.predictions) {
                      // Is still actual?
                      if (autocompleteRequestIndex !== ExtendableObject._postalCodeAutocompleteRequestIndex) {
                        return;
                      }
                      var counter = 0;
                      var tempPostalCodeContainer, diff, postalCodeHtml;
                      var postalCodePredictionsTemp = [];

                      // If session counter is set, increase it.
                      if (ExtendableObject.hasLoadedExtension('SessionExtension')) {
                        ExtendableObject.sessionCounter++;
                      }
                      response.data.result.predictions.forEach(function (postalCodePrediction) {
                        if (counter >= ExtendableObject.config.ux.maxAutocompletePredictionItems) {
                          return false;
                        }
                        counter++;
                        tempPostalCodeContainer = {
                          countryCode: postalCodePrediction.country ? postalCodePrediction.country : ExtendableObject._countryCode,
                          postalCode: postalCodePrediction.postCode ? postalCodePrediction.postCode : '',
                          locality: postalCodePrediction.cityName ? postalCodePrediction.cityName : ''
                        };
                        if (!!postalCodePrediction.subdivisionCode) {
                          tempPostalCodeContainer.subdivisionCode = postalCodePrediction.subdivisionCode;
                        }
                        postalCodePredictionsTemp.push(tempPostalCodeContainer);
                      });
                      ExtendableObject.postalCodePredictions = postalCodePredictionsTemp;
                    }

                    // Check for the old session id and regenerate if necessary.
                    if (((_response$data$error = response.data.error) === null || _response$data$error === void 0 ? void 0 : _response$data$error.code) === -32700 && ExtendableObject.util.updateSessionId) {
                      ExtendableObject.util.updateSessionId();
                    }
                  }).catch(function (e) {
                    console.log(e);
                  }).finally(function () {
                    ExtendableObject._awaits--;
                  });
                }
              }
            }).catch().finally(function () {
              ExtendableObject._awaits--;
            });
          }
        });
        Object.defineProperty(ExtendableObject, 'postalCodePredictions', {
          get: function get() {
            return this._postalCodePredictions;
          },
          set: function set(value) {
            ExtendableObject._awaits++;
            ExtendableObject.util.Promise.resolve(value).then(function (value) {
              if (ExtendableObject.postalCodePredictions !== value) {
                ExtendableObject._postalCodePredictions = value;

                // If any subscriber focused, render it under.
                ExtendableObject.util.renderPostalCodePredictionsDropdown();
              }
            }).catch().finally(function () {
              ExtendableObject._awaits--;
            });
          }
        });
        if (ExtendableObject.config.showDebugInfo) {
          console.log('PostalCodeAutocompleteExtension applied');
        }
        resolve($self);
      }).catch(function (e) {
        console.log('Failed to load because of timeout');
        reject($self);
      });
    });
  }
};
/* harmony default export */ var autocomplete_PostalCodeAutocompleteExtension = (PostalCodeAutocompleteExtension);
;// ./modules/extensions/fields/LocalityExtension.js
var LocalityExtension = {
  name: 'LocalityExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      ExtendableObject._locality = '';
      ExtendableObject._subscribers.locality = [];
      ExtendableObject.cb.setLocality = function (locality) {
        return new ExtendableObject.util.Promise(function (resolve, reject) {
          resolve(locality);
        });
      };
      ExtendableObject.cb.localityChange = function (subscriber) {
        return function (e) {
          ExtendableObject.locality = subscriber.value;
        };
      };
      ExtendableObject.cb.localityBlur = function (subscriber) {
        return function (e) {
          ExtendableObject.waitUntilReady().then(function () {
            if (ExtendableObject.onBlurTimeout) {
              clearTimeout(ExtendableObject.onBlurTimeout);
              ExtendableObject.onBlurTimeout = null;
            }
            ExtendableObject.onBlurTimeout = setTimeout(function () {
              if (ExtendableObject.config.trigger.onblur && !ExtendableObject.anyActive() && ExtendableObject.util.shouldBeChecked() && !window.EnderecoIntegrator.hasSubmit) {
                // Second. Check Address.
                ExtendableObject.onBlurTimeout = null;
                ExtendableObject.util.checkAddress().catch();
              }
            }, 300);
          }).catch();
        };
      };

      // Add getter and setter for fields.
      Object.defineProperty(ExtendableObject, 'locality', {
        get: function get() {
          return this._locality;
        },
        set: function set(value) {
          ExtendableObject._awaits++;
          ExtendableObject.util.Promise.resolve(value).then(function (value) {
            ExtendableObject._awaits++;
            ExtendableObject.cb.setLocality(value).then(function (value) {
              var oldValue = ExtendableObject._locality;
              var newValue = value;
              var notSame = ExtendableObject._locality !== value;

              // Chunk set.
              if (ExtendableObject.hasLoadedExtension('LocalityAutocompleteExtension')) {
                notSame = notSame || ExtendableObject._localityChunk !== value;
              }
              if (notSame) {
                ExtendableObject._locality = value;
                if (ExtendableObject.hasLoadedExtension('LocalityAutocompleteExtension')) {
                  ExtendableObject._localityChunk = value;

                  // Inform all subscribers about the change.
                  ExtendableObject._subscribers.localityChunk.forEach(function (subscriber) {
                    subscriber.value = value;
                  });
                }

                // Inform all subscribers about the change.
                ExtendableObject._subscribers.locality.forEach(function (subscriber) {
                  subscriber.value = value;
                });
                if (ExtendableObject.active) {
                  ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                    detail: {
                      fieldName: 'locality',
                      oldValue: oldValue,
                      newValue: newValue,
                      object: ExtendableObject
                    }
                  }));
                }
              }
            }).catch(function (e) {
              if (ExtendableObject.config.showDebugInfo) {
                console.log('Error resolving locality', e);
              }
            }).finally(function () {
              ExtendableObject._awaits--;
            });
          }).catch().finally(function () {
            ExtendableObject._awaits--;
          });
        }
      });
      ExtendableObject.fieldNames.push('locality');
      if (ExtendableObject.config.showDebugInfo) {
        console.log('LocalityExtension applied');
      }
      resolve($self);
    });
  }
};
/* harmony default export */ var fields_LocalityExtension = (LocalityExtension);
;// ./modules/extensions/checks/LocalityCheckExtension.js
var LocalityCheckExtension = {
  name: 'LocalityCheckExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      ExtendableObject.waitForExtension('LocalityExtension').then(function () {
        // Add email field.
        ExtendableObject._localityStatus = '';
        ExtendableObject._subscribers.localityStatus = [];

        // Add change event hadler.
        ExtendableObject.cb.localityStatusChange = function (subscriber) {
          return function (e) {
            ExtendableObject.localityStatus = subscriber.value;
          };
        };

        // Add the "emaiL" property
        Object.defineProperty(ExtendableObject, 'localityStatus', {
          get: function get() {
            return this._localityStatus;
          },
          set: function set(value) {
            var oldValue = ExtendableObject._localityStatus;
            ExtendableObject._awaits++;
            ExtendableObject.util.Promise.resolve(value).then(function (value) {
              var newValue = value;
              if (oldValue !== newValue) {
                ExtendableObject._localityStatus = newValue;

                // Inform all subscribers about the change.
                ExtendableObject._subscribers.localityStatus.forEach(function (subscriber) {
                  subscriber.value = value;
                });
                ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                  detail: {
                    fieldName: 'localityStatus',
                    oldValue: oldValue,
                    newValue: newValue,
                    object: ExtendableObject
                  }
                }));
              }
            }).catch().finally(function () {
              ExtendableObject._awaits--;
            });
          }
        });
        if (ExtendableObject.config.showDebugInfo) {
          console.log('LocalityCheckExtension applied');
        }
        resolve($self);
      }).catch(function () {
        console.log('Failed to load because of timeout');
        reject($self);
      });
    });
  }
};
/* harmony default export */ var checks_LocalityCheckExtension = (LocalityCheckExtension);
;// ./templates/locality_predictions.html
// Module
var locality_predictions_code = "<div endereco-locality-predictions data-id=\"{{ExtendableObject.id}}\" class=\"endereco-predictions-wrapper endereco-predictions-wrapper--direction-{{direction}} {{#longList}}endereco-predictions-wrapper--longlist{{/longList}}\" style=\"top:{};\"> <div class=\"endereco-predictions-wrapper-inner\"> <ul class=\"endereco-predictions\"> {{#predictions}} <li class=\"endereco-predictions__item {{#isActive}}active{{/isActive}}\" endereco-locality-prediction data-prediction-index=\"{{index}}\" data-prediction-type=\"locality\"> {{#postalCode}}{{{postalCode}}}, {{/postalCode}}{{{localityDiff}}}{{#subdivisionName}}<span class=\"endereco-text-gray\">, {{{subdivisionName}}}</span> {{/subdivisionName}} </li> {{/predictions}} </ul> </div> </div> ";
// Exports
/* harmony default export */ var locality_predictions = (locality_predictions_code);
;// ./modules/extensions/autocomplete/LocalityAutocompleteExtension.js


var LocalityAutocompleteExtension = {
  name: 'LocalityAutocompleteExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      ExtendableObject.waitForExtension('LocalityExtension').then(function () {
        var $localityPredictionTimeout;

        // Add fields.
        ExtendableObject._localityAutocompleteRequestIndex = 1;
        ExtendableObject._localityChunk = '';
        ExtendableObject._localityPredictions = [];
        ExtendableObject._localityPredictionsIndex = 0;
        ExtendableObject._localityTimeout = null;
        ExtendableObject._subscribers.localityChunk = [];

        // Add config templates.
        ExtendableObject.config.templates.localityPredictions = locality_predictions;

        // Add util function
        ExtendableObject.util.renderLocalityPredictionsDropdown = function () {
          // Is subdivision visible?
          var $subdivVisible = false;
          if (0 < ExtendableObject._subscribers.subdivisionCode.length) {
            ExtendableObject._subscribers.subdivisionCode.forEach(function (listener) {
              if (!listener.object.disabled && listener.object.isConnected) {
                $subdivVisible = true;
              }
            });
          }

          // Render dropdown under the input element
          ExtendableObject._subscribers.localityChunk.forEach(function (subscriber) {
            if (document.querySelector('[endereco-locality-predictions]')) {
              ExtendableObject._openDropdowns--;
              document.querySelector('[endereco-locality-predictions]').parentNode.removeChild(document.querySelector('[endereco-locality-predictions]'));
            }

            // If only one prediction and no difference, then dont render, just copy to fields.
            if (1 === ExtendableObject._localityPredictions.length && document.activeElement === subscriber.object && ExtendableObject.config.ux.smartFill) {
              var inputString = ExtendableObject.localityChunk.toLowerCase();
              var bestMatch = ExtendableObject._localityPredictions[0].locality.substring(0, inputString.length).toLowerCase();
              if (2 > ExtendableObject.util.levenstein.get(inputString, bestMatch) && 3 < inputString.length) {
                ExtendableObject.cb.copyLocalityFromPrediction(0);
              }
              return false;
            }

            // Render predictions only if the field is active and there are predictions.
            if (0 < ExtendableObject._localityPredictions.length && document.activeElement === subscriber.object) {
              var startingIndex = 0;
              var preparedPredictions = [];

              // Prepare predictions.
              ExtendableObject._localityPredictions.forEach(function (prediction) {
                var diff = diffChars(ExtendableObject.localityChunk, prediction.locality, {
                  ignoreCase: true
                });
                var localityHtml = '';
                diff.forEach(function (part) {
                  var markClass = part.added ? 'endereco-span--add' : part.removed ? 'endereco-span--remove' : 'endereco-span--neutral';
                  localityHtml += '<span class="' + markClass + '">' + part.value.replace(/[ ]/g, '&nbsp;') + '</span>';
                });
                var tempData = {
                  postalCode: prediction.postalCode,
                  locality: prediction.locality,
                  localityDiff: localityHtml
                };
                if (!!prediction.subdivisionCode) {
                  tempData.subdivisionCode = prediction.subdivisionCode;
                  if ($subdivVisible) {
                    if (!!window.EnderecoIntegrator.subdivisionCodeToNameMapping && !!window.EnderecoIntegrator.subdivisionCodeToNameMapping[prediction.subdivisionCode.toUpperCase()]) {
                      tempData.subdivisionName = window.EnderecoIntegrator.subdivisionCodeToNameMapping[prediction.subdivisionCode.toUpperCase()];
                    } else {
                      if (prediction.subdivisionCode.toUpperCase()) {
                        tempData.subdivisionName = prediction.subdivisionCode.split("-")[1];
                      } else {
                        tempData.subdivisionName = "&nbsp;";
                      }
                    }
                  }
                }
                preparedPredictions.push(tempData);
              });

              // Prepare dropdown.
              var predictionsHtml = ExtendableObject.util.Mustache.render(ExtendableObject.config.templates.localityPredictions, {
                ExtendableObject: ExtendableObject,
                predictions: preparedPredictions,
                offsetTop: subscriber.object.offsetTop + subscriber.object.offsetHeight,
                offsetLeft: subscriber.object.offsetLeft,
                width: subscriber.object.offsetWidth,
                direction: getComputedStyle(subscriber.object).direction,
                longList: preparedPredictions.length > 6,
                index: function index() {
                  return startingIndex - 1; // TODO: add loop finishing function to increase counter.
                },
                isActive: function isActive() {
                  var isActive = startingIndex === ExtendableObject._localityPredictionsIndex;
                  startingIndex++;
                  return isActive;
                }
              });

              // Attach it to HTML.
              subscriber.object.insertAdjacentHTML('afterend', predictionsHtml);
              ExtendableObject._openDropdowns++;
              document.querySelectorAll('[data-id="' + ExtendableObject.id + '"] [endereco-locality-prediction]').forEach(function (DOMElement) {
                DOMElement.addEventListener('mousedown', function (e) {
                  var index = parseInt(this.getAttribute('data-prediction-index'));
                  e.preventDefault();
                  e.stopPropagation();
                  ExtendableObject.cb.copyLocalityFromPrediction(index);
                });
              });
            }
          });
        };
        ExtendableObject.cb.copyLocalityFromPrediction = function () {
          var index = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : null;
          if (null === index) {
            index = ExtendableObject._localityPredictionsIndex;
          }
          if (0 <= index && ExtendableObject.localityPredictions[index].postalCode) {
            ExtendableObject.postalCode = ExtendableObject.localityPredictions[index].postalCode;
          }
          if (0 <= index && ExtendableObject.localityPredictions[index].locality) {
            ExtendableObject.locality = ExtendableObject.localityPredictions[index].locality;
          }
          if (0 <= index && ExtendableObject.localityPredictions[index].subdivisionCode) {
            ExtendableObject.subdivisionCode = ExtendableObject.localityPredictions[index].subdivisionCode;
          }
          ExtendableObject.localityPredictions = [];
          ExtendableObject._localityPredictionsIndex = 0;
        };
        ExtendableObject.cb.localityChunkInput = function (subscriber) {
          return function (e) {
            clearTimeout(ExtendableObject._localityTimeout);
            ExtendableObject._localityTimeout = setTimeout(function () {
              ExtendableObject.localityPredictions = [];
              ExtendableObject._localityPredictionsIndex = 0;
              ExtendableObject.localityChunk = subscriber.value;
            }, ExtendableObject.config.ux.delay.inputAssistant);
            if (ExtendableObject.active) {
              ExtendableObject._changed = true;
              ExtendableObject.addressStatus = [];
            }
          };
        };
        ExtendableObject.cb.localityChunkBlur = function (subscriber) {
          return function (e) {
            var isAnyActive = ExtendableObject._subscribers.locality.some(function (sub) {
              return document.activeElement === sub.object;
            });
            if (!isAnyActive) {
              // Reset values and remove dropdown
              ExtendableObject.localityPredictions = [];
              ExtendableObject._localityPredictionsIndex = 0;
              var predictionsElement = document.querySelector('[endereco-locality-predictions]');
              if (predictionsElement) {
                ExtendableObject._openDropdowns--;
                predictionsElement.parentNode.removeChild(predictionsElement);
              }
            }
            if (!!$localityPredictionTimeout) {
              clearTimeout($localityPredictionTimeout);
            }
          };
        };

        // Key events.
        ExtendableObject.cb.localityChunkKeydown = function () {
          return function (e) {
            if ('ArrowUp' === e.key || 'Up' === e.key) {
              e.preventDefault();
              e.stopPropagation();
              if (-1 < ExtendableObject._localityPredictionsIndex) {
                ExtendableObject._localityPredictionsIndex = ExtendableObject._localityPredictionsIndex - 1;
                ExtendableObject.util.renderLocalityPredictionsDropdown();
              }
            } else if ('ArrowDown' === e.key || 'Down' === e.key) {
              e.preventDefault();
              e.stopPropagation();
              if (ExtendableObject._localityPredictionsIndex < ExtendableObject._localityPredictions.length - 1) {
                ExtendableObject._localityPredictionsIndex = ExtendableObject._localityPredictionsIndex + 1;
                ExtendableObject.util.renderLocalityPredictionsDropdown();
              }
            } else if ('Tab' === e.key || 'Tab' === e.key) {
              // TODO: configurable activate in future releases.
              /*if (0 < ExtendableObject._localityPredictions.length) {
                  e.preventDefault();
                  e.stopPropagation();
                  ExtendableObject.cb.copyLocalityFromPrediction();
              }*/
            } else if ('Enter' === e.key || 'Enter' === e.key) {
              if (0 < ExtendableObject._localityPredictions.length) {
                e.preventDefault();
                e.stopPropagation();
                ExtendableObject.cb.copyLocalityFromPrediction();
              }
            } else if ('Backspace' === e.key || 'Backspace' === e.key) {
              ExtendableObject.config.ux.smartFill = false;
            }
          };
        };

        // Add property.
        Object.defineProperty(ExtendableObject, 'localityChunk', {
          get: function get() {
            return this._localityChunk;
          },
          set: function set(value) {
            ExtendableObject._awaits++;
            ExtendableObject.util.Promise.resolve(value).then(function (value) {
              if (ExtendableObject._localityChunk !== value) {
                if (value.length < ExtendableObject._localityChunk.length) {
                  ExtendableObject.config.ux.smartFill = false;
                }
                ExtendableObject._localityChunk = value;
                if (!!$localityPredictionTimeout) {
                  clearTimeout($localityPredictionTimeout);
                }

                // Get predictions.
                if (ExtendableObject.config.useAutocomplete && ExtendableObject.active) {
                  ExtendableObject._localityAutocompleteRequestIndex++;
                  var autocompleteRequestIndex = ExtendableObject._localityAutocompleteRequestIndex * 1; // Create a copy.
                  var message = {
                    'jsonrpc': '2.0',
                    'id': ExtendableObject._localityAutocompleteRequestIndex,
                    'method': 'cityNameAutocomplete',
                    'params': {
                      'country': ExtendableObject.countryCode,
                      'language': ExtendableObject.config.lang,
                      'postCode': ExtendableObject.postalCode,
                      'cityName': ExtendableObject.localityChunk,
                      'street': ExtendableObject.streetName,
                      'houseNumber': ExtendableObject.buildingNumber
                    }
                  };

                  // Send user data to remote server for validation.
                  ExtendableObject._awaits++;
                  ExtendableObject.util.axios.post(ExtendableObject.config.apiUrl, message, {
                    timeout: ExtendableObject.config.ux.requestTimeout,
                    headers: {
                      'X-Auth-Key': ExtendableObject.config.apiKey,
                      'X-Agent': ExtendableObject.config.agentName,
                      'X-Remote-Api-Url': ExtendableObject.config.remoteApiUrl,
                      'X-Transaction-Referer': window.location.href,
                      'X-Transaction-Id': ExtendableObject.hasLoadedExtension('SessionExtension') ? ExtendableObject.sessionId : 'not_required'
                    }
                  }).then(function (response) {
                    var _response$data$error;
                    if (undefined !== response.data.result && undefined !== response.data.result.predictions) {
                      // Is still actual?
                      if (autocompleteRequestIndex !== ExtendableObject._localityAutocompleteRequestIndex) {
                        return;
                      }
                      var counter = 0;
                      var tempLocalityContainer, diff, localityHtml;
                      var localityPredictionsTemp = [];

                      // If session counter is set, increase it.
                      if (ExtendableObject.hasLoadedExtension('SessionExtension')) {
                        ExtendableObject.sessionCounter++;
                      }
                      response.data.result.predictions.forEach(function (localityPrediction) {
                        if (counter >= ExtendableObject.config.ux.maxAutocompletePredictionItems) {
                          return false;
                        }
                        counter++;
                        tempLocalityContainer = {
                          countryCode: localityPrediction.country ? localityPrediction.country : ExtendableObject._countryCode,
                          postalCode: localityPrediction.postCode ? localityPrediction.postCode : '',
                          locality: localityPrediction.cityName ? localityPrediction.cityName : ''
                        };
                        if (!!localityPrediction.subdivisionCode) {
                          tempLocalityContainer.subdivisionCode = localityPrediction.subdivisionCode;
                        }
                        localityPredictionsTemp.push(tempLocalityContainer);
                      });
                      ExtendableObject.localityPredictions = localityPredictionsTemp;
                    }

                    // Check for the old session id and regenerate if necessary.
                    if (((_response$data$error = response.data.error) === null || _response$data$error === void 0 ? void 0 : _response$data$error.code) === -32000 && ExtendableObject.util.updateSessionId) {
                      ExtendableObject.util.updateSessionId();
                    }
                  }).catch(function (e) {
                    console.log(e);
                  }).finally(function () {
                    ExtendableObject._awaits--;
                  });
                }
              }
            }).catch().finally(function () {
              ExtendableObject._awaits--;
            });
          }
        });
        Object.defineProperty(ExtendableObject, 'localityPredictions', {
          get: function get() {
            return this._localityPredictions;
          },
          set: function set(value) {
            ExtendableObject._awaits++;
            ExtendableObject.util.Promise.resolve(value).then(function (value) {
              if (ExtendableObject.localityPredictions !== value) {
                ExtendableObject._localityPredictions = value;

                // If any subscriber focused, render it under.
                ExtendableObject.util.renderLocalityPredictionsDropdown();
              }
            }).catch().finally(function () {
              ExtendableObject._awaits--;
            });
          }
        });
        if (ExtendableObject.config.showDebugInfo) {
          console.log('LocalityAutocompleteExtension applied');
        }
        resolve($self);
      }).catch(function (e) {
        console.log('Failed to load because of timeout');
        reject($self);
      });
    });
  }
};
/* harmony default export */ var autocomplete_LocalityAutocompleteExtension = (LocalityAutocompleteExtension);
;// ./templates/fullstreet/default.html
// Module
var default_code = "{{{streetName}}} {{{buildingNumber}}} ";
// Exports
/* harmony default export */ var fullstreet_default = (default_code);
;// ./templates/fullstreet/fr.html
// Module
var fr_code = "{{{buildingNumber}}} {{{streetName}}} ";
// Exports
/* harmony default export */ var fr = (fr_code);
;// ./templates/streetNameTemplates.js


var templates = {
  default: fullstreet_default,
  de: fullstreet_default,
  at: fullstreet_default,
  fr: fr,
  us: fr,
  dz: fr,
  ad: fr,
  az: fr,
  am: fr,
  au: fr,
  bh: fr,
  bd: fr,
  bn: fr,
  kh: fr,
  ca: fr,
  cn: fr,
  cx: fr,
  cc: fr,
  cd: fr,
  cg: fr,
  ck: fr,
  cy: fr,
  ci: fr,
  dm: fr,
  eg: fr,
  fj: fr,
  ga: fr,
  gm: fr,
  ge: fr,
  gh: fr,
  gi: fr,
  gp: fr,
  gu: fr,
  gy: fr,
  hk: fr,
  in: fr,
  id: fr,
  ie: fr,
  gb: fr,
  il: fr,
  la: fr,
  lb: fr,
  lu: fr,
  mr: fr,
  mu: fr,
  mc: fr,
  ms: fr,
  ma: fr,
  mm: fr,
  na: fr,
  nz: fr,
  ng: fr,
  nf: fr,
  om: fr,
  pk: fr,
  pw: fr,
  ph: fr,
  pr: fr,
  rw: fr,
  re: fr,
  bl: fr,
  sh: fr,
  kn: fr,
  mf: fr,
  sa: fr,
  sn: fr,
  sl: fr,
  sg: fr,
  tw: fr,
  tz: fr,
  th: fr,
  tg: fr,
  tt: fr,
  tn: fr,
  tm: fr,
  tc: fr,
  vm: fr,
  wf: fr,
  ye: fr,
  zm: fr,
  zw: fr,
  getTemplate: function getTemplate(countryCode) {
    countryCode = countryCode.toLowerCase();
    if (!!this[countryCode]) {
      return this[countryCode];
    } else {
      return this.default;
    }
  }
};
/* harmony default export */ var streetNameTemplates = (templates);
;// ./templates/street_name_predictions.html
// Module
var street_name_predictions_code = "<div endereco-street-name-predictions data-id=\"{{ExtendableObject.id}}\" class=\"endereco-predictions-wrapper endereco-predictions-wrapper--direction-{{direction}} {{#longList}}endereco-predictions-wrapper--longlist{{/longList}}\" style=\"top:{};\"> <div class=\"endereco-predictions-wrapper-inner\"> <ul class=\"endereco-predictions\"> {{#predictions}} <li class=\"endereco-predictions__item {{#isActive}}active{{/isActive}}\" endereco-street-name-prediction data-prediction-index=\"{{index}}\" data-prediction-type=\"streetName\">{{{streetNameDiff}}}</li> {{/predictions}} </ul> </div> </div> ";
// Exports
/* harmony default export */ var street_name_predictions = (street_name_predictions_code);
;// ./modules/extensions/fields/StreetNameExtension.js
function StreetNameExtension_typeof(o) { "@babel/helpers - typeof"; return StreetNameExtension_typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, StreetNameExtension_typeof(o); }
function StreetNameExtension_regeneratorRuntime() { "use strict"; /*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/facebook/regenerator/blob/main/LICENSE */ StreetNameExtension_regeneratorRuntime = function _regeneratorRuntime() { return e; }; var t, e = {}, r = Object.prototype, n = r.hasOwnProperty, o = Object.defineProperty || function (t, e, r) { t[e] = r.value; }, i = "function" == typeof Symbol ? Symbol : {}, a = i.iterator || "@@iterator", c = i.asyncIterator || "@@asyncIterator", u = i.toStringTag || "@@toStringTag"; function define(t, e, r) { return Object.defineProperty(t, e, { value: r, enumerable: !0, configurable: !0, writable: !0 }), t[e]; } try { define({}, ""); } catch (t) { define = function define(t, e, r) { return t[e] = r; }; } function wrap(t, e, r, n) { var i = e && e.prototype instanceof Generator ? e : Generator, a = Object.create(i.prototype), c = new Context(n || []); return o(a, "_invoke", { value: makeInvokeMethod(t, r, c) }), a; } function tryCatch(t, e, r) { try { return { type: "normal", arg: t.call(e, r) }; } catch (t) { return { type: "throw", arg: t }; } } e.wrap = wrap; var h = "suspendedStart", l = "suspendedYield", f = "executing", s = "completed", y = {}; function Generator() {} function GeneratorFunction() {} function GeneratorFunctionPrototype() {} var p = {}; define(p, a, function () { return this; }); var d = Object.getPrototypeOf, v = d && d(d(values([]))); v && v !== r && n.call(v, a) && (p = v); var g = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(p); function defineIteratorMethods(t) { ["next", "throw", "return"].forEach(function (e) { define(t, e, function (t) { return this._invoke(e, t); }); }); } function AsyncIterator(t, e) { function invoke(r, o, i, a) { var c = tryCatch(t[r], t, o); if ("throw" !== c.type) { var u = c.arg, h = u.value; return h && "object" == StreetNameExtension_typeof(h) && n.call(h, "__await") ? e.resolve(h.__await).then(function (t) { invoke("next", t, i, a); }, function (t) { invoke("throw", t, i, a); }) : e.resolve(h).then(function (t) { u.value = t, i(u); }, function (t) { return invoke("throw", t, i, a); }); } a(c.arg); } var r; o(this, "_invoke", { value: function value(t, n) { function callInvokeWithMethodAndArg() { return new e(function (e, r) { invoke(t, n, e, r); }); } return r = r ? r.then(callInvokeWithMethodAndArg, callInvokeWithMethodAndArg) : callInvokeWithMethodAndArg(); } }); } function makeInvokeMethod(e, r, n) { var o = h; return function (i, a) { if (o === f) throw Error("Generator is already running"); if (o === s) { if ("throw" === i) throw a; return { value: t, done: !0 }; } for (n.method = i, n.arg = a;;) { var c = n.delegate; if (c) { var u = maybeInvokeDelegate(c, n); if (u) { if (u === y) continue; return u; } } if ("next" === n.method) n.sent = n._sent = n.arg;else if ("throw" === n.method) { if (o === h) throw o = s, n.arg; n.dispatchException(n.arg); } else "return" === n.method && n.abrupt("return", n.arg); o = f; var p = tryCatch(e, r, n); if ("normal" === p.type) { if (o = n.done ? s : l, p.arg === y) continue; return { value: p.arg, done: n.done }; } "throw" === p.type && (o = s, n.method = "throw", n.arg = p.arg); } }; } function maybeInvokeDelegate(e, r) { var n = r.method, o = e.iterator[n]; if (o === t) return r.delegate = null, "throw" === n && e.iterator.return && (r.method = "return", r.arg = t, maybeInvokeDelegate(e, r), "throw" === r.method) || "return" !== n && (r.method = "throw", r.arg = new TypeError("The iterator does not provide a '" + n + "' method")), y; var i = tryCatch(o, e.iterator, r.arg); if ("throw" === i.type) return r.method = "throw", r.arg = i.arg, r.delegate = null, y; var a = i.arg; return a ? a.done ? (r[e.resultName] = a.value, r.next = e.nextLoc, "return" !== r.method && (r.method = "next", r.arg = t), r.delegate = null, y) : a : (r.method = "throw", r.arg = new TypeError("iterator result is not an object"), r.delegate = null, y); } function pushTryEntry(t) { var e = { tryLoc: t[0] }; 1 in t && (e.catchLoc = t[1]), 2 in t && (e.finallyLoc = t[2], e.afterLoc = t[3]), this.tryEntries.push(e); } function resetTryEntry(t) { var e = t.completion || {}; e.type = "normal", delete e.arg, t.completion = e; } function Context(t) { this.tryEntries = [{ tryLoc: "root" }], t.forEach(pushTryEntry, this), this.reset(!0); } function values(e) { if (e || "" === e) { var r = e[a]; if (r) return r.call(e); if ("function" == typeof e.next) return e; if (!isNaN(e.length)) { var o = -1, i = function next() { for (; ++o < e.length;) if (n.call(e, o)) return next.value = e[o], next.done = !1, next; return next.value = t, next.done = !0, next; }; return i.next = i; } } throw new TypeError(StreetNameExtension_typeof(e) + " is not iterable"); } return GeneratorFunction.prototype = GeneratorFunctionPrototype, o(g, "constructor", { value: GeneratorFunctionPrototype, configurable: !0 }), o(GeneratorFunctionPrototype, "constructor", { value: GeneratorFunction, configurable: !0 }), GeneratorFunction.displayName = define(GeneratorFunctionPrototype, u, "GeneratorFunction"), e.isGeneratorFunction = function (t) { var e = "function" == typeof t && t.constructor; return !!e && (e === GeneratorFunction || "GeneratorFunction" === (e.displayName || e.name)); }, e.mark = function (t) { return Object.setPrototypeOf ? Object.setPrototypeOf(t, GeneratorFunctionPrototype) : (t.__proto__ = GeneratorFunctionPrototype, define(t, u, "GeneratorFunction")), t.prototype = Object.create(g), t; }, e.awrap = function (t) { return { __await: t }; }, defineIteratorMethods(AsyncIterator.prototype), define(AsyncIterator.prototype, c, function () { return this; }), e.AsyncIterator = AsyncIterator, e.async = function (t, r, n, o, i) { void 0 === i && (i = Promise); var a = new AsyncIterator(wrap(t, r, n, o), i); return e.isGeneratorFunction(r) ? a : a.next().then(function (t) { return t.done ? t.value : a.next(); }); }, defineIteratorMethods(g), define(g, u, "Generator"), define(g, a, function () { return this; }), define(g, "toString", function () { return "[object Generator]"; }), e.keys = function (t) { var e = Object(t), r = []; for (var n in e) r.push(n); return r.reverse(), function next() { for (; r.length;) { var t = r.pop(); if (t in e) return next.value = t, next.done = !1, next; } return next.done = !0, next; }; }, e.values = values, Context.prototype = { constructor: Context, reset: function reset(e) { if (this.prev = 0, this.next = 0, this.sent = this._sent = t, this.done = !1, this.delegate = null, this.method = "next", this.arg = t, this.tryEntries.forEach(resetTryEntry), !e) for (var r in this) "t" === r.charAt(0) && n.call(this, r) && !isNaN(+r.slice(1)) && (this[r] = t); }, stop: function stop() { this.done = !0; var t = this.tryEntries[0].completion; if ("throw" === t.type) throw t.arg; return this.rval; }, dispatchException: function dispatchException(e) { if (this.done) throw e; var r = this; function handle(n, o) { return a.type = "throw", a.arg = e, r.next = n, o && (r.method = "next", r.arg = t), !!o; } for (var o = this.tryEntries.length - 1; o >= 0; --o) { var i = this.tryEntries[o], a = i.completion; if ("root" === i.tryLoc) return handle("end"); if (i.tryLoc <= this.prev) { var c = n.call(i, "catchLoc"), u = n.call(i, "finallyLoc"); if (c && u) { if (this.prev < i.catchLoc) return handle(i.catchLoc, !0); if (this.prev < i.finallyLoc) return handle(i.finallyLoc); } else if (c) { if (this.prev < i.catchLoc) return handle(i.catchLoc, !0); } else { if (!u) throw Error("try statement without catch or finally"); if (this.prev < i.finallyLoc) return handle(i.finallyLoc); } } } }, abrupt: function abrupt(t, e) { for (var r = this.tryEntries.length - 1; r >= 0; --r) { var o = this.tryEntries[r]; if (o.tryLoc <= this.prev && n.call(o, "finallyLoc") && this.prev < o.finallyLoc) { var i = o; break; } } i && ("break" === t || "continue" === t) && i.tryLoc <= e && e <= i.finallyLoc && (i = null); var a = i ? i.completion : {}; return a.type = t, a.arg = e, i ? (this.method = "next", this.next = i.finallyLoc, y) : this.complete(a); }, complete: function complete(t, e) { if ("throw" === t.type) throw t.arg; return "break" === t.type || "continue" === t.type ? this.next = t.arg : "return" === t.type ? (this.rval = this.arg = t.arg, this.method = "return", this.next = "end") : "normal" === t.type && e && (this.next = e), y; }, finish: function finish(t) { for (var e = this.tryEntries.length - 1; e >= 0; --e) { var r = this.tryEntries[e]; if (r.finallyLoc === t) return this.complete(r.completion, r.afterLoc), resetTryEntry(r), y; } }, catch: function _catch(t) { for (var e = this.tryEntries.length - 1; e >= 0; --e) { var r = this.tryEntries[e]; if (r.tryLoc === t) { var n = r.completion; if ("throw" === n.type) { var o = n.arg; resetTryEntry(r); } return o; } } throw Error("illegal catch attempt"); }, delegateYield: function delegateYield(e, r, n) { return this.delegate = { iterator: values(e), resultName: r, nextLoc: n }, "next" === this.method && (this.arg = t), y; } }, e; }
function StreetNameExtension_asyncGeneratorStep(n, t, e, r, o, a, c) { try { var i = n[a](c), u = i.value; } catch (n) { return void e(n); } i.done ? t(u) : Promise.resolve(u).then(r, o); }
function StreetNameExtension_asyncToGenerator(n) { return function () { var t = this, e = arguments; return new Promise(function (r, o) { var a = n.apply(t, e); function _next(n) { StreetNameExtension_asyncGeneratorStep(a, r, o, _next, _throw, "next", n); } function _throw(n) { StreetNameExtension_asyncGeneratorStep(a, r, o, _next, _throw, "throw", n); } _next(void 0); }); }; }
/**
 * Extends the base object with "streetName" field handling capabilities.
 * Adds support for street name input with:
 * - Adding streetName property with getter/setter
 * - Providing autocomplete suggestions for street names
 * - Syncing with streetFull when combined with building number
 * - Caching API results for performance
 * - Handling keyboard navigation for autocomplete suggestions
 */




var MAX_PREDICTIONS_BEFORE_SCROLL = 6;
var ERROR_EXPIRED_SESSION = -32700;
var StreetNameExtension = {
  name: 'StreetNameExtension',
  /**
   * Place for registering internal properties needed for streetName functionality.
   * Add here:
   * - Internal storage fields (_fieldName)
   * - Subscriber arrays (_subscribers.fieldName)
   * - Cache objects for API results
   * - Control flags for field behavior
   * - Timeout and sequence tracking variables
   * @param {Object} ExtendableObject - The base object being extended
   */
  registerProperties: function registerProperties(ExtendableObject) {
    // Internal storage for field values
    ExtendableObject._streetName = '';
    ExtendableObject._streetNamePredictions = [];

    // Subscriber storage
    ExtendableObject._subscribers.streetName = [];

    // Cache
    ExtendableObject.streetNameAutocompleteCache = {
      cachedResults: {}
    };

    // Flags
    ExtendableObject._allowToNotifyStreetNameSubscribers = true;
    ExtendableObject._allowFetchStreetNameAutocomplete = true;

    // Timeout and sequence
    ExtendableObject._streetFullComposeTimeout = null;
    ExtendableObject._streetNameAutocompleteTimeout = null;
    ExtendableObject._streetNameAutocompleteRequestIndex = 0;
    ExtendableObject._streetNamePredictionsIndex = 0;
  },
  /**
   * Place for registering fields that should be exposed on the ExtendableObject.
   * Add here:
   * - Public properties with getters/setters
   * - Field tracking registration
   * - Property change handling logic
   * - Prediction management properties
   * @param {Object} ExtendableObject - The base object being extended
   */
  registerFields: function registerFields(ExtendableObject) {
    Object.defineProperty(ExtendableObject, 'streetName', {
      get: function get() {
        return ExtendableObject.getStreetName();
      },
      set: function () {
        var _set = StreetNameExtension_asyncToGenerator(/*#__PURE__*/StreetNameExtension_regeneratorRuntime().mark(function _callee(value) {
          return StreetNameExtension_regeneratorRuntime().wrap(function _callee$(_context) {
            while (1) switch (_context.prev = _context.next) {
              case 0:
                _context.next = 2;
                return ExtendableObject.setStreetName(value);
              case 2:
              case "end":
                return _context.stop();
            }
          }, _callee);
        }));
        function set(_x) {
          return _set.apply(this, arguments);
        }
        return set;
      }()
    });
    ExtendableObject.getStreetName = function () {
      return ExtendableObject._streetName;
    };
    ExtendableObject.setStreetName = /*#__PURE__*/function () {
      var _ref = StreetNameExtension_asyncToGenerator(/*#__PURE__*/StreetNameExtension_regeneratorRuntime().mark(function _callee2(streetNameValue) {
        var needToNotify, needToDisplayAutocompleteDropdown, needToUpdateStreetFull, resolvedValue, streetName;
        return StreetNameExtension_regeneratorRuntime().wrap(function _callee2$(_context2) {
          while (1) switch (_context2.prev = _context2.next) {
            case 0:
              needToNotify = ExtendableObject.active && ExtendableObject._allowToNotifyStreetNameSubscribers;
              needToDisplayAutocompleteDropdown = ExtendableObject.active && ExtendableObject.config.useAutocomplete && ExtendableObject._allowFetchStreetNameAutocomplete;
              needToUpdateStreetFull = ExtendableObject.active && ExtendableObject._allowStreetFullCompose;
              ExtendableObject._awaits++;
              _context2.prev = 4;
              _context2.next = 7;
              return ExtendableObject.util.Promise.resolve(streetNameValue);
            case 7:
              resolvedValue = _context2.sent;
              _context2.next = 10;
              return ExtendableObject.cb.setStreetName(resolvedValue);
            case 10:
              streetName = _context2.sent;
              if (!(streetName === ExtendableObject._streetName)) {
                _context2.next = 13;
                break;
              }
              return _context2.abrupt("return");
            case 13:
              ExtendableObject._streetName = streetName;
              if (ExtendableObject.active) {
                ExtendableObject._changed = true;
                ExtendableObject.addressStatus = [];
              }
              if (needToNotify) {
                // Inform all subscribers about the change.
                ExtendableObject._subscribers.streetName.forEach(function (subscriber) {
                  subscriber.value = streetName;
                });
              }
              if (!needToUpdateStreetFull) {
                _context2.next = 19;
                break;
              }
              _context2.next = 19;
              return ExtendableObject.util.ensureStreetFullIntegrity(ExtendableObject.countryCode, streetName, ExtendableObject.buildingNumber);
            case 19:
              if (!needToDisplayAutocompleteDropdown) {
                _context2.next = 22;
                break;
              }
              _context2.next = 22;
              return ExtendableObject.util.displayStreetNameAutocompleteDropdown(streetName);
            case 22:
              _context2.next = 27;
              break;
            case 24:
              _context2.prev = 24;
              _context2.t0 = _context2["catch"](4);
              console.warn("Error while setting the field 'streetName'", _context2.t0);
            case 27:
              _context2.prev = 27;
              ExtendableObject._awaits--;
              return _context2.finish(27);
            case 30:
            case "end":
              return _context2.stop();
          }
        }, _callee2, null, [[4, 24, 27, 30]]);
      }));
      return function (_x2) {
        return _ref.apply(this, arguments);
      };
    }();
    ExtendableObject.fieldNames.push('streetName');
    Object.defineProperty(ExtendableObject, 'streetNamePredictions', {
      get: function get() {
        return ExtendableObject.getStreetNamePredictions;
      },
      set: function () {
        var _set2 = StreetNameExtension_asyncToGenerator(/*#__PURE__*/StreetNameExtension_regeneratorRuntime().mark(function _callee3(value) {
          return StreetNameExtension_regeneratorRuntime().wrap(function _callee3$(_context3) {
            while (1) switch (_context3.prev = _context3.next) {
              case 0:
                _context3.next = 2;
                return ExtendableObject.setStreetNamePredictions(value);
              case 2:
              case "end":
                return _context3.stop();
            }
          }, _callee3);
        }));
        function set(_x3) {
          return _set2.apply(this, arguments);
        }
        return set;
      }()
    });
    ExtendableObject.getStreetNamePredictions = function () {
      return ExtendableObject._streetNamePredictions;
    };
    ExtendableObject.setStreetNamePredictions = /*#__PURE__*/function () {
      var _ref2 = StreetNameExtension_asyncToGenerator(/*#__PURE__*/StreetNameExtension_regeneratorRuntime().mark(function _callee4(value) {
        var resolvedValue;
        return StreetNameExtension_regeneratorRuntime().wrap(function _callee4$(_context4) {
          while (1) switch (_context4.prev = _context4.next) {
            case 0:
              _context4.next = 2;
              return ExtendableObject.util.Promise.resolve(value);
            case 2:
              resolvedValue = _context4.sent;
              if (ExtendableObject.streetNamePredictions !== resolvedValue) {
                ExtendableObject._streetNamePredictions = resolvedValue;
                ExtendableObject._streetNamePredictionsIndex = 0;
              }
            case 4:
            case "end":
              return _context4.stop();
          }
        }, _callee4);
      }));
      return function (_x4) {
        return _ref2.apply(this, arguments);
      };
    }();
  },
  /**
   * Place for registering event-related callback functions.
   * Add here:
   * - Event handlers (onChange, onInput, onBlur, etc.)
   * - Keyboard interaction handlers
   * - Event propagation control
   * - Event-triggered state updates
   * @param {Object} ExtendableObject - The base object being extended
   */
  registerEventCallbacks: function registerEventCallbacks(ExtendableObject) {
    /**
     * Handler for street name changes (field not in focus)
     * @param {Object} subscriber - The subscriber object that triggered the change
     * @returns {Function} Event handler that updates streetName while preventing circular updates
     */
    ExtendableObject.cb.streetNameChange = function (subscriber) {
      return function () {
        ExtendableObject._allowToNotifyStreetNameSubscribers = false;
        ExtendableObject.streetName = subscriber.value;
        ExtendableObject._allowToNotifyStreetNameSubscribers = true;
      };
    };

    /**
     * Handler for street name changes via direct input (field in focus)
     * @param {Object} subscriber - The subscriber object that triggered the input
     * @returns {Function} Event handler that updates streetName while preventing circular updates
     */
    ExtendableObject.cb.streetNameInput = function (subscriber) {
      return function () {
        ExtendableObject._allowToNotifyStreetNameSubscribers = false;
        ExtendableObject.streetName = subscriber.value;
        ExtendableObject._allowToNotifyStreetNameSubscribers = true;
      };
    };

    /**
     * Handler for field blur events
     * @returns {Function} Event handler that manages validation and dropdown cleanup
     * - Cleans up predictions dropdown
     * - Handles validation timing
     * - Manages field state on blur
     */
    ExtendableObject.cb.streetNameBlur = function () {
      return /*#__PURE__*/StreetNameExtension_asyncToGenerator(/*#__PURE__*/StreetNameExtension_regeneratorRuntime().mark(function _callee6() {
        var isAnyActive;
        return StreetNameExtension_regeneratorRuntime().wrap(function _callee6$(_context6) {
          while (1) switch (_context6.prev = _context6.next) {
            case 0:
              _context6.prev = 0;
              // Handle dropdown cleanup
              isAnyActive = ExtendableObject._subscribers.streetName.some(function (sub) {
                return document.activeElement === sub.object;
              });
              if (!isAnyActive) {
                ExtendableObject.streetNamePredictions = [];
                ExtendableObject.util.removeStreetNamePredictionsDropdown();
              }
              _context6.next = 5;
              return ExtendableObject.waitUntilReady();
            case 5:
              // Clear existing timeout if any
              if (ExtendableObject.onBlurTimeout) {
                clearTimeout(ExtendableObject.onBlurTimeout);
                ExtendableObject.onBlurTimeout = null;
              }

              // Set new timeout for address check
              ExtendableObject.onBlurTimeout = setTimeout(/*#__PURE__*/StreetNameExtension_asyncToGenerator(/*#__PURE__*/StreetNameExtension_regeneratorRuntime().mark(function _callee5() {
                var shouldCheckAddress;
                return StreetNameExtension_regeneratorRuntime().wrap(function _callee5$(_context5) {
                  while (1) switch (_context5.prev = _context5.next) {
                    case 0:
                      shouldCheckAddress = ExtendableObject.config.trigger.onblur && !ExtendableObject.anyActive() && ExtendableObject.util.shouldBeChecked() && !window.EnderecoIntegrator.hasSubmit;
                      if (!shouldCheckAddress) {
                        _context5.next = 12;
                        break;
                      }
                      clearTimeout(ExtendableObject.onBlurTimeout);
                      ExtendableObject.onBlurTimeout = null;
                      _context5.prev = 4;
                      _context5.next = 7;
                      return ExtendableObject.util.checkAddress();
                    case 7:
                      _context5.next = 12;
                      break;
                    case 9:
                      _context5.prev = 9;
                      _context5.t0 = _context5["catch"](4);
                      console.warn('Error checking address:', _context5.t0);
                    case 12:
                    case "end":
                      return _context5.stop();
                  }
                }, _callee5, null, [[4, 9]]);
              })), ExtendableObject.config.ux.delay.onBlur);
              _context6.next = 12;
              break;
            case 9:
              _context6.prev = 9;
              _context6.t0 = _context6["catch"](0);
              console.warn('Error in streetNameBlur handler:', _context6.t0);
            case 12:
            case "end":
              return _context6.stop();
          }
        }, _callee6, null, [[0, 9]]);
      }));
    };

    /**
     * Handler for keyboard events on the streetName field
     * @returns {Function} Event handler for keyboard navigation
     * - Handles up/down arrows for prediction navigation
     * - Handles enter for prediction selection
     * - Handles backspace for smartFill control
     */
    ExtendableObject.cb.streetNameKeydown = function () {
      return function (e) {
        if (e.key === 'ArrowUp' || e.key === 'Up') {
          e.preventDefault();
          e.stopPropagation();
          if (ExtendableObject._streetNamePredictionsIndex > -1) {
            ExtendableObject._streetNamePredictionsIndex = ExtendableObject._streetNamePredictionsIndex - 1;
            ExtendableObject.util.renderStreetNamePredictionsDropdown(ExtendableObject.streetNamePredictions);
          }
        } else if (e.key === 'ArrowDown' || e.key === 'Down') {
          e.preventDefault();
          e.stopPropagation();
          if (ExtendableObject._streetNamePredictionsIndex < ExtendableObject._streetNamePredictions.length - 1) {
            ExtendableObject._streetNamePredictionsIndex = ExtendableObject._streetNamePredictionsIndex + 1;
            ExtendableObject.util.renderStreetNamePredictionsDropdown(ExtendableObject.streetNamePredictions);
          }
        } else if (e.key === 'Tab' || e.key === 'Tab') {
          // TODO: configurable activate in future releases.
          /*
          if (0 < ExtendableObject._streetNamePredictions.length) {
              e.preventDefault();
              e.stopPropagation();
              ExtendableObject.cb.copyStreetNameFromPrediction();
          } */
        } else if (e.key === 'Enter' || e.key === 'Enter') {
          if (ExtendableObject._streetNamePredictions.length > 0) {
            e.preventDefault();
            e.stopPropagation();
            ExtendableObject._allowFetchStreetNameAutocomplete = false;
            ExtendableObject.streetName = ExtendableObject.streetNamePredictions[ExtendableObject._streetNamePredictionsIndex].streetName;
            ExtendableObject.streetNamePredictions = [];
            ExtendableObject._allowFetchStreetNameAutocomplete = true;
            ExtendableObject.util.removeStreetNamePredictionsDropdown();
          }
        } else if (e.key === 'Backspace' || e.key === 'Backspace') {
          ExtendableObject.config.ux.smartFill = false;
        }
      };
    };
  },
  /**
   * Place for registering utility functions used across the extension.
   * Add here:
   * - UI manipulation functions
   * - Data processing helpers
   * - State management utilities
   * - Formatting functions
   * @param {Object} ExtendableObject - The base object being extended
   */
  registerUtilities: function registerUtilities(ExtendableObject) {
    /**
     * Manages the display of street name autocomplete suggestions dropdown
     * @param {string} streetName - Current street name input value
     * @returns {Promise<void>}
     */
    ExtendableObject.util.displayStreetNameAutocompleteDropdown = function (streetName) {
      ExtendableObject.util.removeStreetNamePredictionsDropdown();
      if (ExtendableObject._streetNameAutocompleteTimeout) {
        clearTimeout(ExtendableObject._streetNameAutocompleteTimeout);
      }
      ExtendableObject._streetNameAutocompleteTimeout = setTimeout(/*#__PURE__*/StreetNameExtension_asyncToGenerator(/*#__PURE__*/StreetNameExtension_regeneratorRuntime().mark(function _callee7() {
        var autocompleteResult;
        return StreetNameExtension_regeneratorRuntime().wrap(function _callee7$(_context7) {
          while (1) switch (_context7.prev = _context7.next) {
            case 0:
              _context7.prev = 0;
              _context7.next = 3;
              return ExtendableObject.util.getStreetNamePredictions(streetName);
            case 3:
              autocompleteResult = _context7.sent;
              if (!(autocompleteResult.originalStreetName !== streetName)) {
                _context7.next = 6;
                break;
              }
              return _context7.abrupt("return");
            case 6:
              ExtendableObject.streetNamePredictions = autocompleteResult.predictions;
              ExtendableObject.util.renderStreetNamePredictionsDropdown(autocompleteResult.predictions);
              _context7.next = 13;
              break;
            case 10:
              _context7.prev = 10;
              _context7.t0 = _context7["catch"](0);
              console.warn('Failed fetching predictions', _context7.t0, streetName);
            case 13:
              ExtendableObject._streetNameAutocompleteTimeout = null;
            case 14:
            case "end":
              return _context7.stop();
          }
        }, _callee7, null, [[0, 10]]);
      })), ExtendableObject.config.ux.delay.inputAssistant);
    };

    /**
     * Renders the street name predictions dropdown with diff highlighting
     * @param {Array} predictions - Array of street name predictions
     * Handles:
     * - Dropdown positioning
     * - Diff highlighting for suggestions
     * - Event listeners for selection
     */
    ExtendableObject.util.renderStreetNamePredictionsDropdown = function (predictions) {
      predictions = JSON.parse(JSON.stringify(predictions)); // Create a copy

      // Render dropdown under the input element
      ExtendableObject._subscribers.streetName.forEach(function (subscriber) {
        if (document.querySelector('[endereco-street-name-predictions]')) {
          ExtendableObject._openDropdowns--;
          document.querySelector('[endereco-street-name-predictions]').parentNode.removeChild(document.querySelector('[endereco-street-name-predictions]'));
        }

        // Render predictions only if the field is active and there are predictions.
        if (predictions.length > 0 && document.activeElement === subscriber.object) {
          var startingIndex = 0;
          var preparedPredictions = [];

          // Prepare predictions.
          predictions.forEach(function (prediction) {
            var diff = diffChars(ExtendableObject.streetName, prediction.streetName, {
              ignoreCase: true
            });
            var streetNameHtml = '';
            diff.forEach(function (part) {
              var markClass = part.added ? 'endereco-span--add' : part.removed ? 'endereco-span--remove' : 'endereco-span--neutral';
              streetNameHtml += "<span class=\"".concat(markClass, "\">").concat(part.value.replace(/[ ]/g, '&nbsp;'), "</span>");
            });
            preparedPredictions.push({
              streetName: prediction.streetName,
              streetNameDiff: streetNameHtml
            });
          });

          // Prepare dropdown.
          var predictionsHtml = ExtendableObject.util.Mustache.render(ExtendableObject.config.templates.streetNamePredictions, {
            ExtendableObject: ExtendableObject,
            predictions: preparedPredictions,
            offsetTop: subscriber.object.offsetTop + subscriber.object.offsetHeight,
            offsetLeft: subscriber.object.offsetLeft,
            width: subscriber.object.offsetWidth,
            direction: getComputedStyle(subscriber.object).direction,
            longList: preparedPredictions.length > MAX_PREDICTIONS_BEFORE_SCROLL,
            index: function index() {
              return startingIndex - 1; // TODO: add loop finishing function to increase counter.
            },
            isActive: function isActive() {
              var isActive = startingIndex === ExtendableObject._streetNamePredictionsIndex;
              startingIndex++;
              return isActive;
            }
          });

          // Attach it to HTML.
          subscriber.object.insertAdjacentHTML('afterend', predictionsHtml);
          ExtendableObject._openDropdowns++;
          document.querySelectorAll("[data-id=\"".concat(ExtendableObject.id, "\"] [endereco-street-name-prediction]")).forEach(function (DOMElement) {
            DOMElement.addEventListener('mousedown', function (e) {
              var index = parseInt(this.getAttribute('data-prediction-index'));
              e.preventDefault();
              e.stopPropagation();
              ExtendableObject._allowFetchStreetNameAutocomplete = false;
              ExtendableObject.streetName = predictions[index].streetName;
              ExtendableObject.streetNamePredictions = [];
              ExtendableObject._allowFetchStreetNameAutocomplete = true;
              ExtendableObject.util.removeStreetNamePredictionsDropdown();
            });
          });
        }
      });
    };

    /**
     * Removes the street name predictions dropdown from the DOM
     * Updates the dropdown counter and cleans up related state
     */

    ExtendableObject.util.removeStreetNamePredictionsDropdown = function () {
      ExtendableObject._subscribers.streetName.forEach(function () {
        var dropdown = document.querySelector('[endereco-street-name-predictions]');
        if (dropdown) {
          ExtendableObject._openDropdowns--;
          dropdown.parentNode.removeChild(dropdown);
        }
      });
    };

    /**
     * Ensures street full is properly composed from name and number
     * @param {string} countryCode - Country code for formatting
     * @param {string} streetName - Street name component
     * @param {string} buildingNumber - Building number component
     * @returns {Promise<void>}
     */
    ExtendableObject.util.ensureStreetFullIntegrity = function (countryCode, streetName, buildingNumber) {
      if (ExtendableObject._streetFullComposeTimeout) {
        clearTimeout(ExtendableObject._streetFullComposeTimeout);
      }
      ExtendableObject._streetFullComposeTimeout = setTimeout(function () {
        try {
          var streetFull = ExtendableObject.util.Mustache.render(ExtendableObject.config.templates.streetFull.getTemplate(countryCode), {
            streetName: streetName,
            buildingNumber: buildingNumber
          }).replace(/  +/g, ' ').replace(/(\r\n|\n|\r)/gm, '').trim();

          // Check if result is outdated
          if (ExtendableObject.streetName !== streetName || ExtendableObject.buildingNumber !== buildingNumber) {
            return;
          }
          ExtendableObject._allowStreetFullSplit = false;
          ExtendableObject._allowFetchStreetFullAutocomplete = false;
          ExtendableObject.streetFull = streetFull;
          ExtendableObject._allowStreetFullSplit = true;
          ExtendableObject._allowFetchStreetFullAutocomplete = true;
        } catch (e) {
          console.warn('Street compose failed', e, countryCode, streetName, buildingNumber);
        }
        ExtendableObject._streetFullComposeTimeout = null;
      }, ExtendableObject.config.ux.delay.inputAssistant);
    };
  },
  /**
   * Place for registering API communication functions.
   * Add here:
   * - API request functions
   * - Response handling
   * - Error management
   * - Caching logic
   * @param {Object} ExtendableObject - The base object being extended
   */
  registerAPIHandlers: function registerAPIHandlers(ExtendableObject) {
    /**
     * Fetches street name predictions from API
     * @param {string} streetNameRaw - Raw street name input to get predictions for
     * @returns {Promise<Object>} Prediction results with original input
     * @throws {Error} If input is invalid or API request fails
     */
    ExtendableObject.util.getStreetNamePredictions = /*#__PURE__*/function () {
      var _ref6 = StreetNameExtension_asyncToGenerator(/*#__PURE__*/StreetNameExtension_regeneratorRuntime().mark(function _callee8(streetNameRaw) {
        var streetName, autocompleteResult, _ExtendableObject$has, autocompleteRequestIndex, message, headers, streetNamePredictionsTemp, _result$data, EnderecoAPI, result, _ExtendableObject$uti, _ExtendableObject$uti2;
        return StreetNameExtension_regeneratorRuntime().wrap(function _callee8$(_context8) {
          while (1) switch (_context8.prev = _context8.next) {
            case 0:
              if (!(typeof streetNameRaw !== 'string')) {
                _context8.next = 2;
                break;
              }
              throw new Error('Invalid argument. The streetNameRaw has to be a string');
            case 2:
              streetName = streetNameRaw.trim();
              autocompleteResult = {
                originalStreetName: streetName,
                predictions: []
              }; // Early return
              if (!(streetName === '')) {
                _context8.next = 6;
                break;
              }
              return _context8.abrupt("return", autocompleteResult);
            case 6:
              if (ExtendableObject.streetNameAutocompleteCache.cachedResults[streetName]) {
                _context8.next = 31;
                break;
              }
              autocompleteRequestIndex = ++ExtendableObject._streetNameAutocompleteRequestIndex;
              message = {
                jsonrpc: '2.0',
                id: autocompleteRequestIndex,
                method: 'streetAutocomplete',
                params: {
                  country: ExtendableObject.countryCode,
                  language: ExtendableObject.config.lang,
                  postCode: ExtendableObject.postalCode,
                  cityName: ExtendableObject.locality,
                  street: streetName,
                  houseNumber: ExtendableObject.buildingNumber
                }
              };
              headers = {
                'X-Auth-Key': ExtendableObject.config.apiKey,
                'X-Agent': ExtendableObject.config.agentName,
                'X-Remote-Api-Url': ExtendableObject.config.remoteApiUrl,
                'X-Transaction-Referer': window.location.href,
                'X-Transaction-Id': (_ExtendableObject$has = ExtendableObject.hasLoadedExtension) !== null && _ExtendableObject$has !== void 0 && _ExtendableObject$has.call(ExtendableObject, 'SessionExtension') ? ExtendableObject.sessionId : 'not_required'
              };
              streetNamePredictionsTemp = [];
              _context8.prev = 11;
              EnderecoAPI = ExtendableObject.getEnderecoAPI();
              if (EnderecoAPI) {
                _context8.next = 16;
                break;
              }
              console.warn('EnderecoAPI is not available');
              return _context8.abrupt("return", autocompleteResult);
            case 16:
              _context8.next = 18;
              return EnderecoAPI.sendRequestToAPI(message, headers);
            case 18:
              result = _context8.sent;
              if ((result === null || result === void 0 || (_result$data = result.data) === null || _result$data === void 0 || (_result$data = _result$data.error) === null || _result$data === void 0 ? void 0 : _result$data.code) === ERROR_EXPIRED_SESSION) {
                (_ExtendableObject$uti = (_ExtendableObject$uti2 = ExtendableObject.util).updateSessionId) === null || _ExtendableObject$uti === void 0 || _ExtendableObject$uti.call(_ExtendableObject$uti2);
              }
              if (!(!result || !result.data || !result.data.result)) {
                _context8.next = 23;
                break;
              }
              console.warn("API didn't return a valid result");
              return _context8.abrupt("return", autocompleteResult);
            case 23:
              // If session counter is set, increase it.
              if (ExtendableObject.hasLoadedExtension('SessionExtension')) {
                ExtendableObject.sessionCounter++;
              }
              result.data.result.predictions.forEach(function (streetNamePrediction) {
                if (!streetNamePrediction) return;
                var _ref7 = streetNamePrediction || {},
                  country = _ref7.country,
                  street = _ref7.street,
                  buildingNumber = _ref7.buildingNumber,
                  additionalInfo = _ref7.additionalInfo;
                var tempStreetNameContainer = {
                  countryCode: country !== null && country !== void 0 ? country : ExtendableObject._countryCode,
                  streetName: street !== null && street !== void 0 ? street : '',
                  buildingNumber: buildingNumber !== null && buildingNumber !== void 0 ? buildingNumber : '',
                  additionalInfo: additionalInfo !== null && additionalInfo !== void 0 ? additionalInfo : ''
                };
                if (streetNamePrediction.streetFull === undefined) {
                  tempStreetNameContainer.streetFull = ExtendableObject.util.Mustache.render(streetNameTemplates.getTemplate(tempStreetNameContainer.countryCode), {
                    streetName: tempStreetNameContainer.streetName,
                    buildingNumber: tempStreetNameContainer.buildingNumber,
                    additionalInfo: tempStreetNameContainer.additionalInfo
                  }).replace(/(\r\n|\n|\r)/gm, '').trim();
                }
                streetNamePredictionsTemp.push(tempStreetNameContainer);
              });
              _context8.next = 30;
              break;
            case 27:
              _context8.prev = 27;
              _context8.t0 = _context8["catch"](11);
              console.warn('Error fetching street full predictions', _context8.t0);
            case 30:
              ExtendableObject.streetNameAutocompleteCache.cachedResults[streetName] = streetNamePredictionsTemp;
            case 31:
              autocompleteResult.predictions = ExtendableObject.streetNameAutocompleteCache.cachedResults[streetName];
              return _context8.abrupt("return", autocompleteResult);
            case 33:
            case "end":
              return _context8.stop();
          }
        }, _callee8, null, [[11, 27]]);
      }));
      return function (_x5) {
        return _ref6.apply(this, arguments);
      };
    }();
  },
  /**
   * Place for registering data transformation and validation functions.
   * Add here:
   * - Input filters
   * - Data validators
   * - Value transformers
   * - Custom formatters
   * @param {Object} ExtendableObject - The base object being extended
   */
  registerFilterCallbacks: function registerFilterCallbacks(ExtendableObject) {
    /**
     * Filter function for streetName value processing
     * @param {string} streetName - Street name value to process
     * @returns {string} Processed street name value
     */
    ExtendableObject.cb.setStreetName = function (streetName) {
      return streetName;
    };
  },
  /**
   * Place for registering templates and configuration.
   * Add here:
   * - HTML templates
   * - UI configuration
   * - Display format templates
   * @param {Object} ExtendableObject - The base object being extended
   */
  registerConfig: function registerConfig(ExtendableObject) {
    ExtendableObject.config.templates.streetNamePredictions = street_name_predictions;
  },
  /**
   * Main extension function that sets up all streetName functionality.
   * @param {Object} ExtendableObject - The base object being extended
   * @returns {Promise<Object>} - Resolves to the StreetNameExtension object
   *
   * Executes all registration functions in sequence:
   * 1. registerProperties - Sets up internal state
   * 2. registerFields - Adds streetName field
   * 3. registerConfig - Sets up templates
   * 4. registerEventCallbacks - Adds event handlers
   * 5. registerUtilities - Adds helper functions
   * 6. registerAPIHandlers - Sets up API communication
   * 7. registerFilterCallbacks - Adds data filters
   *
   * All registrations are handled asynchronously to ensure proper setup.
   * The extension object is returned to confirm successful extension.
   */
  extend: function () {
    var _extend = StreetNameExtension_asyncToGenerator(/*#__PURE__*/StreetNameExtension_regeneratorRuntime().mark(function _callee9(ExtendableObject) {
      return StreetNameExtension_regeneratorRuntime().wrap(function _callee9$(_context9) {
        while (1) switch (_context9.prev = _context9.next) {
          case 0:
            _context9.next = 2;
            return StreetNameExtension.registerProperties(ExtendableObject);
          case 2:
            _context9.next = 4;
            return StreetNameExtension.registerFields(ExtendableObject);
          case 4:
            _context9.next = 6;
            return StreetNameExtension.registerConfig(ExtendableObject);
          case 6:
            _context9.next = 8;
            return StreetNameExtension.registerEventCallbacks(ExtendableObject);
          case 8:
            _context9.next = 10;
            return StreetNameExtension.registerUtilities(ExtendableObject);
          case 10:
            _context9.next = 12;
            return StreetNameExtension.registerAPIHandlers(ExtendableObject);
          case 12:
            _context9.next = 14;
            return StreetNameExtension.registerFilterCallbacks(ExtendableObject);
          case 14:
            return _context9.abrupt("return", StreetNameExtension);
          case 15:
          case "end":
            return _context9.stop();
        }
      }, _callee9);
    }));
    function extend(_x6) {
      return _extend.apply(this, arguments);
    }
    return extend;
  }()
};
/* harmony default export */ var fields_StreetNameExtension = (StreetNameExtension);
;// ./modules/extensions/checks/StreetNameCheckExtension.js
var StreetNameCheckExtension = {
  name: 'StreetNameCheckExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      ExtendableObject.waitForExtension('StreetNameExtension').then(function () {
        // Add email field.
        ExtendableObject._streetNameStatus = '';
        ExtendableObject._subscribers.streetNameStatus = [];

        // Add change event hadler.
        ExtendableObject.cb.streetNameStatusChange = function (subscriber) {
          return function (e) {
            ExtendableObject.streetNameStatus = subscriber.value;
          };
        };

        // Add the "emaiL" property
        Object.defineProperty(ExtendableObject, 'streetNameStatus', {
          get: function get() {
            return this._streetNameStatus;
          },
          set: function set(value) {
            var oldValue = ExtendableObject._streetNameStatus;
            ExtendableObject._awaits++;
            ExtendableObject.util.Promise.resolve(value).then(function (value) {
              var newValue = value;
              if (oldValue !== newValue) {
                ExtendableObject._streetNameStatus = newValue;

                // Inform all subscribers about the change.
                ExtendableObject._subscribers.streetNameStatus.forEach(function (subscriber) {
                  subscriber.value = value;
                });
                ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                  detail: {
                    fieldName: 'streetNameStatus',
                    oldValue: oldValue,
                    newValue: newValue,
                    object: ExtendableObject
                  }
                }));
              }
            }).catch().finally(function () {
              ExtendableObject._awaits--;
            });
          }
        });
        if (ExtendableObject.config.showDebugInfo) {
          console.log('StreetNameCheckExtension applied');
        }
        resolve($self);
      }).catch(function () {
        console.log('Failed to load because of timeout');
        reject($self);
      });
    });
  }
};
/* harmony default export */ var checks_StreetNameCheckExtension = (StreetNameCheckExtension);
;// ./modules/extensions/fields/BuildingNumberExtension.js
function BuildingNumberExtension_typeof(o) { "@babel/helpers - typeof"; return BuildingNumberExtension_typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, BuildingNumberExtension_typeof(o); }
function BuildingNumberExtension_regeneratorRuntime() { "use strict"; /*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/facebook/regenerator/blob/main/LICENSE */ BuildingNumberExtension_regeneratorRuntime = function _regeneratorRuntime() { return e; }; var t, e = {}, r = Object.prototype, n = r.hasOwnProperty, o = Object.defineProperty || function (t, e, r) { t[e] = r.value; }, i = "function" == typeof Symbol ? Symbol : {}, a = i.iterator || "@@iterator", c = i.asyncIterator || "@@asyncIterator", u = i.toStringTag || "@@toStringTag"; function define(t, e, r) { return Object.defineProperty(t, e, { value: r, enumerable: !0, configurable: !0, writable: !0 }), t[e]; } try { define({}, ""); } catch (t) { define = function define(t, e, r) { return t[e] = r; }; } function wrap(t, e, r, n) { var i = e && e.prototype instanceof Generator ? e : Generator, a = Object.create(i.prototype), c = new Context(n || []); return o(a, "_invoke", { value: makeInvokeMethod(t, r, c) }), a; } function tryCatch(t, e, r) { try { return { type: "normal", arg: t.call(e, r) }; } catch (t) { return { type: "throw", arg: t }; } } e.wrap = wrap; var h = "suspendedStart", l = "suspendedYield", f = "executing", s = "completed", y = {}; function Generator() {} function GeneratorFunction() {} function GeneratorFunctionPrototype() {} var p = {}; define(p, a, function () { return this; }); var d = Object.getPrototypeOf, v = d && d(d(values([]))); v && v !== r && n.call(v, a) && (p = v); var g = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(p); function defineIteratorMethods(t) { ["next", "throw", "return"].forEach(function (e) { define(t, e, function (t) { return this._invoke(e, t); }); }); } function AsyncIterator(t, e) { function invoke(r, o, i, a) { var c = tryCatch(t[r], t, o); if ("throw" !== c.type) { var u = c.arg, h = u.value; return h && "object" == BuildingNumberExtension_typeof(h) && n.call(h, "__await") ? e.resolve(h.__await).then(function (t) { invoke("next", t, i, a); }, function (t) { invoke("throw", t, i, a); }) : e.resolve(h).then(function (t) { u.value = t, i(u); }, function (t) { return invoke("throw", t, i, a); }); } a(c.arg); } var r; o(this, "_invoke", { value: function value(t, n) { function callInvokeWithMethodAndArg() { return new e(function (e, r) { invoke(t, n, e, r); }); } return r = r ? r.then(callInvokeWithMethodAndArg, callInvokeWithMethodAndArg) : callInvokeWithMethodAndArg(); } }); } function makeInvokeMethod(e, r, n) { var o = h; return function (i, a) { if (o === f) throw Error("Generator is already running"); if (o === s) { if ("throw" === i) throw a; return { value: t, done: !0 }; } for (n.method = i, n.arg = a;;) { var c = n.delegate; if (c) { var u = maybeInvokeDelegate(c, n); if (u) { if (u === y) continue; return u; } } if ("next" === n.method) n.sent = n._sent = n.arg;else if ("throw" === n.method) { if (o === h) throw o = s, n.arg; n.dispatchException(n.arg); } else "return" === n.method && n.abrupt("return", n.arg); o = f; var p = tryCatch(e, r, n); if ("normal" === p.type) { if (o = n.done ? s : l, p.arg === y) continue; return { value: p.arg, done: n.done }; } "throw" === p.type && (o = s, n.method = "throw", n.arg = p.arg); } }; } function maybeInvokeDelegate(e, r) { var n = r.method, o = e.iterator[n]; if (o === t) return r.delegate = null, "throw" === n && e.iterator.return && (r.method = "return", r.arg = t, maybeInvokeDelegate(e, r), "throw" === r.method) || "return" !== n && (r.method = "throw", r.arg = new TypeError("The iterator does not provide a '" + n + "' method")), y; var i = tryCatch(o, e.iterator, r.arg); if ("throw" === i.type) return r.method = "throw", r.arg = i.arg, r.delegate = null, y; var a = i.arg; return a ? a.done ? (r[e.resultName] = a.value, r.next = e.nextLoc, "return" !== r.method && (r.method = "next", r.arg = t), r.delegate = null, y) : a : (r.method = "throw", r.arg = new TypeError("iterator result is not an object"), r.delegate = null, y); } function pushTryEntry(t) { var e = { tryLoc: t[0] }; 1 in t && (e.catchLoc = t[1]), 2 in t && (e.finallyLoc = t[2], e.afterLoc = t[3]), this.tryEntries.push(e); } function resetTryEntry(t) { var e = t.completion || {}; e.type = "normal", delete e.arg, t.completion = e; } function Context(t) { this.tryEntries = [{ tryLoc: "root" }], t.forEach(pushTryEntry, this), this.reset(!0); } function values(e) { if (e || "" === e) { var r = e[a]; if (r) return r.call(e); if ("function" == typeof e.next) return e; if (!isNaN(e.length)) { var o = -1, i = function next() { for (; ++o < e.length;) if (n.call(e, o)) return next.value = e[o], next.done = !1, next; return next.value = t, next.done = !0, next; }; return i.next = i; } } throw new TypeError(BuildingNumberExtension_typeof(e) + " is not iterable"); } return GeneratorFunction.prototype = GeneratorFunctionPrototype, o(g, "constructor", { value: GeneratorFunctionPrototype, configurable: !0 }), o(GeneratorFunctionPrototype, "constructor", { value: GeneratorFunction, configurable: !0 }), GeneratorFunction.displayName = define(GeneratorFunctionPrototype, u, "GeneratorFunction"), e.isGeneratorFunction = function (t) { var e = "function" == typeof t && t.constructor; return !!e && (e === GeneratorFunction || "GeneratorFunction" === (e.displayName || e.name)); }, e.mark = function (t) { return Object.setPrototypeOf ? Object.setPrototypeOf(t, GeneratorFunctionPrototype) : (t.__proto__ = GeneratorFunctionPrototype, define(t, u, "GeneratorFunction")), t.prototype = Object.create(g), t; }, e.awrap = function (t) { return { __await: t }; }, defineIteratorMethods(AsyncIterator.prototype), define(AsyncIterator.prototype, c, function () { return this; }), e.AsyncIterator = AsyncIterator, e.async = function (t, r, n, o, i) { void 0 === i && (i = Promise); var a = new AsyncIterator(wrap(t, r, n, o), i); return e.isGeneratorFunction(r) ? a : a.next().then(function (t) { return t.done ? t.value : a.next(); }); }, defineIteratorMethods(g), define(g, u, "Generator"), define(g, a, function () { return this; }), define(g, "toString", function () { return "[object Generator]"; }), e.keys = function (t) { var e = Object(t), r = []; for (var n in e) r.push(n); return r.reverse(), function next() { for (; r.length;) { var t = r.pop(); if (t in e) return next.value = t, next.done = !1, next; } return next.done = !0, next; }; }, e.values = values, Context.prototype = { constructor: Context, reset: function reset(e) { if (this.prev = 0, this.next = 0, this.sent = this._sent = t, this.done = !1, this.delegate = null, this.method = "next", this.arg = t, this.tryEntries.forEach(resetTryEntry), !e) for (var r in this) "t" === r.charAt(0) && n.call(this, r) && !isNaN(+r.slice(1)) && (this[r] = t); }, stop: function stop() { this.done = !0; var t = this.tryEntries[0].completion; if ("throw" === t.type) throw t.arg; return this.rval; }, dispatchException: function dispatchException(e) { if (this.done) throw e; var r = this; function handle(n, o) { return a.type = "throw", a.arg = e, r.next = n, o && (r.method = "next", r.arg = t), !!o; } for (var o = this.tryEntries.length - 1; o >= 0; --o) { var i = this.tryEntries[o], a = i.completion; if ("root" === i.tryLoc) return handle("end"); if (i.tryLoc <= this.prev) { var c = n.call(i, "catchLoc"), u = n.call(i, "finallyLoc"); if (c && u) { if (this.prev < i.catchLoc) return handle(i.catchLoc, !0); if (this.prev < i.finallyLoc) return handle(i.finallyLoc); } else if (c) { if (this.prev < i.catchLoc) return handle(i.catchLoc, !0); } else { if (!u) throw Error("try statement without catch or finally"); if (this.prev < i.finallyLoc) return handle(i.finallyLoc); } } } }, abrupt: function abrupt(t, e) { for (var r = this.tryEntries.length - 1; r >= 0; --r) { var o = this.tryEntries[r]; if (o.tryLoc <= this.prev && n.call(o, "finallyLoc") && this.prev < o.finallyLoc) { var i = o; break; } } i && ("break" === t || "continue" === t) && i.tryLoc <= e && e <= i.finallyLoc && (i = null); var a = i ? i.completion : {}; return a.type = t, a.arg = e, i ? (this.method = "next", this.next = i.finallyLoc, y) : this.complete(a); }, complete: function complete(t, e) { if ("throw" === t.type) throw t.arg; return "break" === t.type || "continue" === t.type ? this.next = t.arg : "return" === t.type ? (this.rval = this.arg = t.arg, this.method = "return", this.next = "end") : "normal" === t.type && e && (this.next = e), y; }, finish: function finish(t) { for (var e = this.tryEntries.length - 1; e >= 0; --e) { var r = this.tryEntries[e]; if (r.finallyLoc === t) return this.complete(r.completion, r.afterLoc), resetTryEntry(r), y; } }, catch: function _catch(t) { for (var e = this.tryEntries.length - 1; e >= 0; --e) { var r = this.tryEntries[e]; if (r.tryLoc === t) { var n = r.completion; if ("throw" === n.type) { var o = n.arg; resetTryEntry(r); } return o; } } throw Error("illegal catch attempt"); }, delegateYield: function delegateYield(e, r, n) { return this.delegate = { iterator: values(e), resultName: r, nextLoc: n }, "next" === this.method && (this.arg = t), y; } }, e; }
function BuildingNumberExtension_asyncGeneratorStep(n, t, e, r, o, a, c) { try { var i = n[a](c), u = i.value; } catch (n) { return void e(n); } i.done ? t(u) : Promise.resolve(u).then(r, o); }
function BuildingNumberExtension_asyncToGenerator(n) { return function () { var t = this, e = arguments; return new Promise(function (r, o) { var a = n.apply(t, e); function _next(n) { BuildingNumberExtension_asyncGeneratorStep(a, r, o, _next, _throw, "next", n); } function _throw(n) { BuildingNumberExtension_asyncGeneratorStep(a, r, o, _next, _throw, "throw", n); } _next(void 0); }); }; }
/**
 * Extends the base object with "buildingNumber" field handling capabilities.
 * Adds support for building number input with:
 * - Adding buildingNumber property with getter/setter
 * - Syncing with streetFull when combined with street name
 * - Basic event handling for input and validation
 * - Simple value processing and filtering
 */

var BuildingNumberExtension = {
  name: 'BuildingNumberExtension',
  /**
   * Place for registering internal properties needed for buildingNumber functionality.
   * Add here:
   * - Internal storage fields (_fieldName)
   * - Subscriber arrays (_subscribers.fieldName)
   * - Control flags for field behavior
   * @param {Object} ExtendableObject - The base object being extended
   */
  registerProperties: function registerProperties(ExtendableObject) {
    ExtendableObject._buildingNumber = '';
    ExtendableObject._subscribers.buildingNumber = [];

    // Flags
    ExtendableObject._allowToNotifyBuildingNumberSubscribers = true;
  },
  /**
   * Place for registering fields that should be exposed on the ExtendableObject.
   * Add here:
   * - Public properties with getters/setters
   * - Field tracking registration
   * - Property change handling logic
   * @param {Object} ExtendableObject - The base object being extended
   */
  registerFields: function registerFields(ExtendableObject) {
    // Add getter and setter for fields.
    Object.defineProperty(ExtendableObject, 'buildingNumber', {
      get: function get() {
        return ExtendableObject.getBuildingNumber();
      },
      set: function () {
        var _set = BuildingNumberExtension_asyncToGenerator(/*#__PURE__*/BuildingNumberExtension_regeneratorRuntime().mark(function _callee(value) {
          return BuildingNumberExtension_regeneratorRuntime().wrap(function _callee$(_context) {
            while (1) switch (_context.prev = _context.next) {
              case 0:
                _context.next = 2;
                return ExtendableObject.setBuildingNumber(value);
              case 2:
              case "end":
                return _context.stop();
            }
          }, _callee);
        }));
        function set(_x) {
          return _set.apply(this, arguments);
        }
        return set;
      }()
    });
    ExtendableObject.getBuildingNumber = function () {
      return ExtendableObject._buildingNumber;
    };
    ExtendableObject.setBuildingNumber = /*#__PURE__*/function () {
      var _ref = BuildingNumberExtension_asyncToGenerator(/*#__PURE__*/BuildingNumberExtension_regeneratorRuntime().mark(function _callee2(value) {
        var needToNotify, needToUpdateStreetFull, resolvedValue, buildingNumber;
        return BuildingNumberExtension_regeneratorRuntime().wrap(function _callee2$(_context2) {
          while (1) switch (_context2.prev = _context2.next) {
            case 0:
              needToNotify = ExtendableObject.active && ExtendableObject._allowToNotifyBuildingNumberSubscribers;
              needToUpdateStreetFull = ExtendableObject.active && ExtendableObject._allowStreetFullCompose;
              ExtendableObject._awaits++;
              _context2.prev = 3;
              _context2.next = 6;
              return ExtendableObject.util.Promise.resolve(value);
            case 6:
              resolvedValue = _context2.sent;
              _context2.next = 9;
              return ExtendableObject.cb.setBuildingNumber(resolvedValue);
            case 9:
              buildingNumber = _context2.sent;
              if (!(buildingNumber === ExtendableObject._buildingNumber)) {
                _context2.next = 12;
                break;
              }
              return _context2.abrupt("return");
            case 12:
              ExtendableObject._buildingNumber = buildingNumber;
              if (ExtendableObject.active) {
                ExtendableObject._changed = true;
                ExtendableObject.addressStatus = [];
              }
              if (needToNotify) {
                // Inform all subscribers about the change.
                ExtendableObject._subscribers.buildingNumber.forEach(function (subscriber) {
                  subscriber.value = buildingNumber;
                });
              }
              if (!needToUpdateStreetFull) {
                _context2.next = 18;
                break;
              }
              _context2.next = 18;
              return ExtendableObject.util.ensureStreetFullIntegrity(ExtendableObject.countryCode, ExtendableObject.streetName, buildingNumber);
            case 18:
              _context2.next = 23;
              break;
            case 20:
              _context2.prev = 20;
              _context2.t0 = _context2["catch"](3);
              console.warn("Error while setting the field 'buildingNumber'", _context2.t0);
            case 23:
              _context2.prev = 23;
              ExtendableObject._awaits--;
              return _context2.finish(23);
            case 26:
            case "end":
              return _context2.stop();
          }
        }, _callee2, null, [[3, 20, 23, 26]]);
      }));
      return function (_x2) {
        return _ref.apply(this, arguments);
      };
    }();
    ExtendableObject.fieldNames.push('buildingNumber');
  },
  /**
   * Place for registering event-related callback functions.
   * Add here:
   * - Event handlers (onChange, onInput, onBlur, etc.)
   * - Event propagation control
   * - Event-triggered state updates
   * @param {Object} ExtendableObject - The base object being extended
   */
  registerEventCallbacks: function registerEventCallbacks(ExtendableObject) {
    /**
     * Handler for building number changes (field not in focus)
     * @param {Object} subscriber - The subscriber object that triggered the change
     * @returns {Function} Event handler that updates buildingNumber while preventing circular updates
     */
    ExtendableObject.cb.buildingNumberChange = function (subscriber) {
      return function () {
        ExtendableObject._allowToNotifyBuildingNumberSubscribers = false;
        ExtendableObject.buildingNumber = subscriber.value;
        ExtendableObject._allowToNotifyBuildingNumberSubscribers = true;
      };
    };

    /**
     * Handler for building number changes (field in focus)
     * @param {Object} subscriber - The subscriber object that triggered the input
     * @returns {Function} Event handler that updates buildingNumber while preventing circular updates
     */
    ExtendableObject.cb.buildingNumberInput = function (subscriber) {
      return function () {
        ExtendableObject._allowToNotifyBuildingNumberSubscribers = false;
        ExtendableObject.buildingNumber = subscriber.value;
        ExtendableObject._allowToNotifyBuildingNumberSubscribers = true;
      };
    };

    /**
     * Handler for field blur events
     * @returns {Function} Event handler that manages validation timing
     * - Handles validation scheduling
     * - Manages field state on blur
     * - Triggers address checks when needed
     */
    ExtendableObject.cb.buildingNumberBlur = function () {
      return /*#__PURE__*/BuildingNumberExtension_asyncToGenerator(/*#__PURE__*/BuildingNumberExtension_regeneratorRuntime().mark(function _callee4() {
        return BuildingNumberExtension_regeneratorRuntime().wrap(function _callee4$(_context4) {
          while (1) switch (_context4.prev = _context4.next) {
            case 0:
              _context4.prev = 0;
              _context4.next = 3;
              return ExtendableObject.waitUntilReady();
            case 3:
              // Clear existing timeout if any
              if (ExtendableObject.onBlurTimeout) {
                clearTimeout(ExtendableObject.onBlurTimeout);
                ExtendableObject.onBlurTimeout = null;
              }

              // Set new timeout for address check
              ExtendableObject.onBlurTimeout = setTimeout(/*#__PURE__*/BuildingNumberExtension_asyncToGenerator(/*#__PURE__*/BuildingNumberExtension_regeneratorRuntime().mark(function _callee3() {
                var shouldCheckAddress;
                return BuildingNumberExtension_regeneratorRuntime().wrap(function _callee3$(_context3) {
                  while (1) switch (_context3.prev = _context3.next) {
                    case 0:
                      shouldCheckAddress = ExtendableObject.config.trigger.onblur && !ExtendableObject.anyActive() && ExtendableObject.util.shouldBeChecked() && !window.EnderecoIntegrator.hasSubmit;
                      if (!shouldCheckAddress) {
                        _context3.next = 12;
                        break;
                      }
                      clearTimeout(ExtendableObject.onBlurTimeout);
                      ExtendableObject.onBlurTimeout = null;
                      _context3.prev = 4;
                      _context3.next = 7;
                      return ExtendableObject.util.checkAddress();
                    case 7:
                      _context3.next = 12;
                      break;
                    case 9:
                      _context3.prev = 9;
                      _context3.t0 = _context3["catch"](4);
                      console.warn('Error checking address:', _context3.t0);
                    case 12:
                    case "end":
                      return _context3.stop();
                  }
                }, _callee3, null, [[4, 9]]);
              })), ExtendableObject.config.ux.delay.onBlur);
              _context4.next = 10;
              break;
            case 7:
              _context4.prev = 7;
              _context4.t0 = _context4["catch"](0);
              console.warn('Error in buildingNumberBlur handler:', _context4.t0);
            case 10:
            case "end":
              return _context4.stop();
          }
        }, _callee4, null, [[0, 7]]);
      }));
    };
  },
  /**
   * Place for registering utility functions used across the extension.
   * Add here:
   * - Data processing helpers
   * - State management utilities
   * - Formatting functions
   * Note: Currently empty as building number handling is straightforward.
   * @param {Object} ExtendableObject - The base object being extended
   */
  registerUtilities: function registerUtilities() {
    // Doesn't provide any new utility functions.
  },
  /**
   * Place for registering API communication functions.
   * Add here:
   * - API request functions
   * - Response handling
   * - Error management
   * Note: Currently empty as building number doesn't require API communication.
   * @param {Object} ExtendableObject - The base object being extended
   */
  registerAPIHandlers: function registerAPIHandlers() {
    // Doesn't provide any new API functions.
  },
  /**
   * Place for registering data transformation and validation functions.
   * Add here:
   * - Input filters
   * - Data validators
   * - Value transformers
   * @param {Object} ExtendableObject - The base object being extended
   */
  registerFilterCallbacks: function registerFilterCallbacks(ExtendableObject) {
    ExtendableObject.cb.setBuildingNumber = function (buildingNumber) {
      return buildingNumber;
    };
  },
  /**
   * Place for registering templates and UI configuration.
   * Add here:
   * - HTML templates
   * - UI configuration
   * - Display format templates
   * Note: Currently empty as building number doesn't require special templates.
   * @param {Object} ExtendableObject - The base object being extended
   */
  registerConfig: function registerConfig() {
    // Doesn't have any configs.
  },
  /**
   * Main extension function that sets up all buildingNumber functionality.
   * @param {Object} ExtendableObject - The base object being extended
   * @returns {Promise<Object>} - Resolves to the BuildingNumberExtension object
   *
   * Executes all registration functions in sequence:
   * 1. registerProperties - Sets up internal state
   * 2. registerFields - Adds buildingNumber field
   * 3. registerConfig - Sets up templates (empty)
   * 4. registerEventCallbacks - Adds event handlers
   * 5. registerUtilities - Adds helper functions (empty)
   * 6. registerAPIHandlers - Sets up API communication (empty)
   * 7. registerFilterCallbacks - Adds data filters
   *
   * All registrations are handled asynchronously to ensure proper setup.
   * The extension object is returned to confirm successful extension.
   */
  extend: function () {
    var _extend = BuildingNumberExtension_asyncToGenerator(/*#__PURE__*/BuildingNumberExtension_regeneratorRuntime().mark(function _callee5(ExtendableObject) {
      return BuildingNumberExtension_regeneratorRuntime().wrap(function _callee5$(_context5) {
        while (1) switch (_context5.prev = _context5.next) {
          case 0:
            _context5.next = 2;
            return BuildingNumberExtension.registerProperties(ExtendableObject);
          case 2:
            _context5.next = 4;
            return BuildingNumberExtension.registerFields(ExtendableObject);
          case 4:
            _context5.next = 6;
            return BuildingNumberExtension.registerConfig(ExtendableObject);
          case 6:
            _context5.next = 8;
            return BuildingNumberExtension.registerEventCallbacks(ExtendableObject);
          case 8:
            _context5.next = 10;
            return BuildingNumberExtension.registerUtilities(ExtendableObject);
          case 10:
            _context5.next = 12;
            return BuildingNumberExtension.registerAPIHandlers(ExtendableObject);
          case 12:
            _context5.next = 14;
            return BuildingNumberExtension.registerFilterCallbacks(ExtendableObject);
          case 14:
            return _context5.abrupt("return", BuildingNumberExtension);
          case 15:
          case "end":
            return _context5.stop();
        }
      }, _callee5);
    }));
    function extend(_x3) {
      return _extend.apply(this, arguments);
    }
    return extend;
  }()
};
/* harmony default export */ var fields_BuildingNumberExtension = (BuildingNumberExtension);
;// ./modules/extensions/checks/BuildingNumberCheckExtension.js
var BuildingNumberCheckExtension = {
  name: 'BuildingNumberCheckExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      ExtendableObject.waitForExtension('BuildingNumberExtension').then(function () {
        // Add email field.
        ExtendableObject._buildingNumberStatus = '';
        ExtendableObject._subscribers.buildingNumberStatus = [];

        // Add change event hadler.
        ExtendableObject.cb.buildingNumberStatusChange = function (subscriber) {
          return function (e) {
            ExtendableObject.buildingNumberStatus = subscriber.value;
          };
        };

        // Add the "emaiL" property
        Object.defineProperty(ExtendableObject, 'buildingNumberStatus', {
          get: function get() {
            return this._buildingNumberStatus;
          },
          set: function set(value) {
            var oldValue = ExtendableObject._buildingNumberStatus;
            ExtendableObject._awaits++;
            ExtendableObject.util.Promise.resolve(value).then(function (value) {
              var newValue = value;
              if (oldValue !== newValue) {
                ExtendableObject._buildingNumberStatus = newValue;

                // Inform all subscribers about the change.
                ExtendableObject._subscribers.buildingNumberStatus.forEach(function (subscriber) {
                  subscriber.value = value;
                });
                ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                  detail: {
                    fieldName: 'buildingNumberStatus',
                    oldValue: oldValue,
                    newValue: newValue,
                    object: ExtendableObject
                  }
                }));
              }
            }).catch().finally(function () {
              ExtendableObject._awaits--;
            });
          }
        });
        if (ExtendableObject.config.showDebugInfo) {
          console.log('BuildingNumberCheckExtension applied');
        }
        resolve($self);
      }).catch(function () {
        console.log('Failed to load because of timeout');
        reject($self);
      });
    });
  }
};
/* harmony default export */ var checks_BuildingNumberCheckExtension = (BuildingNumberCheckExtension);
;// ./templates/street_full_predictions.html
// Module
var street_full_predictions_code = "<div endereco-street-full-predictions data-id=\"{{ExtendableObject.id}}\" class=\"endereco-predictions-wrapper endereco-predictions-wrapper--direction-{{direction}} {{#longList}}endereco-predictions-wrapper--longlist{{/longList}}\" style=\"top:{};\"> <div class=\"endereco-predictions-wrapper-inner\"> <ul class=\"endereco-predictions\"> {{#predictions}} <li class=\"endereco-predictions__item {{#isActive}}active{{/isActive}}\" endereco-street-full-prediction data-prediction-index=\"{{index}}\" data-prediction-type=\"streetFull\">{{{streetFullDiff}}}</li> {{/predictions}} </ul> </div> </div> ";
// Exports
/* harmony default export */ var street_full_predictions = (street_full_predictions_code);
;// ./modules/extensions/fields/StreetFullExtension.js
function StreetFullExtension_typeof(o) { "@babel/helpers - typeof"; return StreetFullExtension_typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, StreetFullExtension_typeof(o); }
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = StreetFullExtension_unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t.return || t.return(); } finally { if (u) throw o; } } }; }
function StreetFullExtension_unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return StreetFullExtension_arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? StreetFullExtension_arrayLikeToArray(r, a) : void 0; } }
function StreetFullExtension_arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function StreetFullExtension_regeneratorRuntime() { "use strict"; /*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/facebook/regenerator/blob/main/LICENSE */ StreetFullExtension_regeneratorRuntime = function _regeneratorRuntime() { return e; }; var t, e = {}, r = Object.prototype, n = r.hasOwnProperty, o = Object.defineProperty || function (t, e, r) { t[e] = r.value; }, i = "function" == typeof Symbol ? Symbol : {}, a = i.iterator || "@@iterator", c = i.asyncIterator || "@@asyncIterator", u = i.toStringTag || "@@toStringTag"; function define(t, e, r) { return Object.defineProperty(t, e, { value: r, enumerable: !0, configurable: !0, writable: !0 }), t[e]; } try { define({}, ""); } catch (t) { define = function define(t, e, r) { return t[e] = r; }; } function wrap(t, e, r, n) { var i = e && e.prototype instanceof Generator ? e : Generator, a = Object.create(i.prototype), c = new Context(n || []); return o(a, "_invoke", { value: makeInvokeMethod(t, r, c) }), a; } function tryCatch(t, e, r) { try { return { type: "normal", arg: t.call(e, r) }; } catch (t) { return { type: "throw", arg: t }; } } e.wrap = wrap; var h = "suspendedStart", l = "suspendedYield", f = "executing", s = "completed", y = {}; function Generator() {} function GeneratorFunction() {} function GeneratorFunctionPrototype() {} var p = {}; define(p, a, function () { return this; }); var d = Object.getPrototypeOf, v = d && d(d(values([]))); v && v !== r && n.call(v, a) && (p = v); var g = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(p); function defineIteratorMethods(t) { ["next", "throw", "return"].forEach(function (e) { define(t, e, function (t) { return this._invoke(e, t); }); }); } function AsyncIterator(t, e) { function invoke(r, o, i, a) { var c = tryCatch(t[r], t, o); if ("throw" !== c.type) { var u = c.arg, h = u.value; return h && "object" == StreetFullExtension_typeof(h) && n.call(h, "__await") ? e.resolve(h.__await).then(function (t) { invoke("next", t, i, a); }, function (t) { invoke("throw", t, i, a); }) : e.resolve(h).then(function (t) { u.value = t, i(u); }, function (t) { return invoke("throw", t, i, a); }); } a(c.arg); } var r; o(this, "_invoke", { value: function value(t, n) { function callInvokeWithMethodAndArg() { return new e(function (e, r) { invoke(t, n, e, r); }); } return r = r ? r.then(callInvokeWithMethodAndArg, callInvokeWithMethodAndArg) : callInvokeWithMethodAndArg(); } }); } function makeInvokeMethod(e, r, n) { var o = h; return function (i, a) { if (o === f) throw Error("Generator is already running"); if (o === s) { if ("throw" === i) throw a; return { value: t, done: !0 }; } for (n.method = i, n.arg = a;;) { var c = n.delegate; if (c) { var u = maybeInvokeDelegate(c, n); if (u) { if (u === y) continue; return u; } } if ("next" === n.method) n.sent = n._sent = n.arg;else if ("throw" === n.method) { if (o === h) throw o = s, n.arg; n.dispatchException(n.arg); } else "return" === n.method && n.abrupt("return", n.arg); o = f; var p = tryCatch(e, r, n); if ("normal" === p.type) { if (o = n.done ? s : l, p.arg === y) continue; return { value: p.arg, done: n.done }; } "throw" === p.type && (o = s, n.method = "throw", n.arg = p.arg); } }; } function maybeInvokeDelegate(e, r) { var n = r.method, o = e.iterator[n]; if (o === t) return r.delegate = null, "throw" === n && e.iterator.return && (r.method = "return", r.arg = t, maybeInvokeDelegate(e, r), "throw" === r.method) || "return" !== n && (r.method = "throw", r.arg = new TypeError("The iterator does not provide a '" + n + "' method")), y; var i = tryCatch(o, e.iterator, r.arg); if ("throw" === i.type) return r.method = "throw", r.arg = i.arg, r.delegate = null, y; var a = i.arg; return a ? a.done ? (r[e.resultName] = a.value, r.next = e.nextLoc, "return" !== r.method && (r.method = "next", r.arg = t), r.delegate = null, y) : a : (r.method = "throw", r.arg = new TypeError("iterator result is not an object"), r.delegate = null, y); } function pushTryEntry(t) { var e = { tryLoc: t[0] }; 1 in t && (e.catchLoc = t[1]), 2 in t && (e.finallyLoc = t[2], e.afterLoc = t[3]), this.tryEntries.push(e); } function resetTryEntry(t) { var e = t.completion || {}; e.type = "normal", delete e.arg, t.completion = e; } function Context(t) { this.tryEntries = [{ tryLoc: "root" }], t.forEach(pushTryEntry, this), this.reset(!0); } function values(e) { if (e || "" === e) { var r = e[a]; if (r) return r.call(e); if ("function" == typeof e.next) return e; if (!isNaN(e.length)) { var o = -1, i = function next() { for (; ++o < e.length;) if (n.call(e, o)) return next.value = e[o], next.done = !1, next; return next.value = t, next.done = !0, next; }; return i.next = i; } } throw new TypeError(StreetFullExtension_typeof(e) + " is not iterable"); } return GeneratorFunction.prototype = GeneratorFunctionPrototype, o(g, "constructor", { value: GeneratorFunctionPrototype, configurable: !0 }), o(GeneratorFunctionPrototype, "constructor", { value: GeneratorFunction, configurable: !0 }), GeneratorFunction.displayName = define(GeneratorFunctionPrototype, u, "GeneratorFunction"), e.isGeneratorFunction = function (t) { var e = "function" == typeof t && t.constructor; return !!e && (e === GeneratorFunction || "GeneratorFunction" === (e.displayName || e.name)); }, e.mark = function (t) { return Object.setPrototypeOf ? Object.setPrototypeOf(t, GeneratorFunctionPrototype) : (t.__proto__ = GeneratorFunctionPrototype, define(t, u, "GeneratorFunction")), t.prototype = Object.create(g), t; }, e.awrap = function (t) { return { __await: t }; }, defineIteratorMethods(AsyncIterator.prototype), define(AsyncIterator.prototype, c, function () { return this; }), e.AsyncIterator = AsyncIterator, e.async = function (t, r, n, o, i) { void 0 === i && (i = Promise); var a = new AsyncIterator(wrap(t, r, n, o), i); return e.isGeneratorFunction(r) ? a : a.next().then(function (t) { return t.done ? t.value : a.next(); }); }, defineIteratorMethods(g), define(g, u, "Generator"), define(g, a, function () { return this; }), define(g, "toString", function () { return "[object Generator]"; }), e.keys = function (t) { var e = Object(t), r = []; for (var n in e) r.push(n); return r.reverse(), function next() { for (; r.length;) { var t = r.pop(); if (t in e) return next.value = t, next.done = !1, next; } return next.done = !0, next; }; }, e.values = values, Context.prototype = { constructor: Context, reset: function reset(e) { if (this.prev = 0, this.next = 0, this.sent = this._sent = t, this.done = !1, this.delegate = null, this.method = "next", this.arg = t, this.tryEntries.forEach(resetTryEntry), !e) for (var r in this) "t" === r.charAt(0) && n.call(this, r) && !isNaN(+r.slice(1)) && (this[r] = t); }, stop: function stop() { this.done = !0; var t = this.tryEntries[0].completion; if ("throw" === t.type) throw t.arg; return this.rval; }, dispatchException: function dispatchException(e) { if (this.done) throw e; var r = this; function handle(n, o) { return a.type = "throw", a.arg = e, r.next = n, o && (r.method = "next", r.arg = t), !!o; } for (var o = this.tryEntries.length - 1; o >= 0; --o) { var i = this.tryEntries[o], a = i.completion; if ("root" === i.tryLoc) return handle("end"); if (i.tryLoc <= this.prev) { var c = n.call(i, "catchLoc"), u = n.call(i, "finallyLoc"); if (c && u) { if (this.prev < i.catchLoc) return handle(i.catchLoc, !0); if (this.prev < i.finallyLoc) return handle(i.finallyLoc); } else if (c) { if (this.prev < i.catchLoc) return handle(i.catchLoc, !0); } else { if (!u) throw Error("try statement without catch or finally"); if (this.prev < i.finallyLoc) return handle(i.finallyLoc); } } } }, abrupt: function abrupt(t, e) { for (var r = this.tryEntries.length - 1; r >= 0; --r) { var o = this.tryEntries[r]; if (o.tryLoc <= this.prev && n.call(o, "finallyLoc") && this.prev < o.finallyLoc) { var i = o; break; } } i && ("break" === t || "continue" === t) && i.tryLoc <= e && e <= i.finallyLoc && (i = null); var a = i ? i.completion : {}; return a.type = t, a.arg = e, i ? (this.method = "next", this.next = i.finallyLoc, y) : this.complete(a); }, complete: function complete(t, e) { if ("throw" === t.type) throw t.arg; return "break" === t.type || "continue" === t.type ? this.next = t.arg : "return" === t.type ? (this.rval = this.arg = t.arg, this.method = "return", this.next = "end") : "normal" === t.type && e && (this.next = e), y; }, finish: function finish(t) { for (var e = this.tryEntries.length - 1; e >= 0; --e) { var r = this.tryEntries[e]; if (r.finallyLoc === t) return this.complete(r.completion, r.afterLoc), resetTryEntry(r), y; } }, catch: function _catch(t) { for (var e = this.tryEntries.length - 1; e >= 0; --e) { var r = this.tryEntries[e]; if (r.tryLoc === t) { var n = r.completion; if ("throw" === n.type) { var o = n.arg; resetTryEntry(r); } return o; } } throw Error("illegal catch attempt"); }, delegateYield: function delegateYield(e, r, n) { return this.delegate = { iterator: values(e), resultName: r, nextLoc: n }, "next" === this.method && (this.arg = t), y; } }, e; }
function StreetFullExtension_asyncGeneratorStep(n, t, e, r, o, a, c) { try { var i = n[a](c), u = i.value; } catch (n) { return void e(n); } i.done ? t(u) : Promise.resolve(u).then(r, o); }
function StreetFullExtension_asyncToGenerator(n) { return function () { var t = this, e = arguments; return new Promise(function (r, o) { var a = n.apply(t, e); function _next(n) { StreetFullExtension_asyncGeneratorStep(a, r, o, _next, _throw, "next", n); } function _throw(n) { StreetFullExtension_asyncGeneratorStep(a, r, o, _next, _throw, "throw", n); } _next(void 0); }); }; }
/**
 * Extends the base object with "streetFull" field handling capabilities.
 * Adds support for single-line street input ("streetFull") by:
 * - Adding streetFull property with getter/setter
 * - Parsing it into separate street name and building number components
 * - Providing autocomplete suggestions for the full street input
 * - Syncing parsed components with separate streetName/buildingNumber fields if present
 * - Caching API parsing results for performance
 * - Handling keyboard navigation for autocomplete suggestions
 */




var StreetFullExtension_MAX_PREDICTIONS_BEFORE_SCROLL = 6;
var StreetFullExtension_ERROR_EXPIRED_SESSION = -32700;
var StreetFullExtension = {
  name: 'StreetFullExtension',
  /**
   * Place for registering internal properties needed for streetFull functionality.
   * Add here:
   * - Internal storage fields (_fieldName)
   * - Subscriber arrays (_subscribers.fieldName)
   * - Cache objects
   * - Control flags
   * - Timeout and sequence tracking variables
   * @param {Object} ExtendableObject - The base object being extended
   */
  registerProperties: function registerProperties(ExtendableObject) {
    // Internal storage for field values
    ExtendableObject._streetFull = '';
    ExtendableObject._streetFullPredictions = [];

    // Subscriber storage
    ExtendableObject._subscribers.streetFull = [];

    // Cache
    ExtendableObject.streetSplitCache = {
      cachedResults: {}
    };
    ExtendableObject.streetFullAutocompleteCache = {
      cachedResults: {}
    };

    // Flags
    ExtendableObject._allowToNotifyStreetFullSubscribers = true;
    ExtendableObject._allowStreetFullSplit = true;
    ExtendableObject._allowStreetFullCompose = true;
    ExtendableObject._allowFetchStreetFullAutocomplete = true;

    // Timeout and sequence
    ExtendableObject._streetFullAutocompleteTimeout = null;
    ExtendableObject._streetFullAutocompleteRequestIndex = 0;
    ExtendableObject._streetFullSplitTimeout = null;
    ExtendableObject._streetFullSplitRequestIndex = 0;
    ExtendableObject._streetFullPredictionsIndex = 0;
  },
  /**
   * Place for registering fields that should be exposed on the ExtendableObject.
   * Add here:
   * - Public properties with getters/setters
   * - Field tracking registration
   * - Property change handling logic
   * @param {Object} ExtendableObject - The base object being extended
   */
  registerFields: function registerFields(ExtendableObject) {
    // Add getter and setter for fields.
    Object.defineProperty(ExtendableObject, 'streetFull', {
      get: function get() {
        return ExtendableObject.getStreetFull();
      },
      set: function () {
        var _set = StreetFullExtension_asyncToGenerator(/*#__PURE__*/StreetFullExtension_regeneratorRuntime().mark(function _callee(value) {
          return StreetFullExtension_regeneratorRuntime().wrap(function _callee$(_context) {
            while (1) switch (_context.prev = _context.next) {
              case 0:
                _context.next = 2;
                return ExtendableObject.setStreetFull(value);
              case 2:
              case "end":
                return _context.stop();
            }
          }, _callee);
        }));
        function set(_x) {
          return _set.apply(this, arguments);
        }
        return set;
      }()
    });
    ExtendableObject.getStreetFull = function () {
      return ExtendableObject._streetFull;
    };
    ExtendableObject.setStreetFull = /*#__PURE__*/function () {
      var _ref = StreetFullExtension_asyncToGenerator(/*#__PURE__*/StreetFullExtension_regeneratorRuntime().mark(function _callee2(streetFull) {
        var needToNotify, needToDisplayAutocompleteDropdown, needToSplit, resolvedValue;
        return StreetFullExtension_regeneratorRuntime().wrap(function _callee2$(_context2) {
          while (1) switch (_context2.prev = _context2.next) {
            case 0:
              needToNotify = ExtendableObject.active && ExtendableObject._allowToNotifyStreetFullSubscribers;
              needToDisplayAutocompleteDropdown = ExtendableObject.active && ExtendableObject.config.useAutocomplete && ExtendableObject._allowFetchStreetFullAutocomplete;
              needToSplit = ExtendableObject.active && ExtendableObject._allowStreetFullSplit;
              ExtendableObject._awaits++;
              _context2.prev = 4;
              _context2.next = 7;
              return ExtendableObject.util.Promise.resolve(streetFull);
            case 7:
              resolvedValue = _context2.sent;
              _context2.next = 10;
              return ExtendableObject.cb.setStreetFull(resolvedValue);
            case 10:
              resolvedValue = _context2.sent;
              if (!(resolvedValue === ExtendableObject._streetFull)) {
                _context2.next = 13;
                break;
              }
              return _context2.abrupt("return");
            case 13:
              ExtendableObject._streetFull = resolvedValue;
              if (ExtendableObject.active) {
                ExtendableObject._changed = true;
                ExtendableObject.addressStatus = [];
              }
              if (needToNotify) {
                // Inform all subscribers about the change.
                ExtendableObject._subscribers.streetFull.forEach(function (subscriber) {
                  subscriber.value = resolvedValue;
                });
              }
              if (!needToSplit) {
                _context2.next = 19;
                break;
              }
              _context2.next = 19;
              return ExtendableObject.util.ensureStreetPartsIntegrity(resolvedValue);
            case 19:
              if (!needToDisplayAutocompleteDropdown) {
                _context2.next = 22;
                break;
              }
              _context2.next = 22;
              return ExtendableObject.util.displayStreetFullAutocompleteDropdown(resolvedValue);
            case 22:
              _context2.next = 27;
              break;
            case 24:
              _context2.prev = 24;
              _context2.t0 = _context2["catch"](4);
              console.warn("Error while setting the field 'streetFull'", _context2.t0);
            case 27:
              _context2.prev = 27;
              ExtendableObject._awaits--;
              return _context2.finish(27);
            case 30:
            case "end":
              return _context2.stop();
          }
        }, _callee2, null, [[4, 24, 27, 30]]);
      }));
      return function (_x2) {
        return _ref.apply(this, arguments);
      };
    }();
    ExtendableObject.fieldNames.push('streetFull'); // TODO: check if still needed

    Object.defineProperty(ExtendableObject, 'streetFullPredictions', {
      get: function get() {
        return this._streetFullPredictions;
      },
      set: function () {
        var _set2 = StreetFullExtension_asyncToGenerator(/*#__PURE__*/StreetFullExtension_regeneratorRuntime().mark(function _callee3(value) {
          return StreetFullExtension_regeneratorRuntime().wrap(function _callee3$(_context3) {
            while (1) switch (_context3.prev = _context3.next) {
              case 0:
                _context3.next = 2;
                return ExtendableObject.setStreetFullPredictions(value);
              case 2:
              case "end":
                return _context3.stop();
            }
          }, _callee3);
        }));
        function set(_x3) {
          return _set2.apply(this, arguments);
        }
        return set;
      }()
    });
    ExtendableObject.getStreetFullPredictions = function () {
      return ExtendableObject._streetFullPredictions;
    };
    ExtendableObject.setStreetFullPredictions = /*#__PURE__*/function () {
      var _ref2 = StreetFullExtension_asyncToGenerator(/*#__PURE__*/StreetFullExtension_regeneratorRuntime().mark(function _callee4(streetFullPredictions) {
        var resolvedValue;
        return StreetFullExtension_regeneratorRuntime().wrap(function _callee4$(_context4) {
          while (1) switch (_context4.prev = _context4.next) {
            case 0:
              _context4.next = 2;
              return ExtendableObject.util.Promise.resolve(streetFullPredictions);
            case 2:
              resolvedValue = _context4.sent;
              if (ExtendableObject.streetFullPredictions !== resolvedValue) {
                ExtendableObject._streetFullPredictions = resolvedValue;
                ExtendableObject._streetFullPredictionsIndex = 0;
              }
            case 4:
            case "end":
              return _context4.stop();
          }
        }, _callee4);
      }));
      return function (_x4) {
        return _ref2.apply(this, arguments);
      };
    }();
  },
  /**
   * Place for registering event-related callback functions.
   * Add here:
   * - DOM event handlers (onChange, onInput, onBlur, etc.)
   * - Keyboard interaction handlers
   * - Event propagation control
   * - Event-triggered state updates
   * @param {Object} ExtendableObject - The base object being extended
   */
  registerEventCallbacks: function registerEventCallbacks(ExtendableObject) {
    /**
     * Handler for field value changes via normal input events (without the field being in focus)
     * @param {Object} subscriber - The subscriber object that triggered the change
     * @returns {Function} Event handler that updates streetFull while preventing circular updates
     */
    ExtendableObject.cb.streetFullChange = function (subscriber) {
      return function () {
        ExtendableObject._allowToNotifyStreetFullSubscribers = false;
        ExtendableObject.streetFull = subscriber.value;
        ExtendableObject._allowToNotifyStreetFullSubscribers = true;
      };
    };

    /**
     * Handler for field value changes via direct input events (while being in focus)
     * @param {Object} subscriber - The subscriber object that triggered the input
     * @returns {Function} Event handler that updates streetFull while preventing circular updates
     */
    ExtendableObject.cb.streetFullInput = function (subscriber) {
      return function () {
        ExtendableObject._allowToNotifyStreetFullSubscribers = false;
        ExtendableObject.streetFull = subscriber.value;
        ExtendableObject._allowToNotifyStreetFullSubscribers = true;
      };
    };

    /**
     * Handler for field blur events (field focus and de-focus)
     * @returns {Function} Event handler that manages validation and dropdown cleanup
     */
    ExtendableObject.cb.streetFullBlur = function () {
      return /*#__PURE__*/StreetFullExtension_asyncToGenerator(/*#__PURE__*/StreetFullExtension_regeneratorRuntime().mark(function _callee6() {
        var isAnyActive;
        return StreetFullExtension_regeneratorRuntime().wrap(function _callee6$(_context6) {
          while (1) switch (_context6.prev = _context6.next) {
            case 0:
              // Handle dropdown cleanup
              isAnyActive = ExtendableObject._subscribers.streetFull.some(function (sub) {
                return document.activeElement === sub.object;
              });
              if (!isAnyActive) {
                ExtendableObject.streetNamePredictions = [];
                ExtendableObject.util.removeStreetFullPredictionsDropdown();
              }
              _context6.next = 4;
              return ExtendableObject.waitUntilReady();
            case 4:
              // Clear existing timeout if any
              if (ExtendableObject.onBlurTimeout) {
                clearTimeout(ExtendableObject.onBlurTimeout);
                ExtendableObject.onBlurTimeout = null;
              }

              // Set new timeout for address check
              ExtendableObject.onBlurTimeout = setTimeout(/*#__PURE__*/StreetFullExtension_asyncToGenerator(/*#__PURE__*/StreetFullExtension_regeneratorRuntime().mark(function _callee5() {
                var shouldCheckAddress;
                return StreetFullExtension_regeneratorRuntime().wrap(function _callee5$(_context5) {
                  while (1) switch (_context5.prev = _context5.next) {
                    case 0:
                      shouldCheckAddress = ExtendableObject.config.trigger.onblur && !ExtendableObject.anyActive() && ExtendableObject.util.shouldBeChecked() && !window.EnderecoIntegrator.hasSubmit;
                      if (!shouldCheckAddress) {
                        _context5.next = 12;
                        break;
                      }
                      clearTimeout(ExtendableObject.onBlurTimeout);
                      ExtendableObject.onBlurTimeout = null;
                      _context5.prev = 4;
                      _context5.next = 7;
                      return ExtendableObject.util.checkAddress();
                    case 7:
                      _context5.next = 12;
                      break;
                    case 9:
                      _context5.prev = 9;
                      _context5.t0 = _context5["catch"](4);
                      console.warn('Error checking address:', _context5.t0);
                    case 12:
                    case "end":
                      return _context5.stop();
                  }
                }, _callee5, null, [[4, 9]]);
              })), ExtendableObject.config.ux.delay.onBlur);
            case 6:
            case "end":
              return _context6.stop();
          }
        }, _callee6);
      }));
    };

    /**
     * Handler for keyboard events on the streetFull field
     * @returns {Function} Event handler that manages keyboard navigation and selection
     */
    ExtendableObject.cb.streetFullKeydown = function () {
      return function (e) {
        if (e.key === 'ArrowUp' || e.key === 'Up') {
          e.preventDefault();
          e.stopPropagation();
          if (ExtendableObject._streetFullPredictionsIndex > -1) {
            ExtendableObject._streetFullPredictionsIndex = ExtendableObject._streetFullPredictionsIndex - 1;
            ExtendableObject.util.renderStreetFullPredictionsDropdown(ExtendableObject.streetFullPredictions);
          }
        } else if (e.key === 'ArrowDown' || e.key === 'Down') {
          e.preventDefault();
          e.stopPropagation();
          if (ExtendableObject._streetFullPredictionsIndex < ExtendableObject._streetFullPredictions.length - 1) {
            ExtendableObject._streetFullPredictionsIndex = ExtendableObject._streetFullPredictionsIndex + 1;
            ExtendableObject.util.renderStreetFullPredictionsDropdown(ExtendableObject.streetFullPredictions);
          }
        } else if (e.key === 'Tab' || e.key === 'Tab') {
          // TODO: configurable activate in future releases.
          /*
          if (0 < ExtendableObject._streetFullPredictions.length) {
              e.preventDefault();
              e.stopPropagation();
              ExtendableObject.cb.copyStreetFullFromPrediction();
          } */
        } else if (e.key === 'Enter' || e.key === 'Enter') {
          if (ExtendableObject._streetFullPredictions.length > 0) {
            e.preventDefault();
            e.stopPropagation();
            ExtendableObject._allowFetchStreetFullAutocomplete = false;
            ExtendableObject.streetFull = ExtendableObject.streetFullPredictions[ExtendableObject._streetFullPredictionsIndex].streetFull;
            ExtendableObject.streetFullPredictions = [];
            ExtendableObject._allowFetchStreetFullAutocomplete = true;
            ExtendableObject.util.removeStreetFullPredictionsDropdown();
          }
        } else if (e.key === 'Backspace' || e.key === 'Backspace') {
          ExtendableObject.config.ux.smartFill = false;
        }
      };
    };
  },
  /**
   * Place for registering utility functions used across the extended object.
   * Add here:
   * - UI manipulation functions
   * - Data processing helpers
   * - State management utilities
   * - Formatting functions
   * @param {Object} ExtendableObject - The base object being extended
   */
  registerUtilities: function registerUtilities(ExtendableObject) {
    /**
     * Manages the display of autocomplete suggestions dropdown
     * @param {string} streetFull - Current street input value
     * @returns {Promise<void>}
     */
    ExtendableObject.util.displayStreetFullAutocompleteDropdown = function (streetFull) {
      ExtendableObject.util.removeStreetFullPredictionsDropdown();
      if (ExtendableObject._streetFullAutocompleteTimeout) {
        clearTimeout(ExtendableObject._streetFullAutocompleteTimeout);
      }
      ExtendableObject._streetFullAutocompleteTimeout = setTimeout(/*#__PURE__*/StreetFullExtension_asyncToGenerator(/*#__PURE__*/StreetFullExtension_regeneratorRuntime().mark(function _callee7() {
        var autocompleteResult;
        return StreetFullExtension_regeneratorRuntime().wrap(function _callee7$(_context7) {
          while (1) switch (_context7.prev = _context7.next) {
            case 0:
              _context7.prev = 0;
              _context7.next = 3;
              return ExtendableObject.util.getStreetFullPredictions(streetFull);
            case 3:
              autocompleteResult = _context7.sent;
              if (!(autocompleteResult.originalStreetFull !== streetFull)) {
                _context7.next = 6;
                break;
              }
              return _context7.abrupt("return");
            case 6:
              ExtendableObject.streetFullPredictions = autocompleteResult.predictions;
              ExtendableObject.util.renderStreetFullPredictionsDropdown(autocompleteResult.predictions);
              _context7.next = 13;
              break;
            case 10:
              _context7.prev = 10;
              _context7.t0 = _context7["catch"](0);
              console.warn('Failed fetching predictions', _context7.t0, streetFull);
            case 13:
              ExtendableObject._streetFullAutocompleteTimeout = null;
            case 14:
            case "end":
              return _context7.stop();
          }
        }, _callee7, null, [[0, 10]]);
      })), ExtendableObject.config.ux.delay.inputAssistant);
    };

    /**
     * Removes the predictions dropdown from the DOM
     * Updates the dropdown counter and cleans up related state
     */
    ExtendableObject.util.removeStreetFullPredictionsDropdown = function () {
      ExtendableObject._subscribers.streetFull.forEach(function () {
        var dropdown = document.querySelector('[endereco-street-full-predictions]');
        if (dropdown) {
          ExtendableObject._openDropdowns--;
          dropdown.parentNode.removeChild(dropdown);
        }
      });
    };

    /**
     * Renders the predictions dropdown with diff highlighting
     * @param {Array} predictions - Array of address predictions
     * Handles:
     * - Dropdown positioning
     * - Diff highlighting
     * - Event listeners for selection
     */
    ExtendableObject.util.renderStreetFullPredictionsDropdown = function (predictions) {
      predictions = JSON.parse(JSON.stringify(predictions)); // Create a copy

      // Render dropdown under the input element
      ExtendableObject._subscribers.streetFull.forEach(function (subscriber) {
        if (document.querySelector('[endereco-street-full-predictions]')) {
          ExtendableObject._openDropdowns--;
          document.querySelector('[endereco-street-full-predictions]').parentNode.removeChild(document.querySelector('[endereco-street-full-predictions]'));
        }

        // Render predictions only if the field is active and there are predictions.
        if (predictions.length > 0 && document.activeElement === subscriber.object) {
          var startingIndex = 0;
          var preparedPredictions = [];

          // Prepare predictions.
          predictions.forEach(function (prediction) {
            var diff = diffChars(ExtendableObject.streetFull, prediction.streetFull, {
              ignoreCase: true
            });
            var streetFullHtml = '';
            diff.forEach(function (part) {
              var markClass = part.added ? 'endereco-span--add' : part.removed ? 'endereco-span--remove' : 'endereco-span--neutral';
              streetFullHtml += "<span class=\"".concat(markClass, "\">").concat(part.value.replace(/[ ]/g, '&nbsp;'), "</span>");
            });
            preparedPredictions.push({
              streetFull: prediction.streetFull,
              streetFullDiff: streetFullHtml
            });
          });

          // Prepare dropdown.
          var predictionsHtml = ExtendableObject.util.Mustache.render(ExtendableObject.config.templates.streetFullPredictions, {
            ExtendableObject: ExtendableObject,
            predictions: preparedPredictions,
            offsetTop: subscriber.object.offsetTop + subscriber.object.offsetHeight,
            offsetLeft: subscriber.object.offsetLeft,
            width: subscriber.object.offsetWidth,
            direction: getComputedStyle(subscriber.object).direction,
            longList: preparedPredictions.length > StreetFullExtension_MAX_PREDICTIONS_BEFORE_SCROLL,
            index: function index() {
              return startingIndex - 1; // TODO: add loop finishing function to increase counter.
            },
            isActive: function isActive() {
              var isActive = startingIndex === ExtendableObject._streetFullPredictionsIndex;
              startingIndex++;
              return isActive;
            }
          });

          // Attach it to HTML.
          subscriber.object.insertAdjacentHTML('afterend', predictionsHtml);
          ExtendableObject._openDropdowns++;
          document.querySelectorAll("[data-id=\"".concat(ExtendableObject.id, "\"] [endereco-street-full-prediction]")).forEach(function (DOMElement) {
            DOMElement.addEventListener('mousedown', function (e) {
              var index = parseInt(this.getAttribute('data-prediction-index'));
              e.preventDefault();
              e.stopPropagation();
              ExtendableObject._allowFetchStreetFullAutocomplete = false;
              ExtendableObject.streetFull = predictions[index].streetFull;
              ExtendableObject.streetFullPredictions = [];
              ExtendableObject._allowFetchStreetFullAutocomplete = true;
              ExtendableObject.util.removeStreetFullPredictionsDropdown();
            });
          });
        }
      });
    };

    /**
     * Ensures street name and number are properly split and synchronized
     * @param {string} streetFull - Combined street input to split
     * @returns {Promise<void>}
     */
    ExtendableObject.util.ensureStreetPartsIntegrity = function (streetFull) {
      if (ExtendableObject._streetFullSplitTimeout) {
        clearTimeout(ExtendableObject._streetFullSplitTimeout);
      }
      ExtendableObject._streetFullSplitTimeout = setTimeout(/*#__PURE__*/StreetFullExtension_asyncToGenerator(/*#__PURE__*/StreetFullExtension_regeneratorRuntime().mark(function _callee8() {
        var _yield$ExtendableObje, streetName, buildingNumber;
        return StreetFullExtension_regeneratorRuntime().wrap(function _callee8$(_context8) {
          while (1) switch (_context8.prev = _context8.next) {
            case 0:
              _context8.prev = 0;
              _context8.next = 3;
              return ExtendableObject.util.splitStreet(streetFull);
            case 3:
              _yield$ExtendableObje = _context8.sent;
              streetName = _yield$ExtendableObje.streetName;
              buildingNumber = _yield$ExtendableObje.buildingNumber;
              if (!(ExtendableObject.streetFull !== streetFull)) {
                _context8.next = 8;
                break;
              }
              return _context8.abrupt("return");
            case 8:
              ExtendableObject._allowStreetFullCompose = false;
              ExtendableObject._allowFetchStreetNameAutocomplete = false;
              ExtendableObject.streetName = streetName;
              ExtendableObject.buildingNumber = buildingNumber;
              ExtendableObject._allowStreetFullCompose = true;
              ExtendableObject._allowFetchStreetNameAutocomplete = true;
              _context8.next = 19;
              break;
            case 16:
              _context8.prev = 16;
              _context8.t0 = _context8["catch"](0);
              console.warn('Street split failed', _context8.t0, streetFull);
            case 19:
              ExtendableObject._streetFullSplitTimeout = null;
            case 20:
            case "end":
              return _context8.stop();
          }
        }, _callee8, null, [[0, 16]]);
      })), ExtendableObject.config.ux.delay.inputAssistant);
    };

    /**
     * Formats street components according to country-specific template
     * @param {Object} [streetData] - Optional street data object
     * @param {string} [streetData.countryCode] - Country code for formatting
     * @param {string} [streetData.streetName] - Street name
     * @param {string} [streetData.buildingNumber] - Building number
     * @param {string} [streetData.additionalInfo] - Additional address info
     * @returns {string} Formatted street address
     */
    ExtendableObject.util.formatStreetFull = function () {
      var streetData = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : null;
      var $self = ExtendableObject;
      if (streetData === null) {
        streetData = {
          countryCode: $self.countryCode,
          streetName: $self.streetName,
          buildingNumber: $self.buildingNumber,
          additionalInfo: $self.additionalInfo
        };
      }
      return $self.util.Mustache.render($self.config.templates.streetFull.getTemplate(streetData.countryCode), streetData).replace(/  +/g, ' ').replace(/(\r\n|\n|\r)/gm, '').trim();
    };
  },
  /**
   * Place for registering API communication functions.
   * Add here:
   * - API request functions
   * - Response handling
   * - Error management
   * - Caching logic
   * @param {Object} ExtendableObject - The base object being extended
   */
  registerAPIHandlers: function registerAPIHandlers(ExtendableObject) {
    /**
     * Splits a combined street address into components via API
     * @param {string} streetFullRaw - Raw street input to split
     * @returns {Promise<Object>} Split address components
     * @throws {Error} If input is invalid or API request fails
     */
    ExtendableObject.util.splitStreet = /*#__PURE__*/function () {
      var _ref7 = StreetFullExtension_asyncToGenerator(/*#__PURE__*/StreetFullExtension_regeneratorRuntime().mark(function _callee9(streetFullRaw) {
        var streetFull, _ExtendableObject$has, streetFullSplitRequestIndex, splitStreetRequest, _iterator, _step, listener, headers, _result$data, EnderecoAPI, _result, result, splitData, _result$additionalInf;
        return StreetFullExtension_regeneratorRuntime().wrap(function _callee9$(_context9) {
          while (1) switch (_context9.prev = _context9.next) {
            case 0:
              if (!(typeof streetFullRaw !== 'string')) {
                _context9.next = 2;
                break;
              }
              throw new Error('Invalid arguments. The streetFull has to be string');
            case 2:
              if (streetFullRaw.trim()) {
                _context9.next = 4;
                break;
              }
              return _context9.abrupt("return", {
                streetName: '',
                streetFull: '',
                buildingNumber: ''
              });
            case 4:
              streetFull = streetFullRaw.trim(); // Create copy:
              if (ExtendableObject.streetSplitCache.cachedResults[streetFull]) {
                _context9.next = 45;
                break;
              }
              ExtendableObject._awaits++;
              streetFullSplitRequestIndex = ++ExtendableObject._streetFullSplitRequestIndex;
              splitStreetRequest = {
                jsonrpc: '2.0',
                id: streetFullSplitRequestIndex,
                method: 'splitStreet',
                params: {
                  formatCountry: ExtendableObject.countryCode || 'DE',
                  language: ExtendableObject.config.lang,
                  street: streetFull
                }
              };
              if (!(ExtendableObject._subscribers.additionalInfo.length > 0)) {
                _context9.next = 28;
                break;
              }
              _iterator = _createForOfIteratorHelper(ExtendableObject._subscribers.additionalInfo);
              _context9.prev = 11;
              _iterator.s();
            case 13:
              if ((_step = _iterator.n()).done) {
                _context9.next = 20;
                break;
              }
              listener = _step.value;
              if (!(!listener.object.disabled && listener.object.isConnected)) {
                _context9.next = 18;
                break;
              }
              splitStreetRequest.params.additionalInfo = ExtendableObject.address.additionalInfo;
              return _context9.abrupt("break", 20);
            case 18:
              _context9.next = 13;
              break;
            case 20:
              _context9.next = 25;
              break;
            case 22:
              _context9.prev = 22;
              _context9.t0 = _context9["catch"](11);
              _iterator.e(_context9.t0);
            case 25:
              _context9.prev = 25;
              _iterator.f();
              return _context9.finish(25);
            case 28:
              headers = {
                'X-Auth-Key': ExtendableObject.config.apiKey,
                'X-Agent': ExtendableObject.config.agentName,
                'X-Remote-Api-Url': ExtendableObject.config.remoteApiUrl,
                'X-Transaction-Referer': window.location.href,
                'X-Transaction-Id': (_ExtendableObject$has = ExtendableObject.hasLoadedExtension) !== null && _ExtendableObject$has !== void 0 && _ExtendableObject$has.call(ExtendableObject, 'SessionExtension') ? ExtendableObject.sessionId : 'not_required'
              };
              _context9.prev = 29;
              EnderecoAPI = ExtendableObject.getEnderecoAPI();
              _context9.next = 33;
              return EnderecoAPI.sendRequestToAPI(splitStreetRequest, headers);
            case 33:
              _result = _context9.sent;
              if (_result !== null && _result !== void 0 && (_result$data = _result.data) !== null && _result$data !== void 0 && _result$data.result) {
                _context9.next = 36;
                break;
              }
              throw new Error("Invalid API response: Missing 'result' field in response data");
            case 36:
              // Save in cache
              ExtendableObject.streetSplitCache.cachedResults[streetFull] = _result.data.result;
              _context9.next = 42;
              break;
            case 39:
              _context9.prev = 39;
              _context9.t1 = _context9["catch"](29);
              console.warn('Error splitting street full', _context9.t1, streetFull);
            case 42:
              _context9.prev = 42;
              ExtendableObject._awaits--;
              return _context9.finish(42);
            case 45:
              result = ExtendableObject.streetSplitCache.cachedResults[streetFull];
              splitData = {
                originalStreetFull: streetFull,
                // Used to invalidate outdated splits downstream
                streetName: result.streetName || '',
                streetFull: result.street || '',
                buildingNumber: result.houseNumber || ''
              };
              if (result.additionalInfo !== undefined) {
                splitData.additionalInfo = (_result$additionalInf = result.additionalInfo) !== null && _result$additionalInf !== void 0 ? _result$additionalInf : '';
              }
              return _context9.abrupt("return", splitData);
            case 49:
            case "end":
              return _context9.stop();
          }
        }, _callee9, null, [[11, 22, 25, 28], [29, 39, 42, 45]]);
      }));
      return function (_x5) {
        return _ref7.apply(this, arguments);
      };
    }();

    /**
     * Fetches street predictions (including buildingNumber) from API
     * @param {string} streetFullRaw - Raw street input to get predictions for
     * @returns {Promise<Object>} Prediction results with original input
     * @throws {Error} If input is invalid or API request fails
     */
    ExtendableObject.util.getStreetFullPredictions = /*#__PURE__*/function () {
      var _ref8 = StreetFullExtension_asyncToGenerator(/*#__PURE__*/StreetFullExtension_regeneratorRuntime().mark(function _callee10(streetFullRaw) {
        var streetFull, autocompleteResult, _ExtendableObject$has2, autocompleteRequestIndex, message, headers, streetFullPredictionsTemp, _result$data2, EnderecoAPI, result, _ExtendableObject$uti, _ExtendableObject$uti2;
        return StreetFullExtension_regeneratorRuntime().wrap(function _callee10$(_context10) {
          while (1) switch (_context10.prev = _context10.next) {
            case 0:
              if (!(typeof streetFullRaw !== 'string')) {
                _context10.next = 2;
                break;
              }
              throw new Error('Invalid argument. The streetFullRaw has to be a string');
            case 2:
              streetFull = streetFullRaw.trim();
              autocompleteResult = {
                originalStreetFull: streetFull,
                predictions: []
              }; // Early return
              if (!(streetFull === '')) {
                _context10.next = 6;
                break;
              }
              return _context10.abrupt("return", autocompleteResult);
            case 6:
              if (ExtendableObject.streetFullAutocompleteCache.cachedResults[streetFull]) {
                _context10.next = 31;
                break;
              }
              autocompleteRequestIndex = ++ExtendableObject._streetFullAutocompleteRequestIndex;
              message = {
                jsonrpc: '2.0',
                id: autocompleteRequestIndex,
                method: 'streetAutocomplete',
                params: {
                  country: ExtendableObject.countryCode,
                  language: ExtendableObject.config.lang,
                  postCode: ExtendableObject.postalCode,
                  cityName: ExtendableObject.locality,
                  streetFull: streetFull
                }
              };
              headers = {
                'X-Auth-Key': ExtendableObject.config.apiKey,
                'X-Agent': ExtendableObject.config.agentName,
                'X-Remote-Api-Url': ExtendableObject.config.remoteApiUrl,
                'X-Transaction-Referer': window.location.href,
                'X-Transaction-Id': (_ExtendableObject$has2 = ExtendableObject.hasLoadedExtension) !== null && _ExtendableObject$has2 !== void 0 && _ExtendableObject$has2.call(ExtendableObject, 'SessionExtension') ? ExtendableObject.sessionId : 'not_required'
              };
              streetFullPredictionsTemp = [];
              _context10.prev = 11;
              EnderecoAPI = ExtendableObject.getEnderecoAPI();
              if (EnderecoAPI) {
                _context10.next = 16;
                break;
              }
              console.warn('EnderecoAPI is not available');
              return _context10.abrupt("return", autocompleteResult);
            case 16:
              _context10.next = 18;
              return EnderecoAPI.sendRequestToAPI(message, headers);
            case 18:
              result = _context10.sent;
              if ((result === null || result === void 0 || (_result$data2 = result.data) === null || _result$data2 === void 0 || (_result$data2 = _result$data2.error) === null || _result$data2 === void 0 ? void 0 : _result$data2.code) === StreetFullExtension_ERROR_EXPIRED_SESSION) {
                (_ExtendableObject$uti = (_ExtendableObject$uti2 = ExtendableObject.util).updateSessionId) === null || _ExtendableObject$uti === void 0 || _ExtendableObject$uti.call(_ExtendableObject$uti2);
              }
              if (!(!result || !result.data || !result.data.result)) {
                _context10.next = 23;
                break;
              }
              console.warn("API didn't return a valid result");
              return _context10.abrupt("return", autocompleteResult);
            case 23:
              // If session counter is set, increase it.
              if (ExtendableObject.hasLoadedExtension('SessionExtension')) {
                ExtendableObject.sessionCounter++;
              }
              result.data.result.predictions.forEach(function (streetFullPrediction) {
                if (!streetFullPrediction) return;
                var _ref9 = streetFullPrediction || {},
                  country = _ref9.country,
                  street = _ref9.street,
                  buildingNumber = _ref9.buildingNumber,
                  additionalInfo = _ref9.additionalInfo;
                var tempStreetFullContainer = {
                  countryCode: country !== null && country !== void 0 ? country : ExtendableObject._countryCode,
                  streetName: street !== null && street !== void 0 ? street : '',
                  buildingNumber: buildingNumber !== null && buildingNumber !== void 0 ? buildingNumber : '',
                  additionalInfo: additionalInfo !== null && additionalInfo !== void 0 ? additionalInfo : ''
                };
                if (streetFullPrediction.streetFull === undefined) {
                  tempStreetFullContainer.streetFull = ExtendableObject.util.Mustache.render(streetNameTemplates.getTemplate(tempStreetFullContainer.countryCode), {
                    streetName: tempStreetFullContainer.streetName,
                    buildingNumber: tempStreetFullContainer.buildingNumber,
                    additionalInfo: tempStreetFullContainer.additionalInfo
                  }).replace(/(\r\n|\n|\r)/gm, '').trim();
                }
                streetFullPredictionsTemp.push(tempStreetFullContainer);
              });
              _context10.next = 30;
              break;
            case 27:
              _context10.prev = 27;
              _context10.t0 = _context10["catch"](11);
              console.warn('Error fetching street full predictions', _context10.t0);
            case 30:
              ExtendableObject.streetFullAutocompleteCache.cachedResults[streetFull] = streetFullPredictionsTemp;
            case 31:
              autocompleteResult.predictions = ExtendableObject.streetFullAutocompleteCache.cachedResults[streetFull];
              return _context10.abrupt("return", autocompleteResult);
            case 33:
            case "end":
              return _context10.stop();
          }
        }, _callee10, null, [[11, 27]]);
      }));
      return function (_x6) {
        return _ref8.apply(this, arguments);
      };
    }();
  },
  /**
   * Place for registering data transformation and validation functions.
   * Add here:
   * - Input filters
   * - Data validators
   * - Value transformers
   * - Custom formatters
   * @param {Object} ExtendableObject - The base object being extended
   */
  registerFilterCallbacks: function registerFilterCallbacks(ExtendableObject) {
    /**
     * Filter function for streetFull value processing. Its not doing much, but it can be rewritten by
     * user implementation.
     * @param {string} streetFull - Street value to process
     * @returns {string} Processed street value
     */
    ExtendableObject.cb.setStreetFull = function (streetFull) {
      return streetFull;
    };
  },
  /**
   * Place for registering configuration.
   * Add here:
   * - HTML templates
   * - UI configuration
   * - Display format templates
   * - Other configs
   * @param {Object} ExtendableObject - The base object being extended
   */
  registerConfig: function registerConfig(ExtendableObject) {
    // Templates
    ExtendableObject.config.templates.streetFull = streetNameTemplates;
    ExtendableObject.config.templates.streetFullPredictions = street_full_predictions;
  },
  /**
   * Main extension function that sets up all streetFull functionality.
   * @param {Object} ExtendableObject - The base object being extended
   * @returns {Promise<Object>} - Resolves to the StreetFullExtension object
   *
   * Executes all registration functions in sequence:
   * 1. registerProperties - Sets up internal state
   * 2. registerFields - Adds streetFull field
   * 3. registerConfig - Sets up templates and other configs
   * 4. registerEventCallbacks - Adds event handlers
   * 5. registerUtilities - Adds helper functions
   * 6. registerAPIHandlers - Sets up API communication
   * 7. registerFilterCallbacks - Adds data filters
   *
   * All registrations are handled asynchronously to ensure proper setup.
   * The extension object is returned to confirm successful extension.
   */
  extend: function () {
    var _extend = StreetFullExtension_asyncToGenerator(/*#__PURE__*/StreetFullExtension_regeneratorRuntime().mark(function _callee11(ExtendableObject) {
      return StreetFullExtension_regeneratorRuntime().wrap(function _callee11$(_context11) {
        while (1) switch (_context11.prev = _context11.next) {
          case 0:
            _context11.next = 2;
            return StreetFullExtension.registerProperties(ExtendableObject);
          case 2:
            _context11.next = 4;
            return StreetFullExtension.registerFields(ExtendableObject);
          case 4:
            _context11.next = 6;
            return StreetFullExtension.registerConfig(ExtendableObject);
          case 6:
            _context11.next = 8;
            return StreetFullExtension.registerEventCallbacks(ExtendableObject);
          case 8:
            _context11.next = 10;
            return StreetFullExtension.registerUtilities(ExtendableObject);
          case 10:
            _context11.next = 12;
            return StreetFullExtension.registerAPIHandlers(ExtendableObject);
          case 12:
            _context11.next = 14;
            return StreetFullExtension.registerFilterCallbacks(ExtendableObject);
          case 14:
            return _context11.abrupt("return", StreetFullExtension);
          case 15:
          case "end":
            return _context11.stop();
        }
      }, _callee11);
    }));
    function extend(_x7) {
      return _extend.apply(this, arguments);
    }
    return extend;
  }()
};
/* harmony default export */ var fields_StreetFullExtension = (StreetFullExtension);
;// ./modules/extensions/checks/StreetFullCheckExtension.js
var StreetFullCheckExtension = {
  name: 'StreetFullCheckExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      ExtendableObject.waitForExtension('StreetFullExtension').then(function () {
        // Add email field.
        ExtendableObject._streetFullStatus = '';
        ExtendableObject._subscribers.streetFullStatus = [];

        // Add change event hadler.
        ExtendableObject.cb.streetFullStatusChange = function (subscriber) {
          return function (e) {
            ExtendableObject.streetFullStatus = subscriber.value;
          };
        };

        // Add the "emaiL" property
        Object.defineProperty(ExtendableObject, 'streetFullStatus', {
          get: function get() {
            return this._streetFullStatus;
          },
          set: function set(value) {
            var oldValue = ExtendableObject._streetFullStatus;
            ExtendableObject._awaits++;
            ExtendableObject.util.Promise.resolve(value).then(function (value) {
              var newValue = value;
              if (oldValue !== newValue) {
                ExtendableObject._streetFullStatus = newValue;

                // Inform all subscribers about the change.
                ExtendableObject._subscribers.streetFullStatus.forEach(function (subscriber) {
                  subscriber.value = value;
                });
                ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                  detail: {
                    fieldName: 'streetFullStatus',
                    oldValue: oldValue,
                    newValue: newValue,
                    object: ExtendableObject
                  }
                }));
              }
            }).catch().finally(function () {
              ExtendableObject._awaits--;
            });
          }
        });
        if (ExtendableObject.config.showDebugInfo) {
          console.log('StreetFullCheckExtension applied');
        }
        resolve($self);
      }).catch(function () {
        console.log('Failed to load because of timeout');
        reject($self);
      });
    });
  }
};
/* harmony default export */ var checks_StreetFullCheckExtension = (StreetFullCheckExtension);
;// ./modules/extensions/fields/AdditionalInfoExtension.js
var AdditionalInfoExtension = {
  name: 'AdditionalInfoExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      ExtendableObject._additionalInfo = '';
      ExtendableObject._subscribers.additionalInfo = [];
      ExtendableObject.cb.setAdditionalInfo = function (additionalInfo) {
        return new ExtendableObject.util.Promise(function (resolve, reject) {
          resolve(additionalInfo);
        });
      };
      ExtendableObject.cb.additionalInfoChange = function (subscriber) {
        return function (e) {
          ExtendableObject.additionalInfo = subscriber.value;
          if (ExtendableObject.active) {
            ExtendableObject._changed = true;
            ExtendableObject.addressStatus = [];
          }
        };
      };
      ExtendableObject.cb.additionalInfoBlur = function (subscriber) {
        return function (e) {
          ExtendableObject.waitUntilReady().then(function () {
            if (ExtendableObject.onBlurTimeout) {
              clearTimeout(ExtendableObject.onBlurTimeout);
              ExtendableObject.onBlurTimeout = null;
            }
            ExtendableObject.onBlurTimeout = setTimeout(function () {
              if (ExtendableObject.config.trigger.onblur && !ExtendableObject.anyActive() && ExtendableObject.util.shouldBeChecked() && !window.EnderecoIntegrator.hasSubmit) {
                // Second. Check Address.
                ExtendableObject.onBlurTimeout = null;
                ExtendableObject.util.checkAddress().catch();
              }
            }, 300);
          }).catch();
        };
      };

      // Add getter and setter for fields.
      Object.defineProperty(ExtendableObject, 'additionalInfo', {
        get: function get() {
          return this._additionalInfo;
        },
        set: function set(value) {
          ExtendableObject._awaits++;
          ExtendableObject.util.Promise.resolve(value).then(function (value) {
            ExtendableObject._awaits++;
            ExtendableObject.cb.setAdditionalInfo(value).then(function (value) {
              var oldValue = ExtendableObject._additionalInfo;
              var newValue = value;
              var notSame = ExtendableObject._additionalInfo !== value;

              // Chunk set.
              if (ExtendableObject.hasLoadedExtension('AdditionalInfoAutocompleteExtension')) {
                notSame = notSame || ExtendableObject._additionalInfoChunk !== value;
              }
              if (notSame) {
                ExtendableObject._additionalInfo = value;
                if (ExtendableObject.hasLoadedExtension('AdditionalInfoAutocompleteExtension')) {
                  ExtendableObject._additionalInfoChunk = value;
                }

                // Inform all subscribers about the change.
                ExtendableObject._subscribers.additionalInfo.forEach(function (subscriber) {
                  subscriber.value = value;
                });
                if (ExtendableObject.active) {
                  ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                    detail: {
                      fieldName: 'additionalInfo',
                      oldValue: oldValue,
                      newValue: newValue,
                      object: ExtendableObject
                    }
                  }));
                }
              }
            }).catch(function (e) {
              if (ExtendableObject.config.showDebugInfo) {
                console.log('Error resolving additionalInfo', e);
              }
            }).finally(function () {
              ExtendableObject._awaits--;
            });
          }).catch().finally(function () {
            ExtendableObject._awaits--;
          });
        }
      });
      ExtendableObject.fieldNames.push('additionalInfo');
      if (ExtendableObject.config.showDebugInfo) {
        console.log('AdditionalInfoExtension applied');
      }
      resolve($self);
    });
  }
};
/* harmony default export */ var fields_AdditionalInfoExtension = (AdditionalInfoExtension);
;// ./modules/extensions/checks/AdditionalInfoCheckExtension.js
var AdditionalInfoCheckExtension = {
  name: 'AdditionalInfoCheckExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      ExtendableObject.waitForExtension('AdditionalInfoExtension').then(function () {
        // Add email field.
        ExtendableObject._additionalInfoStatus = '';
        ExtendableObject._subscribers.additionalInfoStatus = [];

        // Add change event hadler.
        ExtendableObject.cb.additionalInfoStatusChange = function (subscriber) {
          return function (e) {
            ExtendableObject.additionalInfoStatus = subscriber.value;
          };
        };

        // Add the "emaiL" property
        Object.defineProperty(ExtendableObject, 'additionalInfoStatus', {
          get: function get() {
            return this._additionalInfoStatus;
          },
          set: function set(value) {
            var oldValue = ExtendableObject._additionalInfoStatus;
            ExtendableObject._awaits++;
            ExtendableObject.util.Promise.resolve(value).then(function (value) {
              var newValue = value;
              if (oldValue !== newValue) {
                ExtendableObject._additionalInfoStatus = newValue;

                // Inform all subscribers about the change.
                ExtendableObject._subscribers.additionalInfoStatus.forEach(function (subscriber) {
                  subscriber.value = value;
                });
                ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                  detail: {
                    fieldName: 'additionalInfoStatus',
                    oldValue: oldValue,
                    newValue: newValue,
                    object: ExtendableObject
                  }
                }));
              }
            }).catch().finally(function () {
              ExtendableObject._awaits--;
            });
          }
        });
        if (ExtendableObject.config.showDebugInfo) {
          console.log('AdditionalInfoCheckExtension applied');
        }
        resolve($self);
      }).catch(function () {
        console.log('Failed to load because of timeout');
        reject($self);
      });
    });
  }
};
/* harmony default export */ var checks_AdditionalInfoCheckExtension = (AdditionalInfoCheckExtension);
;// ./modules/extensions/fields/AddressExtension.js
var AddressExtension = {
  name: 'AddressExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      ExtendableObject.waitForExtension(['CountryCodeExtension', 'PostalCodeExtension', 'LocalityExtension', 'StreetNameExtension', 'BuildingNumberExtension', 'AdditionalInfoExtension']).then(function () {
        ExtendableObject._subscribers.address = [];
        ExtendableObject._openDropdowns = 0;
        ExtendableObject.hasOpenDropdowns = function () {
          return ExtendableObject._openDropdowns > 0;
        };
        ExtendableObject.cb.setAddress = function (address) {
          return new ExtendableObject.util.Promise(function (resolve, reject) {
            resolve(address);
          });
        };
        ExtendableObject.cb.addressChange = function (subscriber) {
          return function (e) {
            ExtendableObject.address = subscriber.value;
          };
        };

        // Add getter and setter for fields.
        Object.defineProperty(ExtendableObject, 'address', {
          get: function get() {
            var $address = {};
            ExtendableObject.fieldNames.forEach(function (fieldName) {
              $address[fieldName] = ExtendableObject[fieldName];
            });
            return $address;
          },
          set: function set(value) {
            ExtendableObject._awaits++;
            ExtendableObject.util.Promise.resolve(value).then(function (value) {
              ExtendableObject._awaits++;
              ExtendableObject.cb.setAddress(value).then(function (value) {
                // TODO: parse and set address
              }).catch(function (e) {
                if (ExtendableObject.config.showDebugInfo) {
                  console.log('Error resolving address', e);
                }
              }).finally(function () {
                ExtendableObject._awaits--;
              });
            }).catch().finally(function () {
              ExtendableObject._awaits--;
            });
          }
        });
        if (ExtendableObject.config.showDebugInfo) {
          console.log('AddressExtension applied');
        }
        resolve($self);
      }).catch();
    });
  }
};
/* harmony default export */ var fields_AddressExtension = (AddressExtension);
;// ./templates/fulladdress/default.html
// Module
var fulladdress_default_code = "{{#additionalInfo}} {{#useHtml}}<span class=\"endereco-additional-info\">{{/useHtml}}{{{additionalInfo}}}{{#useHtml}}</span>{{/useHtml}} {{/additionalInfo}} <div></div> {{#streetFull}} {{#useHtml}}<span class=\"endereco-street-full\">{{/useHtml}}{{{streetFull}}}{{#useHtml}}</span>{{/useHtml}} <br> {{/streetFull}} {{^streetFull}} {{#useHtml}}<span class=\"endereco-street-name\">{{/useHtml}}{{{streetName}}}{{#useHtml}}</span>{{/useHtml}} {{#useHtml}}<span class=\"endereco-building-number\">{{/useHtml}}{{{buildingNumber}}}{{#useHtml}}</span>{{/useHtml}} <br> {{/streetFull}} {{#useHtml}}<span class=\"endereco-postal-code\">{{/useHtml}}{{{postalCode}}}{{#useHtml}}</span>{{/useHtml}} {{#useHtml}}<span class=\"endereco-locality\">{{/useHtml}}{{{locality}}}{{#useHtml}}</span>{{/useHtml}} <br> {{#showSubdisivion}}{{#useHtml}}<span class=\"endereco-subdivision-code\">{{/useHtml}}{{{subdivisionName}}}{{#useHtml}}</span>{{/useHtml}} <br>{{/showSubdisivion}} {{#showCountry}}{{#useHtml}}<span class=\"endereco-country-code\">{{/useHtml}}{{{countryName}}}{{#useHtml}}</span>{{/useHtml}}{{/showCountry}} ";
// Exports
/* harmony default export */ var fulladdress_default = (fulladdress_default_code);
;// ./templates/fulladdress/fr.html
// Module
var fulladdress_fr_code = "{{#additionalInfo}} {{#useHtml}}<span class=\"endereco-additional-info\">{{/useHtml}}{{{additionalInfo}}}{{#useHtml}}</span>{{/useHtml}} {{/additionalInfo}} <div></div> {{#streetFull}} {{#useHtml}}<span class=\"endereco-street-full\">{{/useHtml}}{{{streetFull}}}{{#useHtml}}</span>{{/useHtml}} <br> {{/streetFull}} {{^streetFull}} {{#useHtml}}<span class=\"endereco-building-number\">{{/useHtml}}{{{buildingNumber}}}{{#useHtml}}</span>{{/useHtml}} {{#useHtml}}<span class=\"endereco-street-name\">{{/useHtml}}{{{streetName}}}{{#useHtml}}</span>{{/useHtml}} <br> {{/streetFull}} {{#useHtml}}<span class=\"endereco-postal-code\">{{/useHtml}}{{{postalCode}}}{{#useHtml}}</span>{{/useHtml}} {{#useHtml}}<span class=\"endereco-locality\">{{/useHtml}}{{{locality}}}{{#useHtml}}</span>{{/useHtml}} <br> {{#showSubdisivion}}{{#useHtml}}<span class=\"endereco-subdivision-code\">{{/useHtml}}{{{subdivisionName}}}{{#useHtml}}</span>{{/useHtml}} <br>{{/showSubdisivion}} {{#showCountry}}{{#useHtml}}<span class=\"endereco-country-code\">{{/useHtml}}{{{countryName}}}{{#useHtml}}</span>{{/useHtml}}{{/showCountry}} ";
// Exports
/* harmony default export */ var fulladdress_fr = (fulladdress_fr_code);
;// ./templates/addressFullTemplates.js


var addressFullTemplates_templates = {
  default: fulladdress_default,
  de: fulladdress_default,
  at: fulladdress_default,
  fr: fulladdress_fr,
  us: fulladdress_fr,
  dz: fulladdress_fr,
  ad: fulladdress_fr,
  az: fulladdress_fr,
  am: fulladdress_fr,
  au: fulladdress_fr,
  bh: fulladdress_fr,
  bd: fulladdress_fr,
  bn: fulladdress_fr,
  kh: fulladdress_fr,
  ca: fulladdress_fr,
  cn: fulladdress_fr,
  cx: fulladdress_fr,
  cc: fulladdress_fr,
  cd: fulladdress_fr,
  cg: fulladdress_fr,
  ck: fulladdress_fr,
  cy: fulladdress_fr,
  ci: fulladdress_fr,
  dm: fulladdress_fr,
  eg: fulladdress_fr,
  fj: fulladdress_fr,
  ga: fulladdress_fr,
  gm: fulladdress_fr,
  ge: fulladdress_fr,
  gh: fulladdress_fr,
  gi: fulladdress_fr,
  gp: fulladdress_fr,
  gu: fulladdress_fr,
  gy: fulladdress_fr,
  hk: fulladdress_fr,
  in: fulladdress_fr,
  id: fulladdress_fr,
  ie: fulladdress_fr,
  gb: fulladdress_fr,
  il: fulladdress_fr,
  la: fulladdress_fr,
  lb: fulladdress_fr,
  lu: fulladdress_fr,
  mr: fulladdress_fr,
  mu: fulladdress_fr,
  mc: fulladdress_fr,
  ms: fulladdress_fr,
  ma: fulladdress_fr,
  mm: fulladdress_fr,
  na: fulladdress_fr,
  nz: fulladdress_fr,
  ng: fulladdress_fr,
  nf: fulladdress_fr,
  om: fulladdress_fr,
  pk: fulladdress_fr,
  pw: fulladdress_fr,
  ph: fulladdress_fr,
  pr: fulladdress_fr,
  rw: fulladdress_fr,
  re: fulladdress_fr,
  bl: fulladdress_fr,
  sh: fulladdress_fr,
  kn: fulladdress_fr,
  mf: fulladdress_fr,
  sa: fulladdress_fr,
  sn: fulladdress_fr,
  sl: fulladdress_fr,
  sg: fulladdress_fr,
  tw: fulladdress_fr,
  tz: fulladdress_fr,
  th: fulladdress_fr,
  tg: fulladdress_fr,
  tt: fulladdress_fr,
  tn: fulladdress_fr,
  tm: fulladdress_fr,
  tc: fulladdress_fr,
  vm: fulladdress_fr,
  wf: fulladdress_fr,
  ye: fulladdress_fr,
  zm: fulladdress_fr,
  zw: fulladdress_fr,
  getTemplate: function getTemplate(countryCode) {
    countryCode = countryCode.toLowerCase();
    if (!!this[countryCode]) {
      return this[countryCode];
    } else {
      return this.default;
    }
  }
};
/* harmony default export */ var addressFullTemplates = (addressFullTemplates_templates);
;// ./templates/address_check_wrapper_template.html
// Module
var address_check_wrapper_template_code = "<div endereco-popup class=\"endereco-popup-container endereco-popup-container--direction-{{direction}}\"> <div class=\"endereco-modal\"> <div class=\"endereco-modal__header\"> <p class=\"endereco-modal__header-main\">{{{title}}}</p> <p class=\"endereco-modal__header-sub\">{{{EnderecoAddressObject.config.texts.popUpSubline}}}</p> {{#showClose}}<span class=\"endereco-modal__close\" endereco-modal-close></span>{{/showClose}} </div> <div class=\"endereco-modal__body\"> <div class=\"endereco-modal__divider\"> <span class=\"endereco-modal__divider-innertext\">{{{EnderecoAddressObject.config.texts.ourSuggestions}}}</span> </div> <ul class=\"endereco-address-predictions endereco-address-predictions--suggestions\"> {{#predictions}} <li class=\"endereco-address-predictions__item\"> <input class=\"endereco-address-predictions__radio\" type=\"radio\" name=\"endereco-address-predictions\" value=\"{{ index }}\" id=\"endereco-address-predictions__item_{{ index }}\"> <label class=\"endereco-address-predictions__label\" for=\"endereco-address-predictions__item_{{ index }}\">{{{ addressDiff }}}</label> </li> {{loopUp}} {{/predictions}} </ul> <div class=\"endereco-modal__divider\"> <span class=\"endereco-modal__divider-innertext\">{{{EnderecoAddressObject.config.texts.yourInput}}} <small><a href=\"javascript:void(0)\" endereco-edit-address>{{{EnderecoAddressObject.config.texts.editYourInput}}}</a></small></span> </div> <ul class=\"endereco-address-predictions endereco-address-predictions--original\"> <li class=\"endereco-address-predictions__item\"> <input class=\"endereco-address-predictions__radio\" type=\"radio\" name=\"endereco-address-predictions\" value=\"-1\" id=\"endereco-address-predictions__item_original\"> <label class=\"endereco-address-predictions__label\" for=\"endereco-address-predictions__item_original\">{{{ mainAddress }}}</label> </li> </ul> <div class=\"endereco-modal__warning\" endereco-show-if-origin> {{{EnderecoAddressObject.config.texts.warningText}}} </div> {{#showConfirCheckbox}} <div class=\"endereco-modal__address-confirmation\" endereco-show-if-origin> <label> <input type=\"checkbox\" value=\"1\" endereco-confirm-address-checkbox> {{{EnderecoAddressObject.config.texts.confirmMyAddressCheckbox}}} </label> </div> {{/showConfirCheckbox}} </div> <div class=\"endereco-modal__footer\"> {{{button}}} </div> </div> </div> ";
// Exports
/* harmony default export */ var address_check_wrapper_template = (address_check_wrapper_template_code);
;// ./templates/address_not_found_wrapper_template.html
// Module
var address_not_found_wrapper_template_code = "<div endereco-popup class=\"endereco-popup-container endereco-popup-container--direction-{{direction}}\"> <div class=\"endereco-modal\"> <div class=\"endereco-modal__header\"> <p class=\"endereco-modal__header-main\">{{{title}}}</p> <p class=\"endereco-modal__header-sub\">{{{EnderecoAddressObject.config.texts.notFoundSubline}}}</p> {{#showClose}}<span class=\"endereco-modal__close\" endereco-modal-close></span>{{/showClose}} </div> <div class=\"endereco-modal__body\"> <div class=\"endereco-modal__divider\"> <span class=\"endereco-modal__divider-innertext\">{{{EnderecoAddressObject.config.texts.yourInput}}} <small><a href=\"javascript:void(0)\" endereco-edit-address>{{{EnderecoAddressObject.config.texts.editYourInput}}}</a></small></span> </div> <div class=\"endereco-modal__address-container\"> {{{ mainAddress }}} </div> <div class=\"endereco-modal__warning\"> {{{EnderecoAddressObject.config.texts.warningText}}} </div> {{#showConfirCheckbox}} <div class=\"endereco-modal__address-confirmation\"> <label> <input type=\"checkbox\" value=\"1\" endereco-confirm-address-checkbox> {{{EnderecoAddressObject.config.texts.confirmMyAddressCheckbox}}} </label> </div> {{/showConfirCheckbox}} </div> <div class=\"endereco-modal__footer\"> {{{buttonSecondary}}} {{{button}}} </div> </div> </div> ";
// Exports
/* harmony default export */ var address_not_found_wrapper_template = (address_not_found_wrapper_template_code);
;// ./templates/address_no_prediction_wrapper_template.html
// Module
var address_no_prediction_wrapper_template_code = "<div endereco-popup class=\"endereco-popup-container endereco-popup-container--direction-{{direction}}\"> <div class=\"endereco-modal endereco-modal--no-prediction {{{modalClasses}}}\"> <div class=\"endereco-modal__header\"> <p class=\"endereco-modal__header-main\">{{{title}}}</p> <p class=\"endereco-modal__header-sub\">{{{EnderecoAddressObject.config.texts.mistakeNoPredictionSubline}}}</p> {{#showClose}}<span class=\"endereco-modal__close\" endereco-modal-close></span>{{/showClose}} </div> <div class=\"endereco-modal__body\"> <div class=\"endereco-modal__divider\"> <span class=\"endereco-modal__divider-innertext\">{{{EnderecoAddressObject.config.texts.yourInput}}} <small><a href=\"javascript:void(0)\" endereco-edit-address>{{{EnderecoAddressObject.config.texts.editYourInput}}}</a></small></span> </div> {{#hasErrors}} <div class=\"endereco-modal__errors\"> <ul> {{#errors}} <li>{{{.}}}</li> {{/errors}} </ul> </div> {{/hasErrors}} <div class=\"endereco-modal__address-container\"> {{{ mainAddress }}} </div> <div class=\"endereco-modal__warning\"> {{{EnderecoAddressObject.config.texts.warningText}}} </div> {{#showConfirCheckbox}} <div class=\"endereco-modal__address-confirmation\"> <label> <input type=\"checkbox\" value=\"1\" endereco-confirm-address-checkbox> {{{EnderecoAddressObject.config.texts.confirmMyAddressCheckbox}}} </label> </div> {{/showConfirCheckbox}} </div> <div class=\"endereco-modal__footer\"> {{{buttonSecondary}}} {{{button}}} </div> </div> </div> ";
// Exports
/* harmony default export */ var address_no_prediction_wrapper_template = (address_no_prediction_wrapper_template_code);
// EXTERNAL MODULE: ./node_modules/custom-event-polyfill/polyfill.js
var polyfill = __webpack_require__(2770);
;// ./modules/subscriber.js
function subscriber_createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = subscriber_unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t.return || t.return(); } finally { if (u) throw o; } } }; }
function subscriber_unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return subscriber_arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? subscriber_arrayLikeToArray(r, a) : void 0; } }
function subscriber_arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }



function EnderecoSubscriber(propertyName, observableObject) {
  var options = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};
  var defaultOptions = {
    valueContainer: 'value',
    // value, classList, innerHTML
    displayAutocompleteDropdown: false,
    showDebugInfo: false,
    useWatcher: false,
    syncValue: false,
    autosubscribeToStatus: true,
    writeFilterCb: function writeFilterCb(value, selfReference) {
      return new src(function (resolve, reject) {
        return resolve(value);
      });
    },
    readFilterCb: function readFilterCb(value, selfReference) {
      return new src(function (resolve, reject) {
        return resolve(value);
      });
    },
    customSetValue: undefined,
    customGetValue: undefined
  };
  if (!observableObject) {
    return null;
  }
  options = lodash_merge_default()(defaultOptions, options);
  function setupValueChangeDetection(subscriber, callback) {
    var interval = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 300;
    var element = subscriber.object;

    // Initially get the value, ensuring it's treated as a Promise
    src.resolve(subscriber.value).then(function (value) {
      return subscriber.lastValue = value;
    });
    var checkValueChange = function checkValueChange() {
      // Ensure the current value is treated as a Promise
      src.resolve(subscriber.value).then(function (currentValue) {
        if (subscriber.object.type === 'radio') {
          if (subscriber.object.checked) {
            subscriber.lastValue = currentValue;
            callback();
          }
        } else if (subscriber.object.type === 'checkbox') {
          if (currentValue !== subscriber.lastValue && document.activeElement !== element) {
            subscriber.lastValue = currentValue;
            callback();
          }
        } else {
          if (currentValue !== subscriber.lastValue && document.activeElement !== element) {
            subscriber.lastValue = currentValue;
            callback();
          }
        }
      });
    };
    var intervalId = setInterval(checkValueChange, interval);

    // Optional: Mutation observer for element removal
    var observer = setupMutationObserver(element, intervalId);

    // Return cleanup function
    return function () {
      return cleanup(observer, intervalId);
    };
  }
  function setupValueInputDetection(subscriber, callback) {
    var interval = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 10;
    var element = subscriber.object;

    // Initially get the value, ensuring it's treated as a Promise
    src.resolve(subscriber.value).then(function (value) {
      return subscriber.lastValue = value;
    });
    var checkValueInput = function checkValueInput() {
      // Ensure the current value is treated as a Promise
      src.resolve(subscriber.value).then(function (currentValue) {
        if (currentValue !== subscriber.lastValue && document.activeElement === element) {
          subscriber.lastValue = currentValue;
          callback();
        }
      });
    };
    var intervalId = setInterval(checkValueInput, interval);

    // Optional: Mutation observer for element removal
    var observer = setupMutationObserver(element, intervalId);

    // Return cleanup function
    return function () {
      return cleanup(observer, intervalId);
    };
  }
  function setupBlurDetection(subscriber, callback) {
    var interval = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 10;
    var element = subscriber.object;
    var isFocused = document.activeElement === element;
    var checkForBlur = function checkForBlur() {
      var currentlyFocused = document.activeElement === element;
      if (isFocused && !currentlyFocused) {
        callback(subscriber); // Element has lost focus
      }
      isFocused = currentlyFocused;
    };
    var intervalId = setInterval(checkForBlur, interval);

    // Optional: Mutation observer for element removal
    var observer = setupMutationObserver(element, intervalId);

    // Return cleanup function
    return function () {
      return cleanup(observer, intervalId);
    };
  }
  function setupMutationObserver(element, intervalId) {
    var observer = new MutationObserver(function (mutations) {
      var _iterator = subscriber_createForOfIteratorHelper(mutations),
        _step;
      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          var mutation = _step.value;
          if (!document.contains(element)) {
            clearInterval(intervalId);
            observer.disconnect();
            break;
          }
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
    });
    observer.observe(document.body, {
      childList: true,
      subtree: true
    });
    return observer;
  }
  function cleanup(observer, intervalId) {
    clearInterval(intervalId);
    observer.disconnect();
  }
  var subscriber = {
    propertyName: propertyName,
    _subject: null,
    object: observableObject,
    options: options,
    cleanupFunctions: [],
    lastValue: undefined,
    getLastValue: function getLastValue() {
      return this.lastValue;
    },
    updateCheckedValue: function updateCheckedValue(newValue) {
      if (this.dispatchEvent('endereco-change')) {
        this.object.checked = newValue;
        this.dispatchEvent('endereco-blur');
      }
    },
    updateValue: function updateValue(newValue) {
      var updateInnerState = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : true;
      if (this.dispatchEvent('endereco-change')) {
        this.object.value = newValue;
        if (updateInnerState) {
          this.lastValue = newValue;
        }
        this.dispatchEvent('endereco-blur');
      }
    },
    dispatchEvent: function dispatchEvent(eventName) {
      if (this._subject) {
        var event = new this._subject.util.CustomEvent(eventName, {
          bubbles: true,
          cancelable: eventName === 'endereco-change'
        });
        return this.object.dispatchEvent(event);
      }
      return false;
    },
    cleanupResources: function cleanupResources() {
      this.cleanupFunctions.forEach(function (cleanupFunc) {
        return cleanupFunc();
      });
      this.cleanupFunctions = []; // Clear the array after cleanup
    },
    set subject(subject) {
      var $self = this;
      if (!subject) {
        return;
      }
      this._subject = subject;
      var changeCallbackGenerator = this.propertyName + 'Change';
      if (this._subject.cb[changeCallbackGenerator]) {
        var callbackForChange = this._subject.cb[changeCallbackGenerator](this);
        var cleanupChangeListener = setupValueChangeDetection(this, callbackForChange);
        this.cleanupFunctions.push(cleanupChangeListener);
      }
      var inputCallbackGenerator = this.propertyName + 'Input';
      if (this._subject.cb[inputCallbackGenerator]) {
        var callbackForInput = this._subject.cb[inputCallbackGenerator](this);
        var cleanupInputListener = setupValueInputDetection(this, callbackForInput);
        this.cleanupFunctions.push(cleanupInputListener);
      }
      var blurCallbackGenerator = this.propertyName + 'Blur';
      if (undefined !== this._subject.cb[blurCallbackGenerator]) {
        var callbackForBlur = this._subject.cb[blurCallbackGenerator](this);
        var cleanupBlurListener = setupBlurDetection(this, callbackForBlur);
        this.cleanupFunctions.push(cleanupBlurListener);
      }
      var keydownCallbackGenerator = this.propertyName + 'Keydown';
      if (undefined !== this._subject.cb[keydownCallbackGenerator]) {
        var keydownCBFunc = this._subject.cb[keydownCallbackGenerator](this);
        this.object.removeEventListener('keydown', this._subject.cb[keydownCallbackGenerator](this));
        this.object.addEventListener('keydown', this._subject.cb[keydownCallbackGenerator](this));
        this.cleanupFunctions.push(function () {
          $self.object.removeEventListener('keydown', keydownCBFunc);
        });
      }

      // Add autocomplete disabler.
      if (this._subject.config.ux.disableBrowserAutocomplete && ['text', 'number'].includes(this.object.type)) {
        if (/chrom(e|ium)/.test(navigator.userAgent.toLowerCase())) {
          this.object.setAttribute('autocomplete', 'autocomplete_' + Math.random().toString(36).substring(2) + Date.now());
        } else {
          this.object.setAttribute('autocomplete', 'off');
        }
      }
      var selfRef = this;
      var cleanupInterval = setInterval(function () {
        if (!document.contains(selfRef.object)) {
          selfRef.cleanupResources();
          clearInterval(cleanupInterval);
        }
      }, 100);

      // Skip for hidden type inputs.
      if (!!this.object && 'hidden' !== this.object.type) {
        // Subscribe to status.
        // If is fieldName -> subscribe to next visible parent.
        if (subject.fieldNames.includes(this.propertyName) && this.options.autosubscribeToStatus) {
          subject.addSubscriber(new EnderecoSubscriber(this.propertyName + 'Status', this.object.parentNode,
          // Subscribe to the parent,
          {
            valueContainer: 'classList'
          }));
        }
      }

      // Add autocomplete subscriber.
      if (this.options.syncValue) {
        var $this = this;
        var $key = this.propertyName;
        src.resolve($this.value).then(function (subscriberValue) {
          var innerValue = subject[$key];
          if (Array.isArray(innerValue)) {
            innerValue = innerValue.join();
          }
          var innerValueEmpty = !innerValue && 0 !== innerValue;
          var subscriberValueEmpty = !subscriberValue && 0 !== subscriberValue;
          if (!subscriberValueEmpty && innerValueEmpty) {
            subject[$key] = subscriberValue;
          }
          if (subscriberValueEmpty && !innerValueEmpty) {
            subscriber.value = subject[$key];
          }
        }).catch(function (e) {
          if (subject.config.showDebugInfo) {
            console.log('Error syncing values ', e);
          }
        });
      }
    },
    get subject() {
      return this._subject;
    },
    get value() {
      var $self = this;
      var value = '';
      if ('classList' === this.options.valueContainer) {
        value = this.getClassList();
      } else if ('innerHTML' === this.options.valueContainer) {
        value = this.getInnerHTML();
      } else if ('value' === this.options.valueContainer) {
        if ($self.object.disabled) {
          value = '';
        } else {
          value = this.getValue();
        }
      } else {
        value = this.get(this.options.valueContainer);
      }
      return this.options.readFilterCb(value, $self);
    },
    set value(value) {
      var $self = this;
      if ($self.object.disabled && value !== '') {
        return;
      }
      if (!!$self._subject) {
        $self._subject._awaits++;
      }
      this.options.writeFilterCb(value, $self).then(function (value) {
        if ('classList' === $self.options.valueContainer) {
          return $self.setClassList(value);
        } else if ('innerHTML' === $self.options.valueContainer) {
          return $self.setInnerHTML(value);
        } else if ('value' === $self.options.valueContainer) {
          if (!!$self.options.customSetValue) {
            return $self.options.customSetValue($self, value);
          } else {
            return $self.setValue(value);
          }
        } else {
          return $self.set($self.options.valueContainer, value);
        }
      }).catch(function (e) {}).finally(function () {
        if (!!$self._subject) {
          $self._subject._awaits--;
        }
      });
    },
    set: function set(valueType, value) {
      if (this.object instanceof HTMLElement) {
        this.object.setAttribute(valueType, value);
      } else {
        if (undefined !== this.object[valueType]) {
          this.object[valueType] = value;
        }
      }
    },
    setValue: function setValue(value) {
      // Simplify the setting of `newValue` for checkbox and radio inputs
      if (['radio', 'checkbox'].includes(this.object.type)) {
        var newValue = this.object.value === String(value);
        this.updateCheckedValue(newValue);
      } else {
        // Handle array values and standard values
        var _newValue = Array.isArray(value) ? value.join(',') : value;
        this.updateValue(_newValue);
      }
    },
    setClassList: function setClassList(value) {
      // Remove classes from element.
      var $DOMElement = this.object;
      var originalClasses = [];
      var temp = $DOMElement.classList;
      var i;
      for (i = 0; i < temp.length; i++) {
        originalClasses.push(temp[i]);
      }
      var toRemove = [];
      originalClasses.forEach(function (className) {
        if (className.indexOf('endereco-s--') !== -1) {
          toRemove.push(className);
        }
      });
      toRemove.forEach(function (singleValue) {
        $DOMElement.classList.remove(singleValue);
      });

      // Add classes.
      if (Array.isArray(value)) {
        value.forEach(function (singleValue) {
          $DOMElement.classList.add('endereco-s--' + singleValue);
        });
      } else {
        // Assume string.
        $DOMElement.classList.add('endereco-s--' + value);
      }
    },
    setInnerHTML: function setInnerHTML(value) {
      this.object.innerHTML = value;
    },
    get: function get(valueType) {
      if (this.object instanceof HTMLElement) {
        return this.object.getAttribute(valueType);
      } else {
        if (undefined !== this.object[valueType]) {
          return this.object[valueType];
        }
      }
    },
    getValue: function getValue() {
      if (['radio', 'checkbox'].includes(this.object.type)) {
        if (this.object.checked) {
          return this.object.value;
        } else {
          return '';
        }
      } else {
        return this.object.value;
      }
    },
    getClassList: function getClassList() {
      var toReturn = [];
      var originalClasses = [];
      var temp = this.object.classList;
      var i;
      for (i = 0; i < temp.length; i++) {
        originalClasses.push(temp[i]);
      }
      originalClasses.forEach(function (className) {
        if (className.indexOf('endereco-s--') !== -1) {
          toReturn.push(className.replace('endereco-s--', ''));
        }
      });
      if (1 < toReturn.length) {
        return toReturn;
      } else if (1 === toReturn.length) {
        return toReturn[0];
      } else {
        return '';
      }
    },
    getInnerHTML: function getInnerHTML() {
      return this.object.innerHTML;
    }
  };
  return subscriber;
}
/* harmony default export */ var modules_subscriber = (EnderecoSubscriber);
;// ./modules/extensions/checks/AddressCheckExtension.js






var AddressCheckExtension = {
  name: 'AddressCheckExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      ExtendableObject.waitForExtension('AddressExtension').then(function () {
        // Add email field.
        ExtendableObject._addressCheckRequestIndex = 1;
        ExtendableObject._addressStatus = [];
        ExtendableObject._subscribers.addressStatus = [];
        ExtendableObject._addressPredictions = [];
        ExtendableObject._subscribers.addressPredictions = [];
        ExtendableObject._addressPredictionsIndex = 0;
        ExtendableObject._subscribers.addressPredictionsIndex = [];
        ExtendableObject._addressTimestamp = [];
        ExtendableObject._subscribers.addressTimestamp = [];
        ExtendableObject._addressType = 'general_address';
        ExtendableObject._subscribers.addressType = [];
        ExtendableObject._checkedAddress = {};
        ExtendableObject._addressIsBeingChecked = false;

        // Callback collectors.
        ExtendableObject.onAfterAddressCheckNoAction = [];
        ExtendableObject.onAfterAddressCheck = [];
        ExtendableObject.onAfterAddressCheckSelected = [];
        ExtendableObject.onAfterModalRendered = [];
        ExtendableObject.onEditAddress = [];
        ExtendableObject.onConfirmAddress = [];
        ExtendableObject.config.templates.addressFull = addressFullTemplates;
        ExtendableObject.config.templates.addressPredictionsPopupWrapper = address_check_wrapper_template;
        ExtendableObject.config.templates.addressNotFoundPopupWrapper = address_not_found_wrapper_template;
        ExtendableObject.config.templates.addressNoPredictionWrapper = address_no_prediction_wrapper_template;
        ExtendableObject._globalStreetState = 1;
        if (!ExtendableObject.config.templates.button) {
          ExtendableObject.config.templates.button = '<button class="{{{buttonClasses}}}" endereco-use-selection endereco-disabled-until-confirmed>{{{EnderecoAddressObject.config.texts.useSelected}}}</button>';
        }
        if (!ExtendableObject.config.templates.buttonEditAddress) {
          ExtendableObject.config.templates.buttonEditAddress = '<button class="{{{buttonClasses}}}" endereco-edit-address>{{{EnderecoAddressObject.config.texts.editAddress}}}</button>';
        }
        if (!ExtendableObject.config.templates.buttonConfirmAddress) {
          ExtendableObject.config.templates.buttonConfirmAddress = '<button class="{{{buttonClasses}}}" endereco-confirm-address endereco-disabled-until-confirmed>{{{EnderecoAddressObject.config.texts.confirmAddress}}}</button>';
        }
        ExtendableObject.onBlurTimeout = null;
        ExtendableObject.areEssentialsDisabled = function () {
          var allPostalCodesDisabled = ExtendableObject._subscribers.postalCode.length > 0 && !ExtendableObject._subscribers.postalCode.some(function (subscriber) {
            return !subscriber.object.disabled;
          });
          if (allPostalCodesDisabled) {
            return true; // If all postal code subscriber is not disabled, return false early
          }
          var allLocalitiesDisabled = ExtendableObject._subscribers.locality.length > 0 && !ExtendableObject._subscribers.locality.some(function (subscriber) {
            return !subscriber.object.disabled;
          });
          return allLocalitiesDisabled; // Return true if all localities are disabled, false otherwise
        };
        ExtendableObject.waitForPopupAreaToBeFree = function () {
          return new ExtendableObject.util.Promise(function (resolve, reject) {
            var waitForFreePlace = setInterval(function () {
              var isAreaFree = !document.querySelector('[endereco-popup]');
              if (!!window.EnderecoIntegrator.$globalFilters && !!window.EnderecoIntegrator.$globalFilters.isModalAreaFree) {
                window.EnderecoIntegrator.$globalFilters.isModalAreaFree.forEach(function (callback) {
                  isAreaFree = callback(isAreaFree, $self);
                });
              }
              if (isAreaFree) {
                clearInterval(waitForFreePlace);
                resolve();
              }
            }, 100);
          });
        };
        ExtendableObject.waitForAllPopupsToClose = function () {
          return new ExtendableObject.util.Promise(function (resolve, reject) {
            var waitForFreePlace = setInterval(function () {
              if (undefined !== window.EnderecoIntegrator && undefined !== window.EnderecoIntegrator.popupQueue && 0 === window.EnderecoIntegrator.popupQueue) {
                clearInterval(waitForFreePlace);
                resolve();
              }
            }, 100);
          });
        };
        ExtendableObject.waitForAllEnderecoPopupsToClose = function () {
          return new ExtendableObject.util.Promise(function (resolve, reject) {
            var waitForFreePlace = setInterval(function () {
              if (undefined !== window.EnderecoIntegrator && undefined !== window.EnderecoIntegrator.enderecoPopupQueue && 0 === window.EnderecoIntegrator.enderecoPopupQueue) {
                clearInterval(waitForFreePlace);
                resolve();
              }
            }, 100);
          });
        };
        ExtendableObject.util.shouldBeChecked = function () {
          if (!ExtendableObject.countryCode || !ExtendableObject.streetName && !ExtendableObject.streetFull || !ExtendableObject.postalCode || !ExtendableObject.locality) {
            return false;
          }
          if (!['general_address', 'shipping_address', 'billing_address'].includes(ExtendableObject.addressType)) {
            return false;
          }
          if (!ExtendableObject._changed) {
            return false;
          }
          return true;
        };
        ExtendableObject.util.formatAddress = function () {
          var address = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : null;
          var forceCountryDisplay = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;
          var useHtml = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
          var $self = ExtendableObject;
          var useTemplate = 'default';
          var formattedAddress = '';
          var textArea;
          var $subdivVisible = false;
          if (!address) {
            address = JSON.parse(JSON.stringify(ExtendableObject.address));
          } else {
            address = JSON.parse(JSON.stringify(address));
          }

          // Format current address.
          if (undefined !== $self.config.templates.addressFull[$self.countryCode.toLowerCase()]) {
            useTemplate = $self.countryCode.toLowerCase();
          }
          if (address.hasOwnProperty('countryCode')) {
            if (!!window.EnderecoIntegrator.countryCodeToNameMapping && !!window.EnderecoIntegrator.countryCodeToNameMapping[address.countryCode.toUpperCase()]) {
              address.countryName = window.EnderecoIntegrator.countryCodeToNameMapping[address.countryCode.toUpperCase()];
              textArea = document.createElement('textarea');
              textArea.innerHTML = address.countryName;
              address.countryName = textArea.value.toUpperCase();
            } else {
              address.countryName = address.countryCode.toUpperCase();
            }
            address.showCountry = forceCountryDisplay || ExtendableObject.addressStatus.includes('country_code_needs_correction');
          }
          if (address.hasOwnProperty('subdivisionCode')) {
            if (!!window.EnderecoIntegrator.subdivisionCodeToNameMapping && !!window.EnderecoIntegrator.subdivisionCodeToNameMapping[address.subdivisionCode.toUpperCase()]) {
              address.subdivisionName = window.EnderecoIntegrator.subdivisionCodeToNameMapping[address.subdivisionCode.toUpperCase()];
              textArea = document.createElement('textarea');
              textArea.innerHTML = address.subdivisionName;
              address.subdivisionName = textArea.value;
            } else {
              if (address.subdivisionCode.toUpperCase()) {
                address.subdivisionName = address.subdivisionCode.toUpperCase().split("-")[1];
              } else {
                address.subdivisionName = "&nbsp;";
              }
            }
            if (0 < ExtendableObject._subscribers.subdivisionCode.length) {
              ExtendableObject._subscribers.subdivisionCode.forEach(function (listener) {
                if (!listener.object.disabled && listener.object.isConnected) {
                  $subdivVisible = true;
                }
              });
            }
            address.showSubdisivion = "&nbsp;" !== address.subdivisionName && (ExtendableObject.addressStatus.includes('subdivision_code_needs_correction') || ExtendableObject.addressStatus.includes('address_multiple_variants')) && $subdivVisible;
          }
          if (!address.buildingNumber || !address.buildingNumber.trim()) {
            address.buildingNumber = "&nbsp;";
          }
          address.useHtml = useHtml;
          var template = JSON.parse(JSON.stringify($self.config.templates.addressFull[useTemplate]));
          formattedAddress = ExtendableObject.util.Mustache.render(template, address).replace(/  +/g, ' ');
          return formattedAddress;
        };
        ExtendableObject.util.renderAddressPredictionsPopup = function () {
          var options = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {
            onlyInner: false
          };
          // Block a slot immediately.
          if (undefined === window.EnderecoIntegrator) {
            window.EnderecoIntegrator = {};
          }
          if (undefined === window.EnderecoIntegrator.popupQueue) {
            window.EnderecoIntegrator.popupQueue = 0;
            window.EnderecoIntegrator.enderecoPopupQueue = 0;
          }
          window.EnderecoIntegrator.popupQueue++;
          window.EnderecoIntegrator.enderecoPopupQueue++;

          // Check if empty object.
          if (Object.keys(ExtendableObject._checkedAddress).length === 0 && ExtendableObject._checkedAddress.constructor === Object) {
            ExtendableObject._checkedAddress = JSON.parse(JSON.stringify(ExtendableObject.address));
          }
          ExtendableObject.waitUntilReady().then(function () {
            ExtendableObject.waitForPopupAreaToBeFree().then(function () {
              var diff = [];
              var $self = ExtendableObject;

              // Render no popup.
              if (ExtendableObject.addressStatus.includes('address_correct')) {
                // No poup needed.
                window.EnderecoIntegrator.popupQueue--;
                window.EnderecoIntegrator.enderecoPopupQueue--;
                ExtendableObject.onAfterAddressCheckNoAction.forEach(function (cb) {
                  cb(ExtendableObject);
                });
                ExtendableObject.onAfterAddressCheck.forEach(function (cb) {
                  cb(ExtendableObject);
                });

                // Call global submit resume function.
                ExtendableObject.waitForAllEnderecoPopupsToClose().then(function () {
                  if (window.EnderecoIntegrator && window.EnderecoIntegrator.submitResume) {
                    ExtendableObject.beforeSubmitResume();
                    window.EnderecoIntegrator.submitResume();
                  }
                }).catch();
                return;
              }

              // Render popup with corrections.
              if (ExtendableObject.addressStatus.includes('address_multiple_variants') || ExtendableObject.addressStatus.includes('address_needs_correction') && 0 < ExtendableObject.addressPredictions.length && !(ExtendableObject.addressStatus.includes('building_number_is_missing') || ExtendableObject.addressStatus.includes('building_number_not_found') || ExtendableObject.addressStatus.includes('address_minor_correction') && 2 === ExtendableObject._addressCheckRequestIndex)) {
                // Popup needed.
                var startingIndex = -1;
                // Prepare main address.
                var mainAddressHtml = $self.util.formatAddress(ExtendableObject._checkedAddress);
                var firstPrediction = $self.util.formatAddress($self.addressPredictions[0]);
                var mainAddressDiffHtml = '';

                // Calculate main address diff.
                diff = diffWords(mainAddressHtml, firstPrediction, {
                  ignoreCase: false
                });
                diff.forEach(function (part) {
                  var markClass = part.added ? 'endereco-span--add' : part.removed ? 'endereco-span--remove' : 'endereco-span--neutral';
                  mainAddressDiffHtml += '<span class="' + markClass + '">' + part.value + '</span>';
                });

                // Prepare predictions.
                var addressPredictions = [];
                $self.addressPredictions.forEach(function (address) {
                  var addressFormatted = $self.util.formatAddress(address);
                  var addressDiff = '';
                  var diff = diffWords(mainAddressHtml, addressFormatted, {
                    ignoreCase: false
                  });
                  diff.forEach(function (part) {
                    var markClass = part.added ? 'endereco-span--add' : part.removed ? 'endereco-span--remove' : 'endereco-span--neutral';
                    addressDiff += '<span class="' + markClass + '">' + part.value + '</span>';
                  });
                  addressPredictions.push({
                    addressDiff: addressDiff
                  });
                });

                // Render wrapper.
                var indexCounter = 0;
                var useButtonHTML = $self.config.templates.button.replace('{{{buttonClasses}}}', $self.config.templates.primaryButtonClasses);
                var predictionsWrapperHtml = ExtendableObject.util.Mustache.render($self.config.templates.addressPredictionsPopupWrapper.replace('{{{button}}}', useButtonHTML), {
                  EnderecoAddressObject: $self,
                  direction: getComputedStyle(document.querySelector('body')).direction,
                  predictions: addressPredictions,
                  mainAddress: mainAddressDiffHtml,
                  showClose: ExtendableObject.config.ux.allowCloseModal,
                  showConfirCheckbox: ExtendableObject.config.ux.confirmWithCheckbox,
                  button: $self.config.templates.button,
                  title: $self.config.texts.popupHeadlines[$self.addressType],
                  index: function index() {
                    return indexCounter;
                  },
                  loopUp: function loopUp() {
                    indexCounter++;
                    return '';
                  }
                });
                document.querySelector('body').insertAdjacentHTML('beforeend', predictionsWrapperHtml);
                document.querySelector('body').classList.add('endereco-no-scroll');
                ExtendableObject.onAfterModalRendered.forEach(function (cb) {
                  cb(ExtendableObject);
                });

                // Subscribe to events.
                document.querySelectorAll('[endereco-modal-close]').forEach(function (DOMElement) {
                  DOMElement.addEventListener('click', function (e) {
                    e.preventDefault();
                    e.stopPropagation();
                    $self.util.removePopup();
                    window.EnderecoIntegrator.submitResume = undefined;
                    window.EnderecoIntegrator.hasSubmit = false;
                  });
                });
                document.querySelectorAll('[endereco-edit-address]').forEach(function (DOMElement) {
                  DOMElement.addEventListener('click', function (e) {
                    e.preventDefault();
                    e.stopPropagation();
                    $self.waitUntilReady().then(function () {
                      $self.onEditAddress.forEach(function (cb) {
                        cb($self);
                      });
                      $self.waitUntilReady().then(function () {
                        $self.util.removePopup();
                      });
                    }).catch();
                    window.EnderecoIntegrator.submitResume = undefined;
                    window.EnderecoIntegrator.hasSubmit = false;
                  });
                });
                document.querySelectorAll('[endereco-use-selection]').forEach(function (DOMElement) {
                  DOMElement.addEventListener('click', function (e) {
                    e.preventDefault();
                    e.stopPropagation();
                    $self.cb.copyAddressFromPrediction();
                    $self.waitUntilReady().then(function () {
                      $self.onAfterAddressCheckSelected.forEach(function (cb) {
                        cb($self);
                      });
                      $self.waitUntilReady().then(function () {
                        $self.util.removePopup();
                      });
                    }).catch();
                    $self.waitForAllEnderecoPopupsToClose().then(function () {
                      $self.waitUntilReady().then(function () {
                        if (window.EnderecoIntegrator && window.EnderecoIntegrator.submitResume) {
                          ExtendableObject.beforeSubmitResume();
                          window.EnderecoIntegrator.submitResume();
                        }
                      }).catch(function () {
                        if (window.EnderecoIntegrator && window.EnderecoIntegrator.submitResume) {
                          ExtendableObject.beforeSubmitResume();
                          window.EnderecoIntegrator.submitResume();
                        }
                      });
                    }).catch();
                  });
                });
                document.querySelectorAll('[name="endereco-address-predictions"]').forEach(function (DOMElement) {
                  $self.addSubscriber(new modules_subscriber('addressPredictionsIndex', DOMElement, {
                    syncValue: true
                  }));
                });
                if (ExtendableObject.config.ux.confirmWithCheckbox) {
                  document.querySelectorAll('[endereco-confirm-address-checkbox]').forEach(function (DOMElement) {
                    DOMElement.addEventListener('change', function (e) {
                      e.preventDefault();
                      e.stopPropagation();
                      var $isChecked = e.target.checked;
                      e.target.closest('.endereco-modal').querySelectorAll('[endereco-disabled-until-confirmed]').forEach(function (disableableDOM) {
                        if ($isChecked || 0 <= ExtendableObject.addressPredictionsIndex) {
                          disableableDOM.disabled = false;
                        } else {
                          disableableDOM.disabled = true;
                        }
                      });
                      // Find container
                    });
                    var $isChecked = DOMElement.checked;
                    DOMElement.closest('.endereco-modal').querySelectorAll('[endereco-disabled-until-confirmed]').forEach(function (disableableDOM) {
                      if ($isChecked || 0 <= ExtendableObject.addressPredictionsIndex) {
                        disableableDOM.disabled = false;
                      } else {
                        disableableDOM.disabled = true;
                      }
                    });
                  });
                }
                document.querySelectorAll('[name="endereco-address-predictions"]').forEach(function (DOMElement) {
                  DOMElement.addEventListener('change', function (e) {
                    e.preventDefault();
                    e.stopPropagation();
                    var $modal = e.target.closest('.endereco-modal');
                    var $value = parseInt(e.target.value);
                    if (0 <= $value) {
                      $modal.querySelectorAll('[endereco-show-if-origin]').forEach(function (element) {
                        element.style.display = 'none';
                      });
                    } else {
                      $modal.querySelectorAll('[endereco-show-if-origin]').forEach(function (element) {
                        element.style.display = 'block';
                      });
                    }
                    if (ExtendableObject.config.ux.confirmWithCheckbox) {
                      var $isChecked = $modal.querySelector('[endereco-confirm-address-checkbox]').checked;
                      e.target.closest('.endereco-modal').querySelectorAll('[endereco-disabled-until-confirmed]').forEach(function (disableableDOM) {
                        if ($isChecked || 0 <= $value) {
                          disableableDOM.disabled = false;
                        } else {
                          disableableDOM.disabled = true;
                        }
                      });
                    }

                    // Find container
                  });
                  if (0 <= ExtendableObject.addressPredictionsIndex) {
                    DOMElement.closest('.endereco-modal').querySelectorAll('[endereco-show-if-origin]').forEach(function (element) {
                      element.style.display = 'none';
                    });
                  } else {
                    DOMElement.closest('.endereco-modal').querySelectorAll('[endereco-show-if-origin]').forEach(function (element) {
                      element.style.display = 'block';
                    });
                  }
                  if (ExtendableObject.config.ux.confirmWithCheckbox) {
                    var $isChecked = DOMElement.closest('.endereco-modal').querySelector('[endereco-confirm-address-checkbox]').checked;
                    DOMElement.closest('.endereco-modal').querySelectorAll('[endereco-disabled-until-confirmed]').forEach(function (disableableDOM) {
                      if ($isChecked || 0 <= ExtendableObject.addressPredictionsIndex) {
                        disableableDOM.disabled = false;
                      } else {
                        disableableDOM.disabled = true;
                      }
                    });
                  }
                });
                return;
              }

              // Render popup for address correction without predictions.
              if (ExtendableObject.addressStatus.includes('address_minor_correction') && 2 === ExtendableObject._addressCheckRequestIndex && 0 < ExtendableObject.addressPredictions.length) {
                // No poup needed.
                window.EnderecoIntegrator.popupQueue--;
                window.EnderecoIntegrator.enderecoPopupQueue--;
                ExtendableObject._awaits++;
                var addressData = $self.addressPredictions[0];
                addressData.countryCode = addressData.countryCode.toUpperCase();
                $self.fieldNames.forEach(function (fieldName) {
                  if (undefined !== $self[fieldName] && undefined !== addressData[fieldName] && $self[fieldName] !== addressData[fieldName]) {
                    $self[fieldName] = addressData[fieldName];
                  }
                });
                $self.addressStatus = ['address_correct', 'address_selected_automatically'];
                $self.addressPredictions = [];
                $self.waitUntilReady().then(function () {
                  $self.onAfterAddressCheckSelected.forEach(function (cb) {
                    cb($self);
                  });
                }).catch();
                ExtendableObject._awaits--;
                return;
              }

              // Render popup for address correction without predictions.
              if (ExtendableObject.addressStatus.includes('address_needs_correction') && (ExtendableObject.addressStatus.includes('building_number_is_missing') || ExtendableObject.addressStatus.includes('building_number_not_found') || 0 === ExtendableObject.addressPredictions.length)) {
                // Prepare main address.
                // TODO: replace button then replace button classes.
                var mainAddressHtml = $self.util.formatAddress(ExtendableObject._checkedAddress, true, true);
                var editButtonHTML = $self.config.templates.buttonEditAddress.replace('{{{buttonClasses}}}', $self.config.templates.primaryButtonClasses);
                var confirmButtonHTML = $self.config.templates.buttonConfirmAddress.replace('{{{buttonClasses}}}', $self.config.templates.secondaryButtonClasses);
                var errors = [];
                if (ExtendableObject.addressStatus.includes('building_number_is_missing')) {
                  if (!!window.EnderecoIntegrator.config.texts.statuses.building_number_is_missing) {
                    errors.push(window.EnderecoIntegrator.config.texts.statuses.building_number_is_missing);
                  } else {
                    errors.push('Hausnummer fehlt.');
                  }
                }
                if (ExtendableObject.addressStatus.includes('building_number_not_found')) {
                  if (!!window.EnderecoIntegrator.config.texts.statuses.building_number_not_found) {
                    errors.push(window.EnderecoIntegrator.config.texts.statuses.building_number_not_found);
                  } else {
                    errors.push('Hausnummer konnte nicht verifiziert werden. Prüfen Sie die Schreibweise und ob sie im richtigen Feld eingetragen ist.');
                  }
                  errors.push();
                }
                if (ExtendableObject.addressStatus.includes('street_name_needs_correction')) {
                  if (!!window.EnderecoIntegrator.config.texts.statuses.street_name_needs_correction) {
                    errors.push(window.EnderecoIntegrator.config.texts.statuses.street_name_needs_correction);
                  } else {
                    errors.push('Schreibfehler bei der Stra&szlig;e. Nutzen Sie bitte die amtliche Schreibweise.');
                  }
                }
                if (ExtendableObject.addressStatus.includes('locality_needs_correction')) {
                  if (!!window.EnderecoIntegrator.config.texts.statuses.locality_needs_correction) {
                    errors.push(window.EnderecoIntegrator.config.texts.statuses.locality_needs_correction);
                  } else {
                    errors.push('Schreibfehler bei dem Ort. Nutzen Sie bitte die amtliche Schreibweise.');
                  }
                }
                if (ExtendableObject.addressStatus.includes('postal_code_needs_correction')) {
                  if (!!window.EnderecoIntegrator.config.texts.statuses.postal_code_needs_correction) {
                    errors.push(window.EnderecoIntegrator.config.texts.statuses.postal_code_needs_correction);
                  } else {
                    errors.push('Schreibfehler bei der Postleitzahl. Achten Sie auf Vertipper.');
                  }
                }
                if (ExtendableObject.addressStatus.includes('country_code_needs_correction')) {
                  if (!!window.EnderecoIntegrator.config.texts.statuses.country_code_needs_correction) {
                    errors.push(window.EnderecoIntegrator.config.texts.statuses.country_code_needs_correction);
                  } else {
                    errors.push('Falsches Land. Wählen Sie bitte das richtige Land aus.');
                  }
                }
                var modalHTML = ExtendableObject.util.Mustache.render($self.config.templates.addressNoPredictionWrapper.replace('{{{button}}}', editButtonHTML).replace('{{{buttonSecondary}}}', confirmButtonHTML), {
                  EnderecoAddressObject: $self,
                  direction: getComputedStyle(document.querySelector('body')).direction,
                  modalClasses: ExtendableObject.addressStatus.join(' '),
                  showClose: ExtendableObject.config.ux.allowCloseModal,
                  hasErrors: 0 < errors.length,
                  errors: errors,
                  showConfirCheckbox: ExtendableObject.config.ux.confirmWithCheckbox,
                  mainAddress: mainAddressHtml,
                  button: $self.config.templates.button,
                  title: $self.config.texts.popupHeadlines[$self.addressType]
                });
                document.querySelector('body').insertAdjacentHTML('beforeend', modalHTML);
                document.querySelector('body').classList.add('endereco-no-scroll');
                ExtendableObject.onAfterModalRendered.forEach(function (cb) {
                  cb(ExtendableObject);
                });

                // Subscribe to events.
                document.querySelectorAll('[endereco-modal-close]').forEach(function (DOMElement) {
                  DOMElement.addEventListener('click', function (e) {
                    e.preventDefault();
                    e.stopPropagation();
                    $self.util.removePopup();
                    window.EnderecoIntegrator.submitResume = undefined;
                    window.EnderecoIntegrator.hasSubmit = false;
                  });
                });
                document.querySelectorAll('[endereco-edit-address]').forEach(function (DOMElement) {
                  DOMElement.addEventListener('click', function (e) {
                    e.preventDefault();
                    e.stopPropagation();
                    window.EnderecoIntegrator.submitResume = undefined;
                    window.EnderecoIntegrator.hasSubmit = false;
                    $self.waitUntilReady().then(function () {
                      $self.onEditAddress.forEach(function (cb) {
                        cb($self);
                      });
                      $self.waitUntilReady().then(function () {
                        $self.util.removePopup();
                      });
                    }).catch();
                  });
                });
                document.querySelectorAll('[endereco-confirm-address]').forEach(function (DOMElement) {
                  DOMElement.addEventListener('click', function (e) {
                    e.preventDefault();
                    e.stopPropagation();
                    var statusTemp = JSON.parse(JSON.stringify(ExtendableObject.addressStatus));
                    statusTemp.push('address_selected_by_customer');
                    ExtendableObject.addressStatus = statusTemp;
                    $self.waitUntilReady().then(function () {
                      $self.onConfirmAddress.forEach(function (cb) {
                        cb($self);
                      });
                      $self.waitUntilReady().then(function () {
                        $self.util.removePopup();
                      });
                    }).catch();
                    $self.waitForAllEnderecoPopupsToClose().then(function () {
                      $self.waitUntilReady().then(function () {
                        if (window.EnderecoIntegrator && window.EnderecoIntegrator.submitResume) {
                          ExtendableObject.beforeSubmitResume();
                          window.EnderecoIntegrator.submitResume();
                        }
                      }).catch(function () {
                        if (window.EnderecoIntegrator && window.EnderecoIntegrator.submitResume) {
                          ExtendableObject.beforeSubmitResume();
                          window.EnderecoIntegrator.submitResume();
                        }
                      });
                    }).catch();
                  });
                });
                if (ExtendableObject.config.ux.confirmWithCheckbox) {
                  document.querySelectorAll('[endereco-confirm-address-checkbox]').forEach(function (DOMElement) {
                    DOMElement.addEventListener('change', function (e) {
                      e.preventDefault();
                      e.stopPropagation();
                      var $isChecked = e.target.checked;
                      e.target.closest('.endereco-modal').querySelectorAll('[endereco-disabled-until-confirmed]').forEach(function (disableableDOM) {
                        if ($isChecked) {
                          disableableDOM.disabled = false;
                        } else {
                          disableableDOM.disabled = true;
                        }
                      });
                      // Find container
                    });
                    var $isChecked = DOMElement.checked;
                    DOMElement.closest('.endereco-modal').querySelectorAll('[endereco-disabled-until-confirmed]').forEach(function (disableableDOM) {
                      if ($isChecked) {
                        disableableDOM.disabled = false;
                      } else {
                        disableableDOM.disabled = true;
                      }
                    });
                  });
                }
                return;
              }

              // Render popup for not found address.
              if (ExtendableObject.addressStatus.includes('address_not_found') && 0 === ExtendableObject.addressPredictions.length) {
                // Prepare main address.
                // TODO: replace button then replace button classes.
                var mainAddressHtml = $self.util.formatAddress(ExtendableObject._checkedAddress, true);
                var editButtonHTML = $self.config.templates.buttonEditAddress.replace('{{{buttonClasses}}}', $self.config.templates.primaryButtonClasses);
                var confirmButtonHTML = $self.config.templates.buttonConfirmAddress.replace('{{{buttonClasses}}}', $self.config.templates.secondaryButtonClasses);
                var modalHTML = ExtendableObject.util.Mustache.render($self.config.templates.addressNotFoundPopupWrapper.replace('{{{button}}}', editButtonHTML).replace('{{{buttonSecondary}}}', confirmButtonHTML), {
                  EnderecoAddressObject: $self,
                  direction: getComputedStyle(document.querySelector('body')).direction,
                  mainAddress: mainAddressHtml,
                  showClose: ExtendableObject.config.ux.allowCloseModal,
                  showConfirCheckbox: ExtendableObject.config.ux.confirmWithCheckbox,
                  button: $self.config.templates.button,
                  title: $self.config.texts.popupHeadlines[$self.addressType]
                });
                document.querySelector('body').insertAdjacentHTML('beforeend', modalHTML);
                document.querySelector('body').classList.add('endereco-no-scroll');
                ExtendableObject.onAfterModalRendered.forEach(function (cb) {
                  cb(ExtendableObject);
                });

                // Subscribe to events.
                document.querySelectorAll('[endereco-modal-close]').forEach(function (DOMElement) {
                  DOMElement.addEventListener('click', function (e) {
                    e.preventDefault();
                    e.stopPropagation();
                    $self.util.removePopup();
                    window.EnderecoIntegrator.submitResume = undefined;
                    window.EnderecoIntegrator.hasSubmit = false;
                  });
                });
                document.querySelectorAll('[endereco-edit-address]').forEach(function (DOMElement) {
                  DOMElement.addEventListener('click', function (e) {
                    e.preventDefault();
                    e.stopPropagation();
                    window.EnderecoIntegrator.submitResume = undefined;
                    window.EnderecoIntegrator.hasSubmit = false;
                    $self.waitUntilReady().then(function () {
                      $self.onEditAddress.forEach(function (cb) {
                        cb($self);
                      });
                      $self.waitUntilReady().then(function () {
                        $self.util.removePopup();
                      });
                    }).catch();
                  });
                });
                document.querySelectorAll('[endereco-confirm-address]').forEach(function (DOMElement) {
                  DOMElement.addEventListener('click', function (e) {
                    e.preventDefault();
                    e.stopPropagation();
                    ExtendableObject.addressStatus = ['address_not_found', 'address_selected_by_customer'];
                    $self.waitUntilReady().then(function () {
                      $self.onConfirmAddress.forEach(function (cb) {
                        cb($self);
                      });
                      $self.waitUntilReady().then(function () {
                        $self.util.removePopup();
                      });
                    }).catch();
                    $self.waitForAllEnderecoPopupsToClose().then(function () {
                      $self.waitUntilReady().then(function () {
                        if (window.EnderecoIntegrator && window.EnderecoIntegrator.submitResume) {
                          ExtendableObject.beforeSubmitResume();
                          window.EnderecoIntegrator.submitResume();
                        }
                      }).catch(function () {
                        if (window.EnderecoIntegrator && window.EnderecoIntegrator.submitResume) {
                          ExtendableObject.beforeSubmitResume();
                          window.EnderecoIntegrator.submitResume();
                        }
                      });
                    }).catch();
                  });
                });
                if (ExtendableObject.config.ux.confirmWithCheckbox) {
                  document.querySelectorAll('[endereco-confirm-address-checkbox]').forEach(function (DOMElement) {
                    DOMElement.addEventListener('change', function (e) {
                      e.preventDefault();
                      e.stopPropagation();
                      var $isChecked = e.target.checked;
                      e.target.closest('.endereco-modal').querySelectorAll('[endereco-disabled-until-confirmed]').forEach(function (disableableDOM) {
                        if ($isChecked) {
                          disableableDOM.disabled = false;
                        } else {
                          disableableDOM.disabled = true;
                        }
                      });
                      // Find container
                    });
                    var $isChecked = DOMElement.checked;
                    DOMElement.closest('.endereco-modal').querySelectorAll('[endereco-disabled-until-confirmed]').forEach(function (disableableDOM) {
                      if ($isChecked) {
                        disableableDOM.disabled = false;
                      } else {
                        disableableDOM.disabled = true;
                      }
                    });
                  });
                }
                return;
              }
            }).catch();
          }).catch();
        };
        ExtendableObject.util.removePopup = function () {
          if (document.querySelector('[endereco-popup]')) {
            document.querySelector('[endereco-popup]').parentNode.removeChild(document.querySelector('[endereco-popup]'));
            document.querySelector('body').classList.remove('endereco-no-scroll');
            ExtendableObject.addressPredictionsIndex = 0;
            window.EnderecoIntegrator.popupQueue--;
            window.EnderecoIntegrator.enderecoPopupQueue--;
            if (!!ExtendableObject.modalClosed) {
              ExtendableObject.modalClosed();
            }
          }
        };
        ExtendableObject.util.calculateDependingStatuses = function () {
          var value = ExtendableObject.addressStatus;
          // Calculate detailed codes for fieldnames.
          if (ExtendableObject.hasLoadedExtension('CountryCodeCheckExtension')) {
            var countryCodeStatus = [];
            if (value.includes('address_correct') && ExtendableObject.countryCode) {
              countryCodeStatus.push('country_code_correct');
            }
            if (value.includes('address_needs_correction') && ExtendableObject.countryCode) {
              if (value.includes('country_code_correct')) {
                countryCodeStatus.push('country_code_correct');
              } else if (value.includes('country_code_needs_correction')) {
                countryCodeStatus.push('country_code_needs_correction');
              } else if (0 < ExtendableObject.addressPredictions.length && ExtendableObject.addressPredictions[0].countryCode.toUpperCase() !== ExtendableObject.countryCode.toUpperCase()) {
                countryCodeStatus.push('country_code_needs_correction');
              } else {
                countryCodeStatus.push('country_code_correct');
              }
            }
            if (value.includes('address_multiple_variants') && ExtendableObject.countryCode) {
              countryCodeStatus.push('address_multiple_variants');
            }
            if (value.includes('address_not_found') && ExtendableObject.countryCode) {
              countryCodeStatus.push('address_not_found');
            }
            ExtendableObject.waitForActive().then(function () {
              ExtendableObject.countryCodeStatus = countryCodeStatus;
            }).catch();
          }
          if (ExtendableObject.hasLoadedExtension('SubdivisionCodeExtension')) {
            var subdivisionCodes = [];
            var subdivisionRelevant = false;
            if (0 < ExtendableObject._subscribers.subdivisionCode.length) {
              ExtendableObject._subscribers.subdivisionCode.forEach(function (listener) {
                if (!listener.object.disabled && listener.object.isConnected) {
                  subdivisionRelevant = true;
                }
              });
            }
            if (subdivisionRelevant) {
              if (value.includes('subdivision_code_correct') || value.includes('address_correct')) {
                subdivisionCodes.push('subdivision_code_correct');
              } else if (value.includes('subdivision_code_needs_correction')) {
                subdivisionCodes.push('subdivision_code_needs_correction');
              } else if (value.includes('address_not_found')) {
                subdivisionCodes.push('address_not_found');
              } else if (value.includes('address_multiple_variants')) {
                subdivisionCodes.push('address_multiple_variants');
              } else {
                subdivisionCodes = [];
              }
              ExtendableObject.waitForActive().then(function () {
                ExtendableObject.subdivisionCodeStatus = subdivisionCodes;
              }).catch();
            }
          }
          if (ExtendableObject.hasLoadedExtension('PostalCodeExtension')) {
            var postalCodeStatus = [];
            if (value.includes('postal_code_correct') || value.includes('address_correct')) {
              postalCodeStatus.push('postal_code_correct');
            }
            if (value.includes('postal_code_needs_correction')) {
              postalCodeStatus.push('postal_code_needs_correction');
            }
            if (value.includes('address_multiple_variants')) {
              postalCodeStatus.push('address_multiple_variants');
            }
            if (value.includes('address_not_found')) {
              postalCodeStatus.push('address_not_found');
            }
            ExtendableObject.waitForActive().then(function () {
              ExtendableObject.postalCodeStatus = postalCodeStatus;
            }).catch();
          }
          if (ExtendableObject.hasLoadedExtension('LocalityExtension')) {
            var localityStatus = [];
            if (value.includes('locality_correct') || value.includes('address_correct')) {
              localityStatus.push('locality_correct');
            }
            if (value.includes('locality_needs_correction')) {
              localityStatus.push('locality_needs_correction');
            }
            if (value.includes('address_multiple_variants')) {
              localityStatus.push('address_multiple_variants');
            }
            if (value.includes('address_not_found')) {
              localityStatus.push('address_not_found');
            }
            ExtendableObject.waitForActive().then(function () {
              ExtendableObject.localityStatus = localityStatus;
            }).catch();
          }
          if (ExtendableObject.hasLoadedExtension('StreetNameExtension')) {
            var streetNameStatus = [];
            if (value.includes('street_name_correct') || value.includes('address_correct')) {
              streetNameStatus.push('street_name_correct');
            }
            if (value.includes('street_name_needs_correction')) {
              streetNameStatus.push('street_name_needs_correction');
            }
            if (value.includes('address_multiple_variants')) {
              streetNameStatus.push('address_multiple_variants');
            }
            if (value.includes('address_not_found')) {
              streetNameStatus.push('address_not_found');
            }
            ExtendableObject.waitForActive().then(function () {
              ExtendableObject.streetNameStatus = streetNameStatus;
            }).catch();
          }
          if (ExtendableObject.hasLoadedExtension('StreetFullExtension')) {
            var streetFullStatus = [];
            if (value.includes('street_full_correct') || value.includes('address_correct')) {
              streetFullStatus.push('street_full_correct');
            }
            if (value.includes('address_needs_correction')) {
              if (value.includes('street_name_needs_correction') || value.includes('building_number_needs_correction')) {
                streetFullStatus.push('street_full_needs_correction');
              } else {
                streetFullStatus.push('street_full_correct');
              }
            }
            if (value.includes('address_multiple_variants')) {
              streetFullStatus.push('address_multiple_variants');
            }
            if (value.includes('address_not_found')) {
              streetFullStatus.push('address_not_found');
            }
            ExtendableObject.waitForActive().then(function () {
              ExtendableObject.streetFullStatus = streetFullStatus;
            }).catch();
          }
          if (ExtendableObject.hasLoadedExtension('BuildingNumberExtension')) {
            var buildingNumberStatus = [];
            if (value.includes('building_number_correct') || value.includes('address_correct')) {
              buildingNumberStatus.push('building_number_correct');
            }
            if (value.includes('building_number_needs_correction')) {
              buildingNumberStatus.push('building_number_needs_correction');
            }
            if (value.includes('building_number_not_found')) {
              buildingNumberStatus.push('building_number_not_found');
            }
            if (value.includes('building_number_missing')) {
              buildingNumberStatus.push('building_number_missing');
            }
            if (value.includes('address_multiple_variants')) {
              buildingNumberStatus.push('address_multiple_variants');
            }
            if (value.includes('address_not_found')) {
              buildingNumberStatus.push('address_not_found');
            }
            ExtendableObject.waitForActive().then(function () {
              ExtendableObject.buildingNumberStatus = buildingNumberStatus;
            }).catch();
          }
          if (ExtendableObject.hasLoadedExtension('AdditionalInfoExtension')) {
            var additionalInfoStatus = [];
            if (value.includes('address_correct') && ExtendableObject.additionalInfo) {
              additionalInfoStatus.push('additional_info_correct');
            }
            if (value.includes('address_needs_correction') && ExtendableObject.additionalInfo) {
              if (0 < ExtendableObject.addressPredictions.length && ExtendableObject.addressPredictions[0].additionalInfo !== ExtendableObject.additionalInfo) {
                additionalInfoStatus.push('additional_info_needs_correction');
              } else {
                additionalInfoStatus.push('additional_info_correct');
              }
            }
            if (value.includes('address_multiple_variants') && ExtendableObject.additionalInfo) {
              additionalInfoStatus.push('address_multiple_variants');
            }
            if (value.includes('address_not_found') && ExtendableObject.additionalInfo) {
              additionalInfoStatus.push('address_not_found');
            }
            ExtendableObject.waitForActive().then(function () {
              ExtendableObject.additionalInfoStatus = additionalInfoStatus;
            }).catch();
          }
        };

        // Add change event hadler.
        ExtendableObject.cb.addressStatusChange = function (subscriber) {
          return function (e) {
            ExtendableObject.addressStatus = subscriber.value;
          };
        };
        ExtendableObject.cb.addressPredictionsIndexChange = function (subscriber) {
          return function (e) {
            ExtendableObject.addressPredictionsIndex = subscriber.value;
          };
        };
        ExtendableObject.cb.copyAddressFromPrediction = function () {
          var index = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : null;
          ExtendableObject._awaits++;
          var $self = ExtendableObject;
          if (null === index) {
            index = $self.addressPredictionsIndex;
          }
          if (-1 === parseInt(index)) {
            if (!$self.addressStatus.includes('address_selected_by_customer')) {
              var temp = $self.addressStatus.slice();
              if (!$self.addressStatus.includes('address_selected_by_customer')) {
                temp.push('address_selected_by_customer');
                $self.addressStatus = temp;
              }
            }
            ExtendableObject._awaits--;
            return; // Dont copy, -1 is current values.
          }
          var addressData = $self.addressPredictions[index];
          addressData.countryCode = addressData.countryCode.toUpperCase();
          $self.fieldNames.forEach(function (fieldName) {
            if (undefined !== $self[fieldName] && undefined !== addressData[fieldName] && $self[fieldName] !== addressData[fieldName]) {
              $self[fieldName] = addressData[fieldName];
            }
          });
          $self.addressStatus = ['address_correct', 'address_selected_by_customer'];
          ExtendableObject._awaits--;
        };

        // Add the "addressStatus" property
        Object.defineProperty(ExtendableObject, 'addressStatus', {
          get: function get() {
            return this._addressStatus;
          },
          set: function set(value) {
            var oldValue = ExtendableObject._addressStatus;
            ExtendableObject._awaits++;
            ExtendableObject.util.Promise.resolve(value).then(function (value) {
              var newValue = [];
              if (!Array.isArray(value)) {
                value = value.split(',');
              }

              // Transform statuscodes.
              if (value.includes('A1000') && !value.includes('A1100')) {
                if (!value.includes('address_correct')) {
                  value.push('address_correct');
                }
              }
              if (value.includes('A1000') && value.includes('A1100')) {
                if (!value.includes('address_needs_correction')) {
                  value.push('address_needs_correction');
                }
              }
              if (value.includes('A2000')) {
                if (!value.includes('address_multiple_variants')) {
                  value.push('address_multiple_variants');
                }
              }
              if (value.includes('A3000')) {
                if (!value.includes('address_not_found')) {
                  value.push('address_not_found');
                }
              }
              if (0 === value.length) {
                value = ['address_not_checked'];
              }
              if (!ExtendableObject.util.isEqual(oldValue, value)) {
                ExtendableObject._addressStatus = value;

                // Inform all subscribers about the change.
                ExtendableObject._subscribers.addressStatus.forEach(function (subscriber) {
                  subscriber.value = value;
                });
                ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                  detail: {
                    fieldName: 'addressStatus',
                    oldValue: oldValue,
                    newValue: value,
                    object: ExtendableObject
                  }
                }));
                ExtendableObject.waitUntilReady().then(function () {
                  ExtendableObject.util.calculateDependingStatuses();
                }).catch();
              }
            }).catch().finally(function () {
              ExtendableObject._awaits--;
            });
          }
        });

        // Add getter and setter for fields.
        Object.defineProperty(ExtendableObject, 'addressPredictions', {
          get: function get() {
            return ExtendableObject._addressPredictions;
          },
          set: function set(value) {
            var oldValue = ExtendableObject._addressPredictions;
            ExtendableObject._awaits++;
            ExtendableObject.util.Promise.resolve(value).then(function (value) {
              var newValue = value;
              if (!ExtendableObject.util.isEqual(oldValue, newValue)) {
                ExtendableObject._addressPredictions = newValue;

                // Inform all subscribers about the change.
                ExtendableObject._subscribers.addressPredictions.forEach(function (subscriber) {
                  subscriber.value = value;
                });
                ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                  detail: {
                    fieldName: 'addressPredictions',
                    oldValue: oldValue,
                    newValue: newValue,
                    object: ExtendableObject
                  }
                }));
              }
            }).catch().finally(function () {
              ExtendableObject._awaits--;
            });
          }
        });
        Object.defineProperty(ExtendableObject, 'addressType', {
          get: function get() {
            return this._addressType;
          },
          set: function set(value) {
            var oldValue = ExtendableObject._addressType;
            ExtendableObject._awaits++;
            ExtendableObject.util.Promise.resolve(value).then(function (value) {
              if (!ExtendableObject.util.isEqual(oldValue, value)) {
                ExtendableObject._addressType = value;
                ExtendableObject._changed = false;

                // Inform all subscribers about the change.
                ExtendableObject._subscribers.addressType.forEach(function (subscriber) {
                  subscriber.value = value;
                });
                ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                  detail: {
                    fieldName: 'addressType',
                    oldValue: oldValue,
                    newValue: value,
                    object: ExtendableObject
                  }
                }));
                ExtendableObject.addressPredictions = [];
                ExtendableObject.addressStatus = [];
              }
            }).catch().finally(function () {
              ExtendableObject._awaits--;
            });
          }
        });

        // Add getter and setter for fields.
        Object.defineProperty(ExtendableObject, 'addressTimestamp', {
          get: function get() {
            return ExtendableObject._addressTimestamp;
          },
          set: function set(value) {
            var oldValue = ExtendableObject._addressTimestamp;
            ExtendableObject._awaits++;
            ExtendableObject.util.Promise.resolve(value).then(function (value) {
              var newValue = value;
              if (!ExtendableObject.util.isEqual(oldValue, newValue)) {
                ExtendableObject._addressTimestamp = newValue;

                // Inform all subscribers about the change.
                ExtendableObject._subscribers.addressTimestamp.forEach(function (subscriber) {
                  subscriber.value = value;
                });
                ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                  detail: {
                    fieldName: 'addressTimestamp',
                    oldValue: oldValue,
                    newValue: newValue,
                    object: ExtendableObject
                  }
                }));
              }
            }).catch().finally(function () {
              ExtendableObject._awaits--;
            });
          }
        });

        // Add getter and setter for fields.
        Object.defineProperty(ExtendableObject, 'addressPredictionsIndex', {
          get: function get() {
            return this._addressPredictionsIndex;
          },
          set: function set(value) {
            ExtendableObject._awaits++;
            var oldValue = ExtendableObject._addressPredictionsIndex;
            ExtendableObject.util.Promise.resolve(value).then(function (value) {
              var newValue = parseInt(value);
              if (!ExtendableObject.util.isEqual(oldValue, newValue)) {
                ExtendableObject._addressPredictionsIndex = newValue;

                // Inform all subscribers about the change.
                ExtendableObject._subscribers.addressPredictionsIndex.forEach(function (subscriber) {
                  subscriber.value = value;
                });
                ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                  detail: {
                    fieldName: 'addressPredictionsIndex',
                    oldValue: oldValue,
                    newValue: newValue,
                    object: ExtendableObject
                  }
                }));
              }
            }).catch().finally(function () {
              ExtendableObject._awaits--;
            });
          }
        });
        ExtendableObject.util.checkAddress = function () {
          var address = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : null;
          var render = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : true;
          if (!address) {
            address = ExtendableObject.address;
          }
          ExtendableObject._addressIsBeingChecked = true;
          return new ExtendableObject.util.Promise(function (resolve, reject) {
            ExtendableObject.waitUntilReady().then(function () {
              ExtendableObject._addressCheckRequestIndex++;
              var addressCheckRequestIndex = ExtendableObject._addressCheckRequestIndex * 1;
              ExtendableObject._checkedAddress = JSON.parse(JSON.stringify(ExtendableObject.address));
              var message = {
                'jsonrpc': '2.0',
                'id': ExtendableObject._addressCheckRequestIndex,
                'method': 'addressCheck',
                'params': {
                  'country': address.countryCode,
                  'language': ExtendableObject.config.lang,
                  'postCode': address.postalCode,
                  'cityName': address.locality
                }
              };

              // Check if streetName is set and has a value
              if (address.streetName && address.streetName.trim() !== '') {
                // Add streetName and buildingNumber to message.params
                message.params.street = address.streetName;
                message.params.houseNumber = address.buildingNumber;
              } else {
                // If streetName is not set or is empty, use streetFull instead
                message.params.streetFull = address.streetFull;
              }
              if (0 < ExtendableObject._subscribers.subdivisionCode.length) {
                ExtendableObject._subscribers.subdivisionCode.forEach(function (listener) {
                  if (!listener.object.disabled && listener.object.isConnected) {
                    message.params.subdivisionCode = address.subdivisionCode;
                  }
                });
              }
              if (0 < ExtendableObject._subscribers.additionalInfo.length) {
                ExtendableObject._subscribers.additionalInfo.forEach(function (listener) {
                  if (!listener.object.disabled && listener.object.isConnected) {
                    message.params.additionalInfo = address.additionalInfo;
                  }
                });
              }
              ExtendableObject._awaits++;
              ExtendableObject.util.axios.post(ExtendableObject.config.apiUrl, message, {
                timeout: ExtendableObject.config.ux.requestTimeout,
                headers: {
                  'X-Auth-Key': ExtendableObject.config.apiKey,
                  'X-Agent': ExtendableObject.config.agentName,
                  'X-Remote-Api-Url': ExtendableObject.config.remoteApiUrl,
                  'X-Transaction-Referer': window.location.href,
                  'X-Transaction-Id': ExtendableObject.hasLoadedExtension('SessionExtension') ? ExtendableObject.sessionId : 'not_required'
                }
              }).then(function (response) {
                var _response$data$error;
                var tempAddressContainer;
                if (undefined !== response.data.result && undefined !== response.data.result.predictions) {
                  var copyOfPredictions = [];
                  var addressCounter = 0;
                  if (addressCheckRequestIndex !== ExtendableObject._addressCheckRequestIndex) {
                    return;
                  }
                  if (ExtendableObject.anyMissing()) {
                    return;
                  }
                  if (ExtendableObject.areEssentialsDisabled()) {
                    return;
                  }
                  var isStillRelevant = true;
                  window.EnderecoIntegrator.amsFilters.isAddressMetaStillRelevant.some(function (callback) {
                    isStillRelevant = callback(isStillRelevant, ExtendableObject);
                    return !isStillRelevant; // If isStillRelevant is false, some() will stop iterating.
                  });
                  if (!isStillRelevant) {
                    return;
                  }

                  // If session counter is set, increase it.
                  if (ExtendableObject.hasLoadedExtension('SessionExtension')) {
                    ExtendableObject.sessionCounter++;
                  }
                  ExtendableObject.addressTimestamp = Math.floor(Date.now() / 1000);
                  response.data.result.predictions.forEach(function (address) {
                    if (addressCounter >= ExtendableObject.config.ux.maxAddressPredictionItems) {
                      return false;
                    }
                    addressCounter++;
                    tempAddressContainer = {
                      countryCode: address.country ? address.country : ExtendableObject._countryCode,
                      postalCode: address.postCode,
                      locality: address.cityName,
                      streetName: address.street,
                      buildingNumber: address.houseNumber
                    };
                    if (address.hasOwnProperty('subdivisionCode')) {
                      tempAddressContainer.subdivisionCode = address.subdivisionCode;
                    }
                    if (address.hasOwnProperty('additionalInfo')) {
                      tempAddressContainer.additionalInfo = address.additionalInfo;
                    }

                    // Filter out identical copy.
                    if (copyOfPredictions.includes(tempAddressContainer)) {
                      return false;
                    }
                    copyOfPredictions.push(tempAddressContainer);
                  });
                  ExtendableObject.addressStatus = response.data.result.status;
                  ExtendableObject.addressPredictions = copyOfPredictions;

                  // If specific status - render popup.
                  if (render) {
                    ExtendableObject.util.renderAddressPredictionsPopup();
                  }
                  resolve();
                }
                // Check for the old session id and regenerate if necessary.
                else if (((_response$data$error = response.data.error) === null || _response$data$error === void 0 ? void 0 : _response$data$error.code) === -32700 && ExtendableObject.util.updateSessionId) {
                  ExtendableObject.util.updateSessionId();
                  reject(response);
                } else {
                  reject(response);
                }
              }).catch(function (e) {
                reject(e.response);
              }).finally(function () {
                ExtendableObject._awaits--;
                ExtendableObject._changed = false;
                ExtendableObject._addressIsBeingChecked = false;
                ExtendableObject.submitUnblocked();
              });
            }).catch(function (e) {
              console.log(e);
            });
          });
        };
        if (ExtendableObject.config.showDebugInfo) {
          console.log('AddressCheckExtension applied');
        }
        ExtendableObject.states.AddressCheckExtension = {
          active: true
        };
        resolve($self);
      }).catch(function () {
        console.log('Failed to load because of timeout');
        reject($self);
      });
    });
  }
};
/* harmony default export */ var checks_AddressCheckExtension = (AddressCheckExtension);
;// ./modules/extensions/session/SessionExtension.js


var SessionExtension = {
  name: 'SessionExtension',
  extend: function extend(ExtendableObject) {
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      // Initialize properties and subscribers
      initializeProperties(ExtendableObject);

      // Define property accessors
      definePropertyAccessors(ExtendableObject);

      // Set interval for form processing
      processFormsInterval(ExtendableObject);

      // Handle onAfterCreate event
      ExtendableObject.onAfterCreate.push(function () {
        return handleAfterCreate(ExtendableObject);
      });

      // Add a method to update session id.
      ExtendableObject.util.updateSessionId = function () {
        updateSessionId(ExtendableObject);
      };

      // Debug information
      if (ExtendableObject.config.showDebugInfo) {
        console.log('SessionExtension applied');
      }
      resolve(SessionExtension);
    });
  }
};
function initializeProperties(ExtendableObject) {
  ExtendableObject._sessionId = '';
  ExtendableObject._subscribers.sessionId = [];
  ExtendableObject._sessionCounter = '';
  ExtendableObject._subscribers.sessionCounter = [];
  ExtendableObject.forms = [];
  ExtendableObject.formsWithSession = [];
}
function definePropertyAccessors(ExtendableObject) {
  createAccessor(ExtendableObject, 'sessionId');
  createAccessor(ExtendableObject, 'sessionCounter');
}
function createAccessor(ExtendableObject, propertyName) {
  Object.defineProperty(ExtendableObject, propertyName, {
    get: function get() {
      return this['_' + propertyName];
    },
    set: function set(value) {
      var _this = this;
      var oldValue = this['_' + propertyName];
      this._awaits++;
      ExtendableObject.util.Promise.resolve(value).then(function (newValue) {
        if (oldValue !== newValue) {
          _this['_' + propertyName] = newValue;
          notifySubscribers(ExtendableObject, propertyName, newValue);
          fireChangeEvent(ExtendableObject, propertyName, oldValue, newValue);
        }
      }).catch(console.error).finally(function () {
        return _this._awaits--;
      });
    }
  });
}
function notifySubscribers(ExtendableObject, propertyName, newValue) {
  ExtendableObject._subscribers[propertyName].forEach(function (subscriber) {
    subscriber.value = newValue;
  });
}
function fireChangeEvent(ExtendableObject, propertyName, oldValue, newValue) {
  ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
    detail: {
      fieldName: propertyName,
      oldValue: oldValue,
      newValue: newValue,
      object: ExtendableObject
    }
  }));
}
function updateSessionId(ExtendableObject) {
  ExtendableObject.sessionId = esm_browser_v4();
}
function processFormsInterval(ExtendableObject) {
  setInterval(function () {
    if (ExtendableObject.forms.length !== ExtendableObject.formsWithSession.length) {
      ExtendableObject.forms.forEach(function (form) {
        if (!ExtendableObject.formsWithSession.includes(form)) {
          addInputIfNotExists(form, ExtendableObject.fullName + '_session_id', ExtendableObject);
          addInputIfNotExists(form, ExtendableObject.fullName + '_session_counter', ExtendableObject);
          // Subscribe to own fields
          subscribeToField(ExtendableObject, 'sessionId');
          subscribeToField(ExtendableObject, 'sessionCounter');
          ExtendableObject.formsWithSession.push(form);
        }
        // TODO: we need a handling logic, for when the session fields already exist in a form (from server side template)
      });
    }
  }, 500);
}
function addInputIfNotExists(form, name, ExtendableObject) {
  if (!form.querySelector("input[name=\"".concat(name, "\"]"))) {
    var inputHtml = ExtendableObject.util.Mustache.render('<input type="hidden" name="{{name}}" value="">', {
      name: name
    });
    form.insertAdjacentHTML('afterbegin', inputHtml);
  }
}
function handleAfterCreate(ExtendableObject) {
  ExtendableObject.waitForActive().then(function () {
    ExtendableObject.waitUntilReady().then(function () {
      ExtendableObject.syncValues(['sessionId', 'sessionCounter']).then(function () {
        if (!ExtendableObject.sessionId) {
          ExtendableObject.sessionCounter = 0;
          ExtendableObject.sessionId = ExtendableObject.id;
        }
      }).catch(console.error);
    }).catch(console.error);
  }).catch(console.error);
}
function subscribeToField(ExtendableObject, fieldName) {
  var fieldNameMapping = {
    'sessionId': 'session_id',
    'sessionCounter': 'session_counter'
  };
  var subscriber = new modules_subscriber(fieldName, document.querySelector("[name=\"".concat(ExtendableObject.fullName, "_").concat(fieldNameMapping[fieldName], "\"]")));
  ExtendableObject.addSubscriber(subscriber);
}
/* harmony default export */ var session_SessionExtension = (SessionExtension);
;// ./modules/ams.js



// Extensions.





















function EnderecoAddress() {
  var customConfig = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  // Get base object, that will be extended.
  var base = new components_base();
  base.type = 'address';
  base.name = 'ams';
  base.addressType = 'general_address';
  base.forms = [];

  // Override config.
  base.config = lodash_merge_default()(base.config, customConfig);

  // Add extensions.
  base.extensions = [fields_CountryCodeExtension, checks_CountryCodeCheckExtension, fields_SubdivisionCodeExtension, checks_SubdivisionCodeCheckExtension, fields_PostalCodeExtension, checks_PostalCodeCheckExtension, autocomplete_PostalCodeAutocompleteExtension, fields_LocalityExtension, checks_LocalityCheckExtension, autocomplete_LocalityAutocompleteExtension, fields_StreetNameExtension, checks_StreetNameCheckExtension, fields_StreetFullExtension, checks_StreetFullCheckExtension, fields_BuildingNumberExtension, checks_BuildingNumberCheckExtension, fields_AdditionalInfoExtension, checks_AdditionalInfoCheckExtension, fields_AddressExtension, checks_AddressCheckExtension, session_SessionExtension];

  // Load extesions.
  base.loadExtensions();
  base.onSubmitUnblock = [];
  base.submitUnblocked = function () {
    var $self = this;
    this.onSubmitUnblock.forEach(function (cb) {
      cb($self);
    });
  };
  base.onAddressSelect.push(function (AddressObject) {
    AddressObject.waitForAllEnderecoPopupsToClose().then(function () {
      if (window.EnderecoIntegrator && window.EnderecoIntegrator.submitResume) {
        base.beforeSubmitResume();
        window.EnderecoIntegrator.submitResume();
      }
    }).catch();
  });

  // Form submit handler.
  base.cb.onFormSubmit = function (e) {
    window.EnderecoIntegrator.hasSubmit = true;
    if (!base.config.trigger.onsubmit) {
      return true;
    }
    if (base.util.shouldBeChecked()) {
      // First. Block.
      e.preventDefault();
      e.stopPropagation();
      if (base.config.ux.resumeSubmit) {
        if (window.EnderecoIntegrator && !window.EnderecoIntegrator.submitResume) {
          window.EnderecoIntegrator.submitResume = function () {
            if (e.target.dispatchEvent(new base.util.CustomEvent('submit', {
              'bubbles': true,
              'cancelable': true
            }))) {
              e.target.submit();
            }
            window.EnderecoIntegrator.submitResume = undefined;
          };
        }
      }
      setTimeout(function () {
        base.util.checkAddress().catch(function () {
          base.waitForAllEnderecoPopupsToClose().then(function () {
            if (window.EnderecoIntegrator && window.EnderecoIntegrator.submitResume) {
              base.beforeSubmitResume();
              window.EnderecoIntegrator.submitResume();
            }
          }).catch();
        });
      }, 300);
      return false;
    }
  };

  // Call "onCreate" callbacks.
  base.created();
  return base;
}
/* harmony default export */ var ams = (EnderecoAddress);
;// ./modules/extensions/fields/EmailExtension.js
var EmailExtension = {
  name: 'EmailExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      // Add email field.
      ExtendableObject._email = '';
      ExtendableObject._subscribers.email = [];

      // Add change event hadler.
      ExtendableObject.cb.emailChange = function (subscriber) {
        return function (e) {
          ExtendableObject.email = subscriber.value;
        };
      };

      // Add the "emaiL" property
      Object.defineProperty(ExtendableObject, 'email', {
        get: function get() {
          return this._email;
        },
        set: function set(value) {
          var oldValue = ExtendableObject._email;
          ExtendableObject._awaits++;
          ExtendableObject.util.Promise.resolve(value).then(function (value) {
            var newValue = value;
            if (oldValue !== newValue) {
              ExtendableObject._email = newValue;
              ExtendableObject._changed = true;

              // Inform all subscribers about the change.
              ExtendableObject._subscribers.email.forEach(function (subscriber) {
                subscriber.value = value;
              });

              // Fire change event for listeners.
              ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                detail: {
                  fieldName: 'email',
                  oldValue: oldValue,
                  newValue: newValue,
                  object: ExtendableObject
                }
              }));

              // Check address, if the extension is loaded.
              if (ExtendableObject.active && ExtendableObject.hasLoadedExtension('EmailCheckExtension')) {
                ExtendableObject._awaits++;
                if ('' === value.trim()) {
                  ExtendableObject.emailStatus = [];
                } else {
                  ExtendableObject.util.checkEmail().then(function (data) {
                    var statusCollector = {};
                    if (data.status.includes('A1000')) {
                      statusCollector['email_correct'] = true;
                    }
                    if (data.status.includes('A1100')) {
                      statusCollector['email_correct'] = true;
                      statusCollector['email_catchall'] = true;
                    }
                    if (data.status.includes('A1110')) {
                      statusCollector['email_correct'] = true;
                      statusCollector['email_catchall'] = true;
                    }
                    if (data.status.includes('A1400')) {
                      statusCollector['email_correct'] = true;
                      statusCollector['email_disposable'] = true;
                    }
                    if (data.status.includes('A4000')) {
                      statusCollector['email_not_correct'] = true;
                      statusCollector['email_error'] = true;
                    }
                    if (data.status.includes('A4100')) {
                      statusCollector['email_not_correct'] = true;
                      statusCollector['email_smtp_problem'] = true;
                    }
                    if (data.status.includes('A4110')) {
                      statusCollector['email_not_correct'] = true;
                      statusCollector['email_smtp_problem'] = true;
                    }
                    if (data.status.includes('A4200')) {
                      statusCollector['email_not_correct'] = true;
                      statusCollector['email_syntax_error'] = true;
                    }
                    if (data.status.includes('A4300')) {
                      statusCollector['email_not_correct'] = true;
                      statusCollector['email_address_unknown'] = true;
                    }
                    if (data.status.includes('A4400')) {
                      statusCollector['email_not_correct'] = true;
                      statusCollector['email_delivery_failed'] = true;
                    }
                    if (data.status.includes('A4500')) {
                      statusCollector['email_not_correct'] = true;
                      statusCollector['email_relay_error'] = true;
                    }
                    if (data.status.includes('A4600')) {
                      statusCollector['email_not_correct'] = true;
                      statusCollector['email_blocked_by_antispam'] = true;
                    }
                    if (data.status.includes('A4700')) {
                      statusCollector['email_not_correct'] = true;
                      statusCollector['email_cant_receive'] = true;
                    }
                    if (data.status.includes('A4800')) {
                      statusCollector['email_not_correct'] = true;
                      statusCollector['email_no_mailserver_found'] = true;
                    }
                    if (data.status.includes('A4810')) {
                      statusCollector['email_not_correct'] = true;
                      statusCollector['email_mailserver_down'] = true;
                    }
                    if (data.status.includes('A4900')) {
                      statusCollector['email_spam_trap'] = true;
                    }
                    if (data.status.includes('A5000')) {
                      statusCollector['email_could_not_be_verified'] = true;
                    }
                    ExtendableObject.emailStatus = ExtendableObject.util.merge(data.status, Object.keys(statusCollector));
                  }).catch(function (e) {
                    ExtendableObject.emailStatus = [];
                    console.log('Failed checking email', e, ExtendableObject);
                  }).finally(function () {
                    ExtendableObject._awaits--;
                    ExtendableObject._changed = false;
                  });
                }
              }
            }
          }).catch().finally(function () {
            ExtendableObject._awaits--;
          });
        }
      });
      ExtendableObject.fieldNames.push('email');
      if (ExtendableObject.config.showDebugInfo) {
        console.log('EmailExtension applied');
      }
      resolve($self);
    });
  }
};
/* harmony default export */ var fields_EmailExtension = (EmailExtension);
;// ./templates/email_check_status_wrapper.html
// Module
var email_check_status_wrapper_code = "<div data-id=\"{{ExtendableObject.id}}\" class=\"endereco-status-wrapper\"> <ul class=\"endereco-status-wrapper-list\"> {{#statuses}} <li class=\"endereco-status-wrapper-list__item endereco-status-wrapper-list__item--{{status}}\">{{text}}</li> {{/statuses}} </ul> </div> ";
// Exports
/* harmony default export */ var email_check_status_wrapper = (email_check_status_wrapper_code);
;// ./modules/extensions/checks/EmailCheckExtension.js

var EmailCheckExtension = {
  name: 'EmailCheckExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      ExtendableObject.waitForExtension('EmailExtension').then(function () {
        // Add email field.
        ExtendableObject._emailStatus = '';
        ExtendableObject._subscribers.emailStatus = [];
        ExtendableObject.config.templates.statusWrapper = email_check_status_wrapper;
        if (undefined === ExtendableObject.config.ux.showEmailStatus) {
          ExtendableObject.config.ux.showEmailStatus = false;
        }

        // Add change event hadler.
        ExtendableObject.cb.emailStatusChange = function (subscriber) {
          return function (e) {
            ExtendableObject.emailStatus = subscriber.value;
          };
        };
        ExtendableObject.cb.onEmailChecked = [];
        ExtendableObject.cb.onEmailCheckFailed = [];

        // Add the "emaiL" property
        Object.defineProperty(ExtendableObject, 'emailStatus', {
          get: function get() {
            return this._emailStatus;
          },
          set: function set(value) {
            var oldValue = ExtendableObject._emailStatus;
            ExtendableObject._awaits++;
            ExtendableObject.util.Promise.resolve(value).then(function (value) {
              if (typeof value === 'string') {
                if ('' === value) {
                  value = [];
                } else {
                  value = [value];
                }
              }
              var newValue = value;
              if (oldValue !== newValue) {
                ExtendableObject._emailStatus = newValue;
                ExtendableObject._changed = false;

                // Inform all subscribers about the change.
                ExtendableObject._subscribers.emailStatus.forEach(function (subscriber) {
                  subscriber.value = value;
                });
                ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                  detail: {
                    fieldName: 'emailStatus',
                    oldValue: oldValue,
                    newValue: newValue,
                    object: ExtendableObject
                  }
                }));
                if (ExtendableObject.config.ux.showEmailStatus) {
                  ExtendableObject.util.renderStatusMessages();
                }
              }
            }).catch().finally(function () {
              ExtendableObject._awaits--;
            });
          }
        });
        ExtendableObject.util.renderStatusMessages = function () {
          var statuses = [];
          ExtendableObject.emailStatus.forEach(function (emailStatus) {
            if (!!ExtendableObject.config.texts.statuses[emailStatus]) {
              statuses.push({
                status: emailStatus,
                text: ExtendableObject.config.texts.statuses[emailStatus]
              });
            }
          });
          if (document.querySelectorAll('.endereco-status-wrapper[data-id="' + ExtendableObject.id + '"]')) {
            document.querySelectorAll('.endereco-status-wrapper[data-id="' + ExtendableObject.id + '"]').forEach(function (DOMElement) {
              DOMElement.remove();
            });
          }
          if (0 < statuses.length) {
            var wrapperHtml = ExtendableObject.util.Mustache.render(ExtendableObject.config.templates.statusWrapper, {
              'ExtendableObject': ExtendableObject,
              'statuses': statuses
            });
            var messageContainer = document.querySelector(ExtendableObject.config.ux.errorContainer);
            var insertMode = ["beforebegin", "afterbegin", "beforeend", "afterend"].includes(ExtendableObject.config.ux.errorInsertMode) ? ExtendableObject.config.ux.errorInsertMode : "afterbegin";
            if (!!messageContainer) {
              messageContainer.insertAdjacentHTML(insertMode, wrapperHtml);
            } else {
              ExtendableObject._subscribers.email.forEach(function (subscriber) {
                subscriber.object.insertAdjacentHTML('afterend', wrapperHtml);
              });
            }
          }
        };
        ExtendableObject.util.checkEmail = function () {
          var email = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : null;
          var $self = this;
          if (!email) {
            email = ExtendableObject.email;
          }
          return new ExtendableObject.util.Promise(function (resolve, reject) {
            var message = {
              'jsonrpc': '2.0',
              'id': 1,
              'method': 'emailCheck',
              'params': {
                'email': email
              }
            };

            // Send user data to remote server for validation.
            ExtendableObject._awaits++;
            ExtendableObject.util.axios.post(ExtendableObject.config.apiUrl, message, {
              timeout: 6000,
              headers: {
                'X-Auth-Key': ExtendableObject.config.apiKey,
                'X-Agent': ExtendableObject.config.agentName,
                'X-Remote-Api-Url': ExtendableObject.config.remoteApiUrl,
                'X-Transaction-Referer': window.location.href,
                'X-Transaction-Id': ExtendableObject.hasLoadedExtension('SessionExtension') ? ExtendableObject.sessionId : 'not_required'
              }
            }).then(function (response) {
              if (undefined !== response.data.result) {
                // If session counter is set, increase it.
                if (ExtendableObject.hasLoadedExtension('SessionExtension')) {
                  ExtendableObject.sessionCounter++;
                }
                ExtendableObject.cb.onEmailChecked.forEach(function (cb) {
                  cb(ExtendableObject, response.data);
                });
                resolve(response.data.result);
              } else {
                ExtendableObject.cb.onEmailCheckFailed.forEach(function (cb) {
                  cb(ExtendableObject, response.data);
                });
                reject(response.data);
              }
            }).catch(function (e) {
              ExtendableObject.cb.onEmailCheckFailed.forEach(function (cb) {
                cb(ExtendableObject, e.response);
              });
              reject(e.response);
            }).finally(function () {
              ExtendableObject._awaits--;
            });
          });
        };
        if (ExtendableObject.config.showDebugInfo) {
          console.log('EmailCheckExtension applied');
        }
        resolve($self);
      }).catch(function () {
        console.log('Failed to load because of timeout');
        reject($self);
      });
    });
  }
};
/* harmony default export */ var checks_EmailCheckExtension = (EmailCheckExtension);
;// ./modules/emailservices.js



// Extensions.



function EnderecoEmail() {
  var customConfig = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  // Get base object, that will be extended.
  var base = new components_base();
  base.type = 'email';
  base.name = 'emailservices';

  // Override config.
  base.config = lodash_merge_default()(base.config, customConfig);

  // Add extensions.
  base.extensions = [checks_EmailCheckExtension, fields_EmailExtension, session_SessionExtension];

  // Load extesions.
  base.loadExtensions();

  // Call "onCreate" callbacks.
  base.created();
  return base;
}
/* harmony default export */ var emailservices = (EnderecoEmail);
;// ./modules/extensions/fields/SalutationExtension.js
var SalutationExtension = {
  name: 'SalutationExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      // Add email field.
      ExtendableObject._salutation = '';
      ExtendableObject._subscribers.salutation = [];

      // Add change event hadler.
      ExtendableObject.cb.salutationChange = function (subscriber) {
        return function (e) {
          ExtendableObject.salutation = subscriber.value;
          ExtendableObject.waitUntilReady().then(function () {
            if (ExtendableObject.onBlurTimeout) {
              clearTimeout(ExtendableObject.onBlurTimeout);
              ExtendableObject.onBlurTimeout = null;
            }
            ExtendableObject.onBlurTimeout = setTimeout(function () {
              if (!ExtendableObject.anyActive() && ExtendableObject.util.shouldBeChecked() && !window.EnderecoIntegrator.hasSubmit) {
                // Second. Check Address.
                ExtendableObject.onBlurTimeout = null;
                ExtendableObject.util.checkPerson().catch().finally(function () {
                  ExtendableObject._changed = false;
                });
              }
            }, 300);
          }).catch();
        };
      };

      // Add the "salutation" property
      Object.defineProperty(ExtendableObject, 'salutation', {
        get: function get() {
          return this._salutation;
        },
        set: function set(value) {
          var oldValue = ExtendableObject._salutation;
          ExtendableObject._awaits++;
          ExtendableObject.util.Promise.resolve(value).then(function (value) {
            var newValue = value;
            if (oldValue !== newValue) {
              ExtendableObject._salutation = newValue;
              ExtendableObject._changed = true;

              // Inform all subscribers about the change.
              ExtendableObject._subscribers.salutation.forEach(function (subscriber) {
                subscriber.value = value;
              });

              // Fire change event for listeners.
              if (ExtendableObject.active) {
                ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                  detail: {
                    fieldName: 'salutation',
                    oldValue: oldValue,
                    newValue: newValue,
                    object: ExtendableObject
                  }
                }));
              }
            }
          }).catch().finally(function () {
            ExtendableObject._awaits--;
          });
        }
      });
      ExtendableObject.fieldNames.push('salutation');
      if (ExtendableObject.config.showDebugInfo) {
        console.log('SalutationExtension applied');
      }
      resolve($self);
    });
  }
};
/* harmony default export */ var fields_SalutationExtension = (SalutationExtension);
;// ./modules/extensions/fields/LastNameExtension.js
var LastNameExtension = {
  name: 'LastNameExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      // Add email field.
      ExtendableObject._lastName = '';
      ExtendableObject._subscribers.lastName = [];

      // Add change event hadler.
      ExtendableObject.cb.lastNameBlur = function (subscriber) {
        return function (e) {
          ExtendableObject.lastName = subscriber.value;
          ExtendableObject.waitUntilReady().then(function () {
            if (ExtendableObject.onBlurTimeout) {
              clearTimeout(ExtendableObject.onBlurTimeout);
              ExtendableObject.onBlurTimeout = null;
            }
            ExtendableObject.onBlurTimeout = setTimeout(function () {
              if (!ExtendableObject.anyActive() && ExtendableObject.util.shouldBeChecked() && !window.EnderecoIntegrator.hasSubmit) {
                // Second. Check Address.
                ExtendableObject.onBlurTimeout = null;
                ExtendableObject.util.checkPerson().catch().finally(function () {
                  ExtendableObject._changed = false;
                });
              }
            }, 300);
          }).catch();
        };
      };

      // Add the "emaiL" property
      Object.defineProperty(ExtendableObject, 'lastName', {
        get: function get() {
          return this._lastName;
        },
        set: function set(value) {
          var oldValue = ExtendableObject._lastName;
          ExtendableObject._awaits++;
          ExtendableObject.util.Promise.resolve(value).then(function (value) {
            var newValue = value;
            if (oldValue !== newValue) {
              ExtendableObject._lastName = newValue;
              ExtendableObject._changed = true;

              // Inform all subscribers about the change.
              ExtendableObject._subscribers.lastName.forEach(function (subscriber) {
                subscriber.value = value;
              });

              // Fire change event for listeners.
              if (ExtendableObject.active) {
                ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                  detail: {
                    fieldName: 'lastName',
                    oldValue: oldValue,
                    newValue: newValue,
                    object: ExtendableObject
                  }
                }));
              }
            }
          }).catch().finally(function () {
            ExtendableObject._awaits--;
          });
        }
      });
      ExtendableObject.fieldNames.push('lastName');
      if (ExtendableObject.config.showDebugInfo) {
        console.log('LastNameExtension applied');
      }
      resolve($self);
    });
  }
};
/* harmony default export */ var fields_LastNameExtension = (LastNameExtension);
;// ./modules/extensions/fields/FirstNameExtension.js
var FirstNameExtension = {
  name: 'FirstNameExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      // Add email field.
      ExtendableObject._firstName = '';
      ExtendableObject._subscribers.firstName = [];

      // Add change event hadler.
      ExtendableObject.cb.firstNameBlur = function (subscriber) {
        return function (e) {
          ExtendableObject.firstName = subscriber.value;
          ExtendableObject.waitUntilReady().then(function () {
            if (ExtendableObject.onBlurTimeout) {
              clearTimeout(ExtendableObject.onBlurTimeout);
              ExtendableObject.onBlurTimeout = null;
            }
            ExtendableObject.onBlurTimeout = setTimeout(function () {
              if (!ExtendableObject.anyActive() && ExtendableObject.util.shouldBeChecked() && !window.EnderecoIntegrator.hasSubmit) {
                // Second. Check Address.
                ExtendableObject.onBlurTimeout = null;
                ExtendableObject.util.checkPerson().catch().finally(function () {
                  ExtendableObject._changed = false;
                });
              }
            }, 300);
          }).catch();
        };
      };

      // Add the "emaiL" property
      Object.defineProperty(ExtendableObject, 'firstName', {
        get: function get() {
          return this._firstName;
        },
        set: function set(value) {
          var oldValue = ExtendableObject._firstName;
          ExtendableObject._awaits++;
          ExtendableObject.util.Promise.resolve(value).then(function (value) {
            var newValue = value;
            if (oldValue !== newValue) {
              ExtendableObject._firstName = newValue;
              ExtendableObject._changed = true;

              // Inform all subscribers about the change.
              ExtendableObject._subscribers.firstName.forEach(function (subscriber) {
                subscriber.value = value;
              });

              // Fire change event for listeners.
              if (ExtendableObject.active) {
                ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                  detail: {
                    fieldName: 'firstName',
                    oldValue: oldValue,
                    newValue: newValue,
                    object: ExtendableObject
                  }
                }));
              }
            }
          }).catch().finally(function () {
            ExtendableObject._awaits--;
          });
        }
      });
      ExtendableObject.fieldNames.push('firstName');
      if (ExtendableObject.config.showDebugInfo) {
        console.log('FirstNameExtension applied');
      }
      resolve($self);
    });
  }
};
/* harmony default export */ var fields_FirstNameExtension = (FirstNameExtension);
;// ./modules/extensions/fields/TitleExtension.js
var TitleExtension = {
  name: 'TitleExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      // Add email field.
      ExtendableObject._title = '';
      ExtendableObject._subscribers.title = [];

      // Add change event hadler.
      ExtendableObject.cb.titleBlur = function (subscriber) {
        return function (e) {
          ExtendableObject.title = subscriber.value;
          ExtendableObject.waitUntilReady().then(function () {
            if (ExtendableObject.onBlurTimeout) {
              clearTimeout(ExtendableObject.onBlurTimeout);
              ExtendableObject.onBlurTimeout = null;
            }
            ExtendableObject.onBlurTimeout = setTimeout(function () {
              if (!ExtendableObject.anyActive() && ExtendableObject.util.shouldBeChecked() && !window.EnderecoIntegrator.hasSubmit) {
                // Second. Check Address.
                ExtendableObject.onBlurTimeout = null;
                ExtendableObject.util.checkPerson().catch().finally(function () {
                  ExtendableObject._changed = false;
                });
              }
            }, 300);
          }).catch();
        };
      };

      // Add the "emaiL" property
      Object.defineProperty(ExtendableObject, 'title', {
        get: function get() {
          return this._title;
        },
        set: function set(value) {
          var oldValue = ExtendableObject._title;
          ExtendableObject._awaits++;
          ExtendableObject.util.Promise.resolve(value).then(function (value) {
            var newValue = value;
            if (oldValue !== newValue) {
              ExtendableObject._title = newValue;
              ExtendableObject._changed = true;

              // Inform all subscribers about the change.
              ExtendableObject._subscribers.title.forEach(function (subscriber) {
                subscriber.value = value;
              });

              // Fire change event for listeners.
              if (ExtendableObject.active) {
                ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                  detail: {
                    fieldName: 'title',
                    oldValue: oldValue,
                    newValue: newValue,
                    object: ExtendableObject
                  }
                }));
              }
            }
          }).catch().finally(function () {
            ExtendableObject._awaits--;
          });
        }
      });
      ExtendableObject.fieldNames.push('title');
      if (ExtendableObject.config.showDebugInfo) {
        console.log('TitleExtension applied');
      }
      resolve($self);
    });
  }
};
/* harmony default export */ var fields_TitleExtension = (TitleExtension);
;// ./modules/extensions/checks/NameCheckExtension.js
var NameCheckExtension = {
  name: 'NameCheckExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      ExtendableObject.waitForExtension(['SalutationExtension', 'FirstNameExtension', 'LastNameExtension', 'TitleExtension']).then(function () {
        // Add email field.
        ExtendableObject._salutationStatus = '';
        ExtendableObject._firstNameStatus = '';
        ExtendableObject._lastNameStatus = '';
        ExtendableObject._titleStatus = '';
        ExtendableObject._nameScore = '';
        ExtendableObject._subscribers.salutationStatus = [];
        ExtendableObject._subscribers.firstNameStatus = [];
        ExtendableObject._subscribers.lastNameStatus = [];
        ExtendableObject._subscribers.titleStatus = [];
        ExtendableObject._subscribers.nameScore = [];
        ExtendableObject._nameCheckRequestIndex = 1;

        // Add change event hadler.
        ExtendableObject.cb.salutationStatusChange = function (subscriber) {
          return function (e) {
            ExtendableObject.salutationStatus = subscriber.value;
          };
        };
        ExtendableObject.cb.firstNameStatusChange = function (subscriber) {
          return function (e) {
            ExtendableObject.firstNameStatus = subscriber.value;
          };
        };
        ExtendableObject.cb.lastNameStatusChange = function (subscriber) {
          return function (e) {
            ExtendableObject.lastNameStatus = subscriber.value;
          };
        };
        ExtendableObject.cb.nameScoreStatusChange = function (subscriber) {
          return function (e) {
            ExtendableObject.nameScoreStatus = subscriber.value;
          };
        };
        ExtendableObject.cb.titleStatusChange = function (subscriber) {
          return function (e) {
            ExtendableObject.titleStatus = subscriber.value;
          };
        };
        ExtendableObject.isTitleRelevant = function () {
          var isTitleRelevant = false;
          if (0 < ExtendableObject._subscribers.title.length) {
            ExtendableObject._subscribers.title.forEach(function (listener) {
              if (!listener.object.disabled && listener.object.isConnected) {
                isTitleRelevant = true;
              }
            });
          }
          return isTitleRelevant;
        };

        // Add the "emaiL" property
        Object.defineProperty(ExtendableObject, 'salutationStatus', {
          get: function get() {
            return this._salutationStatus;
          },
          set: function set(value) {
            var oldValue = ExtendableObject._salutationStatus;
            ExtendableObject._awaits++;
            ExtendableObject.util.Promise.resolve(value).then(function (value) {
              var newValue = value;
              if (oldValue !== newValue) {
                ExtendableObject._salutationStatus = newValue;

                // Inform all subscribers about the change.
                ExtendableObject._subscribers.salutationStatus.forEach(function (subscriber) {
                  subscriber.value = value;
                });
                ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                  detail: {
                    fieldName: 'salutationStatus',
                    oldValue: oldValue,
                    newValue: newValue,
                    object: ExtendableObject
                  }
                }));
              }
            }).catch().finally(function () {
              ExtendableObject._awaits--;
            });
          }
        });
        Object.defineProperty(ExtendableObject, 'firstNameStatus', {
          get: function get() {
            return this._firstNameStatus;
          },
          set: function set(value) {
            var oldValue = ExtendableObject._firstNameStatus;
            ExtendableObject._awaits++;
            ExtendableObject.util.Promise.resolve(value).then(function (value) {
              var newValue = value;
              if (oldValue !== newValue) {
                ExtendableObject._firstNameStatus = newValue;

                // Inform all subscribers about the change.
                ExtendableObject._subscribers.firstNameStatus.forEach(function (subscriber) {
                  subscriber.value = value;
                });
                ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                  detail: {
                    fieldName: 'firstNameStatus',
                    oldValue: oldValue,
                    newValue: newValue,
                    object: ExtendableObject
                  }
                }));
              }
            }).catch().finally(function () {
              ExtendableObject._awaits--;
            });
          }
        });
        Object.defineProperty(ExtendableObject, 'lastNameStatus', {
          get: function get() {
            return this._lastNameStatus;
          },
          set: function set(value) {
            var oldValue = ExtendableObject._lastNameStatus;
            ExtendableObject._awaits++;
            ExtendableObject.util.Promise.resolve(value).then(function (value) {
              var newValue = value;
              if (oldValue !== newValue) {
                ExtendableObject._lastNameStatus = newValue;

                // Inform all subscribers about the change.
                ExtendableObject._subscribers.lastNameStatus.forEach(function (subscriber) {
                  subscriber.value = value;
                });
                ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                  detail: {
                    fieldName: 'lastNameStatus',
                    oldValue: oldValue,
                    newValue: newValue,
                    object: ExtendableObject
                  }
                }));
              }
            }).catch().finally(function () {
              ExtendableObject._awaits--;
            });
          }
        });
        Object.defineProperty(ExtendableObject, 'titleStatus', {
          get: function get() {
            return this._titleStatus;
          },
          set: function set(value) {
            var oldValue = ExtendableObject._titleStatus;
            ExtendableObject._awaits++;
            ExtendableObject.util.Promise.resolve(value).then(function (value) {
              var newValue = value;
              if (oldValue !== newValue) {
                ExtendableObject._titleStatus = newValue;

                // Inform all subscribers about the change.
                ExtendableObject._subscribers.titleStatus.forEach(function (subscriber) {
                  subscriber.value = value;
                });
                ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                  detail: {
                    fieldName: 'titleStatus',
                    oldValue: oldValue,
                    newValue: newValue,
                    object: ExtendableObject
                  }
                }));
              }
            }).catch().finally(function () {
              ExtendableObject._awaits--;
            });
          }
        });
        Object.defineProperty(ExtendableObject, 'nameScore', {
          get: function get() {
            return this._nameScore;
          },
          set: function set(value) {
            var oldValue = ExtendableObject._nameScore;
            ExtendableObject._awaits++;
            ExtendableObject.util.Promise.resolve(value).then(function (value) {
              var newValue = value;
              if (oldValue !== newValue) {
                ExtendableObject._nameScore = newValue;

                // Inform all subscribers about the change.
                ExtendableObject._subscribers.nameScore.forEach(function (subscriber) {
                  subscriber.value = value;
                });
                ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                  detail: {
                    fieldName: 'nameScore',
                    oldValue: oldValue,
                    newValue: newValue,
                    object: ExtendableObject
                  }
                }));
              }
            }).catch().finally(function () {
              ExtendableObject._awaits--;
            });
          }
        });
        ExtendableObject.util.shouldBeChecked = function () {
          if (!ExtendableObject._changed) {
            return false;
          }
          return true;
        };
        ExtendableObject.util.checkPerson = function () {
          var person = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : null;
          var $self = this;
          if (!person) {
            person = {
              title: ExtendableObject.title,
              firstName: ExtendableObject.firstName,
              lastName: ExtendableObject.lastName,
              salutation: ExtendableObject.salutation
            };
          }
          return new ExtendableObject.util.Promise(function (resolve, reject) {
            var message = {
              'jsonrpc': '2.0',
              'id': ExtendableObject._nameCheckRequestIndex,
              'method': 'nameCheck',
              'params': {
                'firstName': person.firstName,
                'lastName': person.lastName,
                'salutation': person.salutation
              }
            };
            if (0 < ExtendableObject._subscribers.title.length) {
              ExtendableObject._subscribers.title.forEach(function (listener) {
                if (!listener.object.disabled && listener.object.isConnected) {
                  message.params.title = person.title;
                }
              });
            }

            // Send user data to remote server for validation.
            ExtendableObject._awaits++;
            ExtendableObject.util.axios.post(ExtendableObject.config.apiUrl, message, {
              timeout: 2000,
              headers: {
                'X-Auth-Key': ExtendableObject.config.apiKey,
                'X-Agent': ExtendableObject.config.agentName,
                'X-Remote-Api-Url': ExtendableObject.config.remoteApiUrl,
                'X-Transaction-Referer': window.location.href,
                'X-Transaction-Id': ExtendableObject.hasLoadedExtension('SessionExtension') ? ExtendableObject.sessionId : 'not_required'
              }
            }).then(function (response) {
              if (undefined !== response.data.result) {
                // If session counter is set, increase it.
                if (ExtendableObject.hasLoadedExtension('SessionExtension')) {
                  ExtendableObject.sessionCounter++;
                }

                // Process the response.
                // Rewrite input.
                var responseStatus = response.data.result.status;
                var responsePredictions = response.data.result.predictions;
                var salutationCopied = false;
                if (responseStatus.includes('name_is_natural_person') && (responseStatus.includes('name_needs_correction') || responseStatus.includes('name_correct'))) {
                  ExtendableObject.firstNameStatus = [];
                  ExtendableObject.lastNameStatus = [];
                  if (!['m', 'f', 'd'].includes(ExtendableObject.salutation) && !!responsePredictions[0].salutation) {
                    ExtendableObject.salutation = responsePredictions[0].salutation;
                    salutationCopied = true;
                  }
                  if (1 === ExtendableObject._nameCheckRequestIndex) {
                    if (!responseStatus.includes('name_transpositioned')) {
                      ExtendableObject.firstName = responsePredictions[0].firstName;
                      ExtendableObject.lastName = responsePredictions[0].lastName;
                      ExtendableObject.firstNameStatus = ['first_name_correct'];
                      ExtendableObject.lastNameStatus = ['last_name_correct'];
                      if (ExtendableObject.isTitleRelevant()) {
                        ExtendableObject.title = responsePredictions[0].title;
                        if (responsePredictions[0].title !== '') {
                          ExtendableObject.titleStatus = ['title_correct'];
                        }
                      }
                    } else if (responseStatus.includes('name_transpositioned')) {
                      if (ExtendableObject.config.ux.correctTranspositionedNames) {
                        ExtendableObject.lastName = responsePredictions[0].lastName;
                        ExtendableObject.firstName = responsePredictions[0].firstName;
                      } else {
                        ExtendableObject.lastName = responsePredictions[0].firstName;
                        ExtendableObject.firstName = responsePredictions[0].lastName;
                      }
                      if (ExtendableObject.isTitleRelevant()) {
                        ExtendableObject.title = responsePredictions[0].title;
                        if (responsePredictions[0].title !== '') {
                          ExtendableObject.titleStatus = ['title_correct'];
                        }
                      }
                      ExtendableObject.firstNameStatus = ['first_name_correct'];
                      ExtendableObject.lastNameStatus = ['last_name_correct'];
                    }
                  }
                  if (responseStatus.includes('name_correct')) {
                    ExtendableObject.firstNameStatus = ['first_name_correct'];
                    ExtendableObject.lastNameStatus = ['last_name_correct'];
                    if (ExtendableObject.isTitleRelevant() && ExtendableObject.title !== '') {
                      ExtendableObject.titleStatus = ['title_correct'];
                    }
                  }
                  ExtendableObject._changed = false;

                  // Set statuscodes.
                  if (salutationCopied) {
                    ExtendableObject.salutationStatus = ['salutation_correct'];
                  } else {
                    if (!responsePredictions[0].salutation) {
                      ExtendableObject.salutationStatus = [];
                    } else {
                      ExtendableObject.salutationStatus = responseStatus.includes('salutation_needs_correction') ? ['salutation_needs_correction'] : ['salutation_correct'];
                    }
                  }
                } else if (responseStatus.includes('name_not_found')) {
                  ExtendableObject.salutationStatus = ['salutation_needs_correction'];
                  ExtendableObject.firstNameStatus = ['first_name_needs_correction'];
                  ExtendableObject.lastNameStatus = ['last_name_needs_correction'];
                  if (ExtendableObject.isTitleRelevant() && ExtendableObject.title !== '') {
                    ExtendableObject.titleStatus = ['title_needs_correction'];
                  }
                }
                if (undefined !== response.data.result.score) {
                  ExtendableObject.nameScore = response.data.result.score;
                } else {
                  ExtendableObject.nameScore = 1.0;
                }
                resolve();
              } else {
                reject(response.data);
              }
            }).catch(function (e) {
              reject(e.response);
            }).finally(function () {
              ExtendableObject._awaits--;
              ExtendableObject._nameCheckRequestIndex++;
            });
          });
        };
        if (ExtendableObject.config.showDebugInfo) {
          console.log('NameCheckExtension applied');
        }
        resolve($self);
      }).catch(function () {
        console.log('Failed to load because of timeout');
        reject($self);
      });
    });
  }
};
/* harmony default export */ var checks_NameCheckExtension = (NameCheckExtension);
;// ./modules/personservices.js



// Extensions.






function EnderecoPerson() {
  var customConfig = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  // Get base object, that will be extended.
  var base = new components_base();
  base.type = 'person';
  base.name = 'personservices';

  // Override config.
  base.config = lodash_merge_default()(base.config, customConfig);

  // Add extensions.
  base.extensions = [fields_SalutationExtension, fields_FirstNameExtension, fields_LastNameExtension, fields_TitleExtension, session_SessionExtension, checks_NameCheckExtension];

  // Load extesions.
  base.loadExtensions();

  // Call "onCreate" callbacks.
  base.created();
  return base;
}
/* harmony default export */ var personservices = (EnderecoPerson);
;// ./templates/flagButton.html
// Module
var flagButton_code = "<div class=\"endereco-big-flag\"> <div class=\"endereco-flag\"> <svg width=\"15\" height=\"16\"> <rect width=\"16\" height=\"16\" style=\"fill:rgb(180,180,180)\"/> </svg> </div> <div class=\"endereco-cur-down\"> <svg version=\"1.1\" id=\"Layer_1\" xmlns=\"http://www.w3.org/2000/svg\" xmlns:xlink=\"http://www.w3.org/1999/xlink\" x=\"0px\" y=\"0px\" viewBox=\"0 0 330 330\" style=\"enable-background:new 0 0 330 330\" xml:space=\"preserve\"> <path id=\"XMLID_225_\" d=\"M325.607,79.393c-5.857-5.857-15.355-5.858-21.213,0.001l-139.39,139.393L25.607,79.393\n\tc-5.857-5.857-15.355-5.858-21.213,0.001c-5.858,5.858-5.858,15.355,0,21.213l150.004,150c2.813,2.813,6.628,4.393,10.606,4.393\n\ts7.794-1.581,10.606-4.394l149.996-150C331.465,94.749,331.465,85.251,325.607,79.393z\"/> <g> </g> <g> </g> <g> </g> <g> </g> <g> </g> <g> </g> <g> </g> <g> </g> <g> </g> <g> </g> <g> </g> <g> </g> <g> </g> <g> </g> <g> </g> </svg> </div> </div> ";
// Exports
/* harmony default export */ var flagButton = (flagButton_code);
;// ./templates/phoneDropdownHTML.html
// Module
var phoneDropdownHTML_code = "<div class=\"endereco-flag-dropdown endereco-hidden\"> <div class=\"endereco-flag-dropdown-inner\"> {{#countries}} <div class=\"endereco-flag-dropdown-element\" data-code=\"{{{code}}}\"> <div class=\"endereco-small-flag\">{{{flag}}}</div> <div class=\"endereco-country-code-col\">{{{iso2}}}</div> <div class=\"endereco-phone-code-col\">{{{code}}}</div> </div> {{/countries}} </div> </div>";
// Exports
/* harmony default export */ var phoneDropdownHTML = (phoneDropdownHTML_code);
;// ./templates/icons/not_found.svg
// Module
var not_found_code = "<svg width=\"512\" height=\"512\" viewBox=\"0 0 512 512\" fill=\"none\" xmlns=\"http://www.w3.org/2000/svg\"> <rect width=\"512\" height=\"512\" fill=\"#DDDDDD\"/> <rect y=\"256.151\" width=\"512\" height=\"255.85\" fill=\"#AAAAAA\"/> <path d=\"M247.282 324.454C260.887 324.454 268.54 316.633 270.071 304.221C270.071 302.691 270.241 300.991 270.241 299.631C271.771 280.078 279.424 271.576 307.144 253.554C337.585 234.001 354.762 211.558 354.762 179.083C354.762 127.056 312.416 96.2812 255.445 96.2812C213.27 96.2812 178.407 114.474 163.612 145.418C159.701 153.409 158 161.571 158 169.052C158 182.314 166.333 191.325 180.618 191.325C191.672 191.325 200.175 185.374 203.917 175.173C212.08 148.649 229.936 138.277 253.235 138.277C281.465 138.277 303.233 154.6 303.233 179.593C303.233 202.036 290.308 213.768 265.989 229.75C240.82 246.583 223.304 264.946 223.304 292.489V300.311C223.304 314.763 232.317 324.454 247.282 324.454ZM247.623 420.518C264.629 420.518 278.404 407.086 278.404 390.594C278.404 373.931 264.629 360.839 247.623 360.839C230.786 360.839 217.011 373.931 217.011 390.594C217.011 407.086 230.786 420.518 247.623 420.518Z\" fill=\"white\"/> </svg> ";
// Exports
/* harmony default export */ var not_found = (not_found_code);
;// ./templates/flags/af.svg
// Module
var af_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#DB3E00\" width=\"513\" height=\"342\"/> <rect x=\"331\" y=\"0\" fill=\"#479900\" width=\"182\" height=\"342\"/> <rect y=\"0\" width=\"181.8\" height=\"342\"/> <path fill=\"#FFFFFF\" d=\"M256,126.7c-19.4,0-35.2,15.8-35.2,35.2v52.8h70.4v-52.8C291.2,142.4,275.4,126.7,256,126.7z\"/> <path fill=\"#FFFFFF\" d=\"M256,84.3c-47.7,0-86.4,38.7-86.4,86.4S208.3,257,256,257c47.7,0,86.4-38.7,86.4-86.4S303.7,84.3,256,84.3z\n\t M256,242c-39.4,0-71.4-32-71.4-71.4c0-39.4,32-71.4,71.4-71.4c39.4,0,71.4,32,71.4,71.4C327.4,210.1,295.4,242,256,242z\"/> </svg> ";
// Exports
/* harmony default export */ var af = (af_code);
;// ./templates/flags/al.svg
// Module
var al_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"15 0 60 60\"> <rect x=\"0\" fill=\"#ED2024\" width=\"90\" height=\"60\"/> <polygon fill=\"#212121\" points=\"32.5,12.2 32.5,28.2 39.6,30.7 34.6,35.6 37,38.6 42.3,33.1 43.5,35.4 40.5,40.3 44.1,45.9 42.3,48.5\n\t45.1,52.1 47.8,48.4 46.3,45.8 49.3,40 46.6,35.4 47.8,33.1 53,38.6 55.5,35.7 50.3,30.6 57.5,28 57.5,12.2 52.3,14.1 52.2,17.9\n\t48.9,18.2 48.9,15.6 50.6,13.3 56.2,11.1 53.9,10.7 55.3,9.5 56.2,9.9 55.4,8.5 54,9 53,7.9 47.5,9 48.9,10.1 45.1,15 41.3,10.2\n\t42.6,9.1 37.6,7.9 36.2,9 34.7,8.6 33.9,10 34.8,9.4 36.4,10.5 34.1,11 39.6,13 41.2,15.3 41.2,18.2 37.9,17.9 37.9,14.3 \"/> <g> <rect x=\"26.2\" y=\"25\" fill=\"#212121\" width=\"6.4\" height=\"2.5\"/> <rect x=\"26.2\" y=\"20.9\" fill=\"#212121\" width=\"6.4\" height=\"2.7\"/> <rect x=\"26.2\" y=\"16.7\" fill=\"#212121\" width=\"6.4\" height=\"2.6\"/> <rect x=\"26.2\" y=\"12.7\" fill=\"#212121\" width=\"6.4\" height=\"2.5\"/> </g> <g> <rect x=\"57.4\" y=\"25\" fill=\"#212121\" width=\"6.4\" height=\"2.5\"/> <rect x=\"57.4\" y=\"20.9\" fill=\"#212121\" width=\"6.4\" height=\"2.7\"/> <rect x=\"57.4\" y=\"16.7\" fill=\"#212121\" width=\"6.4\" height=\"2.6\"/> <rect x=\"57.4\" y=\"12.7\" fill=\"#212121\" width=\"6.4\" height=\"2.5\"/> </g> <rect x=\"53.4\" y=\"36.1\" fill=\"#212121\" width=\"6.4\" height=\"1.9\"/> <rect x=\"30.2\" y=\"36.1\" fill=\"#212121\" width=\"6.4\" height=\"1.9\"/> </svg> ";
// Exports
/* harmony default export */ var al = (al_code);
;// ./templates/flags/dz.svg
// Module
var dz_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"88.764 0 341.3 341.3\"> <rect y=\"0\" fill=\"#41662E\" width=\"512\" height=\"341.3\"/> <rect x=\"256\" y=\"0\" fill=\"#FFFFFF\" width=\"256\" height=\"341.3\"/> <g> <polygon fill=\"#D80027\" points=\"341.5,105.3 312.1,145.7 264.6,130.3 294,170.7 264.6,211.1 312.1,195.7 341.5,236.1 341.5,186.1\n\t\t389,170.6 341.5,155.2 \t\"/> <path fill=\"#D80027\" d=\"M309.9,276.7c-58.5,0-106-47.5-106-106s47.5-106,106-106c18.3,0,35.4,4.6,50.4,12.7\n\t\tc-23.5-23-55.7-37.2-91.2-37.2c-72,0-130.4,58.4-130.4,130.4s58.4,130.4,130.4,130.4c35.5,0,67.7-14.2,91.2-37.2\n\t\tC345.4,272,328.2,276.7,309.9,276.7z\"/> </g> </svg> ";
// Exports
/* harmony default export */ var dz = (dz_code);
;// ./templates/flags/as.svg
// Module
var as_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"171 0 342 342\"> <rect fill=\"#10338c\" width=\"513\" height=\"342\"/> <polygon fill=\"#D80027\" points=\"513,33 95.3,171 513,310.76 513,341.33 0,171 513,0 \"/> <polyline fill=\"#FFFFFF\" points=\"513,287.18 513,311.76 81.72,171 513,30 513,54.16 \"/> <path fill=\"#A2001D\" d=\"M476.98,140.21l-21.89,10.68l-3.18-15.32l31.19-29.77c0,0-9.42-40.65-13.75-44.98l-112.32,55.82l-6.84,36.76\n\tl-31.9,28.59l-0.4,34.2l34.29-22.76l67.23-2.66l-1.51,38.11h22.23l11.9-44.64l31.55-24.61L476.98,140.21z\"/> <polygon fill=\"#EFC100\" stroke=\"#231F20\" stroke-miterlimit=\"10\" points=\"317.89,238.41 295.65,227.3 317.89,216.19 462.35,216.19 462.35,238.41 \"/> </svg> ";
// Exports
/* harmony default export */ var as = (as_code);
;// ./templates/flags/ad.svg
// Module
var ad_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <style>.st0{fill:#FFDA44}.st1{fill:#0052B4}.st2{fill:#D80027}.st3{fill:none;stroke:#D80027;stroke-width:7;stroke-miterlimit:10}.st4{fill:none;stroke:#D80027;stroke-width:4;stroke-miterlimit:10}.st5{fill:#BC8B00}</style> <rect fill=\"#FFDA44\" width=\"513\" height=\"342\"/> <rect fill=\"#0052B4\" width=\"171\" height=\"342\"/> <rect x=\"342\" fill=\"#D80027\" width=\"171\" height=\"342\"/> <rect x=\"198.9\" y=\"113.6\" fill=\"#D80027\" width=\"57.1\" height=\"64.8\"/> <g stroke=\"#D80027\" stroke-width=\"7\"> <line x1=\"267.6\" y1=\"113.6\" x2=\"267.6\" y2=\"178.3\"/> <line x1=\"284.2\" y1=\"113.6\" x2=\"284.2\" y2=\"178.3\"/> <line x1=\"300.8\" y1=\"113.6\" x2=\"300.8\" y2=\"178.3\"/> </g> <g stroke=\"#D80027\" stroke-width=\"5\"> <line x1=\"247.4\" y1=\"178.4\" x2=\"247.4\" y2=\"243.1\"/> <line x1=\"234.1\" y1=\"178.4\" x2=\"234.1\" y2=\"243.1\"/> <line x1=\"220.8\" y1=\"178.5\" x2=\"220.8\" y2=\"232.4\"/> <line x1=\"207.1\" y1=\"178.5\" x2=\"207.1\" y2=\"225.6\"/> </g> <polygon fill=\"#FFDA44\" points=\"199.9,146 227.5,113.6 256,145.9 227.9,178.3 \"/> <path fill=\"#BC8B00\" d=\"M182.2,95.9v92.2c0,34.3,27.3,54.2,48.6,64.5c-0.8,1.4,25.1,8.3,25.1,8.3s25.9-6.9,25.1-8.3\n\tc21.4-10.3,48.6-30.1,48.6-64.5V95.9H182.2z M312.1,188.1c0,16.9-10,29.4-32.8,43.9c-8.6,5.5-17.5,9-23.3,11\n\tc-5.8-2-14.7-5.5-23.3-11c-22.8-14.5-32.8-28-32.8-43.9v-74.5h112.2L312.1,188.1z\"/> <rect x=\"264.4\" y=\"188.9\" fill=\"#D80027\" width=\"29.5\" height=\"8.6\"/> <rect x=\"264.4\" y=\"206.5\" fill=\"#D80027\" width=\"29.5\" height=\"8.6\"/> </svg> ";
// Exports
/* harmony default export */ var ad = (ad_code);
;// ./templates/flags/ao.svg
// Module
var ao_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"100.89 0 342 342\"> <rect fill=\"#000000\" y=\"171\" width=\"513\" height=\"171\"/> <rect fill=\"#D80027\" class=\"st0\" width=\"513\" height=\"171\"/> <g fill=\"#FFDA44\"> <polygon points=\"332.7,135.6 350.2,127 342.8,108 324.1,114.2 \"/> <polygon points=\"287.1,79.6 292.6,60.8 273.5,53.6 265.5,71.6 \"/> <polygon points=\"316.5,102.8 328.7,87.5 313.8,73.6 299.6,87.2 \"/> <polygon points=\"335.9,171.7 355.4,170.9 355.4,149.6 332.5,149.8 \"/> <polygon points=\"323.3,209.8 342.1,215.3 349.4,196.3 331.4,188.3 \"/> <polygon points=\"294.3,242.2 309.2,254.9 323.6,240.5 310.5,225.8 \"/> <polygon points=\"247.3,94.5 257.1,114.4 279,117.7 263.1,133.2 266.7,155.1 247.1,144.7 227.4,154.9 231.2,133\n  \t215.4,117.5 237.4,114.4 \"/> <path d=\"M250.8,61.3l-2.7,18.3c43.1,3.2,77,39.2,77,83.1c0,46-37.3,82.9-83.4,83.4c-29.2,0.3-51.3-14.8-67-33.7\n  \tl-13.9,12.3c15,19,40.8,39.9,80.9,39.9c56.3,0,101.9-45.6,101.9-101.9C343.7,109.5,302.9,65.8,250.8,61.3z\"/> <path fill=\"#000000\" d=\"M291.9,223.4l-11.8,14c0,0,51.9,38.9,53.6,40.4c1.7,1.5,5.2,2,9.1-2.7c3.7-4.5,2.8-8.1,0.9-9.7\n  C341.7,263.9,291.9,223.4,291.9,223.4z\"/> <path d=\"M206.1,157.2c-7.7,10.3-7.5,23.1,2.8,30.9c0,0,135.2,101.5,136.9,103c1.7,1.5,5.2,2,9.1-2.7\n  \tc3.7-4.5,2.8-8.1,0.9-9.7C353.9,277.2,206.1,157.2,206.1,157.2z\"/> <polygon points=\"253.8,256.3 261.1,274.5 280.6,268.5 275.8,249.4 \"/> <polygon points=\"213.4,252.6 211.4,272.1 231.5,275.7 236,256.6 \"/> <polygon points=\"177.7,231.4 167.4,248 183.8,260.1 196.3,244.9 \"/> </g> </svg> ";
// Exports
/* harmony default export */ var ao = (ao_code);
;// ./templates/flags/ai.svg
// Module
var ai_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#00318b\" width=\"513\" height=\"342\"/> <path fill=\"#FFFFFF\" d=\"M454.8,265.38c7.94-10.93,13.24-24.27,13.24-40.42V104.89c-10.04,7.54-22.5,12.01-36.02,12.01\n\tc-19.64,0-37.07-9.43-48.03-24.01c-10.95,14.58-28.39,24.01-48.03,24.01c-13.52,0-25.99-4.47-36.02-12v120.06\n\tc0,16.16,5.3,29.5,13.24,40.42L454.8,265.38L454.8,265.38z\"/> <path fill=\"#8DCCFF\" d=\"M310.23,260.98C332.65,296.96,384,309,384,309s51.35-12.04,73.77-48.02H310.23L310.23,260.98z\"/> <path fill=\"#D87B00\" d=\"M396.66,172.21c0.32,1.42,8.72,10.17,17.56,20.15c1.39,1.57-8.74,12.63-6.91,12.59\n\tc4.75-0.12,19.27-17.26,19.34,6.65c0.05,15.94-30,27.51-30,27.51h17.47l0.65,14.96c0,0,4.97-13.34,7.59-16.57\n\tc8.1-9.97,20.18-30.07,34.59-27.1c14.41,2.97-13.66-13.92-13.66-13.92s-5.2-15.19-16.63-16.97c-10.06-1.57-14.29-2.51-26.64-7.3\n\tC398.34,171.56,396.14,169.89,396.66,172.21z\"/> <path fill=\"#D87B00\" d=\"M359.69,198.69c1.07-0.99,4.46-12.63,8.69-25.28c0.67-1.99,15.31,1.27,14.35-0.3\n\tc-2.47-4.06-24.58-8.08-3.9-20.08c13.79-8,38.82,12.27,38.82,12.27l-8.72-15.14l12.64-8.03c0,0-14.05,2.35-18.14,1.69\n\tc-12.68-2.04-36.13-2.48-40.75-16.45c-4.62-13.97-5.25,18.79-5.25,18.79s-10.56,12.08-6.4,22.88c3.66,9.5,4.96,13.64,6.97,26.73\n\tC358.29,197.56,357.94,200.3,359.69,198.69z\"/> <path fill=\"#D87B00\" d=\"M395.67,219.87c-1.39-0.43-13.16,2.49-26.22,5.18c-2.06,0.42-6.59-13.88-7.47-12.26\n\tc-2.27,4.17,5.36,25.31-15.41,13.45c-13.84-7.91-8.89-39.73-8.89-39.73l-8.71,15.14l-13.29-6.9c0,0,9.09,10.96,10.57,14.84\n\tc4.6,11.99,16,32.49,6.24,43.49c-9.76,11.01,18.88-4.9,18.88-4.9s15.76,3.07,23-5.96c6.38-7.94,9.3-11.14,19.61-19.45\n\tC395.41,221.65,397.95,220.58,395.67,219.87z\"/> <g> <polygon fill=\"#FFFFFF\" points=\"256.5,0 233.4,0 160,48.9 160,0 96,0 96,48.9 22.6,0 0,0 0,22.7 46.1,53.4 0,53.4 0,117.4 46.1,117.4\n\t\t0,148.1 0,171 22.6,171 96,121.8 96,171 160,171 160,121.8 233.4,171 256.5,171 256.5,148.1 209.9,117.4 256.5,117.4\n\t\t256.5,53.4 209.9,53.4 256.5,22.7 \t\"/> <polygon fill=\"#D80027\" points=\"144,0 112,0 112,69.4 0,69.4 0,101.4 112,101.4 112,171 144,171 144,101.4 256.5,101.4 256.5,69.4\n\t\t144,69.4 \t\"/> <polygon fill=\"#D80027\" points=\"0,171 0,159.7 62.5,117.4 85.1,117.4 7.3,171 \t\"/> <polygon fill=\"#D80027\" points=\"7.3,0.1 85.1,53.3 62.5,53.3 0,11.1 0,0.1 \t\"/> <polygon fill=\"#D80027\" points=\"256.5,0.1 256.5,11.1 193.5,53.4 170.9,53.4 248.7,0.1 \t\"/> <polygon fill=\"#D80027\" points=\"248.7,171 170.9,117.4 193.5,117.4 256.5,159.7 256.5,171 \t\"/> </g> </svg> ";
// Exports
/* harmony default export */ var ai = (ai_code);
;// ./templates/flags/aq.svg
// Module
var aq_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"124 0 496 496\"> <path fill=\"#3A7DCE\" d=\"M0,0h744v496H0V0z\"/> <polygon fill=\"#ffffff\" points=\"120,125 210,188 264,174 287,93 348,57 440,71 545,126 551,216 585,228 585,320 520,435 442,459\n\t\t370,442 388,412 380,380 370,389 208,364 159,279 179,233 130,171 \"/> </svg>";
// Exports
/* harmony default export */ var aq = (aq_code);
;// ./templates/flags/ag.svg
// Module
var ag_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" width=\"513\" height=\"342\"/> <path fill=\"#0072c6\" d=\"M88.8,136.5c-2.2,12.9-3.4,26.2-3.4,39.8c0,13.6,1.2,27,3.4,39.8L256,228.3l167.2-12.1\n\tc2.2-12.9,3.4-26.2,3.4-39.8s-1.2-27-3.4-39.8\"/> <path fill=\"#FFFFFF\" d=\"M423.2,219H88.8c15.8,69.8,84.7,122.3,167.2,122.3S407.4,288.8,423.2,219z\"/> <polygon fill=\"#FFDA44\" points=\"365.9,136.5 146.1,136.5 191,115.4 167.1,71.9 215.9,81.3 222,32 256,68.2 290,32 296.1,81.3\n\t344.9,71.9 321,115.4 \"/> <g fill=\"#ce1126\"> <polygon points=\"256.5,342 0,0 0,342 \t\"/> <polygon points=\"513,342 513,0 256,342 \t\"/> </g> </svg> ";
// Exports
/* harmony default export */ var ag = (ag_code);
;// ./templates/flags/ar.svg
// Module
var ar_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#FFFFFF\" width=\"512\" height=\"342\"/> <rect y=\"0\" fill=\"#338AF3\" width=\"512\" height=\"114\"/> <rect y=\"228\" fill=\"#338AF3\" width=\"512\" height=\"114\"/> <circle fill=\"#FFDA44\" stroke=\"#d6ab00\" stroke-width=\"5\" cx=\"256.5\" cy=\"171\" r=\"40\"/> </svg> ";
// Exports
/* harmony default export */ var ar = (ar_code);
;// ./templates/flags/am.svg
// Module
var am_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#0052B4\" width=\"513\" height=\"342\"/> <rect y=\"0\" fill=\"#D80027\" width=\"513\" height=\"114\"/> <rect y=\"228\" fill=\"#FF9811\" width=\"513\" height=\"114\"/> </svg> ";
// Exports
/* harmony default export */ var am = (am_code);
;// ./templates/flags/aw.svg
// Module
var aw_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"0 0 242 242\"> <rect fill=\"#338AF3\" width=\"363\" height=\"242\"/> <g> <polygon fill=\"#FFFFFF\" points=\"57,96.9 14.7,78.2 57,59.6 75.6,17.4 94.2,59.6 136.5,78.2 94.2,96.9 75.6,139.1 \t\"/> <polygon fill=\"#f30028\" points=\"75.6,40.6 87.1,66.7 113.2,78.2 87.1,89.7 75.6,115.8 64.1,89.7 38,78.2 64.1,66.7 \t\"/> </g> <g fill=\"#FFDA44\"> <rect y=\"152.2\" width=\"363\" height=\"15.7\"/> <rect y=\"183.6\" width=\"363\" height=\"15.7\"/> </g> </svg> ";
// Exports
/* harmony default export */ var aw = (aw_code);
;// ./templates/flags/au.svg
// Module
var au_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect fill=\"#10338c\" width=\"513\" height=\"342\"/> <g fill=\"#FFFFFF\"> <path d=\"M222.2,170.7c0.3-0.3,0.5-0.6,0.8-0.9C222.8,170.1,222.5,170.4,222.2,170.7L222.2,170.7z\"/> <polygon points=\"188,212.6 199,235.5 223.7,229.8 212.7,252.6 232.6,268.4 207.8,274 207.9,299.4 188,283.5\n\t\t168.2,299.4 168.3,274 143.5,268.4 163.4,252.6 152.3,229.8 177.1,235.5 \t\"/> <polygon points=\"385.9,241.1 391.1,252 402.9,249.3 397.6,260.2 407.1,267.7 395.3,270.3 395.3,282.5 385.9,274.9\n\t\t376.4,282.5 376.5,270.3 364.7,267.7 374.2,260.2 368.9,249.3 380.7,252 \t\"/> <polygon points=\"337.3,125.1 342.5,136 354.3,133.3 349,144.2 358.5,151.7 346.7,154.4 346.7,166.5 337.3,158.9\n\t\t327.8,166.5 327.9,154.4 316,151.7 325.5,144.2 320.2,133.3 332,136 \t\"/> <polygon points=\"385.9,58.9 391.1,69.8 402.9,67.1 397.6,78 407.1,85.5 395.3,88.2 395.3,100.3 385.9,92.7\n\t\t376.4,100.3 376.5,88.2 364.7,85.5 374.2,78 368.9,67.1 380.7,69.8 \t\"/> <polygon points=\"428.4,108.6 433.6,119.5 445.4,116.8 440.1,127.7 449.6,135.2 437.8,137.8 437.8,150 428.4,142.4\n\t\t418.9,150 418.9,137.8 407.1,135.2 416.6,127.7 411.3,116.8 423.1,119.5 \t\"/> <polygon points=\"398,166.5 402.1,179.2 415.4,179.2 404.6,187 408.8,199.7 398,191.8 387.2,199.7 391.3,187\n\t\t380.6,179.2 393.9,179.2 \t\"/> <polygon points=\"254.8,0 254.8,30.6 209.7,55.7 254.8,55.7 254.8,115 195.7,115 254.8,147.8 254.8,170.7 228.1,170.7\n\t\t154.6,129.8 154.6,170.7 99,170.7 99,122.1 11.6,170.7 -1.2,170.7 -1.2,140.1 44,115 -1.2,115 -1.2,55.7 57.9,55.7 -1.2,22.8\n\t\t-1.2,0 25.5,0 99,40.8 99,0 154.6,0 154.6,48.6 242.1,0 \t\"/> </g> <polygon fill=\"#D80027\" points=\"142.8,0 110.8,0 110.8,69.3 -1.2,69.3 -1.2,101.3 110.8,101.3 110.8,170.7 142.8,170.7 142.8,101.3\n\t254.8,101.3 254.8,69.3 142.8,69.3 \"/> <polygon fill=\"#0052B4\" points=\"154.6,115 254.8,170.7 254.8,154.9 183,115 \"/> <polygon fill=\"#FFFFFF\" points=\"154.6,115 254.8,170.7 254.8,154.9 183,115 \"/> <g fill=\"#D80027\"> <polygon points=\"154.6,115 254.8,170.7 254.8,154.9 183,115 \t\"/> <polygon points=\"70.7,115 -1.2,154.9 -1.2,170.7 -1.2,170.7 99,115 \t\"/> </g> <polygon fill=\"#0052B4\" points=\"99,55.7 -1.2,0 -1.2,15.7 70.7,55.7 \"/> <polygon fill=\"#FFFFFF\" points=\"99,55.7 -1.2,0 -1.2,15.7 70.7,55.7 \"/> <g fill=\"#D80027\"> <polygon points=\"99,55.7 -1.2,0 -1.2,15.7 70.7,55.7 \t\"/> <polygon points=\"183,55.7 254.8,15.7 254.8,0 254.8,0 154.6,55.7 \t\"/> </g> </svg> ";
// Exports
/* harmony default export */ var au = (au_code);
;// ./templates/flags/at.svg
// Module
var at_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"114\" fill=\"#FFFFFF\" width=\"513\" height=\"114\"/> <rect y=\"0\" fill=\"#D80027\" width=\"513\" height=\"114\"/> <rect y=\"228\" fill=\"#D80027\" width=\"513\" height=\"114\"/> </svg> ";
// Exports
/* harmony default export */ var at = (at_code);
;// ./templates/flags/az.svg
// Module
var az_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect fill=\"#ef3340\" width=\"513\" height=\"342\"/> <rect fill=\"#00b5e2\" width=\"513\" height=\"114\"/> <rect y=\"228\" fill=\"#509e2f\" width=\"513\" height=\"114\"/> <g fill=\"#FFFFFF\"> <path d=\"M265.6,212.6c-23,0-41.6-18.6-41.6-41.6s18.6-41.6,41.6-41.6c7.2,0,13.9,1.8,19.8,5\n\t\tc-9.2-9-21.9-14.6-35.8-14.6c-28.3,0-51.2,22.9-51.2,51.2s22.9,51.2,51.2,51.2c13.9,0,26.6-5.6,35.8-14.6\n\t\tC279.5,210.8,272.8,212.6,265.6,212.6z\"/> <polygon points=\"297.6,142.2 303.1,157.7 318,150.6 310.9,165.5 326.4,171 310.9,176.5 318,191.4 303.1,184.3\n\t\t297.6,199.8 292.1,184.3 277.2,191.4 284.3,176.5 268.8,171 284.3,165.5 277.2,150.6 292.1,157.7 \t\"/> </g> </svg> ";
// Exports
/* harmony default export */ var az = (az_code);
;// ./templates/flags/bs.svg
// Module
var bs_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#ffc72c\" width=\"512\" height=\"341.326\"/> <g> <rect y=\"85.337\" fill=\"#00778b\" width=\"512\" height=\"113.775\"/> <rect y=\"312.888\" fill=\"#00778b\" width=\"512\" height=\"113.775\"/> </g> <polygon points=\"256,256.006 0,426.668 0,85.331 \"/> </svg> ";
// Exports
/* harmony default export */ var bs = (bs_code);
;// ./templates/flags/bh.svg
// Module
var bh_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"61.452 0 341.3 341.3\"> <polygon fill=\"#D80027\" points=\"0,0 512,0 512,341.3 0,341.3 \"/> <polygon fill=\"#FFFFFF\" points=\"222.8,34.3 137.6,68.5 222.8,102.6 137.6,136.7 222.8,170.8 137.6,204.9 222.8,239 137.6,273.1\n\t222.8,307.2 137.6,341.3 0,341.3 0,0 137.6,0 \"/> </svg> ";
// Exports
/* harmony default export */ var bh = (bh_code);
;// ./templates/flags/bd.svg
// Module
var bd_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"49.59 85.333 342 342\"> <rect y=\"85.331\" fill=\"#2d6e41\" width=\"513\" height=\"342\"/> <circle fill=\"#F40B32\" cx=\"218.902\" cy=\"256.5\" r=\"115\"/> </svg> ";
// Exports
/* harmony default export */ var bd = (bd_code);
;// ./templates/flags/bb.svg
// Module
var bb_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#ffc726\" width=\"513\" height=\"342\"/> <g fill=\"#00267f\"> <rect y=\"0\" width=\"171\" height=\"342\"/> <rect x=\"342\" y=\"0\" width=\"171\" height=\"342\"/> </g> <path d=\"M325.74,101.02l-31.97,12.4c-0.68,1.35-5.79,7.54-8.18,53.06h-17.05v-60.42l-12.54-27.38l-12.54,27v60.8H226.4\n\tc-2.39-45.53-7.8-52.48-8.47-53.84l-31.68-11.63c0.15,0.31,15.4,31.34,15.4,78.01v12.54h41.81v71.07h25.08v-71.07h41.81v-12.54\n\tc0-24.13,4.17-44.02,7.68-56.46c3.82-13.57,7.7-21.49,7.74-21.57L325.74,101.02z\"/> </svg> ";
// Exports
/* harmony default export */ var bb = (bb_code);
;// ./templates/flags/by.svg
// Module
var by_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" xmlns:xlink=\"http://www.w3.org/1999/xlink\" viewBox=\"3.42 0 342 342\"> <rect fill=\"#007C30\" width=\"513\" height=\"342\"/> <polyline fill=\"#CE1720\" points=\"0,230 513,230 513,0 513,0 0,0 0,0 \"/> <polygon fill=\"#FFFFFF\" points=\"100,230 100,215.1 100,0 0,0 0,342 513,342 513,342 100,342 \"/> <g fill=\"#CE1720\"> <polygon points=\"28,159.6 8.6,128.2 28,97.4 47.3,128.2 \"/> <polygon points=\"72.6,159.6 53.3,128.2 72.6,97.4 92,128.2 \"/> <polygon points=\"28,241.2 8.6,209.8 28,179 47.3,209.8 \"/> <polygon points=\"72.6,241.2 53.3,209.8 72.6,179 92,209.8 \"/> </g> <g fill=\"none\" stroke=\"#CE1720\" stroke-width=\"7\"> <polygon points=\"28,73.8 11.9,47.8 28,22.2 44,47.8 \"/> <polygon points=\"72.6,73.8 56.6,47.8 72.6,22.2 88.7,47.8 \"/> <polygon points=\"28,318 11.9,291.9 28,266.3 44,291.9 \"/> <polygon points=\"72.6,318 56.6,291.9 72.6,266.3 88.7,291.9 \"/> </g> </svg> ";
// Exports
/* harmony default export */ var by = (by_code);
;// ./templates/flags/be.svg
// Module
var be_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#fdda25\" width=\"513\" height=\"342\"/> <rect y=\"0\" width=\"171\" height=\"342\"/> <rect x=\"342\" y=\"0\" fill=\"#ef3340\" width=\"171\" height=\"342\"/> </svg> ";
// Exports
/* harmony default export */ var be = (be_code);
;// ./templates/flags/bz.svg
// Module
var bz_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"93.885 0 341.3 341.3\"> <rect y=\"0\" fill=\"#003e87\" width=\"512\" height=\"341.3\"/> <g> <circle fill=\"#FFFFFF\" cx=\"260.9\" cy=\"170.9\" r=\"118.9\"/> <circle fill=\"none\" stroke=\"#6DA544\" stroke-width=\"18\" stroke-miterlimit=\"10\" cx=\"261.9\" cy=\"173.1\" r=\"94.5\"/> </g> <g stroke=\"#000000\" stroke-width=\"1\"> <path fill=\"#003e87\" d=\"M261.9,151.5l-50.6,23.4v20c0,11.8,6.1,22.8,16.2,28.9l34.5,15.2l34.5-15.2c10-6.2,16.2-17.1,16.2-28.9v-20\n\t\tL261.9,151.5z\"/> <rect x=\"211.3\" y=\"128.1\" fill=\"#FFDA44\" width=\"101.3\" height=\"46.7\"/> </g> <g fill=\"#ce1127\"> <rect y=\"0\" width=\"512\" height=\"35\"/> <rect y=\"306\" width=\"512\" height=\"35\"/> </g> </svg> ";
// Exports
/* harmony default export */ var bz = (bz_code);
;// ./templates/flags/bj.svg
// Module
var bj_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"58.14 85.333 342 342\"> <rect y=\"85.333\" fill=\"#008751\" width=\"513\" height=\"342\"/> <rect x=\"196.666\" y=\"85.333\" fill=\"#fcd116\" width=\"316.334\" height=\"171\"/> <rect x=\"196.666\" y=\"256\" fill=\"#e8112d\" width=\"316.334\" height=\"171\"/> </svg> ";
// Exports
/* harmony default export */ var bj = (bj_code);
;// ./templates/flags/bm.svg
// Module
var bm_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"116.28 0 342 342\"> <rect y=\"0\" fill=\"#c8102e\" width=\"513\" height=\"342\"/> <g> <rect y=\"0.1\" fill=\"#012169\" width=\"256.5\" height=\"171\"/> <polygon fill=\"#FFFFFF\" points=\"256,0 233.4,0 160,48.9 160,0 96,0 96,48.9 22.6,0 0,0 0,22.7 46.1,53.4 0,53.4 0,117.4 46.1,117.4\n\t\t0,148.1 0,170.7 22.6,170.7 96,121.8 96,170.7 160,170.7 160,121.8 233.4,170.7 256,170.7 256,148.1 209.9,117.4 256,117.4\n\t\t256,53.4 209.9,53.4 256,22.7 \t\"/> <polygon fill=\"#c8102e\" points=\"144,0 112,0 112,69.4 0,69.4 0,101.4 112,101.4 112,170.7 144,170.7 144,101.4 256,101.4 256,69.4\n\t\t144,69.4 \t\"/> <polygon fill=\"#c8102e\" points=\"0,170.7 0,159.7 62.5,117.4 85.1,117.4 7.3,170.7 \t\"/> <polygon fill=\"#c8102e\" points=\"7.3,0.1 85.1,53.3 62.5,53.3 0,11.1 0,0.1 \t\"/> <polygon fill=\"#c8102e\" points=\"256,0.1 256,11.1 193.5,53.4 170.9,53.4 248.7,0.1 \t\"/> <polygon fill=\"#c8102e\" points=\"248.7,170.7 170.9,117.4 193.5,117.4 256,159.7 256,170.7 \t\"/> </g> <g transform=\"translate(13 5)\"> <path fill=\"#ffffff\" d=\"M303.9,94.94v99.69c0,59.81,79.75,99.69,79.75,99.69s79.75-39.88,79.75-99.69V94.94H303.9z\"/> <path fill=\"#2F8F22\" d=\"M436.37,254.44H330.93c23.13,25.08,52.72,39.88,52.72,39.88S413.24,279.52,436.37,254.44z\"/> <circle fill=\"#65B5D2\" cx=\"383.65\" cy=\"214.56\" r=\"39.88\"/> <circle fill=\"#c8102e\" cx=\"343.77\" cy=\"194.63\" r=\"19.94\"/> <circle fill=\"#c8102e\" cx=\"423.52\" cy=\"194.63\" r=\"19.94\"/> <circle fill=\"#c8102e\" cx=\"383.65\" cy=\"154.75\" r=\"19.94\"/> </g> </svg> ";
// Exports
/* harmony default export */ var bm = (bm_code);
;// ./templates/flags/bt.svg
// Module
var bt_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.35 0 341.3 341.3\"> <rect fill=\"#FF7415\" width=\"512\" height=\"341.3\"/> <polyline fill=\"#FFDA44\" points=\"512,0 0,0 0,341.3 \"/> <path fill=\"none\" stroke=\"#FFFFFF\" stroke-width=\"42\" stroke-miterlimit=\"10\" d=\"M128.7,255.5c0,0,35,54,67.3,32.4c56.9-37.9-68.9-108.6-2.9-152.6c58.3-38.8,76.6,103.5,137.6,62.8\n\tc59-39.3-64.7-111.4-9.2-148.4c33.4-22.2,67.1,32.6,67.1,32.6\"/> </svg> ";
// Exports
/* harmony default export */ var bt = (bt_code);
;// ./templates/flags/bo.svg
// Module
var bo_code = "<svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"><path fill=\"#d52b1e\" d=\"M0 0h513v114H0z\"/><path fill=\"#f9e300\" d=\"M0 114h513v114H0z\"/><path fill=\"#007934\" d=\"M0 228h513v114H0z\"/></svg> ";
// Exports
/* harmony default export */ var bo = (bo_code);
;// ./templates/flags/ba.svg
// Module
var ba_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" xmlns:xlink=\"http://www.w3.org/1999/xlink\" viewBox=\"85.5 0 342 342\"> <rect fill=\"#10338C\" width=\"513.1\" height=\"342\"/> <polyline fill=\"#F6C500\" points=\"99.9,0 441.9,0 441.9,342 \"/> <g> <polygon fill=\"#FFFFFF\" points=\"19.9,21.1 40.3,6.2 60.9,21 54,0 26.7,0 \t\"/> <polygon fill=\"#FFFFFF\" points=\"92.3,25.5 84.4,1.3 76.6,25.5 51.3,25.5 71.8,40.3 64,64.3 84.4,49.5 105,64.3 97.1,40.3 117.6,25.4\n\t\t\"/> <polygon fill=\"#FFFFFF\" points=\"136.3,69.2 128.5,45.2 120.7,69.2 95.5,69.2 115.9,84.1 108.1,108.1 128.5,93.3 149.1,108.1\n\t\t141.3,84.1 161.7,69.2 \t\"/> <polygon fill=\"#FFFFFF\" points=\"179.4,112.3 171.6,88.2 163.8,112.3 138.5,112.3 159,127.2 151.2,151.2 171.6,136.4 192.2,151.2\n\t\t184.3,127.2 204.8,112.2 \t\"/> <polygon fill=\"#FFFFFF\" points=\"222.5,155.3 214.7,131.3 206.9,155.3 181.5,155.3 202.1,170.3 194.3,194.3 214.7,179.4 235.1,194.3\n\t\t227.3,170.3 247.9,155.3 \t\"/> <polygon fill=\"#FFFFFF\" points=\"265.6,198.4 257.8,174.4 250,198.4 224.6,198.4 245.2,213.3 237.4,237.4 257.8,222.5 278.2,237.4\n\t\t270.4,213.2 290.9,198.4 \t\"/> <polygon fill=\"#FFFFFF\" points=\"308.7,241.5 300.8,217.5 293,241.5 267.7,241.5 288.2,256.3 280.3,280.5 300.9,265.6 321.3,280.4\n\t\t313.5,256.3 334,241.5 \t\"/> <polygon fill=\"#FFFFFF\" points=\"351.7,284.6 343.9,260.4 336.1,284.6 310.8,284.6 331.2,299.4 323.4,323.5 343.9,308.6 364.4,323.5\n\t\t356.6,299.4 377,284.6 \t\"/> <polygon fill=\"#FFFFFF\" points=\"387,303.5 379.1,327.6 353.8,327.7 373.6,342 400.2,342 420.1,327.6 394.8,327.6 \t\"/> </g> </svg> ";
// Exports
/* harmony default export */ var ba = (ba_code);
;// ./templates/flags/bw.svg
// Module
var bw_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#FFFFFF\" width=\"513\" height=\"342\"/> <g fill=\"#6da9d2\"> <rect y=\"238\" width=\"513\" height=\"104\"/> <rect y=\"0\" width=\"513\" height=\"104\"/> </g> <rect y=\"125.5\" width=\"513\" height=\"89.656\"/> </svg> ";
// Exports
/* harmony default export */ var bw = (bw_code);
;// ./templates/flags/br.svg
// Module
var br_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#009b3a\" width=\"513\" height=\"342\"/> <polygon fill=\"#fedf00\" points=\"256.5,19.3 461.4,170.7 256.5,322 50.6,170.7   \"/> <circle fill=\"#FFFFFF\" cx=\"256.5\" cy=\"171\" r=\"80.4\"/> <path fill=\"#002776\" d=\"M215.9,165.7c-13.9,0-27.4,2.1-40.1,6c0.6,43.9,36.3,79.3,80.3,79.3c27.2,0,51.3-13.6,65.8-34.3\n  C297,185.7,258.7,165.7,215.9,165.7z\"/> <path fill=\"#002776\" d=\"M334.9,186c0.9-5,1.5-10.1,1.5-15.4c0-44.4-36-80.4-80.4-80.4c-33.1,0-61.5,20.1-73.9,48.6\n  c10.9-2.2,22.1-3.4,33.6-3.4C262.5,135.5,304.7,154.9,334.9,186z\"/> </svg> ";
// Exports
/* harmony default export */ var br = (br_code);
;// ./templates/flags/io.svg
// Module
var io_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <path fill=\"#FFFFFF\" d=\"M0-0.3h513V342H0V-0.3z\"/> <path fill=\"#0052B4\" d=\"M462.9,198.1c-4,0-14-5-21-9c-11-6-21-12-31.1-12s-20,6-31.1,12c-7,4-16,9-20,9s-14-5-20-9c-11-6-22-12-32.1-12\n\tc-9,0-20,6-31.1,12c-7,4-16,9-20,9s-13-5-20-9c-11-6-22-12-31.1-12c-10,0-21,6-32.1,12c-6,4-16,9-20,9s-13-5-20-9\n\tc-11-6-21-12-31.1-12s-20,6-31.1,12c-7,4-17,9-21,9s-13-5-20-9c-10-6-20-12-30.1-12v22l19,10c11,6,21,12,31.1,12s21-6,32.1-12\n\tc6-4,16-10,20-10s13,6,20,10c11,6,21,12,31.1,12s20-6,31.1-12c7-4,17-10,21-10s13,6,20,10c11,6,21,12,31.1,12s20-6,31.1-12\n\tc7-4,16-10,20-10s14,6,21,10c11,6,21,12,31.1,12s20-6,31.1-12c7-4,16-10,20-10s14,6,20,10c11,6,22,12,32.1,12s20-6,31.1-12l19-10\n\tv-22c-10,0-20,6-30.1,12C475.9,193.1,466.9,198.1,462.9,198.1z M0,31.7l19,10c11,6,21,12,31.1,12s21-6,32.1-12c6-4,16-10,20-10\n\ts13,6,20,10c11,6,21,12,31.1,12s20-6,31.1-12c7-4,17-10,21-10s13,6,20,10c11,6,21,12,31.1,12s20-6,31.1-12c7-4,16-10,20-10\n\ts14,6,21,10c11,6,21,12,31.1,12s20-6,31.1-12c7-4,16-10,20-10s14,6,20,10c11,6,22,12,32.1,12s20-6,31.1-12l19-10v-22\n\tc-10,1-20,6-30.1,12c-7,4-16,9-20,9s-14-5-21-9c-11-6-21-12-31.1-12s-20,6-31.1,12c-7,4-16,9-20,9s-14-5-20-9c-11-6-22-12-32.1-12\n\ts-20,6-31.1,12c-7,4-16,9-20,9s-13-5-20-9c-11-6-22-12-31.1-12s-21,6-32.1,12c-6,4-16,9-20,9s-13-5-20-9c-11-6-21-12-31.1-12\n\ts-20,6-31.1,12c-7,4-17,9-21,9s-13-5-20-9c-10-6-20-11-30.1-12V31.7z M462.9,86.9c-4,0-14-5-21-9c-11-6-21-12-31.1-12s-20,6-31.1,12\n\tc-7,4-16,9-20,9s-14-5-20-9c-11-6-22-12-32.1-12c-9,0-20,6-31.1,12c-7,4-16,9-20,9s-13-5-20-9c-11-6-22-12-31.1-12\n\tc-10,0-21,6-32.1,12c-6,4-16,9-20,9s-13-5-20-9c-11-6-21-12-31.1-12s-20,6-31.1,12c-7,4-17,9-21,9s-13-5-20-9c-10-6-20-12-30.1-12\n\tv22l19,9c11,6,21,12,31.1,12s21-6,32.1-12c6-4,16-9,20-9s13,5,20,9c11,6,21,12,31.1,12s20-6,31.1-12c7-4,17-9,21-9s13,5,20,9\n\tc11,6,21,12,31.1,12s20-6,31.1-12c7-4,16-9,20-9s14,5,21,9c11,6,21,12,31.1,12s20-6,31.1-12c7-4,16-9,20-9s14,5,20,9\n\tc11,6,22,12,32.1,12s20-6,31.1-12l19-9v-22c-10,0-20,6-30.1,12C475.9,81.8,466.9,86.9,462.9,86.9z M513,120.9c-10,1-20,6-30.1,12\n\tc-7,4-16,10-20,10s-14-6-21-10c-11-6-21-12-31.1-12s-20,6-31.1,12c-7,4-16,10-20,10s-14-6-20-10c-11-6-22-12-32.1-12s-20,6-31.1,12\n\tc-7,4-16,10-20,10s-13-6-20-10c-11-6-22-12-31.1-12s-21,6-32.1,12c-6,4-16,10-20,10s-13-6-20-10c-11-6-21-12-31.1-12s-20,6-31.1,12\n\tc-7,4-17,10-21,10s-13-6-20-10c-10-6-20-11-30.1-12v23c4,1,13,5,19,9c11,6,21,12,31.1,12s21-6,32.1-12c6-4,16-9,20-9s13,5,20,9\n\tc11,6,21,12,31.1,12s20-6,31.1-12c7-4,17-9,21-9s13,5,20,9c11,6,21,12,31.1,12s20-6,31.1-12c7-4,16-9,20-9s14,5,21,9\n\tc11,6,21,12,31.1,12s20-6,31.1-12c7-4,16-9,20-9s14,5,20,9c11,6,22,12,32.1,12s20-6,31.1-12c6-4,15-8,19-9V120.9z M462.9,254.2\n\tc-4,0-14-5-21-9c-11-6-21-12-31.1-12s-20,6-31.1,12c-7,4-16,9-20,9s-14-5-20-9c-11-6-22-12-32.1-12c-9,0-20,6-31.1,12\n\tc-7,4-16,9-20,9s-13-5-20-9c-11-6-22-12-31.1-12c-10,0-21,6-32.1,12c-6,4-16,9-20,9s-13-5-20-9c-11-6-21-12-31.1-12s-20,6-31.1,12\n\tc-7,4-17,9-21,9s-13-5-20-9c-10-6-20-12-30.1-12v22l19,9c11,6,21,12,31.1,12s21-6,32.1-12c6-4,16-9,20-9s13,5,20,9\n\tc11,6,21,12,31.1,12s20-6,31.1-12c7-4,17-9,21-9s13,5,20,9c11,6,21,12,31.1,12s20-6,31.1-12c7-4,16-9,20-9s14,5,21,9\n\tc11,6,21,12,31.1,12s20-6,31.1-12c7-4,16-9,20-9s14,5,20,9c11,6,22,12,32.1,12s20-6,31.1-12l19-9v-22c-10,0-20,6-30.1,12\n\tC475.9,249.2,466.9,254.2,462.9,254.2z M513,288.2c-10,1-20,6-30.1,12c-7,4-16,10-20,10s-14-6-21-10c-11-6-21-12-31.1-12\n\ts-20,6-31.1,12c-7,4-16,10-20,10s-14-6-20-10c-11-6-22-12-32.1-12s-20,6-31.1,12c-7,4-16,10-20,10s-13-6-20-10c-11-6-22-12-31.1-12\n\ts-21,6-32.1,12c-6,4-16,10-20,10s-13-6-20-10c-11-6-21-12-31.1-12s-20,6-31.1,12c-7,4-17,10-21,10s-13-6-20-10c-10-6-20-11-30.1-12\n\tv23c4,0,13,5,19,9c11,6,21,12,31.1,12s21-6,32.1-12c6-4,16-9,20-9s13,5,20,9c11,6,21,12,31.1,12s20-6,31.1-12c7-4,17-9,21-9\n\ts13,5,20,9c11,6,21,12,31.1,12s20-6,31.1-12c7-4,16-9,20-9s14,5,21,9c11,6,21,12,31.1,12s20-6,31.1-12c7-4,16-9,20-9s14,5,20,9\n\tc11,6,22,12,32.1,12s20-6,31.1-12c6-4,15-9,19-9V288.2z\"/> <path fill=\"#FFFFFF\" d=\"M0-0.3h256v171H0V-0.3z\"/> <path fill=\"#496E2D\" d=\"M278,170.7\"/> <g fill=\"#A2001D\"> <path d=\"M389.8,119.9H408v78.8h-18.3L389.8,119.9L389.8,119.9z M389.8,237.7H408v75.1h-18.3L389.8,237.7L389.8,237.7z\"/> <path d=\"M0-85.3\"/> <path d=\"M0-85.3\"/> </g> <g fill=\"#D80027\"> <path d=\"M144-0.3h-32v70H0v32h112v69h32v-69h112v-32H144V-0.3z\"/> <path d=\"M0-0.3v15l57,39h23L0-0.3z\"/> <path d=\"M256-0.3v15l-57,39h-23L256-0.3z\"/> <path d=\"M0-0.3v15l57,39h23L0-0.3z\"/> <path d=\"M256-0.3v15l-57,39h-23L256-0.3z\"/> <path d=\"M0,170.7v-15l57-38h23L0,170.7z\"/> <path d=\"M256,170.7v-15l-57-38h-23L256,170.7z\"/> </g> <g fill=\"#2E52B2\"> <path d=\"M0,22.7v31h46L0,22.7z M96-0.3v49l-73-49C23-0.3,96-0.3,96-0.3z\"/> <path d=\"M256,22.7v31h-46L256,22.7z M160-0.3v49l73-49C233-0.3,160-0.3,160-0.3z\"/> <path d=\"M0,22.7v31h46L0,22.7z M96-0.3v49l-73-49C23-0.3,96-0.3,96-0.3z\"/> <path d=\"M256,22.7v31h-46L256,22.7z M160-0.3v49l73-49C233-0.3,160-0.3,160-0.3z\"/> <path d=\"M0,147.7v-30h46L0,147.7z M96,170.7v-49l-73,49H96z\"/> <path d=\"M256,147.7v-30h-46L256,147.7z M160,170.7v-49l73,49H160z\"/> </g> <path fill=\"#5DA51E\" stroke=\"#45602C\" stroke-width=\"4\" stroke-miterlimit=\"10\" d=\"M462.8,91.5h-29.1l25.7-25.7c4.5-4.4,4.5-11.4,0-15.8l-3.4-3.4c-4.4-4.4-11.4-4.4-15.8,0l-25.9,25.9V37.4\n\tc0-6.2-5.1-11.3-11.3-11.3h-4.7c-6.2,0-11.3,5.1-11.3,11.3v31.7l-23.5-23.8c-4.7-4.7-12.2-4.7-16.9,0l-3.6,3.6\n\tc-4.7,4.7-4.7,12.4,0,17.2l25,25.4h-30.6c-6,0-10.9,4.9-10.9,10.9v4.6c0,6,4.9,10.9,10.9,10.9H369l-20.7,20.7\n\tc-4.4,4.4-4.4,11.4,0,15.8l3.4,3.4c4.4,4.4,11.4,4.4,15.8,0l19.6-19.6V167c0,6.1,5.1,11.1,11.3,11.3h4.7c6.2,0,11.3-5.1,11.3-11.3\n\tv-28.3l22.2,22.6c4.7,4.7,12.4,4.7,17.1,0l3.6-3.6c4.7-4.7,4.7-12.4,0-17.2L435.2,118h27.6c5.9,0,10.7-4.9,10.9-10.9v-4.6\n\tC473.7,96.4,468.8,91.5,462.8,91.5z\"/> <path fill=\"#5DA51E\" d=\"M0,0\"/> <path fill=\"#E2DD24\" stroke=\"#525625\" stroke-width=\"4\" stroke-miterlimit=\"10\" d=\"M439.2,249.1h-79.8c0,0-0.9-13.4-6-21.8c-6.2-10.4-13.9-28.1-1.4-36.7c15.9-10.9,33.7,0.8,48.2,0.8\n\tc11.8,0.1,26.9-14,45-0.8c12.2,8.9,3.9,28.2-1.3,36.7C436.9,238.8,439.2,249.1,439.2,249.1z\"/> </svg>";
// Exports
/* harmony default export */ var io = (io_code);
;// ./templates/flags/vg.svg
// Module
var vg_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"112.64 85.333 341.333 341.333\"> <rect y=\"85.334\" fill=\"#0052B4\" width=\"512\" height=\"341.337\"/> <polygon fill=\"#FFFFFF\" points=\"256,85.334 256,202.66 209.931,202.66 256,233.38 256,255.997 233.372,255.997 160,207.078   160,255.997 96,255.997 96,207.078 22.628,255.997 0,255.997 0,233.38 46.069,202.66 0,202.66 0,138.66 46.069,138.66 0,107.951   0,85.334 22.628,85.334 96,134.241 96,85.334 160,85.334 160,134.241 233.372,85.334 \"/> <g> <polygon fill=\"#D80027\" points=\"144,85.33 112,85.33 112,154.663 0,154.663 0,186.663 112,186.663 112,255.997 144,255.997    144,186.663 256,186.663 256,154.663 144,154.663  \"/> <polygon fill=\"#D80027\" points=\"0,85.329 0,100.412 57.377,138.663 80,138.663  \"/> <polygon fill=\"#D80027\" points=\"256,85.329 256,100.412 198.623,138.663 176,138.663  \"/> </g> <polygon fill=\"#2E52B2\" points=\"256,107.951 256,138.663 209.931,138.663 \"/> <g> <polygon fill=\"#D80027\" points=\"0,85.329 0,100.412 57.377,138.663 80,138.663  \"/> <polygon fill=\"#D80027\" points=\"256,85.329 256,100.412 198.623,138.663 176,138.663  \"/> </g> <polygon fill=\"#2E52B2\" points=\"256,107.951 256,138.663 209.931,138.663 \"/> <g> <polygon fill=\"#D80027\" points=\"0,255.997 0,240.915 57.377,202.663 80,202.663  \"/> <polygon fill=\"#D80027\" points=\"256,255.997 256,240.915 198.623,202.663 176,202.663  \"/> </g> <path fill=\"#FFDA44\" d=\"M384,259.706l-46.129,46.129c8.645,16.675,26.051,28.074,46.129,28.074  c20.078,0,37.484-11.4,46.129-28.074L384,259.706z\"/> <path fill=\"#6DA544\" d=\"M332.058,178.084v81.623v0.001l0,0C332.059,299.467,384,311.649,384,311.649  s51.941-12.182,51.942-51.942l0,0v-0.001v-81.622H332.058z\"/> <rect x=\"372.87\" y=\"215.181\" fill=\"#FFFFFF\" width=\"22.261\" height=\"59.359\"/> <circle fill=\"#A2001D\" cx=\"384\" cy=\"215.181\" r=\"11.13\"/> <g> <rect x=\"346.902\" y=\"192.92\" fill=\"#FFDA44\" width=\"14.84\" height=\"14.84\"/> <rect x=\"346.902\" y=\"226.312\" fill=\"#FFDA44\" width=\"14.84\" height=\"14.84\"/> <rect x=\"346.902\" y=\"259.703\" fill=\"#FFDA44\" width=\"14.84\" height=\"14.84\"/> <rect x=\"406.261\" y=\"192.92\" fill=\"#FFDA44\" width=\"14.84\" height=\"14.84\"/> <rect x=\"406.261\" y=\"226.312\" fill=\"#FFDA44\" width=\"14.84\" height=\"14.84\"/> <rect x=\"406.261\" y=\"259.703\" fill=\"#FFDA44\" width=\"14.84\" height=\"14.84\"/> </g> </svg> ";
// Exports
/* harmony default export */ var vg = (vg_code);
;// ./templates/flags/bn.svg
// Module
var bn_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"93.869 0 341.33 341.33\"> <rect y=\"0\" fill=\"#FFDA44\" width=\"512\" height=\"341.33\"/> <polygon fill=\"#000000\" points=\"0,193.74 512,320.93 512,214.26 0,97.08 \"/> <polyline fill=\"#FFFFFF\" points=\"512,234.26 512,147.59 0,20.41 0,107.08 \"/> <g> <path fill=\"#D80027\" stroke=\"#231F20\" stroke-width=\"3\" stroke-miterlimit=\"10\" d=\"M306.4,134.01c3.87,7.14,6.07,15.33,6.07,24.02c0,21.55-13.51,39.94-32.52,47.19v-59.51\n\t\tc5.7-1.19,10-6.37,10-12.59c6.9,0,12.5-5.75,12.5-12.85h-0.42c6.9,0,12.91-5.75,12.91-12.85h-34.99V76.58l-14.99-27.71l-15,27.71\n\t\tv30.84h-34.99c0,7.1,6.01,12.85,12.91,12.85h-0.42c0,7.1,5.6,12.85,12.5,12.85c0,6.22,4.29,11.4,9.99,12.59v61.37\n\t\tc-22.09-5.39-38.48-25.3-38.48-49.05c0-8.7,2.2-16.88,6.07-24.02c-10.96,11.21-17.72,26.53-17.72,43.44\n\t\tc0,34.32,27.82,62.15,62.15,62.15s62.15-27.83,62.15-62.15C324.12,160.54,317.35,145.21,306.4,134.01z\"/> <path fill=\"#D80027\" stroke=\"#231F20\" stroke-width=\"3\" stroke-miterlimit=\"10\" d=\"M198.46,226.81c0,0,20.69,27.71,66.5,27.71s66.5-27.71,66.5-27.71l13.47,28.37c0,0-21.03,27.71-79.97,27.71\n\t\ts-79.97-27.71-79.97-27.71L198.46,226.81z\"/> <polygon fill=\"#D80027\" stroke=\"#231F20\" stroke-width=\"3\" stroke-miterlimit=\"10\" points=\"129.24,133.18 150.12,133.18 175.05,160.44 175.05,233.89 148.77,233.89 148.77,165.52 \t\"/> <polygon fill=\"#D80027\" stroke=\"#231F20\" stroke-width=\"3\" stroke-miterlimit=\"10\" points=\"400,133.18 379.12,133.18 354.19,160.44 354.19,233.89 380.46,233.89 380.46,165.52 \t\"/> </g> </svg> ";
// Exports
/* harmony default export */ var bn = (bn_code);
;// ./templates/flags/bg.svg
// Module
var bg_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#00966e\" width=\"512\" height=\"341.326\"/> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"113.775\"/> <rect y=\"312.888\" fill=\"#d62612\" width=\"512\" height=\"113.775\"/> </svg> ";
// Exports
/* harmony default export */ var bg = (bg_code);
;// ./templates/flags/bf.svg
// Module
var bf_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#3d944f\" width=\"513\" height=\"342\"/> <rect y=\"0\" fill=\"#ef2b2d\" width=\"513\" height=\"171\"/> <polygon fill=\"#FFDA44\" points=\"256,102.6 272.9,154.6 327.6,154.6 283.4,186.8 300.2,238.8 256,206.7 211.8,238.8 228.6,186.8\n  184.4,154.6 239.1,154.6 \"/> </svg> ";
// Exports
/* harmony default export */ var bf = (bf_code);
;// ./templates/flags/bi.svg
// Module
var bi_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect fill=\"#D80027\" width=\"513\" height=\"342\"/> <g fill=\"#47a644\"> <polygon points=\"215.9,170.7 0,314.6 0,26.8 \t\"/> <polygon points=\"513,26.8 513,314.6 297.1,170.7 \t\"/> </g> <polygon fill=\"#0052B4\" points=\"512,26.8 296.1,170.7 512,314.6 512,341.3 471.9,341.3 256,197.4 40.1,341.3 0,341.3 0,314.6\n\t215.9,170.7 0,26.8 0,0 40.1,0 256,143.9 471.9,0 512,0 \"/> <polygon fill=\"#FFFFFF\" points=\"513,26.8 297.1,170.7 513,314.6 513,342 473,342 256,197.4 39,342 0,342 0,314.6 215.9,170.7 0,26.8\n\t\t0,0 40.1,0 256,143.9 471.9,0 513,0 \t\"/> <circle fill=\"#FFFFFF\" cx=\"251.6\" cy=\"170.7\" r=\"100.2\"/> <g fill=\"#D80027\" stroke=\"#47a644\" stroke-width=\"3\"> <polygon points=\"251.4,103.6 258.8,116.5 273.6,116.5 266.2,129.3 273.6,142.1 258.8,142.1 251.4,155 244,142.1\n\t\t229.2,142.1 236.6,129.3 229.2,116.5 244,116.5 \t\"/> <polygon points=\"290.2,170.3 297.6,183.2 312.4,183.2 305,196 312.4,208.8 297.6,208.8 290.2,221.7 282.8,208.8\n\t\t267.9,208.8 275.3,196 267.9,183.2 282.8,183.2 \t\"/> <polygon points=\"213,170.3 220.4,183.2 235.3,183.2 227.9,196 235.3,208.8 220.4,208.8 213,221.7 205.6,208.8\n\t\t190.8,208.8 198.2,196 190.8,183.2 205.6,183.2 \t\"/> </g> </svg> ";
// Exports
/* harmony default export */ var bi = (bi_code);
;// ./templates/flags/kh.svg
// Module
var kh_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#D80027\" width=\"512\" height=\"341.326\"/> <g> <rect y=\"85.337\" fill=\"#0052B4\" width=\"512\" height=\"113.775\"/> <rect y=\"312.888\" fill=\"#0052B4\" width=\"512\" height=\"113.775\"/> </g> <polygon fill=\"#FFFFFF\" points=\"303.49,281.971 303.49,264.162 291.617,264.162 291.617,240.417 279.745,228.545   267.873,240.417 267.873,216.673 256,204.8 244.127,216.673 244.127,240.417 232.255,228.545 220.383,240.417 220.383,264.162   208.51,264.162 208.51,281.971 196.638,281.971 196.638,299.779 315.362,299.779 315.362,281.971 \"/> </svg> ";
// Exports
/* harmony default export */ var kh = (kh_code);
;// ./templates/flags/cm.svg
// Module
var cm_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#ce1126\" width=\"513\" height=\"342\"/> <rect y=\"0\" fill=\"#007a5e\" width=\"171\" height=\"342\"/> <g fill=\"#fcd116\"> <rect x=\"342\" y=\"0\" width=\"171\" height=\"342\"/> <polygon points=\"256,102.2 273.2,155.2 329,155.2 283.9,188 301.1,241 256,208.3 210.9,241 228.1,188 183,155.2\n\t\t238.8,155.2 \t\"/> </g> </svg> ";
// Exports
/* harmony default export */ var cm = (cm_code);
;// ./templates/flags/ca.svg
// Module
var ca_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#FFFFFF\" width=\"513\" height=\"342\"/> <g fill=\"#ff0000\"> <rect y=\"0\" width=\"142\" height=\"342\"/> <rect x=\"371\" y=\"0\" width=\"142\" height=\"342\"/> <polygon points=\"306.5,206 356.9,180.8 331.7,168.2 331.7,143 281.3,168.2 306.5,117.8 281.3,117.8 256.1,80\n\t\t230.9,117.8 205.7,117.8 230.9,168.2 180.5,143 180.5,168.2 155.3,180.8 205.7,206 193.1,231.2 243.5,231.2 243.5,269 268.7,269\n\t\t268.7,231.2 319.1,231.2 \t\"/> </g> </svg> ";
// Exports
/* harmony default export */ var ca = (ca_code);
;// ./templates/flags/cv.svg
// Module
var cv_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"17.1 0 342 342\"> <rect y=\"0\" fill=\"#003893\" width=\"513\" height=\"342\"/> <g> <rect y=\"182.2\" fill=\"#cf2027\" width=\"513\" height=\"41.4\"/> <rect y=\"140.8\" fill=\"#FFFFFF\" width=\"513\" height=\"41.4\"/> <rect y=\"223.6\" fill=\"#FFFFFF\" width=\"513\" height=\"41.4\"/> </g> <g fill=\"#f7d116\" stroke=\"#000000\"> <polygon points=\"150.4,70 157.3,91.1 179.5,91.1 161.5,104.2 168.4,125.3 150.4,112.2 132.5,125.3 139.3,104.2\n\t\t121.4,91.1 143.6,91.1 \t\"/> <polygon points=\"150.4,279.7 157.3,300.8 179.5,300.8 161.5,313.8 168.4,335 150.4,321.9 132.5,335 139.3,313.8\n\t\t121.4,300.8 143.6,300.8 \t\"/> <polygon points=\"52.8,208.8 59.7,229.9 81.9,229.9 63.9,242.9 70.8,264.1 52.8,251 34.9,264.1 41.7,242.9 23.8,229.9\n\t\t46,229.9 \t\"/> <polygon points=\"90.1,91 97,112.1 119.2,112.1 101.2,125.1 108.1,146.3 90.1,133.2 72.2,146.3 79,125.1 61,112.1\n\t\t83.3,112.1 \t\"/> <polygon points=\"23.8,162 46,162 52.8,140.9 59.7,162 81.9,162 63.9,175.1 70.8,196.2 52.8,183.2 34.9,196.2\n\t\t41.7,175.1 \t\"/> <polygon points=\"72.2,310.9 79,289.8 61,276.8 83.3,276.8 90.1,255.6 97,276.8 119.2,276.8 101.2,289.8 108.1,310.9\n\t\t90.1,297.9 \t\"/> <polygon points=\"248,208.8 241.1,229.9 218.9,229.9 236.9,242.9 230,264.1 248,251 266,264.1 259.1,242.9 277.1,229.9\n\t\t254.9,229.9 \t\"/> <polygon points=\"210.7,91 203.9,112.1 181.7,112.1 199.6,125.1 192.8,146.3 210.7,133.2 228.7,146.3 221.8,125.1\n\t\t239.8,112.1 217.6,112.1 \t\"/> <polygon points=\"277.1,162 254.9,162 248,140.9 241.1,162 218.9,162 236.9,175.1 230,196.2 248,183.2 266,196.2\n\t\t259.1,175.1 \t\"/> <polygon points=\"228.7,310.9 221.8,289.8 239.8,276.8 217.6,276.8 210.7,255.6 203.9,276.8 181.7,276.8 199.6,289.8\n\t\t192.8,310.9 210.7,297.9 \t\"/> </g> </svg> ";
// Exports
/* harmony default export */ var cv = (cv_code);
;// ./templates/flags/ky.svg
// Module
var ky_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"117.76 85.333 341.333 341.333\"> <rect y=\"85.334\" fill=\"#0052B4\" width=\"512\" height=\"341.337\"/> <circle fill=\"#6DA544\" cx=\"384\" cy=\"181.79\" r=\"29.682\"/> <rect x=\"332.054\" y=\"174.377\" fill=\"#D80027\" width=\"103.88\" height=\"44.522\"/> <path fill=\"#496E2D\" d=\"M435.942,255.997v-0.001V255.997z\"/> <path fill=\"#FFDA44\" d=\"M332.058,285.678v29.682h15.616c9.364,9.176,22.18,14.84,36.326,14.84  c14.147,0,26.962-5.664,36.326-14.84h0.776h14.84v-29.682H332.058z\"/> <path fill=\"#338AF3\" d=\"M332.058,211.475v44.522c0,39.759,51.942,51.942,51.942,51.942s51.942-12.182,51.942-51.942v-44.522  H332.058z\"/> <g> <path fill=\"#F3F3F3\" d=\"M383.997,235.256c-12.985,0-12.985,11.873-25.97,11.873s-12.985-11.873-25.97-11.873v20.777   c12.985,0,12.985,11.873,25.97,11.873s12.985-11.873,25.97-11.873c12.986,0,12.986,11.873,25.973,11.873   c12.986,0,12.986-11.873,25.973-11.873v-20.777c-12.986,0-12.986,11.873-25.973,11.873   C396.984,247.128,396.984,235.256,383.997,235.256z\"/> <path fill=\"#F3F3F3\" d=\"M383.997,193.666c-12.985,0-12.985,11.873-25.97,11.873s-12.985-11.873-25.97-11.873v20.777   c12.985,0,12.985,11.873,25.97,11.873s12.985-11.873,25.97-11.873c12.986,0,12.986,11.873,25.973,11.873   c12.986,0,12.986-11.873,25.973-11.873v-20.777c-12.986,0-12.986,11.873-25.973,11.873   C396.984,205.539,396.984,193.666,383.997,193.666z\"/> </g> <polygon fill=\"#FFFFFF\" points=\"256,85.334 256,107.951 209.931,138.66 256,138.66 256,202.66 209.931,202.66 256,233.38   256,255.997 233.372,255.997 160,207.078 160,255.997 96,255.997 96,207.078 22.628,255.997 0,255.997 0,233.38 46.069,202.66   0,202.66 0,138.66 46.069,138.66 0,107.951 0,85.334 22.628,85.334 96,134.241 96,85.334 160,85.334 160,134.241 233.372,85.334 \"/> <g> <polygon fill=\"#D80027\" points=\"144,85.329 112,85.329 112,154.663 0,154.663 0,186.663 112,186.663 112,255.997    144,255.997 144,186.663 256,186.663 256,154.663 144,154.663  \"/> <polygon fill=\"#D80027\" points=\"0,85.329 0,100.411 57.377,138.663 80,138.663  \"/> <polygon fill=\"#D80027\" points=\"256,85.329 256,100.411 198.623,138.663 176,138.663  \"/> <polygon fill=\"#D80027\" points=\"0,85.329 0,100.411 57.377,138.663 80,138.663  \"/> <polygon fill=\"#D80027\" points=\"256,85.329 256,100.411 198.623,138.663 176,138.663  \"/> <polygon fill=\"#D80027\" points=\"0,255.997 0,240.915 57.377,202.663 80,202.663  \"/> <polygon fill=\"#D80027\" points=\"256,255.997 256,240.915 198.623,202.663 176,202.663  \"/> </g> </svg> ";
// Exports
/* harmony default export */ var ky = (ky_code);
;// ./templates/flags/cf.svg
// Module
var cf_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"17.07 0 341.3 341.3\"> <rect fill=\"#d21034\" width=\"512\" height=\"341.3\"/> <g fill=\"#3a9927\"> <rect y=\"170.7\" width=\"211.5\" height=\"85.3\"/> <rect x=\"300.5\" y=\"170.7\" width=\"211.5\" height=\"85.3\"/> </g> <polygon fill=\"#003082\" points=\"0,0 0,0 0,85.3 211.5,85.3 211.5,0 300.5,0 300.5,85.3 512,85.3 512,0 512,0 \"/> <g fill=\"#FFFFFF\"> <rect x=\"300.5\" y=\"85.3\" width=\"211.5\" height=\"85.3\"/> <rect y=\"85.3\" width=\"211.5\" height=\"85.3\"/> </g> <g fill=\"#ffce00\"> <polygon points=\"300.5,341.3 211.5,341.3 211.5,256 0,256 0,341.3 0,341.3 512,341.3 512,341.3 512,256 300.5,256 \t\"/> <polygon points=\"105.7,8.6 114.3,34.8 141.8,34.8 119.5,51 128.1,77.2 105.7,61 83.4,77.2 91.9,51 69.6,34.8\n\t\t97.2,34.8 \t\"/> </g> </svg> ";
// Exports
/* harmony default export */ var cf = (cf_code);
;// ./templates/flags/td.svg
// Module
var td_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#FFDA44\" width=\"512\" height=\"341.337\"/> <rect y=\"85.331\" fill=\"#0052B4\" width=\"170.663\" height=\"341.337\"/> <rect x=\"341.337\" y=\"85.331\" fill=\"#D80027\" width=\"170.663\" height=\"341.337\"/> </svg> ";
// Exports
/* harmony default export */ var td = (td_code);
;// ./templates/flags/cl.svg
// Module
var cl_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#D80027\" width=\"513\" height=\"342\"/> <rect x=\"196\" y=\"0\" fill=\"#FFFFFF\" width=\"317\" height=\"171\"/> <rect y=\"0\" fill=\"#0037A1\" width=\"196\" height=\"171\"/> <polygon fill=\"#FFFFFF\" points=\"98,24.5 113.1,71 162,71 122.4,99.7 137.6,146.2 98,117.5 58.4,146.2 73.6,99.7 34,71 82.9,71 \"/> </svg> ";
// Exports
/* harmony default export */ var cl = (cl_code);
;// ./templates/flags/cn.svg
// Module
var cn_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"75.24 0 342 342\"> <rect fill=\"#D80027\" width=\"513\" height=\"342\"/> <g fill=\"#FFDA44\"> <polygon points=\"226.8,239.2 217.1,223.6 199.2,228 211.1,213.9 201.4,198.3 218.5,205.2 230.3,191.1 229,209.5\n\t\t246.1,216.4 228.2,220.8 \t\"/> <polygon points=\"290.6,82 280.5,97.4 292.1,111.7 274.4,106.9 264.3,122.4 263.3,104 245.6,99.2 262.8,92.6\n\t\t261.8,74.2 273.4,88.5 \t\"/> <polygon points=\"236.2,25.4 234.2,43.7 251,51.3 233,55.1 231,73.4 221.8,57.4 203.9,61.2 216.2,47.5 207,31.6\n\t\t223.8,39.1 \t\"/> <polygon points=\"292.8,161.8 277.9,172.7 283.7,190.2 268.8,179.4 253.9,190.4 259.5,172.8 244.6,162.1 263,162\n\t\t268.6,144.4 274.4,161.9 \t\"/> <polygon points=\"115,46.3 132.3,99.8 188.5,99.8 143.1,132.7 160.4,186.2 115,153.2 69.5,186.2 86.9,132.7 41.4,99.8\n\t97.7,99.8 \"/> </g> </svg> ";
// Exports
/* harmony default export */ var cn = (cn_code);
;// ./templates/flags/cx.svg
// Module
var cx_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"99.006 0 341.3 341.3\"> <rect fill=\"#0021ad\" width=\"512\" height=\"341.3\"/> <polyline fill=\"#1c8a42\" points=\"0,0 512,0 512,341.3 \"/> <g fill=\"#FFFFFF\"> <polygon points=\"77.6,272 83.3,283.9 96.1,280.9 90.4,292.8 100.7,300.9 87.9,303.8 87.9,317 77.6,308.7 67.4,317\n\t\t67.4,303.8 54.6,300.9 64.9,292.8 59.1,280.9 72,283.9 \t\"/> <polygon points=\"40.5,173.1 46.2,185 59,182 53.3,193.8 63.6,202 50.7,204.9 50.8,218.1 40.5,209.8 30.2,218.1\n\t\t30.3,204.9 17.4,202 27.8,193.8 22,182 34.8,185 \t\"/> <polygon points=\"77.6,92.2 83.3,104 96.1,101.1 90.4,112.9 100.7,121.1 87.9,124 87.9,137.1 77.6,128.9 67.4,137.1\n\t\t67.4,124 54.6,121.1 64.9,112.9 59.1,101.1 72,104 \t\"/> <polygon points=\"123.7,155.1 129.4,167 142.2,164 136.5,175.9 146.8,184 134,186.9 134,200.1 123.7,191.9 113.5,200.1\n\t\t113.5,186.9 100.7,184 111,175.9 105.3,164 118.1,167 \t\"/> <polygon points=\"90.8,209.1 95.2,222.8 109.7,222.8 98,231.3 102.5,245 90.8,236.6 79.1,245 83.6,231.3 71.9,222.8\n\t\t86.3,222.8 \t\"/> </g> <g> <circle fill=\"#ffc639\" cx=\"267.1\" cy=\"170.7\" r=\"74.5\"/> <path fill=\"#1c8a42\" d=\"M267.1,220.3h24.8c0,0,10.8-19,0-37.2l24.8-24.8l-12.4-24.8h-12.4c0,0-6.2,18.6-31,18.6s-31-18.6-31-18.6\n\t\th-12.4l12.4,24.8l-12.4,24.8l12.4,12.4c0,0,12.4-24.8,37.2-12.4C267.1,183.1,277.6,198.6,267.1,220.3z\"/> </g> <path fill=\"#ffc639\" d=\"M464.4,92.2c0.6-2.9-0.2-17.6-0.2-20.7c0-21.3-13.9-39.4-33.2-45.7c5.9,12,9.2,25.4,9.2,39.7\n\tc0,4.8-0.4,9.5-1.1,14.1c-2.9-4.7-6.6-8.9-11.2-12.6c-17.1-13.6-40.6-14-57.9-2.5c13.4,2.9,26.3,8.9,37.7,18\n\tc9,7.1,16.2,16.8,21.7,26.1c0,0-17.8,10.9-31,15.1s-42.3,7.9-42.3,7.9c72,12,132-36,132-36C481.6,82.2,472.3,91.6,464.4,92.2z\"/> </svg> ";
// Exports
/* harmony default export */ var cx = (cx_code);
;// ./templates/flags/cc.svg
// Module
var cc_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"151.923 0 341.3 341.3\"> <rect y=\"0\" fill=\"#008000\" width=\"512\" height=\"341.3\"/> <g fill=\"#FFDA44\"> <polygon points=\"422.7,252.4 428.9,265.2 442.7,262 436.5,274.8 447.7,283.6 433.8,286.8 433.8,301 422.7,292.1\n\t\t411.6,301 411.6,286.8 397.8,283.6 408.9,274.8 402.7,262 416.6,265.2 \t\"/> <polygon points=\"376.1,116.1 382.3,129 396.1,125.8 389.9,138.6 401.1,147.4 387.2,150.5 387.2,164.8 376.1,155.9\n\t\t365,164.8 365.1,150.5 351.2,147.4 362.3,138.6 356.1,125.8 370,129 \t\"/> <polygon points=\"413.1,38.3 419.3,51.1 433.1,47.9 426.9,60.7 438.1,69.6 424.2,72.7 424.2,86.9 413.1,78 402,86.9\n\t\t402.1,72.7 388.2,69.6 399.3,60.7 393.1,47.9 407,51.1 \t\"/> <polygon points=\"464.9,96.7 471.1,109.5 485,106.3 478.7,119.1 489.9,128 476,131.1 476.1,145.3 464.9,136.4\n\t\t453.8,145.3 453.9,131.1 440,128 451.2,119.1 444.9,106.3 458.8,109.5 \t\"/> <polygon points=\"436.9,164.8 441.8,179.6 457.4,179.6 444.8,188.8 449.6,203.7 436.9,194.5 424.3,203.7 429.1,188.8\n\t\t416.5,179.6 432.1,179.6 \t\"/> </g> <path fill=\"#FFDA44\" d=\"M306.8,254.7c-49.2,0-89.1-39.9-89.1-89.1s39.9-89.1,89.1-89.1c15.3,0,29.8,3.9,42.4,10.7\n\tC329.4,67.9,302.3,56,272.5,56c-60.5,0-109.6,49.1-109.6,109.6S212,275.3,272.5,275.3c29.8,0,56.9-11.9,76.6-31.3\n\tC336.5,250.8,322.1,254.7,306.8,254.7z\"/> <g> <path fill=\"#FFDA44\" d=\"M140.4,59.5C129.7,41,109.7,28.6,86.8,28.6S44,41,33.3,59.5H140.4z\"/> <path fill=\"#FFDA44\" d=\"M140.6,59.9l-53.8,53.8L33.1,59.9c-5.1,9-8.1,19.4-8.1,30.6c0,34.2,27.7,61.9,61.9,61.9s61.9-27.7,61.9-61.9\n\t\tC148.7,79.3,145.7,68.9,140.6,59.9z\"/> <path fill=\"#A2001D\" d=\"M71.4,98.2v52.2c4.9,1.3,10.1,1.9,15.5,1.9s10.5-0.7,15.5-1.9V98.2H71.4z\"/> </g> </svg> ";
// Exports
/* harmony default export */ var cc = (cc_code);
;// ./templates/flags/co.svg
// Module
var co_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFDA44\" width=\"512\" height=\"341.326\"/> <rect y=\"343.096\" fill=\"#D80027\" width=\"512\" height=\"83.567\"/> <rect y=\"256\" fill=\"#0052B4\" width=\"512\" height=\"87.096\"/> </svg> ";
// Exports
/* harmony default export */ var co = (co_code);
;// ./templates/flags/km.svg
// Module
var km_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"5.12 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#0052B4\" width=\"512\" height=\"341.326\"/> <rect y=\"85.337\" fill=\"#FFDA44\" width=\"512\" height=\"85.337\"/> <rect y=\"170.663\" fill=\"#FFFFFF\" width=\"512\" height=\"85.337\"/> <rect y=\"256\" fill=\"#D80027\" width=\"512\" height=\"85.337\"/> <polygon fill=\"#6DA544\" points=\"256,256.006 0,426.668 0,85.331 \"/> <g> <path fill=\"#FFFFFF\" d=\"M68.638,256c0-24.865,17.476-45.638,40.812-50.734c-3.587-0.784-7.308-1.208-11.13-1.208   c-28.688,0-51.942,23.255-51.942,51.942s23.255,51.942,51.942,51.942c3.822,0,7.543-0.425,11.13-1.208   C86.113,301.638,68.638,280.865,68.638,256z\"/> <polygon fill=\"#FFFFFF\" points=\"108.877,211.478 111.639,219.981 120.579,219.981 113.347,225.237 116.109,233.739    108.877,228.484 101.643,233.739 104.407,225.237 97.173,219.981 106.113,219.981  \"/> <polygon fill=\"#FFFFFF\" points=\"108.877,233.739 111.639,242.242 120.579,242.242 113.347,247.497 116.109,256    108.877,250.745 101.643,256 104.407,247.497 97.173,242.242 106.113,242.242  \"/> <polygon fill=\"#FFFFFF\" points=\"108.877,256 111.639,264.503 120.579,264.503 113.347,269.758 116.109,278.261    108.877,273.006 101.643,278.261 104.407,269.758 97.173,264.503 106.113,264.503  \"/> <polygon fill=\"#FFFFFF\" points=\"108.877,278.261 111.639,286.763 120.579,286.763 113.347,292.019 116.109,300.522    108.877,295.267 101.643,300.522 104.407,292.019 97.173,286.763 106.113,286.763  \"/> </g> </svg> ";
// Exports
/* harmony default export */ var km = (km_code);
;// ./templates/flags/ck.svg
// Module
var ck_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 85.333 342 342\"> <rect y=\"85.332\" fill=\"#01237a\" width=\"513\" height=\"342\"/> <g> <polygon fill=\"#FFFFFF\" points=\"384,156.705 389.283,172.959 406.372,172.959 392.546,183.004 397.827,199.258 384,189.211    370.175,199.258 375.455,183.004 361.63,172.959 378.719,172.959  \"/> <polygon fill=\"#FFFFFF\" points=\"313.791,185.786 329.019,193.544 341.103,181.461 338.43,198.34 353.657,206.099    336.778,208.772 334.104,225.652 326.344,210.425 309.466,213.098 321.552,201.014  \"/> <polygon fill=\"#FFFFFF\" points=\"284.71,255.995 300.964,250.714 300.965,233.625 311.009,247.45 327.263,242.168    317.217,255.995 327.263,269.821 311.009,264.541 300.965,278.366 300.965,261.276  \"/> <polygon fill=\"#FFFFFF\" points=\"313.791,326.204 321.55,310.975 309.466,298.891 326.347,301.565 334.104,286.338    336.778,303.217 353.657,305.889 338.43,313.648 341.103,330.53 329.019,318.443  \"/> <polygon fill=\"#FFFFFF\" points=\"384,355.284 378.719,339.031 361.628,339.031 375.455,328.986 370.175,312.732 384,322.776    397.827,312.732 392.546,328.986 406.372,339.031 389.283,339.031  \"/> <polygon fill=\"#FFFFFF\" points=\"454.209,326.204 438.98,318.446 426.897,330.53 429.57,313.648 414.343,305.892    431.222,303.217 433.897,286.338 441.653,301.565 458.534,298.891 446.448,310.976  \"/> <polygon fill=\"#FFFFFF\" points=\"483.29,255.995 467.036,261.276 467.036,278.366 456.991,264.54 440.737,269.821    450.783,255.995 440.737,242.168 456.991,247.45 467.036,233.625 467.036,250.714  \"/> <polygon fill=\"#FFFFFF\" points=\"454.209,185.788 446.452,201.014 458.534,213.098 441.653,210.425 433.897,225.652    431.222,208.772 414.343,206.097 429.57,198.34 426.897,181.462 438.981,193.544  \"/> <path fill=\"#FFFFFF\" d=\"M0,186.665v16h46.069L0,233.377v7.539l57.377-38.252H80L0,255.998h112v-69.334H0z M96,255.996   H22.628L96,207.083V255.996z\"/> <path fill=\"#FFFFFF\" d=\"M176,138.665l80-53.334H144v69.334h112v-16h-46.069L256,107.951v-7.539l-57.377,38.251H176V138.665z    M160,85.333h73.372L160,134.246V85.333z\"/> <path fill=\"#FFFFFF\" d=\"M144,255.998h112l-80-53.334h22.623L256,240.917v-7.539l-46.069-30.713H256v-16H144V255.998z    M160,207.083l73.372,48.913H160V207.083z\"/> <path fill=\"#FFFFFF\" d=\"M112,85.331H0l80,53.334H57.377L0,100.413v7.539l46.069,30.712H0v16h112V85.331z M96,134.246   L22.628,85.333H96V134.246z\"/> </g> <g> <polygon fill=\"#D80027\" points=\"144,85.331 112,85.331 112,154.665 0,154.665 0,186.665 112,186.665 112,255.998    144,255.998 144,186.665 256,186.665 256,154.665 144,154.665  \"/> <polygon fill=\"#D80027\" points=\"80,138.665 0,85.331 0,100.413 57.377,138.665  \"/> <polygon fill=\"#D80027\" points=\"176,138.665 198.623,138.665 256,100.413 256,85.331  \"/> <polygon fill=\"#D80027\" points=\"57.377,202.665 0,240.917 0,255.998 80,202.665  \"/> <polygon fill=\"#D80027\" points=\"176,202.665 256,255.998 256,240.917 198.623,202.665  \"/> </g> </svg> ";
// Exports
/* harmony default export */ var ck = (ck_code);
;// ./templates/flags/cr.svg
// Module
var cr_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <rect y=\"211.473\" fill=\"#D80027\" width=\"512\" height=\"89.043\"/> <g> <rect y=\"352.462\" fill=\"#0052B4\" width=\"512\" height=\"74.207\"/> <rect y=\"85.331\" fill=\"#0052B4\" width=\"512\" height=\"74.207\"/> </g> </svg> ";
// Exports
/* harmony default export */ var cr = (cr_code);
;// ./templates/flags/hr.svg
// Module
var hr_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect fill=\"#FFFFFF\" width=\"513\" height=\"342\"/> <rect fill=\"#D80027\" width=\"513\" height=\"114\"/> <rect y=\"228\" fill=\"#0052B4\" width=\"513\" height=\"114\"/> <g fill=\"#338AF3\"> <polygon points=\"309.3,113.2 309.3,69.2 326.8,54.8 344.5,69.2 344.5,113.2 \t\"/> <polygon points=\"203.7,113.2 203.7,69.2 186.4,54.8 168.5,69.2 168.5,113.2 \t\"/> <polygon points=\"238.9,113.2 238.9,69.2 256.5,54.8 274.1,69.2 274.1,113.2 \t\"/> </g> <g fill=\"#0052B4\"> <polygon points=\"309.3,113.2 274.1,113.2 274.1,69.2 291.6,54.8 309.3,69.2 \"/> <polygon points=\"238.9,113.2 203.7,113.2 203.7,69.2 221.2,54.8 238.9,69.2 \"/> </g> <path stroke=\"#D80027\" stroke-width=\"1\" fill=\"#FFFFFF\" d=\"M168.5,113.2v101.9c0,24.3,14.4,46.2,35.4,59.4c21.3,13.4,42.1,14.7,52.6,14.7c10.5,0,31.4-1.7,52.6-14.8\n\tc21-13,35.4-35.1,35.4-59.3V113.2L168.5,113.2z\"/> <g fill=\"#D80027\"> <rect x=\"168.5\" y=\"113.2\" width=\"35.2\" height=\"35.2\"/> <rect x=\"238.9\" y=\"113.2\" width=\"35.2\" height=\"35.2\"/> <rect x=\"309.3\" y=\"113.2\" width=\"35.2\" height=\"35.2\"/> <rect x=\"203.7\" y=\"148.4\" width=\"35.2\" height=\"35.2\"/> <rect x=\"274.1\" y=\"148.4\" width=\"35.2\" height=\"35.2\"/> <rect x=\"168.5\" y=\"183.6\" width=\"35.2\" height=\"35.2\"/> <rect x=\"203.7\" y=\"218.8\" width=\"35.2\" height=\"35.2\"/> <rect x=\"238.9\" y=\"183.6\" width=\"35.2\" height=\"35.2\"/> <rect x=\"309.3\" y=\"183.6\" width=\"35.2\" height=\"35.2\"/> <rect x=\"274.1\" y=\"218.8\" width=\"35.2\" height=\"35.2\"/> <path d=\"M309.3,274.3c8.6-5.4,16.2-12.3,22-20.3h-22V274.3z\"/> <path d=\"M181.7,254c5.8,8,13.3,14.9,22,20.4V254H181.7z\"/> <path d=\"M238.9,254v33.7c7.2,1.2,13.3,1.5,17.6,1.5c4.3,0,10.4-0.3,17.6-1.6V254H238.9z\"/> </g> </svg> ";
// Exports
/* harmony default export */ var hr = (hr_code);
;// ./templates/flags/cu.svg
// Module
var cu_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"0 0 341.3 341.3\"> <polygon fill=\"#FF9811\" points=\"0,-40.8 0,-40.8 0,382.1 0,382.1 0,170.7 \"/> <rect fill=\"#FFFFFF\" width=\"512\" height=\"341.3\"/> <g> <rect fill=\"#0052B4\" width=\"512\" height=\"68.3\"/> <rect y=\"136.5\" fill=\"#0052B4\" width=\"512\" height=\"68.3\"/> <rect y=\"273.1\" fill=\"#0052B4\" width=\"512\" height=\"68.3\"/> </g> <polygon fill=\"#D80027\" points=\"256,170.7 0,341.3 0,0 \"/> <polygon fill=\"#FFFFFF\" points=\"86.5,111.4 99.2,150.6 140.5,150.6 107.1,174.8 119.9,214.1 86.5,189.9 53.1,214.1 65.9,174.8\n  32.5,150.6 73.7,150.6 \"/> </svg> ";
// Exports
/* harmony default export */ var cu = (cu_code);
;// ./templates/flags/cw.svg
// Module
var cw_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"11.97 0 342 342\"> <rect y=\"0\" fill=\"#002b7f\" width=\"513\" height=\"342\"/> <rect y=\"233.5\" fill=\"#f9e814\" width=\"513\" height=\"51\"/> <g fill=\"#FFFFFF\"> <polygon points=\"168.7,86.5 181.6,126.3 223.4,126.3 189.6,150.8 202.5,190.5 168.7,166 134.9,190.5 147.8,150.8\n\t\t114,126.3 155.8,126.3 \t\"/> <polygon points=\"85.4,32.5 93.2,56.4 118.2,56.4 97.9,71.1 105.7,94.9 85.4,80.2 65.1,94.9 72.9,71.1 52.6,56.4\n\t\t77.7,56.4 \t\"/> </g> </svg> ";
// Exports
/* harmony default export */ var cw = (cw_code);
;// ./templates/flags/cy.svg
// Module
var cy_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect fill=\"#FFFFFF\" width=\"513\" height=\"342\"/> <path fill=\"#DB7D00\" d=\"M141.7,154.7c0,0,0.2,67.1,74.7,65.3l4.5,13.9h8.9c0,0-7.4-41.1,60.1-41.5c0,0,0-27.6,27.6-27.6s41.5,0,41.5,0\n\ts-66-51.8,58.9-118l1.8-13.1c0,0-129.9,71-198.9,57.2c0,0,10.7,42.5-10.8,42.5c-10.8,0-9.7-8.1-32.3-8.1\n\tc-18.7,0-17.3,19.7-26.3,19.5c-8.9-0.2-18.8-12.3-19.6-10.2C131.1,136.7,141.7,154.7,141.7,154.7z\"/> <g fill=\"#006651\"> <path d=\"M237.2,308.1c6.9-5,13-6.6,22.4-8.3s19.4-4.4,24.6-5.8s-17.7,6.6-23.5,8.3\n\t\tC254.9,303.9,237.2,308.1,237.2,308.1z\"/> <path d=\"M275.1,293.4c-1.9-11.9,2.8-24.3,13.5-29.3C291.1,272.7,283.4,287.3,275.1,293.4z\"/> <path d=\"M293.3,287.2c-5.8-9.8,4-22.6,11.1-28.8C307.7,264.4,301.9,282.1,293.3,287.2z\"/> <path d=\"M310.2,279.6c-6.2-8.4,1.1-23.2,8.8-29C322.1,258.8,319.1,273.8,310.2,279.6z\"/> <path d=\"M327.1,269c-5.6-8-1.7-20.4,6.3-28.4C339.2,247.2,334.3,261.6,327.1,269z\"/> <path d=\"M340.6,258.3c-4.7-7.5,1.1-25.4,8.6-30.4C352.5,234.5,350,253.3,340.6,258.3z\"/> <path d=\"M351.4,255.5c-1.4-10.8,17.4-22.7,25.2-22.4C375.7,242,367.7,251.7,351.4,255.5z\"/> <path d=\"M340.9,267.7c8.8-9.1,26-9.1,32.1-7.2C371.3,265.8,351.1,277.4,340.9,267.7z\"/> <path d=\"M328.7,276.8c12.4-3.3,20.5-6.1,27.9,1.7C351.4,285.1,331.2,283.2,328.7,276.8z\"/> <path d=\"M311,284.8c11.9-6.4,26.3,3,28.5,8.6C326.2,298.9,310.8,286.2,311,284.8z\"/> <path d=\"M294.7,294c10.8-4.1,23.2,1.4,28.2,7.5C317.1,304.2,301.9,307.2,294.7,294z\"/> <path d=\"M279.8,298.7c12.4-1.4,24.4,8,27,13.4C290.9,313.6,284.8,308.9,279.8,298.7z\"/> <path d=\"M275.8,308.1c-6.9-5-13-6.6-22.4-8.3c-9.4-1.7-19.4-4.4-24.6-5.8c-5.3-1.4,17.7,6.6,23.5,8.3\n\t\tC258.1,303.9,275.8,308.1,275.8,308.1z\"/> <path d=\"M237.9,293.4c1.9-11.9-2.8-24.3-13.5-29.3C221.9,272.7,229.6,287.3,237.9,293.4z\"/> <path d=\"M219.7,287.2c5.8-9.8-4-22.6-11.1-28.8C205.3,264.4,211.1,282.1,219.7,287.2z\"/> <path d=\"M202.8,279.6c6.2-8.4-1.1-23.2-8.8-29C190.9,258.8,193.9,273.8,202.8,279.6z\"/> <path d=\"M185.9,269c5.6-8,1.7-20.4-6.3-28.4C173.8,247.2,178.7,261.6,185.9,269z\"/> <path d=\"M172.4,258.3c4.7-7.5-1.1-25.4-8.6-30.4C160.5,234.5,163,253.3,172.4,258.3z\"/> <path d=\"M161.6,255.5c1.4-10.8-17.4-22.7-25.2-22.4C137.3,242,145.3,251.7,161.6,255.5z\"/> <path d=\"M172.1,267.7c-8.8-9.1-26-9.1-32.1-7.2C141.7,265.8,161.9,277.4,172.1,267.7z\"/> <path d=\"M184.3,276.8c-12.4-3.3-20.5-6.1-27.9,1.7C161.6,285.1,181.8,283.2,184.3,276.8z\"/> <path d=\"M202,284.8c-11.9-6.4-26.3,3-28.5,8.6C186.8,298.9,202.2,286.2,202,284.8z\"/> <path d=\"M218.3,294c-10.8-4.1-23.2,1.4-28.2,7.5C195.9,304.2,211.1,307.2,218.3,294z\"/> <path d=\"M233.2,298.7c-12.4-1.4-24.4,8-27,13.4C222.1,313.6,228.2,308.9,233.2,298.7z\"/> </g> </svg> ";
// Exports
/* harmony default export */ var cy = (cy_code);
;// ./templates/flags/cz.svg
// Module
var cz_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"5.12 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#11457e\" width=\"512\" height=\"341.326\"/> <polygon fill=\"#d7141a\" points=\"512,256 512,426.663 0,426.663 215.185,256 \"/> <polygon fill=\"#FFFFFF\" points=\"512,85.337 512,256 215.185,256 0,85.337 \"/> </svg> ";
// Exports
/* harmony default export */ var cz = (cz_code);
;// ./templates/flags/cd.svg
// Module
var cd_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"0 0 341.3 341.3\"> <rect y=\"0\" fill=\"#338AF3\" width=\"512\" height=\"341.3\"/> <polygon fill=\"#FFDA44\" points=\"512,66.9 512,0 411.7,0 0,274.4 0,341.3 100.3,341.3 \"/> <polygon fill=\"#D80027\" points=\"512,0 512,40.1 60.2,341.3 0,341.3 0,301.2 451.8,0 \"/> <polygon fill=\"#FFDA44\" points=\"93.6,31.2 109.9,81.6 163,81.6 120.1,112.8 136.5,163.3 93.6,132.1 50.6,163.3 67,112.8 24.1,81.6\n\t77.2,81.6 \"/> </svg> ";
// Exports
/* harmony default export */ var cd = (cd_code);
;// ./templates/flags/dk.svg
// Module
var dk_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"23.94 0 342 342\"> <rect fill=\"#c60c30\" width=\"513\" height=\"342\"/> <polygon fill=\"#FFFFFF\" points=\"190,0 130,0 130,140 0,140 0,200 130,200 130,342 190,342 190,200 513,200 513,140\n\t190,140 \"/> </svg> ";
// Exports
/* harmony default export */ var dk = (dk_code);
;// ./templates/flags/dj.svg
// Module
var dj_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"13.656 0 341.3 341.3\"> <rect y=\"0\" fill=\"#12ad2b\" width=\"512\" height=\"341.3\"/> <polygon fill=\"#6ab2e7\" points=\"512,0 512,166.7 0,170.8 0,0 \"/> <polygon fill=\"#FFFFFF\" points=\"256,170.7 0,341.3 0,0 \"/> <polygon fill=\"#d7141a\" points=\"89.8,92.5 106.8,144.9 162,144.9 117.4,177.4 134.4,229.8 89.8,197.4 45.2,229.8 62.2,177.4\n\t17.6,144.9 72.8,144.9 \"/> </svg> ";
// Exports
/* harmony default export */ var dj = (dj_code);
;// ./templates/flags/dm.svg
// Module
var dm_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.35 0 341.3 341.3\"> <rect y=\"0\" fill=\"#496E2D\" width=\"512\" height=\"341.3\"/> <polygon points=\"0,110.7 0,150.7 0,190.7 0,230.7 512,230.7 512,190.7 512,150.7 512,110.7 \"/> <rect y=\"110.7\" fill=\"#FFDA44\" width=\"512\" height=\"40\"/> <rect y=\"190.7\" fill=\"#FFFFFF\" width=\"512\" height=\"40\"/> <rect x=\"196\" y=\"0\" width=\"120\" height=\"341.3\"/> <rect x=\"196\" y=\"0\" fill=\"#FFDA44\" width=\"40\" height=\"341.3\"/> <rect x=\"274.7\" y=\"0\" fill=\"#FFFFFF\" width=\"40\" height=\"341.3\"/> <g> <circle fill=\"#D80027\" cx=\"256\" cy=\"170.7\" r=\"123.1\"/> <g fill=\"#496E2D\"> <polygon points=\"256,58.6 260.6,72.8 275.6,72.8 263.5,81.7 268.1,95.9 256,87.1 243.9,95.9 248.5,81.7 236.4,72.8\n\t\t\t251.4,72.8 \t\t\"/> <polygon points=\"190.1,80 202.3,88.8 214.4,80 209.8,94.3 221.9,103.1 206.9,103.1 202.3,117.3 197.6,103.1\n\t\t\t182.6,103.1 194.7,94.3 \t\t\"/> <polygon points=\"149.4,136 164.4,136 169,121.8 173.7,136 188.7,136 176.5,144.8 181.2,159.1 169,150.3 156.9,159.1\n\t\t\t161.5,144.8 \t\t\"/> <polygon points=\"149.4,205.3 161.5,196.5 156.9,182.2 169,191 181.2,182.2 176.5,196.5 188.7,205.3 173.7,205.3\n\t\t\t169,219.6 164.4,205.3 \t\t\"/> <polygon points=\"190.1,261.4 194.7,247.1 182.6,238.3 197.6,238.3 202.3,224 206.9,238.3 221.9,238.3 209.8,247.1\n\t\t\t214.4,261.4 202.3,252.5 \t\t\"/> <polygon points=\"256,282.8 251.4,268.5 236.4,268.5 248.5,259.7 243.9,245.4 256,254.2 268.1,245.4 263.5,259.7\n\t\t\t275.6,268.5 260.6,268.5 \t\t\"/> <polygon points=\"321.9,261.4 309.7,252.5 297.6,261.4 302.2,247.1 290.1,238.3 305.1,238.3 309.7,224 314.4,238.3\n\t\t\t329.4,238.3 317.3,247.1 \t\t\"/> <polygon points=\"362.6,205.3 347.6,205.3 343,219.6 338.3,205.3 323.3,205.3 335.5,196.5 330.8,182.2 343,191\n\t\t\t355.1,182.2 350.5,196.5 \t\t\"/> <polygon points=\"362.6,136 350.5,144.8 355.1,159.1 343,150.3 330.8,159.1 335.5,144.8 323.3,136 338.3,136\n\t\t\t343,121.8 347.6,136 \t\t\"/> <polygon points=\"321.9,80 317.3,94.3 329.4,103.1 314.4,103.1 309.7,117.3 305.1,103.1 290.1,103.1 302.2,94.3\n\t\t\t297.6,80 309.7,88.8 \t\t\"/> </g> <path fill=\"#496E2D\" d=\"M279.3,168.7c-11-21.1-14.5-25.1-14.5-25.1s0.4-9.7,0.4-15.6c0-8.8-7.4-15.8-16.5-15.8\n\t\tc-8.6,0-15.7,2.9-16.5,11c-4.2,0.9-8.6,4.1-8.6,10.7c0,4.8,1,7.3,5.2,9.3c2.1-4.6,4.3-4.8,9.3-6.4c0.8,0.6,1.7,3,2.6,3.4l0.3,1\n\t\tc0,0-13.3,6.6-13.3,30.9c0,29.5,22,45.4,22,45.4l-1.8,0.3l-1.9,7.1h22v-7.2l11,17.5C279.3,234.9,289.2,187.6,279.3,168.7z\"/> </g> </svg> ";
// Exports
/* harmony default export */ var dm = (dm_code);
;// ./templates/flags/do.svg
// Module
var do_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.35 0 341.3 341.3\"> <rect y=\"0\" fill=\"#FFFFFF\" width=\"512\" height=\"341.3\"/> <rect y=\"215.2\" fill=\"#D80027\" width=\"211.5\" height=\"126.1\"/> <rect y=\"0\" fill=\"#0052B4\" width=\"211.5\" height=\"126.2\"/> <rect x=\"300.5\" y=\"0\" fill=\"#D80027\" width=\"211.5\" height=\"126.2\"/> <rect x=\"300.5\" y=\"215.2\" fill=\"#0052B4\" width=\"211.5\" height=\"126.1\"/> <g stroke=\"#FFFFFF\" stroke-width=\"5\" stroke-miterlimit=\"10\"> <path fill=\"#0052B4\" d=\"M256,130h-49.9v49.4c0,0,19.5,6,49.9,6V130z\"/> <path fill=\"#D80027\" d=\"M206.1,179.4v6c0,27.5,22.3,49.9,49.9,49.9v-49.9C225.6,185.4,206.1,179.4,206.1,179.4z\"/> <path fill=\"#0052B4\" d=\"M256,235.3c27.5,0,49.9-22.3,49.9-49.9v-6c0,0-19.5,6-49.9,6V235.3z\"/> <path fill=\"#D80027\" d=\"M256,130v55.4c30.4,0,49.9-6,49.9-6V130H256z\"/> </g> </svg> ";
// Exports
/* harmony default export */ var flags_do = (do_code);
;// ./templates/flags/tl.svg
// Module
var tl_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"0 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#D80027\" width=\"512\" height=\"341.326\"/> <polygon fill=\"#FFDA44\" points=\"256,256 0,90.691 0,134.933 155.826,256 0,377.067 0,421.309 \"/> <polygon points=\"0,90.691 0,421.309 189.217,256 \"/> <polygon fill=\"#FFFFFF\" points=\"44.184,213.36 69.096,236.937 99.217,220.527 84.494,251.507 109.405,275.082   75.393,270.652 60.67,301.63 54.374,267.914 20.362,263.484 50.481,247.076 \"/> </svg> ";
// Exports
/* harmony default export */ var tl = (tl_code);
;// ./templates/flags/ec.svg
// Module
var ec_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.35 0 341.3 341.3\"> <rect y=\"0\" fill=\"#FFDA44\" width=\"512\" height=\"341.3\"/> <rect y=\"170.7\" fill=\"#0052B4\" width=\"512\" height=\"85.3\"/> <rect y=\"256\" fill=\"#D80027\" width=\"512\" height=\"85.3\"/> <circle fill=\"#FFDA44\" cx=\"256\" cy=\"170.7\" r=\"59.4\"/> <path fill=\"#4C1F00\" d=\"M369.2,79.9c0,0-27.2-13.8-33.9-16c-6.7-2.2-49.1-13.2-49.1-7c0,10.3-13.5,12-15.5,12s-2-5.3-14.8-5.3\n\ts-13.7,4.8-15.8,4.8c-2.2,0-14.5-1-14.5-11.5c0-5.2-42.9,4-49.1,7c-6.1,3.1-33.9,16-33.9,16s45.8,2.7,53.9,6.2s43.8,16.5,43.8,16.5\n\tl-2.8,13.3h37.8l-3.8-13.3c0,0,35.1-12.7,43.8-16.5S369.2,79.9,369.2,79.9z\"/> <path fill=\"#57BA17\" d=\"M217.7,171.7c0,21.1,17.2,38.3,38.3,38.3c21.1,0,38.3-17.2,38.3-38.3v-11.5h-76.5V171.7z\"/> <path fill=\"#338AF3\" d=\"M256,110.5c-21.1,0-38.3,17.2-38.3,38.3v11.5h76.5v-11.5C294.3,127.7,277.1,110.5,256,110.5z\"/> </svg> ";
// Exports
/* harmony default export */ var ec = (ec_code);
;// ./templates/flags/eg.svg
// Module
var eg_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.35 0 341.3 341.3\"> <rect y=\"0\" fill=\"#FFFFFF\" width=\"512\" height=\"341.3\"/> <rect y=\"0\" fill=\"#D80027\" width=\"512\" height=\"113.8\"/> <rect y=\"227.6\" width=\"512\" height=\"113.8\"/> <path fill=\"#C09300\" d=\"M220.3,204.4c0,0,0-58.4,4.5-64.7c3.1-4.3,16.8,5.2,22.7,4.5c0,0,4.2-7.5,4.5-12c0.3-4.6-1.1-7.6-4.9-6.2\n\tc0,0-1.2-2.1,0.5-3.3c1.6-1.2,5.6,0.1,5.6,0.1s-0.5-1,1.6-0.9c2.9,0.2,7.2,1.4,7.4,5.6c0.2,3.1,0.3,7.7,0.4,8.7\n\tc0.7,6.8,2.7,8.7,2.7,8.7s18.4-9.2,22-5.2c3.3,3.8,4.5,64.7,4.5,64.7l-18.1-16.8l12.1,29.5c0,0-14.4,2.4-28.9,2.4\n\tc-14.5,0-31.1-4.2-31.1-4.2l13.8-28.2L220.3,204.4z\"/> </svg> ";
// Exports
/* harmony default export */ var eg = (eg_code);
;// ./templates/flags/sv.svg
// Module
var sv_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <g> <rect y=\"85.337\" fill=\"#0052B4\" width=\"512\" height=\"113.775\"/> <rect y=\"312.888\" fill=\"#0052B4\" width=\"512\" height=\"113.775\"/> </g> <polygon fill=\"#FFDA44\" points=\"228.582,261.936 256,214.447 283.418,261.936 \"/> <polygon fill=\"#6DA544\" points=\"291.616,277.616 256,295.425 220.384,277.616 220.384,253.872 291.616,253.872 \"/> <path fill=\"#FFDA44\" d=\"M289.579,216.485l-12.592,12.592c5.37,5.372,8.693,12.791,8.693,20.988  c0,16.392-13.289,29.68-29.68,29.68c-16.392,0-29.68-13.289-29.68-29.68c0-8.195,3.322-15.616,8.693-20.988l-12.592-12.592  c-8.594,8.594-13.91,20.466-13.91,33.579c0,26.228,21.261,47.489,47.489,47.489s47.489-21.261,47.489-47.489  C303.489,236.95,298.173,225.077,289.579,216.485z\"/> </svg> ";
// Exports
/* harmony default export */ var sv = (sv_code);
;// ./templates/flags/gq.svg
// Module
var gq_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"34.2 0 342 342\"> <rect y=\"0\" fill=\"#FFFFFF\" width=\"513\" height=\"342\"/> <rect y=\"0\" fill=\"#6DA544\" width=\"513\" height=\"113.8\"/> <rect y=\"227.6\" fill=\"#D80027\" width=\"513\" height=\"114.4\"/> <polygon fill=\"#0070C8\" points=\"126,170.7 0,341.3 0,0 \"/> <path fill=\"none\" stroke=\"#000000\" stroke-miterlimit=\"10\" d=\"M233.8,139.4v40.4c0,35.6,35.6,35.6,35.6,35.6s35.6,0,35.6-35.6v-40.4H233.8z\"/> <polygon fill=\"#786145\" points=\"264.5,179.8 274.3,179.8 278.3,205.6 260.5,205.6 \"/> <path fill=\"#6DA544\" d=\"M287.2,162c0-9.8-8-14.8-17.8-14.8s-17.8,5-17.8,14.8c-4.9,0-8.9,4-8.9,8.9s4,8.9,8.9,8.9c2.9,0,32.6,0,35.6,0\n\tc4.9,0,8.9-4,8.9-8.9S292.1,162,287.2,162z\"/> <g fill=\"#FFDA00\" stroke=\"#000000\" stroke-miterlimit=\"10\"> <polygon points=\"230.7,120 232.6,123.3 236.4,123.3 234.5,126.6 236.4,129.8 232.6,129.8 230.7,133.1 228.8,129.8\n\t\t225,129.8 226.9,126.6 225,123.3 228.8,123.3 \t\"/> <polygon points=\"246,120 247.9,123.3 251.6,123.3 249.7,126.6 251.6,129.8 247.9,129.8 246,133.1 244.1,129.8\n\t\t240.3,129.8 242.2,126.6 240.3,123.3 244.1,123.3 \t\"/> <polygon points=\"261.3,120 263.2,123.3 266.9,123.3 265,126.6 266.9,129.8 263.2,129.8 261.3,133.1 259.4,129.8\n\t\t255.6,129.8 257.5,126.6 255.6,123.3 259.4,123.3 \t\"/> <polygon points=\"277.1,120 279,123.3 282.8,123.3 280.9,126.6 282.8,129.8 279,129.8 277.1,133.1 275.2,129.8\n\t\t271.5,129.8 273.3,126.6 271.5,123.3 275.2,123.3 \t\"/> <polygon points=\"293.1,120 295,123.3 298.8,123.3 296.9,126.6 298.8,129.8 295,129.8 293.1,133.1 291.2,129.8\n\t\t287.5,129.8 289.3,126.6 287.5,123.3 291.2,123.3 \t\"/> <polygon points=\"308.1,120 310,123.3 313.7,123.3 311.8,126.6 313.7,129.8 310,129.8 308.1,133.1 306.2,129.8\n\t\t302.4,129.8 304.3,126.6 302.4,123.3 306.2,123.3 \t\"/> </g> </svg> ";
// Exports
/* harmony default export */ var gq = (gq_code);
;// ./templates/flags/er.svg
// Module
var er_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"22.191 0 341.3 341.3\"> <rect y=\"0\" fill=\"#338AF3\" width=\"512\" height=\"341.3\"/> <rect y=\"0\" fill=\"#56AF35\" width=\"512\" height=\"170.7\"/> <path fill=\"#D80027\" d=\"M0,341.3c0,0,512-170.7,512-170.7L0,0V341.3z\"/> <g fill=\"#ffc945\"> <path d=\"M134.7,231.5c33.6,0,60.8-27.2,60.8-60.8s-27.2-60.8-60.8-60.8s-60.8,27.2-60.8,60.8S101.1,231.5,134.7,231.5z\n\t\t M134.7,255.8c-47,0-85.2-38.1-85.2-85.2s38.1-85.2,85.2-85.2s85.2,38.1,85.2,85.2S181.7,255.8,134.7,255.8z\"/> <circle cx=\"119.5\" cy=\"148.3\" r=\"17.5\"/> <circle cx=\"148.9\" cy=\"158.5\" r=\"17.5\"/> <circle cx=\"134.7\" cy=\"135.2\" r=\"17.5\"/> <circle cx=\"119.5\" cy=\"172.7\" r=\"17.5\"/> <circle cx=\"149.9\" cy=\"182.8\" r=\"17.5\"/> <circle cx=\"122.5\" cy=\"198\" r=\"17.5\"/> <circle cx=\"145.9\" cy=\"205.2\" r=\"17.5\"/> </g> </svg> ";
// Exports
/* harmony default export */ var er = (er_code);
;// ./templates/flags/ee.svg
// Module
var ee_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#111111\" width=\"512\" height=\"341.326\"/> <rect y=\"85.337\" fill=\"#368FD8\" width=\"512\" height=\"113.775\"/> <rect y=\"312.888\" fill=\"#FFFFFF\" width=\"512\" height=\"113.775\"/> </svg> ";
// Exports
/* harmony default export */ var ee = (ee_code);
;// ./templates/flags/et.svg
// Module
var et_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"3.75 0 15 15\"> <path fill=\"#20AA46\" d=\"M0,0h22.5v5H0V0z\"/> <path fill=\"#E92F3B\" d=\"M0,10h22.5v5H0V10z\"/> <path fill=\"#FADF50\" d=\"M0,5h22.5v5H0V5z\"/> <circle fill=\"#205CCA\" cx=\"11.3\" cy=\"7.5\" r=\"5.2\"/> <g stroke=\"#FFDB3D\" fill=\"none\"> <g stroke-width=\"0.5\"> <path d=\"M11.3,8.8l-2.1,1.5L10,7.9L8,6.4h2.5l0.8-2.3l0.8,2.3l2.6,0l-2.1,1.5l0.8,2.4L11.3,8.8z\"/> <line x1=\"10.3\" y1=\"6.4\" x2=\"12.2\" y2=\"6.4\"/> <line x1=\"9.9\" y1=\"7.8\" x2=\"11.2\" y2=\"8.8\"/> <line x1=\"12\" y1=\"6.2\" x2=\"12.7\" y2=\"8\"/> <line x1=\"10\" y1=\"7.9\" x2=\"10.6\" y2=\"6.1\"/> <line x1=\"11\" y1=\"9\" x2=\"12.6\" y2=\"7.9\"/> </g> <g stroke-width=\"0.25\"> <line x1=\"8.7\" y1=\"3.9\" x2=\"9.8\" y2=\"5.5\"/> <line x1=\"13.8\" y1=\"3.8\" x2=\"12.8\" y2=\"5.4\"/> <line x1=\"11.3\" y1=\"10.1\" x2=\"11.3\" y2=\"12\"/> <line x1=\"8.9\" y1=\"8.3\" x2=\"7.1\" y2=\"9\"/> <line x1=\"15.5\" y1=\"9\" x2=\"13.7\" y2=\"8.3\"/> </g> </g> </svg> ";
// Exports
/* harmony default export */ var et = (et_code);
;// ./templates/flags/fk.svg
// Module
var fk_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.35 0 341.3 341.3\"> <rect fill=\"#0052B4\" width=\"512\" height=\"341.3\"/> <polygon fill=\"#FFFFFF\" points=\"256,0 256,30.6 210.8,55.7 256,55.7 256,115 196.9,115 256,147.8 256,170.7 229.3,170.7 155.8,129.8\n\t155.8,170.7 100.2,170.7 100.2,122.1 12.7,170.7 0,170.7 0,140.1 45.2,115 0,115 0,55.7 59.1,55.7 0,22.8 0,0 26.7,0 100.2,40.8\n\t100.2,0 155.8,0 155.8,48.6 243.3,0 \"/> <polygon fill=\"#D80027\" points=\"144,0 112,0 112,69.3 0,69.3 0,101.3 112,101.3 112,170.7 144,170.7 144,101.3 256,101.3 256,69.3\n\t144,69.3 \"/> <polygon fill=\"#0052B4\" points=\"155.8,115 256,170.7 256,154.9 184.2,115 \"/> <polygon fill=\"#FFFFFF\" points=\"155.8,115 256,170.7 256,154.9 184.2,115 \"/> <g> <polygon fill=\"#D80027\" points=\"155.8,115 256,170.7 256,154.9 184.2,115 \t\"/> <polygon fill=\"#D80027\" points=\"71.8,115 0,154.9 0,170.7 0,170.7 100.2,115 \t\"/> </g> <polygon fill=\"#0052B4\" points=\"100.2,55.6 0,0 0,15.7 71.8,55.6 \"/> <polygon fill=\"#FFFFFF\" points=\"100.2,55.6 0,0 0,15.7 71.8,55.6 \"/> <g> <polygon fill=\"#D80027\" points=\"100.2,55.6 0,0 0,15.7 71.8,55.6 \t\"/> <polygon fill=\"#D80027\" points=\"184.2,55.6 256,15.7 256,0 256,0 155.8,55.6 \t\"/> </g> <g transform=\"translate(13 4)\"> <path fill=\"#1F8BDE\" d=\"M299.2,190.2v-90c0-8.5,6.5-15.3,15-15.3h120.5c8.3,0,15,6.8,15,15.3v90\n\t\t\tc0,75.2-75.2,105.3-75.2,105.3S299.2,265.4,299.2,190.2z\"/> <path fill=\"#FFFFFF\" d=\"M299.2,190.2v-90c0-8.5,6.5-15.3,15-15.3h120.5c8.3,0,15,6.8,15,15.3v90c0,75.2-75.2,105.3-75.2,105.3\n\t\tS299.2,265.4,299.2,190.2z M378.8,276.5c7.9-4.5,15.9-10,23.2-16.7c20.5-18.5,32.6-41.4,32.6-69.7v-90c0-0.3-120.4-0.3-120.4-0.3\n\t\tl0,90.3c0,28.3,12.1,51.2,32.6,69.7c8.3,7.5,17.6,13.9,27.6,19C375.8,278.2,377.2,277.4,378.8,276.5z\"/> <path fill=\"#187536\" d=\"M334.1,189.4c-2.6-7.9,1.7-14.3,10.1-14.3h60.5c8.2,0,12.8,6.3,10.1,14.3l-5.5,16.5c-2.6,7.9-8.9,9.2-15.8,4\n\t\tc0,0,2.6-4.8-19.1-4.8c-21.7,0-19.1,4.8-19.1,4.8c-6.1,5.7-13.1,4-15.8-4C339.6,206,334.1,189.4,334.1,189.4z\"/> <path fill=\"#FFFFFF\" d=\"M344.4,175.1c8.1,1.6,17.8-15,30.1-15c13.1,0,21.8,17.1,30.1,15c7.4-1.8,15-22.2,15-30.1\n\t\tc0-16.6-20.2-30.1-45.1-30.1c-24.9,0-45.1,13.5-45.1,30.1C329.3,153.5,336,173.5,344.4,175.1z\"/> <path fill=\"#FFFFFF\" d=\"M343,266.7c6.2,0.4,12.5-1.8,17.2-6.5l9-9c2.9-2.9,7.5-2.9,10.4,0c0,0,0,0,0,0l9,9c4.7,4.7,11,6.9,17.1,6.5\n\t\tc5.3-0.3,32.5-33.5,25.7-32.6c-4.6,0.6-9.1,2.8-12.7,6.3c0,0,0,0-0.1,0.1l-9,9c-2.9,2.9-7.5,2.9-10.4,0c0,0,0,0,0,0l-9-9\n\t\tc-8.7-8.8-22.9-8.8-31.7-0.1c0,0,0,0-0.1,0.1l-9,9c-2.9,2.9-7.5,2.9-10.4,0c0,0,0,0,0,0l-9-9c-3.5-3.5-7.8-5.6-12.3-6.3\n\t\tC311,233.1,337.8,266.4,343,266.7z M309.1,206c2.9-2.9,7.5-2.9,10.4,0c0,0,0,0,0,0l9,9c8.8,8.7,22.9,8.7,31.7,0l9-9\n\t\tc2.9-2.9,7.5-2.9,10.4,0c0,0,0,0,0,0l9,9c8.8,8.7,22.9,8.7,31.7,0l9-9c2.9-2.9,7.5-2.9,10.4,0c0,0,0,0,0,0c0,0,5.5-14.8,2.5-15.9\n\t\tc-7.9-2.9-17.2-1.2-23.5,5.2c0,0,0,0-0.1,0.1l-9,9c-2.9,2.9-7.5,2.9-10.4,0c0,0,0,0,0,0l-9-9c-8.7-8.8-22.9-8.8-31.7-0.1\n\t\tc0,0,0,0-0.1,0.1l-9,9c-2.9,2.9-7.5,2.9-10.4,0c0,0,0,0,0,0l-9-9c-6.2-6.2-15.1-8-22.8-5.5C304.1,191,309.1,206,309.1,206z\"/> </g> </svg> ";
// Exports
/* harmony default export */ var fk = (fk_code);
;// ./templates/flags/fo.svg
// Module
var fo_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"37.554 0 341.3 341.3\"> <rect y=\"0\" fill=\"#FFFFFF\" width=\"512\" height=\"341.3\"/> <polygon fill=\"#0F61A5\" points=\"512,214.5 206.2,214.5 206.2,341.3 183.7,341.3 141,341.3 118.4,341.3 118.4,214.5 0,214.5 0,192\n\t0,149.3 0,126.8 118.4,126.8 118.4,0 141,0 183.7,0 206.2,0 206.2,126.8 512,126.8 512,149.3 512,192 \"/> <polygon fill=\"#E50E3D\" points=\"512,149.3 512,192 183.7,192 183.7,341.3 141,341.3 141,192 0,192 0,149.3 141,149.3 141,0 183.7,0\n\t183.7,149.3 \"/> </svg> ";
// Exports
/* harmony default export */ var fo = (fo_code);
;// ./templates/flags/fj.svg
// Module
var fj_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.35 0 341.3 341.3\"> <rect y=\"0\" fill=\"#2E52B2\" width=\"512\" height=\"341.3\"/> <polygon fill=\"#FFFFFF\" points=\"256,0 256,22.6 209.9,53.3 256,53.3 256,117.3 209.9,117.3 256,148 256,170.7 233.4,170.7 160,121.7\n\t160,170.7 96,170.7 96,121.7 22.6,170.7 0,170.7 0,148 46.1,117.3 0,117.3 0,53.3 46.1,53.3 0,22.6 0,0 22.6,0 96,48.9 96,0 160,0\n\t160,48.9 233.4,0 \"/> <g> <polygon fill=\"#D80027\" points=\"144,0 112,0 112,69.3 0,69.3 0,101.3 112,101.3 112,170.7 144,170.7 144,101.3 256,101.3 256,69.3\n\t\t144,69.3 \t\"/> <polygon fill=\"#D80027\" points=\"0,0 0,15.1 57.4,53.3 80,53.3 \t\"/> <polygon fill=\"#D80027\" points=\"256,0 256,15.1 198.6,53.3 176,53.3 \t\"/> <polygon fill=\"#D80027\" points=\"0,0 0,15.1 57.4,53.3 80,53.3 \t\"/> <polygon fill=\"#D80027\" points=\"256,0 256,15.1 198.6,53.3 176,53.3 \t\"/> <polygon fill=\"#D80027\" points=\"0,170.7 0,155.6 57.4,117.3 80,117.3 \t\"/> <polygon fill=\"#D80027\" points=\"256,170.7 256,155.6 198.6,117.3 176,117.3 \t\"/> </g> <g> <path fill=\"#F3F3F3\" d=\"M307.1,127.1v92c0,61.6,80.5,80.5,80.5,80.5S468,280.6,468,219v-92l-80.5-23L307.1,127.1z\"/> <path fill=\"#D80027\" d=\"M468,132.8V98.3H307.1v34.5h69v69h-69v23v0h69V296c6.9,2.5,11.5,3.5,11.5,3.5s4.6-1.1,11.5-3.5v-71.2h69v0v-23\n\t\th-69v-69H468z\"/> </g> </svg> ";
// Exports
/* harmony default export */ var fj = (fj_code);
;// ./templates/flags/fi.svg
// Module
var fi_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"37.554 0 341.3 341.3\"> <rect y=\"0\" fill=\"#FFFFFF\" width=\"512\" height=\"341.3\"/> <polygon fill=\"#2E52B2\" points=\"512,129.3 512,212 203.7,212 203.7,341.3 121,341.3 121,212 0,212 0,129.3 121,129.3 121,0 203.7,0\n\t203.7,129.3 \"/> </svg> ";
// Exports
/* harmony default export */ var fi = (fi_code);
;// ./templates/flags/fr.svg
// Module
var flags_fr_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#FFFFFF\" width=\"512\" height=\"341.337\"/> <rect y=\"85.331\" fill=\"#0052B4\" width=\"170.663\" height=\"341.337\"/> <rect x=\"341.337\" y=\"85.331\" fill=\"#D80027\" width=\"170.663\" height=\"341.337\"/> </svg> ";
// Exports
/* harmony default export */ var flags_fr = (flags_fr_code);
;// ./templates/flags/pf.svg
// Module
var pf_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <g> <rect y=\"85.337\" fill=\"#D80027\" width=\"512\" height=\"113.775\"/> <rect y=\"312.888\" fill=\"#D80027\" width=\"512\" height=\"113.775\"/> </g> <path fill=\"#FFDA44\" d=\"M293.991,256c0,20.982-17.01,33.243-37.992,33.243S218.008,276.982,218.008,256  s17.01-37.992,37.992-37.992S293.991,235.018,293.991,256z\"/> <path fill=\"#0052B4\" d=\"M293.991,256c0,20.982-17.01,37.992-37.992,37.992s-37.992-17.01-37.992-37.992\"/> <g> <rect x=\"232.259\" y=\"246.506\" fill=\"#D80027\" width=\"9.498\" height=\"19\"/> <rect x=\"270.247\" y=\"246.506\" fill=\"#D80027\" width=\"9.498\" height=\"19\"/> <rect x=\"251.247\" y=\"232.259\" fill=\"#D80027\" width=\"9.498\" height=\"33.243\"/> </g> </svg> ";
// Exports
/* harmony default export */ var pf = (pf_code);
;// ./templates/flags/ga.svg
// Module
var ga_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFDA44\" width=\"512\" height=\"341.326\"/> <rect y=\"85.337\" fill=\"#6DA544\" width=\"512\" height=\"113.775\"/> <rect y=\"312.888\" fill=\"#0052B4\" width=\"512\" height=\"113.775\"/> </svg> ";
// Exports
/* harmony default export */ var ga = (ga_code);
;// ./templates/flags/gm.svg
// Module
var gm_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <rect y=\"322.783\" fill=\"#1a7e25\" width=\"512\" height=\"103.88\"/> <rect y=\"85.337\" fill=\"#cf0d19\" width=\"512\" height=\"104.515\"/> <rect y=\"210.877\" fill=\"#0052B4\" width=\"512\" height=\"89.656\"/> </svg> ";
// Exports
/* harmony default export */ var gm = (gm_code);
;// ./templates/flags/ge.svg
// Module
var ge_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"3.975 0 15 15\"> <path fill=\"#FFFFFF\" d=\"M0.8,0h21v15h-21V0z\"/> <path fill=\"#eb000e\" d=\"M9.8,6H0v3h9.8v6h3V9h9.8V6h-9.8V0h-3V6z\"/> <path fill=\"#eb000e\" d=\"M17.3,2.7l-0.2-1.2h1l-0.2,1.2l1.2-0.2v1l-1.2-0.2l0.2,1.2h-1l0.2-1.2l-1.2,0.2v-1L17.3,2.7z M4.6,2.7L4.4,1.5\n\t\th1L5.2,2.7l1.2-0.2v1L5.2,3.3l0.2,1.2h-1l0.2-1.2L3.4,3.5v-1C3.4,2.5,4.6,2.7,4.6,2.7z M4.6,11.7l-0.2-1.2h1l-0.2,1.2l1.2-0.2v1\n\t\tl-1.2-0.2l0.2,1.2h-1l0.2-1.2l-1.2,0.2v-1C3.4,11.5,4.6,11.7,4.6,11.7z M17.3,11.7l-0.2-1.2h1l-0.2,1.2l1.2-0.2v1l-1.2-0.2l0.2,1.2\n\t\th-1l0.2-1.2l-1.2,0.2v-1L17.3,11.7z\"/> </svg> ";
// Exports
/* harmony default export */ var ge = (ge_code);
;// ./templates/flags/de.svg
// Module
var de_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#D80027\" width=\"512\" height=\"341.337\"/> <rect y=\"85.331\" width=\"512\" height=\"113.775\"/> <rect y=\"312.882\" fill=\"#FFDA44\" width=\"512\" height=\"113.775\"/> </svg> ";
// Exports
/* harmony default export */ var de = (de_code);
;// ./templates/flags/gh.svg
// Module
var gh_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFDA44\" width=\"512\" height=\"341.326\"/> <rect y=\"85.337\" fill=\"#D80027\" width=\"512\" height=\"113.775\"/> <rect y=\"312.888\" fill=\"#496E2D\" width=\"512\" height=\"113.775\"/> <polygon points=\"255.883,199.111 270.002,242.569 310.368,245.769 273.399,272.63 292.852,312.889 255.883,286.03 218.913,312.889   233.036,269.43 196.067,242.569 241.763,242.569 \"/> </svg> ";
// Exports
/* harmony default export */ var gh = (gh_code);
;// ./templates/flags/gi.svg
// Module
var gi_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#FFFFFF\" width=\"513\" height=\"342\"/> <rect y=\"230\" fill=\"#D80027\" width=\"513\" height=\"112\"/> <polygon fill=\"#D80027\" stroke=\"#000000\" stroke-miterlimit=\"10\" points=\"363.1,131.8 363.1,99.1 374,99.1 374,77.3 352.1,77.3 352.1,88.2 330.3,88.2 330.3,77.3 308.5,77.3\n\t308.5,99.1 319.4,99.1 319.4,131.8 297.6,131.8 297.6,55.5 308.5,55.5 308.5,33.7 286.7,33.7 286.7,44.6 275.8,44.6 275.8,33.7\n\t254,33.7 254,44.6 243.1,44.6 243.1,33.7 221.3,33.7 221.3,55.5 232.2,55.5 232.2,131.8 210.4,131.8 210.4,99.1 221.3,99.1\n\t221.3,77.3 199.5,77.3 199.5,88.2 177.7,88.2 177.7,77.3 155.9,77.3 155.9,99.1 166.8,99.1 166.8,131.8 145,131.8 145,219\n\t384.9,219 384.9,131.8 \"/> <path fill=\"#FFDA44\" stroke=\"#000000\" stroke-miterlimit=\"10\" d=\"M264.9,235.5l-24.2,18.2l19.1,14.3v31.3h-23.2v9l23,0l0,7.9l-23,0v9.3H270V268l19.1-14.3L264.9,235.5z\n\t M264.9,260.8l-10.1-7.1l10.1-7.1l10.1,7.1L264.9,260.8z\"/> <path d=\"M239.6,209.7v-27.9c0,0,0.1-22.3,25-22.3c24.8,0,25.7,22,25.7,22v28.2H239.6z\"/> <path d=\"M170.5,209.7v-24.3c0,0,0.1-18.7,19.6-18.7s20.2,18.4,20.2,18.4v24.5H170.5z\"/> <path d=\"M169.1,209.7v-24.3c0,0,0.1-18.7,19.6-18.7s20.2,18.4,20.2,18.4v24.5H169.1z\"/> <path d=\"M320.9,209.7v-24.3c0,0,0.1-18.7,19.6-18.7c19.5,0,20.2,18.4,20.2,18.4v24.5H320.9z\"/> <path d=\"M329.3,132v-15.7c0,0,0.1-12.1,11.3-12.1c11.3,0,11.7,11.9,11.7,11.9V132H329.3z\"/> <path d=\"M250.5,132v-23.7c0,0,0.1-18.3,14.3-18.3c14.2,0,14.7,18,14.7,18v24H250.5z\"/> <path d=\"M177.5,132v-15.7c0,0,0.1-12.1,11.3-12.1s11.7,11.9,11.7,11.9V132H177.5z\"/> </svg> ";
// Exports
/* harmony default export */ var gi = (gi_code);
;// ./templates/flags/gr.svg
// Module
var gr_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" version=\"1.1\" viewBox=\"3.42 0 342 342\"> <rect fill=\"#FFFFFF\" width=\"513\" height=\"342\"/> <g fill=\"#0d5eaf\"> <rect y=\"0\" width=\"513\" height=\"38\"/> <rect y=\"76\" width=\"513\" height=\"38\"/> <rect y=\"152\" width=\"513\" height=\"38\"/> <rect y=\"228\" width=\"513\" height=\"38\"/> <rect y=\"304\" width=\"513\" height=\"38\"/> <rect width=\"190\" height=\"190\"/> </g> <g> <rect y=\"76\" fill=\"#FFFFFF\" width=\"190\" height=\"38\"/> <rect x=\"76\" fill=\"#FFFFFF\" width=\"38\" height=\"190\"/> </g> </svg> ";
// Exports
/* harmony default export */ var gr = (gr_code);
;// ./templates/flags/gl.svg
// Module
var gl_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"90.63 0 342 342\"> <rect fill=\"#D80027\" width=\"513\" height=\"342\"/> <polygon fill=\"#FFFFFF\" points=\"513,0 513,171 0,171 0,0 \"/> <circle fill=\"#FFFFFF\" cx=\"185.8\" cy=\"171.2\" r=\"117.8\"/> <path fill=\"#D80027\" d=\"M68,171c0-65.1,52.8-117.8,117.8-117.8c65.1,0,117.8,52.8,117.8,117.8\"/> </svg> ";
// Exports
/* harmony default export */ var gl = (gl_code);
;// ./templates/flags/gd.svg
// Module
var gd_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"75.108 0 341.3 341.3\"> <rect y=\"0\" fill=\"#c60a0a\" width=\"512\" height=\"341.3\"/> <polygon fill=\"#3E8446\" points=\"256,170.7 44.5,296.8 44.5,44.5 \"/> <g> <polygon fill=\"#FFDA44\" points=\"256,170.7 467.5,296.8 44.5,296.8 \t\"/> <polygon fill=\"#FFDA44\" points=\"467.5,44.5 256,170.7 44.5,44.5 \t\"/> </g> <polygon fill=\"#3E8446\" points=\"467.5,44.5 467.5,296.8 256,170.7 \"/> <g fill=\"#FFDA44\"> <polygon points=\"256,10.4 259.3,20.6 270,20.6 261.4,26.9 264.7,37.1 256,30.8 247.3,37.1 250.6,26.9 242,20.6\n\t\t252.7,20.6 \t\"/> <polygon points=\"170.1,10.4 173.4,20.6 184.1,20.6 175.5,26.9 178.8,37.1 170.1,30.8 161.4,37.1 164.7,26.9\n\t\t156.1,20.6 166.8,20.6 \t\"/> <polygon points=\"341.9,10.4 345.2,20.6 356,20.6 347.3,26.9 350.6,37.1 341.9,30.8 333.2,37.1 336.5,26.9 327.9,20.6\n\t\t338.6,20.6 \t\"/> <polygon points=\"256,304.2 259.3,314.4 270,314.4 261.4,320.7 264.7,330.9 256,324.6 247.3,330.9 250.6,320.7\n\t\t242,314.4 252.7,314.4 \t\"/> <polygon points=\"170.1,304.2 173.4,314.4 184.1,314.4 175.5,320.7 178.8,330.9 170.1,324.6 161.4,330.9 164.7,320.7\n\t\t156.1,314.4 166.8,314.4 \t\"/> <polygon points=\"341.9,304.2 345.2,314.4 356,314.4 347.3,320.7 350.6,330.9 341.9,324.6 333.2,330.9 336.5,320.7\n\t\t327.9,314.4 338.6,314.4 \t\"/> </g> <g> <circle fill=\"#c60a0a\" cx=\"244.5\" cy=\"170.7\" r=\"76.2\"/> <polygon fill=\"#FFDA44\" points=\"244.5,110.1 258.1,151.9 302.1,151.9 266.5,177.7 280.1,219.5 244.5,193.7 209,219.5 222.6,177.7\n\t\t187,151.9 231,151.9 \t\"/> </g> <path fill=\"#FFDA44\" d=\"M107.7,167.8c4.4,6.9,2.3,16.1-4.6,20.5s-16.1,2.3-20.5-4.6c-7.9-12.5-3.3-33-3.3-33S99.7,155.3,107.7,167.8z\"/> <circle fill=\"#A2001D\" cx=\"99.1\" cy=\"182.1\" r=\"7.4\"/> </svg> ";
// Exports
/* harmony default export */ var gd = (gd_code);
;// ./templates/flags/gu.svg
// Module
var gu_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect fill=\"#D80027\" width=\"512\" height=\"341.3\"/> <rect x=\"11.1\" y=\"11.1\" fill=\"#0052B4\" width=\"489.7\" height=\"319.1\"/> <path fill=\"#7DBEF1\" d=\"M256,285.3c0,0,76.4-51.3,76.4-114.6S256,56,256,56s-76.4,51.3-76.4,114.6S256,285.3,256,285.3z\"/> <polygon fill=\"#1C8AE6\" points=\"179.8,180.7 332.4,180.7 303.1,245.6 207.9,245.6 \"/> <path fill=\"#FFF042\" d=\"M192.3,205.9c0,0,40.5,38.2,51,38.2c12.4,0,12.6-18.4,25.5-25.5c20.2-11.1,51-12.7,51-12.7l-22.8,42.8l-41,36.6\n\tl-46.8-39.7L192.3,205.9z\"/> <path fill=\"#259C7B\" d=\"M256,157.5l-22.5,15.6l7.9-26.2l-21.8-16.5l27.4-0.5l9-25.9l9,25.9l27.4,0.5L270.6,147l7.9,26.2L256,157.5z\"/> <path fill=\"#8E5715\" d=\"M249.7,144.6c-0.9,9.2-1.5,18.4-1.7,27.6c-0.3,11.9,0.3,20.7,2,26c2.2,6.6,7.2,12.9,14.2,18.9\n\tc5.3,4.6,10.6,8.1,14.2,10.1c3.1,1.7,7,0.6,8.7-2.4c1.7-3.1,0.6-7-2.4-8.7l0,0c-4.3-2.5-8.4-5.4-12.2-8.7\n\tc-5.4-4.6-9.1-9.2-10.4-13.3c-1.1-3.4-1.6-11.3-1.4-21.7c0.2-8.9,0.8-17.8,1.7-26.6c0.4-3.5-2.2-6.6-5.7-7\n\tC253.1,138.5,250,141.1,249.7,144.6z\"/> <path fill=\"#FFFFFF\" d=\"M217.8,170.7l25.5,38.2h-25.5V170.7z\"/> <path fill=\"none\" stroke=\"#D80027\" stroke-width=\"12\" stroke-miterlimit=\"10\" d=\"M256,285.3c0,0,76.4-51.3,76.4-114.6S256,56,256,56s-76.4,51.3-76.4,114.6S256,285.3,256,285.3z\"/> </svg> ";
// Exports
/* harmony default export */ var gu = (gu_code);
;// ./templates/flags/gt.svg
// Module
var gt_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#FFFFFF\" width=\"513\" height=\"342\"/> <g> <rect y=\"0\" fill=\"#338AF3\" width=\"171\" height=\"342\"/> <rect x=\"342\" y=\"0\" fill=\"#338AF3\" width=\"171\" height=\"342\"/> </g> <circle fill=\"#DCC26D\" cx=\"256\" cy=\"155.8\" r=\"25.5\"/> <path fill=\"#628A40\" d=\"M194.2,155.9c0,22.1,11.8,42.5,30.8,53.5c5.9,3.4,13.5,1.4,16.9-4.5c3.4-5.9,1.4-13.5-4.5-16.9\n\tc-11.5-6.6-18.5-18.9-18.5-32.1c0-6.8-5.5-12.4-12.4-12.4S194.2,149,194.2,155.9z M289.3,208c17.8-11.4,28.6-31,28.5-52.1\n\tc0-6.8-5.5-12.4-12.4-12.4c-6.8,0-12.4,5.5-12.4,12.4c0,12.7-6.5,24.5-17.1,31.3c-5.8,3.6-7.7,11.2-4.1,17c3.6,5.8,11.2,7.7,17,4.1\n\tC289,208.2,289.1,208.1,289.3,208z\"/> </svg> ";
// Exports
/* harmony default export */ var gt = (gt_code);
;// ./templates/flags/gg.svg
// Module
var gg_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect fill=\"#FFFFFF\" width=\"513\" height=\"342\"/> <polygon fill=\"#D80027\" points=\"308,0 204,0 204,118.7 0,118.7 0,222.7 204,222.7 204,342 308,342 308,222.7 513,222.7 513,118.7 308,118.7 \"/> <polygon fill=\"#FFDA44\" points=\"368.6,188 394.6,205.3 394.6,136 368.6,153.3 273.3,153.3 273.3,58.1 290.6,32.1 221.4,32.1 238.7,58.1 238.7,153.3 143.4,153.3 117.4,136 117.4,205.3 143.4,188 238.7,188 238.7,283.2 221.4,309.2 290.6,309.2 273.3,283.2 273.3,188 \"/> </svg> ";
// Exports
/* harmony default export */ var gg = (gg_code);
;// ./templates/flags/gn.svg
// Module
var gn_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect fill=\"#FFDA44\" width=\"512\" height=\"342\"/> <rect x=\"342\" fill=\"#6DA544\" width=\"171\" height=\"342\"/> <rect fill=\"#D80027\" width=\"171\" height=\"342\"/> </svg> ";
// Exports
/* harmony default export */ var gn = (gn_code);
;// ./templates/flags/gw.svg
// Module
var gw_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"18.773 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#6DA544\" width=\"512\" height=\"341.337\"/> <rect y=\"85.331\" fill=\"#FFDA44\" width=\"512\" height=\"170.663\"/> <rect y=\"85.331\" fill=\"#D80027\" width=\"181.793\" height=\"341.337\"/> <polygon points=\"98.32,195.127 113.427,241.624 162.321,241.624 122.766,270.364 137.874,316.862 98.32,288.125 58.764,316.862   73.875,270.364 34.321,241.624 83.212,241.624 \"/> </svg> ";
// Exports
/* harmony default export */ var gw = (gw_code);
;// ./templates/flags/gy.svg
// Module
var gy_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"27 0 600 600\"> <path fill=\"#009E49\" d=\"M0,0h900v600H0V0z\"/> <path fill=\"#FFFFFF\" d=\"M0,0l947,300L0,600V0z\"/> <path fill=\"#FFD00D\" d=\"M0,26.1L870,300L0,573.9V26.1z\"/> <path fill=\"#2D2D2D\" d=\"M0,0l450,300L0,600V0z\"/> <path fill=\"#D3132F\" d=\"M0,35l397.5,265L0,565V35z\"/> </svg> ";
// Exports
/* harmony default export */ var gy = (gy_code);
;// ./templates/flags/ht.svg
// Module
var ht_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#A2001D\" width=\"513\" height=\"172\"/> <rect y=\"172\" fill=\"#0052B4\" width=\"513\" height=\"172\"/> <polygon fill=\"#FFFFFF\" points=\"381.4,251.5 270.7,237.7 159.9,251.5 159.9,85.4 381.4,85.4 \t\"/> <circle fill=\"#0052B4\" cx=\"270.7\" cy=\"182.3\" r=\"55.4\"/> <circle fill=\"#A2001D\" cx=\"270.7\" cy=\"182.3\" r=\"27.7\"/> <polygon fill=\"#6DA544\" points=\"229.1,113.1 312.2,113.1 270.7,154.6 \t\"/> <rect x=\"256.8\" y=\"140.8\" fill=\"#FFDA44\" width=\"27.7\" height=\"83\"/> <polygon fill=\"#6DA544\" points=\"314.9,215.5 226.4,215.5 159.9,251.5 381.4,251.5 \t\"/> </svg> ";
// Exports
/* harmony default export */ var ht = (ht_code);
;// ./templates/flags/hn.svg
// Module
var hn_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#FFFFFF\" width=\"513\" height=\"342\"/> <g fill=\"#338AF3\"> <rect y=\"0\" width=\"513\" height=\"114\"/> <rect y=\"228\" width=\"513\" height=\"114\"/> <polygon points=\"203.5,117.9 210.2,134.5 227.8,135.5 213.8,146.4 218.5,164.1 203.5,154.2 188.5,164.1 193.2,146.4\n\t\t179.2,135.5 196.8,134.5 \t\"/> <polygon points=\"308.5,117.9 315.2,134.5 332.8,135.5 318.8,146.4 323.5,164.1 308.5,154.2 293.5,164.1 298.2,146.4\n\t\t284.2,135.5 301.8,134.5 \t\"/> <polygon points=\"256,147.6 262.7,164.2 280.3,165.2 266.3,176.1 271,193.8 256,183.9 241,193.8 245.7,176.1\n\t\t231.7,165.2 249.3,164.2 \t\"/> <polygon points=\"203.5,177.3 210.2,193.9 227.8,194.9 213.8,205.8 218.5,223.4 203.5,213.5 188.5,223.4 193.2,205.8\n\t\t179.2,194.9 196.8,193.9 \t\"/> <polygon points=\"308.5,177.3 315.2,193.9 332.8,194.9 318.8,205.8 323.5,223.4 308.5,213.5 293.5,223.4 298.2,205.8\n\t\t284.2,194.9 301.8,193.9 \t\"/> </g> </svg> ";
// Exports
/* harmony default export */ var hn = (hn_code);
;// ./templates/flags/hk.svg
// Module
var hk_code = "<svg viewBox=\"3.5 0.5 14 14\" xmlns=\"http://www.w3.org/2000/svg\"><g fill=\"none\" fill-rule=\"evenodd\"><path fill=\"#FFFFFF\" d=\"M0 0h21v15H0z\"/><path fill=\"#ee1c25\" d=\"M0 0h21v15H0z\"/><path d=\"M12 7.19c-.798-.5-1 .409-1 0 0-.828.895-1.5 2-1.5s2 .672 2 1.5c-.949 0-1.044.5-1.5.5-.56 0-.702 0-1.5-.5zM13.25 7a.25.25 0 1 0 0-.5.25.25 0 0 0 0 .5zm-1.81 1.962c.228-.913-.698-.824-.31-.95.788-.257 1.703.387 2.045 1.438.341 1.05-.021 2.11-.809 2.366-.293-.903-.798-.838-.939-1.272-.173-.533-.217-.668.012-1.582zm.566 1.13a.25.25 0 1 0 .476-.154.25.25 0 0 0-.476.154zM9.58 8.977c.94-.065.57-.919.81-.588.486.67.157 1.74-.737 2.389-.894.65-2.013.632-2.5-.038.768-.558.55-1.018.92-1.286.453-.33.568-.413 1.507-.477zm-.899.888a.25.25 0 1 0 .294.405.25.25 0 0 0-.294-.405zm.312-2.652c.351.874 1.049.258.809.588-.487.67-1.606.687-2.5.038-.894-.65-1.223-1.719-.736-2.39.767.559 1.138.21 1.507.478.453.33.568.413.92 1.286zm-1.124-.58a.25.25 0 1 0-.293.404.25.25 0 0 0 .293-.404zm2.619-.524c-.722.605.08 1.078-.309.951-.788-.256-1.15-1.315-.809-2.365.342-1.05 1.257-1.695 2.045-1.439-.293.903.153 1.147.012 1.581-.173.533-.217.668-.939 1.272zm.205-1.247a.25.25 0 1 0-.475-.155.25.25 0 0 0 .475.155z\" fill=\"#FFFFFF\"/></g></svg> ";
// Exports
/* harmony default export */ var hk = (hk_code);
;// ./templates/flags/hu.svg
// Module
var hu_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <rect y=\"85.337\" fill=\"#D80027\" width=\"512\" height=\"113.775\"/> <rect y=\"312.888\" fill=\"#6DA544\" width=\"512\" height=\"113.775\"/> </svg> ";
// Exports
/* harmony default export */ var hu = (hu_code);
;// ./templates/flags/is.svg
// Module
var is_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"32.427 85.333 341.333 341.333\"> <rect y=\"85.334\" fill=\"#0052B4\" width=\"512\" height=\"341.337\"/> <polygon fill=\"#FFFFFF\" points=\"512,295.883 202.195,295.883 202.195,426.666 183.652,426.666 140.978,426.666   122.435,426.666 122.435,295.883 0,295.883 0,277.329 0,234.666 0,216.111 122.435,216.111 122.435,85.329 140.978,85.329   183.652,85.329 202.195,85.329 202.195,216.111 512,216.111 512,234.666 512,277.329 \"/> <polygon fill=\"#D80027\" points=\"512,234.666 512,277.329 183.652,277.329 183.652,426.666 140.978,426.666 140.978,277.329   0,277.329 0,234.666 140.978,234.666 140.978,85.329 183.652,85.329 183.652,234.666 \"/> </svg> ";
// Exports
/* harmony default export */ var is = (is_code);
;// ./templates/flags/in.svg
// Module
var in_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <path fill=\"#181A93\" d=\"M17.3,0h478.4v342H17.3V0z\"/> <path fill=\"#FFA44A\" d=\"M0,0h513v114H0V0z\"/> <path fill=\"#1A9F0B\" d=\"M0,228h513v114H0V228z\"/> <path fill=\"#FFFFFF\" d=\"M0,114h513v114H0V114z\"/> <ellipse fill=\"#FFFFFF\" cx=\"256.5\" cy=\"171\" rx=\"34.2\" ry=\"34.2\"/> <path fill=\"#181A93\" d=\"M256.5,216.6c-25.1,0-45.6-20.5-45.6-45.6s20.5-45.6,45.6-45.6s45.6,20.5,45.6,45.6S281.6,216.6,256.5,216.6z M256.5,205.2\n\t\tc18.2,0,34.2-16,34.2-34.2s-15.9-34.2-34.2-34.2s-34.2,16-34.2,34.2S238.3,205.2,256.5,205.2z\"/> <ellipse fill=\"#181A93\" cx=\"256.5\" cy=\"171\" rx=\"22.8\" ry=\"22.8\"/> </svg> ";
// Exports
/* harmony default export */ var flags_in = (in_code);
;// ./templates/flags/id.svg
// Module
var id_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.333\" fill=\"#FFFFFF\" width=\"512\" height=\"341.333\"/> <rect y=\"85.333\" fill=\"#EE0000\" width=\"512\" height=\"170.667\"/> </svg> ";
// Exports
/* harmony default export */ var id = (id_code);
;// ./templates/flags/ir.svg
// Module
var ir_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#FFFFFF\" width=\"512\" height=\"342\"/> <rect y=\"0\" fill=\"#6DA544\" width=\"513\" height=\"114\"/> <g fill=\"#D80027\"> <rect y=\"227.9\" width=\"513\" height=\"114\"/> <path d=\"M278.8,134.8c0.1,2,8.7,26.2,4.4,39.4c-6.6,20.3-15.8,21.8-19.8,24.5v-64.7l-6.9-4.2l-6.9,4.2v64.7\n\t\tc-4-2.7-12.4-2.4-19.8-24.5c-4.3-12.7,5.7-37.3,5.8-39.2c0,0-9.5,8.1-15.8,24c-5.9,14.8,1.9,49.6,29.5,54.8\n\t\tc2.3,0.4,4.7,5.6,7.2,5.6c2.1,0,4.1-5.2,6-5.5c28.4-4.6,35-41.7,29.9-55.6C287,143.7,278.8,134.8,278.8,134.8z\"/> </g> <g fill=\"#FFFFFF\" opacity=\"0.5\"> <rect x=\"44.6\" y=\"98.9\" width=\"22.3\" height=\"24.4\"/> <rect y=\"98.9\" width=\"22.3\" height=\"24.4\"/> <rect x=\"89.2\" y=\"98.9\" width=\"22.3\" height=\"24.4\"/> <rect x=\"133.8\" y=\"98.9\" width=\"22.3\" height=\"24.4\"/> <rect x=\"178.4\" y=\"98.9\" width=\"22.3\" height=\"24.4\"/> <rect x=\"223\" y=\"98.9\" width=\"22.3\" height=\"24.4\"/> <rect x=\"267.7\" y=\"98.9\" width=\"22.3\" height=\"24.4\"/> <rect x=\"312.3\" y=\"98.9\" width=\"22.3\" height=\"24.4\"/> <rect x=\"356.9\" y=\"98.9\" width=\"22.3\" height=\"24.4\"/> <rect x=\"401.5\" y=\"98.9\" width=\"22.3\" height=\"24.4\"/> <rect x=\"446.1\" y=\"98.9\" width=\"22.3\" height=\"24.4\"/> <rect x=\"490.7\" y=\"98.9\" width=\"22.3\" height=\"24.4\"/> <rect x=\"44.6\" y=\"216.9\" width=\"22.3\" height=\"25.5\"/> <rect y=\"216.9\" width=\"22.3\" height=\"25.5\"/> <rect x=\"89.2\" y=\"216.9\" width=\"22.3\" height=\"25.5\"/> <rect x=\"133.8\" y=\"216.9\" width=\"22.3\" height=\"25.5\"/> <rect x=\"178.4\" y=\"216.9\" width=\"22.3\" height=\"25.5\"/> <rect x=\"223\" y=\"216.9\" width=\"22.3\" height=\"25.5\"/> <rect x=\"267.7\" y=\"216.9\" width=\"22.3\" height=\"25.5\"/> <rect x=\"312.3\" y=\"216.9\" width=\"22.3\" height=\"25.5\"/> <rect x=\"356.9\" y=\"216.9\" width=\"22.3\" height=\"25.5\"/> <rect x=\"401.5\" y=\"216.9\" width=\"22.3\" height=\"25.5\"/> <rect x=\"446.1\" y=\"216.9\" width=\"22.3\" height=\"25.5\"/> <rect x=\"490.7\" y=\"216.9\" width=\"22.3\" height=\"25.5\"/> </g> </svg> ";
// Exports
/* harmony default export */ var ir = (ir_code);
;// ./templates/flags/iq.svg
// Module
var iq_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#FFFFFF\" width=\"513\" height=\"342\"/> <rect y=\"0\" fill=\"#CE1126\" width=\"513\" height=\"114\"/> <rect y=\"228\" width=\"513\" height=\"114\"/> <g fill=\"#547C31\"> <path d=\"M219.2,160.7c-0.1,0-0.2,0-0.2,0l0,0h-29.1c1.5-5.7,6.6-9.9,12.8-9.9v-19.9c-18.3,0-33.1,14.9-33.1,33.1v16.2\n\t\tv0.3H219c0.1,0,0.2,0,0.2,0c1.8,0,3.3,1.5,3.3,3.3v6.6h-66.2v19.9h86.1v-26.5C242.4,171.1,232,160.7,219.2,160.7z\"/> <polygon points=\"268.8,190.5 268.8,130.9 249,130.9 249,210.4 282.1,210.4 282.1,190.5 \t\"/> <polygon points=\"335,190.5 335,130.9 315.2,130.9 315.2,190.5 308.6,190.5 308.6,170.7 288.7,170.7 288.7,210.4\n\t\t348.3,210.4 348.3,190.5 \t\"/> </g> </svg> ";
// Exports
/* harmony default export */ var iq = (iq_code);
;// ./templates/flags/ie.svg
// Module
var ie_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <rect y=\"85.331\" fill=\"#6DA544\" width=\"170.663\" height=\"341.337\"/> <rect x=\"341.337\" y=\"85.331\" fill=\"#FF9811\" width=\"170.663\" height=\"341.337\"/> </svg> ";
// Exports
/* harmony default export */ var ie = (ie_code);
;// ./templates/flags/im.svg
// Module
var im_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#E52D42\" width=\"513\" height=\"342\"/> <path fill=\"#FFFFFF\" d=\"M393.3,68.2h-45.8l-33.8,80.2l-34.4-22.9c0,0-24.7-59.1-34.4-68.2c-9.7-9.7-18.7-0.6-48.9,7.8\n\tc-29.6,8.4-30.8,18.7-42.2,18.7c-4.8,0-19.9-17.5-27.7-22.3c-12.1-7.8-16.9-4.2-13.9,7.2c1.2,4.8,10.9,10.9,16.9,19.9\n\tc7.8,11.5,12.7,25.9,12.7,25.9s10.9-10.3,16.9-12.1c9-2.4,19.3,1.8,31.4,0c15.1-2.4,31.4-10.9,31.4-10.9l4.2,43.4\n\tc0,0-54.3,50.7-49.5,70.6s56.1,44.6,68.2,62.7c12.1,17.5-7.2,24.1-7.2,32.6s-1.8,19.9,7.2,16.3c9-3.6,10.3-18.1,18.7-31.4\n\tc6-9,10.3-13.9,11.5-17.5c1.8-9-23.5-32-37.4-48.9c-6.6-7.8-19.9-18.7-19.9-18.7l39.2-29.6c0,0,67.6,27.7,82.6,18.7\n\tc15.1-9,19.3-98.3,19.3-98.3l47-11.5L393.3,68.2z\"/> <path fill=\"#F8DD4E\" d=\"M279.3,262.4c-6,0-11.5-4.8-11.5-11.5s4.8-11.5,11.5-11.5c6,0,11.5,4.8,11.5,11.5S285.3,262.4,279.3,262.4z\n\t M325.1,79.6c-6,0-11.5-4.8-11.5-11.5s4.8-11.5,11.5-11.5c6,0,11.5,4.8,11.5,11.5S331.1,79.6,325.1,79.6z M325.1,170.7\n\tc-6,0-11.5-4.8-11.5-11.5c0-6,4.8-11.5,11.5-11.5c6,0,11.5,4.8,11.5,11.5C336,165.9,331.1,170.7,325.1,170.7z M188.2,216.5\n\tc-6,0-11.5-4.8-11.5-11.5c0-6,4.8-11.5,11.5-11.5c6.6,0,11.5,4.8,11.5,11.5C199.7,211.7,194.2,216.5,188.2,216.5z M233.4,79.6\n\tc-6,0-11.5-4.8-11.5-11.5s4.8-11.5,11.5-11.5c6,0,11.5,4.8,11.5,11.5S240.1,79.6,233.4,79.6z M256.3,170.7c-6,0-11.5-10.3-11.5-22.9\n\ts4.8-22.9,11.5-22.9c6,0,11.5,10.3,11.5,22.9S263,170.7,256.3,170.7z M142.3,125.5c-6,0-11.5-4.8-11.5-11.5\n\tc0-6.6,4.8-11.5,11.5-11.5c6.6,0,11.5,4.8,11.5,11.5C153.8,120.6,149,125.5,142.3,125.5z\"/> </svg> ";
// Exports
/* harmony default export */ var im = (im_code);
;// ./templates/flags/il.svg
// Module
var il_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#FFFFFF\" width=\"513\" height=\"342\"/> <g fill=\"#2E52B2\"> <path d=\"M340.6,122.4h-56.1l-28-48.6l-28,48.6h-56.1l28,48.6l-28,48.6h56.1l28,48.6l28-48.6h56.1l-28-48.6L340.6,122.4\n\t\tz M293.2,171l-17.2,33.2h-38.9L219.8,171l17.2-33.2h38.9L293.2,171z M256.5,99.2l11.9,23.3h-23.9L256.5,99.2z M198.2,137.8h23.9\n\t\tl-10.8,21L198.2,137.8z M198.2,204.2l13-22.1l11.9,22.1H198.2z M256.5,241.7l-11.9-22.1h23.9L256.5,241.7z M315.9,204.2h-25\n\t\tl11.9-22.1L315.9,204.2z M289.8,137.8h26.1l-13,22.1L289.8,137.8z\"/> <rect y=\"21.3\" width=\"512\" height=\"42.7\"/> <rect y=\"277.3\" width=\"512\" height=\"42.7\"/> </g> </svg> ";
// Exports
/* harmony default export */ var il = (il_code);
;// ./templates/flags/it.svg
// Module
var it_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <polygon fill=\"#FFFFFF\" points=\"341.334,85.33 170.666,85.33 0,85.33 0,426.662 170.666,426.662 341.334,426.662   512,426.662 512,85.33 \"/> <rect y=\"85.333\" fill=\"#6DA544\" width=\"170.663\" height=\"341.337\"/> <rect x=\"341.337\" y=\"85.333\" fill=\"#D80027\" width=\"170.663\" height=\"341.337\"/> </svg> ";
// Exports
/* harmony default export */ var it = (it_code);
;// ./templates/flags/ci.svg
// Module
var ci_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <rect x=\"330.207\" y=\"85.331\" fill=\"#009e60\" width=\"181.793\" height=\"341.337\"/> <rect y=\"85.331\" fill=\"#f77f00\" width=\"181.793\" height=\"341.337\"/> </svg> ";
// Exports
/* harmony default export */ var ci = (ci_code);
;// ./templates/flags/jm.svg
// Module
var jm_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#6DA544\" width=\"512\" height=\"341.326\"/> <polygon points=\"215.864,256.006 0,399.911 0,112.089 \"/> <polygon points=\"512,112.089 512,399.911 296.136,256.006 \"/> <polygon fill=\"#0052B4\" points=\"512,112.089 296.136,256.006 512,399.911 512,426.668 471.875,426.668 256,282.752   40.125,426.668 0,426.668 0,399.911 215.864,256.006 0,112.089 0,85.331 40.125,85.331 256,229.248 471.875,85.331 512,85.331 \"/> <polygon fill=\"#FFDA44\" points=\"512,112.089 296.136,256.006 512,399.911 512,426.668 471.875,426.668 256,282.752   40.125,426.668 0,426.668 0,399.911 215.864,256.006 0,112.089 0,85.331 40.125,85.331 256,229.248 471.875,85.331 512,85.331 \"/> </svg> ";
// Exports
/* harmony default export */ var jm = (jm_code);
;// ./templates/flags/jp.svg
// Module
var jp_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#FFFFFF\" width=\"512\" height=\"341.337\"/> <circle fill=\"#D80027\" cx=\"256\" cy=\"255.994\" r=\"96\"/> </svg> ";
// Exports
/* harmony default export */ var jp = (jp_code);
;// ./templates/flags/je.svg
// Module
var je_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#FFFFFF\" width=\"513\" height=\"342\"/> <polygon fill=\"#D80027\" points=\"301.8,171 513,30.2 513,0 467.7,0 256.5,140.8 45.3,0 0,0 0,30.2 211.2,171 0,311.8 0,342 45.3,342\n\t256.5,201.2 467.7,342 513,342 513,311.8 \"/> <polygon fill=\"#FFDA44\" points=\"233.7,44.6 256,50.2 278.3,44.6 282.3,23.7 267.4,30.2 256,14.8 244.6,30.2 229.7,23.7 \"/> <path fill=\"#D80027\" d=\"M233.7,44.6c0,0-4,12.9-4,29.9c0,27.9,26.3,41.3,26.3,41.3s26.3-15.5,26.3-41.3c0-15.4-4-29.9-4-29.9\n\ts-8.1-5-22.3-5S233.7,44.6,233.7,44.6z\"/> </svg> ";
// Exports
/* harmony default export */ var je = (je_code);
;// ./templates/flags/jo.svg
// Module
var jo_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"0 85.333 341.333 341.333\"> <rect y=\"85.339\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <rect y=\"85.339\" width=\"512\" height=\"113.775\"/> <rect y=\"312.89\" fill=\"#6DA544\" width=\"512\" height=\"113.775\"/> <polygon fill=\"#D80027\" points=\"256,256.006 0,426.669 0,85.331 \"/> <polygon fill=\"#FFFFFF\" points=\"77.913,224.802 85.789,241.271 103.575,237.161 95.61,253.587 109.913,264.93   92.105,268.944 92.154,287.199 77.913,275.778 63.672,287.199 63.722,268.944 45.913,264.93 60.216,253.587 52.251,237.161   70.037,241.271 \"/> </svg> ";
// Exports
/* harmony default export */ var jo = (jo_code);
;// ./templates/flags/kz.svg
// Module
var kz_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <path fill=\"#36B6CC\" d=\"M0,0h513v342H0V0z\"/> <circle fill=\"#FFD400\" cx=\"256.5\" cy=\"170.9\" r=\"68.4\"/> <path fill=\"#FFD400\" d=\"M256.5,251.5l-27.9,41.7l-7.1-49.7L178.3,269l15.2-47.8l-50,4.2l34.4-36.5L131,170.9l46.8-18l-34.4-36.5\n\t\tl50,4.2l-15.2-47.8l43.2,25.4l7.1-49.7l27.9,41.7l27.9-41.7l7.1,49.7l43.2-25.4l-15.2,47.8l50-4.2L335.1,153l46.8,17.9l-46.8,18\n\t\tl34.4,36.5l-50-4.2l15.2,47.8l-43.2-25.4l-7.1,49.7C284.4,293.2,256.5,251.5,256.5,251.5z M256.5,250.7c44.1,0,79.9-35.8,79.9-79.9\n\t\tS300.6,91,256.5,91s-79.9,35.8-79.9,79.9S212.4,250.7,256.5,250.7z M22.8,28.3c0-3.2,2.4-5.7,5.7-5.7h11.3c3.2,0,5.7,2.4,5.7,5.7\n\t\tv11.3c0.1,3.1-2.4,5.7-5.6,5.7c-0.1,0-0.1,0-0.2,0H28.5c-3.1,0-5.7-2.5-5.7-5.6c0,0,0-0.1,0-0.1C22.8,39.7,22.8,28.3,22.8,28.3z\n\t\t M22.8,74c0-3.2,2.4-5.7,5.7-5.7h11.3c3.2,0,5.7,2.4,5.7,5.7v11.3c0.1,3.1-2.4,5.7-5.6,5.7c-0.1,0-0.1,0-0.2,0H28.5\n\t\tc-3.1,0-5.7-2.5-5.7-5.6c0,0,0-0.1,0-0.1V74z M22.8,119.6c0-3.2,2.4-5.7,5.7-5.7h11.3c3.2,0,5.7,2.4,5.7,5.7v11.3\n\t\tc0.1,3.1-2.4,5.7-5.6,5.7c-0.1,0-0.1,0-0.2,0H28.5c-3.1,0-5.7-2.5-5.7-5.6c0,0,0-0.1,0-0.1C22.8,130.9,22.8,119.6,22.8,119.6z\n\t\t M22.8,165.2c0-3.2,2.4-5.7,5.7-5.7h11.3c3.2,0,5.7,2.4,5.7,5.7v11.3c0.1,3.1-2.4,5.7-5.6,5.7c-0.1,0-0.1,0-0.2,0H28.5\n\t\tc-3.1,0-5.7-2.5-5.7-5.6c0,0,0-0.1,0-0.1C22.8,176.5,22.8,165.2,22.8,165.2z M22.8,210.9c0-3.2,2.4-5.7,5.7-5.7h11.3\n\t\tc3.2,0,5.7,2.4,5.7,5.7v11.3c0.1,3.1-2.4,5.7-5.6,5.7c-0.1,0-0.1,0-0.2,0H28.5c-3.1,0-5.7-2.5-5.7-5.6c0,0,0-0.1,0-0.1V210.9z\n\t\t M22.8,256.5c0-3.2,2.4-5.7,5.7-5.7h11.3c3.2,0,5.7,2.4,5.7,5.7v11.3c0.1,3.1-2.4,5.7-5.6,5.7c-0.1,0-0.1,0-0.2,0H28.5\n\t\tc-3.1,0-5.7-2.5-5.7-5.6c0,0,0-0.1,0-0.1V256.5z M22.8,302.1c0-3.2,2.4-5.7,5.7-5.7h11.3c3.2,0,5.7,2.4,5.7,5.7v11.3\n\t\tc0.1,3.1-2.4,5.7-5.6,5.7c-0.1,0-0.1,0-0.2,0H28.5c-3.1,0-5.7-2.5-5.7-5.6c0,0,0-0.1,0-0.1V302.1z M45.6,279.3\n\t\tc0-3.2,2.4-5.7,5.7-5.7h11.3c3.2,0,5.7,2.4,5.7,5.7v11.3c0.1,3.1-2.4,5.7-5.6,5.7c-0.1,0-0.1,0-0.2,0H51.3c-3.1,0-5.7-2.5-5.7-5.6\n\t\tc0,0,0-0.1,0-0.1V279.3z M45.6,233.7c0-3.2,2.4-5.7,5.7-5.7h11.3c3.2,0,5.7,2.4,5.7,5.7V245c0.1,3.1-2.4,5.7-5.6,5.7\n\t\tc-0.1,0-0.1,0-0.2,0H51.3c-3.1,0-5.7-2.5-5.7-5.6c0,0,0-0.1,0-0.1V233.7z M45.6,188c0-3.2,2.4-5.7,5.7-5.7h11.3\n\t\tc3.2,0,5.7,2.4,5.7,5.7v11.3c0.1,3.1-2.4,5.7-5.6,5.7c-0.1,0-0.1,0-0.2,0H51.3c-3.1,0-5.7-2.5-5.7-5.6c0,0,0-0.1,0-0.1V188z\n\t\t M45.6,142.4c0-3.2,2.4-5.7,5.7-5.7h11.3c3.2,0,5.7,2.4,5.7,5.7v11.3c0.1,3.1-2.4,5.7-5.6,5.7c-0.1,0-0.1,0-0.2,0H51.3\n\t\tc-3.1,0-5.7-2.5-5.7-5.6c0,0,0-0.1,0-0.1C45.6,153.7,45.6,142.4,45.6,142.4z M45.6,96.8c0-3.2,2.4-5.7,5.7-5.7h11.3\n\t\tc3.2,0,5.7,2.4,5.7,5.7v11.3c0.1,3.1-2.4,5.7-5.6,5.7c-0.1,0-0.1,0-0.2,0H51.3c-3.1,0-5.7-2.5-5.7-5.6c0,0,0-0.1,0-0.1\n\t\tC45.6,108.1,45.6,96.8,45.6,96.8z M45.6,51.2c0-3.2,2.4-5.7,5.7-5.7h11.3c3.2,0,5.7,2.4,5.7,5.7v11.3c0.1,3.1-2.4,5.7-5.6,5.7\n\t\tc-0.1,0-0.1,0-0.2,0H51.3c-3.1,0-5.7-2.5-5.7-5.6c0,0,0-0.1,0-0.1V51.2z\"/> </svg> ";
// Exports
/* harmony default export */ var kz = (kz_code);
;// ./templates/flags/ke.svg
// Module
var ke_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#FFFFFF\" width=\"512\" height=\"341.337\"/> <rect y=\"85.331\" width=\"512\" height=\"90.579\"/> <rect y=\"336.089\" fill=\"#496E2D\" width=\"512\" height=\"90.568\"/> <rect y=\"198.606\" fill=\"#A2001D\" width=\"512\" height=\"114.799\"/> <g> <polygon fill=\"#FFFFFF\" points=\"323.265,139.803 297.682,127.994 256,222.376 214.318,127.994 188.735,139.803    240.484,255.994 188.735,372.186 214.318,383.994 256,289.613 297.682,383.994 323.265,372.186 271.516,255.994  \"/> <path fill=\"#FFFFFF\" d=\"M273.376,150.931C263.472,140.115,256,133.898,256,133.898s-7.472,6.216-17.376,17.032v210.127   C248.528,371.873,256,378.091,256,378.091s7.472-6.216,17.376-17.033V150.931z\"/> </g> <g> <path fill=\"#A2001D\" d=\"M209.04,191.226v129.535c10.465,18.542,23.274,33.742,32.872,43.818V147.408   C232.313,157.484,219.504,172.683,209.04,191.226z\"/> <path fill=\"#A2001D\" d=\"M302.96,191.226c-10.465-18.543-23.274-33.742-32.872-43.818V364.58   c9.599-10.077,22.407-25.276,32.872-43.818V191.226z\"/> </g> <path d=\"M302.96,191.226v129.535c10.594-18.774,18.784-40.973,18.784-64.767S313.554,210.001,302.96,191.226z\"/> <path d=\"M209.04,191.226v129.535c-10.594-18.774-18.784-40.973-18.784-64.767S198.446,210.001,209.04,191.226z\"/> </svg> ";
// Exports
/* harmony default export */ var ke = (ke_code);
;// ./templates/flags/ki.svg
// Module
var ki_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"75 0 300 300\"> <path fill=\"#CE1126\" d=\"M0,0h450v300H0V0z\"/> <g fill=\"#FCD116\"> <circle cx=\"227.6\" cy=\"161.2\" r=\"56.2\"/> <polygon points=\"254.1,73.4 230.4,99.4 259,105.9 \t\"/> <polygon points=\"202.6,73.6 197.8,106.7 226.3,98.7 \t\"/> <polygon points=\"296.9,100.3 262.2,108.8 282.3,132.5 \t\"/> <polygon points=\"158.6,98.5 174.1,131.2 194.3,108.5 \t\"/> <polygon points=\"317.6,143.8 283.2,133.9 288.8,163.4 \t\"/> <polygon points=\"135.6,141.2 167.5,161 172.7,133.3 \t\"/> </g> <path fill=\"#003F87\" d=\"M0,175h450v125H0V175z\"/> <path fill=\"#FFFFFF\" d=\"M0,267c15,0,30,13,45,13s30-13,45-13s30,13,45,13s30-13,45-13s30,13,45,13s30-13,45-13s30,13,45,13s30-13,45-13\n\ts30,13,45,13s30-13,45-13v-21c-15,0-30,13-45,13s-30-13-45-13s-30,13-45,13s-30-13-45-13s-30,13-45,13s-30-13-45-13s-30,13-45,13\n\ts-30-13-45-13s-30,13-45,13s-30-13-45-13V267z M0,224c15,0,30,13,45,13s30-13,45-13s30,13,45,13s30-13,45-13s30,13,45,13\n\ts30-13,45-13s30,13,45,13s30-13,45-13s30,13,45,13s30-13,45-13v-21c-15,0-30,13-45,13s-30-13-45-13s-30,13-45,13s-30-13-45-13\n\ts-30,13-45,13s-30-13-45-13s-30,13-45,13s-30-13-45-13s-30,13-45,13s-30-13-45-13V224z M0,181c15,0,30,13,45,13s30-13,45-13\n\ts30,13,45,13s30-13,45-13s30,13,45,13s30-13,45-13s30,13,45,13s30-13,45-13s30,13,45,13s30-13,45-13v-21c-15,0-30,13-45,13\n\ts-30-13-45-13s-30,13-45,13s-30-13-45-13s-30,13-45,13s-30-13-45-13s-30,13-45,13s-30-13-45-13s-30,13-45,13s-30-13-45-13V181z\"/> <path fill-rule=\"evenodd\" clip-rule=\"evenodd\" fill=\"#FCD116\" d=\"M183.2,48.3l-1.1-5.3l22.4-3.7c0,0-30.1-7.1-40-10.7c-14.3-5.1-16.3-12.1-16.3-12.1s44.7,11,64.8,11\n\tc4.3,0,12.8,10.7,12.8,10.7s14.8-14.9,21.9-15.4c32.1-2.3,66.6-6.4,66.6-6.4s-10,6.7-16,8.5c-16.2,4.9-50.6,11.7-50.6,11.7l-4.8,7.5\n\tH277l-14.4,4.3l7.4,6.9c0,0-7.8-4.1-18.1-2.1c-7.1,1.3-15.4,6-22.9,8c-18.7,4.8-29.9-12.8-29.9-12.8S183.2,48.3,183.2,48.3z\"/> </svg> ";
// Exports
/* harmony default export */ var ki = (ki_code);
;// ./templates/flags/xk.svg
// Module
var xk_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.335 0 341.33 341.33\"> <rect y=\"0\" fill=\"#0052B4\" width=\"512\" height=\"341.34\"/> <g> <polygon fill=\"#FFFFFF\" points=\"220.72,42.63 228.67,58.74 246.44,61.32 233.58,73.86 236.62,91.56 220.72,83.2 204.82,91.56\n\t\t207.85,73.86 194.99,61.32 212.77,58.74 \t\"/> <polygon fill=\"#FFFFFF\" points=\"291.28,42.57 299.23,58.67 317.01,61.26 304.15,73.79 307.18,91.5 291.28,83.14 275.38,91.5\n\t\t278.42,73.79 265.56,61.26 283.33,58.67 \t\"/> <polygon fill=\"#FFFFFF\" points=\"353.36,53.93 361.31,70.04 379.08,72.62 366.22,85.16 369.26,102.86 353.36,94.51 337.46,102.86\n\t\t340.5,85.16 327.63,72.62 345.41,70.04 \t\"/> <polygon fill=\"#FFFFFF\" points=\"416.37,72.23 424.32,88.34 442.09,90.93 429.23,103.46 432.27,121.17 416.37,112.81 400.47,121.17\n\t\t403.5,103.46 390.64,90.93 408.42,88.34 \t\"/> <polygon fill=\"#FFFFFF\" points=\"158.64,53.93 166.59,70.04 184.37,72.62 171.5,85.16 174.54,102.86 158.64,94.51 142.74,102.86\n\t\t145.78,85.16 132.92,72.62 150.69,70.04 \t\"/> <polygon fill=\"#FFFFFF\" points=\"95.63,72.23 103.58,88.34 121.36,90.93 108.5,103.46 111.53,121.17 95.63,112.81 79.73,121.17\n\t\t82.77,103.46 69.91,90.93 87.68,88.34 \t\"/> </g> <path fill=\"#FFDA44\" d=\"M217.53,259.33l-27.22-13.61l-27.22-54.43h27.22l27.22-27.22l13.61-27.22l27.22-13.61l13.61,13.61l27.22,13.61\n\tv13.61l13.61,13.61l40.83,27.22L340,245.73l-40.83,40.83l-13.61-27.22l-40.83,27.22v27.22l-13.61-13.61L217.53,259.33z\"/> </svg> ";
// Exports
/* harmony default export */ var xk = (xk_code);
;// ./templates/flags/kw.svg
// Module
var kw_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"0 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <rect y=\"85.337\" fill=\"#6DA544\" width=\"512\" height=\"113.775\"/> <rect y=\"312.888\" fill=\"#D80027\" width=\"512\" height=\"113.775\"/> <polygon points=\"166.957,312.889 0,426.663 0,85.337 166.957,199.111 \"/> </svg> ";
// Exports
/* harmony default export */ var kw = (kw_code);
;// ./templates/flags/kg.svg
// Module
var kg_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85 0 342 342\"> <g fill=\"#D80027\"> <rect y=\"0\" width=\"513\" height=\"342\"/> <circle cx=\"256\" cy=\"170.7\" r=\"170.7\"/> </g> <polygon fill=\"#FFDA44\" points=\"382.4,170.7 330.8,195 358.3,245 302.2,234.3 295.1,290.9 256,249.2 216.9,290.9 209.8,234.3\n\t153.7,245 181.2,195 129.6,170.7 181.2,146.4 153.7,96.3 209.8,107.1 216.9,50.4 256,92.1 295.1,50.4 302.2,107.1 358.3,96.3\n\t330.8,146.4 \"/> <circle fill=\"#D80027\" cx=\"257.4\" cy=\"170.7\" r=\"71.6\"/> <g fill=\"#FFDA44\"> <path d=\"M214.2,170.7c-2.1,0-4.1,0.1-6.2,0.3c0.1,12,4.4,22.9,11.6,31.5c3.8-10.3,9.5-19.6,16.7-27.7\n\t\tC229.4,172.1,222,170.7,214.2,170.7z\"/> <path d=\"M240.8,217.2c5.2,1.9,10.8,2.9,16.6,2.9c5.8,0,11.4-1,16.6-2.9c-2.8-11.1-8.7-21-16.6-28.8\n\t\tC249.5,196.2,243.6,206.1,240.8,217.2z\"/> <path d=\"M300.2,146c-8.5-14.8-24.5-24.7-42.8-24.7c-18.3,0-34.2,9.9-42.8,24.7c15.6,0.1,30.2,4.3,42.8,11.6\n\t\tC270,150.3,284.6,146,300.2,146z\"/> <path d=\"M278.5,174.8c7.2,8,12.9,17.4,16.7,27.7c7.2-8.5,11.5-19.5,11.6-31.5c-2-0.2-4.1-0.3-6.2-0.3\n\t\tC292.9,170.7,285.4,172.1,278.5,174.8z\"/> </g> </svg> ";
// Exports
/* harmony default export */ var kg = (kg_code);
;// ./templates/flags/la.svg
// Module
var la_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#0052B4\" width=\"513\" height=\"342\"/> <g fill=\"#D80027\"> <rect y=\"0.1\" width=\"513\" height=\"90.7\"/> <rect x=\"0\" y=\"251.3\" width=\"513\" height=\"90.7\"/> </g> <circle fill=\"#FFFFFF\" cx=\"256.5\" cy=\"171\" r=\"65.9\"/> </svg> ";
// Exports
/* harmony default export */ var la = (la_code);
;// ./templates/flags/lv.svg
// Module
var lv_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#FFFFFF\" width=\"513\" height=\"342\"/> <g fill=\"#A2001D\"> <rect y=\"0\" class=\"st1\" width=\"513\" height=\"127.6\"/> <rect x=\"0\" y=\"214.4\" class=\"st1\" width=\"513\" height=\"127.6\"/> </g> </svg> ";
// Exports
/* harmony default export */ var lv = (lv_code);
;// ./templates/flags/lb.svg
// Module
var lb_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#FFFFFF\" width=\"513\" height=\"342\"/> <g fill=\"#D80027\"> <rect y=\"0.2\" width=\"513\" height=\"90.7\"/> <rect x=\"0.5\" y=\"251.3\" width=\"512.5\" height=\"90.7\"/> </g> <polygon fill=\"#6DA544\" points=\"290.3,193.5 256,130.7 221.7,193.5 247.4,193.5 247.4,210.7 264.6,210.7 264.6,193.5 \"/> <path fill=\"#14AF5A\" d=\"M241.3,213.1c4.4-4.4,4.4-11.6-0.1-16c0,0,0,0,0,0l4.7,4.7c-5.2-4.7-12.2-6.6-19.1-5.3l-23.5,5.9\n\t\tc-6.1,1.5-6.8-0.4-1.8-4.2l27.1-20.3c5.1-3.8,4.2-6.9-2.3-6.9h-11c-6.4,0-7-2.3-1.5-5.1l25.3-12.7c5.6-2.8,5.1-5.1-1.5-5.1h-11\n\t\tc-6.4,0-7.4-3.1-2.3-6.9l27.1-20.3c5.6-3.8,12.9-3.8,18.5,0l27.1,20.3c5.1,3.8,4.2,6.9-2.3,6.9h-11c-6.4,0-7,2.3-1.5,5.1l25.3,12.7\n\t\tc5.6,2.8,5.1,5.1-1.5,5.1h-11c-6.4,0-7.4,3.1-2.3,6.9l27.1,20.3c5.1,3.8,4.4,5.7-1.8,4.2l-23.5-5.9c-6.8-1.3-13.9,0.6-19.1,5.3\n\t\tl4.7-4.7c-4.4,4.4-4.5,11.5-0.1,16c0,0,0,0,0,0l6.8,6.8c4.4,4.4,3.1,8-3.3,8h-45.8c-6.2,0-7.7-3.6-3.3-8\n\t\tC234.5,220,241.3,213.1,241.3,213.1z\"/> </svg> ";
// Exports
/* harmony default export */ var lb = (lb_code);
;// ./templates/flags/ls.svg
// Module
var ls_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#FFFFFF\" width=\"513\" height=\"342\"/> <rect y=\"0\" fill=\"#0052B4\" width=\"513\" height=\"114\"/> <rect y=\"227.6\" fill=\"#6DA544\" width=\"513\" height=\"114\"/> <path fill=\"#000000\" d=\"M245.2,171l7.7-23.1c2-5.9,5.1-6,7.1,0l7.7,23.1l19.4,29.1c1.7,2.6,1.3,6.3-1.2,8c0,0-6.9,8-29.5,8\n\ts-29.5-8-29.5-8c-2.4-2-2.9-5.4-1.2-8L245.2,171z\"/> <path fill=\"#000000\" d=\"M256.5,171c-12.5,0-22.6-10.1-22.6-22.6c0-12.5,10.1-22.6,22.6-22.6s22.6,10.1,22.6,22.6C279.1,160.9,268.9,171,256.5,171z\n\t M256.5,171c6.2,0,11.3-16.3,11.3-22.6c0-6.2-5.1-11.3-11.3-11.3c-6.2,0-11.3,5.1-11.3,11.3C245.2,154.7,250.2,171,256.5,171z\"/> </svg> ";
// Exports
/* harmony default export */ var ls = (ls_code);
;// ./templates/flags/lr.svg
// Module
var lr_code = "<svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"5.13 0 342 342\"> <g fill=\"#bf0a30\"> <rect x=\"0\" y=\"0\" width=\"512\" height=\"31.1\"/> <rect x=\"0\" y=\"62.2\" width=\"512\" height=\"31.1\"/> <rect x=\"0\" y=\"124.5\" width=\"512\" height=\"31.1\"/> <rect x=\"0\" y=\"186.8\" width=\"512\" height=\"31.1\"/> <rect x=\"0\" y=\"249.3\" width=\"512\" height=\"31.1\"/> <rect x=\"0\" y=\"310.9\" width=\"512\" height=\"31.1\"/> </g> <rect fill=\"#002868\" width=\"155.6\" height=\"155.6\"/> <polygon fill=\"#FFFFFF\" points=\"77.8,30.2 88.5,63.1 123.1,63.1 95.1,83.5 105.8,116.4 77.8,96.1 49.8,116.4 60.5,83.5 32.5,63.1\n  67.1,63.1 \"/> </svg> ";
// Exports
/* harmony default export */ var lr = (lr_code);
;// ./templates/flags/ly.svg
// Module
var ly_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" width=\"512\" height=\"341.326\"/> <rect y=\"85.337\" fill=\"#D80027\" width=\"512\" height=\"113.775\"/> <rect y=\"312.888\" fill=\"#496E2D\" width=\"512\" height=\"113.775\"/> <g> <polygon fill=\"#FFFFFF\" points=\"281.424,236.026 290.386,248.376 304.899,243.67 295.924,256.009 304.885,268.358    290.376,263.634 281.4,275.974 281.41,260.716 266.9,255.991 281.414,251.285  \"/> <path fill=\"#FFFFFF\" d=\"M256.965,286.868c-17.048,0-30.868-13.821-30.868-30.868s13.821-30.868,30.868-30.868   c5.316,0,10.317,1.345,14.684,3.71c-6.851-6.698-16.218-10.833-26.556-10.833c-20.982,0-37.992,17.01-37.992,37.992   s17.01,37.992,37.992,37.992c10.338,0,19.706-4.135,26.556-10.833C267.283,285.525,262.281,286.868,256.965,286.868z\"/> </g> </svg> ";
// Exports
/* harmony default export */ var ly = (ly_code);
;// ./templates/flags/li.svg
// Module
var li_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"51.3 0 342 342\"> <rect y=\"0\" fill=\"#D80027\" width=\"513\" height=\"342\"/> <rect y=\"0\" fill=\"#0052B4\" width=\"513\" height=\"171\"/> <path fill=\"#FFDA44\" d=\"M149.3,98.1c0-14-11.3-25.3-25.3-25.3c-6.5,0-12.4,2.4-16.9,6.4V64.4h8.4V47.5h-8.4v-8.4H90.3v8.4h-8.4v16.9\n\th8.4v14.9c-4.5-4-10.4-6.4-16.9-6.4c-14,0-25.3,11.3-25.3,25.3c0,7.5,3.3,14.2,8.4,18.8v14.9h84.3v-14.9\n\tC146.1,112.3,149.3,105.5,149.3,98.1z\"/> </svg> ";
// Exports
/* harmony default export */ var li = (li_code);
;// ./templates/flags/lt.svg
// Module
var lt_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#6DA544\" width=\"512\" height=\"341.326\"/> <rect y=\"85.337\" fill=\"#FFDA44\" width=\"512\" height=\"113.775\"/> <rect y=\"312.888\" fill=\"#D80027\" width=\"512\" height=\"113.775\"/> </svg> ";
// Exports
/* harmony default export */ var lt = (lt_code);
;// ./templates/flags/lu.svg
// Module
var lu_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <rect y=\"85.337\" fill=\"#D80027\" width=\"512\" height=\"113.775\"/> <rect y=\"312.888\" fill=\"#338AF3\" width=\"512\" height=\"113.775\"/> </svg> ";
// Exports
/* harmony default export */ var lu = (lu_code);
;// ./templates/flags/mo.svg
// Module
var mo_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#00745a\" width=\"512\" height=\"341.337\"/> <path fill=\"#FFFFFF\" d=\"M285.682,248.878c-4.19,0-8.166,0.836-11.861,2.164c2.023-4.509,3.379-9.527,3.379-14.885  c0-21.201-21.201-37.101-21.201-37.101s-21.201,15.901-21.201,37.101c0,5.357,1.356,10.375,3.379,14.885  c-3.693-1.328-7.671-2.164-11.861-2.164c-21.201,0-37.101,21.201-37.101,21.201s15.901,21.201,37.101,21.201  c12.429,0,23.031-7.286,29.682-13.315c6.65,6.03,17.251,13.315,29.682,13.315c21.201,0,37.101-21.201,37.101-21.201  S306.882,248.878,285.682,248.878z\"/> <g> <polygon fill=\"#FFDA44\" points=\"256,152.111 260.38,165.587 274.551,165.587 263.087,173.919 267.463,187.395 256,179.065    244.537,187.395 248.913,173.919 237.449,165.587 251.62,165.587  \"/> <polygon fill=\"#FFDA44\" points=\"202.112,175.683 210.537,183.268 220.355,177.601 215.745,187.956 224.168,195.544    212.894,194.359 208.283,204.714 205.925,193.626 194.65,192.441 204.468,186.772  \"/> <polygon fill=\"#FFDA44\" points=\"162.662,215.132 173.752,217.49 179.42,207.672 180.606,218.946 191.695,221.304    181.338,225.914 182.524,237.188 174.937,228.765 164.58,233.375 170.249,223.557  \"/> <polygon fill=\"#FFDA44\" points=\"309.889,175.683 301.464,183.268 291.646,177.601 296.257,187.956 287.832,195.544    299.107,194.359 303.717,204.714 306.076,193.626 317.351,192.441 307.533,186.772  \"/> <polygon fill=\"#FFDA44\" points=\"349.338,215.132 338.249,217.49 332.58,207.672 331.394,218.946 320.306,221.304    330.663,225.914 329.478,237.188 337.064,228.765 347.421,233.375 341.752,223.557  \"/> </g> <g> <path fill=\"#FFFFFF\" d=\"M256,350.642c16.84,0,32.363-5.619,44.816-15.073h-89.63   C223.637,345.023,239.16,350.642,256,350.642z\"/> <path fill=\"#FFFFFF\" d=\"M187.977,306.12c2.355,5.39,5.341,10.44,8.85,15.073h118.345c3.508-4.632,6.495-9.682,8.85-15.073   H187.977z\"/> </g> </svg> ";
// Exports
/* harmony default export */ var mo = (mo_code);
;// ./templates/flags/mk.svg
// Module
var mk_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.333\" fill=\"#FFDA44\" width=\"512\" height=\"341.337\"/> <g> <polygon fill=\"#D80027\" points=\"383.875,426.662 256,252.286 282.827,426.662  \"/> <polygon fill=\"#D80027\" points=\"133.565,85.33 256,252.286 230.314,85.33  \"/> <polygon fill=\"#D80027\" points=\"229.171,426.662 256,252.286 128.124,426.662  \"/> <polygon fill=\"#D80027\" points=\"0,85.33 0,212.9 256,252.286 28.333,85.33  \"/> <polygon fill=\"#D80027\" points=\"0,426.662 18.212,426.662 256,252.286 0,291.67  \"/> <polygon fill=\"#D80027\" points=\"256,252.286 512,212.9 512,85.33 483.669,85.33  \"/> <polygon fill=\"#D80027\" points=\"281.686,85.33 256,252.286 378.434,85.33  \"/> <polygon fill=\"#D80027\" points=\"512,426.662 512,291.671 256,252.286 493.787,426.662  \"/> <circle fill=\"#D80027\" cx=\"256\" cy=\"252.29\" r=\"59.359\"/> </g> <circle fill=\"#FFDA44\" cx=\"256\" cy=\"252.29\" r=\"44.522\"/> </svg> ";
// Exports
/* harmony default export */ var mk = (mk_code);
;// ./templates/flags/mg.svg
// Module
var mg_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"64.853 85.333 341.333 341.333\"> <path fill=\"#6DA544\" d=\"M512,426.666V85.329c-238.65,0-512,0-512,0v341.337L512,426.666L512,426.666z\"/> <path fill=\"#D80027\" d=\"M512,85.331H0v170.666c0,0,273.35,0,512,0V85.331z\"/> <rect y=\"85.334\" fill=\"#FFFFFF\" width=\"181.793\" height=\"341.337\"/> </svg> ";
// Exports
/* harmony default export */ var mg = (mg_code);
;// ./templates/flags/mw.svg
// Module
var mw_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85 0 342 342\"> <rect y=\"0\" fill=\"#272727\" width=\"512\" height=\"114\"/> <rect y=\"114\" fill=\"#e40112\" width=\"512\" height=\"114\"/> <rect y=\"228\" fill=\"#07893f\" width=\"512\" height=\"114\"/> <circle fill=\"#e40112\" cx=\"256\" cy=\"125\" r=\"95\"/> </svg> ";
// Exports
/* harmony default export */ var mw = (mw_code);
;// ./templates/flags/my.svg
// Module
var my_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#FFFFFF\" width=\"513\" height=\"342\"/> <g fill=\"#cc0000\"> <rect y=\"0\" width=\"513\" height=\"24.4\"/> <rect y=\"48.9\" width=\"513\" height=\"24.4\"/> <rect y=\"97.7\" width=\"513\" height=\"24.4\"/> <rect y=\"146.6\" width=\"513\" height=\"24.4\"/> <rect y=\"195.4\" width=\"513\" height=\"24.4\"/> <rect y=\"244.3\" width=\"513\" height=\"24.4\"/> <rect y=\"293.1\" width=\"513\" height=\"24.4\"/> </g> <rect y=\"0\" fill=\"#000066\" width=\"256.5\" height=\"195.4\"/> <g fill=\"#ffcc00\"> <path d=\"M153.3,42.1C122.6,30.7,88.5,46.3,77.1,77s4.2,64.8,34.9,76.2c13.3,5,28,5,41.4,0\n    c-30.7,24.5-75.4,19.6-100-11.1s-19.6-75.4,11.1-100C90.5,21.4,127.4,21.4,153.3,42.1z M180,117.1l-20.6,23.3l5.4-30.6l-31-1.6\n    l27.3-14.9l-18.1-25.3l28.6,12l8.4-29.9l8.4,29.9l28.6-12L199,93.4l27.3,14.9l-31,1.6l5.4,30.6L180,117.1z\"/> </g> </svg> ";
// Exports
/* harmony default export */ var my = (my_code);
;// ./templates/flags/mv.svg
// Module
var mv_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#d01920\" width=\"512\" height=\"341.333\"/> <rect x=\"144.696\" y=\"174.375\" fill=\"#017c3b\" width=\"222.609\" height=\"163.25\"/> <path fill=\"#FFFFFF\" d=\"M283.484,304.226c-26.637,0-48.232-21.594-48.232-48.232s21.594-48.232,48.232-48.232  c8.306,0,16.12,2.1,22.943,5.797c-10.703-10.467-25.341-16.927-41.494-16.927c-32.784,0-59.362,26.577-59.362,59.362  s26.578,59.362,59.362,59.362c16.154,0,30.791-6.461,41.494-16.927C299.605,302.127,291.791,304.226,283.484,304.226z\"/> </svg> ";
// Exports
/* harmony default export */ var mv = (mv_code);
;// ./templates/flags/ml.svg
// Module
var ml_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#FFDA44\" width=\"512\" height=\"341.337\"/> <rect y=\"85.331\" fill=\"#6DA544\" width=\"170.663\" height=\"341.337\"/> <rect x=\"341.337\" y=\"85.331\" fill=\"#D80027\" width=\"170.663\" height=\"341.337\"/> </svg> ";
// Exports
/* harmony default export */ var ml = (ml_code);
;// ./templates/flags/mt.svg
// Module
var mt_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#FFFFFF\" width=\"513\" height=\"342\"/> <rect x=\"256\" y=\"0\" fill=\"#C31B28\" width=\"256.5\" height=\"342\"/> <polygon fill=\"#ACABB1\" stroke=\"#C31B28\" stroke-width=\"2\" points=\"101.2,68.2 101.2,33 66,33 66,68.2 30.8,68.2 30.8,103.4 66,103.4 66,138.6 101.2,138.6 101.2,103.4\n\t136.4,103.4 136.4,68.2 \"/> </svg> ";
// Exports
/* harmony default export */ var mt = (mt_code);
;// ./templates/flags/mh.svg
// Module
var mh_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#1E509C\" width=\"513\" height=\"342\"/> <path fill=\"#FFFFFF\" d=\"M513,176.1V81.3L0,342L513,176.1z\"/> <path fill=\"#F18D36\" d=\"M513,0v81.3L0,342L513,0z\"/> <polygon fill=\"#FFFFFF\" points=\"126.7,99.3 118.9,45.2 111.2,99.3 90.4,75.6 101.1,107.2 69.6,96.5 93.2,117.3 39.1,125 93.2,132.7\n\t69.6,153.5 101.1,142.8 90.4,174.4 111.2,150.7 118.9,204.8 126.7,150.7 147.4,174.4 136.8,142.8 168.3,153.5 144.7,132.7\n\t198.7,125 144.7,117.3 168.3,96.5 136.8,107.2 147.4,75.6 \"/> </svg> ";
// Exports
/* harmony default export */ var mh = (mh_code);
;// ./templates/flags/mr.svg
// Module
var mr_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 100 342 342\"> <rect y=\"100\" fill=\"#006233\" width=\"513\" height=\"342\"/> <rect y=\"100\" fill=\"#cd2a3e\" width=\"513\" height=\"46\"/> <rect y=\"396\" fill=\"#cd2a3e\" width=\"513\" height=\"46\"/> <path fill=\"#ffc400\" d=\"M256,298.851c-45.956,0-84.348-32.298-93.767-75.429c-1.448,6.63-2.233,13.507-2.233,20.572   c0,53.02,42.979,96,96,96s96-42.98,96-96c0-7.066-0.785-13.942-2.233-20.572C340.348,266.553,301.956,298.851,256,298.851z\"/> <polygon fill=\"#ffc400\" points=\"255.999,171.994 264.934,199.496 293.852,199.496 270.458,216.492 279.394,243.995    255.999,226.997 232.605,243.995 241.542,216.492 218.148,199.496 247.063,199.496  \"/> </svg> ";
// Exports
/* harmony default export */ var mr = (mr_code);
;// ./templates/flags/mu.svg
// Module
var mu_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#f7ce00\" width=\"512\" height=\"341.326\"/> <rect y=\"85.331\" fill=\"#e32737\" width=\"512\" height=\"85.337\"/> <rect y=\"170.657\" fill=\"#191f6a\" width=\"512\" height=\"85.337\"/> <rect y=\"341.331\" fill=\"#00a04e\" width=\"512\" height=\"85.337\"/> </svg> ";
// Exports
/* harmony default export */ var mu = (mu_code);
;// ./templates/flags/yt.svg
// Module
var yt_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"15 0 60 60\"> <rect fill=\"#FFFFFF\" width=\"90\" height=\"60\"/> <path stroke=\"#7f7f7f\" stroke-width=\"0.25\" fill=\"#ABABAB\" d=\"M75.6,39.6c-1.1,0.7,0,1.8-0.6,1.9c-0.6,0.1-1.5-1.4-0.2-2.5c1.6-1.5,5.8,0,5.9,4.3c0,2.2-0.9,6.8-7.2,6.8\n\tc-7.6,0-11.4-5.7-11.4-11.4c0-4.8,3.7-13.5,6.5-14.6c3.1-1.2,6.7-3.5,6.8-5c0-0.5-1.4-0.1-3.3,0.9c-3.1,1.7-6.6,2.5-6.6,1.4\n\ts2.9-3.1,4-4.6c1.2-1.6,0.9-5.5,3-5.5c7.6,0,9.9,7.7,7,11.7c-1.9,2.6,4.3,1,4.3,1s0,5.3-3.2,7.3c-3,1.8-4.8,3.9-4.8,3.9\n\ts0.2-4.6-0.5-3.9c-0.6,0.7-11.1,4.5-10.3,12c0.3,3,4.5,5.4,8.5,5.2s5.7-2.4,5.8-4.9C79.4,39.7,76.3,39.2,75.6,39.6z\"/> <path stroke=\"#7f7f7f\" stroke-width=\"0.25\" fill=\"#ABABAB\" d=\"M10.7,43.6c0.1,2.5,1.8,4.7,5.8,4.9s8.2-2.2,8.5-5.2c0.8-7.5-9.7-11.3-10.3-12c-0.7-0.7-0.5,3.9-0.5,3.9\n\ts-1.8-2.1-4.8-3.9c-3.2-2-3.2-7.3-3.2-7.3s6.2,1.6,4.3-1c-2.9-4-0.6-11.7,7-11.7c2.1,0,1.8,3.9,3,5.5c1.1,1.5,4,3.5,4,4.6\n\tS21,21.7,17.9,20c-1.9-1-3.3-1.4-3.3-0.9c0.1,1.5,3.7,3.8,6.8,5c2.8,1.1,6.5,9.8,6.5,14.6c0,5.7-3.8,11.4-11.4,11.4\n\tc-6.3,0-7.2-4.6-7.2-6.8c0.1-4.3,4.3-5.8,5.9-4.3c1.2,1.1,0.4,2.7-0.2,2.5c-0.6-0.1,0.6-1.2-0.6-1.9C13.8,39.2,10.7,39.7,10.7,43.6z\n\t\"/> <g> <path fill=\"#DE393A\" d=\"M31,28.8c0,0,0,12.1,0,14s2.1,1.6,2.1,1.6s8.2,0,9.3,0c1.2,0,2.6,2.3,2.6,2.3s1.4-2.3,2.5-2.3s9.6,0,9.6,0\n\t\ts2,0.2,2-1.8s0-13.9,0-13.9H31z\"/> <rect x=\"31\" y=\"13.1\" fill=\"#3951A3\" width=\"28\" height=\"15.7\"/> </g> <path fill=\"#FFFFFF\" d=\"M50.1,16.5c-0.4,2.4-2.5,4.2-5.1,4.2c-2.5,0-4.6-1.8-5.1-4.2c-0.5,0.9-0.8,1.9-0.8,3c0,3.3,2.6,5.9,5.9,5.9\n\tc3.3,0,5.9-2.6,5.9-5.9C50.9,18.4,50.6,17.4,50.1,16.5z\"/> <circle fill=\"#FEE05F\" cx=\"39.1\" cy=\"36.5\" r=\"3\"/> <circle fill=\"#FEE05F\" cx=\"50.9\" cy=\"36.5\" r=\"3\"/> </svg> ";
// Exports
/* harmony default export */ var yt = (yt_code);
;// ./templates/flags/mx.svg
// Module
var mx_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <polygon fill=\"#dc2339\" points=\"342,0 171,0 0,0 0,341.3 171,341.3 342,341.3 513,341.3 513,0 \"/> <rect y=\"0\" fill=\"#11865d\" width=\"171\" height=\"342\"/> <rect x=\"171\" y=\"0\" fill=\"#FFFFFF\" width=\"171\" height=\"342\"/> <path fill=\"#8C9157\" d=\"M195.8,171.2c0,21.6,11.5,41.7,30.3,52.5c5.8,3.4,13.2,1.4,16.6-4.4c3.4-5.8,1.4-13.2-4.4-16.6\n\tc-11.3-6.5-18.2-18.5-18.2-31.5c0-6.7-5.4-12.1-12.1-12.1C201.3,159.1,195.8,164.5,195.8,171.2z M289.2,222.3\n\tc17.5-11.1,28-30.4,28-51.1c0-6.7-5.4-12.1-12.1-12.1s-12.1,5.4-12.1,12.1c0,12.4-6.3,24-16.8,30.7c-5.7,3.5-7.5,10.9-4.1,16.7\n\ts10.9,7.5,16.7,4.1C288.8,222.5,289,222.4,289.2,222.3z\"/> <ellipse fill=\"#C59262\" cx=\"256.5\" cy=\"159.1\" rx=\"24.3\" ry=\"36.4\"/> </svg> ";
// Exports
/* harmony default export */ var mx = (mx_code);
;// ./templates/flags/fm.svg
// Module
var fm_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.35 0 341.3 341.3\"> <rect y=\"0\" fill=\"#338AF3\" width=\"512\" height=\"341.3\"/> <g> <polygon fill=\"#FFFFFF\" points=\"256,37.9 266.1,69.1 299,69.1 272.4,88.4 282.6,119.6 256,100.3 229.4,119.6 239.6,88.4 213,69.1\n\t\t245.9,69.1 \t\"/> <polygon fill=\"#FFFFFF\" points=\"123.2,170.7 154.4,160.5 154.4,127.7 173.7,154.3 204.9,144.1 185.6,170.7 204.9,197.2 173.7,187.1\n\t\t154.4,213.6 154.4,180.8 \t\"/> <polygon fill=\"#FFFFFF\" points=\"256,303.5 245.9,272.3 213,272.3 239.6,253 229.4,221.7 256,241 282.6,221.7 272.4,253 299,272.3\n\t\t266.1,272.3 \t\"/> <polygon fill=\"#FFFFFF\" points=\"388.8,170.7 357.6,180.8 357.6,213.6 338.3,187.1 307.1,197.2 326.4,170.7 307.1,144.1 338.3,154.3\n\t\t357.6,127.7 357.6,160.5 \t\"/> </g> </svg> ";
// Exports
/* harmony default export */ var fm = (fm_code);
;// ./templates/flags/md.svg
// Module
var md_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#FFDA44\" width=\"513\" height=\"342\"/> <rect x=\"342\" y=\"0\" fill=\"#D80027\" width=\"171\" height=\"342\"/> <rect y=\"0\" fill=\"#0052B4\" width=\"171\" height=\"342\"/> <path fill=\"#AF7F59\" d=\"M206.2,129.1h33.2L256,79.3l16.6,49.8h33.2v99.6L256,262l-49.8-33.2V129.1z M239.4,162.4v49.8h33.2v-49.8H239.4\n\tz\"/> </svg> ";
// Exports
/* harmony default export */ var md = (md_code);
;// ./templates/flags/mc.svg
// Module
var mc_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.333\" fill=\"#FFFFFF\" width=\"512\" height=\"341.333\"/> <rect y=\"85.333\" fill=\"#c70000\" width=\"512\" height=\"170.667\"/> </svg> ";
// Exports
/* harmony default export */ var mc = (mc_code);
;// ./templates/flags/mn.svg
// Module
var mn_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"22.23 0 342 342\"> <rect y=\"0\" fill=\"#095FAD\" width=\"513\" height=\"342\"/> <g> <rect x=\"342\" y=\"0\" fill=\"#BE1229\" width=\"171\" height=\"342\"/> <rect y=\"0\" fill=\"#BE1229\" width=\"171\" height=\"342\"/> </g> <g fill=\"#FFDA44\"> <rect x=\"108.3\" y=\"166.3\" width=\"14.8\" height=\"74.2\"/> <rect x=\"48.9\" y=\"166.3\" width=\"14.8\" height=\"74.2\"/> <circle cx=\"86\" cy=\"203.4\" r=\"14.8\"/> <rect x=\"71.2\" y=\"225.7\" width=\"29.7\" height=\"14.8\"/> <rect x=\"71.2\" y=\"166.3\" width=\"29.7\" height=\"14.8\"/> <circle cx=\"86\" cy=\"144\" r=\"14.8\"/> <polygon points=\"76.3,123.9 95.8,123.9 86,108.3 \t\"/> </g> </svg> ";
// Exports
/* harmony default export */ var mn = (mn_code);
;// ./templates/flags/me.svg
// Module
var me_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#E6BE53\" width=\"513\" height=\"342\"/> <rect x=\"20\" y=\"20\" fill=\"#E01D24\" width=\"473\" height=\"302\"/> <path fill=\"#E6BE53\" d=\"M248.5,112.9c4.4,4.4,11.6,4.4,16,0l-4.7,4.7l22.7-22.7c4.9-4.5,12.1-5.6,18.2-2.8l2.1,1\n\t\tc5.7,2.9,6.7,8.7,2.3,13.1l4.7-4.7L287,124.3c-4.5,4.9-5.6,12.1-2.8,18.2l1,2.1c2.9,5.7,5.1,15.4,5.1,21.6v-11.3\n\t\tc0,6.2,4.4,9.1,10.2,6.2l2.1-1c5.7-2.9,6.7-8.7,2.3-13.1l4.7,4.7c-4.4-4.4-4.4-11.6,0-16c0,0,0,0,0,0l-4.7,4.7\n\t\tc5.2-5.5,9.6-11.6,13.1-18.2l1-2.1c2.9-5.7,6.3-15.1,7.9-21.2l5.8-23.3c1.5-6,5.9-6.8,9.6-1.8l20.2,26.9c3.8,5.1,6.9,14,6.9,20.6\n\t\tv22.4c0,6.3-2.2,15.8-5.1,21.7l-1,2.1c-3.5,6.6-7.9,12.7-13.1,18.2l-6.8,6.8c-5.4,5.2-11.5,9.6-18.2,13.1l-2.1,1\n\t\tc-5.7,2.9-15.5,5.1-21.6,5.1c-6.2,0-6.9,2.2-1.1,5.1l2.1,1c5.7,2.9,13.8,8.7,18.2,13.1l-4.7-4.7c4.4,4.4,11.6,4.4,16,0l-4.7,4.7\n\t\tc4.4-4.4,11.6-4.4,16,0c0,0,0,0,0,0L339,226c4.6,4.9,5.7,12.1,2.9,18.2l-1,2.1c-2.9,5.7-10.4,10.2-16.4,10.2\n\t\tc-7.5-0.3-14.8-2-21.6-5.1l-2.1-1c-6.5-3.6-11.8-8.9-15.4-15.4l-1-2.1c-2.9-5.7-8.7-6.7-13.1-2.3l4.7-4.7\n\t\tc-4.4,4.7-5.2,11.8-1.8,17.3l10.3,15.4c3.4,5.1,2,12.1-3.1,15.5l-15.4,10.3c-5.7,3.4-12.8,3.4-18.6,0l-15.4-10.3\n\t\tc-5.1-3.4-6.5-10.3-3.1-15.5l10.3-15.4c3.4-5.5,2.6-12.6-1.8-17.3l4.7,4.7c-4.4-4.4-10.2-3.6-13.1,2.3l-1,2.1\n\t\tc-2.9,5.7-9.5,12.4-15.4,15.4l-2.1,1c-5.7,2.9-15.5,5.1-21.6,5.1c-6.9-0.3-13.1-4.2-16.4-10.2l-1-2.1c-2.8-6.1-1.6-13.3,2.9-18.2\n\t\tl-4.7,4.7c4.4-4.4,11.6-4.4,16,0c0,0,0,0,0,0l-4.7-4.7c4.4,4.4,11.6,4.4,16,0l-4.7,4.7c5.5-5.2,11.6-9.6,18.2-13.1l2.1-1\n\t\tc5.7-2.9,5-5.1-1.1-5.1c-7.5-0.3-14.8-2-21.6-5.1l-2.1-1c-6.6-3.5-12.7-7.9-18.2-13.1l-6.8-6.8c-5.2-5.4-9.6-11.5-13.1-18.2l-1-2.1\n\t\tc-3.1-6.8-4.9-14.2-5.1-21.7V121c0-6.3,3.1-15.6,6.9-20.6l20.2-26.9c3.8-5.1,8.1-4.3,9.6,1.8l5.8,23.3c2,7.3,4.6,14.4,7.9,21.2\n\t\tl1,2.1c2.9,5.7,8.7,13.8,13.1,18.2l-4.7-4.7c4.4,4.4,4.4,11.6,0,16c0,0,0,0,0,0l4.7-4.7c-4.4,4.4-3.6,10.2,2.3,13.1l2.1,1\n\t\tc5.7,2.9,10.2,0,10.2-6.2v11.3c0-6.2,2.2-15.7,5.1-21.6l1-2.1c2.8-6.1,1.7-13.2-2.8-18.2l-22.7-22.7l4.7,4.7\n\t\tc-4.4-4.4-3.6-10.2,2.3-13.1l2.1-1c6.1-2.8,13.2-1.7,18.2,2.8l22.7,22.7L248.5,112.9z M230.5,72.2c-4.4-4.4-3.2-9.5,2.8-11.5\n\t\tl12.4-4.2c6-2,15.5-2,21.5,0l12.4,4.2c6,2,7.2,7.1,2.8,11.5l-6.8,6.8c-5.3,4.7-12.1,7.5-19.2,7.9c-6.2,0-14.8-3.5-19.2-7.9\n\t\tC237.3,79,230.5,72.2,230.5,72.2z\"/> <path fill=\"#215F90\" d=\"M233.9,200.2c0,6.2,1.7,6.4,4.2,0.5c0,0,7.1-23.2,18.4-23.2c11.3,0,18.6,23.6,18.6,23.6c2.2,5.7,4,5.3,4-1.1\n\t\tv-11.2c0-18.7-10.1-33.9-22.6-33.9c-12.5,0-22.6,15.2-22.6,33.9V200.2z\"/> </svg> ";
// Exports
/* harmony default export */ var me = (me_code);
;// ./templates/flags/ms.svg
// Module
var ms_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"121.41 0 342 342\"> <rect y=\"0\" fill=\"#1B4991\" width=\"513\" height=\"342\"/> <path fill=\"#00A2B8\" stroke=\"#FFFFFF\" stroke-width=\"3\" d=\"M318.2,106.7v106.5c0,51.9,67.8,67.8,67.8,67.8s67.8-15.9,67.8-67.8V106.7H318.2z\"/> <path fill=\"#A35023\" d=\"M319.7,212.7c0,50.8,66.3,66.3,66.3,66.3s66.3-15.6,66.3-66.3H319.7L319.7,212.7z\"/> <polygon points=\"415.1,155.1 395.7,155.1 395.7,135.7 376.3,135.7 376.3,155.1 357,155.1 357,174.5 376.3,174.5 376.3,232.6\n\t395.7,232.6 395.7,174.5 415.1,174.5 \"/> <polygon fill=\"#FFFFFF\" points=\"256,0 256,30.6 210.8,55.7 256,55.7 256,115 196.9,115 256,147.8 256,170.7 229.3,170.7 155.8,129.8\n\t155.8,170.7 100.2,170.7 100.2,122.1 12.7,170.7 0,170.7 0,140.1 45.2,115 0,115 0,55.7 59.1,55.7 0,22.8 0,0 26.7,0 100.2,40.8\n\t100.2,0 155.8,0 155.8,48.6 243.3,0 \"/> <polygon fill=\"#D80027\" points=\"144,0 112,0 112,69.3 0,69.3 0,101.3 112,101.3 112,170.7 144,170.7 144,101.3 256,101.3 256,69.3\n\t144,69.3 \"/> <polygon fill=\"#0052B4\" points=\"155.8,115 256,170.7 256,154.9 184.2,115 \"/> <polygon fill=\"#FFFFFF\" points=\"155.8,115 256,170.7 256,154.9 184.2,115 \"/> <polygon fill=\"#D80027\" points=\"155.8,115 256,170.7 256,154.9 184.2,115 \t\"/> <polygon fill=\"#D80027\" points=\"71.8,115 0,154.9 0,170.7 0,170.7 100.2,115 \t\"/> <polygon fill=\"#0052B4\" points=\"100.2,55.6 0,0 0,15.7 71.8,55.6 \"/> <polygon fill=\"#FFFFFF\" points=\"100.2,55.6 0,0 0,15.7 71.8,55.6 \"/> <polygon fill=\"#D80027\" points=\"100.2,55.6 0,0 0,15.7 71.8,55.6 \t\"/> <polygon fill=\"#D80027\" points=\"184.2,55.6 256,15.7 256,0 256,0 155.8,55.6 \t\"/> </svg> ";
// Exports
/* harmony default export */ var ms = (ms_code);
;// ./templates/flags/ma.svg
// Module
var ma_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <polygon fill=\"#D80027\" points=\"0,85.337 0,256 0,426.663 512,426.663 512,256 512,85.337 \"/> <path fill=\"#20661b\" d=\"M352,226.484h-73.337L256,156.738l-22.663,69.748H160l59.331,43.107l-22.663,69.749L256,296.232  l59.332,43.107l-22.663-69.749L352,226.484z M235.937,264.195l7.663-23.585h24.8l7.663,23.585l0,0v0.001L256,278.772  L235.937,264.195L235.937,264.195L235.937,264.195z M263.81,226.484h-15.619L256,202.447L263.81,226.484z M288.079,255.466  l-4.827-14.856h25.274L288.079,255.466z M228.748,240.61l-4.827,14.856l-20.447-14.856H228.748z M223.537,302.358l7.81-24.036  l12.636,9.181L223.537,302.358z M268.016,287.502l12.636-9.181l7.81,24.036L268.016,287.502z\"/> </svg> ";
// Exports
/* harmony default export */ var ma = (ma_code);
;// ./templates/flags/mz.svg
// Module
var mz_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"0 85.333 342 342\"> <rect y=\"85.333\" fill=\"#FFFFFF\" width=\"513\" height=\"342\"/> <rect y=\"323.333\" fill=\"#f4d900\" width=\"513\" height=\"104\"/> <rect y=\"85.333\" fill=\"#006d66\" width=\"513\" height=\"104\"/> <rect y=\"204.333\" fill=\"#000000\" width=\"513\" height=\"104\"/> <polygon fill=\"#cb0f31\" points=\"256,256.006 0,426.668 0,85.331 \"/> <polygon fill=\"#f4d900\" points=\"83.477,195.132 98.584,241.63 147.478,241.63 107.924,270.369 123.031,316.868   83.477,288.13 43.922,316.868 59.032,270.369 19.478,241.63 68.37,241.63 \"/> </svg> ";
// Exports
/* harmony default export */ var mz = (mz_code);
;// ./templates/flags/mm.svg
// Module
var mm_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#6DA544\" width=\"512\" height=\"341.326\"/> <rect y=\"85.337\" fill=\"#FFDA44\" width=\"512\" height=\"113.775\"/> <rect y=\"312.888\" fill=\"#D80027\" width=\"512\" height=\"113.775\"/> <path fill=\"#FFFFFF\" d=\"M384,227.261h-97.783L256,134.265l-30.217,92.997H128l79.108,57.475l-30.217,92.998L256,320.925  l79.108,56.81l-30.217-92.998L384,227.261z\"/> </svg> ";
// Exports
/* harmony default export */ var mm = (mm_code);
;// ./templates/flags/na.svg
// Module
var na_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"32.427 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#496E2D\" width=\"512\" height=\"341.326\"/> <polyline fill=\"#0052B4\" points=\"0,426.663 0,85.337 512,85.337 \"/> <polygon fill=\"#FFFFFF\" points=\"512,152.222 512,85.337 411.67,85.337 0,359.778 0,426.663 100.33,426.663 \"/> <polygon fill=\"#A2001D\" points=\"512,85.337 512,125.462 60.193,426.663 0,426.663 0,386.538 451.807,85.337 \"/> <polygon fill=\"#FFDA44\" points=\"187.737,189.212 164.996,199.908 177.106,221.932 152.413,217.208 149.284,242.153   132.085,223.806 114.885,242.153 111.756,217.208 87.063,221.931 99.172,199.908 76.433,189.212 99.173,178.515 87.063,156.493   111.756,161.215 114.886,136.271 132.085,154.618 149.285,136.271 152.413,161.215 177.106,156.493 164.998,178.517 \"/> </svg> ";
// Exports
/* harmony default export */ var na = (na_code);
;// ./templates/flags/nr.svg
// Module
var nr_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"44.373 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#0052B4\" width=\"512\" height=\"341.326\"/> <rect y=\"239.994\" fill=\"#FFDA44\" width=\"512\" height=\"32\"/> <polygon fill=\"#FFFFFF\" points=\"174.802,341.329 155.678,350.325 165.862,368.846 145.095,364.873 142.464,385.851   128,370.422 113.535,385.851 110.905,364.873 90.138,368.846 100.321,350.325 81.198,341.329 100.322,332.334 90.138,313.814   110.904,317.785 113.536,296.807 128,312.236 142.465,296.807 145.095,317.785 165.862,313.814 155.679,332.335 \"/> </svg> ";
// Exports
/* harmony default export */ var nr = (nr_code);
;// ./templates/flags/np.svg
// Module
var np_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"0 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <polygon fill=\"#0052B4\" points=\"0,426.663 0,85.337 280.419,260.087 87.61,260.087 271.186,426.663 \"/> <polygon fill=\"#D80027\" points=\"244.769,249.888 10.199,103.71 10.199,249.888 10.199,416.464 244.769,416.464   61.193,249.888 \"/> <g> <polygon fill=\"#FFFFFF\" points=\"98.003,324.433 83.414,317.57 91.184,303.44 75.34,306.47 73.332,290.465 62.297,302.237    51.261,290.465 49.253,306.47 33.41,303.439 41.18,317.57 26.589,324.433 41.18,331.295 33.41,345.425 49.254,342.396    51.261,358.4 62.297,346.628 73.332,358.4 75.34,342.396 91.183,345.426 83.414,331.296  \"/> <polygon fill=\"#FFFFFF\" points=\"88.268,191.662 77.656,186.671 83.307,176.393 71.784,178.598 70.323,166.957    62.297,175.518 54.271,166.957 52.811,178.598 41.286,176.393 46.938,186.671 36.325,191.662 62.297,196.856  \"/> <path fill=\"#FFFFFF\" d=\"M93.462,191.662c0,17.212-13.953,31.165-31.165,31.165s-31.165-13.953-31.165-31.165\"/> </g> </svg> ";
// Exports
/* harmony default export */ var np = (np_code);
;// ./templates/flags/nl.svg
// Module
var nl_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 85.5 342 342\"> <rect y=\"85.5\" fill=\"#FFFFFF\" width=\"513\" height=\"342\"/> <rect y=\"85.5\" fill=\"#cd1f2a\" width=\"513\" height=\"114\"/> <rect y=\"312\" fill=\"#1d4185\" width=\"513\" height=\"114\"/> </svg> ";
// Exports
/* harmony default export */ var nl = (nl_code);
;// ./templates/flags/nc.svg
// Module
var nc_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"25.596 0 426.7 426.7\"> <path fill=\"#009543\" d=\"M0,293h640v133.7H0V293z\"/> <path fill=\"#ED4135\" d=\"M0,132h640v161.3H0V132z\"/> <path fill=\"#0035AD\" d=\"M0,0h640v133.3H0V0z\"/> <circle fill=\"#FAE600\" stroke=\"#000000\" stroke-width=\"5.3\" cx=\"240\" cy=\"213.3\" r=\"157.3\"/> <path d=\"M307.9,354.5l-31.8,12l-36.1,4.2l-37.8-2.4l-35.9-15.5c0,0,28.6-29.3,40.7-33.5c8.6-3,22.4-3.7,22.4-3.7V122l-0.3-66H246\n\tl-0.5,67.5v191.7c0,0,15.3,1.2,22.1,4.1C278.9,324.2,307.9,354.5,307.9,354.5z\"/> <rect x=\"213\" y=\"91\" transform=\"matrix(0.8949 -0.4462 0.4462 0.8949 -18.4912 115.9796)\" width=\"48\" height=\"12.5\"/> <ellipse cx=\"237.8\" cy=\"280.5\" rx=\"16.8\" ry=\"26.5\"/> <circle cx=\"237.4\" cy=\"143.4\" r=\"20.9\"/> <ellipse cx=\"238.4\" cy=\"211.1\" rx=\"19.9\" ry=\"12.1\"/> <rect x=\"213.8\" y=\"177\" width=\"47\" height=\"10\"/> <rect x=\"213.8\" y=\"234\" width=\"47\" height=\"10\"/> </svg> ";
// Exports
/* harmony default export */ var nc = (nc_code);
;// ./templates/flags/nz.svg
// Module
var nz_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"114.347 85.333 341.333 341.333\"> <rect y=\"85.334\" fill=\"#0052B4\" width=\"512\" height=\"341.337\"/> <g> <polygon fill=\"#D80027\" points=\"425.301,233.745 428.689,244.173 439.652,244.173 430.782,250.617 434.17,261.044    425.301,254.6 416.43,261.044 419.818,250.617 410.948,244.173 421.911,244.173  \"/> <polygon fill=\"#D80027\" points=\"386.107,308.817 391.19,324.459 407.635,324.459 394.33,334.126 399.412,349.766    386.107,340.099 372.802,349.766 377.885,334.126 364.58,324.459 381.025,324.459  \"/> <polygon fill=\"#D80027\" points=\"387.588,185.971 391.824,199.007 405.528,199.007 394.44,207.061 398.675,220.095    387.588,212.039 376.5,220.095 380.735,207.061 369.648,199.007 383.352,199.007  \"/> <polygon fill=\"#D80027\" points=\"349.876,233.291 354.958,248.932 371.404,248.932 358.098,258.598 363.182,274.239    349.876,264.573 336.571,274.239 341.653,258.598 328.348,248.932 344.793,248.932  \"/> </g> <polygon fill=\"#FFFFFF\" points=\"256.003,85.329 256.003,115.893 210.825,140.981 256.003,140.981 256.003,200.34   196.89,200.34 256.003,233.186 256.003,255.992 229.313,255.992 155.829,215.166 155.829,255.992 100.177,255.992 100.177,207.419   12.748,255.992 0.003,255.992 0.003,225.439 45.171,200.34 0.003,200.34 0.003,140.981 59.106,140.981 0.003,108.147 0.003,85.329   26.683,85.329 100.177,126.167 100.177,85.329 155.829,85.329 155.829,133.902 243.259,85.329 \"/> <polygon fill=\"#D80027\" points=\"144,85.33 112,85.33 112,154.664 0,154.664 0,186.664 112,186.664 112,255.998 144,255.998   144,186.664 256,186.664 256,154.664 144,154.664 \"/> <polygon fill=\"#0052B4\" points=\"155.826,200.344 256,255.998 256,240.259 184.153,200.344 \"/> <polygon fill=\"#FFFFFF\" points=\"155.826,200.344 256,255.998 256,240.259 184.153,200.344 \"/> <g> <polygon fill=\"#D80027\" points=\"155.826,200.344 256,255.998 256,240.259 184.153,200.344  \"/> <polygon fill=\"#D80027\" points=\"71.846,200.344 0,240.259 0,255.998 0,255.998 100.174,200.344  \"/> </g> <polygon fill=\"#0052B4\" points=\"100.174,140.983 0,85.33 0,101.068 71.847,140.983 \"/> <polygon fill=\"#FFFFFF\" points=\"100.174,140.983 0,85.33 0,101.068 71.847,140.983 \"/> <g> <polygon fill=\"#D80027\" points=\"100.174,140.983 0,85.33 0,101.068 71.847,140.983  \"/> <polygon fill=\"#D80027\" points=\"184.154,140.983 256,101.068 256,85.33 256,85.33 155.826,140.983  \"/> </g> </svg> ";
// Exports
/* harmony default export */ var nz = (nz_code);
;// ./templates/flags/ni.svg
// Module
var ni_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <g> <rect y=\"85.337\" fill=\"#338AF3\" width=\"512\" height=\"113.775\"/> <rect y=\"312.888\" fill=\"#338AF3\" width=\"512\" height=\"113.775\"/> </g> <path fill=\"#FFDA44\" d=\"M256,214.447c-22.949,0-41.553,18.603-41.553,41.553S233.05,297.553,256,297.553  c22.949,0,41.553-18.603,41.553-41.553S278.949,214.447,256,214.447z M256,279.745c-13.114,0-23.745-10.631-23.745-23.745  s10.631-23.745,23.745-23.745c13.114,0,23.745,10.631,23.745,23.745C279.745,269.114,269.114,279.745,256,279.745z\"/> <polygon fill=\"#0052B4\" points=\"276.563,261.936 256,256 235.437,261.936 228.582,273.809 283.418,273.809 \"/> <polygon fill=\"#338AF3\" points=\"256,226.32 242.291,250.064 256,256 269.709,250.064 \"/> <polygon fill=\"#6DA544\" points=\"235.437,261.936 276.563,261.936 269.709,250.064 242.291,250.064 \"/> </svg> ";
// Exports
/* harmony default export */ var ni = (ni_code);
;// ./templates/flags/ne.svg
// Module
var ne_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <rect y=\"85.337\" fill=\"#e05206\" width=\"512\" height=\"113.775\"/> <rect y=\"312.888\" fill=\"#0db02b\" width=\"512\" height=\"113.775\"/> <circle fill=\"#e05206\" cx=\"256\" cy=\"256\" r=\"32\"/> </svg> ";
// Exports
/* harmony default export */ var ne = (ne_code);
;// ./templates/flags/ng.svg
// Module
var ng_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 85.5 342 342\"> <rect y=\"85.5\" fill=\"#FFFFFF\" width=\"513\" height=\"342\"/> <g fill=\"#007b23\"> <rect y=\"85.5\" width=\"171\" height=\"342\"/> <rect x=\"342\" y=\"85.5\" width=\"171\" height=\"342\"/> </g> </svg> ";
// Exports
/* harmony default export */ var ng = (ng_code);
;// ./templates/flags/nu.svg
// Module
var nu_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"0 85.333 341.333 341.333\"> <rect y=\"85.334\" fill=\"#FFDA44\" width=\"512\" height=\"341.337\"/> <rect y=\"85.334\" fill=\"#FFFFFF\" width=\"256\" height=\"170.663\"/> <g> <polygon fill=\"#D80027\" points=\"144,85.33 112,85.33 112,154.663 0,154.663 0,186.663 112,186.663 112,255.997 144,255.997    144,186.663 256,186.663 256,154.663 144,154.663  \"/> <polygon fill=\"#D80027\" points=\"0,85.329 0,100.412 57.377,138.663 80,138.663  \"/> </g> <g> <polygon fill=\"#2E52B2\" points=\"0,107.951 0,138.663 46.069,138.663  \"/> <polygon fill=\"#2E52B2\" points=\"96,85.331 96,134.244 22.628,85.331  \"/> </g> <polygon fill=\"#D80027\" points=\"256,85.329 256,100.412 198.623,138.663 176,138.663 \"/> <g> <polygon fill=\"#2E52B2\" points=\"256,107.951 256,138.663 209.931,138.663  \"/> <polygon fill=\"#2E52B2\" points=\"160,85.331 160,134.244 233.372,85.331  \"/> </g> <polygon fill=\"#D80027\" points=\"0,85.329 0,100.412 57.377,138.663 80,138.663 \"/> <g> <polygon fill=\"#2E52B2\" points=\"0,107.951 0,138.663 46.069,138.663  \"/> <polygon fill=\"#2E52B2\" points=\"96,85.331 96,134.244 22.628,85.331  \"/> </g> <polygon fill=\"#D80027\" points=\"256,85.329 256,100.412 198.623,138.663 176,138.663 \"/> <g> <polygon fill=\"#2E52B2\" points=\"256,107.951 256,138.663 209.931,138.663  \"/> <polygon fill=\"#2E52B2\" points=\"160,85.331 160,134.244 233.372,85.331  \"/> </g> <polygon fill=\"#D80027\" points=\"0,255.997 0,240.915 57.377,202.663 80,202.663 \"/> <g> <polygon fill=\"#2E52B2\" points=\"0,233.376 0,202.663 46.069,202.663  \"/> <polygon fill=\"#2E52B2\" points=\"96,255.994 96,207.082 22.628,255.994  \"/> </g> <polygon fill=\"#D80027\" points=\"256,255.997 256,240.915 198.623,202.663 176,202.663 \"/> <g> <polygon fill=\"#2E52B2\" points=\"256,233.376 256,202.663 209.931,202.663  \"/> <polygon fill=\"#2E52B2\" points=\"160,255.994 160,207.082 233.372,255.994  \"/> <circle fill=\"#2E52B2\" cx=\"128\" cy=\"170.66\" r=\"22.627\"/> </g> <g> <polygon fill=\"#FFDA44\" points=\"128,154.663 131.97,166.885 144.824,166.885 134.426,174.441 138.397,186.663 128,179.109    117.602,186.663 121.574,174.441 111.176,166.885 124.029,166.885  \"/> <polygon fill=\"#FFDA44\" points=\"128,219.803 130.364,227.081 138.017,227.081 131.827,231.579 134.191,238.857 128,234.359    121.809,238.857 124.173,231.579 117.983,227.081 125.635,227.081  \"/> <polygon fill=\"#FFDA44\" points=\"128,102.469 130.364,109.747 138.017,109.747 131.827,114.245 134.191,121.523 128,117.025    121.809,121.523 124.173,114.245 117.983,109.747 125.635,109.747  \"/> <polygon fill=\"#FFDA44\" points=\"197.931,161.136 200.296,168.413 207.949,168.413 201.757,172.912 204.122,180.191    197.931,175.693 191.74,180.191 194.105,172.912 187.914,168.413 195.566,168.413  \"/> <polygon fill=\"#FFDA44\" points=\"58.069,161.136 60.433,168.413 68.086,168.413 61.895,172.912 64.259,180.191    58.069,175.693 51.878,180.191 54.243,172.912 48.051,168.413 55.704,168.413  \"/> </g> </svg> ";
// Exports
/* harmony default export */ var nu = (nu_code);
;// ./templates/flags/kp.svg
// Module
var kp_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"71.68 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#91DC5A\" width=\"512\" height=\"341.337\"/> <rect y=\"85.331\" fill=\"#FFFFFF\" width=\"512\" height=\"341.337\"/> <g> <rect y=\"85.331\" fill=\"#0052B4\" width=\"512\" height=\"57.188\"/> <rect y=\"369.48\" fill=\"#0052B4\" width=\"512\" height=\"57.177\"/> </g> <rect y=\"165.215\" fill=\"#D80027\" width=\"512\" height=\"181.582\"/> <circle fill=\"#FFFFFF\" cx=\"190.33\" cy=\"255.994\" r=\"65.668\"/> <polygon fill=\"#D80027\" points=\"190.332,190.332 205.066,235.678 252.823,235.678 214.173,263.706 229.022,309.187   190.332,281.027 151.701,309.134 166.491,263.706 127.875,235.678 175.597,235.678 \"/> </svg> ";
// Exports
/* harmony default export */ var kp = (kp_code);
;// ./templates/flags/mp.svg
// Module
var mp_code = "<svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"125 0 500 500\"> <path fill=\"#0071BC\" d=\"M0 0h750v500H0V0z\"/> <g fill=\"#FFF\" stroke=\"#000\" stroke-width=\"1.9\"> <circle cx=\"375\" cy=\"245\" r=\"225\" stroke=\"#000000\" fill=\"#ffffff\"/> <circle cx=\"375\" cy=\"245\" r=\"165\" stroke=\"#000000\" fill=\"#0071BC\"/> </g> <g stroke=\"#000\"> <path fill=\"#8C8A8C\" stroke-width=\"1.9\" d=\"M444.7,450c0.7,11-7.8,20-18.8,20H323.6c-11,0-19.5-9-19-20L321.3,125c0.6-11,10-20,21-20h61.4\n\t\t\tc11,0,20.5,9,21.2,20L444.7,450z\"/> <path fill=\"#FFF\" stroke-width=\"2\" stroke-linejoin=\"round\" d=\"M373 114l30 93h97l-78 56 29 92-78-56-78 57 30-93-79-57h97l30-92z\"/> </g> </svg>";
// Exports
/* harmony default export */ var mp = (mp_code);
;// ./templates/flags/no.svg
// Module
var no_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"37.547 85.333 341.333 341.333\"> <rect y=\"85.334\" fill=\"#D80027\" width=\"512\" height=\"341.337\"/> <polygon fill=\"#FFFFFF\" points=\"512,295.883 202.195,295.883 202.195,426.666 183.652,426.666 140.978,426.666   122.435,426.666 122.435,295.883 0,295.883 0,277.329 0,234.666 0,216.111 122.435,216.111 122.435,85.329 140.978,85.329   183.652,85.329 202.195,85.329 202.195,216.111 512,216.111 512,234.666 512,277.329 \"/> <polygon fill=\"#2E52B2\" points=\"512,234.666 512,277.329 183.652,277.329 183.652,426.666 140.978,426.666 140.978,277.329   0,277.329 0,234.666 140.978,234.666 140.978,85.329 183.652,85.329 183.652,234.666 \"/> </svg> ";
// Exports
/* harmony default export */ var no = (no_code);
;// ./templates/flags/om.svg
// Module
var om_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"44.46 0 342 342\"> <rect fill=\"#D80027\" y=\"0\" width=\"513\" height=\"342\"/> <rect x=\"138\" y=\"0\" fill=\"#FFFFFF\" width=\"377\" height=\"114\"/> <rect x=\"138\" y=\"230\" fill=\"#4A7C3A\" width=\"377\" height=\"114\"/> <g fill=\"none\" stroke=\"#FFFFFF\" stroke-width=\"10\" stroke-miterlimit=\"2\"> <path d=\"M40.3,35.2c0,0,37.1,48,50.8,54.5c13.7,6.5,17.1,5.7,17.1,5.7\"/> <path d=\"M100,35.2c0,0-37.1,45.4-46.7,52.4c-9.6,7-18.7,7.8-18.7,7.8\"/> <line x1=\"51.7\" y1=\"65.3\" x2=\"89.6\" y2=\"65.3\"/> <line x1=\"70.7\" y1=\"35.2\" x2=\"70.7\" y2=\"65.3\"/> </g> </svg> ";
// Exports
/* harmony default export */ var om = (om_code);
;// ./templates/flags/pk.svg
// Module
var pk_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"136.534 85.333 341.333 341.333\"> <g> <polygon fill=\"#FFFFFF\" points=\"393.508,170.516 411.02,189.391 434.383,178.57 421.842,201.057 439.355,219.933    414.092,214.955 401.553,237.443 398.48,211.879 373.217,206.901 396.58,196.081  \"/> <rect y=\"85.343\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> </g> <rect x=\"128\" y=\"85.331\" fill=\"#01411c\" width=\"384\" height=\"341.337\"/> <g> <path fill=\"#FFFFFF\" d=\"M361.909,298.793c-31.037,22.426-74.378,15.446-96.804-15.592   c-22.427-31.038-15.446-74.379,15.593-96.804c9.677-6.992,20.55-11.125,31.613-12.563c-21.283-3.183-43.777,1.613-62.598,15.211   c-38.2,27.602-46.792,80.944-19.191,119.145c27.601,38.199,80.944,46.792,119.145,19.189c18.82-13.598,30.436-33.448,34.096-54.655   C378.924,282.774,371.587,291.8,361.909,298.793z\"/> <polygon fill=\"#FFFFFF\" points=\"360.58,172.889 378.064,191.731 401.386,180.929 388.867,203.376 406.35,222.22    381.131,217.252 368.612,239.702 365.545,214.181 340.325,209.212 363.648,198.41  \"/> </g> </svg> ";
// Exports
/* harmony default export */ var pk = (pk_code);
;// ./templates/flags/pw.svg
// Module
var pw_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"49.493 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#338AF3\" width=\"512\" height=\"341.337\"/> <circle fill=\"#FFDA44\" cx=\"218.902\" cy=\"255.994\" r=\"74.207\"/> </svg> ";
// Exports
/* harmony default export */ var pw = (pw_code);
;// ./templates/flags/ps.svg
// Module
var ps_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"5.12 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <rect y=\"85.337\" width=\"512\" height=\"113.775\"/> <rect y=\"312.888\" fill=\"#268024\" width=\"512\" height=\"113.775\"/> <polygon fill=\"#e4312b\" points=\"256,256.006 0,426.668 0,85.331 \"/> </svg> ";
// Exports
/* harmony default export */ var ps = (ps_code);
;// ./templates/flags/pa.svg
// Module
var pa_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <path fill=\"#0052B4\" d=\"M256,256c0,0,0,85.304,0,170.663H0V256h85.337C166.934,256,256,256,256,256z\"/> <path fill=\"#D80027\" d=\"M512,85.337V256h-85.337C336.184,256,256,256,256,256s0-90.323,0-170.663H512z\"/> <polygon fill=\"#0052B4\" points=\"128,123.034 139.824,159.423 178.087,159.423 147.132,181.914 158.955,218.303 128,195.813   97.045,218.303 108.868,181.914 77.913,159.423 116.176,159.423 \"/> <polygon fill=\"#D80027\" points=\"384,293.697 395.824,330.086 434.087,330.086 403.132,352.577 414.955,388.966 384,366.476   353.045,388.966 364.868,352.577 333.913,330.086 372.176,330.086 \"/> </svg> ";
// Exports
/* harmony default export */ var pa = (pa_code);
;// ./templates/flags/pg.svg
// Module
var pg_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.333\" width=\"512\" height=\"341.337\"/> <polyline fill=\"#A2001D\" points=\"0,85.33 512,85.33 512,426.662 \"/> <g> <polygon fill=\"#FFFFFF\" points=\"204.631,326.488 207.393,334.992 216.334,334.992 209.101,340.247 211.864,348.749    204.631,343.495 197.398,348.749 200.16,340.247 192.927,334.992 201.867,334.992  \"/> <polygon fill=\"#FFFFFF\" points=\"181.797,244.866 186.402,259.038 201.303,259.038 189.247,267.795 193.852,281.967    181.797,273.208 169.742,281.967 174.348,267.795 162.292,259.038 177.193,259.038  \"/> <polygon fill=\"#FFFFFF\" points=\"181.797,348.749 186.401,362.922 201.303,362.922 189.246,371.679 193.852,385.852    181.797,377.093 169.742,385.852 174.346,371.679 162.292,362.922 177.191,362.922  \"/> <polygon fill=\"#FFFFFF\" points=\"225.363,281.967 229.969,296.139 244.87,296.139 232.814,304.897 237.419,319.069    225.363,310.31 213.309,319.069 217.915,304.897 205.859,296.139 220.76,296.139  \"/> <polygon fill=\"#FFFFFF\" points=\"138.23,281.967 142.836,296.139 157.735,296.139 145.681,304.897 150.284,319.069    138.23,310.31 126.175,319.069 130.78,304.897 118.725,296.139 133.625,296.139  \"/> </g> <path fill=\"#FFDA44\" d=\"M376.526,204.163c-7.628-7.628-17.538-12.133-28.189-12.908l31.88-24.795  c-12.698-12.698-29.714-18.431-46.319-17.218c-1.988-7.145-5.778-13.892-11.396-19.511l-12.593,25.186  c-0.826-1.506-1.872-2.923-3.148-4.197c-7.245-7.245-18.991-7.244-26.234,0s-7.245,18.99,0,26.234  c1.276,1.276,2.692,2.322,4.197,3.148l-25.186,12.593c5.62,5.62,12.371,9.412,19.519,11.399  c-1.217,16.606,4.511,33.619,17.209,46.317l27.854-35.811c2.096-2.064,4.862-3.202,7.807-3.202c2.973,0,5.768,1.158,7.87,3.26  c2.103,2.103,3.26,4.897,3.26,7.87c0,2.974-1.158,5.768-3.26,7.87l10.494,10.494c4.905-4.905,7.607-11.428,7.607-18.364  c0-5.675-1.81-11.071-5.153-15.534c4.871,1.3,9.474,3.849,13.288,7.662c11.573,11.572,11.573,30.403,0,41.975l10.494,10.494  c8.409-8.409,13.039-19.59,13.039-31.481C389.565,223.752,384.934,212.572,376.526,204.163z\"/> </svg> ";
// Exports
/* harmony default export */ var pg = (pg_code);
;// ./templates/flags/py.svg
// Module
var py_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <rect y=\"85.337\" fill=\"#D80027\" width=\"512\" height=\"113.775\"/> <rect y=\"312.888\" fill=\"#0052B4\" width=\"512\" height=\"113.775\"/> <path fill=\"#6DA544\" d=\"M289.579,216.738l-12.592,12.592c5.37,5.372,8.693,12.792,8.693,20.988  c0,16.392-13.289,29.68-29.68,29.68c-16.392,0-29.68-13.289-29.68-29.68c0-8.195,3.322-15.616,8.693-20.988l-12.592-12.592  c-8.594,8.594-13.91,20.466-13.91,33.579c0,26.228,21.261,47.489,47.489,47.489s47.489-21.261,47.489-47.489  C303.489,237.205,298.173,225.332,289.579,216.738z\"/> <polygon fill=\"#FFDA44\" points=\"256,232.51 260.421,246.115 274.725,246.115 263.152,254.523 267.573,268.127 256,259.719   244.427,268.127 248.848,254.523 237.275,246.115 251.579,246.115 \"/> </svg> ";
// Exports
/* harmony default export */ var py = (py_code);
;// ./templates/flags/pe.svg
// Module
var pe_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <g> <rect y=\"85.331\" fill=\"#D80027\" width=\"170.663\" height=\"341.337\"/> <rect x=\"341.337\" y=\"85.331\" fill=\"#D80027\" width=\"170.663\" height=\"341.337\"/> </g> </svg> ";
// Exports
/* harmony default export */ var pe = (pe_code);
;// ./templates/flags/ph.svg
// Module
var ph_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"0 85.333 341.333 341.333\"> <g> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> </g> <polygon fill=\"#0052B4\" points=\"512,85.337 512,256 256,256 0,85.337 \"/> <polygon fill=\"#D80027\" points=\"512,256 512,426.663 0,426.663 256,256 \"/> <g> <polygon fill=\"#FFDA44\" points=\"161.908,256 134.62,243.165 149.152,216.737 119.52,222.405 115.765,192.472    95.125,214.487 74.486,192.472 70.731,222.405 41.1,216.736 55.631,243.164 28.343,256 55.631,268.835 41.1,295.263    70.733,289.595 74.486,319.528 95.125,297.513 115.765,319.528 119.52,289.595 149.151,295.264 134.619,268.837  \"/> <polygon fill=\"#FFDA44\" points=\"21.789,117.607 30.87,130.303 45.749,125.589 36.481,138.149 45.562,150.843    30.753,145.911 21.485,158.47 21.602,142.862 6.793,137.928 21.673,133.216  \"/> <polygon fill=\"#FFDA44\" points=\"21.789,353.53 30.87,366.226 45.749,361.512 36.481,374.072 45.562,386.767 30.753,381.834    21.485,394.392 21.602,378.785 6.793,373.851 21.673,369.139  \"/> <polygon fill=\"#FFDA44\" points=\"210.395,235.569 201.314,248.264 186.435,243.551 195.703,256.11 186.622,268.806    201.431,263.873 210.699,276.431 210.582,260.824 225.391,255.89 210.511,251.177  \"/> </g> </svg> ";
// Exports
/* harmony default export */ var ph = (ph_code);
;// ./templates/flags/pn.svg
// Module
var pn_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"114.347 85.333 341.333 341.333\"> <rect y=\"85.333\" fill=\"#0052B4\" width=\"512\" height=\"341.337\"/> <polygon fill=\"#ACABB1\" points=\"400.696,219.822 384,225.387 367.304,219.822 361.739,205.909 367.304,191.996   400.696,191.996 395.13,205.909 \"/> <path fill=\"#338AF3\" d=\"M345.043,219.822v61.217c0,29.821,38.957,38.957,38.957,38.957s38.957-9.137,38.957-38.957v-61.217  H345.043z\"/> <path fill=\"#6DA544\" d=\"M348.555,295.541C358.131,313.927,384,319.996,384,319.996s25.869-6.069,35.445-24.455L384,236.518  L348.555,295.541z\"/> <path fill=\"#FFDA44\" d=\"M422.957,280.421L384,219.822l-38.957,60.599v0.618c0,5.518,1.337,10.328,3.512,14.503L384,240.405  l35.445,55.137c2.175-4.175,3.512-8.983,3.512-14.503V280.421z\"/> <polygon fill=\"#FFFFFF\" points=\"256,85.333 256,115.886 210.833,140.985 256,140.985 256,200.344 196.897,200.344   256,233.179 256,255.996 229.32,255.996 155.826,215.17 155.826,255.996 100.174,255.996 100.174,207.423 12.744,255.996 0,255.996   0,225.442 45.167,200.344 0,200.344 0,140.985 59.103,140.985 0,108.139 0,85.333 26.68,85.333 100.174,126.158 100.174,85.333   155.826,85.333 155.826,133.905 243.256,85.333 \"/> <polygon fill=\"#D80027\" points=\"144,85.33 112,85.33 112,154.662 0,154.662 0,186.662 112,186.662 112,255.996 144,255.996   144,186.662 256,186.662 256,154.662 144,154.662 \"/> <polygon fill=\"#0052B4\" points=\"155.826,200.344 256,255.996 256,240.259 184.153,200.344 \"/> <polygon fill=\"#FFFFFF\" points=\"155.826,200.344 256,255.996 256,240.259 184.153,200.344 \"/> <g> <polygon fill=\"#D80027\" points=\"155.826,200.344 256,255.996 256,240.259 184.153,200.344  \"/> <polygon fill=\"#D80027\" points=\"71.846,200.344 0,240.259 0,255.996 0,255.996 100.174,200.344  \"/> </g> <polygon fill=\"#0052B4\" points=\"100.174,140.982 0,85.33 0,101.067 71.847,140.982 \"/> <polygon fill=\"#FFFFFF\" points=\"100.174,140.982 0,85.33 0,101.067 71.847,140.982 \"/> <g> <polygon fill=\"#D80027\" points=\"100.174,140.982 0,85.33 0,101.067 71.847,140.982  \"/> <polygon fill=\"#D80027\" points=\"184.154,140.982 256,101.067 256,85.33 256,85.33 155.826,140.982  \"/> </g> </svg> ";
// Exports
/* harmony default export */ var pn = (pn_code);
;// ./templates/flags/pl.svg
// Module
var pl_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <g> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"170.663\"/> </g> <rect y=\"256\" fill=\"#D80027\" width=\"512\" height=\"170.663\"/> </svg> ";
// Exports
/* harmony default export */ var pl = (pl_code);
;// ./templates/flags/pt.svg
// Module
var pt_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"30.72 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#D80027\" width=\"512\" height=\"341.326\"/> <polygon fill=\"#6DA544\" points=\"196.641,85.337 196.641,261.565 196.641,426.663 0,426.663 0,85.337 \"/> <circle fill=\"#FFDA44\" cx=\"196.641\" cy=\"256\" r=\"64\"/> <path fill=\"#D80027\" d=\"M160.638,224v40.001c0,19.882,16.118,36,36,36s36-16.118,36-36V224H160.638z\"/> <path fill=\"#FFFFFF\" d=\"M196.638,276c-6.617,0-12-5.383-12-12v-16h24.001v16C208.638,270.616,203.254,276,196.638,276z\"/> </svg> ";
// Exports
/* harmony default export */ var pt = (pt_code);
;// ./templates/flags/pr.svg
// Module
var pr_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"5.12 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <g> <rect y=\"85.337\" fill=\"#D80027\" width=\"512\" height=\"68.263\"/> <rect y=\"221.863\" fill=\"#D80027\" width=\"512\" height=\"68.263\"/> <rect y=\"358.4\" fill=\"#D80027\" width=\"512\" height=\"68.263\"/> </g> <polygon fill=\"#0052B4\" points=\"256,256.006 0,426.668 0,85.331 \"/> <polygon fill=\"#FFFFFF\" points=\"83.477,195.132 98.584,241.63 147.478,241.63 107.924,270.369 123.031,316.868   83.477,288.13 43.922,316.868 59.032,270.369 19.478,241.63 68.37,241.63 \"/> </svg> ";
// Exports
/* harmony default export */ var pr = (pr_code);
;// ./templates/flags/qa.svg
// Module
var qa_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect fill=\"#751A46\" width=\"512\" height=\"342\"/> <polygon fill=\"#FFFFFF\" points=\"0,0 0,342 150.3,342 188,322.4 150.3,303.5 188,284.5 150.3,265.6 188,246.6 150.3,227.6\n\t188,208.7 150.3,189.7 188,170.7 150.3,151.8 188,132.8 188,132.8 188,132.8 150.3,113.9 188,94.9 150.3,75.9 188,57 150.3,38\n\t188,19.1 150.3,0 \"/> </svg> ";
// Exports
/* harmony default export */ var qa = (qa_code);
;// ./templates/flags/cg.svg
// Module
var cg_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 85.333 342 342\"> <rect y=\"85.331\" fill=\"#da1a35\" width=\"513\" height=\"342\"/> <polygon fill=\"#009543\" points=\"443.726,85.331 102.4,426.657 0,426.657 0,85.331 \"/> <polygon fill=\"#fbde4a\" points=\"500.124,85.331 158.798,426.657 11.876,426.657 353.202,85.331 \"/> </svg> ";
// Exports
/* harmony default export */ var cg = (cg_code);
;// ./templates/flags/re.svg
// Module
var re_code = "<svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"106.667 26.666 426.666 426.666\"> <g fill-rule=\"evenodd\" stroke-width=\"1pt\"> <path fill=\"#fff\" d=\"M0 0h640v480H0z\"/> <path fill=\"#00267f\" d=\"M0 0h213.3v480H0z\"/> <path fill=\"#f31830\" d=\"M426.7 0H640v480H426.7z\"/> </g> </svg> ";
// Exports
/* harmony default export */ var re = (re_code);
;// ./templates/flags/ro.svg
// Module
var ro_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#FFDA44\" width=\"512\" height=\"341.326\"/> <rect y=\"85.331\" fill=\"#0052B4\" width=\"170.663\" height=\"341.337\"/> <rect x=\"341.337\" y=\"85.331\" fill=\"#D80027\" width=\"170.663\" height=\"341.337\"/> </svg> ";
// Exports
/* harmony default export */ var ro = (ro_code);
;// ./templates/flags/ru.svg
// Module
var ru_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <polygon fill=\"#FFFFFF\" points=\"0,85.33 0,199.107 0,312.885 0,426.662 512,426.662 512,312.885 512,199.107 512,85.33 \"/> <rect y=\"85.333\" fill=\"#0052B4\" width=\"512\" height=\"341.337\"/> <rect y=\"85.333\" fill=\"#FFFFFF\" width=\"512\" height=\"113.775\"/> <rect y=\"312.884\" fill=\"#D80027\" width=\"512\" height=\"113.775\"/> </svg> ";
// Exports
/* harmony default export */ var ru = (ru_code);
;// ./templates/flags/rw.svg
// Module
var rw_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#338AF3\" width=\"512\" height=\"341.337\"/> <rect y=\"255.994\" fill=\"#FFDA44\" width=\"512\" height=\"81.619\"/> <rect y=\"337.614\" fill=\"#496E2D\" width=\"512\" height=\"89.043\"/> <polygon fill=\"#FFDA44\" points=\"278.261,185.209 299.105,195.013 288.006,215.199 310.638,210.869 313.506,233.734   329.271,216.918 345.037,233.734 347.904,210.869 370.537,215.199 359.438,195.013 380.281,185.209 359.437,175.404   370.537,155.219 347.904,159.548 345.036,136.684 329.271,153.5 313.505,136.684 310.638,159.548 288.004,155.219 299.104,175.406   \"/> </svg> ";
// Exports
/* harmony default export */ var rw = (rw_code);
;// ./templates/flags/bl.svg
// Module
var bl_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.335 0 341.33 341.33\"> <rect fill=\"#FFFFFF\" width=\"512\" height=\"341.33\"/> <path fill=\"#FFFFFF\" d=\"M0-75.85\"/> <path fill=\"#E6BC00\" d=\"M157.31,199.76c0,0-9.04,43.93-19.31,40.39c-10.62-3.54-17.53-18.76-17.53-18.76l-14.52,21.24\n\t\tc0,0,38.18,8.48,41.01,5.47c1.22-1.14,8.21-15.1,10.34-26.05V199.76z M101.18,260.33c0,0-2.48,20.18-1.06,22.66\n\t\tc1.24,2.3,39.48,11.33,39.48,11.33s-24.96,16.46-29.74,20.54L91.8,284.94l-0.71-24.61L101.18,260.33L101.18,260.33z\"/> <path fill=\"#E6BC00\" d=\"M0-75.85\"/> <path fill=\"#E6BC00\" d=\"M112.51,94.64c0,0,39.65,41.6,40.36,45.5c0.71,3.72-4.25,3.19-4.25,3.19s-44.79-37.53-49.92-41.07\n\tC93.39,98.71,112.51,94.64,112.51,94.64L112.51,94.64z\"/> <path fill=\"#ADADAD\" d=\"M99.41,104.73c0,0,16.99-0.71,16.64-11.51C115.7,82.6,104.9,78.35,96.93,80.83\n\tc-7.97,2.3-23.01,13.45-13.63,35.05c9.38,21.42,23.37,29.39,26.2,47.8c0,0-5.46,9.86-19.12,0.35c-16.9-11.77-6.2-38.24-10.44-44.96\n\tc-4.25-7.08-40.95-38.92-40.95-38.92s-30.24,39.37-18,74.92c16.95,49.21,51.74,31.98,64.61,36.76c0,0-42.13,61.96-49.57,67.09\n\tc0,0,24.08,8.32,47.8-18.76c0,0,5.84,9.74,7.08,20.18h10.27c4.78-26.2,51.51-64.26,49.92-82.32\n\tC149.33,156.77,101.89,128.63,99.41,104.73z\"/> <path fill=\"#E6BC00\" d=\"M359.17,201.27c0,0,8.96,42.43,19.23,38.89c10.62-3.54,17.53-18.76,17.53-18.76l14.52,21.24\n\tc0,0-37,8.48-39.83,5.47c-1.21-1.14-9.32-13.79-11.52-24.66L359.17,201.27z M415.22,260.33c0,0,2.48,20.18,1.06,22.66\n\tc-1.24,2.3-39.48,11.33-39.48,11.33s24.96,16.46,29.74,20.54l18.06-29.92l0.71-24.61H415.22z\"/> <path fill=\"#E6BC00\" d=\"M403.89,94.64c0,0-39.65,41.6-40.36,45.5c-0.71,3.72,4.25,3.19,4.25,3.19s44.79-37.53,49.92-41.07\n\tC422.84,98.71,403.89,94.64,403.89,94.64L403.89,94.64z\"/> <g transform=\"translate(6.5 5)\"> <g> <path id=\"b_1_\" fill=\"#125ECC\" d=\"M150.81,82.06h201.79v145.46c0,16.06-9.69,21.39-21.91,31.85l-45.92,39.41\n\t\t\tc-18.25,15.62-47.91,15.57-66.07,0l-46.01-39.43c-12.08-10.35-21.88-15.71-21.88-31.82V82.06z\"/> </g> <path fill=\"#FFE600\" d=\"M194.05,142.71c-7.96,0-14.41-6.45-14.41-14.41s6.45-14.41,14.41-14.41s14.41,6.45,14.41,14.41\n\t\tS202.01,142.71,194.05,142.71z M251.7,139.71c-7.96,0-14.41-6.45-14.41-14.41c0-7.96,6.45-14.41,14.41-14.41\n\t\ts14.41,6.45,14.41,14.41C266.11,133.26,259.66,139.71,251.7,139.71z M309.35,142.71c-7.96,0-14.41-6.45-14.41-14.41\n\t\ts6.45-14.41,14.41-14.41s14.41,6.45,14.41,14.41S317.31,142.71,309.35,142.71z M208.46,269.43c-7.96,0-14.41-6.45-14.41-14.41\n\t\tc0-7.96,6.45-14.41,14.41-14.41s14.41,6.45,14.41,14.41C222.88,262.98,216.42,269.43,208.46,269.43z M251.7,283.85\n\t\tc-7.96,0-14.41-6.45-14.41-14.41c0-7.96,6.45-14.41,14.41-14.41s14.41,6.45,14.41,14.41C266.11,277.39,259.66,283.85,251.7,283.85z\n\t\t M294.94,269.43c-7.96,0-14.41-6.45-14.41-14.41c0-7.96,6.45-14.41,14.41-14.41c7.96,0,14.41,6.45,14.41,14.41\n\t\tC309.35,262.98,302.9,269.43,294.94,269.43z\"/> <path fill=\"#D60537\" d=\"M150.81,170.36h201.79v57.65H150.81V170.36z\"/> </g> <path fill=\"#EACE00\" d=\"M142.9,43.82c0,0,57.65-14.41,115.31-14.41s115.31,14.41,115.31,14.41l-14.41,57.65\n\tc0,0-50.45-14.41-100.89-14.41s-100.89,14.41-100.89,14.41L142.9,43.82z\"/> <path fill=\"#7A6920\" d=\"M258.2,72.65c-7.96,0-14.41-6.45-14.41-14.41s6.45-14.41,14.41-14.41c7.96,0,14.41,6.45,14.41,14.41\n\tS266.16,72.65,258.2,72.65z M315.85,77.65c-7.96,0-14.41-6.45-14.41-14.41s6.45-14.41,14.41-14.41s14.41,6.45,14.41,14.41\n\tS323.81,77.65,315.85,77.65z M200.55,77.65c-7.96,0-14.41-6.45-14.41-14.41s6.45-14.41,14.41-14.41s14.41,6.45,14.41,14.41\n\tS208.51,77.65,200.55,77.65z\"/> <path fill=\"#ADADAD\" d=\"M365.08,178.02c-1.59,18.06,45.14,56.12,49.92,82.32h10.27c1.24-10.44,7.08-20.18,7.08-20.18\n\tc23.72,27.09,47.8,18.76,47.8,18.76c-7.44-5.13-49.57-67.09-49.57-67.09c12.88-4.78,47.67,12.45,64.61-36.76\n\tc12.24-35.55-18-74.92-18-74.92s-36.7,31.84-40.95,38.92c-4.25,6.73,6.46,33.2-10.44,44.96c-13.66,9.51-19.12-0.35-19.12-0.35\n\tc2.83-18.41,16.82-26.38,26.2-47.8c9.38-21.6-5.66-32.75-13.63-35.05c-7.97-2.48-18.76,1.77-19.12,12.39\n\tc-0.35,10.8,16.64,11.51,16.64,11.51C414.29,128.63,366.85,156.77,365.08,178.02z\"/> <circle fill=\"#FFFFFF\" cx=\"258.2\" cy=\"204.19\" r=\"16.93\"/> </svg> ";
// Exports
/* harmony default export */ var bl = (bl_code);
;// ./templates/flags/sh.svg
// Module
var sh_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" xmlns:xlink=\"http://www.w3.org/1999/xlink\" viewBox=\"112.662 0 341.3 341.3\"> <rect y=\"0\" fill=\"#0052B4\" width=\"512\" height=\"341.3\"/> <polygon fill=\"#FFFFFF\" points=\"256,0 256,117.4 209.9,117.4 256,148.1 256,170.7 233.4,170.7 160,121.8 160,170.7 96,170.7 96,121.8\n\t22.6,170.7 0,170.7 0,148.1 46.1,117.4 0,117.4 0,53.4 46.1,53.4 0,22.7 0,0 22.6,0 96,48.9 96,0 160,0 160,48.9 233.4,0 \"/> <g> <polygon fill=\"#D80027\" points=\"144,0 112,0 112,69.4 0,69.4 0,101.4 112,101.4 112,170.7 144,170.7 144,101.4 256,101.4 256,69.4\n\t\t144,69.4 \t\"/> <polygon fill=\"#D80027\" points=\"0,0 0,15.1 57.4,53.4 80,53.4 \t\"/> <polygon fill=\"#D80027\" points=\"256,0 256,15.1 198.6,53.4 176,53.4 \t\"/> </g> <polygon fill=\"#2E52B2\" points=\"256,22.7 256,53.4 209.9,53.4 \"/> <g> <polygon fill=\"#D80027\" points=\"0,0 0,15.1 57.4,53.4 80,53.4 \t\"/> <polygon fill=\"#D80027\" points=\"256,0 256,15.1 198.6,53.4 176,53.4 \t\"/> </g> <polygon fill=\"#2E52B2\" points=\"256,22.7 256,53.4 209.9,53.4 \"/> <g> <polygon fill=\"#D80027\" points=\"0,170.7 0,155.6 57.4,117.4 80,117.4 \t\"/> <polygon fill=\"#D80027\" points=\"256,170.7 256,155.6 198.6,117.4 176,117.4 \t\"/> </g> <g> <path fill=\"#29DBFF\" d=\"M449,139.7c-0.1,44.4-7.2,92.1-65,114.9c-57.8-22.8-64.9-70.5-65-114.9L449,139.7L449,139.7z\"/> <path fill=\"#ffda44\" d=\"M449,139.7c0-16.7-0.9-32.9-0.5-47.1C426.9,83,398.4,81,383.9,81s-42.9,2-64.6,11.6\n\t\tc0.4,14.2-0.5,30.4-0.5,47.1H449z\"/> <path fill=\"#BF521B\" d=\"M369.5,204.5l0.3,10.3L357,215l4.9,13.2l-17.3,0c-18-20.2-23.4-42.4-24.9-68.1l9.1-9.7l8.3,14.3l10.8-12.8\n\t\tl7,7.8l0.8,15.7L369.5,204.5z\"/> </g> <polygon fill=\"#474747\" points=\"436.6,192.5 428,210.8 381,210.8 351.8,191.3 377.5,201.8 402.6,201.8 406.3,195.3 413.5,195.5\n\t415.5,192.5 \"/> </svg> ";
// Exports
/* harmony default export */ var sh = (sh_code);
;// ./templates/flags/kn.svg
// Module
var kn_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#D80027\" width=\"513\" height=\"342\"/> <polyline fill=\"#6DA544\" points=\"512,0 0,0 0,342 \"/> <polygon fill=\"#FFDA44\" points=\"307.7,0 0,194.5 0,342 206.9,342 513,148.5 513,0 \"/> <polygon fill=\"#000000\" points=\"513,0 385.2,0 0,249.4 0,341 126.2,342 513,91.6 \"/> <g fill=\"#FFFFFF\"> <polygon points=\"141.1,187 172.3,211 204.6,188.8 191.5,225.8 222.6,249.7 183.4,248.6 170.2,285.6 159.1,248\n\t\t119.9,246.9 152.2,224.7 \t\"/> <polygon points=\"310.6,70.8 341.8,94.7 374.1,72.5 361,109.5 392.1,133.4 352.9,132.3 339.7,169.3 328.6,131.7\n\t\t289.4,130.6 321.8,108.4 \t\"/> </g> </svg> ";
// Exports
/* harmony default export */ var kn = (kn_code);
;// ./templates/flags/lc.svg
// Module
var lc_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 0 342 342\"> <rect y=\"0\" fill=\"#55B2FF\" width=\"513\" height=\"342\"/> <polygon fill=\"#F3F3F3\" points=\"148.5,298.1 364.5,298.1 256.5,43.9 \"/> <polygon fill=\"#333333\" points=\"186,272.7 256.5,112.4 327,272.7 \"/> <polygon fill=\"#FFDA44\" points=\"148.5,298.1 364.5,298.1 256.5,196.4 \"/> </svg> ";
// Exports
/* harmony default export */ var lc = (lc_code);
;// ./templates/flags/mf.svg
// Module
var mf_code = "<svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"18 0 72 72\"> <path d=\"M0,0H108V72H0z\" fill=\"#003787\"/> <path d=\"M0,0H108L60,48V72H48V48z\" fill=\"#fff\"/> <circle cx=\"54\" cy=\"30\" r=\"8\" fill=\"#f9d90f\"/> <path d=\"M44,30H64L54,48z\" fill=\"#fff\"/> <path d=\"M54,48 38,33H70z\" fill=\"#cf142b\"/> </svg>";
// Exports
/* harmony default export */ var mf = (mf_code);
;// ./templates/flags/pm.svg
// Module
var pm_code = "<svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"106.667 26.666 426.666 426.666\"> <g fill-rule=\"evenodd\" stroke-width=\"1pt\"> <path fill=\"#fff\" d=\"M0 0h640v480H0z\"/> <path fill=\"#00267f\" d=\"M0 0h213.3v480H0z\"/> <path fill=\"#f31830\" d=\"M426.7 0H640v480H426.7z\"/> </g> </svg> ";
// Exports
/* harmony default export */ var pm = (pm_code);
;// ./templates/flags/vc.svg
// Module
var vc_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#FFDA44\" width=\"512\" height=\"341.337\"/> <rect y=\"85.331\" fill=\"#338AF3\" width=\"170.663\" height=\"341.337\"/> <g> <rect x=\"341.337\" y=\"85.331\" fill=\"#6DA544\" width=\"170.663\" height=\"341.337\"/> <polygon fill=\"#6DA544\" points=\"214.261,283.82 180.868,233.734 214.26,183.647 247.652,233.734  \"/> <polygon fill=\"#6DA544\" points=\"297.739,283.82 264.348,233.734 297.739,183.647 331.132,233.734  \"/> <polygon fill=\"#6DA544\" points=\"256,350.603 222.609,300.516 256,250.429 289.391,300.516  \"/> </g> </svg> ";
// Exports
/* harmony default export */ var vc = (vc_code);
;// ./templates/flags/ws.svg
// Module
var ws_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"15.36 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#D80027\" width=\"512\" height=\"341.337\"/> <rect y=\"85.331\" fill=\"#0052B4\" width=\"256\" height=\"170.663\"/> <g> <polygon fill=\"#FFFFFF\" points=\"165.483,181.79 168.247,190.294 177.186,190.294 169.954,195.548 172.717,204.051    165.483,198.796 158.25,204.051 161.013,195.548 153.78,190.294 162.72,190.294  \"/> <polygon fill=\"#FFFFFF\" points=\"120.579,115.007 125.185,129.18 140.085,129.18 128.03,137.937 132.635,152.108    120.579,143.35 108.525,152.108 113.13,137.937 101.074,129.18 115.975,129.18  \"/> <polygon fill=\"#FFFFFF\" points=\"165.101,129.848 169.707,144.02 184.607,144.02 172.552,152.777 177.156,166.95    165.101,158.19 153.047,166.95 157.651,152.777 145.596,144.02 160.496,144.02  \"/> <polygon fill=\"#FFFFFF\" points=\"129.909,189.211 134.515,203.383 149.415,203.383 137.36,212.141 141.964,226.312    129.909,217.553 117.855,226.312 122.459,212.141 110.404,203.383 125.304,203.383  \"/> <polygon fill=\"#FFFFFF\" points=\"90.899,152.108 95.504,166.281 110.404,166.281 98.349,175.038 102.953,189.211    90.899,180.451 78.844,189.211 83.448,175.038 71.393,166.281 86.293,166.281  \"/> </g> </svg> ";
// Exports
/* harmony default export */ var ws = (ws_code);
;// ./templates/flags/sm.svg
// Module
var sm_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.34\" fill=\"#338AF3\" width=\"512\" height=\"341.326\"/> <polygon fill=\"#FFFFFF\" points=\"512,85.334 512,252.024 0,256.175 0,85.334 \"/> <path fill=\"#6DA544\" d=\"M323.744,203.099L256,270.843l-67.744-67.744c-8.76,13.005-13.879,28.658-13.879,45.483v22.261  c0,35.744,23.097,66.193,55.148,77.213c-4.277,8.385-3.556,18.848,2.712,26.671c8.326-6.673,16.945-13.58,24.258-19.439  c7.313,5.859,15.932,12.767,24.258,19.439c6.342-7.915,7.011-18.534,2.564-26.968c31.614-11.261,54.308-41.485,54.308-76.916  v-22.261C337.623,231.756,332.504,216.105,323.744,203.099z\"/> <path fill=\"#FFDA44\" d=\"M256,330.206c-32.732,0-59.362-26.63-59.362-59.362v-22.261c0-32.733,26.63-59.363,59.362-59.363  s59.362,26.63,59.362,59.362v22.261C315.362,303.576,288.732,330.206,256,330.206z\"/> <path fill=\"#338AF3\" d=\"M293.101,270.843v-22.261c0-20.458-16.643-37.101-37.101-37.101s-37.101,16.643-37.101,37.101v22.261  L256,278.264L293.101,270.843z\"/> <path fill=\"#6DA544\" d=\"M218.899,270.843L218.899,270.843c0,20.458,16.643,37.101,37.101,37.101  c20.458,0,37.101-16.643,37.101-37.101l0,0H218.899L218.899,270.843z\"/> <path fill=\"#FFDA44\" d=\"M300.522,189.22c0-12.295-9.966-22.261-22.261-22.261c-5.703,0-10.901,2.146-14.84,5.672v-13.093  h7.421v-14.84h-7.421v-7.421h-14.84v7.421h-7.421v14.84h7.421v13.093c-3.94-3.526-9.138-5.672-14.841-5.672  c-12.295,0-22.261,9.966-22.261,22.261c0,6.591,2.867,12.512,7.421,16.589v13.093h74.203v-13.093  C297.655,201.732,300.522,195.812,300.522,189.22z\"/> </svg> ";
// Exports
/* harmony default export */ var sm = (sm_code);
;// ./templates/flags/st.svg
// Module
var st_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFDA44\" width=\"512\" height=\"341.326\"/> <g> <rect y=\"85.337\" fill=\"#6DA544\" width=\"512\" height=\"113.775\"/> <rect y=\"312.888\" fill=\"#6DA544\" width=\"512\" height=\"113.775\"/> </g> <polygon fill=\"#D80027\" points=\"256,256.006 0,426.668 0,85.331 \"/> <polygon points=\"302.049,226.318 309.417,248.992 333.259,248.992 313.971,263.008 321.337,285.682 302.049,271.667   282.762,285.682 290.128,263.008 270.84,248.992 294.682,248.992 \"/> <polygon points=\"376.252,226.318 383.619,248.992 407.461,248.992 388.173,263.008 395.54,285.682 376.252,271.667 356.964,285.682   364.331,263.008 345.043,248.992 368.885,248.992 \"/> </svg> ";
// Exports
/* harmony default export */ var st = (st_code);
;// ./templates/flags/sa.svg
// Module
var sa_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.333\" fill=\"#055e1c\" width=\"512\" height=\"341.333\"/> <g fill=\"#FFFFFF\"> <path d=\"M183.548,289.386c0,12.295,9.731,22.261,21.736,22.261h65.208c0,10.244,8.11,18.551,18.114,18.551   h21.736c10.004,0,18.114-8.306,18.114-18.551v-22.261H183.548z\"/> <path d=\"M330.264,181.791v51.942c0,8.183-6.5,14.84-14.491,14.84v22.261   c19.976,0,36.226-16.643,36.226-37.101v-51.942L330.264,181.791L330.264,181.791z\"/> <path d=\"M174.491,233.734c0,8.183-6.5,14.84-14.491,14.84v22.261c19.976,0,36.226-16.643,36.226-37.101   v-51.942h-21.736V233.734z\"/> <rect x=\"297.661\" y=\"181.788\" width=\"21.736\" height=\"51.942\"/> <path d=\"M265.057,211.473c0,2.046-1.625,3.71-3.623,3.71c-1.998,0-3.623-1.664-3.623-3.71v-29.682h-21.736   v29.682c0,2.046-1.625,3.71-3.623,3.71s-3.623-1.664-3.623-3.71v-29.682h-21.736v29.682c0,14.32,11.376,25.971,25.358,25.971   c5.385,0,10.38-1.733,14.491-4.677c4.11,2.944,9.106,4.677,14.491,4.677c1.084,0,2.15-0.078,3.2-0.215   c-1.54,6.499-7.255,11.345-14.068,11.345v22.261c19.976,0,36.226-16.643,36.226-37.101v-22.261v-29.682h-21.736L265.057,211.473   L265.057,211.473z\"/> <rect x=\"207.093\" y=\"248.57\" width=\"32.601\" height=\"22.261\"/> </g> </svg> ";
// Exports
/* harmony default export */ var sa = (sa_code);
;// ./templates/flags/sn.svg
// Module
var sn_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#FFDA44\" width=\"512\" height=\"341.326\"/> <rect x=\"330.207\" y=\"85.331\" fill=\"#D80027\" width=\"181.793\" height=\"341.337\"/> <g> <rect y=\"85.331\" fill=\"#496E2D\" width=\"181.793\" height=\"341.337\"/> <polygon fill=\"#496E2D\" points=\"255.999,196.632 270.732,241.979 318.417,241.979 279.841,270.008 294.575,315.356    255.999,287.33 217.422,315.356 232.159,270.008 193.583,241.979 241.264,241.979  \"/> </g> </svg> ";
// Exports
/* harmony default export */ var sn = (sn_code);
;// ./templates/flags/rs.svg
// Module
var rs_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"61.44 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#0052B4\" width=\"512\" height=\"341.337\"/> <rect y=\"85.331\" fill=\"#D80027\" width=\"512\" height=\"113.775\"/> <rect y=\"312.882\" fill=\"#FFFFFF\" width=\"512\" height=\"113.775\"/> <path fill=\"#D80027\" d=\"M129.468,181.793v85.136c0,48.429,63.267,63.267,63.267,63.267S256,315.356,256,266.929v-85.136  H129.468z\"/> <g> <polygon fill=\"#FFDA44\" points=\"155.634,196.634 229.835,196.634 229.835,166.953 214.994,174.373 192.733,152.113    170.474,174.373 155.634,166.953  \"/> <polygon fill=\"#FFDA44\" points=\"241.16,278.782 192.929,230.551 144.698,278.782 160.439,294.522 192.929,262.032    225.419,294.522  \"/> </g> <path fill=\"#FFFFFF\" d=\"M241.16,233.734h-22.504c1.266-2.184,2.001-4.713,2.001-7.418c0-8.196-6.645-14.84-14.84-14.84  c-5.663,0-10.581,3.172-13.083,7.836c-2.502-4.663-7.421-7.836-13.083-7.836c-8.195,0-14.84,6.644-14.84,14.84  c0,2.706,0.736,5.235,2.001,7.418h-22.114c0,8.196,7.139,14.84,15.334,14.84h-0.494c0,8.196,6.644,14.84,14.84,14.84  c0,7.257,5.211,13.286,12.094,14.576l-11.694,26.401c5.603,2.278,11.727,3.544,18.149,3.544c6.422,0,12.545-1.266,18.149-3.544  l-11.694-26.401c6.883-1.29,12.094-7.319,12.094-14.576c8.196,0,14.84-6.644,14.84-14.84h-0.494  C234.021,248.574,241.16,241.93,241.16,233.734z\"/> </svg> ";
// Exports
/* harmony default export */ var rs = (rs_code);
;// ./templates/flags/sc.svg
// Module
var sc_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"87.04 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <polygon fill=\"#FFDA44\" points=\"235.454,85.337 0,426.663 427.345,85.337 \"/> <polygon fill=\"#6DA544\" points=\"512,329.393 0,426.663 512,426.663 \"/> <polygon fill=\"#D80027\" points=\"512,85.337 427.345,85.337 0,426.663 512,222.151 \"/> <polygon fill=\"#0052B4\" points=\"0,85.337 0,426.663 235.454,85.337 \"/> </svg> ";
// Exports
/* harmony default export */ var sc = (sc_code);
;// ./templates/flags/sl.svg
// Module
var sl_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <rect y=\"85.337\" fill=\"#6DA544\" width=\"512\" height=\"113.775\"/> <rect y=\"312.888\" fill=\"#338AF3\" width=\"512\" height=\"113.775\"/> </svg> ";
// Exports
/* harmony default export */ var sl = (sl_code);
;// ./templates/flags/sg.svg
// Module
var sg_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"0 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <rect y=\"85.337\" fill=\"#D80027\" width=\"512\" height=\"170.663\"/> <g> <path fill=\"#FFFFFF\" d=\"M83.478,170.666c0-24.865,17.476-45.637,40.812-50.734c-3.587-0.784-7.308-1.208-11.13-1.208   c-28.688,0-51.942,23.254-51.942,51.941s23.255,51.942,51.942,51.942c3.822,0,7.543-0.425,11.13-1.208   C100.954,216.304,83.478,195.532,83.478,170.666z\"/> <polygon fill=\"#FFFFFF\" points=\"150.261,122.435 153.945,133.772 165.866,133.772 156.221,140.779 159.905,152.116    150.261,145.11 140.616,152.116 144.301,140.779 134.656,133.772 146.577,133.772  \"/> <polygon fill=\"#FFFFFF\" points=\"121.344,144.696 125.027,156.033 136.948,156.033 127.303,163.04 130.987,174.377    121.344,167.371 111.699,174.377 115.384,163.04 105.739,156.033 117.66,156.033  \"/> <polygon fill=\"#FFFFFF\" points=\"179.178,144.696 182.862,156.033 194.783,156.033 185.138,163.04 188.822,174.377    179.178,167.371 169.534,174.377 173.219,163.04 163.574,156.033 175.495,156.033  \"/> <polygon fill=\"#FFFFFF\" points=\"168.047,178.087 171.731,189.424 183.652,189.424 174.008,196.431 177.692,207.768    168.047,200.762 158.404,207.768 162.088,196.431 152.444,189.424 164.364,189.424  \"/> <polygon fill=\"#FFFFFF\" points=\"132.474,178.087 136.157,189.424 148.078,189.424 138.434,196.431 142.118,207.768    132.474,200.762 122.83,207.768 126.514,196.431 116.87,189.424 128.79,189.424  \"/> </g> </svg> ";
// Exports
/* harmony default export */ var sg = (sg_code);
;// ./templates/flags/sx.svg
// Module
var sx_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"3.413 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#0052B4\" width=\"512\" height=\"341.326\"/> <polygon fill=\"#D80027\" points=\"512,85.331 512,252.021 0,256.173 0,85.331 \"/> <polygon fill=\"#FFFFFF\" points=\"256,256.006 0,426.668 0,85.331 \"/> <g> <path fill=\"#FFDA44\" d=\"M59.621,256c-0.116,1.509-0.193,3.031-0.193,4.57c0,32.821,26.607,59.429,59.429,59.429   s59.429-26.607,59.429-59.429c0-1.539-0.078-3.061-0.193-4.57L59.621,256L59.621,256z\"/> <circle fill=\"#FFDA44\" cx=\"118.862\" cy=\"210.287\" r=\"18.286\"/> </g> <path fill=\"#D80027\" d=\"M77.715,205.714v59.429c0,31.494,41.144,41.143,41.144,41.143s41.144-9.649,41.144-41.143v-59.429  H77.715z\"/> <path fill=\"#338AF3\" d=\"M118.877,287.148c-7.632-2.746-22.876-9.767-22.876-22.006v-41.144h45.715v41.144  C141.715,277.422,126.472,284.425,118.877,287.148z\"/> <polygon fill=\"#F3F3F3\" points=\"128.001,246.856 128.001,237.714 118.857,233.143 109.715,237.714 109.715,246.856   105.144,251.429 105.144,269.714 132.572,269.714 132.572,251.429 \"/> </svg> ";
// Exports
/* harmony default export */ var sx = (sx_code);
;// ./templates/flags/sk.svg
// Module
var sk_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"66.56 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <rect y=\"196.641\" fill=\"#0052B4\" width=\"512\" height=\"118.717\"/> <rect y=\"315.359\" fill=\"#D80027\" width=\"512\" height=\"111.304\"/> <path fill=\"#FFFFFF\" d=\"M129.468,181.799v85.136c0,48.429,63.267,63.267,63.267,63.267S256,315.362,256,266.935v-85.136  H129.468z\"/> <path fill=\"#D80027\" d=\"M146.126,184.294v81.941c0,5.472,1.215,10.64,3.623,15.485c23.89,0,59.599,0,85.97,0  c2.408-4.844,3.623-10.012,3.623-15.485v-81.941H146.126z\"/> <polygon fill=\"#FFFFFF\" points=\"221.301,241.427 199.876,241.427 199.876,227.144 214.16,227.144 214.16,212.861   199.876,212.861 199.876,198.577 185.593,198.577 185.593,212.861 171.311,212.861 171.311,227.144 185.593,227.144   185.593,241.427 164.167,241.427 164.167,255.711 185.593,255.711 185.593,269.994 199.876,269.994 199.876,255.711   221.301,255.711 \"/> <path fill=\"#0052B4\" d=\"M169.232,301.658c9.204,5.783,18.66,9.143,23.502,10.636c4.842-1.494,14.298-4.852,23.502-10.636  c9.282-5.833,15.79-12.506,19.484-19.939c-4.075-2.883-9.047-4.583-14.418-4.583c-1.956,0-3.856,0.232-5.682,0.657  c-3.871-8.796-12.658-14.94-22.884-14.94c-10.227,0-19.013,6.144-22.884,14.94c-1.827-0.425-3.728-0.657-5.682-0.657  c-5.372,0-10.344,1.701-14.418,4.583C153.443,289.152,159.95,295.825,169.232,301.658z\"/> </svg> ";
// Exports
/* harmony default export */ var sk = (sk_code);
;// ./templates/flags/si.svg
// Module
var si_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <rect y=\"199.112\" fill=\"#0052B4\" width=\"512\" height=\"113.775\"/> <rect y=\"312.888\" fill=\"#D80027\" width=\"512\" height=\"113.775\"/> <path fill=\"#FFFFFF\" d=\"M233.606,196.639v14.837c0,34.081-44.522,44.522-44.522,44.522s-44.522-10.44-44.522-44.522v-14.837  l-0.145-44.188l89.043-0.266L233.606,196.639z\"/> <path fill=\"#0052B4\" d=\"M233.606,196.639v14.837c0,34.081-44.522,44.522-44.522,44.522s-44.522-10.44-44.522-44.522v-14.837  l-0.145-44.188l89.043-0.266L233.606,196.639z\"/> <path fill=\"#FFFFFF\" d=\"M233.606,196.639v14.837c0,34.081-44.522,44.522-44.522,44.522s-44.522-10.44-44.522-44.522v-14.837  l14.848,14.837l29.674-22.261l29.685,22.261L233.606,196.639z\"/> </svg> ";
// Exports
/* harmony default export */ var si = (si_code);
;// ./templates/flags/sb.svg
// Module
var sb_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"27.307 85.333 341.333 341.333\"> <rect y=\"85.334\" fill=\"#496E2D\" width=\"512\" height=\"341.337\"/> <polyline fill=\"#0052B4\" points=\"0,426.663 0,85.329 512,85.329 \"/> <polygon fill=\"#FFDA44\" points=\"0,396.494 0,426.663 45.255,426.663 512,115.499 512,85.329 466.745,85.329 \"/> <g> <polygon fill=\"#FFFFFF\" points=\"85.688,108.787 90.808,124.543 107.374,124.543 93.971,134.28 99.091,150.037    85.688,140.299 72.283,150.037 77.403,134.28 64,124.543 80.567,124.543  \"/> <polygon fill=\"#FFFFFF\" points=\"170.312,108.787 175.433,124.543 192,124.543 178.597,134.28 183.717,150.037    170.312,140.299 156.909,150.037 162.029,134.28 148.626,124.543 165.192,124.543  \"/> <polygon fill=\"#FFFFFF\" points=\"85.688,191.289 90.808,207.045 107.374,207.045 93.971,216.783 99.091,232.54    85.688,222.801 72.283,232.54 77.403,216.783 64,207.045 80.567,207.045  \"/> <polygon fill=\"#FFFFFF\" points=\"170.312,191.289 175.433,207.045 192,207.045 178.597,216.783 183.717,232.54    170.312,222.801 156.909,232.54 162.029,216.783 148.626,207.045 165.192,207.045  \"/> <polygon fill=\"#FFFFFF\" points=\"128,150.037 133.12,165.793 149.688,165.793 136.283,175.531 141.403,191.289 128,181.55    114.597,191.289 119.717,175.531 106.312,165.793 122.88,165.793  \"/> </g> </svg> ";
// Exports
/* harmony default export */ var sb = (sb_code);
;// ./templates/flags/so.svg
// Module
var so_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#338AF3\" width=\"512\" height=\"341.337\"/> <polygon fill=\"#FFFFFF\" points=\"256,157.273 278.663,227.021 352,227.021 292.668,270.127 315.332,339.876 256,296.769   196.668,339.876 219.332,270.127 160,227.021 233.337,227.021 \"/> </svg> ";
// Exports
/* harmony default export */ var so = (so_code);
;// ./templates/flags/za.svg
// Module
var za_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"35.84 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <polygon points=\"114.024,256.001 0,141.926 0,370.096 \"/> <polygon fill=\"#ffb915\" points=\"161.192,256 0,94.7 0,141.926 114.024,256.001 0,370.096 0,417.234 \"/> <path fill=\"#007847\" d=\"M509.833,289.391L509.833,289.391c0.058-0.44,0.804-0.878,2.167-1.318V256v-33.391h-2.167H222.602  L85.33,85.337H0V94.7L161.192,256L0,417.234v9.429h85.33l137.272-137.272H509.833z\"/> <polygon fill=\"#000c8a\" points=\"503.181,322.783 236.433,322.783 132.552,426.663 512,426.663 512,322.783 \"/> <polygon fill=\"#e1392d\" points=\"503.181,189.217 512,189.217 512,85.337 132.552,85.337 236.433,189.217 \"/> </svg> ";
// Exports
/* harmony default export */ var za = (za_code);
;// ./templates/flags/kr.svg
// Module
var kr_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"0 0 342 342\"> <rect x=\"0\" fill=\"#FFFFFF\" width=\"342\" height=\"342\"/> <g fill=\"#000000\"> <rect x=\"238.7\" y=\"215.2\" transform=\"matrix(0.7071 -0.7071 0.7071 0.7071 -84.344 242.3636)\" width=\"23.4\" height=\"15.6\"/> <rect x=\"211.2\" y=\"242.7\" transform=\"matrix(0.7071 -0.7071 0.7071 0.7071 -111.8675 231.0089)\" width=\"23.4\" height=\"15.6\"/> <rect x=\"271.8\" y=\"248.2\" transform=\"matrix(0.7071 -0.7071 0.7071 0.7071 -98.0097 275.4712)\" width=\"23.4\" height=\"15.6\"/> <rect x=\"244.2\" y=\"275.8\" transform=\"matrix(0.7071 -0.7071 0.7071 0.7071 -125.5836 264.0074)\" width=\"23.4\" height=\"15.6\"/> <rect x=\"255.2\" y=\"231.7\" transform=\"matrix(0.7071 -0.7071 0.7071 0.7071 -91.1976 258.867)\" width=\"23.4\" height=\"15.6\"/> <rect x=\"227.7\" y=\"259.3\" transform=\"matrix(0.7071 -0.7071 0.7071 0.7071 -118.7256 247.5085)\" width=\"23.4\" height=\"15.6\"/> <rect x=\"245.4\" y=\"56.9\" transform=\"matrix(0.7071 -0.7071 0.7071 0.7071 11.9322 204.8196)\" width=\"15.6\" height=\"62.3\"/> <rect x=\"215.1\" y=\"79.1\" transform=\"matrix(0.7071 -0.7071 0.7071 0.7071 1.1122 184.1938)\" width=\"15.6\" height=\"23.4\"/> <rect x=\"242.6\" y=\"106.6\" transform=\"matrix(0.7071 -0.7071 0.7071 0.7071 -10.3145 211.6879)\" width=\"15.6\" height=\"23.4\"/> <rect x=\"248.1\" y=\"46\" transform=\"matrix(0.7071 -0.7071 0.7071 0.7071 34.1495 197.8311)\" width=\"15.6\" height=\"23.4\"/> <rect x=\"275.6\" y=\"73.6\" transform=\"matrix(0.7071 -0.7071 0.7071 0.7071 22.6842 225.4057)\" width=\"15.6\" height=\"23.4\"/> <rect x=\"63.5\" y=\"238.7\" transform=\"matrix(0.7071 -0.7071 0.7071 0.7071 -169.9343 129.4781)\" width=\"15.6\" height=\"62.3\"/> <rect x=\"93.8\" y=\"255.4\" transform=\"matrix(0.7071 -0.7071 0.7071 0.7071 -159.1095 150.0916)\" width=\"15.6\" height=\"23.4\"/> <rect x=\"66.3\" y=\"227.8\" transform=\"matrix(0.7071 -0.7071 0.7071 0.7071 -147.6547 122.5258)\" width=\"15.6\" height=\"23.4\"/> <rect x=\"96.6\" y=\"205.6\" transform=\"matrix(0.7071 -0.7071 0.7071 0.7071 -136.8372 143.1472)\" width=\"15.6\" height=\"62.3\"/> <rect x=\"40.2\" y=\"63.7\" transform=\"matrix(0.7071 -0.7071 0.7071 0.7071 -29.6489 71.3957)\" width=\"62.3\" height=\"15.6\"/> <rect x=\"56.7\" y=\"80.2\" transform=\"matrix(0.7071 -0.7071 0.7071 0.7071 -36.5029 87.8853)\" width=\"62.3\" height=\"15.6\"/> <rect x=\"73.2\" y=\"96.8\" transform=\"matrix(0.7071 -0.7071 0.7071 0.7071 -43.3906 104.4559)\" width=\"62.3\" height=\"15.6\"/> </g> <circle fill=\"#D80027\" cx=\"170.3\" cy=\"170.7\" r=\"62.3\"/> <path fill=\"#0052B4\" d=\"M232.2,178.3c-9.6-25.4-44.3-25-61.8,1.3c-17.6,26.3-43.8,16.9-61.6,0.7c4.6,29.9,30.4,52.7,61.6,52.7\n\tC202.2,233,228.4,209.1,232.2,178.3z\"/> </svg> ";
// Exports
/* harmony default export */ var kr = (kr_code);
;// ./templates/flags/ss.svg
// Module
var ss_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"5.12 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <rect y=\"322.783\" fill=\"#496E2D\" width=\"512\" height=\"103.88\"/> <rect y=\"85.337\" width=\"512\" height=\"104.515\"/> <rect y=\"210.877\" fill=\"#A2001D\" width=\"512\" height=\"89.656\"/> <polygon fill=\"#0052B4\" points=\"256,256.006 0,426.668 0,85.331 \"/> <polygon fill=\"#FFDA44\" points=\"73.178,209.188 94.009,238.255 128.093,227.425 106.886,256.22 127.716,285.289   93.777,274.018 72.569,302.812 72.803,267.05 38.863,255.777 72.946,244.947 \"/> </svg> ";
// Exports
/* harmony default export */ var ss = (ss_code);
;// ./templates/flags/es.svg
// Module
var es_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"3.75 0 15 15\"> <path fill=\"#FFFFFF\" d=\"M0,0h22.5v15H0V0z\"/> <path fill=\"#D03433\" d=\"M0,0h22.5v4H0V0z M0,11h22.5v4H0V11z\"/> <path fill=\"#FBCA46\" d=\"M0,4h22.5v7H0V4z\"/> <path fill=\"#FFFFFF\" d=\"M7.8,7h1v0.5h-1V7z\"/> <path fill=\"#A41517\" d=\"M7.2,8.5C7.2,8.8,7.5,9,7.8,9c0.3,0,0.6-0.2,0.6-0.5L8.5,7H7.1C7.1,7,7.2,8.5,7.2,8.5z M6.6,7\n\t\tc0-0.3,0.2-0.5,0.4-0.5c0,0,0,0,0,0h1.5C8.8,6.5,9,6.7,9,6.9C9,7,9,7,9,7L8.9,8.5c-0.1,0.6-0.5,1-1.1,1c-0.6,0-1-0.4-1.1-1L6.6,7\n\t\tL6.6,7z\"/> <path fill=\"#A41517\" d=\"M6.8,7.5h2V8H8.3L7.8,9L7.3,8H6.8V7.5z M5.3,6h1v3.5h-1V6z M9.3,6h1v3.5h-1V6z M6.8,5.5C6.8,5.2,7,5,7.3,5h1\n\t\tc0.3,0,0.5,0.2,0.5,0.5v0.2C8.8,5.9,8.7,6,8.5,6c0,0,0,0,0,0H7C6.9,6,6.8,5.9,6.8,5.8c0,0,0,0,0,0V5.5z\"/> </svg> ";
// Exports
/* harmony default export */ var es = (es_code);
;// ./templates/flags/lk.svg
// Module
var lk_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"42.75 0 342 342\"> <rect x=\"0\" y=\"0\" fill=\"#FFDA44\" width=\"513\" height=\"342\"/> <rect x=\"222.6\" y=\"22.3\" fill=\"#A2001D\" width=\"267.1\" height=\"296.8\"/> <rect x=\"22.3\" y=\"22.3\" fill=\"#D80027\" width=\"200.3\" height=\"296.8\"/> <rect x=\"200.3\" y=\"0\" fill=\"#FFDA44\" width=\"22.3\" height=\"342\"/> <rect x=\"22.3\" y=\"22.3\" fill=\"#6DA544\" width=\"89\" height=\"296.8\"/> <rect x=\"111.3\" y=\"22.3\" fill=\"#FF9811\" width=\"89\" height=\"296.8\"/> <g> <path fill=\"#FFDA44\" d=\"M368.2,156.8c-1.4-2,0-37.9,0-37.9c0.9-7.4,0.1-8.4,1-11.4c0.9-2.9,5.4-11.6,5.4-11.6s-6-2.3-10.3-0.6\n\t\tc-4.3,1.7-24.7-7.5-31.2,3.4c-4.4,7-23.6,16.2-24.6,14c-1-2.2-5.9-5-6.9-2.9s-0.7,13.9,0,16c0.8,2.6,9.8,2,12.3,3.1\n\t\tc3.7,1.8,12.2,8.2,11.9,10.7c-0.3,2.5-22.5-6-24.2-4.5c-1.4,1.2-1.9,13.2-0.4,15c1.8,2.1,23.4,5.6,25,7.5c1.6,1.9-13.1,7.4-12.3,25\n\t\tc0.7,14.7,8,17.4,8,17.4s-2.4,3.6-7.8,3.1c-6.2-0.6-27.4-17.7-27.4-17.7S283.2,76.4,290.3,64c4.9-8.5-22.7,10.1-22.7,50.8v36.2\n\t\tc0,6.6-1.4,22.4,2.6,34c0,0-0.4,46.9,1.5,49.6s12.7,2.2,14.8,0s0-30,0-30c7.7,0.2,18.3,17.5,32.5,16.4c20.6-1.6,28.7-9.9,34.3-5\n\t\tc8.1,7.1,5.5,23.2,0.4,31.5c-2.2,3.5-14.3,4.7-15.7,7.6c-1.4,2.9,0.5,5.4,0.5,5.4h29.7c0,0,1.4-20.5,2.7-26.1\n\t\tc1.3-5.7-0.6-10.7,5.9-10.4c23.5,1.1,39.8-15.3,45.5-8c3.2,4.1,5.6,29.9,3.3,33.3c-2.2,3.4-13.8,2.8-15.2,6.7\n\t\tc-1.4,4,0.8,4.4,0.8,4.4h29.7c0,0-0.4-21.2,2.5-27.7c2.9-6.5,4.9-10.4,9.3-17.2c4.4-6.8,11.3-15.7,11.3-36.2\n\t\tc0-14.2-8.1-22.8-8.1-22.8h-38.9C402.9,156.7,369.6,158.8,368.2,156.8z\"/> <path fill=\"#FFDA44\" d=\"M232.2,32.3c10.7-10.6,20.2-2.7,22.2,2.1c1.9,4.8,2.1,8.2,2.8,13.6c0.9,6.9,4.9,14,4.9,14s-7.1-3.8-14.7-4.2\n\t\tc-4.7-0.3-9-0.6-13.6-2.5C228.1,53,222.1,42.4,232.2,32.3z\"/> <path fill=\"#FFDA44\" d=\"M480.6,32.2c10.7,10.6,2.8,20.2-2,22.2c-4.8,2-8.2,2.1-13.6,2.8c-6.9,0.9-14,5-14,5s3.7-7.1,4.2-14.7\n\t\tc0.3-4.7,0.6-9.1,2.4-13.6C459.8,28.2,470.4,22.1,480.6,32.2z\"/> <path fill=\"#FFDA44\" d=\"M480.7,309.1c-10.7,10.6-20.2,2.6-22.2-2.2c-1.9-4.8-2.1-8.2-2.7-13.6c-0.8-6.9-4.9-14.1-4.9-14.1\n\t\ts7,3.8,14.7,4.3c4.7,0.3,9,0.6,13.6,2.5C484.9,288.4,490.9,299.1,480.7,309.1z\"/> <path fill=\"#FFDA44\" d=\"M232.5,309c-10.6-10.7-2.7-20.2,2.1-22.2c4.8-1.9,8.2-2.1,13.6-2.8c6.9-0.9,14-4.9,14-4.9s-3.8,7.1-4.2,14.7\n\t\tc-0.3,4.7-0.6,9-2.5,13.6C253.2,313.1,242.6,319.2,232.5,309z\"/> <path fill=\"none\" stroke=\"#FFDA44\" stroke-width=\"6\" stroke-miterlimit=\"10\" d=\"M446.9,162.4c0,0,7.5-2.6,8.4-16.1c0.6-8.6-19.8-16-39-11.5c-19.7,4.6-34-3.1-34-14.6\n\t\tc0-22.9,29.7-16.2,38.9-11.3c9.1,4.9,29.7,23.5,35.1,9.4\"/> </g> </svg> ";
// Exports
/* harmony default export */ var lk = (lk_code);
;// ./templates/flags/sd.svg
// Module
var sd_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"3.413 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <rect y=\"85.337\" fill=\"#D80027\" width=\"512\" height=\"113.775\"/> <rect y=\"312.888\" width=\"512\" height=\"113.775\"/> <polygon fill=\"#496E2D\" points=\"0,426.668 0,85.331 256,256.006 \"/> </svg> ";
// Exports
/* harmony default export */ var sd = (sd_code);
;// ./templates/flags/sr.svg
// Module
var sr_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#FFFFFF\" width=\"512\" height=\"341.337\"/> <rect y=\"196.636\" fill=\"#A2001D\" width=\"512\" height=\"118.728\"/> <g> <rect y=\"352.462\" fill=\"#6DA544\" width=\"512\" height=\"74.207\"/> <rect y=\"85.331\" fill=\"#6DA544\" width=\"512\" height=\"74.207\"/> </g> <polygon fill=\"#FFDA44\" points=\"256.742,218.003 266.172,247.024 296.69,247.024 272,264.963 281.431,293.986   256.742,276.049 232.053,293.986 241.484,264.963 216.794,247.024 247.312,247.024 \"/> </svg> ";
// Exports
/* harmony default export */ var sr = (sr_code);
;// ./templates/flags/sj.svg
// Module
var sj_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"40.96 85.333 341.333 341.333\"> <rect y=\"85.334\" fill=\"#D80027\" width=\"512\" height=\"341.337\"/> <polygon fill=\"#FFFFFF\" points=\"512,295.883 202.195,295.883 202.195,426.666 183.652,426.666 140.978,426.666   122.435,426.666 122.435,295.883 0,295.883 0,277.329 0,234.666 0,216.111 122.435,216.111 122.435,85.329 140.978,85.329   183.652,85.329 202.195,85.329 202.195,216.111 512,216.111 512,234.666 512,277.329 \"/> <polygon fill=\"#2E52B2\" points=\"512,234.666 512,277.329 183.652,277.329 183.652,426.666 140.978,426.666 140.978,277.329   0,277.329 0,234.666 140.978,234.666 140.978,85.329 183.652,85.329 183.652,234.666 \"/> </svg> ";
// Exports
/* harmony default export */ var sj = (sj_code);
;// ./templates/flags/sz.svg
// Module
var sz_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"150 0 600 600\"> <path fill=\"#2B5DEA\" d=\"M0,0h900v600H0V0z\"/> <path fill=\"#FFDF29\" d=\"M0,100h900v400H0V100z\"/> <path fill=\"#D70000\" d=\"M0,150h900v300H0V150z\"/> <path fill=\"#FFFFFF\" d=\"M450,171.4v257.2c114.3,0,171.4-85.7,214.3-128.6C621.4,257.1,564.3,171.4,450,171.4z\"/> <path d=\"M450,171.4c-100,0-171.4,85.7-214.3,128.6C278.6,342.9,350,428.6,450,428.6V171.4z\"/> <rect x=\"346.3\" y=\"254.3\" fill=\"#FFFFFF\" width=\"21\" height=\"91.3\"/> <rect x=\"398.2\" y=\"254.3\" fill=\"#FFFFFF\" width=\"21\" height=\"91.3\"/> <rect x=\"477.8\" y=\"254.3\" width=\"21\" height=\"91.3\"/> <rect x=\"529.7\" y=\"254.3\" width=\"21\" height=\"91.3\"/> </svg> ";
// Exports
/* harmony default export */ var sz = (sz_code);
;// ./templates/flags/se.svg
// Module
var se_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"30.72 85.333 341.333 341.333\"> <rect y=\"85.333\" fill=\"#0052B4\" width=\"512\" height=\"341.337\"/> <polygon fill=\"#FFDA44\" points=\"192,85.33 128,85.33 128,223.996 0,223.996 0,287.996 128,287.996 128,426.662 192,426.662   192,287.996 512,287.996 512,223.996 192,223.996 \"/> </svg> ";
// Exports
/* harmony default export */ var se = (se_code);
;// ./templates/flags/ch.svg
// Module
var ch_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.5 85.333 342 342\"> <rect y=\"85.337\" fill=\"#ff0000\" width=\"513\" height=\"342\"/> <polygon fill=\"#FFFFFF\" points=\"356.174,222.609 289.391,222.609 289.391,155.826 222.609,155.826 222.609,222.609   155.826,222.609 155.826,289.391 222.609,289.391 222.609,356.174 289.391,356.174 289.391,289.391 356.174,289.391 \"/> </svg> ";
// Exports
/* harmony default export */ var ch = (ch_code);
;// ./templates/flags/sy.svg
// Module
var sy_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#FFFFFF\" width=\"512\" height=\"341.337\"/> <rect y=\"85.331\" fill=\"#D80027\" width=\"512\" height=\"113.775\"/> <rect y=\"312.882\" width=\"512\" height=\"113.775\"/> <g> <polygon fill=\"#6DA544\" points=\"187.31,215.184 196.518,243.525 226.32,243.525 202.21,261.043 211.419,289.385    187.31,271.869 163.2,289.385 172.409,261.043 148.3,243.525 178.101,243.525  \"/> <polygon fill=\"#6DA544\" points=\"324.69,215.184 333.899,243.525 363.7,243.525 339.591,261.043 348.8,289.385    324.69,271.869 300.581,289.385 309.79,261.043 285.68,243.525 315.482,243.525  \"/> </g> </svg> ";
// Exports
/* harmony default export */ var sy = (sy_code);
;// ./templates/flags/tw.svg
// Module
var tw_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"6.827 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#D80027\" width=\"512\" height=\"341.326\"/> <rect y=\"85.337\" fill=\"#0052B4\" width=\"256\" height=\"170.663\"/> <polygon fill=\"#FFFFFF\" points=\"186.435,170.669 162.558,181.9 175.272,205.025 149.345,200.064 146.059,226.256   128,206.993 109.94,226.256 106.655,200.064 80.728,205.024 93.442,181.899 69.565,170.669 93.442,159.438 80.728,136.313   106.655,141.273 109.941,115.081 128,134.344 146.06,115.081 149.345,141.273 175.273,136.313 162.558,159.438 \"/> <circle fill=\"#0052B4\" cx=\"128\" cy=\"170.674\" r=\"29.006\"/> <path fill=\"#FFFFFF\" d=\"M128,190.06c-10.692,0-19.391-8.7-19.391-19.391c0-10.692,8.7-19.391,19.391-19.391  c10.692,0,19.391,8.7,19.391,19.391C147.391,181.36,138.692,190.06,128,190.06z\"/> </svg> ";
// Exports
/* harmony default export */ var tw = (tw_code);
;// ./templates/flags/tj.svg
// Module
var tj_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <rect y=\"85.337\" fill=\"#D80027\" width=\"512\" height=\"113.775\"/> <rect y=\"312.888\" fill=\"#6DA544\" width=\"512\" height=\"113.775\"/> <g> <polygon fill=\"#FFDA44\" points=\"226.318,300.522 285.682,300.522 285.682,275.292 273.809,281.229 256,263.421    238.191,281.229 226.318,275.292  \"/> <polygon fill=\"#FFDA44\" points=\"192.355,270.84 195.118,279.344 204.058,279.344 196.825,284.599 199.588,293.101    192.355,287.846 185.121,293.101 187.884,284.599 180.651,279.344 189.591,279.344  \"/> <polygon fill=\"#FFDA44\" points=\"200.921,241.16 203.684,249.662 212.624,249.662 205.391,254.917 208.154,263.421    200.921,258.165 193.687,263.421 196.45,254.917 189.217,249.662 198.157,249.662  \"/> <polygon fill=\"#FFDA44\" points=\"225.409,218.899 228.172,227.401 237.112,227.401 229.879,232.656 232.642,241.16    225.409,235.904 218.175,241.16 220.938,232.656 213.705,227.401 222.645,227.401  \"/> <polygon fill=\"#FFDA44\" points=\"319.645,270.84 316.882,279.344 307.942,279.344 315.175,284.599 312.412,293.101    319.645,287.846 326.879,293.101 324.116,284.599 331.349,279.344 322.409,279.344  \"/> <polygon fill=\"#FFDA44\" points=\"311.079,241.16 308.316,249.662 299.376,249.662 306.609,254.917 303.846,263.421    311.079,258.165 318.313,263.421 315.55,254.917 322.783,249.662 313.843,249.662  \"/> <polygon fill=\"#FFDA44\" points=\"286.591,218.899 283.828,227.401 274.888,227.401 282.121,232.656 279.358,241.16    286.591,235.904 293.825,241.16 291.062,232.656 298.295,227.401 289.355,227.401  \"/> <polygon fill=\"#FFDA44\" points=\"256,207.767 258.763,216.27 267.704,216.27 260.47,221.526 263.233,230.028 256,224.774    248.767,230.028 251.53,221.526 244.296,216.27 253.237,216.27  \"/> </g> </svg> ";
// Exports
/* harmony default export */ var tj = (tj_code);
;// ./templates/flags/tz.svg
// Module
var tz_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#338AF3\" width=\"512\" height=\"341.326\"/> <polyline fill=\"#6DA544\" points=\"0,426.663 0,85.337 512,85.337 \"/> <polygon fill=\"#FFDA44\" points=\"512,152.222 512,85.337 411.67,85.337 0,359.778 0,426.663 100.33,426.663 \"/> <polygon points=\"512,85.337 512,125.462 60.193,426.663 0,426.663 0,386.538 451.807,85.337 \"/> </svg> ";
// Exports
/* harmony default export */ var tz = (tz_code);
;// ./templates/flags/th.svg
// Module
var th_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.334\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <rect y=\"194.056\" fill=\"#0052B4\" width=\"512\" height=\"123.882\"/> <g> <rect y=\"85.334\" fill=\"#D80027\" width=\"512\" height=\"54.522\"/> <rect y=\"372.143\" fill=\"#D80027\" width=\"512\" height=\"54.522\"/> </g> </svg> ";
// Exports
/* harmony default export */ var th = (th_code);
;// ./templates/flags/tg.svg
// Module
var tg_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"0 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFDA44\" width=\"512\" height=\"341.326\"/> <g> <rect y=\"85.337\" fill=\"#496E2D\" width=\"512\" height=\"68.263\"/> <rect y=\"358.4\" fill=\"#496E2D\" width=\"512\" height=\"68.263\"/> <rect y=\"221.863\" fill=\"#496E2D\" width=\"512\" height=\"68.263\"/> </g> <rect y=\"85.337\" fill=\"#D80027\" width=\"204.054\" height=\"204.054\"/> <polygon fill=\"#FFFFFF\" points=\"102.026,133.938 115.286,174.75 158.202,174.75 123.484,199.976 136.744,240.79   102.026,215.566 67.307,240.79 80.57,199.976 45.852,174.75 88.765,174.75 \"/> </svg> ";
// Exports
/* harmony default export */ var tg = (tg_code);
;// ./templates/flags/tk.svg
// Module
var tk_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"18.773 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#0052B4\" width=\"512\" height=\"341.326\"/> <g> <polygon fill=\"#FFFFFF\" points=\"210.715,174.377 214.399,185.715 226.318,185.715 216.675,192.721 220.359,204.058    210.715,197.051 201.071,204.058 204.755,192.721 195.11,185.715 207.031,185.715  \"/> <polygon fill=\"#FFFFFF\" points=\"116.816,281.971 121.421,296.143 136.321,296.143 124.266,304.9 128.872,319.073    116.816,310.313 104.761,319.073 109.365,304.9 97.311,296.143 112.21,296.143  \"/> <polygon fill=\"#FFFFFF\" points=\"144.696,119.679 149.3,133.851 164.201,133.851 152.145,142.609 156.751,156.78    144.696,148.021 132.64,156.78 137.246,142.609 125.191,133.851 140.091,133.851  \"/> <polygon fill=\"#FFFFFF\" points=\"69.539,175.331 74.143,189.503 89.043,189.503 76.988,198.261 81.594,212.432    69.539,203.674 57.483,212.432 62.089,198.261 50.034,189.503 64.934,189.503  \"/> </g> <g> <path fill=\"#FFDA44\" d=\"M483.386,354.503H117.801c0,0,109.234-88.562,310.593-220.938   C428.395,133.565,339.951,262.5,483.386,354.503z\"/> <path fill=\"#FFDA44\" d=\"M117.801,366.089c-11.177,0-11.195,17.37,0,17.37h365.585c11.177,0,11.195-17.37,0-17.37H117.801z\"/> </g> </svg> ";
// Exports
/* harmony default export */ var tk = (tk_code);
;// ./templates/flags/to.svg
// Module
var to_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"11.947 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#D80027\" width=\"512\" height=\"341.337\"/> <rect y=\"85.331\" fill=\"#FFFFFF\" width=\"256\" height=\"170.663\"/> <polygon fill=\"#D80027\" points=\"141.357,157.303 141.357,130.59 114.643,130.59 114.643,157.303 87.93,157.303   87.93,184.016 114.643,184.016 114.643,210.729 141.357,210.729 141.357,184.016 168.07,184.016 168.07,157.303 \"/> </svg> ";
// Exports
/* harmony default export */ var to = (to_code);
;// ./templates/flags/tt.svg
// Module
var tt_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#D80027\" width=\"512\" height=\"341.326\"/> <polygon fill=\"#FFFFFF\" points=\"6.066,85.337 214.027,297.973 345.611,426.663 505.934,426.663 297.973,214.027   166.389,85.337 \"/> <polygon points=\"43.364,85.337 384.69,426.663 468.636,426.663 127.31,85.337 \"/> </svg> ";
// Exports
/* harmony default export */ var tt = (tt_code);
;// ./templates/flags/tn.svg
// Module
var tn_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#D80027\" width=\"512\" height=\"341.337\"/> <circle fill=\"#FFFFFF\" cx=\"256\" cy=\"255.994\" r=\"96\"/> <g> <polygon fill=\"#D80027\" points=\"267.826,219.291 284.296,241.986 310.969,233.337 294.473,256.013 310.941,278.708    284.277,270.027 267.782,292.703 267.799,264.663 241.135,255.981 267.809,247.333  \"/> <path fill=\"#D80027\" d=\"M277.818,312.724c-31.33,0-56.727-25.397-56.727-56.727s25.397-56.727,56.727-56.727   c9.769,0,18.96,2.47,26.985,6.819c-12.589-12.31-29.804-19.909-48.803-19.909c-38.558,0-69.818,31.259-69.818,69.818   s31.26,69.818,69.818,69.818c18.999,0,36.215-7.599,48.803-19.909C296.777,310.254,287.587,312.724,277.818,312.724z\"/> </g> </svg> ";
// Exports
/* harmony default export */ var tn = (tn_code);
;// ./templates/flags/tr.svg
// Module
var tr_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"76.95 0 342 342\"> <rect fill=\"#E30A17\" width=\"513\" height=\"342\"/> <path fill=\"#FFFFFF\" d=\"M259.7,118.6c-13.1-9.5-29-14.6-45.3-14.5c-40.8,0-73.8,30.8-73.8,68.9s33.1,68.9,73.8,68.9\n\tc17.1,0,32.9-5.4,45.3-14.5c-30,38.6-85.7,45.6-124.3,15.5s-45.6-85.7-15.5-124.3s85.7-45.6,124.3-15.5\n\tC250,107.6,255.2,112.9,259.7,118.6z M299.6,184.4l-18.1,21.9l1.2-28.4l-26.4-10.4l27.3-7.6l1.8-28.3l15.6,23.7l27.5-7.1L311,170.2\n\tl15.3,23.9L299.6,184.4L299.6,184.4z\"/> </svg> ";
// Exports
/* harmony default export */ var tr = (tr_code);
;// ./templates/flags/tm.svg
// Module
var tm_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"81.92 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#496E2D\" width=\"512\" height=\"341.326\"/> <g> <path fill=\"#FFFFFF\" d=\"M357.208,183.679c8.397-23.404-1.036-48.858-21.281-61.536c3.641,0.474,7.287,1.331,10.884,2.621   c27.002,9.688,41.038,39.428,31.35,66.431s-39.428,41.038-66.431,31.35c-3.597-1.291-6.957-2.947-10.069-4.895   C325.347,220.734,348.811,207.084,357.208,183.679z\"/> <polygon fill=\"#FFFFFF\" points=\"310.637,115.729 303.34,125.156 292.12,121.13 298.832,130.982 291.536,140.409    302.979,137.071 309.691,146.923 310.052,135.009 321.496,131.67 310.275,127.645  \"/> <polygon fill=\"#FFFFFF\" points=\"330.338,146.448 323.042,155.875 311.821,151.849 318.533,161.701 311.237,171.128    322.68,167.789 329.392,177.642 329.752,165.727 341.197,162.387 329.976,158.363  \"/> <polygon fill=\"#FFFFFF\" points=\"275.9,126.916 268.604,136.343 257.385,132.319 264.096,142.17 256.8,151.596    268.243,148.258 274.955,158.11 275.316,146.195 286.76,142.858 275.539,138.831  \"/> <polygon fill=\"#FFFFFF\" points=\"275.101,162.105 267.805,171.532 256.584,167.506 263.296,177.358 256,186.785    267.444,183.447 274.155,193.299 274.517,181.384 285.96,178.046 274.74,174.02  \"/> <polygon fill=\"#FFFFFF\" points=\"308.585,174.118 301.289,183.545 290.069,179.519 296.781,189.372 289.484,198.798    300.928,195.46 307.64,205.312 308,193.397 319.443,190.059 308.224,186.034  \"/> </g> <rect x=\"83.478\" y=\"85.337\" fill=\"#D80027\" width=\"89.043\" height=\"341.326\"/> <polygon fill=\"#FFFFFF\" points=\"117.458,175.191 102.55,164.086 102.55,148.381 117.458,137.276 138.542,137.276   153.45,148.381 153.45,164.086 138.542,175.191 \"/> <g> <polygon fill=\"#FF9811\" points=\"128,137.276 117.458,137.276 102.55,148.381 102.55,156.059 128,156.059  \"/> <polygon fill=\"#FF9811\" points=\"128,175.191 138.542,175.191 153.45,164.086 153.45,156.074 128,156.074  \"/> </g> <polygon fill=\"#FFFFFF\" points=\"117.458,374.725 102.55,363.619 102.55,347.915 117.458,336.81 138.542,336.81   153.45,347.915 153.45,363.619 138.542,374.725 \"/> <g> <polygon fill=\"#FF9811\" points=\"128,336.81 117.458,336.81 102.55,347.915 102.55,355.593 128,355.593  \"/> <polygon fill=\"#FF9811\" points=\"128,374.725 138.542,374.725 153.45,363.619 153.45,355.608 128,355.608  \"/> </g> <g> <polygon fill=\"#496E2D\" points=\"117.458,274.957 102.55,263.852 102.55,248.149 117.458,237.043 138.542,237.043    153.45,248.149 153.45,263.852 138.542,274.957  \"/> <polygon fill=\"#496E2D\" points=\"153.971,299.391 147.478,299.391 147.478,292.898 136.067,292.898 127.999,284.83    119.932,292.898 108.522,292.898 108.522,299.391 102.029,299.391 102.029,312.377 108.522,312.377 108.522,318.869    119.932,318.869 128,326.938 136.068,318.869 147.478,318.869 147.478,312.377 153.971,312.377  \"/> <polygon fill=\"#496E2D\" points=\"153.971,199.623 147.478,199.623 147.478,193.131 136.067,193.131 127.999,185.062    119.932,193.131 108.522,193.131 108.522,199.623 102.029,199.623 102.029,212.609 108.522,212.609 108.522,219.101    119.932,219.101 128,227.171 136.068,219.101 147.478,219.101 147.478,212.609 153.971,212.609  \"/> </g> <g> <rect x=\"120.576\" y=\"248.576\" fill=\"#D80027\" width=\"14.84\" height=\"14.84\"/> <circle fill=\"#D80027\" cx=\"128\" cy=\"206.113\" r=\"7.421\"/> <circle fill=\"#D80027\" cx=\"128\" cy=\"305.887\" r=\"7.421\"/> </g> </svg> ";
// Exports
/* harmony default export */ var tm = (tm_code);
;// ./templates/flags/tc.svg
// Module
var tc_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"114.347 85.333 341.333 341.333\"> <rect y=\"85.333\" fill=\"#0052B4\" width=\"512\" height=\"341.337\"/> <path fill=\"#FFDA44\" d=\"M332.058,191.996v78.221c0,38.103,51.942,49.779,51.942,49.779s51.942-11.675,51.942-49.779v-78.221  H332.058z\"/> <path fill=\"#FF9811\" d=\"M376.579,220.44c0,7.855-6.644,28.445-14.84,28.445s-14.84-20.589-14.84-28.445  c0-7.856,14.84-14.222,14.84-14.222S376.579,212.585,376.579,220.44z\"/> <path fill=\"#A2001D\" d=\"M415.961,235.93c2.394-5.6,4.257-13.785,4.257-17.86c0-6.546-8.904-11.852-8.904-11.852  s-8.904,5.306-8.904,11.852c0,4.075,1.862,12.26,4.257,17.86l-5.141,11.123c3.022,1.178,6.324,1.831,9.788,1.831  c3.463,0,6.766-0.654,9.788-1.831L415.961,235.93z\"/> <path fill=\"#6DA544\" d=\"M372.87,270.217c0,0-7.421,14.222-7.421,28.445c0,0,22.261,0,37.101,0  c0-14.222-7.421-28.445-7.421-28.445l-11.13-7.111L372.87,270.217z\"/> <path fill=\"#D80027\" d=\"M395.13,270.217v-3.555c0-5.891-4.983-10.666-11.13-10.666c-6.147,0-11.13,4.776-11.13,10.666v3.555  H395.13L395.13,270.217z\"/> <polygon fill=\"#FFFFFF\" points=\"256,85.333 256,115.886 210.833,140.985 256,140.985 256,200.344 196.897,200.344   256,233.179 256,255.996 229.32,255.996 155.826,215.17 155.826,255.996 100.174,255.996 100.174,207.423 12.744,255.996 0,255.996   0,225.442 45.167,200.344 0,200.344 0,140.985 59.103,140.985 0,108.139 0,85.333 26.68,85.333 100.174,126.158 100.174,85.333   155.826,85.333 155.826,133.905 243.256,85.333 \"/> <polygon fill=\"#D80027\" points=\"144,85.33 112,85.33 112,154.662 0,154.662 0,186.662 112,186.662 112,255.996 144,255.996   144,186.662 256,186.662 256,154.662 144,154.662 \"/> <polygon fill=\"#0052B4\" points=\"155.826,200.344 256,255.996 256,240.259 184.153,200.344 \"/> <polygon fill=\"#FFFFFF\" points=\"155.826,200.344 256,255.996 256,240.259 184.153,200.344 \"/> <g> <polygon fill=\"#D80027\" points=\"155.826,200.344 256,255.996 256,240.259 184.153,200.344  \"/> <polygon fill=\"#D80027\" points=\"71.846,200.344 0,240.259 0,255.996 0,255.996 100.174,200.344  \"/> </g> <polygon fill=\"#0052B4\" points=\"100.174,140.982 0,85.33 0,101.067 71.847,140.982 \"/> <polygon fill=\"#FFFFFF\" points=\"100.174,140.982 0,85.33 0,101.067 71.847,140.982 \"/> <g> <polygon fill=\"#D80027\" points=\"100.174,140.982 0,85.33 0,101.067 71.847,140.982  \"/> <polygon fill=\"#D80027\" points=\"184.154,140.982 256,101.067 256,85.33 256,85.33 155.826,140.982  \"/> </g> </svg> ";
// Exports
/* harmony default export */ var tc = (tc_code);
;// ./templates/flags/tv.svg
// Module
var tv_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"150 0 600 600\"> <rect fill=\"#0052B4\" width=\"900\" height=\"600\"/> <path fill=\"#00B2EE\" d=\"M0,300h450V0h450v600H0V300z\"/> <path fill=\"#FFDA44\" d=\"M345.3,480.9l29.4,90.6l-77.1-56H393l-77.1,56L345.3,480.9z\"/> <path fill=\"#FFDA44\" d=\"M706.4,340l29.4,90.6l-77.1-56h95.3l-77.1,56L706.4,340z\"/> <path fill=\"#FFDA44\" d=\"M812.8,261.5l29.4,90.6l-77.1-56h95.3l-77.1,56L812.8,261.5z\"/> <path fill=\"#FFDA44\" d=\"M812.8,37.4l29.4,90.6l-77.1-56h95.3l-77.1,56L812.8,37.4z\"/> <polygon fill=\"#FFFFFF\" points=\"449.9,0 449.9,206.3 368.9,206.3 449.9,260.3 449.9,300 410.2,300 281.2,214.1 281.2,300 168.7,300 168.7,214.1 39.7,300 0,300 0,260.3 81,206.3 0,206.3 0,93.8 81,93.8 0,39.9 0,0 39.7,0 168.7,85.9 168.7,0 281.2,0 281.2,85.9 410.2,0 \"/> <polygon fill=\"#D80027\" points=\"253.1,0 196.8,0 196.8,122 0,122 0,178.2 196.8,178.2 196.8,300 253.1,300 253.1,178.2 449.9,178.2 449.9,122 253.1,122 \t\"/> <polygon fill=\"#2E52B2\" points=\"449.9,39.9 449.9,93.8 368.9,93.8 \"/> <polygon fill=\"#D80027\" points=\"0,300 0,280.7 109.9,206.3 149.6,206.3 12.8,300 \t\"/> <polygon fill=\"#D80027\" points=\"12.8,0.1 149.6,93.8 109.9,93.8 0,19.4 0,0.1 \"/> <polygon fill=\"#D80027\" points=\"449.9,0.2 449.9,19.5 340,93.8 300.3,93.8 437.1,0.2 \"/> <polygon fill=\"#D80027\" points=\"437.1,300 300.3,206.3 340,206.3 449.9,280.7 449.9,300 \"/> <path fill=\"#FFDA44\" d=\"M719.9,131.6l29.4,90.6l-77.1-56h95.3l-77.1,56L719.9,131.6z\"/> <path fill=\"#FFDA44\" d=\"M584,187.7l29.4,90.6l-77.1-56h95.3l-77.1,56L584,187.7z\"/> <path fill=\"#FFDA44\" d=\"M614,430.6l29.4,90.6l-77.1-56h95.3l-77.1,56L614,430.6z\"/> <path fill=\"#FFDA44\" d=\"M488,332l29.4,90.6l-77.1-56h95.3l-77.1,56L488,332z\"/> <path fill=\"#FFDA44\" d=\"M488,464l29.4,90.6l-77.1-56h95.3l-77.1,56L488,464z\"/> </svg> ";
// Exports
/* harmony default export */ var tv = (tv_code);
;// ./templates/flags/vi.svg
// Module
var vi_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.35 0 341.3 341.3\"> <rect y=\"0\" fill=\"#FFFFFF\" width=\"512\" height=\"341.3\"/> <path fill=\"#1583C4\" d=\"M397.7,166.3L346.7,264c-1.1,2-0.3,4.5,1.8,5.6c2,1.1,4.5,0.3,5.6-1.8l51-97.7c1-2.1,0.1-4.6-2-5.5\n\tC401.1,163.7,398.8,164.4,397.7,166.3L397.7,166.3z\"/> <path fill=\"#1583C4\" d=\"M404.9,188l-57.8,75.4c-1.3,1.9-0.8,4.5,1.1,5.8c1.7,1.2,4.1,0.9,5.5-0.7l57.8-75.4c1.4-1.8,1.1-4.4-0.8-5.8\n\tC408.9,185.8,406.3,186.2,404.9,188L404.9,188z\"/> <path fill=\"#1583C4\" d=\"M379,181.3l-32.5,83.1c-0.8,2.1,0.2,4.6,2.4,5.4c2.1,0.8,4.6-0.2,5.4-2.4l32.5-83.1c0.7-2.2-0.4-4.5-2.6-5.3\n\tC382.1,178.4,379.9,179.4,379,181.3z\"/> <path fill=\"#409347\" d=\"M122.1,171.9c0.4,1.7,1.2,4.7,2.2,8.5c1.7,6.4,3.6,12.8,5.6,18.7c2.3,6.9,14.7,31.4,20.2,39.1\n\tc6.9,9.7,14.1,19.3,21.5,28.6c1.6,1.9,4.5,2.1,6.4,0.5c1.8-1.6,2.1-4.3,0.7-6.2c-7.3-9.2-14.4-18.6-21.2-28.2\n\tc-4.2-5.8-17.2-31.1-19-36.7c-2.9-8.7-5.4-17.6-7.6-26.5c-0.5-2.4-2.9-4-5.3-3.5c-2.4,0.5-4,2.9-3.5,5.3\n\tC122,171.7,122.1,171.8,122.1,171.9L122.1,171.9z\"/> <g> <path fill=\"#FFD836\" stroke=\"#231F20\" stroke-miterlimit=\"10\" d=\"M297.1,114.9c0,0-17.6-72.1-36.1-72.1c-6.9,0-11.9-0.6-15.4,6.4c-0.9,1.7-20.7-1.6-19.1,20.8\n\t\tc0.5,6.9,1.1-9.8,14.9-3.4c6.3,3-16.4,48.2-16.4,48.2H297.1z\"/> <path fill=\"#FFD836\" stroke=\"#231F20\" stroke-miterlimit=\"10\" d=\"M466.1,48.4l-90.9,22c-75.3,18.3-42.1,44.4-42.1,44.4l-72.1,12l-72.1-12c0,0,33.2-26.1-42.1-44.4l-90.9-22\n\t\tC43.1,45.3,38.1,51.8,45.3,63c0,0,106.6,104,126.8,112c20.2,8.1,40.8,0,40.8,0s-14.9,8.7-24,24c-9.2,15.3-4.6,34.7-38.9,40.2\n\t\tc-5.5,6.6-5,14,2.9,19.9c7.9,5.8,72.1-48.1,72.1-48.1s-24,24.4-24,36.1c0,5.1,44.1,53.8,60.1,53.8s60.1-48.7,60.1-53.8\n\t\tc0-11.7-24-36.1-24-36.1s64.2,53.9,72.1,48.1s11.4-19.3,2.8-19.9c-28-1.8-29.7-24.9-38.9-40.2c-9.2-15.3-24-24-24-24\n\t\ts20.6,8.1,40.8,0S476.7,63,476.7,63C483.9,51.8,479,45.3,466.1,48.4z\"/> </g> <path fill=\"#FFFFFF\" stroke=\"#231F20\" stroke-miterlimit=\"10\" d=\"M201.3,111.8v80.3c0,45.7,59.7,59.7,59.7,59.7s59.7-14,59.7-59.7v-80.3H201.3L201.3,111.8z\"/> <g> <path fill=\"#D80027\" d=\"M225.2,137.7v97.3c8.2,6,17,10.2,23.9,12.9V137.7H225.2L225.2,137.7z\"/> <path fill=\"#D80027\" d=\"M296.8,137.7v97.3c-8.2,6-17,10.2-23.9,12.9V137.7H296.8L296.8,137.7z\"/> </g> <rect x=\"201.3\" y=\"111.8\" fill=\"#0052B4\" width=\"119.4\" height=\"37.9\"/> <polygon fill=\"#1583C4\" points=\"60.1,210.5 38.2,152.4 15.7,152.4 47.5,233.2 72.7,233.2 104.2,152.4 81.7,152.4 \"/> <rect x=\"451.5\" y=\"151.9\" fill=\"#1583C4\" width=\"18.9\" height=\"81.7\"/> </svg>";
// Exports
/* harmony default export */ var vi = (vi_code);
;// ./templates/flags/ug.svg
// Module
var ug_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.35 0 341.3 341.3\"> <rect y=\"0\" fill=\"#232323\" width=\"512\" height=\"341.3\"/> <rect y=\"56.9\" fill=\"#FFDA44\" width=\"512\" height=\"56.9\"/> <rect y=\"113.8\" fill=\"#D32300\" width=\"512\" height=\"56.9\"/> <rect y=\"227.6\" fill=\"#FFDA44\" width=\"512\" height=\"56.9\"/> <rect y=\"284.4\" fill=\"#D32300\" width=\"512\" height=\"56.9\"/> <circle fill=\"#FFFFFF\" cx=\"256\" cy=\"170.7\" r=\"80.7\"/> <path fill=\"#000000\" d=\"M234.5,127.5c0.9-1.4-19.6-2-19.6-2c1.7-2.5,18.4-10.5,18.4-10.5s-0.9-6,2.7-9.8l-4.5-9.8c0,0,7-3.4,18.4-3.4\n\ts19.9,7,21,13.1l-6.4,3.4c-0.1,4.7,1.7,11.6-4.3,17.3c-5.9,5.7-8.3,6.9-8.5,18.8c0,3.9,2.3,8.2,8.8,11.7\n\tc17.4,9.3,38.6,28.2,44.6,33.9c6.1,5.7,5.1,19.2,2.6,25.2s-12.1,14.3-14.3,13.6c-2.2-0.6,1.2-9.2-3.5-11.6c0,0-8.9-7.6-16.8,0.6\n\ts-0.2,25.2,3.9,28c4.1,2.8,1.7,5-1.9,5h-28.1c-3.8,0-5.4-2.3-1.9-5c7.6-3.8,12.4-15.3,7.4-20.3c-4.9-4.9-18.3,1.5-21.5,6.3\n\tc-2.8,2.8-8.8,3.6-12.9-1.4s-4.1-10.4-1.1-12.5c7-4.8,0,0,0.1-0.1c0,0,11.9-8.6,29.9-9c3.9,0,4.8-2.2,2.2-4.8\n\tc0,0-30.9-27.4-28.1-44.2C224.1,141.7,241.1,133.7,234.5,127.5C233.2,125.7,234.5,127.5,234.5,127.5z\"/> </svg> ";
// Exports
/* harmony default export */ var ug = (ug_code);
;// ./templates/flags/ua.svg
// Module
var ua_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFDA44\" width=\"512\" height=\"341.326\"/> <rect y=\"85.337\" fill=\"#338AF3\" width=\"512\" height=\"170.663\"/> </svg> ";
// Exports
/* harmony default export */ var ua = (ua_code);
;// ./templates/flags/ae.svg
// Module
var ae_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"51.3 0 342 342\"> <rect y=\"0\" fill=\"#FFFFFF\" width=\"513\" height=\"342\"/> <rect y=\"0\" fill=\"#009e49\" width=\"513\" height=\"114\"/> <rect y=\"228\" width=\"513\" height=\"114\"/> <rect y=\"0\" fill=\"#ce1126\" width=\"171\" height=\"342\"/> </svg> ";
// Exports
/* harmony default export */ var ae = (ae_code);
;// ./templates/flags/gb.svg
// Module
var gb_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.333\" fill=\"#FFFFFF\" width=\"512\" height=\"341.337\"/> <polygon fill=\"#D80027\" points=\"288,85.33 224,85.33 224,223.996 0,223.996 0,287.996 224,287.996 224,426.662 288,426.662   288,287.996 512,287.996 512,223.996 288,223.996 \"/> <g> <polygon fill=\"#0052B4\" points=\"393.785,315.358 512,381.034 512,315.358  \"/> <polygon fill=\"#0052B4\" points=\"311.652,315.358 512,426.662 512,395.188 368.307,315.358  \"/> <polygon fill=\"#0052B4\" points=\"458.634,426.662 311.652,344.998 311.652,426.662  \"/> </g> <polygon fill=\"#FFFFFF\" points=\"311.652,315.358 512,426.662 512,395.188 368.307,315.358 \"/> <polygon fill=\"#D80027\" points=\"311.652,315.358 512,426.662 512,395.188 368.307,315.358 \"/> <g> <polygon fill=\"#0052B4\" points=\"90.341,315.356 0,365.546 0,315.356  \"/> <polygon fill=\"#0052B4\" points=\"200.348,329.51 200.348,426.661 25.491,426.661  \"/> </g> <polygon fill=\"#D80027\" points=\"143.693,315.358 0,395.188 0,426.662 0,426.662 200.348,315.358 \"/> <g> <polygon fill=\"#0052B4\" points=\"118.215,196.634 0,130.958 0,196.634  \"/> <polygon fill=\"#0052B4\" points=\"200.348,196.634 0,85.33 0,116.804 143.693,196.634  \"/> <polygon fill=\"#0052B4\" points=\"53.366,85.33 200.348,166.994 200.348,85.33  \"/> </g> <polygon fill=\"#FFFFFF\" points=\"200.348,196.634 0,85.33 0,116.804 143.693,196.634 \"/> <polygon fill=\"#D80027\" points=\"200.348,196.634 0,85.33 0,116.804 143.693,196.634 \"/> <g> <polygon fill=\"#0052B4\" points=\"421.659,196.636 512,146.446 512,196.636  \"/> <polygon fill=\"#0052B4\" points=\"311.652,182.482 311.652,85.331 486.509,85.331  \"/> </g> <polygon fill=\"#D80027\" points=\"368.307,196.634 512,116.804 512,85.33 512,85.33 311.652,196.634 \"/> </svg> ";
// Exports
/* harmony default export */ var gb = (gb_code);
;// ./templates/flags/us.svg
// Module
var us_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"59.85 0 342 342\"> <rect y=\"0\" fill=\"#FFFFFF\" width=\"513\" height=\"342\"/> <g fill=\"#D80027\"> <rect y=\"0\" width=\"513\" height=\"26.3\"/> <rect y=\"52.6\" width=\"513\" height=\"26.3\"/> <rect y=\"105.2\" width=\"513\" height=\"26.3\"/> <rect y=\"157.8\" width=\"513\" height=\"26.3\"/> <rect y=\"210.5\" width=\"513\" height=\"26.3\"/> <rect y=\"263.1\" width=\"513\" height=\"26.3\"/> <rect y=\"315.7\" width=\"513\" height=\"26.3\"/> </g> <rect fill=\"#2E52B2\" width=\"256.5\" height=\"184.1\"/> <g fill=\"#FFFFFF\"> <polygon points=\"47.8,138.9 43.8,126.1 39.4,138.9 26.2,138.9 36.9,146.6 32.9,159.4 43.8,151.5 54.4,159.4\n\t\t50.3,146.6 61.2,138.9 \t\"/> <polygon points=\"104.1,138.9 100,126.1 95.8,138.9 82.6,138.9 93.3,146.6 89.3,159.4 100,151.5 110.8,159.4\n\t\t106.8,146.6 117.5,138.9 \t\"/> <polygon points=\"160.6,138.9 156.3,126.1 152.3,138.9 138.8,138.9 149.8,146.6 145.6,159.4 156.3,151.5 167.3,159.4\n\t\t163.1,146.6 173.8,138.9 \t\"/> <polygon points=\"216.8,138.9 212.8,126.1 208.6,138.9 195.3,138.9 206.1,146.6 202.1,159.4 212.8,151.5 223.6,159.4\n\t\t219.3,146.6 230.3,138.9 \t\"/> <polygon points=\"100,75.3 95.8,88.1 82.6,88.1 93.3,96 89.3,108.6 100,100.8 110.8,108.6 106.8,96 117.5,88.1\n\t\t104.1,88.1 \t\"/> <polygon points=\"43.8,75.3 39.4,88.1 26.2,88.1 36.9,96 32.9,108.6 43.8,100.8 54.4,108.6 50.3,96 61.2,88.1\n\t\t47.8,88.1 \t\"/> <polygon points=\"156.3,75.3 152.3,88.1 138.8,88.1 149.8,96 145.6,108.6 156.3,100.8 167.3,108.6 163.1,96 173.8,88.1\n\t\t160.6,88.1 \t\"/> <polygon points=\"212.8,75.3 208.6,88.1 195.3,88.1 206.1,96 202.1,108.6 212.8,100.8 223.6,108.6 219.3,96 230.3,88.1\n\t\t216.8,88.1 \t\"/> <polygon points=\"43.8,24.7 39.4,37.3 26.2,37.3 36.9,45.2 32.9,57.9 43.8,50 54.4,57.9 50.3,45.2 61.2,37.3 47.8,37.3\n\t\t\t\"/> <polygon points=\"100,24.7 95.8,37.3 82.6,37.3 93.3,45.2 89.3,57.9 100,50 110.8,57.9 106.8,45.2 117.5,37.3\n\t\t104.1,37.3 \t\"/> <polygon points=\"156.3,24.7 152.3,37.3 138.8,37.3 149.8,45.2 145.6,57.9 156.3,50 167.3,57.9 163.1,45.2 173.8,37.3\n\t\t160.6,37.3 \t\"/> <polygon points=\"212.8,24.7 208.6,37.3 195.3,37.3 206.1,45.2 202.1,57.9 212.8,50 223.6,57.9 219.3,45.2 230.3,37.3\n\t\t216.8,37.3 \t\"/> </g> </svg> ";
// Exports
/* harmony default export */ var us = (us_code);
;// ./templates/flags/uy.svg
// Module
var uy_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"10.26 0 342 342\"> <rect fill=\"#FFFFFF\" width=\"513\" height=\"342\"/> <g fill=\"#0038a8\"> <rect y=\"38\" width=\"513\" height=\"38\"/> <rect y=\"114\" width=\"513\" height=\"38\"/> <rect y=\"190\" width=\"513\" height=\"38\"/> <rect y=\"266\" width=\"513\" height=\"38\"/> </g> <rect fill=\"#FFFFFF\" width=\"256.5\" height=\"190\"/> <path fill=\"#FED443\" d=\"M128.3,138.7l-15.1,22.6l-3.8-26.9l-23.4,13.8l8.2-25.9L67,124.5l18.6-19.8L60.3,95l25.4-9.7L67,65.5l27.1,2.3\n\tl-8.2-25.9l23.4,13.8l3.9-26.9l15.1,22.6l15.1-22.6l3.8,26.9l23.4-13.8l-8.2,25.9l27.1-2.2l-18.6,19.8l25.4,9.7l-25.4,9.7l18.6,19.8\n\tl-27.1-2.2l8.2,25.9l-23.4-13.8l-3.9,26.9L128.3,138.7z M128.3,138.2c23.9,0.9,44-17.6,44.9-41.5c0.9-23.9-17.6-44-41.5-44.9\n\tc-1.1,0-2.3,0-3.4,0c-23.9,0.8-42.6,20.8-41.8,44.6C87.1,119.2,105.5,137.5,128.3,138.2L128.3,138.2z M128.3,127.6\n\tc-18.8,0-34-15.2-34-34s15.2-34,34-34s34,15.2,34,34S147,127.6,128.3,127.6z\"/> </svg> ";
// Exports
/* harmony default export */ var uy = (uy_code);
;// ./templates/flags/uz.svg
// Module
var uz_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#D80027\" width=\"512\" height=\"341.326\"/> <rect y=\"322.783\" fill=\"#6DA544\" width=\"512\" height=\"103.88\"/> <rect id=\"SVGCleanerId_0\" y=\"85.337\" fill=\"#338AF3\" width=\"512\" height=\"104.515\"/> <rect y=\"210.877\" fill=\"#FFFFFF\" width=\"512\" height=\"89.656\"/> <g> <rect id=\"SVGCleanerId_0_1_\" y=\"85.337\" fill=\"#338AF3\" width=\"512\" height=\"104.515\"/> </g> <g> <path fill=\"#FFFFFF\" d=\"M188.688,137.589c0-15.984,11.234-29.339,26.236-32.614c-2.306-0.503-4.698-0.777-7.155-0.777   c-18.442,0-33.391,14.949-33.391,33.391s14.949,33.391,33.391,33.391c2.458,0,4.85-0.273,7.155-0.777   C199.922,166.928,188.688,153.573,188.688,137.589z\"/> <polygon fill=\"#FFFFFF\" points=\"234.658,152.766 236.919,159.723 244.234,159.723 238.316,164.024 240.577,170.98    234.658,166.68 228.74,170.98 231.001,164.024 225.083,159.723 232.398,159.723  \"/> <polygon fill=\"#FFFFFF\" points=\"258.006,152.766 260.266,159.723 267.581,159.723 261.663,164.024 263.924,170.98    258.006,166.68 252.088,170.98 254.348,164.024 248.431,159.723 255.745,159.723  \"/> <polygon fill=\"#FFFFFF\" points=\"281.353,152.766 283.613,159.723 290.928,159.723 285.011,164.024 287.271,170.98    281.353,166.68 275.435,170.98 277.695,164.024 271.777,159.723 279.092,159.723  \"/> <polygon fill=\"#FFFFFF\" points=\"304.7,152.766 306.962,159.723 314.277,159.723 308.358,164.024 310.619,170.98    304.7,166.68 298.782,170.98 301.044,164.024 295.125,159.723 302.439,159.723  \"/> <polygon fill=\"#FFFFFF\" points=\"328.048,152.766 330.308,159.723 337.623,159.723 331.705,164.024 333.966,170.98    328.048,166.68 322.13,170.98 324.39,164.024 318.473,159.723 325.787,159.723  \"/> <polygon fill=\"#FFFFFF\" points=\"258.006,128.482 260.266,135.438 267.581,135.438 261.663,139.738 263.924,146.695    258.006,142.396 252.088,146.695 254.348,139.738 248.431,135.438 255.745,135.438  \"/> <polygon fill=\"#FFFFFF\" points=\"281.353,128.482 283.613,135.438 290.928,135.438 285.011,139.738 287.271,146.695    281.353,142.396 275.435,146.695 277.695,139.738 271.777,135.438 279.092,135.438  \"/> <polygon fill=\"#FFFFFF\" points=\"304.7,128.482 306.962,135.438 314.277,135.438 308.358,139.738 310.619,146.695    304.7,142.396 298.782,146.695 301.044,139.738 295.125,135.438 302.439,135.438  \"/> <polygon fill=\"#FFFFFF\" points=\"328.048,128.482 330.308,135.438 337.623,135.438 331.705,139.738 333.966,146.695    328.048,142.396 322.13,146.695 324.39,139.738 318.473,135.438 325.787,135.438  \"/> <polygon fill=\"#FFFFFF\" points=\"281.353,104.198 283.613,111.154 290.928,111.154 285.011,115.454 287.271,122.411    281.353,118.111 275.435,122.411 277.695,115.454 271.777,111.154 279.092,111.154  \"/> <polygon fill=\"#FFFFFF\" points=\"304.7,104.198 306.962,111.154 314.277,111.154 308.358,115.454 310.619,122.411    304.7,118.111 298.782,122.411 301.044,115.454 295.125,111.154 302.439,111.154  \"/> <polygon fill=\"#FFFFFF\" points=\"328.048,104.198 330.308,111.154 337.623,111.154 331.705,115.454 333.966,122.411    328.048,118.111 322.13,122.411 324.39,115.454 318.473,111.154 325.787,111.154  \"/> </g> </svg> ";
// Exports
/* harmony default export */ var uz = (uz_code);
;// ./templates/flags/vu.svg
// Module
var vu_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"0 85.333 341.333 341.333\"> <rect y=\"85.337\" width=\"512\" height=\"341.326\"/> <polygon fill=\"#6DA544\" points=\"512,256 512,426.663 0,426.663 215.185,256 \"/> <polygon fill=\"#D80027\" points=\"512,85.337 512,256 215.185,256 0,85.337 \"/> <polygon points=\"221.001,239.304 26.868,85.337 8.956,85.337 217.124,250.435 512,250.435 512,239.304 \"/> <polygon points=\"8.956,426.663 26.868,426.663 221.001,272.696 512,272.696 512,261.565 217.124,261.565 \"/> <polygon points=\"0,92.44 0,106.646 188.317,256 0,405.354 0,419.559 206.229,256 \"/> <g> <polygon fill=\"#FFDA44\" points=\"8.956,85.337 0,85.337 0,92.44 206.229,256 0,419.559 0,426.663 8.956,426.663    217.124,261.565 512,261.565 512,250.435 217.124,250.435  \"/> <path fill=\"#FFDA44\" d=\"M63.718,292.382v-14.295c14.265,0,25.87-11.606,25.87-25.869c0-10.092-8.211-18.303-18.304-18.303   c-6.875,0-12.469,5.593-12.469,12.469c0,4.397,3.577,7.974,7.974,7.974c2.485,0,4.508-2.023,4.508-4.508h14.295   c0,10.368-8.435,18.804-18.802,18.804c-12.279-0.002-22.269-9.993-22.269-22.271c0-14.758,12.006-26.764,26.764-26.764   c17.975,0,32.599,14.623,32.599,32.599C103.884,274.363,85.866,292.382,63.718,292.382z\"/> </g> </svg> ";
// Exports
/* harmony default export */ var vu = (vu_code);
;// ./templates/flags/va.svg
// Module
var va_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#FFDA44\" width=\"512\" height=\"341.326\"/> <rect x=\"256\" y=\"85.331\" fill=\"#FFFFFF\" width=\"256\" height=\"341.337\"/> <path fill=\"#ACABB1\" d=\"M321.353,233.837l32.073,42.43c-5.053,7.651-5.026,17.961,0.817,25.692  c7.414,9.807,21.374,11.748,31.182,4.335c9.807-7.414,11.748-21.374,4.334-31.182c-5.843-7.731-15.756-10.568-24.495-7.795  l-49.988-66.129l-11.838,8.949l-17.759,13.424l17.899,23.677L321.353,233.837z M367.528,282.617  c3.27-2.472,7.923-1.824,10.395,1.445c2.471,3.269,1.824,7.923-1.445,10.395c-3.269,2.47-7.923,1.824-10.394-1.446  C363.613,289.743,364.259,285.088,367.528,282.617z\"/> <path fill=\"#FFDA44\" d=\"M376.367,247.24l17.899-23.677l-17.759-13.424l-11.838-8.949l-49.988,66.129  c-8.74-2.775-18.651,0.063-24.495,7.795c-7.414,9.808-5.473,23.768,4.334,31.182c9.808,7.414,23.768,5.473,31.182-4.335  c5.845-7.731,5.871-18.041,0.817-25.692l32.073-42.43L376.367,247.24z M313.863,293.011c-2.471,3.27-7.125,3.916-10.394,1.446  c-3.27-2.472-3.916-7.126-1.445-10.395c2.471-3.268,7.125-3.916,10.395-1.445C315.687,285.088,316.334,289.743,313.863,293.011z\"/> </svg> ";
// Exports
/* harmony default export */ var va = (va_code);
;// ./templates/flags/ve.svg
// Module
var ve_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"3.75 0 15 15\"> <rect fill=\"#FFCE00\" width=\"22.5\" height=\"5\"/> <rect y=\"5\" fill=\"#203899\" width=\"22.5\" height=\"5\"/> <rect y=\"10\" fill=\"#D82B2B\" width=\"22.5\" height=\"5\"/> <polygon fill=\"#FFFFFF\" points=\"12.13,5.38 12.27,5.84 12.72,5.98 12.33,6.25 12.34,6.73 11.96,6.44 11.51,6.59 11.66,6.15 11.38,5.77\n\t11.85,5.77 \"/> <polygon fill=\"#FFFFFF\" points=\"10.36,5.38 10.63,5.77 11.1,5.77 10.82,6.15 10.97,6.6 10.52,6.44 10.14,6.72 10.15,6.25 9.76,5.97\n\t10.21,5.84 \"/> <polygon fill=\"#FFFFFF\" points=\"13.8,5.94 13.79,6.42 14.18,6.69 13.72,6.83 13.58,7.28 13.31,6.9 12.83,6.9 13.12,6.52 12.97,6.07\n\t13.42,6.23 \"/> <polygon fill=\"#FFFFFF\" points=\"8.69,5.96 9.09,6.23 9.53,6.05 9.4,6.51 9.7,6.87 9.23,6.89 8.97,7.29 8.81,6.85 8.35,6.73 8.72,6.43\n\t\"/> <polygon fill=\"#FFFFFF\" points=\"15.24,7.13 15.04,7.57 15.29,7.97 14.82,7.92 14.51,8.28 14.41,7.82 13.98,7.64 14.39,7.4 14.42,6.93\n\t14.77,7.24 \"/> <polygon fill=\"#FFFFFF\" points=\"15.43,8.25 15.64,8.68 16.11,8.75 15.77,9.08 15.85,9.54 15.43,9.32 15.01,9.54 15.09,9.08 14.75,8.75\n\t15.22,8.68 \"/> <polygon fill=\"#FFFFFF\" points=\"7,8.2 7.21,8.63 7.68,8.69 7.34,9.02 7.42,9.49 7,9.27 6.58,9.49 6.66,9.02 6.32,8.69 6.79,8.63 \"/> <polygon fill=\"#FFFFFF\" points=\"7.31,7.05 7.76,7.19 8.14,6.9 8.14,7.38 8.53,7.64 8.08,7.79 7.95,8.25 7.67,7.87 7.2,7.88 7.47,7.5\n\t\"/> </svg> ";
// Exports
/* harmony default export */ var ve = (ve_code);
;// ./templates/flags/vn.svg
// Module
var vn_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <polygon fill=\"#D80027\" points=\"196.641,85.337 0,85.337 0,426.663 196.641,426.663 512,426.663 512,85.337 \"/> <polygon fill=\"#FFDA44\" points=\"256,157.279 278.663,227.026 352,227.026 292.668,270.132 315.332,339.881 256,296.774   196.668,339.881 219.332,270.132 160,227.026 233.337,227.026 \"/> </svg> ";
// Exports
/* harmony default export */ var vn = (vn_code);
;// ./templates/flags/wf.svg
// Module
var wf_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"9 0 100 100\"> <rect fill=\"#ED2939\" width=\"150\" height=\"100\"/> <rect fill=\"#002395\" width=\"20\" height=\"44.33\"/> <rect x=\"20\" fill=\"#FFFFFF\" width=\"20\" height=\"44.33\"/> <path fill=\"none\" stroke=\"#FFFFFF\" stroke-width=\"3\" d=\"M0,44.33h62.75V0\"/> <path fill=\"#FFFFFF\" d=\"M108.08,43.29L87.96,23.17h40.25L108.08,43.29z M101.38,50L81.25,29.88v40.25L101.38,50z M108.08,56.71\n\tL87.96,76.83h40.25L108.08,56.71z M114.79,50l20.13-20.13v40.25L114.79,50z\"/> </svg> ";
// Exports
/* harmony default export */ var wf = (wf_code);
;// ./templates/flags/eh.svg
// Module
var eh_code = "<?xml version=\"1.0\" encoding=\"utf-8\"?> <svg version=\"1.1\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.35 0 341.3 341.3\"> <rect y=\"0\" fill=\"#FFFFFF\" width=\"512\" height=\"341.3\"/> <rect y=\"0\" width=\"512\" height=\"113.8\"/> <rect y=\"227.6\" fill=\"#428823\" width=\"512\" height=\"113.8\"/> <g> <polygon fill=\"#D80027\" points=\"256,170.7 0,341.3 0,0 \t\"/> <path fill=\"#D80027\" d=\"M309.1,170.7c0-22.9,13.1-42.1,34.6-46.8c-3.3-0.7-6.7-1.1-10.3-1.1c-26.4,0-47.9,21.4-47.9,47.9\n\t\ts21.4,47.9,47.9,47.9c3.5,0,7-0.4,10.3-1.1C322.2,212.7,309.1,193.6,309.1,170.7z\"/> <polygon fill=\"#D80027\" points=\"365,129.2 375.3,160.9 408.6,160.9 381.6,180.5 391.9,212.2 365,192.6 338,212.2 348.3,180.5\n\t\t321.3,160.9 354.7,160.9 \t\"/> </g> </svg> ";
// Exports
/* harmony default export */ var eh = (eh_code);
;// ./templates/flags/ye.svg
// Module
var ye_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"85.333 85.333 341.333 341.333\"> <rect y=\"85.337\" fill=\"#FFFFFF\" width=\"512\" height=\"341.326\"/> <rect y=\"85.337\" fill=\"#D80027\" width=\"512\" height=\"113.775\"/> <rect y=\"312.888\" width=\"512\" height=\"113.775\"/> </svg> ";
// Exports
/* harmony default export */ var ye = (ye_code);
;// ./templates/flags/zm.svg
// Module
var zm_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"170.667 85.333 341.333 341.333\"> <rect y=\"85.331\" fill=\"#496E2D\" width=\"512\" height=\"341.337\"/> <path fill=\"#FF9811\" d=\"M490.668,195.476h-48c0-8.836-7.164-16-16-16s-16,7.164-16,16h-48c0,8.836,7.697,16,16.533,16h-0.533  c0,8.836,7.162,16,16,16c0,8.836,7.162,16,16,16h32c8.836,0,16-7.164,16-16c8.836,0,16-7.164,16-16h-0.533  C482.972,211.476,490.668,204.312,490.668,195.476z\"/> <rect x=\"341.337\" y=\"255.994\" fill=\"#D80027\" width=\"56.888\" height=\"170.663\"/> <rect x=\"455.112\" y=\"255.994\" fill=\"#FF9811\" width=\"56.888\" height=\"170.663\"/> <rect x=\"398.225\" y=\"255.994\" width=\"56.888\" height=\"170.663\"/> </svg> ";
// Exports
/* harmony default export */ var zm = (zm_code);
;// ./templates/flags/zw.svg
// Module
var zw_code = "<?xml version=\"1.0\" encoding=\"UTF-8\"?> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"13.653 85.333 341.333 341.333\"> <rect y=\"85.333\" width=\"512\" height=\"341.337\"/> <g> <rect y=\"134.196\" fill=\"#FFDA44\" width=\"512\" height=\"48.868\"/> <rect y=\"329.668\" fill=\"#FFDA44\" width=\"512\" height=\"48.868\"/> </g> <rect y=\"85.333\" fill=\"#057f44\" width=\"512\" height=\"48.868\"/> <g> <rect y=\"183.069\" fill=\"#D80027\" width=\"512\" height=\"48.868\"/> <rect y=\"280.806\" fill=\"#D80027\" width=\"512\" height=\"48.868\"/> </g> <rect y=\"378.542\" fill=\"#057f44\" width=\"512\" height=\"48.128\"/> <polygon fill=\"#FFFFFF\" points=\"276.992,255.996 106.329,426.659 0,426.659 0,85.333 106.329,85.333 \"/> <polygon points=\"256,255.996 85.334,426.662 106.321,426.662 276.988,255.996 106.321,85.33 85.334,85.33 \"/> <polygon fill=\"#D80027\" points=\"102.465,202.57 115.724,243.382 158.641,243.382 123.923,268.608 137.183,309.422   102.465,284.198 67.745,309.422 81.007,268.608 46.289,243.382 89.204,243.382 \"/> <path fill=\"#FFDA44\" d=\"M138.94,259.335l-34.559-12.243c0,0-2.553-23.955-2.708-24.766  c-1.173-6.18-6.603-10.851-13.123-10.851c-7.376,0-13.357,5.98-13.357,13.357c0,1.223,0.178,2.402,0.486,3.528l-9.689,9.755  c0,0,11.509,0,17.229,0c0,17.882-13.344,17.882-13.344,35.691l7.402,17.809h44.522l7.422-17.809h-0.004  c0.744-1.709,1.194-3.47,1.381-5.231C136.995,265.986,138.94,259.335,138.94,259.335z\"/> </svg> ";
// Exports
/* harmony default export */ var zw = (zw_code);
;// ./modules/extensions/fields/PhoneExtension.js
var PhoneExtension = {
  name: 'PhoneExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      ExtendableObject._phone = '';
      ExtendableObject._subscribers.phone = [];
      ExtendableObject.cb.setPhone = function (phone) {
        return new ExtendableObject.util.Promise(function (resolve, reject) {
          resolve(phone);
        });
      };
      ExtendableObject.cb.phoneChange = function (subscriber) {
        return function (e) {
          ExtendableObject.phone = subscriber.value;
          ExtendableObject._changed = true;
        };
      };
      ExtendableObject.cb.phoneInput = function (subscriber) {
        return function (e) {
          ExtendableObject.phone = subscriber.value;
          ExtendableObject._changed = true;
        };
      };
      ExtendableObject.cb.phoneBlur = function (subscriber) {
        return function (subscriber) {
          ExtendableObject.waitUntilReady().then(function () {
            if (ExtendableObject.onBlurTimeout) {
              clearTimeout(ExtendableObject.onBlurTimeout);
              ExtendableObject.onBlurTimeout = null;
            }
            ExtendableObject.onBlurTimeout = setTimeout(function () {
              var newStatus;
              if (!ExtendableObject.anyActive() && ExtendableObject.util.shouldBeChecked() && !window.EnderecoIntegrator.hasSubmit) {
                // Second. Check Address.
                clearTimeout(ExtendableObject.onBlurTimeout);
                ExtendableObject.onBlurTimeout = null;
                if (ExtendableObject.active && ExtendableObject.hasLoadedExtension('PhoneCheckExtension')) {
                  ExtendableObject._awaits++;
                  ExtendableObject.phoneStatus = [];
                  ExtendableObject.util.checkPhone().then(function (data) {
                    // If format is defined, rewrite field.
                    if (ExtendableObject.config.phoneFormat) {
                      if (data.status.includes('phone_format_needs_correction')) {
                        newStatus = data.status;
                        newStatus.push('phone_correct');
                        newStatus.push('phone_format_' + ExtendableObject.config.phoneFormat.toLowerCase());
                        if (newStatus.indexOf('phone_format_unknown') !== -1) {
                          newStatus.splice(newStatus.indexOf('phone_format_unknown'), 1);
                        }
                        if (newStatus.indexOf('phone_format_needs_correction') !== -1) {
                          newStatus.splice(newStatus.indexOf('phone_format_needs_correction'), 1);
                        }
                        if (newStatus.indexOf('phone_needs_correction') !== -1) {
                          newStatus.splice(newStatus.indexOf('phone_needs_correction'), 1);
                        }
                        ExtendableObject.phone = data.predictions[0].phone;
                        ExtendableObject.phoneStatus = newStatus;
                      } else {
                        ExtendableObject.phoneStatus = data.status;
                      }
                    } else {
                      ExtendableObject.phoneStatus = data.status;
                    }
                  }).catch(function (e) {
                    ExtendableObject.phoneStatus = [];
                    console.log('Failed checking phone', e, ExtendableObject);
                  }).finally(function () {
                    ExtendableObject._awaits--;
                    ExtendableObject._changed = false;
                  });
                }
              }
            }, 300);
          }).catch();
        };
      };

      // Add getter and setter for fields.
      Object.defineProperty(ExtendableObject, 'phone', {
        get: function get() {
          return this._phone;
        },
        set: function set(value) {
          ExtendableObject._awaits++;
          ExtendableObject.util.Promise.resolve(value).then(function (value) {
            ExtendableObject._awaits++;
            ExtendableObject.cb.setPhone(value).then(function (value) {
              var oldValue = ExtendableObject._phone;
              var newValue = value;
              var notSame = oldValue !== newValue;
              if (notSame) {
                ExtendableObject._phone = newValue;
                ExtendableObject._changed = true;

                // Inform all subscribers about the change.
                ExtendableObject._subscribers.phone.forEach(function (subscriber) {
                  subscriber.value = newValue;
                });
                if (ExtendableObject.active) {
                  ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                    detail: {
                      fieldName: 'phone',
                      oldValue: oldValue,
                      newValue: newValue,
                      object: ExtendableObject
                    }
                  }));
                }
              }
            }).catch(function (e) {
              if (ExtendableObject.config.showDebugInfo) {
                console.log('Error resolving phoneFull', e);
              }
            }).finally(function () {
              ExtendableObject._awaits--;
            });
          }).catch().finally(function () {
            ExtendableObject._awaits--;
          });
        }
      });
      ExtendableObject.fieldNames.push('phone');
      if (ExtendableObject.config.showDebugInfo) {
        console.log('PhoneExtension applied');
      }
      resolve($self);
    });
  }
};
/* harmony default export */ var fields_PhoneExtension = (PhoneExtension);
;// ./templates/phone_check_status_wrapper.html
// Module
var phone_check_status_wrapper_code = "<div data-id=\"{{ExtendableObject.id}}\" class=\"endereco-status-wrapper\"> <ul class=\"endereco-status-wrapper-list\"> {{#statuses}} <li class=\"endereco-status-wrapper-list__item endereco-status-wrapper-list__item--{{status}}\">{{text}}</li> {{/statuses}} {{#requiredFormat}} <li class=\"endereco-status-wrapper-list__item endereco-status-wrapper-list__item--phone_format_hint\">{{{requiredFormat}}}</li> {{/requiredFormat}} </ul> </div> ";
// Exports
/* harmony default export */ var phone_check_status_wrapper = (phone_check_status_wrapper_code);
;// ./modules/extensions/checks/PhoneCheckExtension.js

var PhoneCheckExtension = {
  name: 'PhoneCheckExtension',
  extend: function extend(ExtendableObject) {
    var $self = this;
    return new ExtendableObject.util.Promise(function (resolve, reject) {
      ExtendableObject.waitForExtension('PhoneExtension').then(function () {
        // Add email field.
        ExtendableObject._phoneStatus = [];
        ExtendableObject._subscribers.phoneStatus = [];
        ExtendableObject.phoneCheckRequestCounter = 1;
        ExtendableObject.config.templates.statusWrapper = phone_check_status_wrapper;
        if (undefined === ExtendableObject.config.ux.showPhoneStatus) {
          ExtendableObject.config.ux.showPhoneStatus = false;
        }

        // Add change event hadler.
        ExtendableObject.cb.phoneStatusChange = function (subscriber) {
          return function (e) {
            ExtendableObject.phoneStatus = subscriber.value;
          };
        };
        ExtendableObject.util.shouldBeChecked = function () {
          if (!ExtendableObject.phone) {
            return false;
          }
          if (!ExtendableObject._changed) {
            return false;
          }
          return true;
        };

        // Add the "emaiL" property
        Object.defineProperty(ExtendableObject, 'phoneStatus', {
          get: function get() {
            return this._phoneStatus;
          },
          set: function set(value) {
            var oldValue = ExtendableObject._phoneStatus;
            ExtendableObject._awaits++;
            ExtendableObject.util.Promise.resolve(value).then(function (value) {
              var index;
              var newValue = value;
              if (oldValue !== newValue) {
                // Check if the type of phone is fitting in.
                if ('mobile' === ExtendableObject.numberType && !newValue.includes('phone_is_mobile') && newValue.length > 0) {
                  index = newValue.indexOf('phone_correct');
                  if (index !== -1) {
                    newValue.splice(index, 1);
                  }
                  newValue.push('phone_wrong_type');
                  newValue.push('phone_should_be_mobile');
                } else if ('fixedLine' === ExtendableObject.numberType && !newValue.includes('phone_is_fixed_line') && newValue.length > 0) {
                  index = newValue.indexOf('phone_correct');
                  if (index !== -1) {
                    newValue.splice(index, 1);
                  }
                  newValue.push('phone_wrong_type');
                  newValue.push('phone_should_be_fixed');
                }
                ExtendableObject._phoneStatus = newValue;

                // Inform all subscribers about the change.
                ExtendableObject._subscribers.phoneStatus.forEach(function (subscriber) {
                  subscriber.value = value;
                });
                ExtendableObject.fire(new ExtendableObject.util.CustomEvent('change', {
                  detail: {
                    fieldName: 'phoneStatus',
                    oldValue: oldValue,
                    newValue: newValue,
                    object: ExtendableObject
                  }
                }));
                if (ExtendableObject.config.ux.showPhoneErrors) {
                  ExtendableObject.util.renderStatusMessages();
                }
              }
            }).catch().finally(function () {
              ExtendableObject._awaits--;
            });
          }
        });
        ExtendableObject.util.renderStatusMessages = function () {
          var statuses = [];
          var formatMessage = '';
          var uniqueContainer = {};
          if (!Array.isArray(ExtendableObject.phoneStatus)) {
            return;
          }
          ExtendableObject.phoneStatus.forEach(function (phoneStatus) {
            if (!!ExtendableObject.config.texts.statuses[phoneStatus] && !uniqueContainer[phoneStatus]) {
              statuses.push({
                status: phoneStatus,
                text: ExtendableObject.config.texts.statuses[phoneStatus]
              });
              uniqueContainer[phoneStatus] = true;
            }
          });
          statuses = statuses.filter(function (value, index, self) {
            return self.indexOf(value) === index;
          });
          if (document.querySelectorAll('.endereco-status-wrapper[data-id="' + ExtendableObject.id + '"]')) {
            document.querySelectorAll('.endereco-status-wrapper[data-id="' + ExtendableObject.id + '"]').forEach(function (DOMElement) {
              DOMElement.remove();
            });
          }
          if (0 < statuses.length) {
            if (ExtendableObject.config.phoneFormat && ExtendableObject.config.texts.requiredFormat[ExtendableObject.config.phoneFormat] && ExtendableObject.phoneStatus.includes('phone_format_needs_correction')) {
              formatMessage = ExtendableObject.config.texts.requiredFormat[ExtendableObject.config.phoneFormat];
            }
            var wrapperHtml = ExtendableObject.util.Mustache.render(ExtendableObject.config.templates.statusWrapper, {
              'ExtendableObject': ExtendableObject,
              'statuses': statuses,
              'requiredFormat': formatMessage
            });
            ExtendableObject._subscribers.phone.forEach(function (subscriber) {
              subscriber.object.insertAdjacentHTML('afterend', wrapperHtml);
            });
          }
        };
        ExtendableObject.util.checkPhone = function () {
          var phone = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : null;
          var $self = this;
          if (!phone) {
            phone = ExtendableObject.phone;
          }
          ExtendableObject._phoneStatus = [];
          return new ExtendableObject.util.Promise(function (resolve, reject) {
            var message = {
              'jsonrpc': '2.0',
              'id': ExtendableObject.phoneCheckRequestCounter,
              'method': 'phoneCheck',
              'params': {
                'phone': phone
              }
            };
            if (!!ExtendableObject.countryCode) {
              message.params.countryCode = ExtendableObject.countryCode;
            } else {
              var _window$EnderecoInteg;
              message.params.countryCode = (_window$EnderecoInteg = window.EnderecoIntegrator.defaultCountry) !== null && _window$EnderecoInteg !== void 0 ? _window$EnderecoInteg : 'DE';
            }
            if (ExtendableObject.config.phoneFormat) {
              message.params.format = ExtendableObject.config.phoneFormat;
            }

            // Send user data to remote server for validation.
            ExtendableObject._awaits++;
            ExtendableObject.util.axios.post(ExtendableObject.config.apiUrl, message, {
              timeout: 6000,
              headers: {
                'X-Auth-Key': ExtendableObject.config.apiKey,
                'X-Agent': ExtendableObject.config.agentName,
                'X-Remote-Api-Url': ExtendableObject.config.remoteApiUrl,
                'X-Transaction-Referer': window.location.href,
                'X-Transaction-Id': ExtendableObject.hasLoadedExtension('SessionExtension') ? ExtendableObject.sessionId : 'not_required'
              }
            }).then(function (response) {
              if (undefined !== response.data.result) {
                // If session counter is set, increase it.
                if (ExtendableObject.hasLoadedExtension('SessionExtension')) {
                  ExtendableObject.sessionCounter++;
                }
                resolve(response.data.result);
              } else {
                reject(response.data);
              }
            }).catch(function (e) {
              reject(e.response);
            }).finally(function () {
              ExtendableObject.phoneCheckRequestCounter++;
              ExtendableObject._awaits--;
            });
          });
        };
        if (ExtendableObject.config.showDebugInfo) {
          console.log('PhoneCheckExtension applied');
        }
        resolve($self);
      }).catch(function () {
        console.log('Failed to load because of timeout');
        reject($self);
      });
    });
  }
};
/* harmony default export */ var checks_PhoneCheckExtension = (PhoneCheckExtension);
;// ./modules/phoneservices.js






// Flags


















































































































































































































































// Extensions.




function EnderecoPhone() {
  var customConfig = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  // Get base object, that will be extended.
  var base = new components_base();
  base.type = 'phone';
  base.name = 'phoneservices';
  base.numberType = 'general';
  base._flagContainerDom = undefined;
  // Override config.
  base.config = lodash_merge_default()(base.config, customConfig);

  // This source: 
  base.mapping = [{
    "iso2": "DE",
    "code": "+49",
    "flag": de
  }, {
    "iso2": "AT",
    "code": "+43",
    "flag": at
  }, {
    "iso2": "CH",
    "code": "+41",
    "flag": ch
  }, {
    "iso2": "AF",
    "code": "+93",
    "flag": af
  }, {
    "iso2": "AL",
    "code": "+355",
    "flag": al
  }, {
    "iso2": "DZ",
    "code": "+213",
    "flag": dz
  }, {
    "iso2": "AS",
    "code": "+1-684",
    "flag": as
  }, {
    "iso2": "AD",
    "code": "+376",
    "flag": ad
  }, {
    "iso2": "AO",
    "code": "+244",
    "flag": ao
  }, {
    "iso2": "AI",
    "code": "+1-264",
    "flag": ai
  }, {
    "iso2": "AQ",
    "code": "+672",
    "flag": aq
  }, {
    "iso2": "AG",
    "code": "+1-268",
    "flag": ag
  }, {
    "iso2": "AR",
    "code": "+54",
    "flag": ar
  }, {
    "iso2": "AM",
    "code": "+374",
    "flag": am
  }, {
    "iso2": "AW",
    "code": "+297",
    "flag": aw
  }, {
    "iso2": "AU",
    "code": "+61",
    "flag": au
  }, {
    "iso2": "AZ",
    "code": "+994",
    "flag": az
  }, {
    "iso2": "BS",
    "code": "+1-242",
    "flag": bs
  }, {
    "iso2": "BH",
    "code": "+973",
    "flag": bh
  }, {
    "iso2": "BD",
    "code": "+880",
    "flag": bd
  }, {
    "iso2": "BB",
    "code": "+1-246",
    "flag": bb
  }, {
    "iso2": "BY",
    "code": "+375",
    "flag": by
  }, {
    "iso2": "BE",
    "code": "+32",
    "flag": be
  }, {
    "iso2": "BZ",
    "code": "+501",
    "flag": bz
  }, {
    "iso2": "BJ",
    "code": "+229",
    "flag": bj
  }, {
    "iso2": "BM",
    "code": "+1-441",
    "flag": bm
  }, {
    "iso2": "BT",
    "code": "+975",
    "flag": bt
  }, {
    "iso2": "BO",
    "code": "+591",
    "flag": bo
  }, {
    "iso2": "BA",
    "code": "+387",
    "flag": ba
  }, {
    "iso2": "BW",
    "code": "+267",
    "flag": bw
  }, {
    "iso2": "BR",
    "code": "+55",
    "flag": br
  }, {
    "iso2": "IO",
    "code": "+246",
    "flag": io
  }, {
    "iso2": "VG",
    "code": "+1-284",
    "flag": vg
  }, {
    "iso2": "BN",
    "code": "+673",
    "flag": bn
  }, {
    "iso2": "BG",
    "code": "+359",
    "flag": bg
  }, {
    "iso2": "BF",
    "code": "+226",
    "flag": bf
  }, {
    "iso2": "BI",
    "code": "+257",
    "flag": bi
  }, {
    "iso2": "KH",
    "code": "+855",
    "flag": kh
  }, {
    "iso2": "CM",
    "code": "+237",
    "flag": cm
  }, {
    "iso2": "CA",
    "code": "+1",
    "flag": ca
  }, {
    "iso2": "CV",
    "code": "+238",
    "flag": cv
  }, {
    "iso2": "KY",
    "code": "+1-345",
    "flag": ky
  }, {
    "iso2": "CF",
    "code": "+236",
    "flag": cf
  }, {
    "iso2": "TD",
    "code": "+235",
    "flag": td
  }, {
    "iso2": "CL",
    "code": "+56",
    "flag": cl
  }, {
    "iso2": "CN",
    "code": "+86",
    "flag": cn
  }, {
    "iso2": "CX",
    "code": "+61",
    "flag": cx
  }, {
    "iso2": "CC",
    "code": "+61",
    "flag": cc
  }, {
    "iso2": "CO",
    "code": "+57",
    "flag": co
  }, {
    "iso2": "KM",
    "code": "+269",
    "flag": km
  }, {
    "iso2": "CK",
    "code": "+682",
    "flag": ck
  }, {
    "iso2": "CR",
    "code": "+506",
    "flag": cr
  }, {
    "iso2": "HR",
    "code": "+385",
    "flag": hr
  }, {
    "iso2": "CU",
    "code": "+53",
    "flag": cu
  }, {
    "iso2": "CW",
    "code": "+599",
    "flag": cw
  }, {
    "iso2": "CY",
    "code": "+357",
    "flag": cy
  }, {
    "iso2": "CZ",
    "code": "+420",
    "flag": cz
  }, {
    "iso2": "CD",
    "code": "+243",
    "flag": cd
  }, {
    "iso2": "DK",
    "code": "+45",
    "flag": dk
  }, {
    "iso2": "DJ",
    "code": "+253",
    "flag": dj
  }, {
    "iso2": "DM",
    "code": "+1-767",
    "flag": dm
  }, {
    "iso2": "DO",
    "code": "+1-809",
    "flag": flags_do
  }, {
    "iso2": "DO",
    "code": "+ 1-829",
    "flag": flags_do
  }, {
    "iso2": "DO",
    "code": "+ 1-849",
    "flag": flags_do
  }, {
    "iso2": "TL",
    "code": "+670",
    "flag": tl
  }, {
    "iso2": "EC",
    "code": "+593",
    "flag": ec
  }, {
    "iso2": "EG",
    "code": "+20",
    "flag": eg
  }, {
    "iso2": "SV",
    "code": "+503",
    "flag": sv
  }, {
    "iso2": "GQ",
    "code": "+240",
    "flag": gq
  }, {
    "iso2": "ER",
    "code": "+291",
    "flag": er
  }, {
    "iso2": "EE",
    "code": "+372",
    "flag": ee
  }, {
    "iso2": "ET",
    "code": "+251",
    "flag": et
  }, {
    "iso2": "FK",
    "code": "+500",
    "flag": fk
  }, {
    "iso2": "FO",
    "code": "+298",
    "flag": fo
  }, {
    "iso2": "FJ",
    "code": "+679",
    "flag": fj
  }, {
    "iso2": "FI",
    "code": "+358",
    "flag": fi
  }, {
    "iso2": "FR",
    "code": "+33",
    "flag": flags_fr
  }, {
    "iso2": "PF",
    "code": "+689",
    "flag": pf
  }, {
    "iso2": "GA",
    "code": "+241",
    "flag": ga
  }, {
    "iso2": "GM",
    "code": "+220",
    "flag": gm
  }, {
    "iso2": "GE",
    "code": "+995",
    "flag": ge
  }, {
    "iso2": "GH",
    "code": "+233",
    "flag": gh
  }, {
    "iso2": "GI",
    "code": "+350",
    "flag": gi
  }, {
    "iso2": "GR",
    "code": "+30",
    "flag": gr
  }, {
    "iso2": "GL",
    "code": "+299",
    "flag": gl
  }, {
    "iso2": "GD",
    "code": "+1-473",
    "flag": gd
  }, {
    "iso2": "GU",
    "code": "+1-671",
    "flag": gu
  }, {
    "iso2": "GT",
    "code": "+502",
    "flag": gt
  }, {
    "iso2": "GG",
    "code": "+44-1481",
    "flag": gg
  }, {
    "iso2": "GN",
    "code": "+224",
    "flag": gn
  }, {
    "iso2": "GW",
    "code": "+245",
    "flag": gw
  }, {
    "iso2": "GY",
    "code": "+592",
    "flag": gy
  }, {
    "iso2": "HT",
    "code": "+509",
    "flag": ht
  }, {
    "iso2": "HN",
    "code": "+504",
    "flag": hn
  }, {
    "iso2": "HK",
    "code": "+852",
    "flag": hk
  }, {
    "iso2": "HU",
    "code": "+36",
    "flag": hu
  }, {
    "iso2": "IS",
    "code": "+354",
    "flag": is
  }, {
    "iso2": "IN",
    "code": "+91",
    "flag": flags_in
  }, {
    "iso2": "ID",
    "code": "+62",
    "flag": id
  }, {
    "iso2": "IR",
    "code": "+98",
    "flag": ir
  }, {
    "iso2": "IQ",
    "code": "+964",
    "flag": iq
  }, {
    "iso2": "IE",
    "code": "+353",
    "flag": ie
  }, {
    "iso2": "IM",
    "code": "+44-1624",
    "flag": im
  }, {
    "iso2": "IL",
    "code": "+972",
    "flag": il
  }, {
    "iso2": "IT",
    "code": "+39",
    "flag": it
  }, {
    "iso2": "CI",
    "code": "+225",
    "flag": ci
  }, {
    "iso2": "JM",
    "code": "+1-876",
    "flag": jm
  }, {
    "iso2": "JP",
    "code": "+81",
    "flag": jp
  }, {
    "iso2": "JE",
    "code": "+44-1534",
    "flag": je
  }, {
    "iso2": "JO",
    "code": "+962",
    "flag": jo
  }, {
    "iso2": "KZ",
    "code": "+7",
    "flag": kz
  }, {
    "iso2": "KE",
    "code": "+254",
    "flag": ke
  }, {
    "iso2": "KI",
    "code": "+686",
    "flag": ki
  }, {
    "iso2": "XK",
    "code": "+383",
    "flag": xk
  }, {
    "iso2": "KW",
    "code": "+965",
    "flag": kw
  }, {
    "iso2": "KG",
    "code": "+996",
    "flag": kg
  }, {
    "iso2": "LA",
    "code": "+856",
    "flag": la
  }, {
    "iso2": "LV",
    "code": "+371",
    "flag": lv
  }, {
    "iso2": "LB",
    "code": "+961",
    "flag": lb
  }, {
    "iso2": "LS",
    "code": "+266",
    "flag": ls
  }, {
    "iso2": "LR",
    "code": "+231",
    "flag": lr
  }, {
    "iso2": "LY",
    "code": "+218",
    "flag": ly
  }, {
    "iso2": "LI",
    "code": "+423",
    "flag": li
  }, {
    "iso2": "LT",
    "code": "+370",
    "flag": lt
  }, {
    "iso2": "LU",
    "code": "+352",
    "flag": lu
  }, {
    "iso2": "MO",
    "code": "+853",
    "flag": mo
  }, {
    "iso2": "MK",
    "code": "+389",
    "flag": mk
  }, {
    "iso2": "MG",
    "code": "+261",
    "flag": mg
  }, {
    "iso2": "MW",
    "code": "+265",
    "flag": mw
  }, {
    "iso2": "MY",
    "code": "+60",
    "flag": my
  }, {
    "iso2": "MV",
    "code": "+960",
    "flag": mv
  }, {
    "iso2": "ML",
    "code": "+223",
    "flag": ml
  }, {
    "iso2": "MT",
    "code": "+356",
    "flag": mt
  }, {
    "iso2": "MH",
    "code": "+692",
    "flag": mh
  }, {
    "iso2": "MR",
    "code": "+222",
    "flag": mr
  }, {
    "iso2": "MU",
    "code": "+230",
    "flag": mu
  }, {
    "iso2": "YT",
    "code": "+262",
    "flag": yt
  }, {
    "iso2": "MX",
    "code": "+52",
    "flag": mx
  }, {
    "iso2": "FM",
    "code": "+691",
    "flag": fm
  }, {
    "iso2": "MD",
    "code": "+373",
    "flag": md
  }, {
    "iso2": "MC",
    "code": "+377",
    "flag": mc
  }, {
    "iso2": "MN",
    "code": "+976",
    "flag": mn
  }, {
    "iso2": "ME",
    "code": "+382",
    "flag": me
  }, {
    "iso2": "MS",
    "code": "+1-664",
    "flag": ms
  }, {
    "iso2": "MA",
    "code": "+212",
    "flag": ma
  }, {
    "iso2": "MZ",
    "code": "+258",
    "flag": mz
  }, {
    "iso2": "MM",
    "code": "+95",
    "flag": mm
  }, {
    "iso2": "NA",
    "code": "+264",
    "flag": na
  }, {
    "iso2": "NR",
    "code": "+674",
    "flag": nr
  }, {
    "iso2": "NP",
    "code": "+977",
    "flag": np
  }, {
    "iso2": "NL",
    "code": "+31",
    "flag": nl
  }, {
    "iso2": "AN",
    "code": "+599",
    "flag": nl
  }, {
    "iso2": "NC",
    "code": "+687",
    "flag": nc
  }, {
    "iso2": "NZ",
    "code": "+64",
    "flag": nz
  }, {
    "iso2": "NI",
    "code": "+505",
    "flag": ni
  }, {
    "iso2": "NE",
    "code": "+227",
    "flag": ne
  }, {
    "iso2": "NG",
    "code": "+234",
    "flag": ng
  }, {
    "iso2": "NU",
    "code": "+683",
    "flag": nu
  }, {
    "iso2": "KP",
    "code": "+850",
    "flag": kp
  }, {
    "iso2": "MP",
    "code": "+1-670",
    "flag": mp
  }, {
    "iso2": "NO",
    "code": "+47",
    "flag": no
  }, {
    "iso2": "OM",
    "code": "+968",
    "flag": om
  }, {
    "iso2": "PK",
    "code": "+92",
    "flag": pk
  }, {
    "iso2": "PW",
    "code": "+680",
    "flag": pw
  }, {
    "iso2": "PS",
    "code": "+970",
    "flag": ps
  }, {
    "iso2": "PA",
    "code": "+507",
    "flag": pa
  }, {
    "iso2": "PG",
    "code": "+675",
    "flag": pg
  }, {
    "iso2": "PY",
    "code": "+595",
    "flag": py
  }, {
    "iso2": "PE",
    "code": "+51",
    "flag": pe
  }, {
    "iso2": "PH",
    "code": "+63",
    "flag": ph
  }, {
    "iso2": "PN",
    "code": "+64",
    "flag": pn
  }, {
    "iso2": "PL",
    "code": "+48",
    "flag": pl
  }, {
    "iso2": "PT",
    "code": "+351",
    "flag": pt
  }, {
    "iso2": "PR",
    "code": "+1-787",
    "flag": pr
  }, {
    "iso2": "PR",
    "code": "+ 1-939",
    "flag": pr
  }, {
    "iso2": "QA",
    "code": "+974",
    "flag": qa
  }, {
    "iso2": "CG",
    "code": "+242",
    "flag": cg
  }, {
    "iso2": "RE",
    "code": "+262",
    "flag": re
  }, {
    "iso2": "RO",
    "code": "+40",
    "flag": ro
  }, {
    "iso2": "RU",
    "code": "+7",
    "flag": ru
  }, {
    "iso2": "RW",
    "code": "+250",
    "flag": rw
  }, {
    "iso2": "BL",
    "code": "+590",
    "flag": bl
  }, {
    "iso2": "SH",
    "code": "+290",
    "flag": sh
  }, {
    "iso2": "KN",
    "code": "+1-869",
    "flag": kn
  }, {
    "iso2": "LC",
    "code": "+1-758",
    "flag": lc
  }, {
    "iso2": "MF",
    "code": "+590",
    "flag": mf
  }, {
    "iso2": "PM",
    "code": "+508",
    "flag": pm
  }, {
    "iso2": "VC",
    "code": "+1-784",
    "flag": vc
  }, {
    "iso2": "WS",
    "code": "+685",
    "flag": ws
  }, {
    "iso2": "SM",
    "code": "+378",
    "flag": sm
  }, {
    "iso2": "ST",
    "code": "+239",
    "flag": st
  }, {
    "iso2": "SA",
    "code": "+966",
    "flag": sa
  }, {
    "iso2": "SN",
    "code": "+221",
    "flag": sn
  }, {
    "iso2": "RS",
    "code": "+381",
    "flag": rs
  }, {
    "iso2": "SC",
    "code": "+248",
    "flag": sc
  }, {
    "iso2": "SL",
    "code": "+232",
    "flag": sl
  }, {
    "iso2": "SG",
    "code": "+65",
    "flag": sg
  }, {
    "iso2": "SX",
    "code": "+1-721",
    "flag": sx
  }, {
    "iso2": "SK",
    "code": "+421",
    "flag": sk
  }, {
    "iso2": "SI",
    "code": "+386",
    "flag": si
  }, {
    "iso2": "SB",
    "code": "+677",
    "flag": sb
  }, {
    "iso2": "SO",
    "code": "+252",
    "flag": so
  }, {
    "iso2": "ZA",
    "code": "+27",
    "flag": za
  }, {
    "iso2": "KR",
    "code": "+82",
    "flag": kr
  }, {
    "iso2": "SS",
    "code": "+211",
    "flag": ss
  }, {
    "iso2": "ES",
    "code": "+34",
    "flag": es
  }, {
    "iso2": "LK",
    "code": "+94",
    "flag": lk
  }, {
    "iso2": "SD",
    "code": "+249",
    "flag": sd
  }, {
    "iso2": "SR",
    "code": "+597",
    "flag": sr
  }, {
    "iso2": "SJ",
    "code": "+47",
    "flag": sj
  }, {
    "iso2": "SZ",
    "code": "+268",
    "flag": sz
  }, {
    "iso2": "SE",
    "code": "+46",
    "flag": se
  }, {
    "iso2": "SY",
    "code": "+963",
    "flag": sy
  }, {
    "iso2": "TW",
    "code": "+886",
    "flag": tw
  }, {
    "iso2": "TJ",
    "code": "+992",
    "flag": tj
  }, {
    "iso2": "TZ",
    "code": "+255",
    "flag": tz
  }, {
    "iso2": "TH",
    "code": "+66",
    "flag": th
  }, {
    "iso2": "TG",
    "code": "+228",
    "flag": tg
  }, {
    "iso2": "TK",
    "code": "+690",
    "flag": tk
  }, {
    "iso2": "TO",
    "code": "+676",
    "flag": to
  }, {
    "iso2": "TT",
    "code": "+1-868",
    "flag": tt
  }, {
    "iso2": "TN",
    "code": "+216",
    "flag": tn
  }, {
    "iso2": "TR",
    "code": "+90",
    "flag": tr
  }, {
    "iso2": "TM",
    "code": "+993",
    "flag": tm
  }, {
    "iso2": "TC",
    "code": "+1-649",
    "flag": tc
  }, {
    "iso2": "TV",
    "code": "+688",
    "flag": tv
  }, {
    "iso2": "VI",
    "code": "+1-340",
    "flag": vi
  }, {
    "iso2": "UG",
    "code": "+256",
    "flag": ug
  }, {
    "iso2": "UA",
    "code": "+380",
    "flag": ua
  }, {
    "iso2": "AE",
    "code": "+971",
    "flag": ae
  }, {
    "iso2": "GB",
    "code": "+44",
    "flag": gb
  }, {
    "iso2": "US",
    "code": "+1",
    "flag": us
  }, {
    "iso2": "UY",
    "code": "+598",
    "flag": uy
  }, {
    "iso2": "UZ",
    "code": "+998",
    "flag": uz
  }, {
    "iso2": "VU",
    "code": "+678",
    "flag": vu
  }, {
    "iso2": "VA",
    "code": "+379",
    "flag": va
  }, {
    "iso2": "VE",
    "code": "+58",
    "flag": ve
  }, {
    "iso2": "VN",
    "code": "+84",
    "flag": vn
  }, {
    "iso2": "WF",
    "code": "+681",
    "flag": wf
  }, {
    "iso2": "EH",
    "code": "+212",
    "flag": eh
  }, {
    "iso2": "YE",
    "code": "+967",
    "flag": ye
  }, {
    "iso2": "ZM",
    "code": "+260",
    "flag": zm
  }, {
    "iso2": "ZW",
    "code": "+263",
    "flag": zw
  }];

  // Add extensions.
  base.extensions = [fields_PhoneExtension, checks_PhoneCheckExtension, fields_CountryCodeExtension, session_SessionExtension];

  // Load extesions.
  base.loadExtensions();

  // Call "onCreate" callbacks.
  base.created();
  base.getFlagHTML = function (numberDigits) {
    var queryStr = numberDigits.trim();
    var returnHtml = '';
    if ('00' === queryStr.substring(0, 2)) {
      queryStr = queryStr.replace(/^.{2}/g, '+');
    }
    while (1) {
      base.mapping.forEach(function (country) {
        if (country.code === queryStr && '' !== queryStr) {
          returnHtml = country.flag;
          base.currePrefix = queryStr;
        }
      });
      queryStr = queryStr.slice(0, -1);
      if ('' !== returnHtml) {
        break;
      }
      if (0 === queryStr.length) {
        break;
      }
    }
    if ('' === returnHtml) {
      returnHtml = not_found;
      base.currePrefix = '';
    }
    return returnHtml;
  };
  base.currePrefix = false;
  base.setPrefix = function (input, prefix) {
    if (base.currePrefix !== prefix) {
      var inputValue = input.value;
      if ("" !== base.currePrefix) {
        inputValue = inputValue.substring(base.currePrefix.length);
      }
      input.value = prefix + inputValue;
      if (!!window.jQuery && !!window.jQuery.trigger) {
        window.jQuery(input).trigger('change').trigger('blur');
      } else if (!!window.$ && !!window.$.trigger) {
        window.$(input).trigger('change').trigger('blue');
      } else {
        input.dispatchEvent(new Event('change'));
        input.dispatchEvent(new Event('blur'));
      }
    }
  };

  // Render flags method.
  base.renderFlags = function () {
    if (!!window.EnderecoIntegrator.config.ux.showPhoneFlag && ['E164', 'INTERNATIONAL'].includes(window.EnderecoIntegrator.config.phoneFormat)) {
      base._subscribers.phone.forEach(function (subscriber) {
        var DOMElement = subscriber.object;
        var flagsHTML = flagButton;
        var dropdownHTML = mustache_mustache.render(phoneDropdownHTML, {
          "countries": base.mapping
        });
        var heightOfInput = 0;
        var heightOfFlag = 0;
        var offsetFromParent = 0;
        var flagElement, dropdownElement;
        var topOffset = 0;
        var dropdownTopOffset = 0;
        var leftOffset = 0;
        var dividerSize = 0;
        var widthOfFlag = 0;
        var lastHeight = 0;
        if (!DOMElement.classList.contains('endereco-field-has-flags')) {
          // Add flags class
          DOMElement.classList.add('endereco-field-has-flags');

          // Add relative
          DOMElement.parentElement.classList.add('endereco-relative-container');

          // Add flags container.
          DOMElement.insertAdjacentHTML('beforebegin', flagsHTML);
          flagElement = DOMElement.parentElement.querySelector('.endereco-big-flag');
          base._flagContainerDom = flagElement;
          offsetFromParent = DOMElement.offsetTop;
          heightOfInput = DOMElement.offsetHeight;
          lastHeight = heightOfInput;
          dividerSize = 0.80;
          if (heightOfInput > 30) {
            dividerSize = 0.5;
          }
          heightOfFlag = heightOfInput * dividerSize;
          topOffset = offsetFromParent + (heightOfInput - heightOfFlag) / 2;
          leftOffset = (heightOfInput - heightOfFlag) / 2;
          dropdownTopOffset = topOffset + heightOfInput;
          flagElement.style.top = "".concat(topOffset, "px");
          flagElement.style.left = "".concat(leftOffset, "px");
          flagElement.querySelector('.endereco-flag').style.height = "".concat(heightOfFlag, "px");
          flagElement.querySelector('.endereco-flag').style.width = "".concat(heightOfFlag, "px");
          widthOfFlag = flagElement.offsetWidth + (heightOfInput - heightOfFlag) / 2;
          DOMElement.style.paddingLeft = "".concat(widthOfFlag, "px");

          // Add input listener.
          var $oldValue = DOMElement.value;
          setInterval(function () {
            var $newValue = DOMElement.value;
            if ($oldValue !== $newValue) {
              $oldValue = $newValue;
              flagElement.querySelector('.endereco-flag').innerHTML = base.getFlagHTML(DOMElement.value.substring(0, 10));
            }
          }, 1);

          // Set default value.
          flagElement.querySelector('.endereco-flag').innerHTML = base.getFlagHTML(DOMElement.value.substring(0, 10));

          // Add autocomplete für diverse codes.
          DOMElement.insertAdjacentHTML('afterend', dropdownHTML);
          dropdownElement = DOMElement.parentElement.querySelector('.endereco-flag-dropdown');
          dropdownElement.style.top = "".concat(dropdownTopOffset, "px");
          document.querySelector('body').addEventListener('click', function (e) {
            if (!dropdownElement.contains(e.target)) {
              dropdownElement.classList.add("endereco-hidden");
            }
          });

          // Add click listener to big flag.
          flagElement.addEventListener('click', function (e) {
            e.preventDefault();
            e.stopPropagation();
            var dropdownDOM = this.parentElement.querySelector('.endereco-flag-dropdown');
            dropdownDOM.classList.toggle("endereco-hidden");
            return false;
          });

          // Add click listener to dropdown elements.
          dropdownElement.querySelectorAll('.endereco-flag-dropdown-element').forEach(function (element) {
            element.addEventListener('click', function (e) {
              base.setPrefix(DOMElement, this.getAttribute("data-code"));
              flagElement.querySelector('.endereco-flag').innerHTML = base.getFlagHTML(DOMElement.value.substring(0, 10));
              dropdownElement.classList.add("endereco-hidden");
            });
          });

          // Add height listener.
          setInterval(function () {
            offsetFromParent = DOMElement.offsetTop;
            heightOfInput = DOMElement.offsetHeight;
            if (lastHeight !== heightOfInput) {
              lastHeight = heightOfInput;
              dividerSize = 0.80;
              if (heightOfInput > 30) {
                dividerSize = 0.5;
              }
              heightOfFlag = heightOfInput * dividerSize;
              topOffset = offsetFromParent + (heightOfInput - heightOfFlag) / 2;
              leftOffset = (heightOfInput - heightOfFlag) / 2;
              dropdownTopOffset = topOffset + heightOfInput;
              flagElement.style.top = "".concat(topOffset, "px");
              flagElement.style.left = "".concat(leftOffset, "px");
              flagElement.querySelector('.endereco-flag').style.height = "".concat(heightOfFlag, "px");
              flagElement.querySelector('.endereco-flag').style.width = "".concat(heightOfFlag, "px");
              widthOfFlag = flagElement.offsetWidth + (heightOfInput - heightOfFlag) / 2;
              DOMElement.style.paddingLeft = "".concat(widthOfFlag, "px");
              dropdownElement.style.top = "".concat(dropdownTopOffset, "px");
            }
          }, 1);
        }
      });
    }
  };
  return base;
}
/* harmony default export */ var phoneservices = (EnderecoPhone);
// EXTERNAL MODULE: ./node_modules/core-js/fn/promise/finally.js
var promise_finally = __webpack_require__(8978);
;// ./modules/integrator.js
function integrator_typeof(o) { "@babel/helpers - typeof"; return integrator_typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, integrator_typeof(o); }







var EnderecoIntegrator = {
  amsFilters: {
    isAddressMetaStillRelevant: []
  },
  popupQueue: 0,
  enderecoPopupQueue: 0,
  ready: false,
  loaded: true,
  themeName: undefined,
  countryMappingUrl: '',
  defaultCountry: 'de',
  defaultCountrySelect: false,
  billingAutocheck: false,
  shippingAutocheck: false,
  editingIntent: false,
  thirdPartyModals: 0,
  subdivisionMapping: {},
  subdivisionMappingReverse: {},
  countryMapping: {},
  countryMappingReverse: {},
  constructors: {
    "EnderecoAddressObject": ams,
    "EnderecoSubscriber": modules_subscriber,
    "EnderecoEmailObject": emailservices,
    "EnderecoPersonObject": personservices,
    "EnderecoPhoneObject": phoneservices
  },
  globalSpace: {
    reloadPage: function reloadPage() {
      location.reload();
    }
  },
  config: {
    apiUrl: '',
    remoteApiUrl: '',
    apiKey: '',
    lang: function () {
      if (document.querySelector('html').lang) {
        // TODO: check if the language is in the list of possible languages and return "de" if not.
        return document.documentElement.lang.slice(0, 2);
      } else {
        return 'de';
      }
    }(),
    showDebugInfo: true,
    splitStreet: true,
    ux: {
      smartFill: true,
      smartFillBlockTime: 600,
      resumeSubmit: true,
      disableBrowserAutocomplete: true,
      maxAutocompletePredictionItems: 100,
      maxAddressPredictionItems: 3,
      useStandardCss: true,
      cssFilePath: '',
      confirmWithCheckbox: false,
      correctTranspositionedNames: false,
      delay: {
        inputAssistant: 100,
        streetCopy: 600
      },
      requestTimeout: 8000
    },
    trigger: {
      onblur: true,
      onsubmit: true
    },
    texts: {
      popUpHeadline: 'Adresse pr&uuml;fen',
      popUpSubline: 'Die eingegebene Adresse scheint nicht korrekt oder unvollständig zu sein. Bitte eine korrekte Adresse wählen.',
      yourInput: 'Ihre Eingabe:',
      editYourInput: '(bearbeiten)',
      ourSuggestions: 'Unsere Vorschläge:',
      useSelected: 'Auswahl übernehmen',
      popupHeadlines: {
        general_address: 'Adresse pr&uuml;fen',
        billing_address: 'Rechnungsadresse pr&uuml;fen',
        shipping_address: 'Lieferadresse pr&uuml;fen'
      }
    },
    templates: {}
  },
  postfix: {
    ams: {
      countryCode: '',
      postalCode: '',
      locality: '',
      streetFull: '',
      streetName: '',
      buildingNumber: '',
      addressStatus: '',
      addressTimestamp: '',
      addressPredictions: '',
      additionalInfo: ''
    },
    emailServices: {
      email: ''
    },
    personServices: {
      salutation: '',
      firstName: '',
      lastName: '',
      nameScore: ''
    }
  },
  activeServices: {
    ams: true,
    emailService: true,
    personService: true
  },
  checkAllCallback: function checkAllCallback() {
    return;
  },
  mappings: {
    gender: {
      'M': 'mr',
      'F': 'ms',
      getByCode: function getByCode(code) {
        if (this[code]) {
          return this[code];
        } else {
          return '';
        }
      },
      getBySalutation: function getBySalutation(salutation) {
        var $return = '';
        for (var prop in this) {
          if (Object.prototype.hasOwnProperty.call(this, prop)) {
            if (this[prop] === salutation) {
              return prop;
            }
          }
        }
        return $return;
      }
    }
  },
  resolvers: {
    addressPredictionsWrite: function addressPredictionsWrite(value, subscriber) {
      return new Promise(function (resolve, reject) {
        if (!value) {
          resolve(value);
          return;
        }
        try {
          resolve(JSON.stringify(value));
        } catch (e) {
          console.log('Error stringify json', e);
          reject(e);
        }
      });
    },
    addressPredictionsRead: function addressPredictionsRead(value, subscriber) {
      return new Promise(function (resolve, reject) {
        if (!value) {
          resolve(value);
          return;
        }
        try {
          resolve(JSON.parse(value));
        } catch (e) {
          console.log('Error parse json', e);
          reject(e);
        }
      });
    }
  },
  initPhoneServices: function initPhoneServices(prefix) {
    var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {
      postfixCollection: {},
      name: 'default'
    };
    $self = this;
    if (!this.activeServices.phs) {
      return;
    }
    var $self = this;
    var config = JSON.parse(JSON.stringify(this.config));
    if (!!options.config) {
      config = lodash_merge_default()(config, options.config);
    }
    var originalPostfix = lodash_merge_default()({}, $self.postfix.phs);
    var postfix;
    if ('object' === integrator_typeof(prefix)) {
      postfix = lodash_merge_default()(originalPostfix, prefix);
      prefix = '';
    } else {
      var newObject = {};
      Object.keys(originalPostfix).forEach(function (key) {
        newObject[key] = prefix + originalPostfix[key];
      });
      postfix = lodash_merge_default()(newObject, options.postfixCollection);
    }
    var EPHSO = new phoneservices(config);
    EPHSO.fullName = options.name + '_' + EPHSO.name;

    // Start setting default values.
    if (!!options.numberType) {
      EPHSO.numberType = options.numberType;
    }
    EPHSO.waitForAllExtension().then(function () {
      // Add subscribers.
      if ($self.dispatchEvent('endereco.ams.before-adding-subscribers')) {
        // In general with every subscriber we first check, if the html element exists
        // Then we trigger an event.
        if (document.querySelector($self.getSelector(postfix.phone)) && $self.dispatchEvent('endereco.ams.before-adding-phone-subscriber')) {
          var phoneSubscriberOptions = {};
          if (!!$self.resolvers.phoneWrite) {
            phoneSubscriberOptions['writeFilterCb'] = function (value, subscriber) {
              return $self.resolvers.phoneWrite(value, subscriber);
            };
          }
          if (!!$self.resolvers.phoneRead) {
            phoneSubscriberOptions['readFilterCb'] = function (value) {
              return $self.resolvers.phoneRead(value, subscriber);
            };
          }
          if (!!$self.resolvers.phoneSetValue) {
            phoneSubscriberOptions['customSetValue'] = function (subscriber, value) {
              return $self.resolvers.phoneSetValue(subscriber, value);
            };
          }
          var phoneSubscriber = new modules_subscriber('phone', document.querySelector($self.getSelector(postfix.phone)), phoneSubscriberOptions);
          EPHSO.addSubscriber(phoneSubscriber);
          $self.dispatchEvent('endereco.ams.after-adding-phone-subscriber'); // Add after hook.
        }

        // In general with every subscriber we first check, if the html element exists
        // Then we trigger an event.
        if (!!postfix.countryCode && document.querySelector($self.getSelector(postfix.countryCode)) && $self.dispatchEvent('endereco.ams.before-adding-country-code-subscriber')) {
          var countryCodeSubscriberOptions = {};
          if (!!$self.resolvers.countryCodeWrite) {
            countryCodeSubscriberOptions['writeFilterCb'] = function (value, subscriber) {
              return $self.resolvers.countryCodeWrite(value, subscriber);
            };
          }
          if (!!$self.resolvers.countryCodeRead) {
            countryCodeSubscriberOptions['readFilterCb'] = function (value, subscriber) {
              return $self.resolvers.countryCodeRead(value, subscriber);
            };
          }
          if (!!$self.resolvers.countryCodeSetValue) {
            countryCodeSubscriberOptions['customSetValue'] = function (subscriber, value) {
              return $self.resolvers.countryCodeSetValue(subscriber, value);
            };
          }
          var countryCodeSubscriber = new modules_subscriber('countryCode', document.querySelector($self.getSelector(postfix.countryCode)), countryCodeSubscriberOptions);
          EPHSO.addSubscriber(countryCodeSubscriber);
          $self.dispatchEvent('endereco.ams.after-adding-country-code-subscriber'); // Add after hook.
        }
        $self.dispatchEvent('endereco.ams.after-adding-subscribers');
        EPHSO.waitUntilReady().then(function () {
          EPHSO.syncValues().then(function () {
            EPHSO.waitUntilReady().then(function () {
              // Start setting default values.
              if (!!options.numberType) {
                EPHSO.numberType = options.numberType;
              } else if (!!window.EnderecoIntegrator.config.defaultPhoneType) {
                EPHSO.numberType = window.EnderecoIntegrator.config.defaultPhoneType;
              }
              EPHSO.renderFlags();
              EPHSO._changed = false;
              EPHSO.activate();
              if (!!$self.afterPHSActivation) {
                $self.afterPHSActivation.forEach(function (callback) {
                  callback(EPHSO);
                });
              }
            }).catch();
          }).catch();
        }).catch();
      }
    }).catch();
    this.integratedObjects[EPHSO.fullName] = EPHSO;
    return EPHSO;
  },
  initAMS: function initAMS(prefix) {
    var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {
      postfixCollection: {},
      addressType: 'general_address',
      name: 'default',
      beforeActivation: undefined
    };
    $self = this;
    if (!this.activeServices.ams) {
      return;
    }
    var $self = this;
    var config = JSON.parse(JSON.stringify(this.config));
    if (!!options.config) {
      config = lodash_merge_default()(config, options.config);
    }
    var originalPostfix = lodash_merge_default()({}, $self.postfix.ams);
    var postfix;
    if ('object' === integrator_typeof(prefix)) {
      postfix = lodash_merge_default()(originalPostfix, prefix);
      prefix = '';
    } else {
      var newObject = {};
      Object.keys(originalPostfix).forEach(function (key) {
        newObject[key] = prefix + originalPostfix[key];
      });
      postfix = lodash_merge_default()(newObject, options.postfixCollection);
    }
    var EAO = new ams(config);
    EAO.fullName = options.name + '_' + EAO.name;

    // If change order.
    if (EAO.config.ux.changeFieldsOrder) {
      $self.changeFieldsOrder(postfix);
    }
    EAO.waitForAllExtension().then(function () {
      // Add subscribers.
      if ($self.dispatchEvent('endereco.ams.before-adding-subscribers')) {
        // In general with every subscriber we first check, if the html element exists
        // Then we trigger an event.
        if (document.querySelector($self.getSelector(postfix.countryCode)) && $self.dispatchEvent('endereco.ams.before-adding-country-code-subscriber')) {
          var countryCodeSubscriberOptions = {};
          if (!!$self.resolvers.countryCodeWrite) {
            countryCodeSubscriberOptions['writeFilterCb'] = function (value, subscriber) {
              return $self.resolvers.countryCodeWrite(value, subscriber);
            };
          }
          if (!!$self.resolvers.countryCodeRead) {
            countryCodeSubscriberOptions['readFilterCb'] = function (value, subscriber) {
              return $self.resolvers.countryCodeRead(value, subscriber);
            };
          }
          if (!!$self.resolvers.countryCodeSetValue) {
            countryCodeSubscriberOptions['customSetValue'] = function (subscriber, value) {
              return $self.resolvers.countryCodeSetValue(subscriber, value);
            };
          }
          var countryCodeSubscriber = new modules_subscriber('countryCode', document.querySelector($self.getSelector(postfix.countryCode)), countryCodeSubscriberOptions);
          EAO.addSubscriber(countryCodeSubscriber);
          $self.dispatchEvent('endereco.ams.after-adding-country-code-subscriber'); // Add after hook.
        }
        document.querySelectorAll($self.getSelector(postfix.subdivisionCode)).forEach(function (DOMElement) {
          var subdivisionCodeSubscriberOptions = {};
          if (!!$self.resolvers.subdivisionCodeWrite) {
            subdivisionCodeSubscriberOptions['writeFilterCb'] = function (value, subscriber) {
              return $self.resolvers.subdivisionCodeWrite(value, subscriber);
            };
          }
          if (!!$self.resolvers.subdivisionCodeRead) {
            subdivisionCodeSubscriberOptions['readFilterCb'] = function (value, subscriber) {
              return $self.resolvers.subdivisionCodeRead(value, subscriber);
            };
          }
          if (!!$self.resolvers.subdivisionCodeSetValue) {
            subdivisionCodeSubscriberOptions['customSetValue'] = function (subscriber, value) {
              return $self.resolvers.subdivisionCodeSetValue(subscriber, value);
            };
          }
          var subdivisionCodeSubscriber = new modules_subscriber('subdivisionCode', DOMElement, subdivisionCodeSubscriberOptions);
          EAO.addSubscriber(subdivisionCodeSubscriber);
        });
        if (document.querySelector($self.getSelector(postfix.postalCode)) && $self.dispatchEvent('endereco.ams.before-adding-postal-code-subscriber')) {
          var postalCodeSubscriber = new modules_subscriber('postalCode', document.querySelector($self.getSelector(postfix.postalCode)), {
            displayAutocompleteDropdown: true
          });
          EAO.addSubscriber(postalCodeSubscriber);
          $self.dispatchEvent('endereco.ams.after-adding-postal-code-subscriber'); // Add after hook.
        }
        if (document.querySelector($self.getSelector(postfix.locality)) && $self.dispatchEvent('endereco.ams.before-adding-locality-subscriber')) {
          var localitySubscriber = new modules_subscriber('locality', document.querySelector($self.getSelector(postfix.locality)), {
            displayAutocompleteDropdown: true
          });
          EAO.addSubscriber(localitySubscriber);
          $self.dispatchEvent('endereco.ams.after-adding-locality-subscriber'); // Add after hook.
        }
        if (document.querySelector($self.getSelector(postfix.streetFull)) && $self.dispatchEvent('endereco.ams.before-adding-street-full-subscriber')) {
          var streetFullSubscriber = new modules_subscriber('streetFull', document.querySelector($self.getSelector(postfix.streetFull)), {
            displayAutocompleteDropdown: true
          });
          EAO.addSubscriber(streetFullSubscriber);
          $self.dispatchEvent('endereco.ams.after-adding-street-full-subscriber'); // Add after hook.
        }
        if (document.querySelector($self.getSelector(postfix.streetName)) && $self.dispatchEvent('endereco.ams.before-adding-street-name-subscriber')) {
          var streetNameSubscriber = new modules_subscriber('streetName', document.querySelector($self.getSelector(postfix.streetName)), {
            displayAutocompleteDropdown: true
          });
          EAO.addSubscriber(streetNameSubscriber);
          $self.dispatchEvent('endereco.ams.after-adding-street-name-subscriber'); // Add after hook.
        }
        if (document.querySelector($self.getSelector(postfix.buildingNumber)) && $self.dispatchEvent('endereco.ams.before-adding-building-number-subscriber')) {
          var buildingNumberSubscriber = new modules_subscriber('buildingNumber', document.querySelector($self.getSelector(postfix.buildingNumber)));
          EAO.addSubscriber(buildingNumberSubscriber);
          $self.dispatchEvent('endereco.ams.after-adding-building-number-subscriber'); // Add after hook.
        }
        if (document.querySelector($self.getSelector(postfix.additionalInfo)) && $self.dispatchEvent('endereco.ams.before-adding-additional-info-subscriber')) {
          var additionalInfoSubscriber = new modules_subscriber('additionalInfo', document.querySelector($self.getSelector(postfix.additionalInfo)));
          EAO.addSubscriber(additionalInfoSubscriber);
          $self.dispatchEvent('endereco.ams.after-adding-additional-info-subscriber'); // Add after hook.
        }
        if (document.querySelector($self.getSelector(postfix.addressTimestamp)) && $self.dispatchEvent('endereco.ams.before-adding-address-timestamp-subscriber')) {
          var addressTimestampSubscriber = new modules_subscriber('addressTimestamp', document.querySelector($self.getSelector(postfix.addressTimestamp)));
          EAO.addSubscriber(addressTimestampSubscriber);
          $self.dispatchEvent('endereco.ams.after-adding-address-timestamp-subscriber'); // Add after hook.
        }
        if (document.querySelector($self.getSelector(postfix.addressPredictions)) && $self.dispatchEvent('endereco.ams.before-adding-address-predictions-subscriber')) {
          var addressPredictionsSubscriber = new modules_subscriber('addressPredictions', document.querySelector($self.getSelector(postfix.addressPredictions)), {
            writeFilterCb: function writeFilterCb(value, subscriber) {
              if (!!$self.resolvers.addressPredictionsWrite) {
                return $self.resolvers.addressPredictionsWrite(value, subscriber);
              } else {
                return new EAO.util.Promise(function (resolve, reject) {
                  resolve(value);
                });
              }
            },
            readFilterCb: function readFilterCb(value, subscriber) {
              if (!!$self.resolvers.addressPredictionsRead) {
                return $self.resolvers.addressPredictionsRead(value, subscriber);
              } else {
                return new EAO.util.Promise(function (resolve, reject) {
                  resolve(value);
                });
              }
            }
          });
          EAO.addSubscriber(addressPredictionsSubscriber);
          $self.dispatchEvent('endereco.ams.after-adding-address-predictions-subscriber'); // Add after hook.
        }
        if (document.querySelector($self.getSelector(postfix.addressStatus)) && $self.dispatchEvent('endereco.ams.before-adding-address-status-subscriber')) {
          var addressStatusSubscriber = new modules_subscriber('addressStatus', document.querySelector($self.getSelector(postfix.addressStatus)));
          EAO.addSubscriber(addressStatusSubscriber);
          $self.dispatchEvent('endereco.ams.after-adding-address-status-subscriber'); // Add after hook.
        }
        $self.dispatchEvent('endereco.ams.after-adding-subscribers');
        EAO.waitUntilReady().then(function () {
          EAO.syncValues().then(function () {
            EAO.waitUntilReady().then(function () {
              if (!EAO.countryCode && $self.defaultCountrySelect) {
                EAO.countryCode = $self.defaultCountry;
              }

              // Start setting default values.
              if (!!options.addressType) {
                EAO._addressType = options.addressType;
              }
              if (!!options.beforeActivation) {
                options.beforeActivation(EAO);
              }
              EAO._changed = false;
              EAO.activate();
              $self.afterAMSActivation.forEach(function (callback) {
                callback(EAO);
              });
              EAO.waitUntilReady().then(function () {
                EAO.util.calculateDependingStatuses();
              }).catch();

              // If automated check is active, render the address selection field.
              if (!$self.editingIntent && EAO.active && (EAO.addressStatus.includes('address_needs_correction') || EAO.addressStatus.includes('address_multiple_variants') || EAO.addressStatus.includes('address_not_found')) && !EAO.addressStatus.includes('address_selected_by_customer') && !EAO.addressStatus.includes('address_selected_automatically')) {
                EAO.util.renderAddressPredictionsPopup();
              }

              // Special ajax form handler.
              if (!!options.ajaxForm) {
                $self.onAjaxFormHandler.forEach(function (callback) {
                  callback(EAO);
                });
              }
            }).catch();
          }).catch();
        }).catch();
      }
    }).catch();
    this.integratedObjects[EAO.fullName] = EAO;
    return EAO;
  },
  afterAMSActivation: [],
  onAjaxFormHandler: [],
  initEmailServices: function initEmailServices(prefix) {
    var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {
      postfixCollection: {},
      name: 'default'
    };
    if (!this.activeServices.emailService) {
      return;
    }
    var $self = this;
    var config = JSON.parse(JSON.stringify(this.config));
    var originalPostfix = lodash_merge_default()({}, $self.postfix.emailServices);
    var postfix;
    if ('object' === integrator_typeof(prefix)) {
      postfix = lodash_merge_default()(originalPostfix, prefix);
      prefix = '';
    } else {
      var newObject = {};
      Object.keys(originalPostfix).forEach(function (key) {
        newObject[key] = prefix + originalPostfix[key];
      });
      postfix = lodash_merge_default()(newObject, options.postfixCollection);
    }
    if (!!options.errorContainer) {
      config.ux.errorContainer = options.errorContainer;
    }
    if (!!options.errorInsertMode) {
      config.ux.errorInsertMode = options.errorInsertMode;
    }
    var EEO = new emailservices(config);
    EEO.fullName = options.name + '_' + EEO.name;
    EEO._awaits++;
    EEO.waitForAllExtension().then(function () {
      if ($self.dispatchEvent('endereco.es.before-adding-subscribers')) {
        if (document.querySelector($self.getSelector(prefix + postfix.email)) && $self.dispatchEvent('endereco.es.before-adding-email-subscriber')) {
          var emailSubscriber = new modules_subscriber('email', document.querySelector($self.getSelector(prefix + postfix.email)), {
            writeFilterCb: function writeFilterCb(value, subscriber) {
              if (!!$self.resolvers.emailWrite) {
                return $self.resolvers.emailWrite(value, subscriber);
              } else {
                return new EEO.util.Promise(function (resolve, reject) {
                  resolve(value);
                });
              }
            },
            readFilterCb: function readFilterCb(value, subscriber) {
              if (!!$self.resolvers.emailRead) {
                return $self.resolvers.emailRead(value, subscriber);
              } else {
                return new EEO.util.Promise(function (resolve, reject) {
                  resolve(value);
                });
              }
            }
          });
          EEO.addSubscriber(emailSubscriber);
          $self.dispatchEvent('endereco.es.after-adding-email-subscriber'); // Add after hook.
        }
        if (document.querySelector($self.getSelector(prefix + postfix.email)) && $self.dispatchEvent('endereco.es.before-adding-email-status-subscriber')) {
          var emailStatusSubscriber = new modules_subscriber('emailStatus', document.querySelector($self.getSelector(prefix + postfix.email)), {
            writeFilterCb: function writeFilterCb(value, subscriber) {
              if (!!$self.resolvers.emailStatusWrite) {
                return $self.resolvers.emailStatusWrite(value, subscriber);
              } else {
                return new EEO.util.Promise(function (resolve, reject) {
                  resolve(value);
                });
              }
            },
            readFilterCb: function readFilterCb(value, subscriber) {
              if (!!$self.resolvers.emailStatusRead) {
                return $self.resolvers.emailStatusRead(value, subscriber);
              } else {
                return new EEO.util.Promise(function (resolve, reject) {
                  resolve(value);
                });
              }
            },
            valueContainer: 'classList'
          });
          EEO.addSubscriber(emailStatusSubscriber);
          $self.dispatchEvent('endereco.es.after-adding-email-status-subscriber'); // Add after hook.
        }
        $self.dispatchEvent('endereco.es.after-adding-subscribers');
      }
      EEO._awaits--;
    }).catch();
    EEO.waitUntilReady().then(function () {
      EEO.syncValues().then(function () {
        EEO.waitUntilReady().then(function () {
          EEO._changed = false;
          EEO.activate();
        }).catch();
      }).catch();
    }).catch();
    this.integratedObjects[EEO.fullName] = EEO;
    return EEO;
  },
  initPersonServices: function initPersonServices() {
    var prefix = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : '';
    var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {
      postfixCollection: {},
      name: 'default'
    };
    $self = this;
    if (!this.activeServices.personService) {
      return;
    }
    var $self = this;
    var config = JSON.parse(JSON.stringify(this.config));
    if (!!options.config) {
      config = lodash_merge_default()(config, options.config);
    }
    var originalPostfix = lodash_merge_default()({}, $self.postfix.personServices);
    var postfix;
    if ('object' === integrator_typeof(prefix)) {
      postfix = lodash_merge_default()(originalPostfix, prefix);
      prefix = '';
    } else {
      postfix = lodash_merge_default()(originalPostfix, options.postfixCollection);
    }
    var EPO = new personservices(config);
    EPO.fullName = options.name + '_' + EPO.name;
    EPO._awaits++;
    EPO.waitForAllExtension().then(function () {
      // Add subscribers.
      if ($self.dispatchEvent('endereco.ps.before-adding-subscribers')) {
        // In general with every subscriber we first check, if the html element exists
        if (document.querySelector($self.getSelector(prefix + postfix.salutation)) && $self.dispatchEvent('endereco.ps.before-adding-salutation-subscriber')) {
          var salutationSubscriberOptions = {};
          if (!!$self.resolvers.salutationWrite) {
            salutationSubscriberOptions['writeFilterCb'] = function (value, subscriber) {
              return $self.resolvers.salutationWrite(value, subscriber);
            };
          }
          if (!!$self.resolvers.salutationRead) {
            salutationSubscriberOptions['readFilterCb'] = function (value, subscriber) {
              return $self.resolvers.salutationRead(value, subscriber);
            };
          }
          if (!!$self.resolvers.salutationSetValue) {
            salutationSubscriberOptions['customSetValue'] = function (subscriber, value) {
              return $self.resolvers.salutationSetValue(subscriber, value);
            };
          }
          var salutationSubscriber = new modules_subscriber('salutation', document.querySelector($self.getSelector(prefix + postfix.salutation)), salutationSubscriberOptions);
          EPO.addSubscriber(salutationSubscriber);
          $self.dispatchEvent('endereco.ps.after-adding-salutation-subscriber'); // Add after hook.
        }
        // In general with every subscriber we first check, if the html element exists
        if (document.querySelector($self.getSelector(prefix + postfix.firstName)) && $self.dispatchEvent('endereco.ps.before-adding-first-name-subscriber')) {
          var firstNameSubscriber = new modules_subscriber('firstName', document.querySelector($self.getSelector(prefix + postfix.firstName)), {
            writeFilterCb: function writeFilterCb(value, subscriber) {
              if (!!$self.resolvers.firstNameWrite) {
                return $self.resolvers.firstNameWrite(value, subscriber);
              } else {
                return new EPO.util.Promise(function (resolve, reject) {
                  resolve(value);
                });
              }
            },
            readFilterCb: function readFilterCb(value, subscriber) {
              if (!!$self.resolvers.firstNameRead) {
                return $self.resolvers.firstNameRead(value, subscriber);
              } else {
                return new EPO.util.Promise(function (resolve, reject) {
                  resolve(value);
                });
              }
            }
          });
          EPO.addSubscriber(firstNameSubscriber);
          $self.dispatchEvent('endereco.ps.after-adding-first-name-subscriber'); // Add after hook.
        }
        if (document.querySelector($self.getSelector(prefix + postfix.lastName)) && $self.dispatchEvent('endereco.ps.before-adding-last-name-subscriber')) {
          var lastNameSubscriberOptions = {};
          if (!!$self.resolvers.lastNameWrite) {
            lastNameSubscriberOptions['writeFilterCb'] = function (value, subscriber) {
              return $self.resolvers.lastNameWrite(value, subscriber);
            };
          }
          if (!!$self.resolvers.lastNameRead) {
            lastNameSubscriberOptions['readFilterCb'] = function (value, subscriber) {
              return $self.resolvers.lastNameRead(value, subscriber);
            };
          }
          if (!!$self.resolvers.lastNameSetValue) {
            lastNameSubscriberOptions['customSetValue'] = function (subscriber, value) {
              return $self.resolvers.lastNameSetValue(subscriber, value);
            };
          }
          var lastNameSubscriber = new modules_subscriber('lastName', document.querySelector($self.getSelector(prefix + postfix.lastName)), lastNameSubscriberOptions);
          EPO.addSubscriber(lastNameSubscriber);
          $self.dispatchEvent('endereco.ps.after-adding-last-name-subscriber'); // Add after hook.
        }
        if (document.querySelector($self.getSelector(prefix + postfix.title)) && $self.dispatchEvent('endereco.ps.before-adding-title-subscriber')) {
          var titleSubscriberOptions = {};
          if (!!$self.resolvers.titleWrite) {
            titleSubscriberOptions['writeFilterCb'] = function (value, subscriber) {
              return $self.resolvers.titleWrite(value, subscriber);
            };
          }
          if (!!$self.resolvers.titleRead) {
            titleSubscriberOptions['readFilterCb'] = function (value, subscriber) {
              return $self.resolvers.titleRead(value, subscriber);
            };
          }
          if (!!$self.resolvers.titleSetValue) {
            titleSubscriberOptions['customSetValue'] = function (subscriber, value) {
              return $self.resolvers.titleSetValue(subscriber, value);
            };
          }
          var titleSubscriber = new modules_subscriber('title', document.querySelector($self.getSelector(prefix + postfix.title)), titleSubscriberOptions);
          EPO.addSubscriber(titleSubscriber);
          $self.dispatchEvent('endereco.ps.after-adding-title-subscriber'); // Add after hook.
        }
        if (document.querySelector($self.getSelector(prefix + postfix.nameScore)) && $self.dispatchEvent('endereco.ps.before-adding-name-score-subscriber')) {
          var nameScoreSubscriberOptions = {};
          if (!!$self.resolvers.nameScoreWrite) {
            nameScoreSubscriberOptions['writeFilterCb'] = function (value, subscriber) {
              return $self.resolvers.nameScoreWrite(value, subscriber);
            };
          }
          if (!!$self.resolvers.nameScoreRead) {
            nameScoreSubscriberOptions['readFilterCb'] = function (value, subscriber) {
              return $self.resolvers.nameScoreRead(value, subscriber);
            };
          }
          if (!!$self.resolvers.nameScoreSetValue) {
            nameScoreSubscriberOptions['customSetValue'] = function (subscriber, value) {
              return $self.resolvers.nameScoreSetValue(subscriber, value);
            };
          }
          var nameScoreSubscriber = new modules_subscriber('nameScore', document.querySelector($self.getSelector(prefix + postfix.nameScore)), nameScoreSubscriberOptions);
          EPO.addSubscriber(nameScoreSubscriber);
          $self.dispatchEvent('endereco.ps.after-adding-name-score-subscriber'); // Add after hook.
        }
      }
      EPO._awaits--;
    });
    EPO.waitUntilReady().then(function () {
      EPO.syncValues().then(function () {
        EPO.waitUntilReady().then(function () {
          // Start setting default values.
          EPO._changed = false;
          EPO.activate();
        }).catch();
      }).catch();
    }).catch();
    this.integratedObjects[EPO.fullName] = EPO;
    return EPO;
  },
  waitUntilReady: function waitUntilReady() {
    var $self = this;
    return new Promise(function (resolve, reject) {
      var interval = setInterval(function () {
        if (window.EnderecoIntegrator.ready) {
          clearInterval(interval);
          resolve();
        }
      }, 100);
    });
  },
  getSelector: function getSelector(possibleSelector) {
    var selector = '';
    if (!possibleSelector) {
      selector = null;
    } else if (possibleSelector.indexOf('#') === -1 && possibleSelector.indexOf('.') === -1 && possibleSelector.indexOf('=') === -1) {
      selector = '[name="' + possibleSelector + '"]';
    } else {
      selector = possibleSelector;
    }
    return selector;
  },
  integratedObjects: {},
  asyncCallbacks: [],
  addCss: function addCss() {
    // Clean up beforehand (just in case)
    var stylesDOM = document.getElementById('#endereco-styles-include');
    if (stylesDOM) {
      stylesDOM.remove();
    }
    if (this.config.ux.useStandardCss) {
      if (!this.css && !this.config.ux.cssFilePath) {
        return;
      }
      var cssLink = this.config.ux.cssFilePath || 'data:text/css;charset=UTF-8,' + encodeURIComponent(this.css);
      var head = document.querySelector('head');
      var linkElement = document.createElement('link');
      linkElement.setAttribute('id', 'endereco-styles-include');
      linkElement.setAttribute('rel', 'stylesheet');
      linkElement.setAttribute('type', 'text/css');
      linkElement.setAttribute('href', cssLink);
      head.appendChild(linkElement);
    }
  },
  addBodyClass: function addBodyClass() {
    if (!!this.themeName) {
      document.querySelector('body').classList.add('endereco-theme--' + this.themeName);
    } else {
      document.querySelector('body').classList.add('endereco-theme--current-theme');
    }
  },
  dispatchEvent: function dispatchEvent(event) {
    return true;
  },
  _createParentLine: function _createParentLine(fieldName, collector, collection) {
    if (document.querySelector(this.getSelector(collection[fieldName]))) {
      collector[fieldName] = {
        commonElementIndex: 0,
        rowElementIndex: 0,
        columnElementIndex: 0,
        parentLine: [document.querySelector(this.getSelector(collection[fieldName]))]
      };
      while (1) {
        if (collector[fieldName].parentLine[collector[fieldName].parentLine.length - 1].parentNode) {
          var temp = collector[fieldName].parentLine[collector[fieldName].parentLine.length - 1].parentNode;
          collector[fieldName].parentLine.push(temp);
        } else {
          break;
        }
      }
    }
  },
  _firstBeforeSecond: function _firstBeforeSecond(firstFieldName, secondFieldName, collector) {
    if (!collector[firstFieldName] || !collector[secondFieldName]) {
      return;
    }

    // Find commen parent.
    var firstFieldData = collector[firstFieldName];
    var firstFieldIndex = 0;
    var secondFieldData = collector[secondFieldName];
    var secondFieldIndex = 0;
    var commonParentDOM = undefined;
    if (firstFieldData.parentLine && secondFieldData.parentLine) {
      firstFieldData.parentLine.forEach(function (firstFieldParentDOM) {
        if (commonParentDOM) {
          return;
        }
        secondFieldIndex = 0;
        secondFieldData.parentLine.forEach(function (secondFieldParentDOM) {
          if (commonParentDOM) {
            return;
          }
          if (firstFieldParentDOM === secondFieldParentDOM) {
            commonParentDOM = firstFieldParentDOM;
            firstFieldData.commonElementIndex = firstFieldIndex;
            firstFieldData.rowElementIndex = Math.max(firstFieldIndex - 1, 0);
            firstFieldData.columnElementIndex = Math.max(firstFieldIndex - 2, 0);
            secondFieldData.commonElementIndex = secondFieldIndex;
            secondFieldData.rowElementIndex = Math.max(secondFieldIndex - 1, 0);
            secondFieldData.columnElementIndex = Math.max(secondFieldIndex - 2, 0);
          }
          secondFieldIndex++;
        });
        firstFieldIndex++;
      });
      if (commonParentDOM) {
        commonParentDOM.insertBefore(firstFieldData.parentLine[firstFieldData.rowElementIndex], secondFieldData.parentLine[secondFieldData.rowElementIndex]);
      }
    }
  },
  _test: {},
  changeFieldsOrder: function changeFieldsOrder(collection) {
    var fieldNamesOrder = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : ['countryCode', 'subdivisionCode', 'postalCode', 'locality', 'streetFull', 'streetName', 'buildingNumber', 'additionalInfo'];
    var myStructure = {};

    // Create parent line for additional info if it exists.
    this._createParentLine('additionalInfo', this._test, collection);
    this._createParentLine('buildingNumber', this._test, collection);
    this._createParentLine('streetName', this._test, collection);
    this._createParentLine('streetFull', this._test, collection);
    this._createParentLine('locality', this._test, collection);
    this._createParentLine('postalCode', this._test, collection);
    this._createParentLine('countryCode', this._test, collection);
    this._createParentLine('subdivisionCode', this._test, collection);

    // Ensure position.
    var reversedArray = new Array();
    for (var i = fieldNamesOrder.length - 1; i >= 0; i--) {
      // Filterout if not existing.
      if (document.querySelector(this.getSelector(collection[fieldNamesOrder[i]]))) {
        reversedArray.push(fieldNamesOrder[i]);
      }
    }

    // Change positions in the DOM.
    for (var j = 0; j < reversedArray.length - 1; j++) {
      this._firstBeforeSecond(reversedArray[j + 1], reversedArray[j], this._test);
    }
  }
};
EnderecoIntegrator.waitUntilReady().then(function () {
  if (window.EnderecoIntegrator.config) {
    EnderecoIntegrator.config = lodash_merge_default()(EnderecoIntegrator.config, window.EnderecoIntegrator.config);
  }
  EnderecoIntegrator.addCss();
  EnderecoIntegrator.addBodyClass();
}).catch();
/* harmony default export */ var integrator = (EnderecoIntegrator);
// EXTERNAL MODULE: ./themes/default-theme.scss
var default_theme = __webpack_require__(2997);
;// ./bundle.js




if ('NodeList' in window && !NodeList.prototype.forEach) {
  NodeList.prototype.forEach = function (callback, thisArg) {
    thisArg = thisArg || window;
    for (var i = 0; i < this.length; i++) {
      callback.call(thisArg, this[i], i, this);
    }
  };
}
if (!window.Promise) {
  window.Promise = src;
}
integrator.postfix = {
  ams: {
    countryCode: '',
    postalCode: '',
    locality: '',
    streetFull: '',
    streetName: '',
    buildingNumber: '',
    addressStatus: '',
    addressTimestamp: '',
    addressPredictions: '',
    additionalInfo: ''
  },
  personServices: {
    salutation: '',
    firstName: ''
  },
  emailServices: {
    email: ''
  }
};
if (default_theme/* default */.A) {
  integrator.css = default_theme/* default */.A[0][1];
}
integrator.resolvers.countryCodeWrite = function (value, subscriber) {
  return new src(function (resolve, reject) {
    resolve(value);
  });
};
integrator.resolvers.countryCodeRead = function (value, subscriber) {
  return new src(function (resolve, reject) {
    resolve(value);
  });
};
integrator.resolvers.salutationWrite = function (value, subscriber) {
  return new src(function (resolve, reject) {
    resolve(value);
  });
};
integrator.resolvers.salutationRead = function (value, subscriber) {
  return new src(function (resolve, reject) {
    resolve(value);
  });
};
integrator.amsFilters.isAddressMetaStillRelevant.push(function (isStillRelevant, EAO) {
  var invalidateAddressForm = document.querySelector('#invalidate-address-form');
  if (invalidateAddressForm && invalidateAddressForm.checked) {
    isStillRelevant = false;
  }
  return isStillRelevant;
});
if (window.EnderecoIntegrator) {
  window.EnderecoIntegrator = lodash_merge_default()(window.EnderecoIntegrator, integrator);
} else {
  window.EnderecoIntegrator = integrator;
}
window.EnderecoIntegrator.asyncCallbacks.forEach(function (cb) {
  cb();
});
window.EnderecoIntegrator.asyncCallbacks = [];
window.EnderecoIntegrator.waitUntilReady().then(function () {});
var $waitForConfig = setInterval(function () {
  if (typeof enderecoLoadAMSConfig === 'function') {
    enderecoLoadAMSConfig();
    clearInterval($waitForConfig);
  }
}, 1);
}();
/******/ })()
;